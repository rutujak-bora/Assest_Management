import require$$0 from "events";
import require$$0$2 from "assert";
import { c as commonjsGlobal, g as getDefaultExportFromCjs } from "./react.mjs";
import { r as requireCommon$1, a as requireBson, b as requireLib$1 } from "./mongodb.mjs";
import require$$0$1 from "stream";
import require$$1 from "util";
import { r as requireKareem } from "./kareem.mjs";
import { r as requireMs } from "./ms.mjs";
import { r as requireMpath } from "./mpath.mjs";
import { r as requireMquery } from "./mquery.mjs";
import { r as require$$9 } from "./sift.mjs";
import require$$2 from "async_hooks";
var mongoose$2 = { exports: {} };
var nodeMongodbNative = {};
var bulkWriteResult;
var hasRequiredBulkWriteResult;
function requireBulkWriteResult() {
  if (hasRequiredBulkWriteResult) return bulkWriteResult;
  hasRequiredBulkWriteResult = 1;
  const BulkWriteResult = requireCommon$1().BulkWriteResult;
  bulkWriteResult = BulkWriteResult;
  return bulkWriteResult;
}
var connectionState = { exports: {} };
var hasRequiredConnectionState;
function requireConnectionState() {
  if (hasRequiredConnectionState) return connectionState.exports;
  hasRequiredConnectionState = 1;
  (function(module, exports) {
    const STATES = module.exports = /* @__PURE__ */ Object.create(null);
    const disconnected = "disconnected";
    const connected = "connected";
    const connecting = "connecting";
    const disconnecting = "disconnecting";
    const uninitialized = "uninitialized";
    STATES[0] = disconnected;
    STATES[1] = connected;
    STATES[2] = connecting;
    STATES[3] = disconnecting;
    STATES[99] = uninitialized;
    STATES[disconnected] = 0;
    STATES[connected] = 1;
    STATES[connecting] = 2;
    STATES[disconnecting] = 3;
    STATES[uninitialized] = 99;
  })(connectionState);
  return connectionState.exports;
}
var immediate;
var hasRequiredImmediate;
function requireImmediate() {
  if (hasRequiredImmediate) return immediate;
  hasRequiredImmediate = 1;
  const nextTick = typeof process !== "undefined" && typeof process.nextTick === "function" ? process.nextTick.bind(process) : (cb) => setTimeout(cb, 0);
  immediate = function immediate2(cb) {
    return nextTick(cb);
  };
  return immediate;
}
var collection$1;
var hasRequiredCollection$1;
function requireCollection$1() {
  if (hasRequiredCollection$1) return collection$1;
  hasRequiredCollection$1 = 1;
  const EventEmitter = require$$0.EventEmitter;
  const STATES = requireConnectionState();
  const immediate2 = requireImmediate();
  function Collection(name, conn, opts) {
    if (opts === void 0) {
      opts = {};
    }
    this.opts = opts;
    this.name = name;
    this.collectionName = name;
    this.conn = conn;
    this.queue = [];
    this.buffer = !conn?._hasOpened;
    this.emitter = new EventEmitter();
    if (STATES.connected === this.conn.readyState) {
      this.onOpen();
    }
  }
  Collection.prototype.name;
  Collection.prototype.collectionName;
  Collection.prototype.conn;
  Collection.prototype.onOpen = function() {
    this.buffer = false;
    immediate2(() => this.doQueue());
  };
  Collection.prototype.onClose = function() {
  };
  Collection.prototype.addQueue = function(name, args) {
    this.queue.push([name, args]);
    return this;
  };
  Collection.prototype.removeQueue = function(name, args) {
    const index = this.queue.findIndex((v) => v[0] === name && v[1] === args);
    if (index === -1) {
      return false;
    }
    this.queue.splice(index, 1);
    return true;
  };
  Collection.prototype.doQueue = function() {
    for (const method of this.queue) {
      if (typeof method[0] === "function") {
        method[0].apply(this, method[1]);
      } else {
        this[method[0]].apply(this, method[1]);
      }
    }
    this.queue = [];
    const _this = this;
    immediate2(function() {
      _this.emitter.emit("queue");
    });
    return this;
  };
  Collection.prototype.ensureIndex = function() {
    throw new Error("Collection#ensureIndex unimplemented by driver");
  };
  Collection.prototype.createIndex = function() {
    throw new Error("Collection#createIndex unimplemented by driver");
  };
  Collection.prototype.findAndModify = function() {
    throw new Error("Collection#findAndModify unimplemented by driver");
  };
  Collection.prototype.findOneAndUpdate = function() {
    throw new Error("Collection#findOneAndUpdate unimplemented by driver");
  };
  Collection.prototype.findOneAndDelete = function() {
    throw new Error("Collection#findOneAndDelete unimplemented by driver");
  };
  Collection.prototype.findOneAndReplace = function() {
    throw new Error("Collection#findOneAndReplace unimplemented by driver");
  };
  Collection.prototype.findOne = function() {
    throw new Error("Collection#findOne unimplemented by driver");
  };
  Collection.prototype.find = function() {
    throw new Error("Collection#find unimplemented by driver");
  };
  Collection.prototype.insert = function() {
    throw new Error("Collection#insert unimplemented by driver");
  };
  Collection.prototype.insertOne = function() {
    throw new Error("Collection#insertOne unimplemented by driver");
  };
  Collection.prototype.insertMany = function() {
    throw new Error("Collection#insertMany unimplemented by driver");
  };
  Collection.prototype.save = function() {
    throw new Error("Collection#save unimplemented by driver");
  };
  Collection.prototype.updateOne = function() {
    throw new Error("Collection#updateOne unimplemented by driver");
  };
  Collection.prototype.updateMany = function() {
    throw new Error("Collection#updateMany unimplemented by driver");
  };
  Collection.prototype.deleteOne = function() {
    throw new Error("Collection#deleteOne unimplemented by driver");
  };
  Collection.prototype.deleteMany = function() {
    throw new Error("Collection#deleteMany unimplemented by driver");
  };
  Collection.prototype.getIndexes = function() {
    throw new Error("Collection#getIndexes unimplemented by driver");
  };
  Collection.prototype.watch = function() {
    throw new Error("Collection#watch unimplemented by driver");
  };
  Collection.prototype._shouldBufferCommands = function _shouldBufferCommands() {
    const opts = this.opts;
    if (opts.bufferCommands != null) {
      return opts.bufferCommands;
    }
    if (opts?.schemaUserProvidedOptions?.bufferCommands != null) {
      return opts.schemaUserProvidedOptions.bufferCommands;
    }
    return this.conn._shouldBufferCommands();
  };
  Collection.prototype._getBufferTimeoutMS = function _getBufferTimeoutMS() {
    const conn = this.conn;
    const opts = this.opts;
    if (opts.bufferTimeoutMS != null) {
      return opts.bufferTimeoutMS;
    }
    if (opts?.schemaUserProvidedOptions?.bufferTimeoutMS != null) {
      return opts.schemaUserProvidedOptions.bufferTimeoutMS;
    }
    return conn._getBufferTimeoutMS();
  };
  collection$1 = Collection;
  return collection$1;
}
var mongooseError;
var hasRequiredMongooseError;
function requireMongooseError() {
  if (hasRequiredMongooseError) return mongooseError;
  hasRequiredMongooseError = 1;
  class MongooseError extends Error {
  }
  Object.defineProperty(MongooseError.prototype, "name", {
    value: "MongooseError"
  });
  mongooseError = MongooseError;
  return mongooseError;
}
var symbols$1 = {};
var hasRequiredSymbols$1;
function requireSymbols$1() {
  if (hasRequiredSymbols$1) return symbols$1;
  hasRequiredSymbols$1 = 1;
  symbols$1.arrayAtomicsBackupSymbol = /* @__PURE__ */ Symbol("mongoose#Array#atomicsBackup");
  symbols$1.arrayAtomicsSymbol = /* @__PURE__ */ Symbol("mongoose#Array#_atomics");
  symbols$1.arrayParentSymbol = /* @__PURE__ */ Symbol("mongoose#Array#_parent");
  symbols$1.arrayPathSymbol = /* @__PURE__ */ Symbol("mongoose#Array#_path");
  symbols$1.arraySchemaSymbol = /* @__PURE__ */ Symbol("mongoose#Array#_schema");
  symbols$1.documentArrayParent = /* @__PURE__ */ Symbol("mongoose#documentArrayParent");
  symbols$1.documentIsSelected = /* @__PURE__ */ Symbol("mongoose#Document#isSelected");
  symbols$1.documentIsModified = /* @__PURE__ */ Symbol("mongoose#Document#isModified");
  symbols$1.documentModifiedPaths = /* @__PURE__ */ Symbol("mongoose#Document#modifiedPaths");
  symbols$1.documentSchemaSymbol = /* @__PURE__ */ Symbol("mongoose#Document#schema");
  symbols$1.getSymbol = /* @__PURE__ */ Symbol("mongoose#Document#get");
  symbols$1.modelSymbol = /* @__PURE__ */ Symbol("mongoose#Model");
  symbols$1.objectIdSymbol = /* @__PURE__ */ Symbol("mongoose#ObjectId");
  symbols$1.populateModelSymbol = /* @__PURE__ */ Symbol("mongoose#PopulateOptions#Model");
  symbols$1.schemaTypeSymbol = /* @__PURE__ */ Symbol("mongoose#schemaType");
  symbols$1.sessionNewDocuments = /* @__PURE__ */ Symbol("mongoose#ClientSession#newDocuments");
  symbols$1.scopeSymbol = /* @__PURE__ */ Symbol("mongoose#Document#scope");
  symbols$1.validatorErrorSymbol = /* @__PURE__ */ Symbol("mongoose#validatorError");
  return symbols$1;
}
var objectid$1;
var hasRequiredObjectid$1;
function requireObjectid$1() {
  if (hasRequiredObjectid$1) return objectid$1;
  hasRequiredObjectid$1 = 1;
  const ObjectId = requireBson().ObjectId;
  const objectIdSymbol = requireSymbols$1().objectIdSymbol;
  Object.defineProperty(ObjectId.prototype, "_id", {
    enumerable: false,
    configurable: true,
    get: function() {
      return this;
    }
  });
  if (!Object.hasOwn(ObjectId.prototype, "valueOf")) {
    ObjectId.prototype.valueOf = function objectIdValueOf() {
      return this.toString();
    };
  }
  ObjectId.prototype[objectIdSymbol] = true;
  objectid$1 = ObjectId;
  return objectid$1;
}
var getConstructorName;
var hasRequiredGetConstructorName;
function requireGetConstructorName() {
  if (hasRequiredGetConstructorName) return getConstructorName;
  hasRequiredGetConstructorName = 1;
  getConstructorName = function getConstructorName2(val) {
    if (val == null) {
      return void 0;
    }
    if (typeof val.constructor !== "function") {
      return void 0;
    }
    return val.constructor.name;
  };
  return getConstructorName;
}
var options = {};
var hasRequiredOptions;
function requireOptions() {
  if (hasRequiredOptions) return options;
  hasRequiredOptions = 1;
  options.internalToObjectOptions = {
    transform: false,
    virtuals: false,
    getters: false,
    _skipDepopulateTopLevel: true,
    depopulate: true,
    flattenDecimals: false,
    useProjection: false,
    versionKey: true,
    flattenObjectIds: false,
    flattenUUIDs: false
  };
  return options;
}
var decimal128$2;
var hasRequiredDecimal128$2;
function requireDecimal128$2() {
  if (hasRequiredDecimal128$2) return decimal128$2;
  hasRequiredDecimal128$2 = 1;
  decimal128$2 = requireBson().Decimal128;
  return decimal128$2;
}
var specialProperties;
var hasRequiredSpecialProperties;
function requireSpecialProperties() {
  if (hasRequiredSpecialProperties) return specialProperties;
  hasRequiredSpecialProperties = 1;
  specialProperties = /* @__PURE__ */ new Set(["__proto__", "constructor", "prototype"]);
  return specialProperties;
}
var isMongooseArray = {};
var hasRequiredIsMongooseArray;
function requireIsMongooseArray() {
  if (hasRequiredIsMongooseArray) return isMongooseArray;
  hasRequiredIsMongooseArray = 1;
  isMongooseArray.isMongooseArray = function(mongooseArray) {
    return Array.isArray(mongooseArray) && mongooseArray.isMongooseArray;
  };
  return isMongooseArray;
}
var isMongooseObject;
var hasRequiredIsMongooseObject;
function requireIsMongooseObject() {
  if (hasRequiredIsMongooseObject) return isMongooseObject;
  hasRequiredIsMongooseObject = 1;
  const isMongooseArray2 = requireIsMongooseArray().isMongooseArray;
  isMongooseObject = function(v) {
    return v != null && (isMongooseArray2(v) || // Array or Document Array
    v.$__ != null || // Document
    v.isMongooseBuffer || // Buffer
    v.$isMongooseMap);
  };
  return isMongooseObject;
}
var getFunctionName;
var hasRequiredGetFunctionName;
function requireGetFunctionName() {
  if (hasRequiredGetFunctionName) return getFunctionName;
  hasRequiredGetFunctionName = 1;
  const functionNameRE = /^function\s*([^\s(]+)/;
  getFunctionName = function(fn) {
    return fn.name || (fn.toString().trim().match(functionNameRE) || [])[1];
  };
  return getFunctionName;
}
var isBsonType_1;
var hasRequiredIsBsonType;
function requireIsBsonType() {
  if (hasRequiredIsBsonType) return isBsonType_1;
  hasRequiredIsBsonType = 1;
  function isBsonType(obj, typename) {
    return obj != null && obj._bsontype === typename;
  }
  isBsonType_1 = isBsonType;
  return isBsonType_1;
}
var isObject;
var hasRequiredIsObject;
function requireIsObject() {
  if (hasRequiredIsObject) return isObject;
  hasRequiredIsObject = 1;
  isObject = function(arg) {
    return Buffer.isBuffer(arg) || Object.prototype.toString.call(arg) === "[object Object]";
  };
  return isObject;
}
var isPOJO;
var hasRequiredIsPOJO;
function requireIsPOJO() {
  if (hasRequiredIsPOJO) return isPOJO;
  hasRequiredIsPOJO = 1;
  isPOJO = function isPOJO2(arg) {
    if (arg == null || typeof arg !== "object") {
      return false;
    }
    const proto = Object.getPrototypeOf(arg);
    return !proto || proto.constructor.name === "Object";
  };
  return isPOJO;
}
var trusted = {};
var hasRequiredTrusted;
function requireTrusted() {
  if (hasRequiredTrusted) return trusted;
  hasRequiredTrusted = 1;
  const trustedSymbol = /* @__PURE__ */ Symbol("mongoose#trustedSymbol");
  trusted.trustedSymbol = trustedSymbol;
  trusted.trusted = function trusted2(obj) {
    if (obj == null || typeof obj !== "object" && typeof obj !== "function") {
      return obj;
    }
    obj[trustedSymbol] = true;
    return obj;
  };
  return trusted;
}
var clone_1;
var hasRequiredClone;
function requireClone() {
  if (hasRequiredClone) return clone_1;
  hasRequiredClone = 1;
  const Decimal = requireDecimal128$2();
  const ObjectId = requireObjectid$1();
  const specialProperties2 = requireSpecialProperties();
  const isMongooseObject2 = requireIsMongooseObject();
  const getFunctionName2 = requireGetFunctionName();
  const isBsonType = requireIsBsonType();
  const isMongooseArray2 = requireIsMongooseArray().isMongooseArray;
  const isObject2 = requireIsObject();
  const isPOJO2 = requireIsPOJO();
  const symbols2 = requireSymbols$1();
  const trustedSymbol = requireTrusted().trustedSymbol;
  const BSON = requireBson();
  const UUID = BSON.UUID;
  const Binary = BSON.Binary;
  function clone(obj, options2, isArrayChild) {
    if (obj == null) {
      return obj;
    }
    if (isBsonType(obj, "Double")) {
      return new BSON.Double(obj.value);
    }
    if (typeof obj === "number" || typeof obj === "string" || typeof obj === "boolean" || typeof obj === "bigint") {
      return obj;
    }
    if (Array.isArray(obj)) {
      return cloneArray(obj, options2);
    }
    if (isMongooseObject2(obj)) {
      if (options2) {
        if (options2.flattenUUIDs) {
          if (obj instanceof Binary && obj._subtype === Binary.SUBTYPE_UUID) {
            return obj.toString();
          }
          if (obj?.isMongooseBuffer && obj._subtype === Binary.SUBTYPE_UUID) {
            return obj.toUUID().toString();
          }
        }
        if (options2.retainDocuments && obj.$__ != null) {
          const clonedDoc = obj.$clone();
          if (obj.__index != null) {
            clonedDoc.__index = obj.__index;
          }
          if (obj.__parentArray != null) {
            clonedDoc.__parentArray = options2.parentArray ?? obj.__parentArray;
          }
          clonedDoc.$__setParent(options2.parentDoc ?? obj.$__parent);
          return clonedDoc;
        }
        if (options2.retainDocuments && obj.$isMongooseMap) {
          const clonedParent = options2.parentDoc ?? obj.$__parent;
          const MongooseMap = obj.constructor;
          const ret2 = new MongooseMap({}, obj.$__path, clonedParent, obj.$__schemaType);
          for (const [key, value] of obj) {
            ret2.$__set(key, clone(value, { ...options2, parentDoc: clonedParent }));
          }
          return ret2;
        }
      }
      if (isPOJO2(obj) && obj.$__ != null && obj._doc != null) {
        return obj._doc;
      }
      let ret;
      if (options2?.json && typeof obj.toJSON === "function") {
        ret = obj.toJSON(options2);
      } else {
        ret = obj.toObject(options2);
      }
      return ret;
    }
    const objConstructor = obj.constructor;
    if (objConstructor) {
      switch (getFunctionName2(objConstructor)) {
        case "Object":
          return cloneObject(obj, options2, isArrayChild);
        case "Date":
          return new objConstructor(+obj);
        case "RegExp":
          return cloneRegExp(obj);
      }
    }
    if (isBsonType(obj, "ObjectId")) {
      if (options2?.flattenObjectIds) {
        return obj.toJSON();
      }
      return new ObjectId(obj.id);
    }
    if (isBsonType(obj, "Decimal128")) {
      if (options2?.flattenDecimals) {
        return obj.toJSON();
      }
      return Decimal.fromString(obj.toString());
    }
    if (obj instanceof UUID) {
      if (options2?.flattenUUIDs) {
        return obj.toJSON();
      }
      return new UUID(obj.buffer);
    }
    if (!objConstructor && isObject2(obj)) {
      return cloneObject(obj, options2, isArrayChild);
    }
    if (typeof obj === "object" && obj[symbols2.schemaTypeSymbol]) {
      return obj.clone();
    }
    if (options2?.bson && typeof obj.toBSON === "function") {
      return obj;
    }
    if (typeof obj.valueOf === "function") {
      return obj.valueOf();
    }
    return cloneObject(obj, options2, isArrayChild);
  }
  clone_1 = clone;
  function cloneObject(obj, options2, isArrayChild) {
    const minimize = options2?.minimize;
    const omitUndefined2 = options2?.omitUndefined;
    const seen = options2?._seen;
    const ret = {};
    let hasKeys;
    if (seen && seen.has(obj)) {
      return seen.get(obj);
    } else if (seen) {
      seen.set(obj, ret);
    }
    if (trustedSymbol in obj && options2?.copyTrustedSymbol !== false) {
      ret[trustedSymbol] = obj[trustedSymbol];
    }
    const keys = Object.keys(obj);
    const len = keys.length;
    for (let i = 0; i < len; ++i) {
      const key = keys[i];
      if (specialProperties2.has(key)) {
        continue;
      }
      const val = clone(obj[key], options2, false);
      if ((minimize === false || omitUndefined2) && typeof val === "undefined") {
        delete ret[key];
      } else if (minimize !== true || typeof val !== "undefined") {
        hasKeys || (hasKeys = true);
        ret[key] = val;
      }
    }
    return minimize && !isArrayChild ? hasKeys && ret : ret;
  }
  function cloneArray(arr, options2) {
    let i = 0;
    const len = arr.length;
    let ret = null;
    if (options2?.retainDocuments) {
      const newParent = options2.parentDoc ?? (arr.isMongooseArray ? arr.$parent() : void 0);
      if (arr.isMongooseDocumentArray) {
        ret = new (arr.$schemaType()).schema.base.Types.DocumentArray([], arr.$path(), newParent, arr.$schemaType());
      } else if (arr.isMongooseArray) {
        ret = new (arr.$parent()).schema.base.Types.Array([], arr.$path(), newParent, arr.$schemaType());
      } else {
        ret = new Array(len);
      }
    } else {
      ret = new Array(len);
    }
    arr = isMongooseArray2(arr) ? arr.__array : arr;
    if (ret.isMongooseDocumentArray) {
      options2 = { ...options2, parentArray: ret };
      for (i = 0; i < len; ++i) {
        const doc = clone(arr[i], options2, true);
        if (typeof doc?.$setIndex === "function") {
          doc.$setIndex(i);
        }
        ret.set(i, doc, true);
      }
    } else {
      for (i = 0; i < len; ++i) {
        ret[i] = clone(arr[i], options2, true);
      }
    }
    return ret;
  }
  function cloneRegExp(regexp) {
    const ret = new RegExp(regexp.source, regexp.flags);
    if (ret.lastIndex !== regexp.lastIndex) {
      ret.lastIndex = regexp.lastIndex;
    }
    return ret;
  }
  return clone_1;
}
var collection;
var hasRequiredCollection;
function requireCollection() {
  if (hasRequiredCollection) return collection;
  hasRequiredCollection = 1;
  const MongooseCollection = requireCollection$1();
  const MongooseError = requireMongooseError();
  const Collection = requireLib$1().Collection;
  const ObjectId = requireObjectid$1();
  const getConstructorName2 = requireGetConstructorName();
  const internalToObjectOptions = requireOptions().internalToObjectOptions;
  const stream = require$$0$1;
  const util = require$$1;
  const formatToObjectOptions = Object.freeze({ ...internalToObjectOptions, copyTrustedSymbol: false });
  function NativeCollection(name, conn, options2) {
    this.collection = null;
    this.Promise = options2.Promise || Promise;
    this.modelName = options2.modelName;
    delete options2.modelName;
    this._closed = false;
    MongooseCollection.apply(this, arguments);
  }
  Object.setPrototypeOf(NativeCollection.prototype, MongooseCollection.prototype);
  NativeCollection.prototype.onOpen = function() {
    this.collection = this.conn.db.collection(this.name);
    MongooseCollection.prototype.onOpen.call(this);
    return this.collection;
  };
  NativeCollection.prototype.onClose = function(force) {
    MongooseCollection.prototype.onClose.call(this, force);
  };
  NativeCollection.prototype._getCollection = function _getCollection() {
    if (this.collection) {
      return this.collection;
    }
    if (this.conn.db != null) {
      this.collection = this.conn.db.collection(this.name);
      return this.collection;
    }
    return null;
  };
  function iter(i) {
    NativeCollection.prototype[i] = function() {
      const collection2 = this._getCollection();
      const args = arguments;
      const _this = this;
      const globalDebug = _this?.conn?.base?.options?.debug;
      const connectionDebug = _this?.conn?.options?.debug;
      const debug = connectionDebug == null ? globalDebug : connectionDebug;
      if (this.conn.$wasForceClosed) {
        const error2 = new MongooseError("Connection was force closed");
        if (args.length > 0 && typeof args[args.length - 1] === "function") {
          args[args.length - 1](error2);
          return;
        } else {
          throw error2;
        }
      }
      let opId = null;
      let argsArray = null;
      const hasOperationListeners = this.conn.listenerCount("operation-start") > 0 || this.conn.listenerCount("operation-end") > 0;
      if (hasOperationListeners || debug) {
        opId = new ObjectId();
      }
      let timeout = null;
      let waitForBufferPromise = null;
      if (this._shouldBufferCommands() && this.buffer) {
        if (opId == null) {
          opId = new ObjectId();
        }
        this.conn.emit("buffer", {
          _id: opId,
          modelName: _this.modelName,
          collectionName: _this.name,
          method: i,
          args
        });
        const bufferTimeoutMS = this._getBufferTimeoutMS();
        waitForBufferPromise = new Promise((resolve, reject) => {
          this.addQueue(resolve);
          timeout = setTimeout(() => {
            const removed = this.removeQueue(resolve);
            if (removed) {
              const message = "Operation `" + this.name + "." + i + "()` buffering timed out after " + bufferTimeoutMS + "ms";
              const err = new MongooseError(message);
              this.conn.emit("buffer-end", { _id: opId, modelName: _this.modelName, collectionName: _this.name, method: i, error: err });
              reject(err);
            }
          }, bufferTimeoutMS);
        });
        return waitForBufferPromise.then(() => {
          if (timeout) {
            clearTimeout(timeout);
          }
          return this[i].apply(this, args);
        });
      }
      if (debug) {
        if (typeof debug === "function") {
          if (argsArray == null) {
            argsArray = Array.from(args);
          }
          let argsToAdd = null;
          if (typeof argsArray[argsArray.length - 1] == "function") {
            argsToAdd = argsArray.slice(0, argsArray.length - 1);
          } else {
            argsToAdd = argsArray;
          }
          debug.apply(
            _this,
            [_this.name, i].concat(argsToAdd)
          );
        } else if (debug instanceof stream.Writable) {
          this.$printToStream(_this.name, i, args, debug);
        } else {
          const color = debug.color == null ? true : debug.color;
          const shell = debug.shell == null ? false : debug.shell;
          const timestamp = debug.timestamp == null ? false : debug.timestamp;
          this.$print(_this.name, i, args, color, shell, timestamp);
        }
      }
      if (hasOperationListeners) {
        if (argsArray == null) {
          argsArray = Array.from(args);
        }
        this.conn.emit("operation-start", { _id: opId, modelName: _this.modelName, collectionName: this.name, method: i, params: argsArray });
      }
      try {
        if (collection2 == null) {
          const message = "Cannot call `" + this.name + "." + i + "()` before initial connection is complete if `bufferCommands = false`. Make sure you `await mongoose.connect()` if you have `bufferCommands = false`.";
          throw new MongooseError(message);
        }
        const ret = collection2[i].apply(collection2, args);
        if (typeof ret?.then === "function") {
          return ret.then(
            (result) => {
              if (timeout != null) {
                clearTimeout(timeout);
              }
              if (hasOperationListeners) {
                this.conn.emit("operation-end", { _id: opId, modelName: _this.modelName, collectionName: this.name, method: i, result });
              }
              return result;
            },
            (error2) => {
              if (timeout != null) {
                clearTimeout(timeout);
              }
              if (hasOperationListeners) {
                this.conn.emit("operation-end", { _id: opId, modelName: _this.modelName, collectionName: this.name, method: i, error: error2 });
              }
              throw error2;
            }
          );
        }
        if (hasOperationListeners) {
          this.conn.emit("operation-end", { _id: opId, modelName: _this.modelName, collectionName: this.name, method: i, result: ret });
        }
        if (timeout != null) {
          clearTimeout(timeout);
        }
        return ret;
      } catch (error2) {
        if (timeout != null) {
          clearTimeout(timeout);
        }
        if (hasOperationListeners) {
          this.conn.emit("operation-end", { _id: opId, modelName: _this.modelName, collectionName: this.name, method: i, error: error2 });
        }
        throw error2;
      }
    };
  }
  for (const key of Object.getOwnPropertyNames(Collection.prototype)) {
    const descriptor = Object.getOwnPropertyDescriptor(Collection.prototype, key);
    if (descriptor.get !== void 0) {
      continue;
    }
    if (typeof Collection.prototype[key] !== "function") {
      continue;
    }
    iter(key);
  }
  NativeCollection.prototype.$print = function(name, i, args, color, shell, timestamp) {
    let prefix = "";
    if (timestamp) {
      const ts = (/* @__PURE__ */ new Date()).toISOString();
      prefix = color ? `\x1B[0;90m[${ts}]\x1B[0m ` : `[${ts}] `;
    }
    const moduleName = color ? "\x1B[0;36mMongoose:\x1B[0m " : "Mongoose: ";
    const functionCall = [name, i].join(".");
    const _args = [];
    for (let j = args.length - 1; j >= 0; --j) {
      if (this.$format(args[j]) || _args.length) {
        _args.unshift(this.$format(args[j], color, shell));
      }
    }
    const params = "(" + _args.join(", ") + ")";
    console.info(prefix + moduleName + functionCall + params);
  };
  NativeCollection.prototype.$printToStream = function(name, i, args, stream2) {
    const functionCall = [name, i].join(".");
    const _args = [];
    for (let j = args.length - 1; j >= 0; --j) {
      if (this.$format(args[j]) || _args.length) {
        _args.unshift(this.$format(args[j]));
      }
    }
    const params = "(" + _args.join(", ") + ")";
    stream2.write(functionCall + params, "utf8");
  };
  NativeCollection.prototype.$format = function(arg, color, shell) {
    const type2 = typeof arg;
    if (type2 === "function" || type2 === "undefined") return "";
    return format(arg, false, color, shell);
  };
  function inspectable(representation) {
    const ret = {
      inspect: function() {
        return representation;
      }
    };
    if (util.inspect.custom) {
      ret[util.inspect.custom] = ret.inspect;
    }
    return ret;
  }
  function map2(o) {
    return format(o, true);
  }
  function formatObjectId(x, key) {
    x[key] = inspectable('ObjectId("' + x[key].toHexString() + '")');
  }
  function formatDate(x, key, shell) {
    if (shell) {
      x[key] = inspectable('ISODate("' + x[key].toUTCString() + '")');
    } else {
      x[key] = inspectable('new Date("' + x[key].toUTCString() + '")');
    }
  }
  function format(obj, sub, color, shell) {
    if (typeof obj?.toBSON === "function") {
      obj = obj.toBSON();
    }
    if (obj == null) {
      return obj;
    }
    const clone = requireClone();
    let x = sub ? obj : clone(obj, formatToObjectOptions);
    const constructorName = getConstructorName2(x);
    if (constructorName === "Binary") {
      x = "BinData(" + x.sub_type + ', "' + x.toString("base64") + '")';
    } else if (constructorName === "ObjectId") {
      x = inspectable('ObjectId("' + x.toHexString() + '")');
    } else if (constructorName === "Date") {
      x = inspectable('new Date("' + x.toUTCString() + '")');
    } else if (constructorName === "Object") {
      const keys = Object.keys(x);
      const numKeys = keys.length;
      let key;
      for (let i = 0; i < numKeys; ++i) {
        key = keys[i];
        if (x[key]) {
          let error2;
          if (typeof x[key].toBSON === "function") {
            try {
              x[key] = x[key].toBSON();
            } catch (_error) {
              error2 = _error;
            }
          }
          const _constructorName = getConstructorName2(x[key]);
          if (_constructorName === "Binary") {
            x[key] = "BinData(" + x[key].sub_type + ', "' + x[key].buffer.toString("base64") + '")';
          } else if (_constructorName === "Object") {
            x[key] = format(x[key], true);
          } else if (_constructorName === "ObjectId") {
            formatObjectId(x, key);
          } else if (_constructorName === "Date") {
            formatDate(x, key, shell);
          } else if (_constructorName === "ClientSession") {
            x[key] = inspectable('ClientSession("' + (x[key]?.id?.id?.buffer || "").toString("hex") + '")');
          } else if (Array.isArray(x[key])) {
            x[key] = x[key].map(map2);
          } else if (error2 != null) {
            throw error2;
          }
        }
      }
    }
    if (sub) {
      return x;
    }
    return util.inspect(x, false, 10, color).replace(/\n/g, "").replace(/\s{2,}/g, " ");
  }
  NativeCollection.prototype.getIndexes = NativeCollection.prototype.indexInformation;
  collection = NativeCollection;
  return collection;
}
var changeStream;
var hasRequiredChangeStream;
function requireChangeStream() {
  if (hasRequiredChangeStream) return changeStream;
  hasRequiredChangeStream = 1;
  const EventEmitter = require$$0.EventEmitter;
  const MongooseError = requireMongooseError();
  const driverChangeStreamEvents = ["close", "change", "end", "error", "resumeTokenChanged"];
  class ChangeStream extends EventEmitter {
    constructor(changeStreamPromise, pipeline, options2) {
      super();
      this.driverChangeStream = null;
      this.closed = false;
      this.bindedEvents = false;
      this.pipeline = pipeline;
      this.options = options2;
      this.errored = false;
      if (options2?.hydrate && !options2.model) {
        throw new Error(
          "Cannot create change stream with `hydrate: true` unless calling `Model.watch()`"
        );
      }
      this.$driverChangeStreamPromise = changeStreamPromise.then(
        (driverChangeStream) => {
          this.driverChangeStream = driverChangeStream;
          setImmediate(() => this.emit("ready"));
          return this;
        },
        (err) => {
          this.errored = true;
          this.emit("error", err);
          throw err;
        }
      );
    }
    _bindEvents() {
      if (this.bindedEvents) {
        return;
      }
      this.bindedEvents = true;
      if (this.driverChangeStream == null) {
        this.$driverChangeStreamPromise.then(
          () => {
            this.driverChangeStream.on("close", () => {
              this.closed = true;
            });
            driverChangeStreamEvents.forEach((ev) => {
              this.driverChangeStream.on(ev, (data) => {
                if (data?.fullDocument != null && this.options?.hydrate) {
                  data.fullDocument = this.options.model.hydrate(data.fullDocument);
                }
                this.emit(ev, data);
              });
            });
          },
          () => {
          }
          // No need to register events if opening change stream failed
        );
        return;
      }
      this.driverChangeStream.on("close", () => {
        this.closed = true;
      });
      driverChangeStreamEvents.forEach((ev) => {
        this.driverChangeStream.on(ev, (data) => {
          if (data?.fullDocument != null && this.options?.hydrate) {
            data.fullDocument = this.options.model.hydrate(data.fullDocument);
          }
          this.emit(ev, data);
        });
      });
    }
    hasNext(cb) {
      if (this.errored) {
        throw new MongooseError("Cannot call hasNext() on errored ChangeStream");
      }
      if (this.driverChangeStream != null) {
        return this.driverChangeStream.hasNext(cb);
      }
      return this.$driverChangeStreamPromise.then(
        () => this.driverChangeStream.hasNext(cb),
        (err) => {
          if (cb != null) {
            return cb(err);
          }
          throw err;
        }
      );
    }
    next(cb) {
      if (this.errored) {
        throw new MongooseError("Cannot call next() on errored ChangeStream");
      }
      if (this.options?.hydrate) {
        if (cb != null) {
          const originalCb = cb;
          cb = (err, data) => {
            if (err != null) {
              return originalCb(err);
            }
            if (data.fullDocument != null) {
              data.fullDocument = this.options.model.hydrate(data.fullDocument);
            }
            return originalCb(null, data);
          };
        }
        let maybePromise;
        if (this.driverChangeStream != null) {
          maybePromise = this.driverChangeStream.next(cb);
        } else {
          maybePromise = this.$driverChangeStreamPromise.then(
            () => this.driverChangeStream.next(cb),
            (err) => {
              if (cb != null) {
                return cb(err);
              }
              throw err;
            }
          );
        }
        if (typeof maybePromise?.then === "function") {
          maybePromise = maybePromise.then((data) => {
            if (data.fullDocument != null) {
              data.fullDocument = this.options.model.hydrate(data.fullDocument);
            }
            return data;
          });
        }
        return maybePromise;
      }
      if (this.driverChangeStream != null) {
        return this.driverChangeStream.next(cb);
      }
      return this.$driverChangeStreamPromise.then(
        () => this.driverChangeStream.next(cb),
        (err) => {
          if (cb != null) {
            return cb(err);
          }
          throw err;
        }
      );
    }
    addListener(event, handler) {
      if (this.errored) {
        throw new MongooseError("Cannot call addListener() on errored ChangeStream");
      }
      this._bindEvents();
      return super.addListener(event, handler);
    }
    on(event, handler) {
      if (this.errored) {
        throw new MongooseError("Cannot call on() on errored ChangeStream");
      }
      this._bindEvents();
      return super.on(event, handler);
    }
    once(event, handler) {
      if (this.errored) {
        throw new MongooseError("Cannot call once() on errored ChangeStream");
      }
      this._bindEvents();
      return super.once(event, handler);
    }
    close() {
      this.closed = true;
      if (this.driverChangeStream) {
        return this.driverChangeStream.close();
      } else {
        return this.$driverChangeStreamPromise.then(
          () => this.driverChangeStream.close(),
          () => {
          }
          // No need to close if opening the change stream failed
        );
      }
    }
  }
  changeStream = ChangeStream;
  return changeStream;
}
var schema$1 = { exports: {} };
var pathTrie;
var hasRequiredPathTrie;
function requirePathTrie() {
  if (hasRequiredPathTrie) return pathTrie;
  hasRequiredPathTrie = 1;
  const wildcardSegment = "$*";
  const numericSegmentRegex = /^\d+$/;
  class PathTrieNode {
    constructor() {
      this.children = /* @__PURE__ */ new Map();
      this.terminal = false;
      this.hasWildcard = false;
    }
  }
  class PathTrie {
    /**
     * @param {string[]} [paths] paths to add to the trie
     */
    constructor(paths) {
      this.root = new PathTrieNode();
      if (paths != null) {
        for (const path of paths) {
          this.add(path);
        }
      }
    }
    /**
     * Add a dotted path, like `settings.$*` or `profile.firstName`, to the trie.
     *
     * @param {string} path
     */
    add(path) {
      if (path.indexOf(".") === -1) {
        this._addSegment(this.root, path).terminal = true;
        return;
      }
      let node = this.root;
      for (const segment of path.split(".")) {
        node = this._addSegment(node, segment);
      }
      node.terminal = true;
    }
    /**
     * @param {PathTrieNode} node
     * @param {string} segment
     */
    _addSegment(node, segment) {
      let child = node.children.get(segment);
      if (child == null) {
        child = new PathTrieNode();
        node.children.set(segment, child);
        if (segment === wildcardSegment) {
          node.hasWildcard = true;
        }
      }
      return child;
    }
    /**
     * Check if `path` or any of its ancestor paths exist in the trie. `path` is
     * either a single segment string or a pre-split array of segments — lookups
     * never split, callers are responsible for splitting dotted paths.
     * For example, with a trie containing 'profile':
     *   matchesPathOrAncestor(['profile', 'firstName']) === true
     *   matchesPathOrAncestor('profile') === true
     * But with a trie containing 'profile.firstName':
     *   matchesPathOrAncestor('profile') === false
     *
     * @param {string|string[]} path
     */
    matchesPathOrAncestor(path) {
      return this._walk(path) === true;
    }
    /**
     * Check if `path`, any of its ancestor paths, or any of its descendant paths
     * exist in the trie. `path` is either a single segment string or a pre-split
     * array of segments — lookups never split, callers are responsible for
     * splitting dotted paths. For example:
     *   new PathTrie(['profile']).overlapsPath(['profile', 'firstName']) === true
     *   new PathTrie(['profile.firstName']).overlapsPath('profile') === true
     *
     * @param {string|string[]} path
     */
    overlapsPath(path) {
      return this._walk(path) !== false;
    }
    /**
     * Walk `path` through the trie. `path` is assumed to be pre-split - if the
     * path has multiple segments, pass in an array of strings. Returns `true` if
     * a terminal node was found at `path` or one of its ancestors, `false` if the
     * walk dead-ended, and the array of reached nodes if `path` is a strict prefix
     * of entries in the trie.
     *
     * @param {string|string[]} path
     */
    _walk(path) {
      if (typeof path === "string") {
        const next = [];
        const literal = this.root.children.get(path);
        if (literal != null) {
          if (literal.terminal) {
            return true;
          }
          next.push(literal);
        }
        if (this.root.hasWildcard) {
          const wildcard = this.root.children.get(wildcardSegment);
          if (wildcard.terminal) {
            return true;
          }
          next.push(wildcard);
        }
        return next.length === 0 ? false : next;
      }
      let nodes = [this.root];
      for (const segment of path) {
        const next = [];
        const isNumericSegment = numericSegmentRegex.test(segment);
        for (const node of nodes) {
          if (isNumericSegment) {
            next.push(node);
          }
          const literal = node.children.get(segment);
          if (literal != null) {
            if (literal.terminal) {
              return true;
            }
            next.push(literal);
          }
          if (node.hasWildcard) {
            const wildcard = node.children.get(wildcardSegment);
            if (wildcard.terminal) {
              return true;
            }
            next.push(wildcard);
          }
        }
        if (next.length === 0) {
          return false;
        }
        nodes = next;
      }
      return nodes;
    }
  }
  pathTrie = PathTrie;
  return pathTrie;
}
var schemaType = { exports: {} };
var error = { exports: {} };
var messages = { exports: {} };
var hasRequiredMessages;
function requireMessages() {
  if (hasRequiredMessages) return messages.exports;
  hasRequiredMessages = 1;
  (function(module, exports) {
    const msg = module.exports = {};
    msg.DocumentNotFoundError = null;
    msg.general = {};
    msg.general.default = "Validator failed for path `{PATH}` with value `{VALUE}`";
    msg.general.required = "Path `{PATH}` is required.";
    msg.general.allowNull = "Path `{PATH}` does not allow null values.";
    msg.Number = {};
    msg.Number.min = "Path `{PATH}` ({VALUE}) is less than minimum allowed value ({MIN}).";
    msg.Number.max = "Path `{PATH}` ({VALUE}) is more than maximum allowed value ({MAX}).";
    msg.Number.enum = "`{VALUE}` is not a valid enum value for path `{PATH}`.";
    msg.Date = {};
    msg.Date.min = "Path `{PATH}` ({VALUE}) is before minimum allowed value ({MIN}).";
    msg.Date.max = "Path `{PATH}` ({VALUE}) is after maximum allowed value ({MAX}).";
    msg.String = {};
    msg.String.enum = "`{VALUE}` is not a valid enum value for path `{PATH}`.";
    msg.String.match = "Path `{PATH}` is invalid ({VALUE}).";
    msg.String.minlength = "Path `{PATH}` (`{VALUE}`, length {LENGTH}) is shorter than the minimum allowed length ({MINLENGTH}).";
    msg.String.maxlength = "Path `{PATH}` (`{VALUE}`, length {LENGTH}) is longer than the maximum allowed length ({MAXLENGTH}).";
  })(messages);
  return messages.exports;
}
var cast$1;
var hasRequiredCast$1;
function requireCast$1() {
  if (hasRequiredCast$1) return cast$1;
  hasRequiredCast$1 = 1;
  const MongooseError = requireMongooseError();
  const util = require$$1;
  class CastError extends MongooseError {
    constructor(type2, value, path, reason, schemaType2) {
      if (arguments.length > 0) {
        const valueType = getValueType(value);
        const messageFormat = getMessageFormat(schemaType2);
        const msg = formatMessage(null, type2, value, path, messageFormat, valueType, reason);
        super(msg);
        this.init(type2, value, path, reason, schemaType2);
      } else {
        super(formatMessage());
      }
    }
    toJSON() {
      return {
        stringValue: this.stringValue,
        valueType: this.valueType,
        kind: this.kind,
        value: this.value,
        path: this.path,
        reason: this.reason,
        name: this.name,
        message: this.message
      };
    }
    /*!
     * ignore
     */
    init(type2, value, path, reason, schemaType2) {
      this.stringValue = getStringValue(value);
      this.messageFormat = getMessageFormat(schemaType2);
      this.kind = type2;
      this.value = value;
      this.path = path;
      this.reason = reason;
      this.valueType = getValueType(value);
    }
    /**
     * ignore
     * @param {Readonly<CastError>} other
     * @api private
     */
    copy(other) {
      this.messageFormat = other.messageFormat;
      this.stringValue = other.stringValue;
      this.kind = other.kind;
      this.value = other.value;
      this.path = other.path;
      this.reason = other.reason;
      this.message = other.message;
      this.valueType = other.valueType;
    }
    /*!
     * ignore
     */
    setModel(model2) {
      this.message = formatMessage(
        model2,
        this.kind,
        this.value,
        this.path,
        this.messageFormat,
        this.valueType,
        this.reason
      );
    }
  }
  Object.defineProperty(CastError.prototype, "name", {
    value: "CastError"
  });
  function getStringValue(value) {
    let stringValue = util.inspect(value);
    stringValue = stringValue.replace(/^'|'$/g, '"');
    if (!stringValue.startsWith('"')) {
      stringValue = '"' + stringValue + '"';
    }
    return stringValue;
  }
  function getValueType(value) {
    if (value == null) {
      return "" + value;
    }
    const t = typeof value;
    if (t !== "object") {
      return t;
    }
    if (typeof value.constructor !== "function") {
      return t;
    }
    return value.constructor.name;
  }
  function getMessageFormat(schemaType2) {
    const messageFormat = schemaType2?._castErrorMessage || null;
    if (typeof messageFormat === "string" || typeof messageFormat === "function") {
      return messageFormat;
    }
  }
  function formatMessage(model2, kind, value, path, messageFormat, valueType, reason) {
    if (typeof messageFormat === "string") {
      const stringValue = getStringValue(value);
      let ret = messageFormat.replace("{KIND}", kind).replace("{VALUE}", stringValue).replace("{PATH}", path);
      if (model2 != null) {
        ret = ret.replace("{MODEL}", model2.modelName);
      }
      return ret;
    } else if (typeof messageFormat === "function") {
      return messageFormat(value, path, model2, kind);
    } else {
      const stringValue = getStringValue(value);
      const valueTypeMsg = valueType ? " (type " + valueType + ")" : "";
      let ret = "Cast to " + kind + " failed for value " + stringValue + valueTypeMsg + ' at path "' + path + '"';
      if (model2 != null) {
        ret += ' for model "' + model2.modelName + '"';
      }
      if (reason != null && typeof reason.constructor === "function" && reason.constructor.name !== "AssertionError" && reason.constructor.name !== "Error" && reason.constructor.name !== "BSONError") {
        ret += ' because of "' + reason.constructor.name + '"';
      }
      return ret;
    }
  }
  cast$1 = CastError;
  return cast$1;
}
var notFound;
var hasRequiredNotFound;
function requireNotFound() {
  if (hasRequiredNotFound) return notFound;
  hasRequiredNotFound = 1;
  const MongooseError = requireMongooseError();
  const util = require$$1;
  class DocumentNotFoundError extends MongooseError {
    constructor(filter, model2, numAffected, result) {
      let msg;
      const messages2 = MongooseError.messages;
      if (messages2.DocumentNotFoundError != null) {
        msg = typeof messages2.DocumentNotFoundError === "function" ? messages2.DocumentNotFoundError(filter, model2) : messages2.DocumentNotFoundError;
      } else {
        msg = 'No document found for query "' + util.inspect(filter) + '" on model "' + model2 + '"';
      }
      super(msg);
      this.result = result;
      this.numAffected = numAffected;
      this.filter = filter;
      this.query = filter;
    }
  }
  Object.defineProperty(DocumentNotFoundError.prototype, "name", {
    value: "DocumentNotFoundError"
  });
  notFound = DocumentNotFoundError;
  return notFound;
}
var combinePathErrors;
var hasRequiredCombinePathErrors;
function requireCombinePathErrors() {
  if (hasRequiredCombinePathErrors) return combinePathErrors;
  hasRequiredCombinePathErrors = 1;
  combinePathErrors = function combinePathErrors2(err) {
    const keys = Object.keys(err.errors || {});
    const len = keys.length;
    const msgs = [];
    let key;
    for (let i = 0; i < len; ++i) {
      key = keys[i];
      if (err === err.errors[key]) {
        continue;
      }
      msgs.push(key + ": " + err.errors[key].message);
    }
    return msgs.join(", ");
  };
  return combinePathErrors;
}
var validation;
var hasRequiredValidation;
function requireValidation() {
  if (hasRequiredValidation) return validation;
  hasRequiredValidation = 1;
  const MongooseError = requireMongooseError();
  const getConstructorName2 = requireGetConstructorName();
  const util = require$$1;
  const combinePathErrors2 = requireCombinePathErrors();
  class ValidationError extends MongooseError {
    constructor(instance) {
      let _message;
      if (getConstructorName2(instance) === "model") {
        _message = instance.constructor.modelName + " validation failed";
      } else {
        _message = "Validation failed";
      }
      super(_message);
      this.errors = {};
      this._message = _message;
      if (instance) {
        instance.$errors = this.errors;
      }
    }
    /**
     * Console.log helper
     */
    toString() {
      return this.name + ": " + combinePathErrors2(this);
    }
    /**
    * add message
    * @param {string} path
    * @param {string|Error} error
    * @api private
    */
    addError(path, error2) {
      if (error2 instanceof ValidationError) {
        const { errors } = error2;
        for (const errorPath of Object.keys(errors)) {
          this.addError(`${path}.${errorPath}`, errors[errorPath]);
        }
        return;
      }
      this.errors[path] = error2;
      this.message = this._message + ": " + combinePathErrors2(this);
    }
  }
  if (util.inspect.custom) {
    ValidationError.prototype[util.inspect.custom] = ValidationError.prototype.inspect;
  }
  Object.defineProperty(ValidationError.prototype, "toJSON", {
    enumerable: false,
    writable: false,
    configurable: true,
    value: function() {
      return Object.assign({}, this, { name: this.name, message: this.message });
    }
  });
  Object.defineProperty(ValidationError.prototype, "name", {
    value: "ValidationError"
  });
  validation = ValidationError;
  return validation;
}
var validator;
var hasRequiredValidator;
function requireValidator() {
  if (hasRequiredValidator) return validator;
  hasRequiredValidator = 1;
  const MongooseError = requireMongooseError();
  class ValidatorError extends MongooseError {
    constructor(properties, doc) {
      let msg = properties.message;
      if (!msg) {
        msg = MongooseError.messages.general.default;
      }
      const message = formatMessage(msg, properties, doc);
      super(message);
      properties = Object.assign({}, properties, { message });
      this.properties = properties;
      this.kind = properties.type;
      this.path = properties.path;
      this.value = properties.value;
      this.reason = properties.reason;
    }
    /**
     * toString helper
     * TODO remove? This defaults to `${this.name}: ${this.message}`
     * @api private
     */
    toString() {
      return this.message;
    }
    /**
     * Ensure `name` and `message` show up in toJSON output re: gh-9296
     * @api private
     */
    toJSON() {
      return Object.assign({ name: this.name, message: this.message }, this);
    }
  }
  Object.defineProperty(ValidatorError.prototype, "name", {
    value: "ValidatorError"
  });
  Object.defineProperty(ValidatorError.prototype, "properties", {
    enumerable: false,
    writable: true,
    value: null
  });
  ValidatorError.prototype.formatMessage = formatMessage;
  function formatMessage(msg, properties, doc) {
    if (typeof msg === "function") {
      return msg(properties, doc);
    }
    const propertyNames = Object.keys(properties);
    for (const propertyName of propertyNames) {
      if (propertyName === "message") {
        continue;
      }
      msg = msg.replace("{" + propertyName.toUpperCase() + "}", properties[propertyName]);
    }
    return msg;
  }
  validator = ValidatorError;
  return validator;
}
var version$1;
var hasRequiredVersion;
function requireVersion() {
  if (hasRequiredVersion) return version$1;
  hasRequiredVersion = 1;
  const MongooseError = requireMongooseError();
  class VersionError extends MongooseError {
    constructor(doc, currentVersion, modifiedPaths2) {
      const modifiedPathsStr = modifiedPaths2.join(", ");
      super('No matching document found for id "' + doc._doc._id + '" version ' + currentVersion + ' modifiedPaths "' + modifiedPathsStr + '"');
      this.version = currentVersion;
      this.modifiedPaths = modifiedPaths2;
    }
  }
  Object.defineProperty(VersionError.prototype, "name", {
    value: "VersionError"
  });
  version$1 = VersionError;
  return version$1;
}
var parallelSave;
var hasRequiredParallelSave;
function requireParallelSave() {
  if (hasRequiredParallelSave) return parallelSave;
  hasRequiredParallelSave = 1;
  const MongooseError = requireMongooseError();
  class ParallelSaveError extends MongooseError {
    constructor(doc) {
      const msg = "Can't save() the same doc multiple times in parallel. Document: ";
      super(msg + doc._doc._id);
    }
  }
  Object.defineProperty(ParallelSaveError.prototype, "name", {
    value: "ParallelSaveError"
  });
  parallelSave = ParallelSaveError;
  return parallelSave;
}
var overwriteModel;
var hasRequiredOverwriteModel;
function requireOverwriteModel() {
  if (hasRequiredOverwriteModel) return overwriteModel;
  hasRequiredOverwriteModel = 1;
  const MongooseError = requireMongooseError();
  class OverwriteModelError extends MongooseError {
    constructor(name) {
      super("Cannot overwrite `" + name + "` model once compiled.");
    }
  }
  Object.defineProperty(OverwriteModelError.prototype, "name", {
    value: "OverwriteModelError"
  });
  overwriteModel = OverwriteModelError;
  return overwriteModel;
}
var missingSchema;
var hasRequiredMissingSchema;
function requireMissingSchema() {
  if (hasRequiredMissingSchema) return missingSchema;
  hasRequiredMissingSchema = 1;
  const MongooseError = requireMongooseError();
  class MissingSchemaError extends MongooseError {
    constructor(name) {
      const msg = `Schema hasn't been registered for model "` + name + '".\nUse mongoose.model(name, schema)';
      super(msg);
    }
  }
  Object.defineProperty(MissingSchemaError.prototype, "name", {
    value: "MissingSchemaError"
  });
  missingSchema = MissingSchemaError;
  return missingSchema;
}
var bulkSaveIncompleteError;
var hasRequiredBulkSaveIncompleteError;
function requireBulkSaveIncompleteError() {
  if (hasRequiredBulkSaveIncompleteError) return bulkSaveIncompleteError;
  hasRequiredBulkSaveIncompleteError = 1;
  const MongooseError = requireMongooseError();
  class MongooseBulkSaveIncompleteError extends MongooseError {
    constructor(modelName, documents, bulkWriteResult2) {
      const matchedCount = bulkWriteResult2?.matchedCount ?? 0;
      const insertedCount = bulkWriteResult2?.insertedCount ?? 0;
      let preview = documents.map((doc) => doc._id).join(", ");
      if (preview.length > 100) {
        preview = preview.slice(0, 100) + "...";
      }
      const numDocumentsNotUpdated = documents.length - matchedCount - insertedCount;
      super(`${modelName}.bulkSave() was not able to update ${numDocumentsNotUpdated} of the given documents due to incorrect version or optimistic concurrency, document ids: ${preview}`);
      this.modelName = modelName;
      this.documents = documents;
      this.bulkWriteResult = bulkWriteResult2;
      this.numDocumentsNotUpdated = numDocumentsNotUpdated;
    }
  }
  Object.defineProperty(MongooseBulkSaveIncompleteError.prototype, "name", {
    value: "MongooseBulkSaveIncompleteError"
  });
  bulkSaveIncompleteError = MongooseBulkSaveIncompleteError;
  return bulkSaveIncompleteError;
}
var allServersUnknown;
var hasRequiredAllServersUnknown;
function requireAllServersUnknown() {
  if (hasRequiredAllServersUnknown) return allServersUnknown;
  hasRequiredAllServersUnknown = 1;
  const getConstructorName2 = requireGetConstructorName();
  allServersUnknown = function allServersUnknown2(topologyDescription) {
    if (getConstructorName2(topologyDescription) !== "TopologyDescription") {
      return false;
    }
    const servers = Array.from(topologyDescription.servers.values());
    return servers.length > 0 && servers.every((server) => server.type === "Unknown");
  };
  return allServersUnknown;
}
var isAtlas;
var hasRequiredIsAtlas;
function requireIsAtlas() {
  if (hasRequiredIsAtlas) return isAtlas;
  hasRequiredIsAtlas = 1;
  const getConstructorName2 = requireGetConstructorName();
  isAtlas = function isAtlas2(topologyDescription) {
    if (getConstructorName2(topologyDescription) !== "TopologyDescription") {
      return false;
    }
    if (topologyDescription.servers.size === 0) {
      return false;
    }
    for (const server of topologyDescription.servers.values()) {
      if (server.host.endsWith(".mongodb.net") === false || server.port !== 27017) {
        return false;
      }
    }
    return true;
  };
  return isAtlas;
}
var isSSLError;
var hasRequiredIsSSLError;
function requireIsSSLError() {
  if (hasRequiredIsSSLError) return isSSLError;
  hasRequiredIsSSLError = 1;
  const getConstructorName2 = requireGetConstructorName();
  const nonSSLMessage = "Client network socket disconnected before secure TLS connection was established";
  isSSLError = function isSSLError2(topologyDescription) {
    if (getConstructorName2(topologyDescription) !== "TopologyDescription") {
      return false;
    }
    const descriptions = Array.from(topologyDescription.servers.values());
    return descriptions.length > 0 && descriptions.every((descr) => descr.error && descr.error.message.indexOf(nonSSLMessage) !== -1);
  };
  return isSSLError;
}
var serverSelection;
var hasRequiredServerSelection;
function requireServerSelection() {
  if (hasRequiredServerSelection) return serverSelection;
  hasRequiredServerSelection = 1;
  const MongooseError = requireMongooseError();
  const allServersUnknown2 = requireAllServersUnknown();
  const isAtlas2 = requireIsAtlas();
  const isSSLError2 = requireIsSSLError();
  const atlasMessage = "Could not connect to any servers in your MongoDB Atlas cluster. One common reason is that you're trying to access the database from an IP that isn't whitelisted. Make sure your current IP address is on your Atlas cluster's IP whitelist: https://www.mongodb.com/docs/atlas/security-whitelist/";
  const sslMessage = "Mongoose is connecting with SSL enabled, but the server is not accepting SSL connections. Please ensure that the MongoDB server you are connecting to is configured to accept SSL connections. Learn more: https://mongoosejs.com/docs/tutorials/ssl.html";
  class MongooseServerSelectionError extends MongooseError {
    /**
     * MongooseServerSelectionError constructor
     *
     * @api private
     */
    assimilateError(err) {
      const reason = err.reason;
      const isAtlasWhitelistError = isAtlas2(reason) && allServersUnknown2(reason) && err.message.indexOf("bad auth") === -1 && err.message.indexOf("Authentication failed") === -1;
      if (isAtlasWhitelistError) {
        this.message = atlasMessage;
      } else if (isSSLError2(reason)) {
        this.message = sslMessage;
      } else {
        this.message = err.message;
      }
      for (const key in err) {
        if (key !== "name") {
          this[key] = err[key];
        }
      }
      this.cause = reason;
      return this;
    }
  }
  Object.defineProperty(MongooseServerSelectionError.prototype, "name", {
    value: "MongooseServerSelectionError"
  });
  serverSelection = MongooseServerSelectionError;
  return serverSelection;
}
var divergentArray;
var hasRequiredDivergentArray;
function requireDivergentArray() {
  if (hasRequiredDivergentArray) return divergentArray;
  hasRequiredDivergentArray = 1;
  const MongooseError = requireMongooseError();
  class DivergentArrayError extends MongooseError {
    constructor(paths) {
      const msg = "For your own good, using `document.save()` to update an array which was selected using an $elemMatch or $slice projection OR populated using skip, limit, query conditions, or exclusion of the _id field when the operation results in a $pop or $set of the entire array is not supported. The following path(s) would have been modified unsafely:\n  " + paths.join("\n  ") + "\nUse Model.updateOne() to update these arrays instead. See https://mongoosejs.com/docs/faq.html#divergent-array-error for more information.";
      super(msg);
    }
  }
  Object.defineProperty(DivergentArrayError.prototype, "name", {
    value: "DivergentArrayError"
  });
  divergentArray = DivergentArrayError;
  return divergentArray;
}
var strict;
var hasRequiredStrict;
function requireStrict() {
  if (hasRequiredStrict) return strict;
  hasRequiredStrict = 1;
  const MongooseError = requireMongooseError();
  class StrictModeError extends MongooseError {
    constructor(path, msg, immutable) {
      msg = msg || "Field `" + path + "` is not in schema and strict mode is set to throw.";
      super(msg);
      this.isImmutableError = !!immutable;
      this.path = path;
    }
  }
  Object.defineProperty(StrictModeError.prototype, "name", {
    value: "StrictModeError"
  });
  strict = StrictModeError;
  return strict;
}
var strictPopulate;
var hasRequiredStrictPopulate;
function requireStrictPopulate() {
  if (hasRequiredStrictPopulate) return strictPopulate;
  hasRequiredStrictPopulate = 1;
  const MongooseError = requireMongooseError();
  class StrictPopulateError extends MongooseError {
    constructor(path, msg) {
      msg = msg || "Cannot populate path `" + path + "` because it is not in your schema. Set the `strictPopulate` option to false to override.";
      super(msg);
      this.path = path;
    }
  }
  Object.defineProperty(StrictPopulateError.prototype, "name", {
    value: "StrictPopulateError"
  });
  strictPopulate = StrictPopulateError;
  return strictPopulate;
}
var hasRequiredError;
function requireError() {
  if (hasRequiredError) return error.exports;
  hasRequiredError = 1;
  (function(module, exports) {
    const MongooseError = requireMongooseError();
    module.exports = MongooseError;
    MongooseError.messages = requireMessages();
    MongooseError.Messages = MongooseError.messages;
    MongooseError.CastError = requireCast$1();
    MongooseError.DocumentNotFoundError = requireNotFound();
    MongooseError.ValidationError = requireValidation();
    MongooseError.ValidatorError = requireValidator();
    MongooseError.VersionError = requireVersion();
    MongooseError.ParallelSaveError = requireParallelSave();
    MongooseError.OverwriteModelError = requireOverwriteModel();
    MongooseError.MissingSchemaError = requireMissingSchema();
    MongooseError.MongooseBulkSaveIncompleteError = requireBulkSaveIncompleteError();
    MongooseError.MongooseServerSelectionError = requireServerSelection();
    MongooseError.DivergentArrayError = requireDivergentArray();
    MongooseError.StrictModeError = requireStrict();
    MongooseError.StrictPopulateError = requireStrictPopulate();
  })(error);
  return error.exports;
}
var propertyOptions;
var hasRequiredPropertyOptions;
function requirePropertyOptions() {
  if (hasRequiredPropertyOptions) return propertyOptions;
  hasRequiredPropertyOptions = 1;
  propertyOptions = Object.freeze({
    enumerable: true,
    configurable: true,
    writable: true,
    value: void 0
  });
  return propertyOptions;
}
var schemaTypeOptions;
var hasRequiredSchemaTypeOptions;
function requireSchemaTypeOptions() {
  if (hasRequiredSchemaTypeOptions) return schemaTypeOptions;
  hasRequiredSchemaTypeOptions = 1;
  const clone = requireClone();
  class SchemaTypeOptions {
    constructor(obj) {
      if (obj == null) {
        return this;
      }
      Object.assign(this, clone(obj));
    }
  }
  const opts = requirePropertyOptions();
  Object.defineProperty(SchemaTypeOptions.prototype, "type", opts);
  Object.defineProperty(SchemaTypeOptions.prototype, "validate", opts);
  Object.defineProperty(SchemaTypeOptions.prototype, "cast", opts);
  Object.defineProperty(SchemaTypeOptions.prototype, "required", opts);
  Object.defineProperty(SchemaTypeOptions.prototype, "allowNull", opts);
  Object.defineProperty(SchemaTypeOptions.prototype, "default", opts);
  Object.defineProperty(SchemaTypeOptions.prototype, "ref", opts);
  Object.defineProperty(SchemaTypeOptions.prototype, "refPath", opts);
  Object.defineProperty(SchemaTypeOptions.prototype, "select", opts);
  Object.defineProperty(SchemaTypeOptions.prototype, "index", opts);
  Object.defineProperty(SchemaTypeOptions.prototype, "unique", opts);
  Object.defineProperty(SchemaTypeOptions.prototype, "immutable", opts);
  Object.defineProperty(SchemaTypeOptions.prototype, "sparse", opts);
  Object.defineProperty(SchemaTypeOptions.prototype, "text", opts);
  Object.defineProperty(SchemaTypeOptions.prototype, "transform", opts);
  schemaTypeOptions = SchemaTypeOptions;
  return schemaTypeOptions;
}
var boolean$1 = { exports: {} };
var hasRequiredBoolean$1;
function requireBoolean$1() {
  if (hasRequiredBoolean$1) return boolean$1.exports;
  hasRequiredBoolean$1 = 1;
  (function(module) {
    const CastError = requireCast$1();
    module.exports = function castBoolean(value, path) {
      if (module.exports.convertToTrue.has(value)) {
        return true;
      }
      if (module.exports.convertToFalse.has(value)) {
        return false;
      }
      if (value == null) {
        return value;
      }
      throw new CastError("boolean", value, path);
    };
    module.exports.convertToTrue = /* @__PURE__ */ new Set([true, "true", 1, "1", "yes"]);
    module.exports.convertToFalse = /* @__PURE__ */ new Set([false, "false", 0, "0", "no"]);
  })(boolean$1);
  return boolean$1.exports;
}
var exists;
var hasRequiredExists;
function requireExists() {
  if (hasRequiredExists) return exists;
  hasRequiredExists = 1;
  const castBoolean = requireBoolean$1();
  exists = function(val) {
    const path = this?.path ?? null;
    return castBoolean(val, path);
  };
  return exists;
}
var type;
var hasRequiredType;
function requireType() {
  if (hasRequiredType) return type;
  hasRequiredType = 1;
  type = function(val) {
    if (Array.isArray(val)) {
      if (!val.every((v) => typeof v === "number" || typeof v === "string")) {
        throw new Error("$type array values must be strings or numbers");
      }
      return val;
    }
    if (typeof val !== "number" && typeof val !== "string") {
      throw new Error("$type parameter must be number, string, or array of numbers and strings");
    }
    return val;
  };
  return type;
}
var handleImmutable$1;
var hasRequiredHandleImmutable$1;
function requireHandleImmutable$1() {
  if (hasRequiredHandleImmutable$1) return handleImmutable$1;
  hasRequiredHandleImmutable$1 = 1;
  const StrictModeError = requireStrict();
  handleImmutable$1 = function(schematype) {
    if (schematype.$immutable) {
      schematype.$immutableSetter = createImmutableSetter(
        schematype.path,
        schematype.options.immutable
      );
      schematype.set(schematype.$immutableSetter);
    } else if (schematype.$immutableSetter) {
      schematype.setters = schematype.setters.filter((fn) => fn !== schematype.$immutableSetter);
      delete schematype.$immutableSetter;
    }
  };
  function createImmutableSetter(path, immutable) {
    return function immutableSetter(v, _priorVal, _doc, options2) {
      if (this == null || this.$__ == null) {
        return v;
      }
      if (this.isNew) {
        return v;
      }
      if (options2?.overwriteImmutable) {
        return v;
      }
      const _immutable = typeof immutable === "function" ? immutable.call(this, this) : immutable;
      if (!_immutable) {
        return v;
      }
      const _value = this.$__.priorDoc != null ? this.$__.priorDoc.$__getValue(path) : this.$__getValue(path);
      if (this.$__.strictMode === "throw" && v !== _value) {
        throw new StrictModeError(path, "Path `" + path + "` is immutable and strict mode is set to throw.", true);
      }
      return _value;
    };
  }
  return handleImmutable$1;
}
var isAsyncFunction;
var hasRequiredIsAsyncFunction;
function requireIsAsyncFunction() {
  if (hasRequiredIsAsyncFunction) return isAsyncFunction;
  hasRequiredIsAsyncFunction = 1;
  isAsyncFunction = function isAsyncFunction2(v) {
    return typeof v === "function" && v.constructor && v.constructor.name === "AsyncFunction";
  };
  return isAsyncFunction;
}
var isSimpleValidator;
var hasRequiredIsSimpleValidator;
function requireIsSimpleValidator() {
  if (hasRequiredIsSimpleValidator) return isSimpleValidator;
  hasRequiredIsSimpleValidator = 1;
  isSimpleValidator = function isSimpleValidator2(obj) {
    const keys = Object.keys(obj);
    let result = true;
    for (let i = 0, len = keys.length; i < len; ++i) {
      if (typeof obj[keys[i]] === "object" && obj[keys[i]] !== null) {
        result = false;
        break;
      }
    }
    return result;
  };
  return isSimpleValidator;
}
var utils = {};
var populateOptions;
var hasRequiredPopulateOptions;
function requirePopulateOptions() {
  if (hasRequiredPopulateOptions) return populateOptions;
  hasRequiredPopulateOptions = 1;
  const clone = requireClone();
  class PopulateOptions {
    constructor(obj) {
      this._docs = {};
      this._childDocs = [];
      if (obj == null) {
        return;
      }
      obj = clone(obj);
      Object.assign(this, obj);
      if (typeof obj.subPopulate === "object") {
        this.populate = obj.subPopulate;
      }
      if (obj.perDocumentLimit != null && obj.limit != null) {
        throw new Error("Can not use `limit` and `perDocumentLimit` at the same time. Path: `" + obj.path + "`.");
      }
    }
  }
  populateOptions = PopulateOptions;
  return populateOptions;
}
var isMongooseDocumentArray = {};
var hasRequiredIsMongooseDocumentArray;
function requireIsMongooseDocumentArray() {
  if (hasRequiredIsMongooseDocumentArray) return isMongooseDocumentArray;
  hasRequiredIsMongooseDocumentArray = 1;
  isMongooseDocumentArray.isMongooseDocumentArray = function(mongooseDocumentArray) {
    return Array.isArray(mongooseDocumentArray) && mongooseDocumentArray.isMongooseDocumentArray;
  };
  return isMongooseDocumentArray;
}
var merge;
var hasRequiredMerge;
function requireMerge() {
  if (hasRequiredMerge) return merge;
  hasRequiredMerge = 1;
  merge = function merge2(s1, s2, skipConflictingPaths) {
    const paths = Object.keys(s2.tree);
    const pathsToAdd = {};
    for (const key of paths) {
      if (skipConflictingPaths && (s1.paths[key] || s1.nested[key] || s1.singleNestedPaths[key])) {
        continue;
      }
      pathsToAdd[key] = s2.tree[key];
    }
    s1.options._isMerging = true;
    s1.add(pathsToAdd, null);
    delete s1.options._isMerging;
    s1.callQueue = s1.callQueue.concat(s2.callQueue);
    s1.method(s2.methods);
    s1.static(s2.statics);
    for (const [option, value] of Object.entries(s2._userProvidedOptions)) {
      if (!(option in s1._userProvidedOptions)) {
        s1.set(option, value);
      }
    }
    for (const query2 in s2.query) {
      s1.query[query2] = s2.query[query2];
    }
    for (const virtual in s2.virtuals) {
      s1.virtuals[virtual] = s2.virtuals[virtual].clone();
    }
    s1._indexes = s1._indexes.concat(s2._indexes || []);
    s1.s.hooks.merge(s2.s.hooks, false);
  };
  return merge;
}
var document = { exports: {} };
var internal = { exports: {} };
var stateMachine = { exports: {} };
var hasRequiredStateMachine;
function requireStateMachine() {
  if (hasRequiredStateMachine) return stateMachine.exports;
  hasRequiredStateMachine = 1;
  (function(module, exports) {
    requireUtils();
    const StateMachine = module.exports = function StateMachine2() {
    };
    StateMachine.ctor = function() {
      const states = [...arguments];
      const ctor = function() {
        this.paths = {};
        this.states = {};
      };
      ctor.prototype = new StateMachine();
      ctor.prototype.constructor = ctor;
      ctor.prototype.stateNames = states;
      states.forEach(function(state) {
        ctor.prototype[state] = function(path) {
          this._changeState(path, state);
        };
      });
      return ctor;
    };
    StateMachine.prototype._changeState = function _changeState(path, nextState) {
      const prevState = this.paths[path];
      if (prevState === nextState) {
        return;
      }
      if (prevState !== void 0) {
        const prevBucket = this.states[prevState];
        if (prevBucket) delete prevBucket[path];
      }
      this.paths[path] = nextState;
      this.states[nextState] = this.states[nextState] || {};
      this.states[nextState][path] = true;
    };
    StateMachine.prototype.clearAllExcept = function clearAllExcept(state) {
      const bucket = this.states[state];
      const keys = bucket == null ? [] : Object.keys(bucket);
      if (keys.length === 0) {
        this.paths = {};
        this.states = {};
        return;
      }
      this.paths = {};
      for (const path of keys) {
        this.paths[path] = state;
      }
      this.states = {
        [state]: this.states[state]
      };
    };
    StateMachine.prototype.clear = function clear(state) {
      if (this.states[state] == null) {
        return;
      }
      const keys = Object.keys(this.states[state]);
      if (keys.length === 0) {
        return;
      }
      this.states[state] = {};
      let i = keys.length;
      while (i--) {
        delete this.paths[keys[i]];
      }
    };
    StateMachine.prototype.clearPath = function clearPath(path) {
      const state = this.paths[path];
      if (!state) {
        return;
      }
      delete this.paths[path];
      delete this.states[state][path];
    };
    StateMachine.prototype.getStatePaths = function getStatePaths(state) {
      if (this.states[state] != null) {
        return this.states[state];
      }
      return {};
    };
    StateMachine.prototype.some = function some() {
      const _this = this;
      const what = arguments.length ? arguments : this.stateNames;
      return Array.prototype.some.call(what, function(state) {
        if (_this.states[state] == null) {
          return false;
        }
        return Object.keys(_this.states[state]).length;
      });
    };
    StateMachine.prototype._iter = function _iter(iterMethod) {
      return function() {
        let states = [...arguments];
        const callback = states.pop();
        if (!states.length) states = this.stateNames;
        const _this = this;
        const paths = states.reduce(function(paths2, state) {
          if (_this.states[state] == null) {
            return paths2;
          }
          return paths2.concat(Object.keys(_this.states[state]));
        }, []);
        return paths[iterMethod](function(path, i, paths2) {
          return callback(path, i, paths2);
        });
      };
    };
    StateMachine.prototype.forEach = function forEach() {
      this.forEach = this._iter("forEach");
      return this.forEach.apply(this, arguments);
    };
    StateMachine.prototype.map = function map2() {
      this.map = this._iter("map");
      return this.map.apply(this, arguments);
    };
    StateMachine.prototype.clone = function clone() {
      const result = new this.constructor();
      result.paths = { ...this.paths };
      for (const state of this.stateNames) {
        if (!(state in this.states)) {
          continue;
        }
        result.states[state] = this.states[state] == null ? this.states[state] : { ...this.states[state] };
      }
      return result;
    };
  })(stateMachine);
  return stateMachine.exports;
}
var hasRequiredInternal;
function requireInternal() {
  if (hasRequiredInternal) return internal.exports;
  hasRequiredInternal = 1;
  (function(module, exports) {
    const StateMachine = requireStateMachine();
    const ActiveRoster = StateMachine.ctor("require", "modify", "init", "default", "ignore");
    module.exports = InternalCache;
    function InternalCache() {
      this.activePaths = new ActiveRoster();
    }
    InternalCache.prototype.strictMode = true;
    InternalCache.prototype.fullPath = void 0;
    InternalCache.prototype.selected = void 0;
    InternalCache.prototype.shardval = void 0;
    InternalCache.prototype.saveError = void 0;
    InternalCache.prototype.validationError = void 0;
    InternalCache.prototype.adhocPaths = void 0;
    InternalCache.prototype.removing = void 0;
    InternalCache.prototype.inserting = void 0;
    InternalCache.prototype.saving = void 0;
    InternalCache.prototype.version = void 0;
    InternalCache.prototype._id = void 0;
    InternalCache.prototype.ownerDocument = void 0;
    InternalCache.prototype.populate = void 0;
    InternalCache.prototype.populated = void 0;
    InternalCache.prototype.primitiveAtomics = void 0;
    InternalCache.prototype.wasPopulated = false;
    InternalCache.prototype.scope = void 0;
    InternalCache.prototype.session = null;
    InternalCache.prototype.pathsToScopes = null;
    InternalCache.prototype.cachedRequired = null;
  })(internal);
  return internal.exports;
}
var buffer$1;
var hasRequiredBuffer$1;
function requireBuffer$1() {
  if (hasRequiredBuffer$1) return buffer$1;
  hasRequiredBuffer$1 = 1;
  const Binary = requireBson().Binary;
  const UUID = requireBson().UUID;
  const utils2 = requireUtils();
  function MongooseBuffer(value, encode, offset) {
    let val = value;
    if (value == null) {
      val = 0;
    }
    let encoding;
    let path;
    let doc;
    if (Array.isArray(encode)) {
      path = encode[0];
      doc = encode[1];
    } else {
      encoding = encode;
    }
    let buf;
    if (typeof val === "number" || val instanceof Number) {
      buf = Buffer.alloc(val);
    } else {
      buf = Buffer.from(val, encoding, offset);
    }
    utils2.decorate(buf, MongooseBuffer.mixin);
    buf.isMongooseBuffer = true;
    buf[MongooseBuffer.pathSymbol] = path;
    buf[parentSymbol] = doc;
    buf._subtype = 0;
    return buf;
  }
  const pathSymbol = /* @__PURE__ */ Symbol.for("mongoose#Buffer#_path");
  const parentSymbol = /* @__PURE__ */ Symbol.for("mongoose#Buffer#_parent");
  MongooseBuffer.pathSymbol = pathSymbol;
  MongooseBuffer.mixin = {
    /**
     * Default subtype for the Binary representing this Buffer
     *
     * @api private
     * @property _subtype
     * @memberOf MongooseBuffer.mixin
     * @static
     */
    _subtype: void 0,
    /**
     * Marks this buffer as modified.
     *
     * @api private
     * @method _markModified
     * @memberOf MongooseBuffer.mixin
     * @static
     */
    _markModified: function() {
      const parent = this[parentSymbol];
      if (parent) {
        parent.markModified(this[MongooseBuffer.pathSymbol]);
      }
      return this;
    },
    /**
     * Writes the buffer.
     *
     * @api public
     * @method write
     * @memberOf MongooseBuffer.mixin
     * @static
     */
    write: function() {
      const written = Buffer.prototype.write.apply(this, arguments);
      if (written > 0) {
        this._markModified();
      }
      return written;
    },
    /**
     * Copies the buffer.
     *
     * #### Note:
     *
     * `Buffer#copy` does not mark `target` as modified so you must copy from a `MongooseBuffer` for it to work as expected. This is a work around since `copy` modifies the target, not this.
     *
     * @return {number} The number of bytes copied.
     * @param {Buffer} target
     * @method copy
     * @memberOf MongooseBuffer.mixin
     * @static
     */
    copy: function(target) {
      const ret = Buffer.prototype.copy.apply(this, arguments);
      if (target?.isMongooseBuffer) {
        target._markModified();
      }
      return ret;
    }
  };
  utils2.each(
    [
      // node < 0.5
      "writeUInt8",
      "writeUInt16",
      "writeUInt32",
      "writeInt8",
      "writeInt16",
      "writeInt32",
      "writeFloat",
      "writeDouble",
      "fill",
      "utf8Write",
      "binaryWrite",
      "asciiWrite",
      "set",
      // node >= 0.5
      "writeUInt16LE",
      "writeUInt16BE",
      "writeUInt32LE",
      "writeUInt32BE",
      "writeInt16LE",
      "writeInt16BE",
      "writeInt32LE",
      "writeInt32BE",
      "writeFloatLE",
      "writeFloatBE",
      "writeDoubleLE",
      "writeDoubleBE"
    ],
    function(method) {
      if (!Buffer.prototype[method]) {
        return;
      }
      MongooseBuffer.mixin[method] = function() {
        const ret = Buffer.prototype[method].apply(this, arguments);
        this._markModified();
        return ret;
      };
    }
  );
  MongooseBuffer.mixin.toObject = function(options2) {
    const subtype = typeof options2 === "number" ? options2 : this._subtype || 0;
    return new Binary(Buffer.from(this), subtype);
  };
  MongooseBuffer.mixin.$toObject = MongooseBuffer.mixin.toObject;
  MongooseBuffer.mixin.toBSON = function() {
    return new Binary(this, this._subtype || 0);
  };
  MongooseBuffer.mixin.toUUID = function() {
    if (this._subtype !== 4) {
      throw new Error("Cannot convert a Buffer with subtype " + this._subtype + " to a UUID");
    }
    return new UUID(this);
  };
  MongooseBuffer.mixin.equals = function(other) {
    if (!Buffer.isBuffer(other)) {
      return false;
    }
    if (this.length !== other.length) {
      return false;
    }
    for (let i = 0; i < this.length; ++i) {
      if (this[i] !== other[i]) {
        return false;
      }
    }
    return true;
  };
  MongooseBuffer.mixin.subtype = function(subtype) {
    if (typeof subtype !== "number") {
      throw new TypeError("Invalid subtype. Expected a number");
    }
    if (this._subtype !== subtype) {
      this._markModified();
    }
    this._subtype = subtype;
  };
  MongooseBuffer.Binary = Binary;
  buffer$1 = MongooseBuffer;
  return buffer$1;
}
var symbols = {};
var hasRequiredSymbols;
function requireSymbols() {
  if (hasRequiredSymbols) return symbols;
  hasRequiredSymbols = 1;
  symbols.schemaMixedSymbol = /* @__PURE__ */ Symbol.for("mongoose:schema_mixed");
  symbols.builtInMiddleware = /* @__PURE__ */ Symbol.for("mongoose:built-in-middleware");
  return symbols;
}
var mixed;
var hasRequiredMixed;
function requireMixed() {
  if (hasRequiredMixed) return mixed;
  hasRequiredMixed = 1;
  const SchemaType = requireSchemaType();
  const symbols2 = requireSymbols();
  const isObject2 = requireIsObject();
  const utils2 = requireUtils();
  function SchemaMixed(path, options2, _schemaOptions, parentSchema) {
    if (options2?.default) {
      const def = options2.default;
      if (Array.isArray(def) && def.length === 0) {
        options2.default = Array;
      } else if (!options2.shared && isObject2(def) && utils2.hasOwnKeys(def) === false) {
        options2.default = function() {
          return {};
        };
      }
    }
    SchemaType.call(this, path, options2, "Mixed", parentSchema);
    this[symbols2.schemaMixedSymbol] = true;
  }
  SchemaMixed.schemaName = "Mixed";
  SchemaMixed.defaultOptions = {};
  SchemaMixed.prototype = Object.create(SchemaType.prototype);
  SchemaMixed.prototype.constructor = SchemaMixed;
  SchemaMixed.get = SchemaType.get;
  SchemaMixed.set = SchemaType.set;
  SchemaMixed.setters = [];
  SchemaMixed.prototype.cast = function(val) {
    if (val instanceof Error) {
      return utils2.errorToPOJO(val);
    }
    return val;
  };
  SchemaMixed.prototype.castForQuery = function($cond, val) {
    return val;
  };
  SchemaMixed.prototype.toJSONSchema = function toJSONSchema(_options) {
    return {};
  };
  mixed = SchemaMixed;
  return mixed;
}
var modifiedPathsSnapshot;
var hasRequiredModifiedPathsSnapshot;
function requireModifiedPathsSnapshot() {
  if (hasRequiredModifiedPathsSnapshot) return modifiedPathsSnapshot;
  hasRequiredModifiedPathsSnapshot = 1;
  modifiedPathsSnapshot = class ModifiedPathsSnapshot {
    constructor(subdocSnapshot, activePaths, version2) {
      this.subdocSnapshot = subdocSnapshot;
      this.activePaths = activePaths;
      this.version = version2;
    }
  };
  return modifiedPathsSnapshot;
}
var objectExpected;
var hasRequiredObjectExpected;
function requireObjectExpected() {
  if (hasRequiredObjectExpected) return objectExpected;
  hasRequiredObjectExpected = 1;
  const MongooseError = requireMongooseError();
  class ObjectExpectedError extends MongooseError {
    constructor(path, val) {
      const typeDescription = Array.isArray(val) ? "array" : "primitive value";
      super("Tried to set nested object field `" + path + `\` to ${typeDescription} \`` + val + "`");
      this.path = path;
    }
  }
  Object.defineProperty(ObjectExpectedError.prototype, "name", {
    value: "ObjectExpectedError"
  });
  objectExpected = ObjectExpectedError;
  return objectExpected;
}
var objectParameter;
var hasRequiredObjectParameter;
function requireObjectParameter() {
  if (hasRequiredObjectParameter) return objectParameter;
  hasRequiredObjectParameter = 1;
  const MongooseError = requireMongooseError();
  class ObjectParameterError extends MongooseError {
    constructor(value, paramName, fnName) {
      super('Parameter "' + paramName + '" to ' + fnName + '() must be an object, got "' + (value == null ? value : value.toString()) + '" (type ' + typeof value + ")");
    }
  }
  Object.defineProperty(ObjectParameterError.prototype, "name", {
    value: "ObjectParameterError"
  });
  objectParameter = ObjectParameterError;
  return objectParameter;
}
var parallelValidate;
var hasRequiredParallelValidate;
function requireParallelValidate() {
  if (hasRequiredParallelValidate) return parallelValidate;
  hasRequiredParallelValidate = 1;
  const MongooseError = requireMongooseError();
  class ParallelValidateError extends MongooseError {
    constructor(doc) {
      const msg = "Can't validate() the same doc multiple times in parallel. Document: ";
      super(msg + doc._doc._id);
    }
  }
  Object.defineProperty(ParallelValidateError.prototype, "name", {
    value: "ParallelValidateError"
  });
  parallelValidate = ParallelValidateError;
  return parallelValidate;
}
var hasIncludedChildren;
var hasRequiredHasIncludedChildren;
function requireHasIncludedChildren() {
  if (hasRequiredHasIncludedChildren) return hasIncludedChildren;
  hasRequiredHasIncludedChildren = 1;
  hasIncludedChildren = function hasIncludedChildren2(fields) {
    const hasIncludedChildren3 = {};
    const keys = Object.keys(fields);
    for (const key of keys) {
      if (key.indexOf(".") === -1) {
        hasIncludedChildren3[key] = 1;
        continue;
      }
      const parts = key.split(".");
      let c = parts[0];
      for (let i = 0; i < parts.length; ++i) {
        hasIncludedChildren3[c] = 1;
        if (i + 1 < parts.length) {
          c = c + "." + parts[i + 1];
        }
      }
    }
    return hasIncludedChildren3;
  };
  return hasIncludedChildren;
}
var isNestedProjection;
var hasRequiredIsNestedProjection;
function requireIsNestedProjection() {
  if (hasRequiredIsNestedProjection) return isNestedProjection;
  hasRequiredIsNestedProjection = 1;
  isNestedProjection = function isNestedProjection2(val) {
    if (val == null || typeof val !== "object") {
      return false;
    }
    return val.$slice == null && val.$elemMatch == null && val.$meta == null && val.$ == null;
  };
  return isNestedProjection;
}
var applyDefaults;
var hasRequiredApplyDefaults;
function requireApplyDefaults() {
  if (hasRequiredApplyDefaults) return applyDefaults;
  hasRequiredApplyDefaults = 1;
  const isNestedProjection2 = requireIsNestedProjection();
  applyDefaults = function applyDefaults2(doc, fields, exclude, hasIncludedChildren2, isBeforeSetters, pathsToSkip, options2) {
    const schemaPaths = doc.$__schema.paths;
    const skipParentChangeTracking = options2?.skipParentChangeTracking;
    const skipId = doc.$__.skipId;
    let hasPostSetterDefaults = false;
    for (const p in schemaPaths) {
      let def;
      let curPath = "";
      if (p === "_id" && skipId) {
        continue;
      }
      const type2 = schemaPaths[p];
      if (type2.defaultValue === void 0) {
        continue;
      }
      if (typeof type2.defaultValue === "function" && !type2.defaultValue.$runBeforeSetters) {
        hasPostSetterDefaults = true;
      }
      const path = type2.splitPath();
      const len = path.length;
      if (path[len - 1] === "$*") {
        continue;
      }
      let included = false;
      let doc_ = doc._doc;
      if (len === 1) {
        if (exclude === true) {
          if (p in fields) {
            continue;
          }
        } else if (exclude === false && fields) {
          const hasSubpaths = type2.$isSingleNested || type2.$isMongooseDocumentArray;
          if (p in fields && !isNestedProjection2(fields[p]) || hasSubpaths && hasIncludedChildren2 != null && hasIncludedChildren2[p]) {
            included = true;
          } else if (hasIncludedChildren2 != null && !hasIncludedChildren2[p]) {
            continue;
          }
        }
        if (doc_[p] !== void 0) {
          continue;
        }
        if (isBeforeSetters != null) {
          if (typeof type2.defaultValue === "function") {
            if (!type2.defaultValue.$runBeforeSetters && isBeforeSetters) {
              continue;
            }
            if (type2.defaultValue.$runBeforeSetters && !isBeforeSetters) {
              continue;
            }
          } else if (!isBeforeSetters) {
            continue;
          }
        }
        if (pathsToSkip && pathsToSkip[p]) {
          continue;
        }
        if (fields && exclude !== null && !(exclude === true || included)) {
          continue;
        }
        try {
          def = type2.getDefault(doc, false);
        } catch (err) {
          doc.invalidate(p, err);
          continue;
        }
        if (typeof def !== "undefined") {
          doc_[p] = def;
          applyChangeTracking(doc, p, skipParentChangeTracking);
        }
        continue;
      }
      for (let j = 0; j < len; ++j) {
        if (doc_ == null) {
          break;
        }
        const piece = path[j];
        curPath = curPath.length ? curPath + "." + piece : piece;
        if (exclude === true) {
          if (curPath in fields) {
            break;
          }
        } else if (exclude === false && fields && !included) {
          const hasSubpaths = type2.$isSingleNested || type2.$isMongooseDocumentArray;
          if (curPath in fields && !isNestedProjection2(fields[curPath]) || j === len - 1 && hasSubpaths && hasIncludedChildren2 != null && hasIncludedChildren2[curPath]) {
            included = true;
          } else if (hasIncludedChildren2 != null && !hasIncludedChildren2[curPath]) {
            break;
          }
        }
        if (j === len - 1) {
          if (doc_[piece] !== void 0) {
            break;
          }
          if (isBeforeSetters != null) {
            if (typeof type2.defaultValue === "function") {
              if (!type2.defaultValue.$runBeforeSetters && isBeforeSetters) {
                break;
              }
              if (type2.defaultValue.$runBeforeSetters && !isBeforeSetters) {
                break;
              }
            } else if (!isBeforeSetters) {
              continue;
            }
          }
          if (pathsToSkip && pathsToSkip[curPath]) {
            break;
          }
          if (fields && exclude !== null) {
            if (exclude === true) {
              if (p in fields) {
                continue;
              }
              try {
                def = type2.getDefault(doc, false);
              } catch (err) {
                doc.invalidate(p, err);
                break;
              }
              if (typeof def !== "undefined") {
                doc_[piece] = def;
                applyChangeTracking(doc, p, skipParentChangeTracking);
              }
            } else if (included) {
              try {
                def = type2.getDefault(doc, false);
              } catch (err) {
                doc.invalidate(p, err);
                break;
              }
              if (typeof def !== "undefined") {
                doc_[piece] = def;
                applyChangeTracking(doc, p, skipParentChangeTracking);
              }
            }
          } else {
            try {
              def = type2.getDefault(doc, false);
            } catch (err) {
              doc.invalidate(p, err);
              break;
            }
            if (typeof def !== "undefined") {
              doc_[piece] = def;
              applyChangeTracking(doc, p, skipParentChangeTracking);
            }
          }
        } else {
          doc_ = doc_[piece];
        }
      }
    }
    return hasPostSetterDefaults;
  };
  function applyChangeTracking(doc, fullPath, skipParentChangeTracking) {
    doc.$__.activePaths.default(fullPath);
    if (!skipParentChangeTracking && doc.$isSubdocument && doc.$isSingleNested && doc.$parent() != null) {
      doc.$parent().$__.activePaths.default(doc.$__pathRelativeToParent(fullPath));
    }
  }
  return applyDefaults;
}
var cleanModifiedSubpaths;
var hasRequiredCleanModifiedSubpaths;
function requireCleanModifiedSubpaths() {
  if (hasRequiredCleanModifiedSubpaths) return cleanModifiedSubpaths;
  hasRequiredCleanModifiedSubpaths = 1;
  cleanModifiedSubpaths = function cleanModifiedSubpaths2(doc, path, options2) {
    options2 = options2 || {};
    const skipDocArrays = options2.skipDocArrays;
    let deleted = 0;
    if (!doc) {
      return deleted;
    }
    for (const modifiedPath of Object.keys(doc.$__.activePaths.getStatePaths("modify"))) {
      if (skipDocArrays) {
        const schemaType2 = doc.$__schema.path(modifiedPath);
        if (schemaType2?.$isMongooseDocumentArray) {
          continue;
        }
      }
      if (modifiedPath.startsWith(path + ".")) {
        doc.$__.activePaths.clearPath(modifiedPath);
        ++deleted;
        if (doc.$isSubdocument) {
          cleanParent(doc, modifiedPath);
        }
      }
    }
    return deleted;
  };
  function cleanParent(doc, path, seen = /* @__PURE__ */ new Set()) {
    if (seen.has(doc)) {
      throw new Error("Infinite subdocument loop: subdoc with _id " + doc._id + " is a parent of itself");
    }
    const parent = doc.$parent();
    const newPath = doc.$__pathRelativeToParent(void 0, false) + "." + path;
    parent.$__.activePaths.clearPath(newPath);
    if (parent.$isSubdocument) {
      cleanParent(parent, newPath, seen);
    }
  }
  return cleanModifiedSubpaths;
}
var isInPathsToSave;
var hasRequiredIsInPathsToSave;
function requireIsInPathsToSave() {
  if (hasRequiredIsInPathsToSave) return isInPathsToSave;
  hasRequiredIsInPathsToSave = 1;
  isInPathsToSave = function isInPathsToSave2(path, pathsToSaveSet, pathsToSave) {
    if (pathsToSaveSet.has(path)) {
      return true;
    }
    for (const pathToSave of pathsToSave) {
      if (path.slice(0, pathToSave.length) === pathToSave && path.charAt(pathToSave.length) === ".") {
        return true;
      }
    }
    return false;
  };
  return isInPathsToSave;
}
var compile = {};
var hasRequiredCompile;
function requireCompile() {
  if (hasRequiredCompile) return compile;
  hasRequiredCompile = 1;
  const clone = requireClone();
  const documentSchemaSymbol = requireSymbols$1().documentSchemaSymbol;
  const internalToObjectOptions = requireOptions().internalToObjectOptions;
  const utils2 = requireUtils();
  let Document;
  const getSymbol = requireSymbols$1().getSymbol;
  const scopeSymbol = requireSymbols$1().scopeSymbol;
  const isPOJO2 = utils2.isPOJO;
  compile.compile = compile$1;
  compile.defineKey = defineKey;
  const _isEmptyOptions = Object.freeze({
    minimize: true,
    virtuals: false,
    getters: false,
    transform: false
  });
  const noDottedPathGetOptions = Object.freeze({
    noDottedPath: true
  });
  function compile$1(tree, proto, prefix, options2) {
    Document = Document || requireDocument();
    const typeKey = options2.typeKey;
    for (const key of Object.keys(tree)) {
      const limb = tree[key];
      const hasSubprops = isPOJO2(limb) && utils2.hasOwnKeys(limb) && (!limb[typeKey] || typeKey === "type" && isPOJO2(limb.type) && limb.type.type);
      const subprops = hasSubprops ? limb : null;
      defineKey({ prop: key, subprops, prototype: proto, prefix, options: options2 });
    }
  }
  function defineKey({ prop, subprops, prototype, prefix, options: options2 }) {
    Document = Document || requireDocument();
    const path = (prefix ? prefix + "." : "") + prop;
    prefix = prefix || "";
    const useGetOptions = prefix ? Object.freeze({}) : noDottedPathGetOptions;
    if (subprops) {
      Object.defineProperty(prototype, prop, {
        enumerable: true,
        configurable: true,
        get: function() {
          if (!this.$__) {
            return void 0;
          }
          const _this = this;
          if (!this.$__.getters) {
            this.$__.getters = {};
          }
          if (!this.$__.getters[path]) {
            const nested = Object.create(Document.prototype, getOwnPropertyDescriptors(this));
            if (!prefix) {
              nested.$__[scopeSymbol] = this;
            }
            nested.$__.nestedPath = path;
            Object.defineProperty(nested, "schema", {
              enumerable: false,
              configurable: true,
              writable: false,
              value: prototype.schema
            });
            Object.defineProperty(nested, "$__schema", {
              enumerable: false,
              configurable: true,
              writable: false,
              value: prototype.schema
            });
            Object.defineProperty(nested, documentSchemaSymbol, {
              enumerable: false,
              configurable: true,
              writable: false,
              value: prototype.schema
            });
            Object.defineProperty(nested, "toObject", {
              enumerable: false,
              configurable: true,
              writable: false,
              value: function() {
                return clone(_this.get(path, null, {
                  virtuals: this && this.schema && this.schema.options && this.schema.options.toObject && this.schema.options.toObject.virtuals || null
                }));
              }
            });
            Object.defineProperty(nested, "$__get", {
              enumerable: false,
              configurable: true,
              writable: false,
              value: function() {
                return _this.get(path, null, {
                  virtuals: this?.schema?.options?.toObject?.virtuals || null
                });
              }
            });
            Object.defineProperty(nested, "toJSON", {
              enumerable: false,
              configurable: true,
              writable: false,
              value: function() {
                return _this.get(path, null, {
                  virtuals: this?.schema?.options?.toJSON?.virtuals || null
                });
              }
            });
            Object.defineProperty(nested, "$__isNested", {
              enumerable: false,
              configurable: true,
              writable: false,
              value: true
            });
            Object.defineProperty(nested, "$isEmpty", {
              enumerable: false,
              configurable: true,
              writable: false,
              value: function() {
                return Object.keys(this.get(path, null, _isEmptyOptions) || {}).length === 0;
              }
            });
            Object.defineProperty(nested, "$__parent", {
              enumerable: false,
              configurable: true,
              writable: false,
              value: this
            });
            compile$1(subprops, nested, path, options2);
            this.$__.getters[path] = nested;
          }
          return this.$__.getters[path];
        },
        set: function(v) {
          if (v?.$__isNested) {
            v = v.$__get();
          } else if (v instanceof Document && !v.$__isNested) {
            v = v.$toObject(internalToObjectOptions);
          }
          const doc = this.$__[scopeSymbol] || this;
          doc.$set(path, v);
        }
      });
    } else {
      Object.defineProperty(prototype, prop, {
        enumerable: true,
        configurable: true,
        get: function() {
          return this[getSymbol].call(
            this.$__[scopeSymbol] || this,
            path,
            null,
            useGetOptions
          );
        },
        set: function(v) {
          this.$set.call(this.$__[scopeSymbol] || this, path, v);
        }
      });
    }
  }
  function getOwnPropertyDescriptors(object) {
    const result = {};
    Object.getOwnPropertyNames(object).forEach(function(key) {
      const skip = [
        "isNew",
        "$__",
        "$errors",
        "errors",
        "_doc",
        "$locals",
        "$op",
        "__parentArray",
        "__index",
        "$isDocumentArrayElement"
      ].indexOf(key) === -1;
      if (skip) {
        return;
      }
      result[key] = Object.getOwnPropertyDescriptor(object, key);
      result[key].enumerable = false;
    });
    return result;
  }
  return compile;
}
var firstKey;
var hasRequiredFirstKey;
function requireFirstKey() {
  if (hasRequiredFirstKey) return firstKey;
  hasRequiredFirstKey = 1;
  firstKey = function firstKey2(obj) {
    if (obj == null) {
      return null;
    }
    return Object.keys(obj)[0];
  };
  return firstKey;
}
var common = {};
var hasRequiredCommon;
function requireCommon() {
  if (hasRequiredCommon) return common;
  hasRequiredCommon = 1;
  const Binary = requireBson().Binary;
  const isBsonType = requireIsBsonType();
  const isMongooseObject2 = requireIsMongooseObject();
  const MongooseError = requireError();
  const util = require$$1;
  common.flatten = flatten;
  common.modifiedPaths = modifiedPaths2;
  function flatten(update, path, options2, schema2) {
    let keys;
    if (update && isMongooseObject2(update) && !Buffer.isBuffer(update)) {
      keys = Object.keys(update.toObject({ transform: false, virtuals: false }) || {});
    } else {
      keys = Object.keys(update || {});
    }
    const numKeys = keys.length;
    const result = {};
    path = path ? path + "." : "";
    for (let i = 0; i < numKeys; ++i) {
      const key = keys[i];
      const val = update[key];
      result[path + key] = val;
      const keySchema = schema2?.path?.(path + key);
      const isNested = schema2?.nested?.[path + key];
      if (keySchema?.instance === "Mixed") continue;
      if (shouldFlatten(val)) {
        if (options2?.skipArrays && Array.isArray(val)) {
          continue;
        }
        const flat = flatten(val, path + key, options2, schema2);
        for (const k in flat) {
          result[k] = flat[k];
        }
        if (Array.isArray(val)) {
          result[path + key] = val;
        }
      }
      if (isNested) {
        const paths = Object.keys(schema2.paths);
        for (const p of paths) {
          if (p.startsWith(path + key + ".") && !Object.hasOwn(result, p)) {
            result[p] = void 0;
          }
        }
      }
    }
    return result;
  }
  function modifiedPaths2(update, path, result, recursion = null) {
    if (update == null || typeof update !== "object") {
      return;
    }
    if (recursion == null) {
      recursion = {
        raw: { update, path },
        trace: /* @__PURE__ */ new WeakSet()
      };
    }
    if (recursion.trace.has(update)) {
      throw new MongooseError(`a circular reference in the update value, updateValue:
${util.inspect(recursion.raw.update, { showHidden: false, depth: 1 })}
updatePath: '${recursion.raw.path}'`);
    }
    recursion.trace.add(update);
    const keys = Object.keys(update || {});
    const numKeys = keys.length;
    result = result || {};
    path = path ? path + "." : "";
    for (let i = 0; i < numKeys; ++i) {
      const key = keys[i];
      let val = update[key];
      const _path = path + key;
      result[_path] = true;
      if (!Buffer.isBuffer(val) && isMongooseObject2(val)) {
        val = val.toObject({ transform: false, virtuals: false });
      }
      if (shouldFlatten(val)) {
        modifiedPaths2(val, path + key, result, recursion);
      }
    }
    recursion.trace.delete(update);
    return result;
  }
  function shouldFlatten(val) {
    return val && typeof val === "object" && !(val instanceof Date) && !isBsonType(val, "ObjectId") && (!Array.isArray(val) || val.length !== 0) && !(val instanceof Buffer) && !isBsonType(val, "Decimal128") && !(val instanceof Binary);
  }
  return common;
}
var get;
var hasRequiredGet;
function requireGet() {
  if (hasRequiredGet) return get;
  hasRequiredGet = 1;
  get = function get2(obj, path, def) {
    let parts;
    let isPathArray = false;
    if (typeof path === "string") {
      if (path.indexOf(".") === -1) {
        const _v = getProperty(obj, path);
        if (_v == null) {
          return def;
        }
        return _v;
      }
      parts = path.split(".");
    } else {
      isPathArray = true;
      parts = path;
      if (parts.length === 1) {
        const _v = getProperty(obj, parts[0]);
        if (_v == null) {
          return def;
        }
        return _v;
      }
    }
    let rest = path;
    let cur = obj;
    for (const part of parts) {
      if (cur == null) {
        return def;
      }
      if (!isPathArray && cur[rest] != null) {
        return cur[rest];
      }
      cur = getProperty(cur, part);
      if (!isPathArray) {
        rest = rest.substring(part.length + 1);
      }
    }
    return cur == null ? def : cur;
  };
  function getProperty(obj, prop) {
    if (obj == null) {
      return obj;
    }
    if (obj instanceof Map) {
      return obj.get(prop);
    }
    return obj[prop];
  }
  return get;
}
var areDiscriminatorValuesEqual;
var hasRequiredAreDiscriminatorValuesEqual;
function requireAreDiscriminatorValuesEqual() {
  if (hasRequiredAreDiscriminatorValuesEqual) return areDiscriminatorValuesEqual;
  hasRequiredAreDiscriminatorValuesEqual = 1;
  const isBsonType = requireIsBsonType();
  areDiscriminatorValuesEqual = function areDiscriminatorValuesEqual2(a, b) {
    if (typeof a === "string" && typeof b === "string") {
      return a === b;
    }
    if (typeof a === "number" && typeof b === "number") {
      return a === b;
    }
    if (isBsonType(a, "ObjectId") && isBsonType(b, "ObjectId")) {
      return a.toString() === b.toString();
    }
    return false;
  };
  return areDiscriminatorValuesEqual;
}
var getSchemaDiscriminatorByValue;
var hasRequiredGetSchemaDiscriminatorByValue;
function requireGetSchemaDiscriminatorByValue() {
  if (hasRequiredGetSchemaDiscriminatorByValue) return getSchemaDiscriminatorByValue;
  hasRequiredGetSchemaDiscriminatorByValue = 1;
  const areDiscriminatorValuesEqual2 = requireAreDiscriminatorValuesEqual();
  getSchemaDiscriminatorByValue = function getSchemaDiscriminatorByValue2(schema2, value) {
    if (schema2?.discriminators == null) {
      return null;
    }
    for (const key of Object.keys(schema2.discriminators)) {
      const discriminatorSchema = schema2.discriminators[key];
      if (discriminatorSchema.discriminatorMapping == null) {
        continue;
      }
      if (areDiscriminatorValuesEqual2(discriminatorSchema.discriminatorMapping.value, value)) {
        return discriminatorSchema;
      }
    }
    return null;
  };
  return getSchemaDiscriminatorByValue;
}
var getEmbeddedDiscriminatorPath$1;
var hasRequiredGetEmbeddedDiscriminatorPath$1;
function requireGetEmbeddedDiscriminatorPath$1() {
  if (hasRequiredGetEmbeddedDiscriminatorPath$1) return getEmbeddedDiscriminatorPath$1;
  hasRequiredGetEmbeddedDiscriminatorPath$1 = 1;
  const get2 = requireGet();
  const getSchemaDiscriminatorByValue2 = requireGetSchemaDiscriminatorByValue();
  getEmbeddedDiscriminatorPath$1 = function getEmbeddedDiscriminatorPath2(doc, path, options2) {
    options2 = options2 || {};
    const typeOnly = options2.typeOnly;
    const parts = Array.isArray(path) ? path : path.indexOf(".") === -1 ? [path] : path.split(".");
    let schemaType2 = null;
    let type2 = "adhocOrUndefined";
    const schema2 = getSchemaDiscriminatorByValue2(doc.schema, doc.get(doc.schema.options.discriminatorKey)) || doc.schema;
    for (let i = 0; i < parts.length; ++i) {
      const subpath = parts.slice(0, i + 1).join(".");
      schemaType2 = schema2.path(subpath);
      if (schemaType2 == null) {
        type2 = "adhocOrUndefined";
        continue;
      }
      if (schemaType2.instance === "Mixed") {
        return typeOnly ? "real" : schemaType2;
      }
      type2 = schema2.pathType(subpath);
      if ((schemaType2.$isSingleNested || schemaType2.$isMongooseDocumentArrayElement) && schemaType2.schema.discriminators != null) {
        const discriminators = schemaType2.schema.discriminators;
        const discriminatorKey = doc.get(subpath + "." + get2(schemaType2, "schema.options.discriminatorKey"));
        if (discriminatorKey == null || discriminators[discriminatorKey] == null) {
          continue;
        }
        const rest = parts.slice(i + 1).join(".");
        return getEmbeddedDiscriminatorPath2(doc.get(subpath), rest, options2);
      }
    }
    return typeOnly ? type2 : schemaType2;
  };
  return getEmbeddedDiscriminatorPath$1;
}
var getKeysInSchemaOrder;
var hasRequiredGetKeysInSchemaOrder;
function requireGetKeysInSchemaOrder() {
  if (hasRequiredGetKeysInSchemaOrder) return getKeysInSchemaOrder;
  hasRequiredGetKeysInSchemaOrder = 1;
  const get2 = requireGet();
  getKeysInSchemaOrder = function getKeysInSchemaOrder2(schema2, val, path) {
    const valKeys = Object.keys(val);
    if (valKeys.length <= 1) {
      return valKeys;
    }
    const schemaKeys = path != null ? Object.keys(get2(schema2.tree, path, {})) : Object.keys(schema2.tree);
    const remaining = new Set(valKeys);
    const keys = [];
    for (const key of schemaKeys) {
      if (remaining.delete(key)) {
        keys.push(key);
      }
    }
    for (const key of remaining) {
      keys.push(key);
    }
    return keys;
  };
  return getKeysInSchemaOrder;
}
var getSubdocumentStrictValue;
var hasRequiredGetSubdocumentStrictValue;
function requireGetSubdocumentStrictValue() {
  if (hasRequiredGetSubdocumentStrictValue) return getSubdocumentStrictValue;
  hasRequiredGetSubdocumentStrictValue = 1;
  getSubdocumentStrictValue = function getSubdocumentStrictValue2(schema2, parts) {
    if (parts.length === 1) {
      return void 0;
    }
    let cur = parts[0];
    let strict2 = void 0;
    for (let i = 0; i < parts.length - 1; ++i) {
      const curSchemaType = schema2.path(cur);
      if (curSchemaType?.schema) {
        strict2 = curSchemaType.schema.options.strict;
        schema2 = curSchemaType.schema;
        cur = curSchemaType.$isMongooseDocumentArray && !isNaN(parts[i + 1]) ? "" : parts[i + 1];
      } else {
        cur += cur.length ? "." + parts[i + 1] : parts[i + 1];
      }
    }
    return strict2;
  };
  return getSubdocumentStrictValue;
}
var handleSpreadDoc;
var hasRequiredHandleSpreadDoc;
function requireHandleSpreadDoc() {
  if (hasRequiredHandleSpreadDoc) return handleSpreadDoc;
  hasRequiredHandleSpreadDoc = 1;
  const utils2 = requireUtils();
  const keysToSkip = /* @__PURE__ */ new Set(["__index", "__parentArray", "_doc"]);
  handleSpreadDoc = function handleSpreadDoc2(v, includeExtraKeys) {
    if (utils2.isPOJO(v) && v.$__ != null && v._doc != null) {
      if (includeExtraKeys) {
        const extraKeys = {};
        for (const key of Object.keys(v)) {
          if (typeof key === "symbol") {
            continue;
          }
          if (key[0] === "$") {
            continue;
          }
          if (keysToSkip.has(key)) {
            continue;
          }
          extraKeys[key] = v[key];
        }
        return { ...v._doc, ...extraKeys };
      }
      return v._doc;
    }
    return v;
  };
  return handleSpreadDoc;
}
var isDefiningProjection;
var hasRequiredIsDefiningProjection;
function requireIsDefiningProjection() {
  if (hasRequiredIsDefiningProjection) return isDefiningProjection;
  hasRequiredIsDefiningProjection = 1;
  isDefiningProjection = function isDefiningProjection2(val) {
    if (val == null) {
      return true;
    }
    if (typeof val === "object") {
      return !("$meta" in val) && !("$slice" in val);
    }
    return true;
  };
  return isDefiningProjection;
}
var isExclusive;
var hasRequiredIsExclusive;
function requireIsExclusive() {
  if (hasRequiredIsExclusive) return isExclusive;
  hasRequiredIsExclusive = 1;
  const isDefiningProjection2 = requireIsDefiningProjection();
  const isPOJO2 = requireIsPOJO();
  isExclusive = function isExclusive2(projection) {
    if (projection == null) {
      return null;
    }
    const keys = Object.keys(projection);
    let exclude = null;
    if (keys.length === 1 && keys[0] === "_id") {
      exclude = !projection._id;
    } else {
      for (let ki = 0; ki < keys.length; ++ki) {
        const key = keys[ki];
        if (key !== "_id" && isDefiningProjection2(projection[key])) {
          exclude = isPOJO2(projection[key]) ? isExclusive2(projection[key]) ?? exclude : !projection[key];
          if (exclude != null) {
            break;
          }
        }
      }
    }
    return exclude;
  };
  return isExclusive;
}
var isPathExcluded;
var hasRequiredIsPathExcluded;
function requireIsPathExcluded() {
  if (hasRequiredIsPathExcluded) return isPathExcluded;
  hasRequiredIsPathExcluded = 1;
  const isDefiningProjection2 = requireIsDefiningProjection();
  isPathExcluded = function isPathExcluded2(projection, path) {
    if (projection == null) {
      return false;
    }
    if (path === "_id") {
      return projection._id === 0;
    }
    const paths = Object.keys(projection);
    let type2 = null;
    for (const _path of paths) {
      if (isDefiningProjection2(projection[_path])) {
        type2 = projection[path] === 1 ? "inclusive" : "exclusive";
        break;
      }
    }
    if (type2 === "inclusive") {
      return projection[path] !== 1;
    }
    if (type2 === "exclusive") {
      return projection[path] === 0;
    }
    return false;
  };
  return isPathExcluded;
}
var markArraySubdocsPopulated;
var hasRequiredMarkArraySubdocsPopulated;
function requireMarkArraySubdocsPopulated() {
  if (hasRequiredMarkArraySubdocsPopulated) return markArraySubdocsPopulated;
  hasRequiredMarkArraySubdocsPopulated = 1;
  const utils2 = requireUtils();
  markArraySubdocsPopulated = function markArraySubdocsPopulated2(doc, populated) {
    if (doc._doc._id == null || populated == null || populated.length === 0) {
      return;
    }
    const id = String(doc._doc._id);
    for (const item of populated) {
      if (item.isVirtual) {
        continue;
      }
      const path = item.path;
      const pieces = path.split(".");
      for (let i = 0; i < pieces.length - 1; ++i) {
        const subpath = pieces.slice(0, i + 1).join(".");
        const rest = pieces.slice(i + 1).join(".");
        const val = doc.get(subpath);
        if (val == null) {
          continue;
        }
        if (utils2.isMongooseDocumentArray(val)) {
          for (let j = 0; j < val.length; ++j) {
            if (val[j]) {
              val[j].populated(rest, item._docs[id] == null ? void 0 : item._docs[id][j], item);
            }
          }
          break;
        }
      }
    }
  };
  return markArraySubdocsPopulated;
}
var minimize_1;
var hasRequiredMinimize;
function requireMinimize() {
  if (hasRequiredMinimize) return minimize_1;
  hasRequiredMinimize = 1;
  const { isPOJO: isPOJO2 } = requireUtils();
  minimize_1 = minimize;
  function minimize(obj) {
    const keys = Object.keys(obj);
    let i = keys.length;
    let hasKeys;
    let key;
    let val;
    while (i--) {
      key = keys[i];
      val = obj[key];
      if (isPOJO2(val)) {
        obj[key] = minimize(val);
      }
      if (void 0 === obj[key]) {
        delete obj[key];
        continue;
      }
      hasKeys = true;
    }
    return hasKeys ? obj : void 0;
  }
  return minimize_1;
}
var parentPaths;
var hasRequiredParentPaths;
function requireParentPaths() {
  if (hasRequiredParentPaths) return parentPaths;
  hasRequiredParentPaths = 1;
  const dotRE = /\./g;
  parentPaths = function parentPaths2(path) {
    if (path.indexOf(".") === -1) {
      return [path];
    }
    const pieces = path.split(dotRE);
    const len = pieces.length;
    const ret = new Array(len);
    let cur = "";
    for (let i = 0; i < len; ++i) {
      cur += cur.length !== 0 ? "." + pieces[i] : pieces[i];
      ret[i] = cur;
    }
    return ret;
  };
  return parentPaths;
}
var queryHelpers = {};
var checkEmbeddedDiscriminatorKeyProjection;
var hasRequiredCheckEmbeddedDiscriminatorKeyProjection;
function requireCheckEmbeddedDiscriminatorKeyProjection() {
  if (hasRequiredCheckEmbeddedDiscriminatorKeyProjection) return checkEmbeddedDiscriminatorKeyProjection;
  hasRequiredCheckEmbeddedDiscriminatorKeyProjection = 1;
  checkEmbeddedDiscriminatorKeyProjection = function checkEmbeddedDiscriminatorKeyProjection2(userProjection, path, schema2, selected, addedPaths) {
    const userProjectedInPath = Object.keys(userProjection).reduce((cur, key) => cur || key.startsWith(path + "."), false);
    const _discriminatorKey = path + "." + schema2.options.discriminatorKey;
    if (!userProjectedInPath && addedPaths.length === 1 && addedPaths[0] === _discriminatorKey) {
      selected.splice(selected.indexOf(_discriminatorKey), 1);
    }
  };
  return checkEmbeddedDiscriminatorKeyProjection;
}
var getDiscriminatorByValue;
var hasRequiredGetDiscriminatorByValue;
function requireGetDiscriminatorByValue() {
  if (hasRequiredGetDiscriminatorByValue) return getDiscriminatorByValue;
  hasRequiredGetDiscriminatorByValue = 1;
  const areDiscriminatorValuesEqual2 = requireAreDiscriminatorValuesEqual();
  getDiscriminatorByValue = function getDiscriminatorByValue2(discriminators, value) {
    if (discriminators == null) {
      return null;
    }
    for (const name of Object.keys(discriminators)) {
      const it = discriminators[name];
      if (it.schema && it.schema.discriminatorMapping && areDiscriminatorValuesEqual2(it.schema.discriminatorMapping.value, value)) {
        return it;
      }
    }
    return null;
  };
  return getDiscriminatorByValue;
}
var isPathSelectedInclusive;
var hasRequiredIsPathSelectedInclusive;
function requireIsPathSelectedInclusive() {
  if (hasRequiredIsPathSelectedInclusive) return isPathSelectedInclusive;
  hasRequiredIsPathSelectedInclusive = 1;
  isPathSelectedInclusive = function isPathSelectedInclusive2(fields, path) {
    const chunks = path.split(".");
    let cur = "";
    let j;
    let keys;
    let numKeys;
    for (let i = 0; i < chunks.length; ++i) {
      cur += cur.length ? "." : "" + chunks[i];
      if (fields[cur]) {
        keys = Object.keys(fields);
        numKeys = keys.length;
        for (j = 0; j < numKeys; ++j) {
          if (keys[i].indexOf(cur + ".") === 0 && keys[i].indexOf(path) !== 0) {
            continue;
          }
        }
        return true;
      }
    }
    return false;
  };
  return isPathSelectedInclusive;
}
var hasRequiredQueryHelpers;
function requireQueryHelpers() {
  if (hasRequiredQueryHelpers) return queryHelpers;
  hasRequiredQueryHelpers = 1;
  (function(exports) {
    const PopulateOptions = requirePopulateOptions();
    const checkEmbeddedDiscriminatorKeyProjection2 = requireCheckEmbeddedDiscriminatorKeyProjection();
    const get2 = requireGet();
    const getDiscriminatorByValue2 = requireGetDiscriminatorByValue();
    const isDefiningProjection2 = requireIsDefiningProjection();
    const clone = requireClone();
    const isPathSelectedInclusive2 = requireIsPathSelectedInclusive();
    exports.preparePopulationOptionsMQ = function preparePopulationOptionsMQ(query2, options2) {
      const _populate = query2._mongooseOptions.populate;
      const pop = Object.keys(_populate).reduce((vals, key) => vals.concat([_populate[key]]), []);
      if (options2.lean != null) {
        pop.filter((p) => p?.options?.lean == null).forEach(makeLean(options2.lean));
      }
      const session = query2?.options?.session || null;
      if (session != null) {
        pop.forEach((path) => {
          if (path.options == null) {
            path.options = { session };
            return;
          }
          if (!("session" in path.options)) {
            path.options.session = session;
          }
        });
      }
      const projection = query2._fieldsForExec();
      for (let i = 0; i < pop.length; ++i) {
        if (pop[i] instanceof PopulateOptions) {
          pop[i] = new PopulateOptions({
            ...pop[i],
            _queryProjection: projection,
            _localModel: query2.model
          });
        } else {
          pop[i]._queryProjection = projection;
          pop[i]._localModel = query2.model;
        }
      }
      return pop;
    };
    exports.createModel = function createModel(model2, doc, fields, userProvidedFields, options2) {
      model2.hooks.execPreSync("createModel", doc);
      const discriminatorMapping = model2.schema ? model2.schema.discriminatorMapping : null;
      const key = discriminatorMapping?.isRoot ? discriminatorMapping.key : null;
      const _opts = {
        skipId: true,
        isNew: false,
        willInit: true
      };
      if (options2?.defaults != null) {
        _opts.defaults = options2.defaults;
      }
      if (options2?.strict != null) {
        _opts.strict = options2.strict;
      }
      const value = doc[key];
      if (key && value && model2.discriminators) {
        const discriminator2 = model2.discriminators[value] || getDiscriminatorByValue2(model2.discriminators, value);
        if (discriminator2) {
          const _fields = clone(userProvidedFields);
          exports.applyPaths(_fields, discriminator2.schema);
          return new discriminator2(void 0, _fields, _opts);
        }
      }
      return new model2(void 0, fields, _opts);
    };
    exports.createModelAndInit = function createModelAndInit(model2, doc, fields, userProvidedFields, options2, populatedIds, callback) {
      const initOpts = {};
      if (populatedIds) {
        initOpts.populated = populatedIds;
      }
      if (options2?.middleware != null) {
        initOpts.middleware = options2.middleware;
      }
      const casted = exports.createModel(model2, doc, fields, userProvidedFields, options2);
      try {
        casted.$init(doc, initOpts, callback);
      } catch (error2) {
        callback(error2, casted);
      }
    };
    exports.applyPaths = function applyPaths(fields, schema2, sanitizeProjection2) {
      let exclude;
      let keys;
      const minusPathsToSkip = /* @__PURE__ */ new Set();
      if (fields) {
        keys = Object.keys(fields);
        const minusPaths = [];
        for (let i = 0; i < keys.length; ++i) {
          const key = keys[i];
          if (keys[i][0] !== "-") {
            continue;
          }
          delete fields[key];
          if (key === "-_id") {
            fields["_id"] = 0;
          } else {
            minusPaths.push(key.slice(1));
          }
        }
        keys = Object.keys(fields);
        for (let keyIndex = 0; keyIndex < keys.length; ++keyIndex) {
          if (keys[keyIndex][0] === "+") {
            continue;
          }
          const field = fields[keys[keyIndex]];
          if (!isDefiningProjection2(field)) {
            continue;
          }
          if (keys[keyIndex] === "_id" && keys.length > 1) {
            continue;
          }
          if (keys[keyIndex] === schema2.options.discriminatorKey && keys.length > 1 && field != null && !field) {
            continue;
          }
          exclude = !field;
          break;
        }
        for (const path of minusPaths) {
          const type2 = schema2.path(path);
          if (!type2?.selected || exclude !== false) {
            fields[path] = 0;
            exclude = true;
          } else if (type2?.selected && exclude === false) {
            minusPathsToSkip.add(path);
          }
        }
      }
      const selected = [];
      const excluded = [];
      const stack = [];
      analyzeSchema(schema2);
      switch (exclude) {
        case true:
          for (const fieldName of excluded) {
            if (hasExcludedAncestor(fields, fieldName)) {
              continue;
            }
            fields[fieldName] = 0;
          }
          break;
        case false:
          if (schema2?.paths["_id"]?.options?.select === false) {
            fields._id = 0;
          }
          for (const fieldName of selected) {
            if (minusPathsToSkip.has(fieldName)) {
              continue;
            }
            if (isPathSelectedInclusive2(fields, fieldName)) {
              continue;
            }
            fields[fieldName] = fields[fieldName] || 1;
          }
          break;
        case void 0:
          if (fields == null) {
            break;
          }
          for (const key of Object.keys(fields || {})) {
            if (key.startsWith("+")) {
              delete fields[key];
            }
          }
          for (const fieldName of excluded) {
            if (fields[fieldName] != null) {
              continue;
            }
            fields[fieldName] = 0;
          }
          break;
      }
      function analyzeSchema(schema3, prefix) {
        prefix || (prefix = "");
        if (stack.indexOf(schema3) !== -1) {
          return [];
        }
        stack.push(schema3);
        const addedPaths = [];
        schema3.eachPath(function(path, type2) {
          if (prefix) path = prefix + "." + path;
          if (type2.$isSchemaMap || path.endsWith(".$*")) {
            const plusPath = "+" + path;
            const hasPlusPath = fields && plusPath in fields;
            if (type2.options?.select === false && !hasPlusPath) {
              excluded.push(path);
            }
            return;
          }
          let addedPath = analyzePath(path, type2);
          if (addedPath == null && !Array.isArray(type2) && type2.$isMongooseArray && !type2.$isMongooseDocumentArray) {
            addedPath = analyzePath(path, type2.embeddedSchemaType);
          }
          if (addedPath != null) {
            addedPaths.push(addedPath);
          }
          if (type2.schema) {
            const _addedPaths = analyzeSchema(type2.schema, path);
            if (exclude === false) {
              checkEmbeddedDiscriminatorKeyProjection2(
                fields,
                path,
                type2.schema,
                selected,
                _addedPaths
              );
            }
          }
        });
        stack.pop();
        return addedPaths;
      }
      function analyzePath(path, type2) {
        if (fields == null) {
          return;
        }
        if (typeof type2.selected !== "boolean") {
          return;
        }
        if (type2.selected === false && fields[path]) {
          if (sanitizeProjection2) {
            fields[path] = 0;
          }
          return;
        }
        if (!exclude && type2.selected && path === schema2.options.discriminatorKey && fields[path] != null && !fields[path]) {
          delete fields[path];
          return;
        }
        if (exclude === false && type2.selected && fields[path] != null && !fields[path]) {
          delete fields[path];
          return;
        }
        const plusPath = "+" + path;
        const hasPlusPath = fields && plusPath in fields;
        if (hasPlusPath) {
          delete fields[plusPath];
          if (exclude === false && keys.length > 1 && !~keys.indexOf(path) && !sanitizeProjection2) {
            fields[path] = 1;
          } else if (exclude == null && sanitizeProjection2 && type2.selected === false) {
            fields[path] = 0;
          }
          return;
        }
        const pieces = path.split(".");
        let cur = "";
        for (let i = 0; i < pieces.length; ++i) {
          cur += cur.length ? "." + pieces[i] : pieces[i];
          if (excluded.indexOf(cur) !== -1) {
            return;
          }
        }
        if (!exclude && (type2?.options?.$skipDiscriminatorCheck || false)) {
          let cur2 = "";
          for (let i = 0; i < pieces.length; ++i) {
            cur2 += (cur2.length === 0 ? "" : ".") + pieces[i];
            const projection = get2(fields, cur2, false) || get2(fields, cur2 + ".$", false);
            if (projection && typeof projection !== "object") {
              return;
            }
          }
        }
        (type2.selected ? selected : excluded).push(path);
        return path;
      }
      function hasExcludedAncestor(fields2, path) {
        let i = -1;
        while ((i = path.indexOf(".", i + 1)) !== -1) {
          const ancestor = fields2[path.slice(0, i)];
          if (ancestor != null && !ancestor) {
            return true;
          }
        }
        return false;
      }
    };
    function makeLean(val) {
      return function(option) {
        option.options || (option.options = {});
        if (Array.isArray(val?.virtuals)) {
          val = Object.assign({}, val);
          val.virtuals = val.virtuals.filter((path) => typeof path === "string" && path.startsWith(option.path + ".")).map((path) => path.slice(option.path.length + 1));
        }
        option.options.lean = val;
      };
    }
  })(queryHelpers);
  return queryHelpers;
}
var buildMiddlewareFilter_1;
var hasRequiredBuildMiddlewareFilter;
function requireBuildMiddlewareFilter() {
  if (hasRequiredBuildMiddlewareFilter) return buildMiddlewareFilter_1;
  hasRequiredBuildMiddlewareFilter = 1;
  const symbols2 = requireSymbols();
  const isBuiltInMiddleware = (hook) => hook.fn[symbols2.builtInMiddleware];
  function buildMiddlewareFilter(options2, phase) {
    const shouldRun = options2?.middleware?.[phase] ?? options2?.middleware ?? true;
    return shouldRun ? null : isBuiltInMiddleware;
  }
  buildMiddlewareFilter_1 = {
    buildMiddlewareFilter
  };
  return buildMiddlewareFilter_1;
}
var isPromise_1;
var hasRequiredIsPromise;
function requireIsPromise() {
  if (hasRequiredIsPromise) return isPromise_1;
  hasRequiredIsPromise = 1;
  function isPromise(val) {
    return !!val && (typeof val === "object" || typeof val === "function") && typeof val.then === "function";
  }
  isPromise_1 = isPromise;
  return isPromise_1;
}
var getDeepestSubdocumentForPath;
var hasRequiredGetDeepestSubdocumentForPath;
function requireGetDeepestSubdocumentForPath() {
  if (hasRequiredGetDeepestSubdocumentForPath) return getDeepestSubdocumentForPath;
  hasRequiredGetDeepestSubdocumentForPath = 1;
  getDeepestSubdocumentForPath = function getDeepestSubdocumentForPath2(doc, parts, schema2) {
    let curPath = parts[0];
    let curSchema = schema2;
    let subdoc = doc;
    for (let i = 0; i < parts.length - 1; ++i) {
      const curSchemaType = curSchema.path(curPath);
      if (curSchemaType?.schema) {
        let newSubdoc = subdoc.get(curPath);
        curSchema = curSchemaType.schema;
        curPath = parts[i + 1];
        if (Array.isArray(newSubdoc) && !isNaN(curPath)) {
          newSubdoc = newSubdoc[curPath];
          curPath = "";
        }
        if (newSubdoc == null) {
          break;
        }
        subdoc = newSubdoc;
      } else {
        curPath += curPath.length ? "." + parts[i + 1] : parts[i + 1];
      }
    }
    return subdoc;
  };
  return getDeepestSubdocumentForPath;
}
var subdocument$1;
var hasRequiredSubdocument$1;
function requireSubdocument$1() {
  if (hasRequiredSubdocument$1) return subdocument$1;
  hasRequiredSubdocument$1 = 1;
  const Document = requireDocument();
  const internalToObjectOptions = requireOptions().internalToObjectOptions;
  const util = require$$1;
  const utils2 = requireUtils();
  subdocument$1 = Subdocument;
  function Subdocument(value, fields, parent, options2) {
    if (parent != null) {
      const parentOptions = { isNew: parent.isNew };
      if ("defaults" in parent.$__) {
        parentOptions.defaults = parent.$__.defaults;
      }
      options2 = Object.assign(parentOptions, options2);
    }
    if (options2?.path != null) {
      this.$basePath = options2.path;
    }
    Document.call(this, value, fields, options2);
    delete this.$__.priorDoc;
  }
  Subdocument.prototype = Object.create(Document.prototype);
  Object.defineProperty(Subdocument.prototype, "$isSubdocument", {
    configurable: false,
    writable: false,
    value: true
  });
  Object.defineProperty(Subdocument.prototype, "$isSingleNested", {
    configurable: false,
    writable: false,
    value: true
  });
  Subdocument.prototype.toBSON = function() {
    return this.toObject(internalToObjectOptions);
  };
  Subdocument.prototype.save = async function save(options2) {
    options2 = options2 || {};
    if (!options2.suppressWarning) {
      utils2.warn("mongoose: calling `save()` on a subdoc does **not** save the document to MongoDB, it only runs save middleware. Use `subdoc.save({ suppressWarning: true })` to hide this warning if you're sure this behavior is right for your app.");
    }
    return await this.$__save(options2);
  };
  Subdocument.prototype.$__fullPath = function(path) {
    if (!this.$__.fullPath) {
      this.ownerDocument();
    }
    return path ? this.$__.fullPath + "." + path : this.$__.fullPath;
  };
  Subdocument.prototype.$__pathRelativeToParent = function(p) {
    if (p == null) {
      return this.$basePath;
    }
    if (!this.$basePath) {
      return p;
    }
    return [this.$basePath, p].join(".");
  };
  Subdocument.prototype.$__save = async function $__save(options2) {
    try {
      await this._execDocumentPreHooks("save", options2, [options2]);
    } catch (error2) {
      await this._execDocumentPostHooks("save", options2, error2);
      return;
    }
    await this._execDocumentPostHooks("save", options2);
  };
  Subdocument.prototype.$isValid = function(path) {
    const parent = this.$parent();
    const fullPath = this.$__pathRelativeToParent(path);
    if (parent != null && fullPath != null) {
      return parent.$isValid(fullPath);
    }
    return Document.prototype.$isValid.call(this, path);
  };
  Subdocument.prototype.markModified = function(path) {
    Document.prototype.markModified.call(this, path);
    const parent = this.$parent();
    const fullPath = this.$__pathRelativeToParent(path);
    if (parent == null || fullPath == null) {
      return;
    }
    const myPath = this.$__pathRelativeToParent().replace(/\.$/, "");
    if (parent.isDirectModified(myPath) || this.isNew) {
      return;
    }
    this.$__parent.markModified(fullPath, this);
  };
  Subdocument.prototype.isModified = function(paths, options2, modifiedPaths2) {
    const parent = this.$parent();
    if (parent != null) {
      if (Array.isArray(paths) || typeof paths === "string") {
        paths = Array.isArray(paths) ? paths : paths.split(" ");
        paths = paths.map((p) => this.$__pathRelativeToParent(p)).filter((p) => p != null);
      } else if (!paths) {
        paths = this.$__pathRelativeToParent();
      }
      return parent.$isModified(paths, options2, modifiedPaths2);
    }
    return Document.prototype.isModified.call(this, paths, options2, modifiedPaths2);
  };
  Subdocument.prototype.$markValid = function(path) {
    Document.prototype.$markValid.call(this, path);
    const parent = this.$parent();
    const fullPath = this.$__pathRelativeToParent(path);
    if (parent != null && fullPath != null) {
      parent.$markValid(fullPath);
    }
  };
  Subdocument.prototype.invalidate = function(path, err, val) {
    Document.prototype.invalidate.call(this, path, err, val);
    const parent = this.$parent();
    const fullPath = this.$__pathRelativeToParent(path);
    if (parent != null && fullPath != null) {
      parent.invalidate(fullPath, err, val);
    } else if (err.kind === "cast" || err.name === "CastError" || fullPath == null) {
      throw err;
    }
    return this.ownerDocument().$__.validationError;
  };
  Subdocument.prototype.$ignore = function(path) {
    Document.prototype.$ignore.call(this, path);
    const parent = this.$parent();
    const fullPath = this.$__pathRelativeToParent(path);
    if (parent != null && fullPath != null) {
      parent.$ignore(fullPath);
    }
  };
  Subdocument.prototype.ownerDocument = function() {
    if (this.$__.ownerDocument) {
      return this.$__.ownerDocument;
    }
    let parent = this;
    const paths = [];
    const seenDocs = /* @__PURE__ */ new Set([parent]);
    while (true) {
      if (typeof parent.$__pathRelativeToParent !== "function") {
        break;
      }
      paths.unshift(parent.$__pathRelativeToParent(void 0, true));
      const _parent = parent.$parent();
      if (_parent == null) {
        break;
      }
      parent = _parent;
      if (seenDocs.has(parent)) {
        throw new Error("Infinite subdocument loop: subdoc with _id " + parent._id + " is a parent of itself");
      }
      seenDocs.add(parent);
    }
    this.$__.fullPath = paths.join(".");
    this.$__.ownerDocument = parent;
    return this.$__.ownerDocument;
  };
  Subdocument.prototype.$__fullPathWithIndexes = function() {
    let parent = this;
    const paths = [];
    const seenDocs = /* @__PURE__ */ new Set([parent]);
    while (true) {
      if (typeof parent.$__pathRelativeToParent !== "function") {
        break;
      }
      paths.unshift(parent.$__pathRelativeToParent(void 0, false));
      const _parent = parent.$parent();
      if (_parent == null) {
        break;
      }
      parent = _parent;
      if (seenDocs.has(parent)) {
        throw new Error("Infinite subdocument loop: subdoc with _id " + parent._id + " is a parent of itself");
      }
      seenDocs.add(parent);
    }
    return paths.join(".");
  };
  Subdocument.prototype.parent = function() {
    return this.$__parent;
  };
  Subdocument.prototype.$parent = Subdocument.prototype.parent;
  Subdocument.prototype.$__setParent = function $__setParent(parent) {
    this.$__parent = parent;
  };
  Subdocument.prototype.$__removeFromParent = function() {
    this.$__parent.set(this.$basePath, null);
  };
  Subdocument.prototype.deleteOne = function deleteOne(options2) {
    registerRemoveListener(this);
    if (!options2?.noop) {
      this.$__removeFromParent();
      const owner = this.ownerDocument();
      owner.$__.removedSubdocs = owner.$__.removedSubdocs || [];
      owner.$__.removedSubdocs.push(this);
    }
  };
  Subdocument.prototype.populate = function() {
    throw new Error('Mongoose does not support calling populate() on nested docs. Instead of `doc.nested.populate("path")`, use `doc.populate("nested.path")`');
  };
  Subdocument.prototype.inspect = function() {
    return this.toObject();
  };
  if (util.inspect.custom) {
    Subdocument.prototype[util.inspect.custom] = Subdocument.prototype.inspect;
  }
  Subdocument.prototype.$toObject = function $toObject(options2, json) {
    const ret = Document.prototype.$toObject.call(this, options2, json);
    if (utils2.hasOwnKeys(ret) === false && options2?._calledWithOptions != null) {
      const minimize = options2._calledWithOptions?.minimize ?? this?.$__schemaTypeOptions?.minimize ?? options2.minimize;
      if (minimize && !this.constructor.$__required && !this.$isDocumentArrayElement) {
        return void 0;
      }
    }
    return ret;
  };
  function registerRemoveListener(sub) {
    const owner = sub.ownerDocument();
    function emitRemove() {
      owner.$removeListener("save", emitRemove);
      owner.$removeListener("deleteOne", emitRemove);
      sub.emit("deleteOne", sub);
      sub.constructor.emit("deleteOne", sub);
    }
    owner.$on("save", emitRemove);
    owner.$on("deleteOne", emitRemove);
  }
  return subdocument$1;
}
var arraySubdocument;
var hasRequiredArraySubdocument;
function requireArraySubdocument() {
  if (hasRequiredArraySubdocument) return arraySubdocument;
  hasRequiredArraySubdocument = 1;
  const EventEmitter = require$$0.EventEmitter;
  const Subdocument = requireSubdocument$1();
  const utils2 = requireUtils();
  const documentArrayParent = requireSymbols$1().documentArrayParent;
  function ArraySubdocument(obj, parentArr, skipId, fields, index) {
    if (utils2.isMongooseDocumentArray(parentArr)) {
      this.__parentArray = parentArr;
      this[documentArrayParent] = parentArr.$parent();
    } else {
      this.__parentArray = void 0;
      this[documentArrayParent] = void 0;
    }
    this.$setIndex(index);
    this.$__parent = this[documentArrayParent];
    let options2;
    if (typeof skipId === "object" && skipId != null) {
      options2 = { isNew: true, ...skipId };
      skipId = void 0;
    } else {
      options2 = { isNew: true };
    }
    Subdocument.call(this, obj, fields, this[documentArrayParent], options2);
  }
  ArraySubdocument.prototype = Object.create(Subdocument.prototype);
  ArraySubdocument.prototype.constructor = ArraySubdocument;
  Object.defineProperty(ArraySubdocument.prototype, "$isSingleNested", {
    configurable: false,
    writable: false,
    value: false
  });
  Object.defineProperty(ArraySubdocument.prototype, "$isDocumentArrayElement", {
    configurable: false,
    writable: false,
    value: true
  });
  for (const i in EventEmitter.prototype) {
    ArraySubdocument[i] = EventEmitter.prototype[i];
  }
  ArraySubdocument.prototype.$setIndex = function(index) {
    this.__index = index;
    if (this.$__?.validationError != null) {
      const keys = Object.keys(this.$__.validationError.errors);
      for (const key of keys) {
        this.invalidate(key, this.$__.validationError.errors[key]);
      }
    }
  };
  ArraySubdocument.prototype.populate = function() {
    throw new Error('Mongoose does not support calling populate() on nested docs. Instead of `doc.arr[0].populate("path")`, use `doc.populate("arr.0.path")`');
  };
  ArraySubdocument.prototype.$__removeFromParent = function() {
    const _id = this._doc._id;
    if (!_id) {
      throw new Error("For your own good, Mongoose does not know how to remove an ArraySubdocument that has no _id");
    }
    this.__parentArray.pull({ _id });
  };
  ArraySubdocument.prototype.$__fullPath = function(path, skipIndex) {
    if (this.__index == null) {
      return null;
    }
    if (!this.$__.fullPath) {
      this.ownerDocument();
    }
    if (skipIndex) {
      return path ? this.$__.fullPath + "." + path : this.$__.fullPath;
    }
    return path ? this.$__.fullPath + "." + this.__index + "." + path : this.$__.fullPath + "." + this.__index;
  };
  ArraySubdocument.prototype.$__pathRelativeToParent = function(path, skipIndex) {
    if (this.__index == null || !this.__parentArray?.$path) {
      return null;
    }
    if (skipIndex) {
      return path == null ? this.__parentArray.$path() : this.__parentArray.$path() + "." + path;
    }
    if (path == null) {
      return this.__parentArray.$path() + "." + this.__index;
    }
    return this.__parentArray.$path() + "." + this.__index + "." + path;
  };
  ArraySubdocument.prototype.$parent = function() {
    return this[documentArrayParent];
  };
  ArraySubdocument.prototype.$__setParent = function $__setParent(parent) {
    this[documentArrayParent] = parent;
    this.$__parent = parent;
  };
  ArraySubdocument.prototype.parentArray = function() {
    return this.__parentArray;
  };
  arraySubdocument = ArraySubdocument;
  return arraySubdocument;
}
var methods_1$1;
var hasRequiredMethods$1;
function requireMethods$1() {
  if (hasRequiredMethods$1) return methods_1$1;
  hasRequiredMethods$1 = 1;
  const Document = requireDocument();
  const ArraySubdocument = requireArraySubdocument();
  const MongooseError = requireMongooseError();
  const cleanModifiedSubpaths2 = requireCleanModifiedSubpaths();
  const clone = requireClone();
  const internalToObjectOptions = requireOptions().internalToObjectOptions;
  const mpath = requireMpath();
  const utils2 = requireUtils();
  const isBsonType = requireIsBsonType();
  const arrayAtomicsBackupSymbol = requireSymbols$1().arrayAtomicsBackupSymbol;
  const arrayAtomicsSymbol = requireSymbols$1().arrayAtomicsSymbol;
  const arrayParentSymbol = requireSymbols$1().arrayParentSymbol;
  const arrayPathSymbol = requireSymbols$1().arrayPathSymbol;
  const arraySchemaSymbol = requireSymbols$1().arraySchemaSymbol;
  const populateModelSymbol = requireSymbols$1().populateModelSymbol;
  const slicedSymbol = /* @__PURE__ */ Symbol("mongoose#Array#sliced");
  const _basePush = Array.prototype.push;
  const methods = {
    /**
     * Depopulates stored atomic operation values as necessary for direct insertion to MongoDB.
     *
     * If no atomics exist, we return all array values after conversion.
     *
     * @return {Array}
     * @method $__getAtomics
     * @memberOf MongooseArray
     * @instance
     * @api private
     */
    $__getAtomics() {
      const ret = [];
      const keys = Object.keys(this[arrayAtomicsSymbol] || {});
      let i = keys.length;
      const opts = Object.assign({}, internalToObjectOptions, { _isNested: true });
      if (i === 0) {
        ret[0] = ["$set", this.toObject(opts)];
        return ret;
      }
      while (i--) {
        const op = keys[i];
        let val = this[arrayAtomicsSymbol][op];
        if (utils2.isMongooseObject(val)) {
          val = val.toObject(opts);
        } else if (Array.isArray(val)) {
          val = this.toObject.call(val, opts);
        } else if (Array.isArray(val?.$each)) {
          val.$each = this.toObject.call(val.$each, opts);
        } else if (typeof val?.valueOf === "function") {
          val = val.valueOf();
        }
        if (op === "$addToSet") {
          val = { $each: val };
        }
        ret.push([op, val]);
      }
      return ret;
    },
    /**
     * Public API for getting atomics. Alias for $__getAtomics() that can be
     * implemented by custom container types.
     *
     * @return {Array}
     * @method getAtomics
     * @memberOf MongooseArray
     * @instance
     * @api public
     */
    getAtomics() {
      return this.$__getAtomics();
    },
    /**
     * Clears all pending atomic operations. Called by Mongoose after save().
     *
     * @return {void}
     * @method clearAtomics
     * @memberOf MongooseArray
     * @instance
     * @api public
     */
    clearAtomics() {
      this[arrayAtomicsBackupSymbol] = this[arrayAtomicsSymbol];
      this[arrayAtomicsSymbol] = {};
    },
    /*!
     * ignore
     */
    $atomics() {
      return this[arrayAtomicsSymbol];
    },
    /*!
     * ignore
     */
    $parent() {
      return this[arrayParentSymbol];
    },
    /*!
     * ignore
     */
    $path() {
      return this[arrayPathSymbol];
    },
    /*!
     * ignore
     */
    $schemaType() {
      return this[arraySchemaSymbol];
    },
    /**
     * Atomically shifts the array at most one time per document `save()`.
     *
     * #### Note:
     *
     * _Calling this multiple times on an array before saving sends the same command as calling it once._
     * _This update is implemented using the MongoDB [$pop](https://www.mongodb.com/docs/manual/reference/operator/update/pop/) method which enforces this restriction._
     *
     *      doc.array = [1,2,3];
     *
     *      const shifted = doc.array.$shift();
     *      console.log(shifted); // 1
     *      console.log(doc.array); // [2,3]
     *
     *      // no affect
     *      shifted = doc.array.$shift();
     *      console.log(doc.array); // [2,3]
     *
     *      doc.save(function (err) {
     *        if (err) return handleError(err);
     *
     *        // we saved, now $shift works again
     *        shifted = doc.array.$shift();
     *        console.log(shifted ); // 2
     *        console.log(doc.array); // [3]
     *      })
     *
     * @api public
     * @memberOf MongooseArray
     * @instance
     * @method $shift
     * @see mongodb https://www.mongodb.com/docs/manual/reference/operator/update/pop/
     */
    $shift() {
      this._registerAtomic("$pop", -1);
      this._markModified();
      const __array = this.__array;
      if (__array._shifted) {
        return;
      }
      __array._shifted = true;
      return [].shift.call(__array);
    },
    /**
     * Pops the array atomically at most one time per document `save()`.
     *
     * #### NOTE:
     *
     * _Calling this multiple times on an array before saving sends the same command as calling it once._
     * _This update is implemented using the MongoDB [$pop](https://www.mongodb.com/docs/manual/reference/operator/update/pop/) method which enforces this restriction._
     *
     *      doc.array = [1,2,3];
     *
     *      const popped = doc.array.$pop();
     *      console.log(popped); // 3
     *      console.log(doc.array); // [1,2]
     *
     *      // no affect
     *      popped = doc.array.$pop();
     *      console.log(doc.array); // [1,2]
     *
     *      doc.save(function (err) {
     *        if (err) return handleError(err);
     *
     *        // we saved, now $pop works again
     *        popped = doc.array.$pop();
     *        console.log(popped); // 2
     *        console.log(doc.array); // [1]
     *      })
     *
     * @api public
     * @method $pop
     * @memberOf MongooseArray
     * @instance
     * @see mongodb https://www.mongodb.com/docs/manual/reference/operator/update/pop/
     * @method $pop
     * @memberOf MongooseArray
     */
    $pop() {
      this._registerAtomic("$pop", 1);
      this._markModified();
      if (this._popped) {
        return;
      }
      this._popped = true;
      return [].pop.call(this);
    },
    /*!
     * ignore
     */
    $schema() {
      return this[arraySchemaSymbol];
    },
    /**
     * Casts a member based on this arrays schema.
     *
     * @param {any} value
     * @return {any} the casted value
     * @method _cast
     * @api private
     * @memberOf MongooseArray
     */
    _cast(value) {
      let populated = false;
      let Model;
      const parent = this[arrayParentSymbol];
      if (parent) {
        populated = parent.$populated(this[arrayPathSymbol], true);
      }
      if (populated && value != null) {
        Model = populated.options[populateModelSymbol];
        if (Model == null) {
          throw new MongooseError("No populated model found for path `" + this[arrayPathSymbol] + "`. This is likely a bug in Mongoose, please report an issue on github.com/Automattic/mongoose.");
        }
        if (Buffer.isBuffer(value) || isBsonType(value, "ObjectId") || !utils2.isObject(value)) {
          value = { _id: value };
        }
        const isDisc = value.schema?.discriminatorMapping?.key !== void 0;
        if (!isDisc) {
          value = new Model(value);
        }
        return this[arraySchemaSymbol].embeddedSchemaType.applySetters(value, parent, true);
      }
      return this[arraySchemaSymbol].embeddedSchemaType.applySetters(value, parent, false);
    },
    /**
     * Internal helper for .map()
     *
     * @api private
     * @return {number}
     * @method _mapCast
     * @memberOf MongooseArray
     */
    _mapCast(val, index) {
      return this._cast(val, this.length + index);
    },
    /**
     * Marks this array as modified.
     *
     * If it bubbles up from an embedded document change, then it takes the following arguments (otherwise, takes 0 arguments)
     *
     * @param {ArraySubdocument} subdoc the embedded doc that invoked this method on the Array
     * @param {string} embeddedPath the path which changed in the subdoc
     * @method _markModified
     * @api private
     * @memberOf MongooseArray
     */
    _markModified(elem) {
      const parent = this[arrayParentSymbol];
      let dirtyPath;
      if (parent) {
        dirtyPath = this[arrayPathSymbol];
        if (arguments.length) {
          dirtyPath = dirtyPath + "." + elem;
        }
        if (dirtyPath != null && dirtyPath.endsWith(".$")) {
          return this;
        }
        parent.markModified(dirtyPath, arguments.length !== 0 ? elem : parent);
      }
      return this;
    },
    /**
     * Register an atomic operation with the parent.
     *
     * @param {Array} op operation
     * @param {any} val
     * @method _registerAtomic
     * @api private
     * @memberOf MongooseArray
     */
    _registerAtomic(op, val) {
      if (this[slicedSymbol]) {
        return;
      }
      if (op === "$set") {
        this[arrayAtomicsSymbol] = { $set: val };
        cleanModifiedSubpaths2(this[arrayParentSymbol], this[arrayPathSymbol]);
        this._markModified();
        return this;
      }
      const atomics = this[arrayAtomicsSymbol];
      if (op === "$pop" && !("$pop" in atomics)) {
        const _this = this;
        this[arrayParentSymbol].once("save", function() {
          _this._popped = _this._shifted = null;
        });
      }
      if (atomics.$set || utils2.hasOwnKeys(atomics) && !(op in atomics)) {
        this[arrayAtomicsSymbol] = { $set: this };
        return this;
      }
      let selector;
      if (op === "$pullAll" || op === "$addToSet") {
        atomics[op] || (atomics[op] = []);
        atomics[op] = atomics[op].concat(val);
      } else if (op === "$pullDocs") {
        const pullOp = atomics["$pull"] || (atomics["$pull"] = {});
        if (val[0] instanceof ArraySubdocument) {
          selector = pullOp["$or"] || (pullOp["$or"] = []);
          Array.prototype.push.apply(selector, val.map((v) => {
            return v.toObject({
              transform: (doc, ret) => {
                if (v == null || v.$__ == null) {
                  return ret;
                }
                Object.keys(v.$__.activePaths.getStatePaths("default")).forEach((path) => {
                  mpath.unset(path, ret);
                  _minimizePath(ret, path);
                });
                return ret;
              },
              virtuals: false
            });
          }));
        } else {
          selector = pullOp["_id"] || (pullOp["_id"] = { $in: [] });
          selector["$in"] = selector["$in"].concat(val);
        }
      } else if (op === "$push") {
        atomics.$push = atomics.$push || { $each: [] };
        if (val != null && utils2.hasUserDefinedProperty(val, "$each")) {
          atomics.$push = val;
        } else {
          if (val.length === 1) {
            atomics.$push.$each.push(val[0]);
          } else if (val.length < 1e4) {
            atomics.$push.$each.push(...val);
          } else {
            for (const v of val) {
              atomics.$push.$each.push(v);
            }
          }
        }
      } else {
        atomics[op] = val;
      }
      return this;
    },
    /**
     * Adds values to the array if not already present.
     *
     * #### Example:
     *
     *     console.log(doc.array) // [2,3,4]
     *     const added = doc.array.addToSet(4,5);
     *     console.log(doc.array) // [2,3,4,5]
     *     console.log(added)     // [5]
     *
     * @param {...any} [args]
     * @return {Array} the values that were added
     * @memberOf MongooseArray
     * @api public
     * @method addToSet
     */
    addToSet() {
      _checkManualPopulation(this, arguments);
      _depopulateIfNecessary(this, arguments);
      const values = [].map.call(arguments, this._mapCast, this);
      const added = [];
      let type2 = "";
      if (values[0] instanceof ArraySubdocument) {
        type2 = "doc";
      } else if (values[0] instanceof Date) {
        type2 = "date";
      } else if (isBsonType(values[0], "ObjectId")) {
        type2 = "ObjectId";
      }
      const rawValues = utils2.isMongooseArray(values) ? values.__array : values;
      const rawArray = utils2.isMongooseArray(this) ? this.__array : this;
      rawValues.forEach(function(v) {
        let found;
        const val = +v;
        switch (type2) {
          case "doc":
            found = this.some(function(doc) {
              return doc.equals(v);
            });
            break;
          case "date":
            found = this.some(function(d) {
              return +d === val;
            });
            break;
          case "ObjectId":
            found = this.find((o) => o.toString() === v.toString());
            break;
          default:
            found = ~this.indexOf(v);
            break;
        }
        if (!found) {
          this._markModified();
          rawArray.push(v);
          this._registerAtomic("$addToSet", v);
          [].push.call(added, v);
        }
      }, this);
      return added;
    },
    /**
     * Returns the number of pending atomic operations to send to the db for this array.
     *
     * @api private
     * @return {number}
     * @method hasAtomics
     * @memberOf MongooseArray
     */
    hasAtomics() {
      if (!utils2.isPOJO(this[arrayAtomicsSymbol])) {
        return 0;
      }
      return Object.keys(this[arrayAtomicsSymbol]).length;
    },
    /**
     * Return whether or not the `obj` is included in the array.
     *
     * @param {object} obj the item to check
     * @param {number} fromIndex
     * @return {boolean}
     * @api public
     * @method includes
     * @memberOf MongooseArray
     */
    includes(obj, fromIndex) {
      const ret = this.indexOf(obj, fromIndex);
      return ret !== -1;
    },
    /**
     * Return the index of `obj` or `-1` if not found.
     *
     * @param {object} obj the item to look for
     * @param {number} fromIndex
     * @return {number}
     * @api public
     * @method indexOf
     * @memberOf MongooseArray
     */
    indexOf(obj, fromIndex) {
      if (isBsonType(obj, "ObjectId")) {
        obj = obj.toString();
      }
      fromIndex = fromIndex == null ? 0 : fromIndex;
      const len = this.length;
      for (let i = fromIndex; i < len; ++i) {
        if (obj == this[i]) {
          return i;
        }
      }
      return -1;
    },
    /**
     * Helper for console.log
     *
     * @api public
     * @method inspect
     * @memberOf MongooseArray
     */
    inspect() {
      return JSON.stringify(this);
    },
    /**
     * Pushes items to the array non-atomically.
     *
     * #### Note:
     *
     * _marks the entire array as modified, which if saved, will store it as a `$set` operation, potentially overwriting any changes that happen between when you retrieved the object and when you save it._
     *
     * @param {...any} [args]
     * @api public
     * @method nonAtomicPush
     * @memberOf MongooseArray
     */
    nonAtomicPush() {
      const values = [].map.call(arguments, this._mapCast, this);
      this._markModified();
      const ret = [].push.apply(this, values);
      this._registerAtomic("$set", this);
      return ret;
    },
    /**
     * Wraps [`Array#pop`](https://developer.mozilla.org/en/JavaScript/Reference/Global_Objects/Array/pop) with proper change tracking.
     *
     * #### Note:
     *
     * _marks the entire array as modified which will pass the entire thing to $set potentially overwriting any changes that happen between when you retrieved the object and when you save it._
     *
     * @see MongooseArray#$pop https://mongoosejs.com/docs/api/array.html#MongooseArray.prototype.$pop()
     * @api public
     * @method pop
     * @memberOf MongooseArray
     */
    pop() {
      this._markModified();
      const ret = [].pop.call(this);
      this._registerAtomic("$set", this);
      return ret;
    },
    /**
     * Pulls items from the array atomically. Equality is determined by casting
     * the provided value to an embedded document and comparing using
     * [the `Document.equals()` function.](https://mongoosejs.com/docs/api/document.html#Document.prototype.equals())
     *
     * #### Example:
     *
     *     doc.array.pull(ObjectId)
     *     doc.array.pull({ _id: 'someId' })
     *     doc.array.pull(36)
     *     doc.array.pull('tag 1', 'tag 2')
     *
     * To remove a document from a subdocument array we may pass an object with a matching `_id`.
     *
     *     doc.subdocs.push({ _id: 4815162342 })
     *     doc.subdocs.pull({ _id: 4815162342 }) // removed
     *
     * Or we may passing the _id directly and let mongoose take care of it.
     *
     *     doc.subdocs.push({ _id: 4815162342 })
     *     doc.subdocs.pull(4815162342); // works
     *
     * The first pull call will result in a atomic operation on the database, if pull is called repeatedly without saving the document, a $set operation is used on the complete array instead, overwriting possible changes that happened on the database in the meantime.
     *
     * @param {...any} [args]
     * @see mongodb https://www.mongodb.com/docs/manual/reference/operator/update/pull/
     * @api public
     * @method pull
     * @memberOf MongooseArray
     */
    pull() {
      const values = [].map.call(arguments, (v, i2) => this._cast(v, i2, { defaults: false }), this);
      let cur = this;
      if (utils2.isMongooseArray(cur)) {
        cur = cur.__array;
      }
      let i = cur.length;
      let mem;
      this._markModified();
      while (i--) {
        mem = cur[i];
        if (mem instanceof Document) {
          const some = values.some(function(v) {
            return mem.equals(v);
          });
          if (some) {
            cur.splice(i, 1);
          }
        } else if (~this.indexOf.call(values, mem)) {
          cur.splice(i, 1);
        }
      }
      if (values[0] instanceof ArraySubdocument) {
        this._registerAtomic("$pullDocs", values.map(function(v) {
          const _id = v.$__getValue("_id");
          if (_id === void 0 || v.$isDefault("_id")) {
            return v;
          }
          return _id;
        }));
      } else {
        this._registerAtomic("$pullAll", values);
      }
      if (cleanModifiedSubpaths2(this[arrayParentSymbol], this[arrayPathSymbol]) > 0) {
        this._registerAtomic("$set", this);
      }
      return this;
    },
    /**
     * Wraps [`Array#push`](https://developer.mozilla.org/en/JavaScript/Reference/Global_Objects/Array/push) with proper change tracking.
     *
     * #### Example:
     *
     *     const schema = Schema({ nums: [Number] });
     *     const Model = mongoose.model('Test', schema);
     *
     *     const doc = await Model.create({ nums: [3, 4] });
     *     doc.nums.push(5); // Add 5 to the end of the array
     *     await doc.save();
     *
     *     // You can also pass an object with `$each` as the
     *     // first parameter to use MongoDB's `$position`
     *     doc.nums.push({
     *       $each: [1, 2],
     *       $position: 0
     *     });
     *     doc.nums; // [1, 2, 3, 4, 5]
     *
     * @param {...object} [args]
     * @api public
     * @method push
     * @memberOf MongooseArray
     */
    push() {
      let values = arguments;
      let atomic = values;
      const isOverwrite = values[0] != null && utils2.hasUserDefinedProperty(values[0], "$each");
      const arr = utils2.isMongooseArray(this) ? this.__array : this;
      if (isOverwrite) {
        atomic = values[0];
        values = values[0].$each;
      }
      if (this[arraySchemaSymbol] == null) {
        return _basePush.apply(this, values);
      }
      _checkManualPopulation(this, values);
      _depopulateIfNecessary(this, values);
      values = [].map.call(values, this._mapCast, this);
      let ret;
      const atomics = this[arrayAtomicsSymbol];
      this._markModified();
      if (isOverwrite) {
        atomic.$each = values;
        if ((atomics.$push && atomics.$push.$each && atomics.$push.$each.length || 0) !== 0 && atomics.$push.$position != atomic.$position) {
          if (atomic.$position != null) {
            [].splice.apply(arr, [atomic.$position, 0].concat(values));
            ret = arr.length;
          } else {
            ret = [].push.apply(arr, values);
          }
          this._registerAtomic("$set", this);
        } else if (atomic.$position != null) {
          [].splice.apply(arr, [atomic.$position, 0].concat(values));
          ret = this.length;
        } else {
          ret = [].push.apply(arr, values);
        }
      } else {
        atomic = values;
        ret = _basePush.apply(arr, values);
      }
      this._registerAtomic("$push", atomic);
      return ret;
    },
    /**
     * Alias of [pull](https://mongoosejs.com/docs/api/array.html#MongooseArray.prototype.pull())
     *
     * @see MongooseArray#pull https://mongoosejs.com/docs/api/array.html#MongooseArray.prototype.pull()
     * @see mongodb https://www.mongodb.com/docs/manual/reference/operator/update/pull/
     * @api public
     * @memberOf MongooseArray
     * @instance
     * @method remove
     */
    remove() {
      return this.pull.apply(this, arguments);
    },
    /**
     * Sets the casted `val` at index `i` and marks the array modified.
     *
     * #### Example:
     *
     *     // given documents based on the following
     *     const Doc = mongoose.model('Doc', new Schema({ array: [Number] }));
     *
     *     const doc = new Doc({ array: [2,3,4] })
     *
     *     console.log(doc.array) // [2,3,4]
     *
     *     doc.array.set(1,"5");
     *     console.log(doc.array); // [2,5,4] // properly cast to number
     *     doc.save() // the change is saved
     *
     *     // VS not using array#set
     *     doc.array[1] = "5";
     *     console.log(doc.array); // [2,"5",4] // no casting
     *     doc.save() // change is not saved
     *
     * @return {Array} this
     * @api public
     * @method set
     * @memberOf MongooseArray
     */
    set(i, val, skipModified) {
      const arr = this.__array;
      if (skipModified) {
        arr[i] = val;
        return this;
      }
      const value = methods._cast.call(this, val, i);
      methods._markModified.call(this, i);
      arr[i] = value;
      return this;
    },
    /**
     * Wraps [`Array#shift`](https://developer.mozilla.org/en/JavaScript/Reference/Global_Objects/Array/unshift) with proper change tracking.
     *
     * #### Example:
     *
     *     doc.array = [2,3];
     *     const res = doc.array.shift();
     *     console.log(res) // 2
     *     console.log(doc.array) // [3]
     *
     * #### Note:
     *
     * _marks the entire array as modified, which if saved, will store it as a `$set` operation, potentially overwriting any changes that happen between when you retrieved the object and when you save it._
     *
     * @api public
     * @method shift
     * @memberOf MongooseArray
     */
    shift() {
      const arr = utils2.isMongooseArray(this) ? this.__array : this;
      this._markModified();
      const ret = [].shift.call(arr);
      this._registerAtomic("$set", this);
      return ret;
    },
    /**
     * Wraps [`Array#sort`](https://developer.mozilla.org/en/JavaScript/Reference/Global_Objects/Array/sort) with proper change tracking.
     *
     * #### Note:
     *
     * _marks the entire array as modified, which if saved, will store it as a `$set` operation, potentially overwriting any changes that happen between when you retrieved the object and when you save it._
     *
     * @api public
     * @method sort
     * @memberOf MongooseArray
     * @see MasteringJS: Array sort https://masteringjs.io/tutorials/fundamentals/array-sort
     */
    sort() {
      const arr = utils2.isMongooseArray(this) ? this.__array : this;
      const ret = [].sort.apply(arr, arguments);
      this._registerAtomic("$set", this);
      return ret;
    },
    /**
     * Wraps [`Array#splice`](https://developer.mozilla.org/en/JavaScript/Reference/Global_Objects/Array/splice) with proper change tracking and casting.
     *
     * #### Note:
     *
     * _marks the entire array as modified, which if saved, will store it as a `$set` operation, potentially overwriting any changes that happen between when you retrieved the object and when you save it._
     *
     * @api public
     * @method splice
     * @memberOf MongooseArray
     * @see MasteringJS: Array splice https://masteringjs.io/tutorials/fundamentals/array-splice
     */
    splice() {
      let ret;
      const arr = utils2.isMongooseArray(this) ? this.__array : this;
      this._markModified();
      _checkManualPopulation(this, Array.prototype.slice.call(arguments, 2));
      if (arguments.length) {
        let vals;
        if (this[arraySchemaSymbol] == null) {
          vals = arguments;
        } else {
          vals = [];
          for (let i = 0; i < arguments.length; ++i) {
            vals[i] = i < 2 ? arguments[i] : this._cast(arguments[i], arguments[0] + (i - 2));
          }
        }
        ret = [].splice.apply(arr, vals);
        this._registerAtomic("$set", this);
      }
      return ret;
    },
    /*!
     * ignore
     */
    toBSON() {
      return this.toObject(internalToObjectOptions);
    },
    /**
     * Returns a native js Array.
     *
     * @param {object} options
     * @return {Array}
     * @api public
     * @method toObject
     * @memberOf MongooseArray
     */
    toObject(options2) {
      const arr = utils2.isMongooseArray(this) ? this.__array : this;
      if (options2?.depopulate) {
        options2 = clone(options2);
        options2._isNested = true;
        return [].concat(arr).map(function(doc) {
          return doc instanceof Document ? doc.toObject(options2) : doc;
        });
      }
      return [].concat(arr);
    },
    $toObject() {
      return this.constructor.prototype.toObject.apply(this, arguments);
    },
    /**
     * Wraps [`Array#unshift`](https://developer.mozilla.org/en/JavaScript/Reference/Global_Objects/Array/unshift) with proper change tracking.
     *
     * #### Note:
     *
     * _marks the entire array as modified, which if saved, will store it as a `$set` operation, potentially overwriting any changes that happen between when you retrieved the object and when you save it._
     *
     * @api public
     * @method unshift
     * @memberOf MongooseArray
     */
    unshift() {
      _checkManualPopulation(this, arguments);
      let values;
      if (this[arraySchemaSymbol] == null) {
        values = arguments;
      } else {
        values = [].map.call(arguments, this._cast, this);
      }
      const arr = utils2.isMongooseArray(this) ? this.__array : this;
      this._markModified();
      [].unshift.apply(arr, values);
      this._registerAtomic("$set", this);
      return this.length;
    }
  };
  function _isAllSubdocs(docs, ref) {
    if (!ref) {
      return false;
    }
    for (const arg of docs) {
      if (arg == null) {
        return false;
      }
      const model2 = arg.constructor;
      if (!(arg instanceof Document) || model2.modelName !== ref && model2.baseModelName !== ref) {
        return false;
      }
    }
    return true;
  }
  function _minimizePath(obj, parts, i) {
    if (typeof parts === "string") {
      if (parts.indexOf(".") === -1) {
        return;
      }
      parts = mpath.stringToParts(parts);
    }
    i = i || 0;
    if (i >= parts.length) {
      return;
    }
    if (obj == null || typeof obj !== "object") {
      return;
    }
    _minimizePath(obj[parts[0]], parts, i + 1);
    if (obj[parts[0]] != null && typeof obj[parts[0]] === "object" && utils2.hasOwnKeys(obj[parts[0]]) === false) {
      delete obj[parts[0]];
    }
  }
  function _checkManualPopulation(arr, docs) {
    const ref = arr == null ? null : arr[arraySchemaSymbol]?.embeddedSchemaType?.options?.ref || null;
    if (arr.length === 0 && docs.length !== 0) {
      if (_isAllSubdocs(docs, ref)) {
        arr[arrayParentSymbol].$populated(arr[arrayPathSymbol], [], {
          [populateModelSymbol]: docs[0].constructor
        });
      }
    }
  }
  function _depopulateIfNecessary(arr, docs) {
    const ref = arr == null ? null : arr[arraySchemaSymbol]?.embeddedSchemaType?.options?.ref || null;
    const parentDoc = arr[arrayParentSymbol];
    const path = arr[arrayPathSymbol];
    if (!ref || !parentDoc.populated(path)) {
      return;
    }
    for (const doc of docs) {
      if (doc == null) {
        continue;
      }
      if (typeof doc !== "object" || doc instanceof String || doc instanceof Number || doc instanceof Buffer || utils2.isMongooseType(doc)) {
        parentDoc.depopulate(path);
        break;
      }
    }
  }
  const returnVanillaArrayMethods = [
    "filter",
    "flat",
    "flatMap",
    "map",
    "slice"
  ];
  for (const method of returnVanillaArrayMethods) {
    if (Array.prototype[method] == null) {
      continue;
    }
    methods[method] = function() {
      const _arr = utils2.isMongooseArray(this) ? this.__array : this;
      const arr = [].concat(_arr);
      return arr[method].apply(arr, arguments);
    };
  }
  methods_1$1 = methods;
  return methods_1$1;
}
var methods_1;
var hasRequiredMethods;
function requireMethods() {
  if (hasRequiredMethods) return methods_1;
  hasRequiredMethods = 1;
  const ArrayMethods = requireMethods$1();
  const Document = requireDocument();
  const getDiscriminatorByValue2 = requireGetDiscriminatorByValue();
  const internalToObjectOptions = requireOptions().internalToObjectOptions;
  const utils2 = requireUtils();
  const isBsonType = requireIsBsonType();
  const arrayParentSymbol = requireSymbols$1().arrayParentSymbol;
  const arrayPathSymbol = requireSymbols$1().arrayPathSymbol;
  const arraySchemaSymbol = requireSymbols$1().arraySchemaSymbol;
  const documentArrayParent = requireSymbols$1().documentArrayParent;
  const _baseReverse = Array.prototype.reverse;
  const _baseToString = Array.prototype.toString;
  const methods = {
    /*!
     * ignore
     */
    toBSON() {
      return this.toObject(internalToObjectOptions);
    },
    toString() {
      return _baseToString.call(this.__array.map((subdoc) => {
        if (subdoc?.$__ != null) {
          return subdoc.toString();
        }
        return subdoc;
      }));
    },
    /*!
     * ignore
     */
    getArrayParent() {
      return this[arrayParentSymbol];
    },
    /*!
     * ignore
     */
    $schemaType() {
      return this[arraySchemaSymbol];
    },
    /**
     * Overrides MongooseArray#cast
     *
     * @method _cast
     * @api private
     * @memberOf MongooseDocumentArray
     */
    _cast(value, index, options2) {
      if (this[arraySchemaSymbol] == null) {
        return value;
      }
      let Constructor = this[arraySchemaSymbol].Constructor;
      const isInstance = Constructor.$isMongooseDocumentArray ? utils2.isMongooseDocumentArray(value) : value instanceof Constructor;
      if (isInstance || // Hack re: #5001, see #5005
      value?.constructor?.baseCasterConstructor === Constructor) {
        if (!(value[documentArrayParent] && value.__parentArray)) {
          value[documentArrayParent] = this[arrayParentSymbol];
          value.__parentArray = this;
        }
        value.$setIndex(index);
        return value;
      }
      if (value == null) {
        return null;
      }
      if (Buffer.isBuffer(value) || isBsonType(value, "ObjectId") || !utils2.isObject(value)) {
        value = { _id: value };
      }
      if (value && Constructor.discriminators && Constructor.schema && Constructor.schema.options && Constructor.schema.options.discriminatorKey) {
        if (typeof value[Constructor.schema.options.discriminatorKey] === "string" && Constructor.discriminators[value[Constructor.schema.options.discriminatorKey]]) {
          Constructor = Constructor.discriminators[value[Constructor.schema.options.discriminatorKey]];
        } else {
          const constructorByValue = getDiscriminatorByValue2(Constructor.discriminators, value[Constructor.schema.options.discriminatorKey]);
          if (constructorByValue) {
            Constructor = constructorByValue;
          }
        }
      }
      if (Constructor.$isMongooseDocumentArray) {
        return Constructor.cast(value, this, void 0, void 0, index);
      }
      const ret = new Constructor(value, this, options2, void 0, index);
      ret.isNew = true;
      return ret;
    },
    /**
     * Searches array items for the first document with a matching _id.
     *
     * #### Example:
     *
     *     const embeddedDoc = m.array.id(some_id);
     *
     * @return {EmbeddedDocument|null} the subdocument or null if not found.
     * @param {ObjectId|string|number|Buffer} id
     * @method id
     * @api public
     * @memberOf MongooseDocumentArray
     */
    id(id) {
      if (id == null) {
        return null;
      }
      const schemaType2 = this[arraySchemaSymbol];
      let idSchemaType = null;
      if (schemaType2?.schema) {
        idSchemaType = schemaType2.schema.path("_id");
      } else if (schemaType2?.casterConstructor?.schema) {
        idSchemaType = schemaType2.casterConstructor.schema.path("_id");
      }
      let castedId = null;
      if (idSchemaType) {
        try {
          castedId = idSchemaType.cast(id);
        } catch {
        }
      }
      let _id;
      for (const val of this) {
        if (!val) {
          continue;
        }
        _id = val.get("_id");
        if (_id == null) {
          continue;
        } else if (_id instanceof Document) {
          _id = _id.get("_id");
          if (castedId != null && utils2.deepEqual(castedId, _id)) {
            return val;
          } else if (castedId == null && (id == _id || utils2.deepEqual(id, _id))) {
            return val;
          }
        } else if (castedId != null && utils2.deepEqual(castedId, _id)) {
          return val;
        } else if (castedId == null && (_id == id || utils2.deepEqual(id, _id))) {
          return val;
        }
      }
      return null;
    },
    /**
     * Returns a native js Array of plain js objects
     *
     * #### Note:
     *
     * _Each sub-document is converted to a plain object by calling its `#toObject` method._
     *
     * @param {object} [options] optional options to pass to each documents `toObject` method call during conversion
     * @return {Array}
     * @method toObject
     * @api public
     * @memberOf MongooseDocumentArray
     */
    toObject(options2) {
      return [].concat(this.map(function(doc) {
        if (doc == null) {
          return null;
        }
        if (typeof doc.toObject !== "function") {
          return doc;
        }
        return doc.toObject(options2);
      }));
    },
    $toObject() {
      return this.constructor.prototype.toObject.apply(this, arguments);
    },
    /**
     * Wraps [`Array#push`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/push) with proper change tracking.
     *
     * @param {...object} [args]
     * @api public
     * @method push
     * @memberOf MongooseDocumentArray
     */
    push() {
      const shouldReindex = _shouldReindexAfterPush(arguments);
      const ret = ArrayMethods.push.apply(this, arguments);
      if (shouldReindex) {
        _updateSubdocIndexes(this);
      }
      _updateParentPopulated(this);
      return ret;
    },
    /**
     * Wraps [`Array#pop`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/pop) with proper change tracking.
     * @api private
     */
    pop() {
      const ret = ArrayMethods.pop.apply(this, arguments);
      _updateParentPopulated(this);
      return ret;
    },
    /*!
     * ignore
     */
    $pop() {
      const ret = ArrayMethods.$pop.apply(this, arguments);
      _updateParentPopulated(this);
      return ret;
    },
    /**
     * Pulls items from the array atomically.
     *
     * @param {...object} [args]
     * @api public
     * @method pull
     * @memberOf MongooseDocumentArray
     */
    pull() {
      const ret = ArrayMethods.pull.apply(this, arguments);
      _updateSubdocIndexes(this);
      _updateParentPopulated(this);
      return ret;
    },
    /**
     * Wraps [`Array#shift`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/shift) with proper change tracking.
     * @api private
     */
    shift() {
      const ret = ArrayMethods.shift.apply(this, arguments);
      _updateSubdocIndexes(this);
      _updateParentPopulated(this);
      return ret;
    },
    /*!
     * ignore
     */
    $shift() {
      const ret = ArrayMethods.$shift.apply(this, arguments);
      _updateSubdocIndexes(this);
      _updateParentPopulated(this);
      return ret;
    },
    /**
     * Wraps [`Array#splice`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/splice) with proper change tracking and casting.
     * @api private
     */
    splice() {
      const ret = ArrayMethods.splice.apply(this, arguments);
      _updateSubdocIndexes(this);
      _updateParentPopulated(this);
      return ret;
    },
    /**
     * Wraps [`Array#reverse`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/reverse)
     * with proper change tracking.
     * @api private
     */
    reverse() {
      _baseReverse.call(this.__array);
      this._registerAtomic("$set", this);
      _updateSubdocIndexes(this);
      _updateParentPopulated(this);
      return this;
    },
    /**
     * Wraps [`Array#sort`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/sort)
     * with proper change tracking.
     *
     * @param {Function} [compareFunction]
     * @api private
     */
    sort() {
      const ret = ArrayMethods.sort.apply(this, arguments);
      _updateSubdocIndexes(this);
      _updateParentPopulated(this);
      return ret;
    },
    /**
     * Wraps [`Array#unshift`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/unshift)
     * with proper change tracking.
     * @api private
     */
    unshift() {
      const ret = ArrayMethods.unshift.apply(this, arguments);
      _updateSubdocIndexes(this);
      _updateParentPopulated(this);
      return ret;
    },
    /**
     * Helper for console.log
     *
     * @method inspect
     * @api public
     * @memberOf MongooseDocumentArray
     */
    inspect() {
      return this.toObject();
    },
    /**
     * Creates a subdocument casted to this schema.
     *
     * This is the same subdocument constructor used for casting.
     *
     * @param {object} obj the value to cast to this arrays SubDocument schema
     * @method create
     * @api public
     * @memberOf MongooseDocumentArray
     */
    create(obj) {
      let Constructor = this[arraySchemaSymbol].Constructor;
      if (obj && Constructor.discriminators && Constructor.schema && Constructor.schema.options && Constructor.schema.options.discriminatorKey) {
        if (typeof obj[Constructor.schema.options.discriminatorKey] === "string" && Constructor.discriminators[obj[Constructor.schema.options.discriminatorKey]]) {
          Constructor = Constructor.discriminators[obj[Constructor.schema.options.discriminatorKey]];
        } else {
          const constructorByValue = getDiscriminatorByValue2(Constructor.discriminators, obj[Constructor.schema.options.discriminatorKey]);
          if (constructorByValue) {
            Constructor = constructorByValue;
          }
        }
      }
      return new Constructor(obj, this);
    },
    /*!
     * ignore
     */
    notify(event) {
      const _this = this;
      return function notify(val, _arr) {
        _arr = _arr || _this;
        let i = _arr.length;
        while (i--) {
          if (_arr[i] == null) {
            continue;
          }
          switch (event) {
            // only swap for save event for now, we may change this to all event types later
            case "save":
              val = _this[i];
              break;
          }
          if (utils2.isMongooseArray(_arr[i])) {
            notify(val, _arr[i]);
          } else if (_arr[i]) {
            _arr[i].emit(event, val);
          }
        }
      };
    },
    set(i, val, skipModified) {
      const arr = this.__array;
      if (skipModified) {
        arr[i] = val;
        return this;
      }
      const value = methods._cast.call(this, val, i);
      methods._markModified.call(this, i);
      arr[i] = value;
      return this;
    },
    _markModified(elem, embeddedPath) {
      const parent = this[arrayParentSymbol];
      let dirtyPath;
      if (parent) {
        dirtyPath = this[arrayPathSymbol];
        if (arguments.length) {
          if (embeddedPath != null) {
            const index = elem.__index;
            dirtyPath = dirtyPath + "." + index + "." + embeddedPath;
          } else {
            dirtyPath = dirtyPath + "." + elem;
          }
        }
        if (dirtyPath != null && dirtyPath.endsWith(".$")) {
          return this;
        }
        parent.markModified(dirtyPath, arguments.length !== 0 ? elem : parent);
      }
      return this;
    }
  };
  methods_1 = methods;
  function _updateSubdocIndexes(arr) {
    const rawArray = utils2.isMongooseArray(arr) ? arr.__array : arr;
    for (let i = 0; i < rawArray.length; ++i) {
      if (typeof rawArray[i]?.$setIndex === "function") {
        rawArray[i].$setIndex(i);
      }
    }
  }
  function _shouldReindexAfterPush(args) {
    return args[0] != null && utils2.hasUserDefinedProperty(args[0], "$each") && args[0].$position != null;
  }
  function _updateParentPopulated(arr) {
    const parent = arr[arrayParentSymbol];
    if (!parent || parent.$__.populated == null) return;
    const populatedPaths = Object.keys(parent.$__.populated).filter((p) => p.startsWith(arr[arrayPathSymbol] + "."));
    for (const path of populatedPaths) {
      const remnant = path.slice((arr[arrayPathSymbol] + ".").length);
      if (!Array.isArray(parent.$__.populated[path].value)) {
        continue;
      }
      parent.$__.populated[path].value = arr.map((val) => val.$populated(remnant));
    }
  }
  return methods_1;
}
var documentArray$1;
var hasRequiredDocumentArray$1;
function requireDocumentArray$1() {
  if (hasRequiredDocumentArray$1) return documentArray$1;
  hasRequiredDocumentArray$1 = 1;
  const ArrayMethods = requireMethods$1();
  const DocumentArrayMethods = requireMethods();
  const arrayAtomicsSymbol = requireSymbols$1().arrayAtomicsSymbol;
  const arrayAtomicsBackupSymbol = requireSymbols$1().arrayAtomicsBackupSymbol;
  const arrayParentSymbol = requireSymbols$1().arrayParentSymbol;
  const arrayPathSymbol = requireSymbols$1().arrayPathSymbol;
  const arraySchemaSymbol = requireSymbols$1().arraySchemaSymbol;
  const _basePush = Array.prototype.push;
  const numberRE = /^\d+$/;
  function MongooseDocumentArray(values, path, doc, schematype) {
    const __array = [];
    const internals = {
      [arrayAtomicsSymbol]: {},
      [arrayAtomicsBackupSymbol]: void 0,
      [arrayPathSymbol]: path,
      [arraySchemaSymbol]: void 0,
      [arrayParentSymbol]: void 0
    };
    if (Array.isArray(values)) {
      if (values[arrayPathSymbol] === path && values[arrayParentSymbol] === doc) {
        internals[arrayAtomicsSymbol] = Object.assign({}, values[arrayAtomicsSymbol]);
      }
      values.forEach((v) => {
        _basePush.call(__array, v);
      });
    }
    internals[arrayPathSymbol] = path;
    internals.__array = __array;
    if (doc?.$__) {
      internals[arrayParentSymbol] = doc;
      internals[arraySchemaSymbol] = doc.$__schema.path(path);
      while (internals[arraySchemaSymbol] != null && internals[arraySchemaSymbol].$isMongooseArray && !internals[arraySchemaSymbol].$isMongooseDocumentArray) {
        internals[arraySchemaSymbol] = internals[arraySchemaSymbol].embeddedSchemaType;
      }
    }
    const proxy = new Proxy(__array, {
      get: function(target, prop) {
        if (prop === "isMongooseArray" || prop === "isMongooseArrayProxy" || prop === "isMongooseDocumentArray" || prop === "isMongooseDocumentArrayProxy") {
          return true;
        }
        if (Object.hasOwn(internals, prop)) {
          return internals[prop];
        }
        if (Object.hasOwn(DocumentArrayMethods, prop)) {
          return DocumentArrayMethods[prop];
        }
        if (schematype?.virtuals && Object.hasOwn(schematype.virtuals, prop)) {
          return schematype.virtuals[prop].applyGetters(void 0, target);
        }
        if (Object.hasOwn(ArrayMethods, prop)) {
          return ArrayMethods[prop];
        }
        return __array[prop];
      },
      set: function(target, prop, value) {
        if (typeof prop === "string" && numberRE.test(prop)) {
          DocumentArrayMethods.set.call(proxy, prop, value, false);
        } else if (Object.hasOwn(internals, prop)) {
          internals[prop] = value;
        } else if (schematype?.virtuals && Object.hasOwn(schematype.virtuals, prop)) {
          schematype.virtuals[prop].applySetters(value, target);
        } else {
          __array[prop] = value;
        }
        return true;
      }
    });
    return proxy;
  }
  documentArray$1 = MongooseDocumentArray;
  return documentArray$1;
}
var hasRequiredDocument;
function requireDocument() {
  if (hasRequiredDocument) return document.exports;
  hasRequiredDocument = 1;
  (function(module, exports) {
    const DivergentArrayError = requireDivergentArray();
    const EventEmitter = require$$0.EventEmitter;
    const InternalCache = requireInternal();
    const MongooseBuffer = requireBuffer$1();
    const MongooseError = requireError();
    const MixedSchema = requireMixed();
    const ModifiedPathsSnapshot = requireModifiedPathsSnapshot();
    const ObjectExpectedError = requireObjectExpected();
    const ObjectParameterError = requireObjectParameter();
    const ParallelValidateError = requireParallelValidate();
    const PathTrie = requirePathTrie();
    const Schema = requireSchema();
    const StrictModeError = requireStrict();
    const ValidationError = requireValidation();
    const ValidatorError = requireValidator();
    const $__hasIncludedChildren = requireHasIncludedChildren();
    const applyDefaults2 = requireApplyDefaults();
    const cleanModifiedSubpaths2 = requireCleanModifiedSubpaths();
    const clone = requireClone();
    const isInPathsToSave2 = requireIsInPathsToSave();
    const compile2 = requireCompile().compile;
    const defineKey = requireCompile().defineKey;
    const firstKey2 = requireFirstKey();
    const flatten = requireCommon().flatten;
    const getEmbeddedDiscriminatorPath2 = requireGetEmbeddedDiscriminatorPath$1();
    const getKeysInSchemaOrder2 = requireGetKeysInSchemaOrder();
    const getSubdocumentStrictValue2 = requireGetSubdocumentStrictValue();
    const handleSpreadDoc2 = requireHandleSpreadDoc();
    const isDefiningProjection2 = requireIsDefiningProjection();
    const isExclusive2 = requireIsExclusive();
    const isPathExcluded2 = requireIsPathExcluded();
    const inspect = require$$1.inspect;
    const internalToObjectOptions = requireOptions().internalToObjectOptions;
    const markArraySubdocsPopulated2 = requireMarkArraySubdocsPopulated();
    const minimize = requireMinimize();
    const mpath = requireMpath();
    const parentPaths2 = requireParentPaths();
    const queryhelpers = requireQueryHelpers();
    const { buildMiddlewareFilter } = requireBuildMiddlewareFilter();
    const utils2 = requireUtils();
    const isPromise = requireIsPromise();
    const deepEqual = utils2.deepEqual;
    const isMongooseObject2 = utils2.isMongooseObject;
    const applyDefaultsBeforeSettersOptions = Object.freeze({ skipParentChangeTracking: true });
    const arrayAtomicsBackupSymbol = requireSymbols$1().arrayAtomicsBackupSymbol;
    const arrayAtomicsSymbol = requireSymbols$1().arrayAtomicsSymbol;
    const documentArrayParent = requireSymbols$1().documentArrayParent;
    const documentIsModified = requireSymbols$1().documentIsModified;
    const documentModifiedPaths = requireSymbols$1().documentModifiedPaths;
    const documentSchemaSymbol = requireSymbols$1().documentSchemaSymbol;
    const getSymbol = requireSymbols$1().getSymbol;
    const modelSymbol = requireSymbols$1().modelSymbol;
    const populateModelSymbol = requireSymbols$1().populateModelSymbol;
    const scopeSymbol = requireSymbols$1().scopeSymbol;
    const schemaMixedSymbol = requireSymbols().schemaMixedSymbol;
    const getDeepestSubdocumentForPath2 = requireGetDeepestSubdocumentForPath();
    const sessionNewDocuments = requireSymbols$1().sessionNewDocuments;
    let DocumentArray;
    let Embedded;
    const specialProperties2 = utils2.specialProperties;
    const VERSION_WHERE = 1;
    const VERSION_INC = 2;
    const VERSION_ALL = VERSION_WHERE | VERSION_INC;
    function Document(obj, fields, options2) {
      if (typeof options2 === "boolean") {
        throw new Error("The skipId parameter has been removed. Use { skipId: true } in the options parameter instead.");
      }
      options2 = options2 == null ? {} : Object.assign({}, options2);
      let skipId = options2.skipId;
      this.$__ = new InternalCache();
      if (this.$__schema == null) {
        const _schema = utils2.isObject(fields) && !fields.instanceOfSchema ? new Schema(fields) : fields;
        this.$__setSchema(_schema);
        fields = options2;
        skipId = options2.skipId;
      }
      if (options2.isNew != null && options2.isNew !== true) {
        this.$isNew = options2.isNew;
      }
      if (options2.priorDoc != null) {
        this.$__.priorDoc = options2.priorDoc;
      }
      if (skipId) {
        this.$__.skipId = skipId;
      }
      if (obj != null && typeof obj !== "object") {
        throw new ObjectParameterError(obj, "obj", "Document");
      }
      let defaults = true;
      if (options2.defaults !== void 0) {
        this.$__.defaults = options2.defaults;
        defaults = options2.defaults;
      }
      const schema2 = this.$__schema;
      if (typeof fields === "boolean" || fields === "throw") {
        if (fields !== true) {
          this.$__.strictMode = fields;
        }
        fields = void 0;
      } else if (options2.strict !== void 0) {
        this.$__.strictMode = options2.strict;
      } else if (schema2.options.strict !== true) {
        this.$__.strictMode = schema2.options.strict;
      }
      const requiredPaths = schema2.requiredPaths();
      for (const path of requiredPaths) {
        this.$__.activePaths.require(path);
      }
      let exclude = null;
      if (utils2.isPOJO(fields) && utils2.hasOwnKeys(fields)) {
        exclude = isExclusive2(fields);
        this.$__.selected = fields;
        this.$__.exclude = exclude;
      }
      const hasIncludedChildren2 = exclude === false && fields ? $__hasIncludedChildren(fields) : null;
      let hasPostSetterDefaults = true;
      if (this._doc == null) {
        this.$__buildDoc(obj, fields, skipId, exclude, hasIncludedChildren2, false);
        if (defaults) {
          hasPostSetterDefaults = applyDefaults2(this, fields, exclude, hasIncludedChildren2, true, null, applyDefaultsBeforeSettersOptions);
        }
      }
      if (obj) {
        if (this.$__original_set) {
          this.$__original_set(obj, void 0, true, options2);
        } else {
          this.$set(obj, void 0, true, options2);
        }
        if (obj.$__ != null && obj instanceof Document) {
          this.$isNew = obj.$isNew;
        }
      }
      if (options2.willInit && defaults) {
        if (options2.skipDefaults) {
          this.$__.skipDefaults = options2.skipDefaults;
        }
      } else if (defaults && hasPostSetterDefaults) {
        applyDefaults2(this, fields, exclude, hasIncludedChildren2, false, options2.skipDefaults);
      }
      if (!this.$__.strictMode && obj) {
        const _this = this;
        const keys = Object.keys(this._doc);
        keys.forEach(function(key) {
          if (!(key in schema2.tree) && !(key in schema2.methods) && !(key in schema2.virtuals) && !key.startsWith("$")) {
            defineKey({ prop: key, subprops: null, prototype: _this });
          }
        });
      }
      applyQueue(this);
    }
    Document.prototype.$isMongooseDocumentPrototype = true;
    Object.defineProperty(Document.prototype, "isNew", {
      get: function() {
        return this.$isNew;
      },
      set: function(value) {
        this.$isNew = value;
      }
    });
    Object.defineProperty(Document.prototype, "errors", {
      get: function() {
        return this.$errors;
      },
      set: function(value) {
        this.$errors = value;
      }
    });
    Document.prototype.$isNew = true;
    utils2.each(
      [
        "on",
        "once",
        "emit",
        "listeners",
        "removeListener",
        "setMaxListeners",
        "removeAllListeners",
        "addListener"
      ],
      function(emitterFn) {
        Document.prototype[emitterFn] = function() {
          if (!this.$__.emitter) {
            if (emitterFn === "emit") {
              return;
            }
            this.$__.emitter = new EventEmitter();
            this.$__.emitter.setMaxListeners(0);
          }
          return this.$__.emitter[emitterFn].apply(this.$__.emitter, arguments);
        };
        Document.prototype[`$${emitterFn}`] = Document.prototype[emitterFn];
      }
    );
    Document.prototype.constructor = Document;
    for (const i in EventEmitter.prototype) {
      Document[i] = EventEmitter.prototype[i];
    }
    Document.prototype.$__schema;
    Document.prototype.schema;
    Object.defineProperty(Document.prototype, "$locals", {
      configurable: false,
      enumerable: false,
      get: function() {
        if (this.$__.locals == null) {
          this.$__.locals = {};
        }
        return this.$__.locals;
      },
      set: function(v) {
        this.$__.locals = v;
      }
    });
    Document.prototype.isNew;
    Object.defineProperty(Document.prototype, "$where", {
      configurable: false,
      enumerable: false,
      writable: true
    });
    Document.prototype.id;
    Document.prototype.$errors;
    Object.defineProperty(Document.prototype, "$op", {
      get: function() {
        return this.$__.op || null;
      },
      set: function(value) {
        this.$__.op = value;
      }
    });
    function $applyDefaultsToNested(val, path, doc) {
      if (val == null) {
        return;
      }
      const paths = Object.keys(doc.$__schema.paths);
      const plen = paths.length;
      const pathPieces = path.indexOf(".") === -1 ? [path] : path.split(".");
      for (let i = 0; i < plen; ++i) {
        let curPath = "";
        const p = paths[i];
        if (!p.startsWith(path + ".")) {
          continue;
        }
        const type2 = doc.$__schema.paths[p];
        const pieces = type2.splitPath().slice(pathPieces.length);
        const len = pieces.length;
        if (type2.defaultValue === void 0) {
          continue;
        }
        let cur = val;
        for (let j = 0; j < len; ++j) {
          if (cur == null) {
            break;
          }
          const piece = pieces[j];
          if (j === len - 1) {
            if (cur[piece] !== void 0) {
              break;
            }
            try {
              const def = type2.getDefault(doc, false);
              if (def !== void 0) {
                cur[piece] = def;
              }
            } catch (err) {
              doc.invalidate(path + "." + curPath, err);
              break;
            }
            break;
          }
          curPath += (!curPath.length ? "" : ".") + piece;
          cur[piece] = cur[piece] || {};
          cur = cur[piece];
        }
      }
    }
    Document.prototype.$__buildDoc = function(obj, fields, skipId, exclude, hasIncludedChildren2) {
      if (!utils2.hasOwnKeys(this.$__schema.nested)) {
        this._doc = {};
        return;
      }
      const doc = {};
      const paths = Object.keys(this.$__schema.paths);
      const plen = paths.length;
      let ii = 0;
      for (; ii < plen; ++ii) {
        const p = paths[ii];
        if (p.includes("$*")) {
          continue;
        }
        const path = this.$__schema.paths[p].splitPath();
        const len = path.length;
        if (len === 1) {
          continue;
        }
        const last = len - 1;
        let curPath = "";
        let doc_ = doc;
        let included = false;
        for (let i = 0; i < len; ++i) {
          const piece = path[i];
          if (!curPath.length) {
            curPath = piece;
          } else {
            curPath += "." + piece;
          }
          if (exclude === true) {
            if (curPath in fields) {
              break;
            }
          } else if (exclude === false && fields && !included) {
            if (curPath in fields) {
              included = true;
            } else if (!hasIncludedChildren2[curPath]) {
              break;
            }
          }
          if (i < last) {
            doc_ = doc_[piece] || (doc_[piece] = {});
          }
        }
      }
      this._doc = doc;
    };
    Document.prototype.toBSON = function() {
      return this.toObject(internalToObjectOptions);
    };
    Document.prototype.init = function(doc, opts, fn) {
      if (typeof opts === "function") {
        fn = opts;
        opts = null;
      }
      if (doc == null) {
        throw new ObjectParameterError(doc, "doc", "init");
      }
      this.$__init(doc, opts);
      if (fn) {
        fn(null, this);
      }
      return this;
    };
    Document.prototype.$init = function() {
      return this.constructor.prototype.init.apply(this, arguments);
    };
    Document.prototype.$__init = function(doc, opts) {
      if (doc == null) {
        throw new ObjectParameterError(doc, "doc", "init");
      }
      this.$isNew = false;
      opts = opts || {};
      if (doc._id != null && opts.populated && opts.populated.length) {
        const id = String(doc._id);
        for (const item of opts.populated) {
          if (item.isVirtual) {
            this.$populated(item.path, utils2.getValue(item.path, doc), item);
          } else {
            this.$populated(item.path, item._docs[id], item);
          }
          if (item._childDocs == null) {
            continue;
          }
          for (const child of item._childDocs) {
            if (child?.$__ == null) {
              continue;
            }
            child.$__.parent = this;
          }
          item._childDocs = [];
        }
      }
      init(this, doc, this._doc, opts);
      markArraySubdocsPopulated2(this, opts.populated);
      this.$emit("init", this);
      this.constructor.emit("init", this);
      const hasIncludedChildren2 = this.$__.exclude === false && this.$__.selected ? $__hasIncludedChildren(this.$__.selected) : null;
      applyDefaults2(this, this.$__.selected, this.$__.exclude, hasIncludedChildren2, false, this.$__.skipDefaults);
      return this;
    };
    function init(self, obj, doc, opts, prefix) {
      prefix = prefix || "";
      if (typeof obj !== "object" || Array.isArray(obj)) {
        throw new ObjectExpectedError(self.$basePath, obj);
      }
      if (obj.$__ != null) {
        obj = obj._doc;
      }
      const keys = Object.keys(obj);
      const len = keys.length;
      let schemaType2;
      let path;
      let i;
      const strict2 = self.$__.strictMode;
      const docSchema = self.$__schema;
      const strictRead = docSchema.options.strictRead;
      for (let index = 0; index < len; ++index) {
        i = keys[index];
        if (specialProperties2.has(i)) {
          continue;
        }
        path = prefix ? prefix + i : i;
        schemaType2 = docSchema.path(path);
        if (docSchema.$isRootDiscriminator && !self.$__isSelected(path)) {
          continue;
        }
        const value = obj[i];
        if (!schemaType2 && strictRead && docSchema.pathType(path) === "adhocOrUndefined") {
          if (strictRead === "throw") {
            throw new StrictModeError(path, "Field `" + path + "` is not in schema and strictRead is set to throw.");
          } else if (strictRead === true) {
            continue;
          }
        }
        if (!schemaType2 && utils2.isPOJO(value)) {
          if (!doc[i]) {
            doc[i] = {};
            if (!strict2 && !(i in docSchema.tree) && !(i in docSchema.methods) && !(i in docSchema.virtuals)) {
              self[i] = doc[i];
            } else if (opts?.virtuals && i in docSchema.virtuals) {
              self[i] = doc[i];
            }
          }
          init(self, value, doc[i], opts, path + ".");
        } else if (!schemaType2) {
          if (strictRead === "throw") {
            throw new StrictModeError(path, "Field `" + path + "` is not in schema and strictRead is set to throw.");
          } else if (strictRead === true) {
            continue;
          }
          doc[i] = value;
          if (!strict2 && !prefix) {
            self[i] = value;
          } else if (opts?.virtuals && i in docSchema.virtuals) {
            self[i] = value;
          } else if (opts?.strict === "throw") {
            throw new StrictModeError(i);
          }
        } else {
          if (Object.hasOwn(doc, i) && value !== void 0 && !opts.hydratedPopulatedDocs) {
            delete doc[i];
          }
          if (value === null) {
            doc[i] = schemaType2._castNullish(null);
          } else if (value !== void 0) {
            const wasPopulated = value.$__ == null ? null : value.$__.wasPopulated;
            if (schemaType2 && !wasPopulated && !opts.hydratedPopulatedDocs) {
              try {
                if (opts?.setters) {
                  const overrideInit = false;
                  doc[i] = schemaType2.applySetters(value, self, overrideInit, null, opts);
                } else {
                  doc[i] = schemaType2.cast(value, self, true, void 0, opts);
                }
              } catch (e) {
                self.invalidate(e.path, new ValidatorError({
                  path: e.path,
                  message: e.message,
                  type: "cast",
                  value: e.value,
                  reason: e
                }));
              }
            } else if (schemaType2 && opts.hydratedPopulatedDocs) {
              doc[i] = schemaType2.cast(value, self, true, void 0, { hydratedPopulatedDocs: true });
              if (doc[i]?.$__?.wasPopulated) {
                self.$populated(path, doc[i].$__.wasPopulated.value, doc[i].$__.wasPopulated.options);
              } else if (Array.isArray(doc[i]) && doc[i].length && doc[i][0]?.$__?.wasPopulated) {
                self.$populated(path, doc[i].map((populatedDoc) => populatedDoc?.$__?.wasPopulated?.value).filter((val) => val != null), doc[i][0].$__.wasPopulated.options);
              }
            } else {
              doc[i] = value;
            }
          }
          if (!self.$isModified(path)) {
            self.$__.activePaths.init(path);
          }
        }
      }
    }
    Document.prototype.updateOne = function updateOne(update, options2) {
      const query2 = this.constructor.updateOne();
      const self = this;
      query2.pre(async function queryPreUpdateOne() {
        const res = await self._execDocumentPreHooks("updateOne", options2, [self, update, options2]);
        if (res[0] !== self || res[1] !== update || res[2] !== options2) {
          throw new Error("Document updateOne pre hooks cannot overwrite arguments");
        }
        query2.updateOne({ _id: self._doc._id }, update, options2);
        if (self.$where != null) {
          this.where(self.$where);
        }
        if (self.$session() != null) {
          if (!("session" in query2.options)) {
            query2.options.session = self.$session();
          }
        }
        return res;
      });
      query2.post(function queryPostUpdateOne() {
        return self._execDocumentPostHooks("updateOne", options2);
      });
      return query2;
    };
    Document.prototype.replaceOne = function replaceOne() {
      const args = [...arguments];
      args.unshift({ _id: this._doc._id });
      return this.constructor.replaceOne.apply(this.constructor, args);
    };
    Document.prototype.$session = function $session(session) {
      if (arguments.length === 0) {
        if (this.$__.session?.hasEnded) {
          this.$__.session = null;
          return null;
        }
        return this.$__.session;
      }
      if (session?.hasEnded) {
        throw new MongooseError("Cannot set a document's session to a session that has ended. Make sure you haven't called `endSession()` on the session you are passing to `$session()`.");
      }
      if (session == null && this.$__.session == null) {
        return;
      }
      this.$__.session = session;
      if (!this.$isSubdocument) {
        const subdocs = this.$getAllSubdocs();
        for (const child of subdocs) {
          child.$session(session);
        }
      }
      return session;
    };
    Document.prototype.$timestamps = function $timestamps(value) {
      if (arguments.length === 0) {
        if (this.$__.timestamps != null) {
          return this.$__.timestamps;
        }
        if (this.$__schema) {
          return this.$__schema.options.timestamps;
        }
        return void 0;
      }
      const currentValue = this.$timestamps();
      if (value !== currentValue) {
        this.$__.timestamps = value;
      }
      return this;
    };
    Document.prototype.overwrite = function overwrite(obj) {
      const keys = new Set(Object.keys(this._doc));
      for (const key of Object.keys(obj)) {
        keys.add(key);
      }
      const schemaOptions = this.$__schema.options;
      for (const key of keys) {
        if (key === "_id") {
          continue;
        }
        if (schemaOptions.versionKey && key === schemaOptions.versionKey) {
          continue;
        }
        if (schemaOptions.discriminatorKey && key === schemaOptions.discriminatorKey) {
          continue;
        }
        this.$set(key, obj[key]);
      }
      return this;
    };
    Document.prototype.$set = function $set(path, val, type2, options2) {
      if (utils2.isPOJO(type2)) {
        options2 = type2;
        type2 = void 0;
      }
      const merge2 = options2?.merge;
      const adhoc = type2 && type2 !== true;
      const constructing = type2 === true;
      let adhocs;
      let keys;
      let i = 0;
      let pathtype;
      let key;
      let prefix;
      const userSpecifiedStrict = options2 && "strict" in options2;
      let strict2 = userSpecifiedStrict ? options2.strict : this.$__.strictMode;
      if (adhoc) {
        adhocs = this.$__.adhocPaths || (this.$__.adhocPaths = {});
        adhocs[path] = this.$__schema.interpretAsType(path, type2, this.$__schema.options);
      }
      if (path == null) {
        [path, val] = [val, path];
      } else if (typeof path !== "string") {
        if (path instanceof Document) {
          if (path.$__isNested) {
            path = path.toObject();
          } else {
            path = path.$__schema === this.$__schema ? applyVirtuals(path, { ...path._doc }) : path._doc;
          }
        }
        if (path == null) {
          [path, val] = [val, path];
        }
        prefix = val ? val + "." : "";
        keys = getKeysInSchemaOrder2(this.$__schema, path);
        const len = keys.length;
        const _skipMinimizeTopLevel = options2?._skipMinimizeTopLevel || false;
        if (len === 0 && _skipMinimizeTopLevel) {
          delete options2._skipMinimizeTopLevel;
          if (val) {
            this.$set(val, {});
          }
          return this;
        }
        if (_skipMinimizeTopLevel) {
          options2 = Object.assign({}, options2, { _skipMinimizeTopLevel: false });
        }
        for (let i2 = 0; i2 < len; ++i2) {
          key = keys[i2];
          const pathName = prefix ? prefix + key : key;
          pathtype = this.$__schema.pathType(pathName);
          const valForKey = path[key];
          if (type2 === true && !prefix && valForKey != null && pathtype === "nested" && this._doc[key] != null) {
            delete this._doc[key];
          }
          if (utils2.isNonBuiltinObject(valForKey) && pathtype === "nested") {
            this.$set(pathName, valForKey, constructing, options2);
            $applyDefaultsToNested(this.$get(pathName), pathName, this);
            continue;
          } else if (strict2) {
            if (constructing && valForKey === void 0 && this.$get(pathName) !== void 0) {
              continue;
            }
            if (pathtype === "adhocOrUndefined") {
              pathtype = getEmbeddedDiscriminatorPath2(this, pathName, { typeOnly: true });
            }
            if (pathtype === "real" || pathtype === "virtual") {
              this.$set(pathName, valForKey, constructing, options2);
            } else if (pathtype === "nested" && valForKey instanceof Document) {
              this.$set(
                pathName,
                valForKey.toObject({ transform: false }),
                constructing,
                options2
              );
            } else if (strict2 === "throw") {
              if (pathtype === "nested") {
                throw new ObjectExpectedError(key, valForKey);
              } else {
                throw new StrictModeError(key);
              }
            } else if (pathtype === "nested" && valForKey == null) {
              this.$set(pathName, valForKey, constructing, options2);
            }
          } else {
            this.$set(pathName, valForKey, constructing, options2);
          }
        }
        const orderedDoc = {};
        const orderedKeys = Object.keys(this.$__schema.tree);
        for (let i2 = 0, len2 = orderedKeys.length; i2 < len2; ++i2) {
          (key = orderedKeys[i2]) && Object.hasOwn(this._doc, key) && (orderedDoc[key] = void 0);
        }
        this._doc = Object.assign(orderedDoc, this._doc);
        return this;
      }
      let pathType = this.$__schema.pathType(path);
      let parts = null;
      if (pathType === "adhocOrUndefined") {
        parts = path.indexOf(".") === -1 ? [path] : path.split(".");
        pathType = getEmbeddedDiscriminatorPath2(this, parts, { typeOnly: true });
      }
      if (pathType === "adhocOrUndefined" && !userSpecifiedStrict) {
        if (parts == null) {
          parts = path.indexOf(".") === -1 ? [path] : path.split(".");
        }
        const subdocStrict = getSubdocumentStrictValue2(this.$__schema, parts);
        if (subdocStrict !== void 0) {
          strict2 = subdocStrict;
        }
      }
      val = handleSpreadDoc2(val, true);
      const priorVal = this.$__.priorDoc != null ? this.$__.priorDoc.$__getValue(path) : constructing ? void 0 : this.$__getValue(path);
      if (pathType === "nested" && val) {
        if (typeof val === "object" && val != null) {
          if (val.$__ != null) {
            val = val.toObject(internalToObjectOptions);
          }
          if (val == null) {
            this.invalidate(path, new MongooseError.CastError("Object", val, path));
            return this;
          }
          const wasModified = this.$isModified(path);
          const hasInitialVal = this.$__.savedState != null && Object.hasOwn(this.$__.savedState, path);
          if (this.$__.savedState != null && !this.$isNew && !Object.hasOwn(this.$__.savedState, path)) {
            const initialVal = this.$__getValue(path);
            this.$__.savedState[path] = initialVal;
            const keys3 = Object.keys(initialVal || {});
            for (const key2 of keys3) {
              this.$__.savedState[path + "." + key2] = initialVal[key2];
            }
          }
          if (!merge2) {
            this.$__setValue(path, null);
            cleanModifiedSubpaths2(this, path);
          } else {
            return this.$set(val, path, constructing, options2);
          }
          const keys2 = getKeysInSchemaOrder2(this.$__schema, val, path);
          this.$__setValue(path, {});
          for (const key2 of keys2) {
            this.$set(path + "." + key2, val[key2], constructing, { ...options2, _skipMarkModified: true });
          }
          if (priorVal != null && (!wasModified || hasInitialVal) && utils2.deepEqual(hasInitialVal ? this.$__.savedState[path] : priorVal, val)) {
            this.unmarkModified(path);
          } else {
            this.markModified(path);
          }
          return this;
        }
        this.invalidate(path, new MongooseError.CastError("Object", val, path));
        return this;
      }
      let schema2;
      if (parts == null) {
        parts = path.indexOf(".") === -1 ? [path] : path.split(".");
      }
      if (typeof this.$__schema.aliases[parts[0]] === "string") {
        parts[0] = this.$__schema.aliases[parts[0]];
      }
      if (pathType === "adhocOrUndefined" && strict2) {
        let mixed2;
        for (i = 0; i < parts.length; ++i) {
          const subpath = parts.slice(0, i + 1).join(".");
          if (i + 1 < parts.length && this.$__schema.pathType(subpath) === "virtual") {
            mpath.set(path, val, this);
            return this;
          }
          schema2 = this.$__schema.path(subpath);
          if (schema2 == null) {
            continue;
          }
          if (schema2 instanceof MixedSchema) {
            mixed2 = true;
            break;
          } else if (schema2.$isSchemaMap && schema2.$__schemaType instanceof MixedSchema && i < parts.length - 1) {
            mixed2 = true;
            schema2 = schema2.$__schemaType;
            break;
          }
        }
        if (schema2 == null) {
          schema2 = getEmbeddedDiscriminatorPath2(this, path);
        }
        if (!mixed2 && !schema2) {
          if (strict2 === "throw") {
            throw new StrictModeError(path);
          }
          return this;
        }
      } else if (pathType === "virtual") {
        schema2 = this.$__schema.virtualpath(path);
        schema2.applySetters(val, this);
        return this;
      } else {
        schema2 = this.$__path(path);
      }
      let cur = this._doc;
      let curPath = "";
      for (i = 0; i < parts.length - 1; ++i) {
        cur = cur instanceof Map ? cur.get(parts[i]) : cur[parts[i]];
        curPath += (curPath.length !== 0 ? "." : "") + parts[i];
        if (!cur) {
          this.$set(curPath, {});
          if (!this.$__isSelected(curPath)) {
            this.unmarkModified(curPath);
          }
          cur = this.$__getValue(curPath);
        }
      }
      let pathToMark;
      if (parts.length <= 1) {
        pathToMark = path;
      } else {
        const len = parts.length;
        for (i = 0; i < len; ++i) {
          const subpath = parts.slice(0, i + 1).join(".");
          if (this.$get(subpath, null, { getters: false }) === null) {
            pathToMark = subpath;
            break;
          }
        }
        if (!pathToMark) {
          pathToMark = path;
        }
      }
      if (!schema2) {
        this.$__set(pathToMark, path, options2, constructing, parts, schema2, val, priorVal);
        if (pathType === "nested" && val == null) {
          cleanModifiedSubpaths2(this, path);
        }
        return this;
      }
      if (schema2.$isSingleNested || schema2.$isMongooseArray) {
        _markValidSubpaths(this, path);
      }
      if (val != null && merge2 && schema2.$isSingleNested) {
        if (val instanceof Document) {
          val = val.toObject({ virtuals: false, transform: false });
        }
        const keys2 = Object.keys(val);
        for (const key2 of keys2) {
          this.$set(path + "." + key2, val[key2], constructing, options2);
        }
        return this;
      }
      let shouldSet = true;
      try {
        const refMatches = !(val instanceof Document) ? false : (() => {
          if (schema2.options == null) {
            return false;
          }
          const model2 = val.constructor;
          const refOpt = typeof schema2.options.ref === "function" && !schema2.options.ref[modelSymbol] ? schema2.options.ref.call(this, this) : schema2.options.ref;
          const ref = refOpt?.modelName || refOpt;
          if (ref != null && (ref === model2.modelName || ref === model2.baseModelName)) {
            return true;
          }
          let refPath = schema2.options.refPath;
          if (refPath == null) {
            return false;
          }
          if (typeof refPath === "function" && !refPath[modelSymbol]) {
            let fullPath = path;
            const fullPathWithIndexes = this.$__fullPathWithIndexes?.();
            if (fullPathWithIndexes?.length) {
              fullPath = fullPathWithIndexes + "." + path;
            }
            refPath = refPath.call(this, this, fullPath);
          }
          if (typeof refPath !== "string") {
            throw new MongooseError("`refPath` must be a string or a function that returns a string, got " + inspect(refPath));
          }
          const modelName = this.ownerDocument().get(refPath);
          return modelName === model2.modelName || modelName === model2.baseModelName;
        })();
        let didPopulate = false;
        if (refMatches && val instanceof Document && (!val.$__.wasPopulated || utils2.deepEqual(val.$__.wasPopulated.value, val._doc._id))) {
          const unpopulatedValue = schema2?.$isSingleNested ? schema2.cast(val, this) : val._doc._id;
          this.$populated(path, unpopulatedValue, { [populateModelSymbol]: val.constructor });
          val.$__.wasPopulated = { value: unpopulatedValue };
          didPopulate = true;
        }
        let popOpts;
        const typeKey = this.$__schema.options.typeKey;
        if (schema2.options && Array.isArray(schema2.options[typeKey]) && schema2.options[typeKey].length && schema2.options[typeKey][0] && schema2.options[typeKey][0].ref && _isManuallyPopulatedArray(val, schema2.options[typeKey][0].ref)) {
          popOpts = { [populateModelSymbol]: val[0].constructor };
          this.$populated(path, val.map(function(v) {
            return v._doc._id;
          }), popOpts);
          for (const doc of val) {
            doc.$__.wasPopulated = { value: doc._doc._id };
          }
          didPopulate = true;
        }
        if (!refMatches || !schema2.$isSingleNested || !val.$__) {
          let setterContext = this;
          if (this.$__schema.singleNestedPaths[path] != null && parts.length > 1) {
            setterContext = getDeepestSubdocumentForPath2(this, parts, this.schema);
          }
          if (options2?.overwriteImmutable) {
            val = schema2.applySetters(val, setterContext, false, priorVal, { path, overwriteImmutable: true });
          } else {
            val = schema2.applySetters(val, setterContext, false, priorVal, { path });
          }
        }
        if (Array.isArray(val) && !Array.isArray(schema2) && schema2.$isMongooseDocumentArray && val.length !== 0 && val[0]?.$__?.populated != null) {
          const populatedPaths = Object.keys(val[0].$__.populated);
          for (const populatedPath of populatedPaths) {
            this.$populated(
              path + "." + populatedPath,
              val.map((v) => v.$populated(populatedPath)),
              val[0].$__.populated[populatedPath].options
            );
          }
          didPopulate = true;
        }
        if (!didPopulate && this.$__.populated) {
          if (Array.isArray(val) && this.$__.populated[path]) {
            for (let i2 = 0; i2 < val.length; ++i2) {
              if (val[i2] instanceof Document) {
                val.set(i2, val[i2]._doc._id, true);
              }
            }
          }
          delete this.$__.populated[path];
        }
        if (val != null && schema2.$isSingleNested) {
          _checkImmutableSubpaths(val, schema2, priorVal);
        }
        this.$markValid(path);
      } catch (e) {
        if (e instanceof MongooseError.StrictModeError && e.isImmutableError) {
          this.invalidate(path, e);
        } else if (e instanceof MongooseError.CastError) {
          this.invalidate(e.path, e);
          if (e.$originalErrorPath) {
            this.invalidate(
              path,
              new MongooseError.CastError(schema2.instance, val, path, e.$originalErrorPath)
            );
          }
        } else {
          this.invalidate(
            path,
            new MongooseError.CastError(schema2.instance, val, path, e)
          );
        }
        shouldSet = false;
      }
      if (shouldSet) {
        let savedState = null;
        let savedStatePath = null;
        if (!constructing) {
          const doc = this.$isSubdocument ? this.ownerDocument() : this;
          savedState = doc.$__.savedState;
          savedStatePath = this.$isSubdocument ? this.$__.fullPath + "." + path : path;
          doc.$__saveInitialState(savedStatePath);
        }
        this.$__set(pathToMark, path, options2, constructing, parts, schema2, val, priorVal);
        const isInTransaction = !!this.$__.session?.transaction;
        const isModifiedWithinTransaction = this.$__.session && this.$__.session[sessionNewDocuments] && this.$__.session[sessionNewDocuments].has(this) && this.$__.session[sessionNewDocuments].get(this).modifiedPaths && !this.$__.session[sessionNewDocuments].get(this).modifiedPaths.has(savedStatePath);
        if (savedState != null && Object.hasOwn(savedState, savedStatePath) && (!isInTransaction || isModifiedWithinTransaction) && utils2.deepEqual(val, savedState[savedStatePath])) {
          this.unmarkModified(path);
        }
      }
      if (schema2.$isSingleNested && (this.isDirectModified(path) || val == null)) {
        cleanModifiedSubpaths2(this, path);
      } else if (schema2.$isSchemaMap && val == null) {
        cleanModifiedSubpaths2(this, path);
      }
      return this;
    };
    function _isManuallyPopulatedArray(val, ref) {
      if (!Array.isArray(val)) {
        return false;
      }
      if (val.length === 0) {
        return false;
      }
      for (const el of val) {
        if (!(el instanceof Document)) {
          return false;
        }
        const modelName = el.constructor.modelName;
        if (modelName == null) {
          return false;
        }
        if (el.constructor.modelName != ref && el.constructor.baseModelName != ref) {
          return false;
        }
      }
      return true;
    }
    Document.prototype.set = Document.prototype.$set;
    Document.prototype.$__shouldModify = function(pathToMark, path, options2, constructing, parts, schema2, val, priorVal) {
      if (options2?._skipMarkModified) {
        return false;
      }
      if (this.$isNew) {
        return true;
      }
      if (path in this.$__.activePaths.getStatePaths("modify")) {
        return true;
      }
      if (val === void 0 && !this.$__isSelected(path)) {
        return true;
      }
      if (val === void 0 && path in this.$__.activePaths.getStatePaths("default")) {
        return false;
      }
      if (this.$populated(path) && val instanceof Document && deepEqual(val._doc._id, priorVal)) {
        return false;
      }
      if (!deepEqual(val, priorVal !== void 0 ? priorVal : utils2.getValue(path, this))) {
        return true;
      }
      if (!constructing && val !== null && val !== void 0 && path in this.$__.activePaths.getStatePaths("default") && deepEqual(val, schema2.getDefault(this, constructing))) {
        return true;
      }
      return false;
    };
    Document.prototype.$__set = function(pathToMark, path, options2, constructing, parts, schema2, val, priorVal) {
      Embedded = Embedded || requireArraySubdocument();
      const shouldModify = this.$isNew && !options2?._skipMarkModified ? true : this.$__shouldModify(pathToMark, path, options2, constructing, parts, schema2, val, priorVal);
      if (shouldModify) {
        if (this.$__.primitiveAtomics?.[path]) {
          delete this.$__.primitiveAtomics[path];
          if (utils2.hasOwnKeys(this.$__.primitiveAtomics) === false) {
            delete this.$__.primitiveAtomics;
          }
        }
        this.markModified(pathToMark);
        if (val && utils2.isMongooseArray(val)) {
          val._registerAtomic("$set", val);
          if (utils2.isMongooseDocumentArray(val)) {
            val.forEach(function(item) {
              item && item.__parentArray && (item.__parentArray = val);
            });
          }
        }
      } else if (Array.isArray(val) && Array.isArray(priorVal) && utils2.isMongooseArray(val) && utils2.isMongooseArray(priorVal)) {
        val[arrayAtomicsSymbol] = priorVal[arrayAtomicsSymbol];
        val[arrayAtomicsBackupSymbol] = priorVal[arrayAtomicsBackupSymbol];
        if (utils2.isMongooseDocumentArray(val)) {
          val.forEach((doc) => {
            if (doc != null) {
              doc.$isNew = false;
            }
          });
        }
      }
      let obj = this._doc;
      let i = 0;
      const l = parts.length;
      let cur = "";
      for (; i < l; i++) {
        const next = i + 1;
        const last = next === l;
        cur += cur ? "." + parts[i] : parts[i];
        if (specialProperties2.has(parts[i])) {
          continue;
        }
        if (last) {
          if (obj instanceof Map) {
            obj.set(parts[i], val);
          } else if (obj.$isSingleNested) {
            if (!(parts[i] in obj)) {
              obj[parts[i]] = val;
              obj._doc[parts[i]] = val;
            } else {
              obj._doc[parts[i]] = val;
            }
            if (shouldModify) {
              obj.markModified(parts[i]);
            }
          } else {
            obj[parts[i]] = val;
          }
        } else {
          const isMap = obj instanceof Map;
          let value = isMap ? obj.get(parts[i]) : obj[parts[i]];
          if (utils2.isPOJO(value)) {
            obj = value;
          } else if (value && value instanceof Embedded) {
            obj = value;
          } else if (value && !Array.isArray(value) && value.$isSingleNested) {
            obj = value;
          } else if (value && Array.isArray(value)) {
            obj = value;
          } else if (value == null) {
            value = {};
            if (isMap) {
              obj.set(parts[i], value);
            } else {
              obj[parts[i]] = value;
            }
            obj = value;
          } else {
            obj = value;
          }
        }
      }
    };
    Document.prototype.$__getValue = function(path) {
      if (typeof path !== "string" && !Array.isArray(path)) {
        throw new TypeError(
          `Invalid \`path\`. Must be either string or array. Got "${path}" (type ${typeof path})`
        );
      }
      return utils2.getValue(path, this._doc);
    };
    Document.prototype.$inc = function $inc(path, val) {
      if (val == null) {
        val = 1;
      }
      if (Array.isArray(path)) {
        path.forEach((p) => this.$inc(p, val));
        return this;
      }
      const schemaType2 = this.$__path(path);
      if (schemaType2 == null) {
        if (this.$__.strictMode === "throw") {
          throw new StrictModeError(path);
        } else if (this.$__.strictMode === true) {
          return this;
        }
      } else if (schemaType2.instance !== "Number") {
        this.invalidate(path, new MongooseError.CastError(schemaType2.instance, val, path));
        return this;
      }
      const currentValue = this.$__getValue(path) || 0;
      let shouldSet = false;
      let valToSet = null;
      let valToInc = val;
      try {
        val = schemaType2.cast(val);
        valToSet = schemaType2.applySetters(currentValue + val, this);
        valToInc = valToSet - currentValue;
        shouldSet = true;
      } catch (err) {
        this.invalidate(path, new MongooseError.CastError("number", val, path, err));
      }
      if (shouldSet) {
        this.$__.primitiveAtomics = this.$__.primitiveAtomics || {};
        if (this.$__.primitiveAtomics[path] == null) {
          this.$__.primitiveAtomics[path] = { $inc: valToInc };
        } else {
          this.$__.primitiveAtomics[path].$inc += valToInc;
        }
        this.markModified(path);
        this.$__setValue(path, valToSet);
      }
      return this;
    };
    Document.prototype.$__setValue = function $__setValue(path, val) {
      if (Array.isArray(path) && path.length === 1) {
        this._doc[path[0]] = val;
        return this;
      }
      utils2.setValue(path, val, this._doc);
      return this;
    };
    Document.prototype.get = function(path, type2, options2) {
      let adhoc;
      if (options2 == null) {
        options2 = {};
      }
      if (type2) {
        adhoc = this.$__schema.interpretAsType(path, type2, this.$__schema.options);
      }
      const noDottedPath = options2.noDottedPath;
      let schema2 = noDottedPath ? Object.hasOwn(this.$__schema.paths, path) ? this.$__schema.paths[path] : void 0 : this.$__path(path);
      if (schema2 == null) {
        schema2 = this.$__schema.virtualpath(path);
        if (schema2 != null) {
          return schema2.applyGetters(void 0, this);
        }
      }
      if (noDottedPath) {
        let obj2 = Object.hasOwn(this._doc, path) ? this._doc[path] : void 0;
        if (adhoc) {
          obj2 = adhoc.cast(obj2);
        }
        if (schema2 != null && options2.getters !== false) {
          return schema2.applyGetters(obj2, this);
        }
        return obj2;
      }
      if (schema2?.instance === "Mixed") {
        const virtual = this.$__schema.virtualpath(path);
        if (virtual != null) {
          schema2 = virtual;
        }
      }
      const hasDot = path.indexOf(".") !== -1;
      let obj = this._doc;
      const pieces = hasDot ? path.split(".") : [path];
      if (typeof this.$__schema.aliases[pieces[0]] === "string") {
        pieces[0] = this.$__schema.aliases[pieces[0]];
      }
      for (let i = 0, l = pieces.length; i < l; i++) {
        if (specialProperties2.has(pieces[i])) {
          return void 0;
        }
        if (obj && obj._doc) {
          obj = obj._doc;
        }
        if (obj == null) {
          obj = void 0;
        } else if (obj instanceof Map) {
          obj = obj.get(pieces[i], { getters: false });
        } else if (i === l - 1) {
          obj = utils2.getValue(pieces[i], obj);
        } else {
          obj = obj[pieces[i]];
        }
      }
      if (adhoc) {
        obj = adhoc.cast(obj);
      }
      if (schema2 != null && options2.getters !== false) {
        obj = schema2.applyGetters(obj, this);
      } else if (Object.hasOwn(this.$__schema.nested, path) && options2.virtuals) {
        return applyVirtuals(this, clone(obj) || {}, { path });
      }
      return obj;
    };
    Document.prototype[getSymbol] = Document.prototype.get;
    Document.prototype.$get = Document.prototype.get;
    Document.prototype.$__path = function(path) {
      const adhocs = this.$__.adhocPaths;
      const adhocType = adhocs && Object.hasOwn(adhocs, path) ? adhocs[path] : null;
      if (adhocType) {
        return adhocType;
      }
      return this.$__schema.path(path);
    };
    Document.prototype.markModified = function(path, scope) {
      this.$__saveInitialState(path);
      this.$__.activePaths.modify(path);
      if (scope != null && !this.$isSubdocument) {
        this.$__.pathsToScopes = this.$__pathsToScopes || {};
        this.$__.pathsToScopes[path] = scope;
      }
    };
    Document.prototype.$__saveInitialState = function $__saveInitialState(path) {
      const savedState = this.$__.savedState;
      const savedStatePath = path;
      if (savedState != null) {
        const firstDot = savedStatePath.indexOf(".");
        const topLevelPath = firstDot === -1 ? savedStatePath : savedStatePath.slice(0, firstDot);
        if (!Object.hasOwn(savedState, topLevelPath)) {
          savedState[topLevelPath] = clone(this.$__getValue(topLevelPath));
        }
      }
    };
    Document.prototype.unmarkModified = function(path) {
      this.$__.activePaths.init(path);
      if (this.$__.pathsToScopes != null) {
        delete this.$__.pathsToScopes[path];
      }
    };
    Document.prototype.$ignore = function(path) {
      this.$__.activePaths.ignore(path);
    };
    Document.prototype.directModifiedPaths = function() {
      return Object.keys(this.$__.activePaths.getStatePaths("modify"));
    };
    Document.prototype.$isEmpty = function(path) {
      const isEmptyOptions = {
        minimize: true,
        virtuals: false,
        getters: false,
        transform: false
      };
      if (arguments.length !== 0) {
        const v = this.$get(path);
        if (v == null) {
          return true;
        }
        if (typeof v !== "object") {
          return false;
        }
        if (utils2.isPOJO(v)) {
          return _isEmpty(v);
        }
        return Object.keys(v.toObject(isEmptyOptions)).length === 0;
      }
      return Object.keys(this.toObject(isEmptyOptions)).length === 0;
    };
    function _isEmpty(v) {
      if (v == null) {
        return true;
      }
      if (typeof v !== "object" || Array.isArray(v)) {
        return false;
      }
      for (const key of Object.keys(v)) {
        if (!_isEmpty(v[key])) {
          return false;
        }
      }
      return true;
    }
    Document.prototype.modifiedPaths = function(options2) {
      options2 = options2 || {};
      const directModifiedPaths = Object.keys(this.$__.activePaths.getStatePaths("modify"));
      const result = /* @__PURE__ */ new Set();
      let i = 0;
      let j = 0;
      const len = directModifiedPaths.length;
      for (i = 0; i < len; ++i) {
        const path = directModifiedPaths[i];
        const parts = parentPaths2(path);
        const pLen = parts.length;
        for (j = 0; j < pLen; ++j) {
          result.add(parts[j]);
        }
        if (!options2.includeChildren) {
          continue;
        }
        let ii = 0;
        let cur = this.$get(path);
        if (typeof cur === "object" && cur !== null) {
          if (cur._doc) {
            cur = cur._doc;
          }
          const len2 = cur.length;
          if (Array.isArray(cur)) {
            for (ii = 0; ii < len2; ++ii) {
              const subPath = path + "." + ii;
              if (!result.has(subPath)) {
                result.add(subPath);
                if (cur[ii] != null && cur[ii].$__) {
                  const modified = cur[ii].modifiedPaths();
                  let iii = 0;
                  const iiiLen = modified.length;
                  for (iii = 0; iii < iiiLen; ++iii) {
                    result.add(subPath + "." + modified[iii]);
                  }
                }
              }
            }
          } else {
            const keys = Object.keys(cur);
            let ii2 = 0;
            const len3 = keys.length;
            for (ii2 = 0; ii2 < len3; ++ii2) {
              result.add(path + "." + keys[ii2]);
            }
          }
        }
      }
      return Array.from(result);
    };
    Document.prototype[documentModifiedPaths] = Document.prototype.modifiedPaths;
    Document.prototype.isModified = function(paths, options2, modifiedPaths2) {
      if (paths) {
        const ignoreAtomics = options2?.ignoreAtomics;
        const directModifiedPathsObj = this.$__.activePaths.states.modify;
        if (directModifiedPathsObj == null) {
          return false;
        }
        if (typeof paths === "string") {
          paths = paths.indexOf(" ") === -1 ? [paths] : paths.split(" ");
        }
        for (const path of paths) {
          if (directModifiedPathsObj[path] != null) {
            return true;
          }
        }
        const modified = modifiedPaths2 || this[documentModifiedPaths]();
        const isModifiedChild = paths.some(function(path) {
          return !!~modified.indexOf(path);
        });
        let directModifiedPaths = Object.keys(directModifiedPathsObj);
        if (ignoreAtomics) {
          directModifiedPaths = directModifiedPaths.filter((path) => {
            const value = this.$__getValue(path);
            if (value?.[arrayAtomicsSymbol] != null && value[arrayAtomicsSymbol].$set === void 0) {
              return false;
            }
            return true;
          });
        }
        return isModifiedChild || paths.some(function(path) {
          return directModifiedPaths.some(function(mod) {
            return mod === path || path.startsWith(mod + ".");
          });
        });
      }
      return this.$__.activePaths.some("modify");
    };
    Document.prototype.$isModified = Document.prototype.isModified;
    Document.prototype[documentIsModified] = Document.prototype.isModified;
    Document.prototype.$isDefault = function(path) {
      if (path == null) {
        return this.$__.activePaths.some("default");
      }
      if (typeof path === "string" && path.indexOf(" ") === -1) {
        return Object.hasOwn(this.$__.activePaths.getStatePaths("default"), path);
      }
      let paths = path;
      if (!Array.isArray(paths)) {
        paths = paths.split(" ");
      }
      return paths.some((path2) => Object.hasOwn(this.$__.activePaths.getStatePaths("default"), path2));
    };
    Document.prototype.$isDeleted = function(val) {
      if (arguments.length === 0) {
        return !!this.$__.isDeleted;
      }
      this.$__.isDeleted = !!val;
      return this;
    };
    Document.prototype.isDirectModified = function(path) {
      if (path == null) {
        return this.$__.activePaths.some("modify");
      }
      if (typeof path === "string" && path.indexOf(" ") === -1) {
        const res = Object.hasOwn(this.$__.activePaths.getStatePaths("modify"), path);
        if (res || path.indexOf(".") === -1) {
          return res;
        }
        const pieces = path.split(".");
        for (let i = 0; i < pieces.length - 1; ++i) {
          const subpath = pieces.slice(0, i + 1).join(".");
          const subdoc = this.$get(subpath);
          if (subdoc?.$__ != null && subdoc.isDirectModified(pieces.slice(i + 1).join("."))) {
            return true;
          }
        }
        return false;
      }
      let paths = path;
      if (typeof paths === "string") {
        paths = paths.split(" ");
      }
      return paths.some((path2) => this.isDirectModified(path2));
    };
    Document.prototype.isInit = function(path) {
      if (path == null) {
        return this.$__.activePaths.some("init");
      }
      if (typeof path === "string" && path.indexOf(" ") === -1) {
        return Object.hasOwn(this.$__.activePaths.getStatePaths("init"), path);
      }
      let paths = path;
      if (!Array.isArray(paths)) {
        paths = paths.split(" ");
      }
      return paths.some((path2) => Object.hasOwn(this.$__.activePaths.getStatePaths("init"), path2));
    };
    Document.prototype.isSelected = function isSelected(path) {
      if (this.$__.selected == null) {
        return true;
      }
      if (!path) {
        return false;
      }
      if (path === "_id") {
        return this.$__.selected._id !== 0;
      }
      if (path.indexOf(" ") !== -1) {
        path = path.split(" ");
      }
      if (Array.isArray(path)) {
        return path.some((p) => this.$__isSelected(p));
      }
      const paths = Object.keys(this.$__.selected);
      let inclusive = null;
      if (paths.length === 1 && paths[0] === "_id") {
        return this.$__.selected._id === 0;
      }
      for (const cur of paths) {
        if (cur === "_id") {
          continue;
        }
        if (!isDefiningProjection2(this.$__.selected[cur])) {
          continue;
        }
        inclusive = !!this.$__.selected[cur];
        break;
      }
      if (inclusive === null) {
        return true;
      }
      if (path in this.$__.selected) {
        return inclusive;
      }
      const pathHasDot = path.indexOf(".") !== -1;
      for (const cur of paths) {
        if (cur === "_id") {
          continue;
        }
        if (cur.charAt(path.length) === "." && cur.slice(0, path.length) === path) {
          return inclusive || cur !== path + ".";
        }
        if (pathHasDot && path.charAt(cur.length) === "." && path.slice(0, cur.length) === cur) {
          return inclusive;
        }
      }
      return !inclusive;
    };
    Document.prototype.$__isSelected = Document.prototype.isSelected;
    Document.prototype.isDirectSelected = function isDirectSelected(path) {
      if (this.$__.selected == null) {
        return true;
      }
      if (path === "_id") {
        return this.$__.selected._id !== 0;
      }
      if (path.indexOf(" ") !== -1) {
        path = path.split(" ");
      }
      if (Array.isArray(path)) {
        return path.some((p) => this.isDirectSelected(p));
      }
      const paths = Object.keys(this.$__.selected);
      let inclusive = null;
      if (paths.length === 1 && paths[0] === "_id") {
        return this.$__.selected._id === 0;
      }
      for (const cur of paths) {
        if (cur === "_id") {
          continue;
        }
        if (!isDefiningProjection2(this.$__.selected[cur])) {
          continue;
        }
        inclusive = !!this.$__.selected[cur];
        break;
      }
      if (inclusive === null) {
        return true;
      }
      if (Object.hasOwn(this.$__.selected, path)) {
        return inclusive;
      }
      return !inclusive;
    };
    Document.prototype.validate = async function validate(pathsToValidate, options2) {
      if (typeof pathsToValidate === "function" || typeof options2 === "function" || typeof arguments[2] === "function") {
        throw new MongooseError("Document.prototype.validate() no longer accepts a callback");
      }
      this.$op = "validate";
      if (arguments.length === 1) {
        if (typeof arguments[0] === "object" && !Array.isArray(arguments[0])) {
          options2 = arguments[0];
          pathsToValidate = null;
        }
      }
      if (options2 && typeof options2.pathsToSkip === "string") {
        const isOnePathOnly = options2.pathsToSkip.indexOf(" ") === -1;
        options2.pathsToSkip = isOnePathOnly ? [options2.pathsToSkip] : options2.pathsToSkip.split(" ");
      }
      const _skipParallelValidateCheck = options2?._skipParallelValidateCheck;
      if (this.$isSubdocument != null) ;
      else if (this.$__.validating && !_skipParallelValidateCheck) {
        throw new ParallelValidateError(this);
      } else if (!_skipParallelValidateCheck) {
        this.$__.validating = true;
      }
      const hasValidateHooks = this.$__middleware.hasHooks("validate");
      try {
        try {
          if (hasValidateHooks) {
            [options2] = await this._execDocumentPreHooks("validate", options2, [options2]);
          } else if (!_skipParallelValidateCheck) {
            await Promise.resolve();
          }
        } catch (error3) {
          if (hasValidateHooks) {
            await this._execDocumentPostHooks("validate", options2, error3);
          }
          return;
        }
        if (this.$__.saveOptions && this.$__.saveOptions.pathsToSave && !pathsToValidate) {
          pathsToValidate = [...this.$__.saveOptions.pathsToSave];
        }
        const hasValidateModifiedOnlyOption = options2 && typeof options2 === "object" && "validateModifiedOnly" in options2;
        const pathsToSkip = options2?.pathsToSkip || null;
        let shouldValidateModifiedOnly;
        if (hasValidateModifiedOnlyOption) {
          shouldValidateModifiedOnly = !!options2.validateModifiedOnly;
        } else {
          shouldValidateModifiedOnly = this.$__schema.options.validateModifiedOnly;
        }
        this.$__.validateModifiedOnly = shouldValidateModifiedOnly;
        const validateAllPaths = options2?.validateAllPaths;
        if (validateAllPaths) {
          if (pathsToSkip) {
            throw new TypeError("Cannot set both `validateAllPaths` and `pathsToSkip`");
          }
          if (pathsToValidate) {
            throw new TypeError("Cannot set both `validateAllPaths` and `pathsToValidate`");
          }
          if (hasValidateModifiedOnlyOption && shouldValidateModifiedOnly) {
            throw new TypeError("Cannot set both `validateAllPaths` and `validateModifiedOnly`");
          }
        }
        const _this = this;
        let paths;
        let doValidateOptionsByPath;
        if (validateAllPaths) {
          paths = new Set(Object.keys(this.$__schema.paths));
          for (const path of paths) {
            const schemaType2 = this.$__schema.path(path);
            if (!schemaType2?.$isMongooseArray) {
              continue;
            }
            const val = this.$__getValue(path);
            if (!val) {
              continue;
            }
            _pushNestedArrayPaths(val, paths, path);
          }
          paths = [...paths];
          doValidateOptionsByPath = {};
        } else {
          const pathDetails = _getPathsToValidate(this, pathsToValidate, pathsToSkip, options2?._nestedValidate);
          paths = shouldValidateModifiedOnly ? pathDetails[0].filter((path) => this.$isModified(path)) : pathDetails[0];
          doValidateOptionsByPath = pathDetails[1];
        }
        if (typeof pathsToValidate === "string") {
          pathsToValidate = pathsToValidate.split(" ");
        }
        if (paths.length === 0) {
          const error3 = _completeValidate(this);
          if (hasValidateHooks) {
            await this._execDocumentPostHooks("validate", options2, error3);
          } else if (error3 != null) {
            throw error3;
          }
          return;
        }
        const validated = {};
        const pathsToSave = Array.isArray(this.$__.saveOptions?.pathsToSave) ? new Set(this.$__.saveOptions.pathsToSave) : null;
        const promises = [];
        for (let i = 0; i < paths.length; ++i) {
          const path = paths[i];
          if (pathsToSave != null && !pathsToSave.has(path)) {
            promises.push(Promise.resolve(null));
            continue;
          }
          if (path == null || validated[path]) {
            promises.push(Promise.resolve(null));
            continue;
          }
          validated[path] = true;
          const schemaType2 = _this.$__schema.path(path);
          if (!schemaType2) {
            promises.push(Promise.resolve(null));
            continue;
          }
          if (!_this.$isValid(path)) {
            promises.push(Promise.resolve(null));
            continue;
          }
          if (schemaType2[schemaMixedSymbol] != null && path !== schemaType2.path) {
            promises.push(Promise.resolve(null));
            continue;
          }
          let val = _this.$__getValue(path);
          let pop;
          if (pop = _this.$populated(path)) {
            val = pop;
          } else if (val?.$__?.wasPopulated) {
            val = val._doc._id;
          }
          const scope = _this.$__.pathsToScopes != null && path in _this.$__.pathsToScopes ? _this.$__.pathsToScopes[path] : _this;
          const doValidateOptions = {
            ...doValidateOptionsByPath[path],
            path,
            validateAllPaths,
            _nestedValidate: true
          };
          promises.push(
            schemaType2.doValidate(val, scope, doValidateOptions).then(
              () => null,
              (err) => {
                const isSubdoc = schemaType2.$isSingleNested || schemaType2.$isArraySubdocument || schemaType2.$isMongooseDocumentArray;
                if (isSubdoc && err instanceof ValidationError) {
                  return null;
                }
                return err;
              }
            )
          );
        }
        const validationErrors = await Promise.all(promises);
        for (let i = 0; i < validationErrors.length; ++i) {
          const validationError = validationErrors[i];
          if (validationError == null) {
            continue;
          }
          if (this.$__.validationError == null) {
            this.$__.validationError = new ValidationError(this);
          }
          _this.invalidate(paths[i], validationError, void 0, true);
        }
        const error2 = _completeValidate(this);
        if (hasValidateHooks) {
          await this._execDocumentPostHooks("validate", options2, error2);
        } else if (error2 != null) {
          throw error2;
        }
      } finally {
        this.$__.validateModifiedOnly = void 0;
        this.$op = null;
        this.$__.validating = null;
      }
    };
    Document.prototype.$validate = Document.prototype.validate;
    function _completeValidate(doc) {
      let error2 = doc.$__.validationError;
      doc.$__.validationError = null;
      doc.$__.validating = null;
      if (doc.$__.validateModifiedOnly && error2 != null) {
        const errors = Object.keys(error2.errors);
        for (const errPath of errors) {
          if (!doc.$isModified(errPath)) {
            delete error2.errors[errPath];
          }
        }
        if (utils2.hasOwnKeys(error2.errors) === false) {
          error2 = void 0;
        }
      }
      doc.$__.cachedRequired = null;
      doc.$emit("validate", doc);
      doc.constructor.emit("validate", doc);
      if (error2) {
        for (const key in error2.errors) {
          if (!doc[documentArrayParent] && error2.errors[key] instanceof MongooseError.CastError) {
            doc.invalidate(key, error2.errors[key]);
          }
        }
      }
      return error2;
    }
    const STATES_TO_VALIDATE = Object.freeze(["init", "default", "modify"]);
    const STAR_CHAR_CODE = "*".charCodeAt(0);
    function _getPathsToValidate(doc, pathsToValidate, pathsToSkip, isNestedValidate) {
      const doValidateOptions = {};
      const schema2 = doc.$__schema;
      const schemaPaths = schema2.paths;
      const activeStates = doc.$__.activePaths.states;
      let _modifiedPaths;
      const getModifiedPaths = () => _modifiedPaths ??= doc.modifiedPaths();
      let paths = [];
      const requireStates = activeStates.require;
      if (requireStates != null) {
        let modifiedPaths2 = null;
        for (const path in requireStates) {
          const type2 = Object.hasOwn(schemaPaths, path) ? schemaPaths[path] : schema2.path(path);
          if (typeof type2?.originalRequiredValue === "function") {
            const cachedRequired2 = doc.$__.cachedRequired ?? (doc.$__.cachedRequired = {});
            try {
              cachedRequired2[path] = type2.originalRequiredValue.call(doc, doc);
            } catch (err) {
              doc.invalidate(path, err);
            }
          }
          if (!doc.$__isSelected(path)) {
            if (modifiedPaths2 === null) {
              modifiedPaths2 = doc.modifiedPaths();
            }
            if (!doc.$isModified(path, null, modifiedPaths2)) {
              continue;
            }
          }
          if (path.charCodeAt(path.length - 1) === STAR_CHAR_CODE && path.endsWith(".$*")) {
            continue;
          }
          const cachedRequired = doc.$__.cachedRequired;
          if (cachedRequired != null && path in cachedRequired) {
            if (cachedRequired[path]) {
              paths.push(path);
            }
          } else {
            paths.push(path);
          }
        }
      }
      for (let i = 0; i < STATES_TO_VALIDATE.length; ++i) {
        const statePaths = activeStates[STATES_TO_VALIDATE[i]];
        if (statePaths == null) {
          continue;
        }
        for (const p in statePaths) {
          if (p.charCodeAt(p.length - 1) === STAR_CHAR_CODE && p.endsWith(".$*")) {
            continue;
          }
          const _pathType = Object.hasOwn(schemaPaths, p) ? schemaPaths[p] : schema2.path(p);
          if (_pathType) {
            if (!_pathType.schema && !_pathType.embeddedSchemaType && _pathType.validators.length === 0 && !_pathType.$parentSchemaDocArray && // gh-15957: skip this optimization for SchemaMap as maps can contain subdocuments
            // that need validation even if the map itself has no validators
            !_pathType.$isSchemaMap && !_pathType.$isSchemaUnion) {
              continue;
            } else if (_pathType.$isMongooseArray && !_pathType.$isMongooseDocumentArray && // Skip document arrays...
            !_pathType.embeddedSchemaType.$isMongooseArray && // and arrays of arrays
            _pathType.validators.length === 0 && // and arrays with top-level validators
            _pathType.embeddedSchemaType.validators.length === 0) {
              continue;
            }
          }
          paths.push(p);
        }
      }
      if (paths.length === 0 && doc.$__hasOnlyPrimitiveValues()) {
        return [paths, doValidateOptions];
      }
      paths = new Set(paths);
      if (!isNestedValidate) {
        const topLevelSubdocs = [];
        for (const path of Object.keys(doc.$__schema.paths)) {
          const schemaType2 = doc.$__schema.path(path);
          if (schemaType2.$isSingleNested) {
            const subdoc = doc.$get(path);
            if (subdoc) {
              topLevelSubdocs.push(subdoc);
            }
          } else if (schemaType2.$isMongooseDocumentArray) {
            const arr = doc.$get(path);
            if (arr?.length) {
              for (const subdoc of arr) {
                if (subdoc) {
                  topLevelSubdocs.push(subdoc);
                }
              }
            }
          }
        }
        const modifiedPaths2 = getModifiedPaths();
        for (const modifiedPath of modifiedPaths2) {
          const modifiedPathType = doc.$__schema.path(modifiedPath);
          if (modifiedPathType != null && modifiedPathType.$isMongooseDocumentArray && modifiedPathType.validators.length > 0) {
            paths.add(modifiedPath);
          }
        }
        for (const subdoc of topLevelSubdocs) {
          if (subdoc.$basePath) {
            const fullPathToSubdoc = subdoc.$__pathRelativeToParent();
            for (const modifiedPath of subdoc.modifiedPaths()) {
              paths.delete(fullPathToSubdoc + "." + modifiedPath);
            }
            const subdocParent = subdoc.$parent();
            if (subdocParent == null) {
              throw new Error("Cannot validate subdocument that does not have a parent");
            }
            if (doc.$isModified(fullPathToSubdoc, null, modifiedPaths2) && // Avoid using isDirectModified() here because that does additional checks on whether the parent path
            // is direct modified, which can cause performance issues re: gh-14897
            !Object.hasOwn(subdocParent.$__.activePaths.getStatePaths("modify"), fullPathToSubdoc) && !subdocParent.$isDefault(fullPathToSubdoc)) {
              paths.add(fullPathToSubdoc);
              if (doc.$__.pathsToScopes == null) {
                doc.$__.pathsToScopes = {};
              }
              doc.$__.pathsToScopes[fullPathToSubdoc] = subdoc.$isDocumentArrayElement ? subdoc.__parentArray : subdoc.$parent();
              doValidateOptions[fullPathToSubdoc] = { skipSchemaValidators: true };
              if (subdoc.$isDocumentArrayElement && subdoc.__index != null) {
                doValidateOptions[fullPathToSubdoc].index = subdoc.__index;
              }
            }
          }
        }
      }
      if (!doc.$__hasOnlyPrimitiveValues()) {
        for (const path of paths) {
          const _pathType = doc.$__schema.path(path);
          if (_pathType && _pathType.$isMongooseDocumentArray) {
            for (const p of paths) {
              if (p == null || p.startsWith(_pathType.path + ".")) {
                paths.delete(p);
              }
            }
          }
        }
      }
      if (Array.isArray(pathsToValidate)) {
        paths = _handlePathsToValidate(paths, pathsToValidate);
      } else if (Array.isArray(pathsToSkip)) {
        paths = _handlePathsToSkip(paths, pathsToSkip);
      }
      _addArrayPathsToValidate(doc, paths);
      const flattenOptions = { skipArrays: true };
      for (const pathToCheck of paths) {
        if (doc.$__schema.nested[pathToCheck]) {
          let _v = doc.$__getValue(pathToCheck);
          if (isMongooseObject2(_v)) {
            _v = _v.toObject({ transform: false });
          }
          const flat = flatten(_v, pathToCheck, flattenOptions, doc.$__schema);
          const singleNestedPaths = doc.$__schema.singleNestedPaths;
          for (const path of Object.keys(flat)) {
            if (!Object.hasOwn(singleNestedPaths, path) && !path.endsWith(".$*")) {
              paths.add(path);
            }
          }
        }
      }
      for (const path of paths) {
        const _pathType = doc.$__schema.path(path);
        if (!_pathType) {
          continue;
        }
        if (_pathType.$parentSchemaDocArray && typeof _pathType.$parentSchemaDocArray.path === "string") {
          paths.add(_pathType.$parentSchemaDocArray.path);
        }
        if (!_pathType.$isSchemaMap) {
          continue;
        }
        const val = doc.$__getValue(path);
        if (val == null) {
          continue;
        }
        for (const key of val.keys()) {
          paths.add(path + "." + key);
        }
      }
      paths = Array.from(paths);
      return [paths, doValidateOptions];
    }
    function _addArrayPathsToValidate(doc, paths) {
      for (const path of paths) {
        const _pathType = doc.$__schema.path(path);
        if (!_pathType) {
          continue;
        }
        if (!_pathType.$isMongooseArray || // To avoid potential performance issues, skip doc arrays whose children
        // are not required. `getPositionalPathType()` may be slow, so avoid
        // it unless we have a case of #6364
        !Array.isArray(_pathType) && _pathType.$isMongooseDocumentArray && !_pathType?.schemaOptions?.required) {
          continue;
        }
        if (_pathType.$isMongooseArray && !_pathType.$isMongooseDocumentArray && // Skip document arrays...
        !_pathType.embeddedSchemaType.$isMongooseArray && // and arrays of arrays
        _pathType.embeddedSchemaType.validators.length === 0) {
          continue;
        }
        const val = doc.$__getValue(path);
        _pushNestedArrayPaths(val, paths, path);
      }
    }
    function _pushNestedArrayPaths(val, paths, path) {
      if (val != null) {
        const numElements = val.length;
        for (let j = 0; j < numElements; ++j) {
          if (Array.isArray(val[j])) {
            _pushNestedArrayPaths(val[j], paths, path + "." + j);
          } else {
            paths.add(path + "." + j);
          }
        }
      }
    }
    Document.prototype._execDocumentPreHooks = function _execDocumentPreHooks(opName, options2, argsForHooks) {
      const filter = buildMiddlewareFilter(options2, "pre");
      return this.$__middleware.execPre(opName, this, argsForHooks || [], { filter });
    };
    Document.prototype._execDocumentPostHooks = function _execDocumentPostHooks(opName, options2, error2) {
      const filter = buildMiddlewareFilter(options2, "post");
      return this.$__middleware.execPost(opName, this, [this], { error: error2, filter });
    };
    function _handlePathsToValidate(paths, pathsToValidate) {
      const _pathsToValidate = new Set(pathsToValidate);
      const parentPaths3 = /* @__PURE__ */ new Map([]);
      for (const path of pathsToValidate) {
        if (path.indexOf(".") === -1) {
          continue;
        }
        const pieces = path.split(".");
        let cur = pieces[0];
        for (let i = 1; i < pieces.length; ++i) {
          parentPaths3.set(cur, path);
          cur = cur + "." + pieces[i];
        }
      }
      const ret = /* @__PURE__ */ new Set();
      for (const path of paths) {
        if (_pathsToValidate.has(path)) {
          ret.add(path);
        } else if (parentPaths3.has(path)) {
          ret.add(parentPaths3.get(path));
        }
      }
      return ret;
    }
    function _handlePathsToSkip(paths, pathsToSkip) {
      pathsToSkip = new Set(pathsToSkip);
      paths = Array.from(paths).filter((p) => !pathsToSkip.has(p));
      return new Set(paths);
    }
    Document.prototype.validateSync = function() {
      utils2.warn("Mongoose: `Document.prototype.validateSync()` is deprecated and will be removed in Mongoose 10. Use `Document.prototype.validate()` instead.");
      return this.$__validateSync.apply(this, arguments);
    };
    Document.prototype.$__validateSync = function(pathsToValidate, options2) {
      const _this = this;
      if (arguments.length === 1 && typeof arguments[0] === "object" && !Array.isArray(arguments[0])) {
        options2 = arguments[0];
        pathsToValidate = null;
      }
      const hasValidateModifiedOnlyOption = options2 && typeof options2 === "object" && "validateModifiedOnly" in options2;
      let shouldValidateModifiedOnly;
      if (hasValidateModifiedOnlyOption) {
        shouldValidateModifiedOnly = !!options2.validateModifiedOnly;
      } else {
        shouldValidateModifiedOnly = this.$__schema.options.validateModifiedOnly;
      }
      let pathsToSkip = options2?.pathsToSkip;
      const validateAllPaths = options2?.validateAllPaths;
      if (validateAllPaths) {
        if (pathsToSkip) {
          throw new TypeError("Cannot set both `validateAllPaths` and `pathsToSkip`");
        }
        if (pathsToValidate) {
          throw new TypeError("Cannot set both `validateAllPaths` and `pathsToValidate`");
        }
      }
      if (typeof pathsToValidate === "string") {
        const isOnePathOnly = pathsToValidate.indexOf(" ") === -1;
        pathsToValidate = isOnePathOnly ? [pathsToValidate] : pathsToValidate.split(" ");
      } else if (typeof pathsToSkip === "string" && pathsToSkip.indexOf(" ") !== -1) {
        pathsToSkip = pathsToSkip.split(" ");
      }
      let paths;
      let skipSchemaValidators;
      if (validateAllPaths) {
        paths = new Set(Object.keys(this.$__schema.paths));
        for (const path of paths) {
          const schemaType2 = this.$__schema.path(path);
          if (!schemaType2?.$isMongooseArray) {
            continue;
          }
          const val = this.$__getValue(path);
          if (!val) {
            continue;
          }
          _pushNestedArrayPaths(val, paths, path);
        }
        paths = [...paths];
        skipSchemaValidators = {};
      } else {
        const pathDetails = _getPathsToValidate(this, pathsToValidate, pathsToSkip);
        paths = shouldValidateModifiedOnly ? pathDetails[0].filter((path) => this.$isModified(path)) : pathDetails[0];
        skipSchemaValidators = pathDetails[1];
      }
      const validating = {};
      for (let i = 0, len = paths.length; i < len; ++i) {
        const path = paths[i];
        if (validating[path]) {
          continue;
        }
        validating[path] = true;
        const p = _this.$__schema.path(path);
        if (!p) {
          continue;
        }
        if (!_this.$isValid(path)) {
          continue;
        }
        const val = _this.$__getValue(path);
        const err2 = p.doValidateSync(val, _this, {
          skipSchemaValidators: skipSchemaValidators[path],
          path,
          validateModifiedOnly: shouldValidateModifiedOnly,
          validateAllPaths
        });
        if (err2) {
          const isSubdoc = p.$isSingleNested || p.$isArraySubdocument || p.$isMongooseDocumentArray;
          if (isSubdoc && err2 instanceof ValidationError) {
            continue;
          }
          _this.invalidate(path, err2, void 0, true);
        }
      }
      const err = _this.$__.validationError;
      _this.$__.validationError = void 0;
      _this.$emit("validate", _this);
      _this.constructor.emit("validate", _this);
      if (err) {
        for (const key in err.errors) {
          if (err.errors[key] instanceof MongooseError.CastError) {
            _this.invalidate(key, err.errors[key]);
          }
        }
      }
      return err;
    };
    Document.prototype.invalidate = function(path, err, val, kind) {
      if (!this.$__.validationError) {
        this.$__.validationError = new ValidationError(this);
      }
      if (this.$__.validationError.errors[path]) {
        return;
      }
      if (!err || typeof err === "string") {
        err = new ValidatorError({
          path,
          message: err,
          type: kind || "user defined",
          value: val
        });
      }
      if (this.$__.validationError === err) {
        return this.$__.validationError;
      }
      this.$__.validationError.addError(path, err);
      return this.$__.validationError;
    };
    Document.prototype.$markValid = function(path) {
      if (!this.$__.validationError?.errors[path]) {
        return;
      }
      delete this.$__.validationError.errors[path];
      if (utils2.hasOwnKeys(this.$__.validationError.errors) === false) {
        this.$__.validationError = null;
      }
    };
    function _markValidSubpaths(doc, path) {
      if (!doc.$__.validationError) {
        return;
      }
      const keys = Object.keys(doc.$__.validationError.errors);
      for (const key of keys) {
        if (key.startsWith(path + ".")) {
          delete doc.$__.validationError.errors[key];
        }
      }
      if (utils2.hasOwnKeys(doc.$__.validationError.errors) === false) {
        doc.$__.validationError = null;
      }
    }
    function _checkImmutableSubpaths(subdoc, schematype, priorVal) {
      const schema2 = schematype.schema;
      if (schema2 == null) {
        return;
      }
      for (const key of Object.keys(schema2.paths)) {
        const path = schema2.paths[key];
        if (path.$immutableSetter == null) {
          continue;
        }
        const oldVal = priorVal == null ? void 0 : priorVal.$__getValue(key);
        path.$immutableSetter.call(subdoc, oldVal);
      }
    }
    Document.prototype.$isValid = function(path) {
      if (this.$__.validationError == null || utils2.hasOwnKeys(this.$__.validationError.errors) === false) {
        return true;
      }
      if (path == null) {
        return false;
      }
      if (path.indexOf(" ") !== -1) {
        path = path.split(" ");
      }
      if (Array.isArray(path)) {
        return path.some((p) => this.$__.validationError.errors[p] == null);
      }
      return this.$__.validationError.errors[path] == null;
    };
    Document.prototype.$__reset = function reset(skipBackup, hasOnlyPrimitiveValues) {
      hasOnlyPrimitiveValues = hasOnlyPrimitiveValues ?? this.$__hasOnlyPrimitiveValues();
      const subdocs = !this.$isSubdocument && !hasOnlyPrimitiveValues ? this.$getAllSubdocs({ useCache: true }) : null;
      if (subdocs?.length > 0) {
        for (const subdoc of subdocs) {
          subdoc.$__reset(skipBackup);
        }
      }
      if (!hasOnlyPrimitiveValues) {
        this.$__resetAtomics();
      }
      if (!skipBackup) {
        this.$__.backup = {};
        this.$__.backup.activePaths = {
          modify: Object.assign({}, this.$__.activePaths.getStatePaths("modify")),
          default: Object.assign({}, this.$__.activePaths.getStatePaths("default"))
        };
        this.$__.backup.validationError = this.$__.validationError;
        this.$__.backup.errors = this.$errors;
      }
      this.$__.activePaths.clearAllExcept("init");
      if (this.$__.validationError) {
        this.$__.validationError = void 0;
      }
      if (this.$errors) {
        this.$errors = void 0;
      }
      for (const path of this.$__schema.requiredPaths()) {
        this.$__.activePaths.require(path);
      }
      return this;
    };
    Document.prototype.$__resetAtomics = function $__resetAtomics() {
      const activePaths = this.$__.activePaths;
      const modifyPaths = activePaths.getStatePaths("modify");
      const defaultPaths = activePaths.getStatePaths("default");
      _clearAtomicsOnPaths(this, modifyPaths);
      _clearAtomicsOnPaths(this, defaultPaths);
    };
    function _clearAtomicsOnPaths(doc, paths) {
      if (paths == null) {
        return;
      }
      const keys = Object.keys(paths);
      for (let i = 0; i < keys.length; ++i) {
        const type2 = doc.$__getValue(keys[i]);
        if (type2 != null) {
          if (typeof type2.clearAtomics === "function") {
            type2.clearAtomics();
          } else if (type2[arrayAtomicsSymbol]) {
            type2[arrayAtomicsBackupSymbol] = type2[arrayAtomicsSymbol];
            type2[arrayAtomicsSymbol] = {};
          }
        }
      }
    }
    Document.prototype.$__undoReset = function $__undoReset() {
      if (this.$__.backup?.activePaths == null) {
        return;
      }
      this.$__.activePaths.states.modify = this.$__.backup.activePaths.modify;
      this.$__.activePaths.states.default = this.$__.backup.activePaths.default;
      this.$__.validationError = this.$__.backup.validationError;
      this.$errors = this.$__.backup.errors;
      for (const dirt of this.$__dirty()) {
        const type2 = dirt.value;
        if (type2?.[arrayAtomicsSymbol] && type2[arrayAtomicsBackupSymbol]) {
          type2[arrayAtomicsSymbol] = type2[arrayAtomicsBackupSymbol];
        }
      }
      if (!this.$isSubdocument) {
        for (const subdoc of this.$getAllSubdocs()) {
          subdoc.$__undoReset();
        }
      }
    };
    Document.prototype.$__dirty = function() {
      const _this = this;
      let all = this.$__.activePaths.map("modify", function(path) {
        return {
          path,
          value: _this.$__getValue(path),
          schema: _this.$__path(path)
        };
      });
      all = all.concat(this.$__.activePaths.map("default", function(path) {
        if (path === "_id" || _this.$__getValue(path) == null) {
          return;
        }
        return {
          path,
          value: _this.$__getValue(path),
          schema: _this.$__path(path)
        };
      }));
      const allPaths = new Map(all.filter((el) => el != null).map((el) => [el.path, el.value]));
      const minimal = [];
      all.forEach(function(item) {
        if (!item) {
          return;
        }
        let top = null;
        let foundParent = false;
        const array2 = parentPaths2(item.path);
        for (let i = 0; i < array2.length - 1; i++) {
          if (allPaths.has(array2[i])) {
            top = allPaths.get(array2[i]);
            foundParent = true;
            break;
          }
        }
        if (!foundParent) {
          minimal.push(item);
        } else if (top != null && top[arrayAtomicsSymbol] != null && top.hasAtomics()) {
          top[arrayAtomicsSymbol] = {};
          top[arrayAtomicsSymbol].$set = top;
        }
      });
      return minimal;
    };
    Document.prototype.$__setSchema = function(schema2) {
      compile2(schema2.tree, this, void 0, schema2.options);
      for (const key of Object.keys(schema2.virtuals)) {
        schema2.virtuals[key]._applyDefaultGetters();
      }
      if (schema2.path("schema") == null) {
        this.schema = schema2;
      }
      this.$__schema = schema2;
      this.$__middleware = schema2._getDocumentMiddleware();
      this[documentSchemaSymbol] = schema2;
    };
    Document.prototype.$__getArrayPathsToValidate = function() {
      DocumentArray || (DocumentArray = requireDocumentArray$1());
      return this.$__.activePaths.map("init", "modify", (function(i) {
        return this.$__getValue(i);
      }).bind(this)).filter(function(val) {
        return val && Array.isArray(val) && utils2.isMongooseDocumentArray(val) && val.length;
      }).reduce(function(seed, array2) {
        return seed.concat(array2);
      }, []).filter(function(doc) {
        return doc;
      });
    };
    Document.prototype.$getAllSubdocs = function(options2) {
      if (options2?.useCache && this.$__.saveOptions?.__subdocs) {
        return this.$__.saveOptions.__subdocs;
      }
      DocumentArray || (DocumentArray = requireDocumentArray$1());
      Embedded = Embedded || requireArraySubdocument();
      const subDocs = [];
      function getSubdocs(doc) {
        const newSubdocs = [];
        for (const { model: model2 } of doc.$__schema.childSchemas) {
          const val = doc.$__getValue(model2.path);
          if (val == null) {
            continue;
          }
          if (val.$__) {
            newSubdocs.push(val);
          }
          if (Array.isArray(val)) {
            for (const el of val) {
              if (el?.$__) {
                newSubdocs.push(el);
              }
            }
          }
          if (val instanceof Map) {
            for (const el of val.values()) {
              if (el?.$__) {
                newSubdocs.push(el);
              }
            }
          }
        }
        for (const subdoc of newSubdocs) {
          getSubdocs(subdoc);
        }
        subDocs.push(...newSubdocs);
      }
      getSubdocs(this);
      if (this.$__.saveOptions) {
        this.$__.saveOptions.__subdocs = subDocs;
      }
      return subDocs;
    };
    function applyQueue(doc) {
      const q = doc.$__schema && doc.$__schema.callQueue;
      if (!q.length) {
        return;
      }
      for (const pair of q) {
        if (pair[0] !== "pre" && pair[0] !== "post" && pair[0] !== "on") {
          doc[pair[0]].apply(doc, pair[1]);
        }
      }
    }
    Document.prototype.$__handleReject = function handleReject(err) {
      if (this.$listeners("error").length) {
        this.$emit("error", err);
      } else if (this.constructor.listeners?.("error").length) {
        this.constructor.emit("error", err);
      }
    };
    Document.prototype.$toObject = function(options2, json) {
      const defaultOptions = this.$__schema._defaultToObjectOptions(json);
      const hasOnlyPrimitiveValues = this.$__hasOnlyPrimitiveValues();
      options2 = utils2.isPOJO(options2) ? { ...options2 } : {};
      options2._calledWithOptions = options2._calledWithOptions || { ...options2 };
      let _minimize;
      if (options2._calledWithOptions.minimize != null) {
        _minimize = options2.minimize;
      } else if (this.$__schemaTypeOptions?.minimize != null) {
        _minimize = this.$__schemaTypeOptions.minimize;
      } else if (defaultOptions?.minimize != null) {
        _minimize = defaultOptions.minimize;
      } else {
        _minimize = this.$__schema.options.minimize;
      }
      options2.minimize = _minimize;
      if (!hasOnlyPrimitiveValues) {
        options2._seen = options2._seen || /* @__PURE__ */ new Map();
      }
      const depopulate = options2._calledWithOptions.depopulate ?? defaultOptions?.depopulate ?? options2.depopulate ?? false;
      if (depopulate && options2._isNested && this.$__.wasPopulated) {
        return clone(this.$__.wasPopulated.value || this._doc._id, options2);
      }
      if (defaultOptions != null) {
        for (const key of Object.keys(defaultOptions)) {
          if (options2[key] == null) {
            options2[key] = defaultOptions[key];
          }
        }
      }
      options2._isNested = true;
      options2.json = json;
      const parentOptions = options2._parentOptions;
      options2._parentOptions = this.$isSubdocument ? options2 : null;
      const schemaFieldsOnly = options2._calledWithOptions.schemaFieldsOnly ?? options2.schemaFieldsOnly ?? defaultOptions?.schemaFieldsOnly ?? false;
      let ret;
      if (hasOnlyPrimitiveValues && !options2.flattenObjectIds) {
        ret = this.$__toObjectShallow(schemaFieldsOnly);
      } else if (schemaFieldsOnly) {
        ret = {};
        for (const path of Object.keys(this.$__schema.paths)) {
          const value = this.$__getValue(path);
          if (value === void 0) {
            continue;
          }
          let pathToSet = path;
          let objToSet = ret;
          if (path.indexOf(".") !== -1) {
            const segments = path.split(".");
            pathToSet = segments[segments.length - 1];
            for (let i = 0; i < segments.length - 1; ++i) {
              objToSet[segments[i]] = objToSet[segments[i]] ?? {};
              objToSet = objToSet[segments[i]];
            }
          }
          if (value === null) {
            objToSet[pathToSet] = null;
            continue;
          }
          objToSet[pathToSet] = clone(value, options2);
        }
      } else {
        ret = clone(this._doc, options2) || {};
      }
      const getters = options2._calledWithOptions.getters ?? options2.getters ?? defaultOptions?.getters ?? false;
      if (getters) {
        applyGetters(this, ret);
        if (options2.minimize) {
          ret = minimize(ret) || {};
        }
      }
      const virtuals = options2._calledWithOptions.virtuals ?? defaultOptions?.virtuals ?? parentOptions?.virtuals ?? void 0;
      if (virtuals || getters && virtuals !== false) {
        applyVirtuals(this, ret, options2, options2);
      }
      if (options2.versionKey === false && this.$__schema.options.versionKey) {
        delete ret[this.$__schema.options.versionKey];
      }
      const transform = options2._calledWithOptions.transform ?? true;
      let transformFunction = void 0;
      if (transform === true) {
        transformFunction = defaultOptions?.transform;
      } else if (typeof transform === "function") {
        transformFunction = transform;
      }
      if (transform) {
        applySchemaTypeTransforms(this, ret);
      }
      if (options2.useProjection) {
        omitDeselectedFields(this, ret);
      }
      if (typeof transformFunction === "function") {
        const xformed = transformFunction(this, ret, options2);
        if (typeof xformed !== "undefined") {
          ret = xformed;
        }
      }
      return ret;
    };
    Document.prototype.$__toObjectShallow = function $__toObjectShallow(schemaFieldsOnly) {
      const ret = {};
      if (this._doc != null) {
        const keys = schemaFieldsOnly ? Object.keys(this.$__schema.paths) : Object.keys(this._doc);
        for (const key of keys) {
          const value = this._doc[key];
          if (value instanceof Date) {
            ret[key] = new Date(value);
          } else if (value !== void 0) {
            ret[key] = value;
          }
        }
      }
      return ret;
    };
    Document.prototype.toObject = function(options2) {
      return this.$toObject(options2);
    };
    function applyVirtuals(self, json, options2, toObjectOptions) {
      const schema2 = self.$__schema;
      const virtuals = schema2.virtuals;
      const paths = Object.keys(virtuals);
      let i = paths.length;
      const numPaths = i;
      let path;
      let assignPath;
      let cur = self._doc;
      let v;
      const aliases = typeof toObjectOptions?.aliases === "boolean" ? toObjectOptions.aliases : true;
      options2 = options2 || {};
      let virtualsToApply = null;
      if (Array.isArray(options2.virtuals)) {
        virtualsToApply = new Set(options2.virtuals);
      } else if (options2.virtuals?.pathsToSkip) {
        virtualsToApply = new Set(paths);
        for (let i2 = 0; i2 < options2.virtuals.pathsToSkip.length; i2++) {
          if (virtualsToApply.has(options2.virtuals.pathsToSkip[i2])) {
            virtualsToApply.delete(options2.virtuals.pathsToSkip[i2]);
          }
        }
      }
      if (!cur) {
        return json;
      }
      for (i = 0; i < numPaths; ++i) {
        path = paths[i];
        if (virtualsToApply != null && !virtualsToApply.has(path)) {
          continue;
        }
        if (!aliases && Object.hasOwn(schema2.aliases, path)) {
          continue;
        }
        assignPath = path;
        if (options2.path != null) {
          if (!path.startsWith(options2.path + ".")) {
            continue;
          }
          assignPath = path.substring(options2.path.length + 1);
        }
        if (assignPath.indexOf(".") === -1 && assignPath === path) {
          v = virtuals[path].applyGetters(void 0, self);
          if (v === void 0) {
            continue;
          }
          v = clone(v, options2);
          json[assignPath] = v;
          continue;
        }
        const parts = assignPath.split(".");
        v = clone(self.get(path), options2);
        if (v === void 0) {
          continue;
        }
        const plen = parts.length;
        cur = json;
        for (let j = 0; j < plen - 1; ++j) {
          cur[parts[j]] = cur[parts[j]] || {};
          cur = cur[parts[j]];
        }
        cur[parts[plen - 1]] = v;
      }
      return json;
    }
    function applyGetters(self, json) {
      const schema2 = self.$__schema;
      const paths = Object.keys(schema2.paths);
      let i = paths.length;
      let path;
      let cur = self._doc;
      let v;
      if (!cur) {
        return json;
      }
      while (i--) {
        path = paths[i];
        const schemaType2 = schema2.paths[path];
        if (!schemaType2.getters?.length && !schemaType2.embeddedSchemaType?.getters?.length) {
          continue;
        }
        if (!self.$__isSelected(path)) {
          continue;
        }
        if (path.indexOf(".") === -1) {
          json[path] = schemaType2.applyGetters(
            json[path],
            self
          );
          if (Array.isArray(json[path]) && schemaType2.embeddedSchemaType) {
            for (let i2 = 0; i2 < json[path].length; ++i2) {
              json[path][i2] = schemaType2.embeddedSchemaType.applyGetters(
                json[path][i2],
                self
              );
            }
          }
          continue;
        }
        const parts = path.split(".");
        const plen = parts.length;
        const last = plen - 1;
        let branch = json;
        let part;
        cur = self._doc;
        for (let ii = 0; ii < plen; ++ii) {
          part = parts[ii];
          v = cur[part];
          if (branch != null && typeof branch !== "object") {
            break;
          } else if (ii === last) {
            branch[part] = schemaType2.applyGetters(
              branch[part],
              self
            );
            if (Array.isArray(branch[part]) && schemaType2.embeddedSchemaType) {
              for (let i2 = 0; i2 < branch[part].length; ++i2) {
                branch[part][i2] = schemaType2.embeddedSchemaType.applyGetters(
                  branch[part][i2],
                  self
                );
              }
            }
          } else if (v == null) {
            if (part in cur) {
              branch[part] = v;
            }
            break;
          } else {
            branch = branch[part] || (branch[part] = {});
          }
          cur = v;
        }
      }
      return json;
    }
    function applySchemaTypeTransforms(self, json) {
      const schema2 = self.$__schema;
      const paths = schema2.pathsWithTransforms();
      const cur = self._doc;
      if (!cur || !paths) {
        return json;
      }
      for (const path of paths) {
        const schematype = schema2.paths[path];
        const topLevelTransformFunction = schematype.options.transform ?? schematype.constructor?.defaultOptions?.transform;
        const embeddedSchemaTypeTransformFunction = schematype.embeddedSchemaType?.options?.transform ?? schematype.embeddedSchemaType?.constructor?.defaultOptions?.transform;
        if (typeof topLevelTransformFunction === "function") {
          const val = self.$get(path);
          if (val === void 0) {
            continue;
          }
          const transformedValue = topLevelTransformFunction.call(self, val);
          throwErrorIfPromise(path, transformedValue);
          utils2.setValue(path, transformedValue, json);
        } else if (typeof embeddedSchemaTypeTransformFunction === "function") {
          const val = self.$get(path);
          if (val === void 0) {
            continue;
          }
          const vals = [].concat(val);
          for (let i = 0; i < vals.length; ++i) {
            const transformedValue = embeddedSchemaTypeTransformFunction.call(self, vals[i]);
            vals[i] = transformedValue;
            throwErrorIfPromise(path, transformedValue);
          }
          json[path] = vals;
        }
      }
      return json;
    }
    function throwErrorIfPromise(path, transformedValue) {
      if (isPromise(transformedValue)) {
        throw new Error("`transform` function must be synchronous, but the transform on path `" + path + "` returned a promise.");
      }
    }
    function omitDeselectedFields(self, json) {
      const schema2 = self.$__schema;
      const paths = Object.keys(schema2.paths || {});
      const cur = self._doc;
      if (!cur) {
        return json;
      }
      let selected = self.$__.selected;
      if (selected === void 0) {
        selected = {};
        queryhelpers.applyPaths(selected, schema2);
      }
      if (selected == null || utils2.hasOwnKeys(selected) === false) {
        return json;
      }
      for (const path of paths) {
        if (selected[path] != null && !selected[path]) {
          delete json[path];
        }
      }
      return json;
    }
    Document.prototype.toJSON = function(options2) {
      return this.$toObject(options2, true);
    };
    Document.prototype.ownerDocument = function() {
      return this;
    };
    Document.prototype.parent = function() {
      if (this.$isSubdocument || this.$__.wasPopulated) {
        return this.$__.parent;
      }
      return this;
    };
    Document.prototype.$parent = Document.prototype.parent;
    Document.prototype.$__setParent = function $__setParent(parent) {
      this.$__.parent = parent;
      this.$__parent = parent;
    };
    Document.prototype.inspect = function(options2) {
      const isPOJO2 = utils2.isPOJO(options2);
      let opts;
      if (isPOJO2) {
        opts = options2;
        opts.minimize = false;
      }
      const ret = arguments.length > 0 ? this.toObject(opts) : this.toObject();
      if (ret == null) {
        return "MongooseDocument { " + ret + " }";
      }
      return ret;
    };
    if (inspect.custom) {
      Document.prototype[inspect.custom] = Document.prototype.inspect;
    }
    Document.prototype.toString = function() {
      const ret = this.inspect();
      if (typeof ret === "string") {
        return ret;
      }
      return inspect(ret);
    };
    Document.prototype.equals = function(doc) {
      if (!doc) {
        return false;
      }
      const tid = this.$__getValue("_id");
      const docid = doc.$__ != null ? doc.$__getValue("_id") : doc;
      if (!tid && !docid) {
        return deepEqual(this, doc);
      }
      return tid && tid.equals ? tid.equals(docid) : tid === docid;
    };
    Document.prototype.populate = async function populate() {
      const pop = {};
      const args = [...arguments];
      if (typeof args[args.length - 1] === "function") {
        throw new MongooseError("Document.prototype.populate() no longer accepts a callback");
      }
      if (args.length !== 0) {
        const res = utils2.populate.apply(null, args);
        for (const populateOptions2 of res) {
          pop[populateOptions2.path] = populateOptions2;
        }
      }
      const paths = utils2.object.vals(pop);
      let topLevelModel = this.constructor;
      if (this.$__isNested) {
        topLevelModel = this.$__[scopeSymbol].constructor;
        const nestedPath = this.$__.nestedPath;
        paths.forEach(function(populateOptions2) {
          populateOptions2.path = nestedPath + "." + populateOptions2.path;
        });
      }
      if (this.$session() != null) {
        const session = this.$session();
        paths.forEach((path) => {
          if (path.options == null) {
            path.options = { session };
            return;
          }
          if (!("session" in path.options)) {
            path.options.session = session;
          }
        });
      }
      paths.forEach((p) => {
        p._localModel = topLevelModel;
      });
      return topLevelModel.populate(this, paths);
    };
    Document.prototype.$getPopulatedDocs = function $getPopulatedDocs() {
      let keys = [];
      if (this.$__.populated != null) {
        keys = keys.concat(Object.keys(this.$__.populated));
      }
      let result = [];
      for (const key of keys) {
        const value = this.$get(key);
        if (Array.isArray(value)) {
          result = result.concat(value);
        } else if (value instanceof Document) {
          result.push(value);
        }
      }
      return result;
    };
    Document.prototype.populated = function(path, val, options2) {
      if (val == null || val === true) {
        if (!this.$__.populated) {
          return void 0;
        }
        if (typeof path !== "string") {
          return void 0;
        }
        const _path = path.endsWith(".$*") ? path.replace(/\.\$\*$/, "") : path;
        const v = this.$__.populated[_path];
        if (v) {
          return val === true ? v : v.value;
        }
        return void 0;
      }
      this.$__.populated || (this.$__.populated = {});
      this.$__.populated[path] = { value: val, options: options2 };
      const pieces = path.split(".");
      for (let i = 0; i < pieces.length - 1; ++i) {
        const subpath = pieces.slice(0, i + 1).join(".");
        const subdoc = this.$get(subpath);
        if (subdoc?.$__ != null && this.$populated(subpath)) {
          const rest = pieces.slice(i + 1).join(".");
          subdoc.$populated(rest, val, options2);
          break;
        }
      }
      return val;
    };
    Document.prototype.$populated = Document.prototype.populated;
    Document.prototype.$assertPopulated = function $assertPopulated(path, values) {
      if (Array.isArray(path)) {
        path.forEach((p) => this.$assertPopulated(p, values));
        return this;
      }
      if (arguments.length > 1) {
        this.$set(values);
      }
      if (!this.$populated(path)) {
        throw new MongooseError(`Expected path "${path}" to be populated`);
      }
      return this;
    };
    Document.prototype.depopulate = function(path) {
      if (typeof path === "string") {
        path = path.indexOf(" ") === -1 ? [path] : path.split(" ");
      }
      let populatedIds;
      const virtualKeys = this.$$populatedVirtuals ? Object.keys(this.$$populatedVirtuals) : [];
      const populated = this.$__?.populated || {};
      if (arguments.length === 0) {
        for (const virtualKey of virtualKeys) {
          delete this.$$populatedVirtuals[virtualKey];
          delete this._doc[virtualKey];
          delete populated[virtualKey];
        }
        const keys = Object.keys(populated);
        for (const key of keys) {
          populatedIds = this.$populated(key);
          if (!populatedIds) {
            continue;
          }
          delete populated[key];
          if (Array.isArray(populatedIds)) {
            const arr = utils2.getValue(key, this._doc);
            if (arr.isMongooseArray) {
              const rawArray = arr.__array;
              for (let i = 0; i < rawArray.length; ++i) {
                const subdoc = rawArray[i];
                if (subdoc == null) {
                  continue;
                }
                rawArray[i] = subdoc instanceof Document ? subdoc._doc._id : subdoc._id;
              }
            } else {
              utils2.setValue(key, populatedIds, this._doc);
            }
          } else {
            utils2.setValue(key, populatedIds, this._doc);
          }
        }
        return this;
      }
      for (const singlePath of path) {
        populatedIds = this.$populated(singlePath);
        delete populated[singlePath];
        if (virtualKeys.indexOf(singlePath) !== -1) {
          delete this.$$populatedVirtuals[singlePath];
          delete this._doc[singlePath];
        } else if (populatedIds) {
          if (Array.isArray(populatedIds)) {
            const arr = utils2.getValue(singlePath, this._doc);
            if (arr.isMongooseArray) {
              const rawArray = arr.__array;
              for (let i = 0; i < rawArray.length; ++i) {
                const subdoc = rawArray[i];
                if (subdoc == null) {
                  continue;
                }
                rawArray[i] = subdoc instanceof Document ? subdoc._doc._id : subdoc._id;
              }
            } else {
              utils2.setValue(singlePath, populatedIds, this._doc);
            }
          } else {
            utils2.setValue(singlePath, populatedIds, this._doc);
          }
        }
      }
      return this;
    };
    Document.prototype.$__fullPath = function(path) {
      return path || "";
    };
    Document.prototype.$getChanges = function() {
      const delta = this.$__delta();
      const changes = delta ? delta[1] : {};
      return changes;
    };
    Document.prototype.getChanges = function() {
      utils2.warn("`getChanges()` is deprecated, use `$getChanges()` instead.");
      return this.$getChanges();
    };
    Document.prototype.$__delta = function $__delta(pathsToSave, pathsToSaveSet) {
      const allDirty = this.$__dirty();
      let dirty = allDirty;
      let unsavedDirty = null;
      if (pathsToSave != null) {
        dirty = [];
        unsavedDirty = [];
        for (const data of allDirty) {
          if (isInPathsToSave2(data.path, pathsToSaveSet, pathsToSave)) {
            dirty.push(data);
          } else {
            unsavedDirty.push(data);
          }
        }
      }
      const optimisticConcurrency = this.$__schema.options.optimisticConcurrency;
      if (optimisticConcurrency) {
        if (Array.isArray(optimisticConcurrency)) {
          if (!this.$__schema.options._optimisticConcurrencyTrie) {
            this.$__schema.options._optimisticConcurrencyTrie = new PathTrie(optimisticConcurrency);
          }
          const optimisticConcurrencyTrie = this.$__schema.options._optimisticConcurrencyTrie;
          const modPaths = this.directModifiedPaths();
          const hasRelevantModPaths = pathsToSave == null ? modPaths.find((path) => optimisticConcurrencyTrie.overlapsPath(path.indexOf(".") === -1 ? path : path.split("."))) : modPaths.find((path) => optimisticConcurrencyTrie.overlapsPath(path.indexOf(".") === -1 ? path : path.split(".")) && isInPathsToSave2(path, pathsToSaveSet, pathsToSave));
          if (hasRelevantModPaths) {
            this.$__.version = dirty.length ? VERSION_ALL : VERSION_WHERE;
          }
        } else if (Array.isArray(optimisticConcurrency?.exclude)) {
          if (!this.$__schema.options._optimisticConcurrencyExcludeTrie) {
            this.$__schema.options._optimisticConcurrencyExcludeTrie = new PathTrie(optimisticConcurrency.exclude);
          }
          const optimisticConcurrencyExcludeTrie = this.$__schema.options._optimisticConcurrencyExcludeTrie;
          const modPaths = this.directModifiedPaths();
          const hasRelevantModPaths = pathsToSave == null ? modPaths.find((path) => !optimisticConcurrencyExcludeTrie.matchesPathOrAncestor(path.indexOf(".") === -1 ? path : path.split("."))) : modPaths.find((path) => !optimisticConcurrencyExcludeTrie.matchesPathOrAncestor(path.indexOf(".") === -1 ? path : path.split(".")) && isInPathsToSave2(path, pathsToSaveSet, pathsToSave));
          if (hasRelevantModPaths) {
            this.$__.version = dirty.length ? VERSION_ALL : VERSION_WHERE;
          }
        } else {
          this.$__.version = dirty.length ? VERSION_ALL : VERSION_WHERE;
        }
      }
      if (!dirty.length && VERSION_ALL !== this.$__.version) {
        return;
      }
      const where = {};
      const delta = {};
      const len = dirty.length;
      const divergent = [];
      let d = 0;
      where._id = this._doc._id;
      if (where?._id?.$__ != null) {
        where._id = where._id.toObject({ transform: false, depopulate: true });
      }
      for (; d < len; ++d) {
        const data = dirty[d];
        let value = data.value;
        const match = checkDivergentArray(this, data.path, value);
        if (match) {
          divergent.push(match);
          continue;
        }
        const pop = this.$populated(data.path, true);
        if (!pop && this.$__.selected) {
          const pathSplit = data.path.split(".");
          const top = pathSplit[0];
          if (this.$__.selected[top] && this.$__.selected[top].$elemMatch) {
            if (pathSplit.length > 1 && pathSplit[1] == 0 && typeof where[top] === "undefined") {
              where[top] = this.$__.selected[top];
              pathSplit[1] = "$";
              data.path = pathSplit.join(".");
            } else {
              divergent.push(data.path);
              continue;
            }
          }
        }
        if (this.$isDefault(data.path) && this.$__.selected) {
          if (data.path.indexOf(".") === -1 && isPathExcluded2(this.$__.selected, data.path)) {
            continue;
          }
          const pathsToCheck = parentPaths2(data.path);
          if (pathsToCheck.find((path) => isPathExcluded2(this.$__.isSelected, path))) {
            continue;
          }
        }
        if (divergent.length) continue;
        if (value === void 0) {
          operand(this, where, delta, data, 1, "$unset");
        } else if (value === null) {
          operand(this, where, delta, data, null);
        } else if (typeof value.getAtomics === "function") {
          handleAtomics(this, where, delta, data, value);
        } else if (value[MongooseBuffer.pathSymbol] && Buffer.isBuffer(value)) {
          value = value.toObject();
          operand(this, where, delta, data, value);
        } else {
          if (this.$__.primitiveAtomics?.[data.path] != null) {
            const val = this.$__.primitiveAtomics[data.path];
            const op = firstKey2(val);
            operand(this, where, delta, data, val[op], op);
          } else {
            value = clone(value, {
              depopulate: true,
              transform: false,
              virtuals: false,
              getters: false,
              omitUndefined: true,
              _isNested: true
            });
            operand(this, where, delta, data, value);
          }
        }
      }
      if (divergent.length) {
        throw new DivergentArrayError(divergent);
      }
      if (this.$__.version) {
        this.$__version(where, delta);
      }
      if (utils2.hasOwnKeys(delta) === false) {
        return [where, null, unsavedDirty];
      }
      return [where, delta, unsavedDirty];
    };
    function checkDivergentArray(doc, path, array2) {
      const pop = doc.$populated(path, true);
      if (!pop && doc.$__.selected) {
        const top = path.split(".")[0];
        if (doc.$__.selected[top] && doc.$__.selected[top].$slice != null && utils2.isMongooseArray(array2)) {
          const atomics = array2[arrayAtomicsSymbol];
          if (atomics.$set) {
            return top;
          }
        }
        if (doc.$__.selected[top + ".$"]) {
          return top;
        }
      }
      if (!(pop && utils2.isMongooseArray(array2))) return;
      const check = pop.options.match || pop.options.options && Object.hasOwn(pop.options.options, "limit") || // 0 is not permitted
      pop.options.options?.skip || // 0 is permitted
      pop.options.select && // deselected _id?
      (pop.options.select._id === 0 || /\s?-_id\s?/.test(pop.options.select));
      if (check) {
        const atomics = array2[arrayAtomicsSymbol];
        if (utils2.hasOwnKeys(atomics) === false || atomics.$set || atomics.$pop) {
          return path;
        }
      }
    }
    function operand(self, where, delta, data, val, op) {
      op || (op = "$set");
      if (!delta[op]) delta[op] = {};
      delta[op][data.path] = val;
      if (self.$__schema.options.versionKey === false) return;
      if (shouldSkipVersioning(self, data.path)) return;
      if (VERSION_ALL === (VERSION_ALL & self.$__.version)) return;
      if (self.$__schema.options.optimisticConcurrency === true || Array.isArray(self.$__schema.options.optimisticConcurrency) || Array.isArray(self.$__schema.options.optimisticConcurrency?.exclude)) {
        return;
      }
      switch (op) {
        case "$set":
        case "$unset":
        case "$pop":
        case "$pull":
        case "$pullAll":
        case "$push":
        case "$addToSet":
        case "$inc":
          break;
        default:
          return;
      }
      if (op === "$push" || op === "$addToSet" || op === "$pullAll" || op === "$pull") {
        if (/\.\d+\.|\.\d+$/.test(data.path)) {
          self.$__.version = VERSION_ALL;
        } else {
          self.$__.version |= VERSION_INC;
        }
      } else if (/^\$p/.test(op)) {
        self.$__.version = VERSION_ALL;
      } else if (Array.isArray(val)) {
        self.$__.version = VERSION_ALL;
      } else if (/\.\d+\.|\.\d+$/.test(data.path)) {
        self.$__.version |= VERSION_WHERE;
      }
    }
    function handleAtomics(self, where, delta, data, value) {
      if (delta.$set?.[data.path]) {
        return;
      }
      if (typeof value.getAtomics === "function") {
        value.getAtomics().forEach(function(atomic) {
          const op2 = atomic[0];
          const val2 = atomic[1];
          operand(self, where, delta, data, val2, op2);
        });
        return;
      }
      if (typeof value.$__getAtomics === "function") {
        value.$__getAtomics().forEach(function(atomic) {
          const op2 = atomic[0];
          const val2 = atomic[1];
          operand(self, where, delta, data, val2, op2);
        });
        return;
      }
      const atomics = value[arrayAtomicsSymbol];
      const ops = Object.keys(atomics);
      let i = ops.length;
      let val;
      let op;
      if (i === 0) {
        if (utils2.isMongooseObject(value)) {
          value = value.toObject({ depopulate: 1, _isNested: true });
        } else if (value.valueOf) {
          value = value.valueOf();
        }
        return operand(self, where, delta, data, value);
      }
      function iter(mem) {
        return utils2.isMongooseObject(mem) ? mem.toObject({ depopulate: 1, _isNested: true }) : mem;
      }
      while (i--) {
        op = ops[i];
        val = atomics[op];
        if (utils2.isMongooseObject(val)) {
          val = val.toObject({ depopulate: true, transform: false, _isNested: true });
        } else if (Array.isArray(val)) {
          val = val.map(iter);
        } else if (val.valueOf) {
          val = val.valueOf();
        }
        if (op === "$addToSet") {
          val = { $each: val };
        }
        operand(self, where, delta, data, val, op);
      }
    }
    function shouldSkipVersioning(self, path) {
      const skipVersioning = self.$__schema.options.skipVersioning;
      if (!skipVersioning) return false;
      path = path.replace(/\.\d+\./, ".");
      return skipVersioning[path];
    }
    Document.prototype.$clone = function() {
      const Model = this.constructor;
      const clonedDoc = new Model();
      clonedDoc.$isNew = this.$isNew;
      if (this._doc) {
        clonedDoc._doc = clone(this._doc, { retainDocuments: true, parentDoc: clonedDoc });
      }
      if (this.$__) {
        const Cache = this.$__.constructor;
        const clonedCache = new Cache();
        for (const key of Object.getOwnPropertyNames(this.$__)) {
          if (key === "activePaths") {
            continue;
          }
          clonedCache[key] = clone(this.$__[key]);
        }
        Object.assign(
          clonedCache.activePaths,
          clone({ ...this.$__.activePaths })
        );
        clonedDoc.$__ = clonedCache;
      }
      return clonedDoc;
    };
    Document.prototype.$createModifiedPathsSnapshot = function $createModifiedPathsSnapshot() {
      const subdocSnapshot = /* @__PURE__ */ new WeakMap();
      if (!this.$isSubdocument) {
        const subdocs = this.$getAllSubdocs();
        for (const child of subdocs) {
          subdocSnapshot.set(child, child.$__.activePaths.clone());
        }
      }
      return new ModifiedPathsSnapshot(
        subdocSnapshot,
        this.$__.activePaths.clone(),
        this.$__.version
      );
    };
    Document.prototype.$restoreModifiedPathsSnapshot = function $restoreModifiedPathsSnapshot(snapshot) {
      this.$__.activePaths = snapshot.activePaths.clone();
      this.$__.version = snapshot.version;
      if (!this.$isSubdocument) {
        const subdocs = this.$getAllSubdocs();
        for (const child of subdocs) {
          if (snapshot.subdocSnapshot.has(child)) {
            child.$__.activePaths = snapshot.subdocSnapshot.get(child);
          }
        }
      }
      return this;
    };
    Document.prototype.$clearModifiedPaths = function $clearModifiedPaths() {
      this.$__.activePaths.clear("modify");
      this.$__.activePaths.clear("init");
      this.$__.version = 0;
      if (!this.$isSubdocument) {
        const subdocs = this.$getAllSubdocs();
        for (const child of subdocs) {
          child.$clearModifiedPaths();
        }
      }
      return this;
    };
    Document.prototype.$__hasOnlyPrimitiveValues = function $__hasOnlyPrimitiveValues() {
      if (this.$__.populated) {
        return false;
      }
      const doc = this._doc;
      for (const key in doc) {
        const v = doc[key];
        if (v == null || typeof v !== "object") {
          continue;
        }
        if (Array.isArray(v)) {
          return false;
        }
        if (v instanceof Date || v instanceof Boolean || v instanceof Number || v instanceof String) {
          continue;
        }
        const bsontype = v._bsontype;
        if (bsontype === "ObjectId" || bsontype === "Decimal128") {
          continue;
        }
        return false;
      }
      return true;
    };
    Document.prototype._applyVersionIncrement = function _applyVersionIncrement() {
      if (!this.$__.version) return;
      const doIncrement = VERSION_INC === (VERSION_INC & this.$__.version);
      this.$__.version = void 0;
      if (doIncrement) {
        const key = this.$__schema.options.versionKey;
        const version2 = this.$__getValue(key) || 0;
        this.$__setValue(key, version2 + 1);
      }
    };
    Document.VERSION_WHERE = VERSION_WHERE;
    Document.VERSION_INC = VERSION_INC;
    Document.VERSION_ALL = VERSION_ALL;
    Document.ValidationError = ValidationError;
    module.exports = Document;
  })(document);
  return document.exports;
}
var hasRequiredUtils;
function requireUtils() {
  if (hasRequiredUtils) return utils;
  hasRequiredUtils = 1;
  (function(exports) {
    const UUID = requireBson().UUID;
    const ms = requireMs();
    const mpath = requireMpath();
    const ObjectId = requireObjectid$1();
    const PopulateOptions = requirePopulateOptions();
    const clone = requireClone();
    const immediate2 = requireImmediate();
    const isObject2 = requireIsObject();
    const isMongooseArray2 = requireIsMongooseArray();
    const isMongooseDocumentArray2 = requireIsMongooseDocumentArray();
    const isBsonType = requireIsBsonType();
    const isPOJO2 = requireIsPOJO();
    const getFunctionName2 = requireGetFunctionName();
    const isMongooseObject2 = requireIsMongooseObject();
    const schemaMerge = requireMerge();
    const specialProperties2 = requireSpecialProperties();
    const { trustedSymbol } = requireTrusted();
    let Document;
    exports.specialProperties = specialProperties2;
    exports.isMongooseArray = isMongooseArray2.isMongooseArray;
    exports.isMongooseDocumentArray = isMongooseDocumentArray2.isMongooseDocumentArray;
    exports.registerMongooseArray = isMongooseArray2.registerMongooseArray;
    exports.registerMongooseDocumentArray = isMongooseDocumentArray2.registerMongooseDocumentArray;
    const oneSpaceRE = /\s/;
    const manySpaceRE = /\s+/;
    exports.toCollectionName = function(name, pluralize2) {
      if (name === "system.profile") {
        return name;
      }
      if (name === "system.indexes") {
        return name;
      }
      if (typeof pluralize2 === "function") {
        if (typeof name !== "string") {
          throw new TypeError("Collection name must be a string");
        }
        if (name.length === 0) {
          throw new TypeError("Collection name cannot be empty");
        }
        return pluralize2(name);
      }
      return name;
    };
    exports.deepEqual = function deepEqual(a, b) {
      if (a === b) {
        return true;
      }
      if (typeof a !== "object" || typeof b !== "object") {
        return a === b;
      }
      if (a instanceof Date && b instanceof Date) {
        return a.getTime() === b.getTime();
      }
      if (isBsonType(a, "ObjectId") && isBsonType(b, "ObjectId") || isBsonType(a, "Decimal128") && isBsonType(b, "Decimal128")) {
        return a.toString() === b.toString();
      }
      if (a instanceof RegExp && b instanceof RegExp) {
        return a.source === b.source && a.ignoreCase === b.ignoreCase && a.multiline === b.multiline && a.global === b.global && a.dotAll === b.dotAll && a.unicode === b.unicode && a.sticky === b.sticky && a.hasIndices === b.hasIndices;
      }
      if (a == null || b == null) {
        return false;
      }
      if (a.prototype !== b.prototype) {
        return false;
      }
      if (a instanceof Map || b instanceof Map) {
        if (!(a instanceof Map) || !(b instanceof Map)) {
          return false;
        }
        return deepEqual(Array.from(a.keys()), Array.from(b.keys())) && deepEqual(Array.from(a.values()), Array.from(b.values()));
      }
      if (a instanceof Set || b instanceof Set) {
        if (!(a instanceof Set) || !(b instanceof Set)) {
          return false;
        }
        if (a.size !== b.size) {
          return false;
        }
        return deepEqual(Array.from(a.values()), Array.from(b.values()));
      }
      if (a instanceof Number && b instanceof Number) {
        return a.valueOf() === b.valueOf();
      }
      if (Buffer.isBuffer(a)) {
        return exports.buffer.areEqual(a, b);
      }
      if (Array.isArray(a) || Array.isArray(b)) {
        if (!Array.isArray(a) || !Array.isArray(b)) {
          return false;
        }
        const len = a.length;
        if (len !== b.length) {
          return false;
        }
        for (let i = 0; i < len; ++i) {
          if (!deepEqual(a[i], b[i])) {
            return false;
          }
        }
        return true;
      }
      if (a.$__ != null) {
        a = a._doc;
      } else if (isMongooseObject2(a)) {
        a = a.toObject();
      }
      if (b.$__ != null) {
        b = b._doc;
      } else if (isMongooseObject2(b)) {
        b = b.toObject();
      }
      const ka = Object.keys(a);
      const kb = Object.keys(b);
      const kaLength = ka.length;
      if (kaLength !== kb.length) {
        return false;
      }
      for (let i = kaLength - 1; i >= 0; i--) {
        if (ka[i] !== kb[i]) {
          return false;
        }
      }
      for (const key of ka) {
        if (!deepEqual(a[key], b[key])) {
          return false;
        }
      }
      return true;
    };
    exports.last = function(arr) {
      if (arr == null) {
        return void 0;
      }
      if (arr.length > 0) {
        return arr[arr.length - 1];
      }
      return void 0;
    };
    exports.cloneArrays = function cloneArrays(arr) {
      if (!Array.isArray(arr)) {
        return arr;
      }
      return arr.map((el) => exports.cloneArrays(el));
    };
    exports.omit = function omit(obj, keys) {
      if (keys == null) {
        return Object.assign({}, obj);
      }
      if (!Array.isArray(keys)) {
        keys = [keys];
      }
      const ret = Object.assign({}, obj);
      for (const key of keys) {
        delete ret[key];
      }
      return ret;
    };
    exports.clonePOJOsAndArrays = function clonePOJOsAndArrays(val) {
      if (val == null) {
        return val;
      }
      if (val.$__ != null) {
        return val;
      }
      if (isPOJO2(val)) {
        val = { ...val };
        for (const key of Object.keys(val)) {
          val[key] = exports.clonePOJOsAndArrays(val[key]);
        }
        return val;
      }
      if (Array.isArray(val)) {
        val = [...val];
        for (let i = 0; i < val.length; ++i) {
          val[i] = exports.clonePOJOsAndArrays(val[i]);
        }
        return val;
      }
      return val;
    };
    exports.merge = function merge2(to, from, options2, path) {
      options2 = options2 || {};
      if (from == null) {
        return to;
      }
      const keys = Object.keys(from);
      let i = 0;
      const len = keys.length;
      let key;
      if (from[trustedSymbol]) {
        to[trustedSymbol] = from[trustedSymbol];
      }
      path = path || "";
      const omitNested = options2.omitNested || {};
      while (i < len) {
        key = keys[i++];
        if (options2.omit && options2.omit[key]) {
          continue;
        }
        if (omitNested[path]) {
          continue;
        }
        if (specialProperties2.has(key)) {
          continue;
        }
        if (to[key] == null) {
          to[key] = exports.clonePOJOsAndArrays(from[key]);
        } else if (exports.isObject(from[key])) {
          if (!exports.isObject(to[key])) {
            to[key] = {};
          }
          if (from[key] != null) {
            if (options2.isDiscriminatorSchemaMerge && (from[key].$isSingleNested && to[key].$isMongooseDocumentArray || from[key].$isMongooseDocumentArray && to[key].$isSingleNested)) {
              continue;
            } else if (from[key].instanceOfSchema) {
              if (to[key].instanceOfSchema) {
                schemaMerge(to[key], from[key].clone(), options2.isDiscriminatorSchemaMerge);
              } else {
                to[key] = from[key].clone();
              }
              continue;
            } else if (isBsonType(from[key], "ObjectId")) {
              to[key] = new ObjectId(from[key]);
              continue;
            }
          }
          merge2(to[key], from[key], options2, path ? path + "." + key : key);
        } else if (options2.overwrite) {
          to[key] = from[key];
        }
      }
      return to;
    };
    exports.toObject = function toObject(obj) {
      Document || (Document = requireDocument());
      let ret;
      if (obj == null) {
        return obj;
      }
      if (obj instanceof Document) {
        return obj.toObject();
      }
      if (Array.isArray(obj)) {
        ret = [];
        for (const doc of obj) {
          ret.push(toObject(doc));
        }
        return ret;
      }
      if (exports.isPOJO(obj)) {
        ret = {};
        if (obj[trustedSymbol]) {
          ret[trustedSymbol] = obj[trustedSymbol];
        }
        for (const k of Object.keys(obj)) {
          if (specialProperties2.has(k)) {
            continue;
          }
          ret[k] = toObject(obj[k]);
        }
        return ret;
      }
      return obj;
    };
    exports.isObject = isObject2;
    exports.isPOJO = requireIsPOJO();
    exports.isNonBuiltinObject = function isNonBuiltinObject(val) {
      return typeof val === "object" && !Array.isArray(val) && !(val instanceof Date) && !(val instanceof Boolean) && !(val instanceof Number) && !(val instanceof String) && !exports.isMongooseType(val) && !(val instanceof UUID) && val != null;
    };
    exports.isEmptyObject = function isEmptyObject(val) {
      if (val == null || typeof val !== "object") {
        return false;
      }
      return exports.hasOwnKeys(val) === false;
    };
    exports.hasOwnKeys = function hasOwnKeys(obj) {
      for (const key in obj) {
        if (Object.prototype.hasOwnProperty.call(obj, key)) {
          return true;
        }
      }
      return false;
    };
    exports.hasKey = function hasKey(obj, key) {
      const props = Object.keys(obj);
      for (const prop of props) {
        if (prop === key) {
          return true;
        }
        if (exports.isPOJO(obj[prop]) && exports.hasKey(obj[prop], key)) {
          return true;
        }
      }
      return false;
    };
    exports.tick = function tick(callback) {
      if (typeof callback !== "function") {
        return;
      }
      return function() {
        try {
          callback.apply(this, arguments);
        } catch (err) {
          immediate2(function() {
            throw err;
          });
        }
      };
    };
    exports.isMongooseType = function(v) {
      return isBsonType(v, "ObjectId") || isBsonType(v, "Decimal128") || v instanceof Buffer;
    };
    exports.isMongooseObject = isMongooseObject2;
    exports.expires = function expires(object) {
      if (!(object && object.constructor.name === "Object")) {
        return;
      }
      if (!("expires" in object)) {
        return;
      }
      object.expireAfterSeconds = typeof object.expires !== "string" ? object.expires : Math.round(ms(object.expires) / 1e3);
      delete object.expires;
    };
    exports.populate = function populate(path, select, model2, match, options2, subPopulate, justOne, count) {
      let obj = null;
      if (arguments.length === 1) {
        if (path instanceof PopulateOptions) {
          path._docs = {};
          path._childDocs = [];
          return [path];
        }
        if (Array.isArray(path)) {
          const singles = makeSingles(path);
          return singles.map((o) => exports.populate(o)[0]);
        }
        if (exports.isObject(path)) {
          obj = Object.assign({}, path);
        } else {
          obj = { path };
        }
      } else if (typeof model2 === "object") {
        obj = {
          path,
          select,
          match: model2,
          options: match
        };
      } else {
        obj = {
          path,
          select,
          model: model2,
          match,
          options: options2,
          populate: subPopulate,
          justOne,
          count
        };
      }
      if (typeof obj.path !== "string" && !(Array.isArray(obj.path) && obj.path.every((el) => typeof el === "string"))) {
        throw new TypeError("utils.populate: invalid path. Expected string or array of strings. Got typeof `" + typeof path + "`");
      }
      return _populateObj(obj);
      function makeSingles(arr) {
        const ret = [];
        arr.forEach(function(obj2) {
          if (oneSpaceRE.test(obj2.path)) {
            const paths = obj2.path.split(manySpaceRE);
            paths.forEach(function(p) {
              const copy = Object.assign({}, obj2);
              copy.path = p;
              ret.push(copy);
            });
          } else {
            ret.push(obj2);
          }
        });
        return ret;
      }
    };
    function _populateObj(obj) {
      if (Array.isArray(obj.populate)) {
        const ret2 = [];
        obj.populate.forEach(function(obj2) {
          if (oneSpaceRE.test(obj2.path)) {
            const copy = Object.assign({}, obj2);
            const paths2 = copy.path.split(manySpaceRE);
            paths2.forEach(function(p) {
              copy.path = p;
              ret2.push(exports.populate(copy)[0]);
            });
          } else {
            ret2.push(exports.populate(obj2)[0]);
          }
        });
        obj.populate = exports.populate(ret2);
      } else if (obj.populate != null && typeof obj.populate === "object") {
        obj.populate = exports.populate(obj.populate);
      }
      const ret = [];
      const paths = oneSpaceRE.test(obj.path) ? obj.path.split(manySpaceRE) : Array.isArray(obj.path) ? obj.path : [obj.path];
      if (obj.options != null) {
        obj.options = clone(obj.options);
      }
      for (const path of paths) {
        ret.push(new PopulateOptions(Object.assign({}, obj, { path })));
      }
      return ret;
    }
    exports.getValue = function(path, obj, map2) {
      return mpath.get(path, obj, getValueLookup, map2);
    };
    const mapGetterOptions = Object.freeze({ getters: false });
    function getValueLookup(obj, part) {
      if (part === "$*" && obj instanceof Map) {
        return obj;
      }
      let _from = obj?._doc || obj;
      if (_from?.isMongooseArrayProxy) {
        _from = _from.__array;
      }
      return _from instanceof Map ? _from.get(part, mapGetterOptions) : _from[part];
    }
    exports.setValue = function(path, val, obj, map2, _copying) {
      mpath.set(path, val, obj, "_doc", map2, _copying);
    };
    exports.object = {};
    exports.object.vals = function vals(o) {
      if (o == null) {
        return [];
      }
      const keys = Object.keys(o);
      let i = keys.length;
      const ret = [];
      while (i--) {
        ret.push(o[keys[i]]);
      }
      return ret;
    };
    exports.isNullOrUndefined = function(val) {
      return val == null;
    };
    exports.array = {};
    exports.array.flatten = function flatten(arr, filter, ret) {
      ret || (ret = []);
      arr.forEach(function(item) {
        if (Array.isArray(item)) {
          flatten(item, filter, ret);
        } else {
          if (!filter || filter(item)) {
            ret.push(item);
          }
        }
      });
      return ret;
    };
    exports.hasUserDefinedProperty = function(obj, key) {
      if (obj == null) {
        return false;
      }
      if (Array.isArray(key)) {
        for (const k of key) {
          if (exports.hasUserDefinedProperty(obj, k)) {
            return true;
          }
        }
        return false;
      }
      if (Object.hasOwn(obj, key)) {
        return true;
      }
      if (typeof obj === "object" && key in obj) {
        const v = obj[key];
        return v !== Object.prototype[key] && v !== Array.prototype[key];
      }
      return false;
    };
    const MAX_ARRAY_INDEX = Math.pow(2, 32) - 1;
    exports.isArrayIndex = function(val) {
      if (typeof val === "number") {
        return val >= 0 && val <= MAX_ARRAY_INDEX;
      }
      if (typeof val === "string") {
        if (!/^\d+$/.test(val)) {
          return false;
        }
        val = +val;
        return val >= 0 && val <= MAX_ARRAY_INDEX;
      }
      return false;
    };
    exports.array.unique = function(arr) {
      const primitives = /* @__PURE__ */ new Set();
      const ids = /* @__PURE__ */ new Set();
      const ret = [];
      for (const item of arr) {
        if (typeof item === "number" || typeof item === "string" || item == null) {
          if (primitives.has(item)) {
            continue;
          }
          ret.push(item);
          primitives.add(item);
        } else if (isBsonType(item, "ObjectId")) {
          const idStr = item.toString();
          if (ids.has(idStr)) {
            continue;
          }
          ret.push(item);
          ids.add(idStr);
        } else {
          ret.push(item);
        }
      }
      return ret;
    };
    exports.buffer = {};
    exports.buffer.areEqual = function(a, b) {
      if (!Buffer.isBuffer(a)) {
        return false;
      }
      if (!Buffer.isBuffer(b)) {
        return false;
      }
      return a.equals(b);
    };
    exports.getFunctionName = getFunctionName2;
    exports.decorate = function(destination, source) {
      for (const key in source) {
        if (specialProperties2.has(key)) {
          continue;
        }
        destination[key] = source[key];
      }
    };
    exports.mergeClone = function(to, fromObj) {
      if (isMongooseObject2(fromObj)) {
        fromObj = fromObj.toObject({
          transform: false,
          virtuals: false,
          depopulate: true,
          getters: false,
          flattenDecimals: false
        });
      }
      const keys = Object.keys(fromObj);
      const len = keys.length;
      let i = 0;
      let key;
      while (i < len) {
        key = keys[i++];
        if (specialProperties2.has(key)) {
          continue;
        }
        if (typeof to[key] === "undefined") {
          to[key] = clone(fromObj[key], {
            transform: false,
            virtuals: false,
            depopulate: true,
            getters: false,
            flattenDecimals: false
          });
        } else {
          let val = fromObj[key];
          if (val?.valueOf && !(val instanceof Date)) {
            val = val.valueOf();
          }
          if (exports.isObject(val)) {
            let obj = val;
            if (isMongooseObject2(val) && !val.isMongooseBuffer) {
              obj = obj.toObject({
                transform: false,
                virtuals: false,
                depopulate: true,
                getters: false,
                flattenDecimals: false
              });
            }
            if (val.isMongooseBuffer) {
              obj = Buffer.from(obj);
            }
            exports.mergeClone(to[key], obj);
          } else {
            to[key] = clone(val, {
              flattenDecimals: false
            });
          }
        }
      }
    };
    exports.each = function(arr, fn) {
      for (const item of arr) {
        fn(item);
      }
    };
    exports.renameObjKey = function(oldObj, oldKey, newKey) {
      const keys = Object.keys(oldObj);
      return keys.reduce(
        (acc, val) => {
          if (val === oldKey) {
            acc[newKey] = oldObj[oldKey];
          } else {
            acc[val] = oldObj[val];
          }
          return acc;
        },
        {}
      );
    };
    exports.getOption = function(name) {
      const sources = Array.prototype.slice.call(arguments, 1);
      for (const source of sources) {
        if (source == null) {
          continue;
        }
        if (source[name] != null) {
          return source[name];
        }
      }
      return null;
    };
    exports.noop = function() {
    };
    exports.errorToPOJO = function errorToPOJO(error2) {
      const isError = error2 instanceof Error;
      if (!isError) {
        throw new Error("`error` must be `instanceof Error`.");
      }
      const ret = {};
      for (const properyName of Object.getOwnPropertyNames(error2)) {
        ret[properyName] = error2[properyName];
      }
      return ret;
    };
    exports.warn = function warn(message) {
      return process.emitWarning(message, { code: "MONGOOSE" });
    };
    exports.injectTimestampsOption = function injectTimestampsOption(writeOperation, timestampsOption) {
      if (timestampsOption == null) {
        return;
      }
      writeOperation.timestamps = timestampsOption;
    };
  })(utils);
  return utils;
}
var hasRequiredSchemaType;
function requireSchemaType() {
  if (hasRequiredSchemaType) return schemaType.exports;
  hasRequiredSchemaType = 1;
  (function(module, exports) {
    const MongooseError = requireError();
    const SchemaTypeOptions = requireSchemaTypeOptions();
    const $exists = requireExists();
    const $type = requireType();
    const clone = requireClone();
    const handleImmutable2 = requireHandleImmutable$1();
    const isAsyncFunction2 = requireIsAsyncFunction();
    const isSimpleValidator2 = requireIsSimpleValidator();
    const schemaTypeSymbol = requireSymbols$1().schemaTypeSymbol;
    const utils2 = requireUtils();
    const validatorErrorSymbol = requireSymbols$1().validatorErrorSymbol;
    const documentIsModified = requireSymbols$1().documentIsModified;
    const populateModelSymbol = requireSymbols$1().populateModelSymbol;
    const CastError = MongooseError.CastError;
    const ValidatorError = MongooseError.ValidatorError;
    const setOptionsForDefaults = { _skipMarkModified: true };
    function SchemaType(path, options2, instance, parentSchema) {
      this[schemaTypeSymbol] = true;
      this.path = path;
      this.instance = instance;
      this.schemaName = this.constructor.schemaName;
      this.validators = [];
      this.getters = Object.hasOwn(this.constructor, "getters") ? this.constructor.getters.slice() : [];
      this.setters = Object.hasOwn(this.constructor, "setters") ? this.constructor.setters.slice() : [];
      this.splitPath();
      options2 = options2 || {};
      const defaultOptions = this.constructor.defaultOptions || {};
      const defaultOptionsKeys = Object.keys(defaultOptions);
      for (const option of defaultOptionsKeys) {
        if (option === "validate") {
          this.validate(defaultOptions.validate);
        } else if (Object.hasOwn(defaultOptions, option) && !Object.hasOwn(options2, option)) {
          options2[option] = defaultOptions[option];
        }
      }
      if (options2.select == null) {
        delete options2.select;
      }
      const Options = this.OptionsConstructor || SchemaTypeOptions;
      this.options = new Options(options2);
      this.parentSchema = parentSchema;
      this._index = null;
      if (utils2.hasUserDefinedProperty(this.options, "immutable")) {
        this.$immutable = this.options.immutable;
        handleImmutable2(this);
      }
      const keys = Object.keys(this.options);
      for (const prop of keys) {
        if (prop === "cast") {
          if (Array.isArray(this.options[prop])) {
            this.castFunction.apply(this, this.options[prop]);
          } else {
            this.castFunction(this.options[prop]);
          }
          continue;
        }
        if (utils2.hasUserDefinedProperty(this.options, prop) && typeof this[prop] === "function") {
          if (prop === "index" && this._index) {
            if (options2.index === false) {
              const index = this._index;
              if (typeof index === "object" && index != null) {
                if (index.unique) {
                  throw new MongooseError('Path "' + this.path + '" may not have `index` set to false and `unique` set to true');
                }
                if (index.sparse) {
                  throw new MongooseError('Path "' + this.path + '" may not have `index` set to false and `sparse` set to true');
                }
              }
              this._index = false;
            }
            continue;
          }
          const val = options2[prop];
          if (prop === "default") {
            this.default(val);
            continue;
          }
          const opts = Array.isArray(val) ? val : [val];
          this[prop].apply(this, opts);
        }
      }
      Object.defineProperty(this, "$$context", {
        enumerable: false,
        configurable: false,
        writable: true,
        value: null
      });
    }
    SchemaType.prototype.OptionsConstructor = SchemaTypeOptions;
    SchemaType.prototype.path;
    SchemaType.prototype.toJSON = function toJSON() {
      const res = { ...this };
      delete res.parentSchema;
      return res;
    };
    SchemaType.prototype._createJSONSchemaTypeDefinition = function _createJSONSchemaTypeDefinition(type2, bsonType, options2) {
      const useBsonType = options2?.useBsonType;
      const isRequired = options2?._overrideRequired ?? (this.options.required == null ? options2?._defaultRequired === true || this.path === "_id" : this.options.required && typeof this.options.required !== "function");
      const allowNull = this.options.allowNull !== false;
      if (useBsonType) {
        if (isRequired || !allowNull) {
          return { bsonType };
        }
        return { bsonType: [bsonType, "null"] };
      }
      if (isRequired || !allowNull) {
        return { type: type2 };
      }
      return { type: [type2, "null"] };
    };
    SchemaType.prototype._addJSONSchemaEnum = function _addJSONSchemaEnum(definition) {
      if (!Array.isArray(this.enumValues) || this.enumValues.length === 0) {
        return definition;
      }
      const allowsNull = Array.isArray(definition.type ?? definition.bsonType);
      definition.enum = allowsNull ? [...this.enumValues, null] : [...this.enumValues];
      return definition;
    };
    SchemaType.prototype.validators;
    SchemaType.prototype.isRequired;
    SchemaType.prototype.splitPath = function() {
      if (this._presplitPath != null) {
        return this._presplitPath;
      }
      if (this.path == null) {
        return void 0;
      }
      this._presplitPath = this.path.indexOf(".") === -1 ? [this.path] : this.path.split(".");
      return this._presplitPath;
    };
    SchemaType.cast = function cast2(caster) {
      if (arguments.length === 0) {
        return this._cast;
      }
      if (caster === false) {
        caster = (v) => v;
      }
      this._cast = caster;
      return this._cast;
    };
    SchemaType.prototype.castFunction = function castFunction(caster, message) {
      if (arguments.length === 0) {
        return this._castFunction;
      }
      if (caster === false) {
        caster = this.constructor._defaultCaster || ((v) => v);
      }
      if (typeof caster === "string") {
        this._castErrorMessage = caster;
        return this._castFunction;
      }
      if (caster != null) {
        this._castFunction = caster;
      }
      if (message != null) {
        this._castErrorMessage = message;
      }
      return this._castFunction;
    };
    SchemaType.prototype.cast = function cast2() {
      throw new MongooseError("Base SchemaType class does not implement a `cast()` function");
    };
    SchemaType.set = function set(option, value) {
      if (!Object.hasOwn(this, "defaultOptions")) {
        this.defaultOptions = Object.assign({}, this.defaultOptions);
      }
      this.defaultOptions[option] = value;
    };
    SchemaType.get = function(getter) {
      this.getters = Object.hasOwn(this, "getters") ? this.getters : [];
      this.getters.push(getter);
    };
    SchemaType.prototype.default = function(val) {
      if (arguments.length === 1) {
        if (val === void 0) {
          this.defaultValue = void 0;
          return void 0;
        }
        if (val?.instanceOfSchema) {
          throw new MongooseError("Cannot set default value of path `" + this.path + "` to a mongoose Schema instance.");
        }
        this.defaultValue = val;
        return this.defaultValue;
      } else if (arguments.length > 1) {
        this.defaultValue = [...arguments];
      }
      return this.defaultValue;
    };
    SchemaType.prototype.index = function(options2) {
      this._index = options2;
      utils2.expires(this._index);
      return this;
    };
    SchemaType.prototype.unique = function unique(value, message) {
      if (this._index === false) {
        if (!value) {
          return;
        }
        throw new MongooseError('Path "' + this.path + '" may not have `index` set to false and `unique` set to true');
      }
      if (!Object.hasOwn(this.options, "index") && value === false) {
        return this;
      }
      if (this._index == null || this._index === true) {
        this._index = {};
      } else if (typeof this._index === "string") {
        this._index = { type: this._index };
      }
      this._index.unique = !!value;
      if (typeof message === "string") {
        this._duplicateKeyErrorMessage = message;
      }
      return this;
    };
    SchemaType.prototype.text = function(bool) {
      if (this._index === false) {
        if (!bool) {
          return this;
        }
        throw new MongooseError('Path "' + this.path + '" may not have `index` set to false and `text` set to true');
      }
      if (!Object.hasOwn(this.options, "index") && bool === false) {
        return this;
      }
      if (this._index === null || this._index === void 0 || typeof this._index === "boolean") {
        this._index = {};
      } else if (typeof this._index === "string") {
        this._index = { type: this._index };
      }
      this._index.text = bool;
      return this;
    };
    SchemaType.prototype.sparse = function(bool) {
      if (this._index === false) {
        if (!bool) {
          return this;
        }
        throw new MongooseError('Path "' + this.path + '" may not have `index` set to false and `sparse` set to true');
      }
      if (!Object.hasOwn(this.options, "index") && bool === false) {
        return this;
      }
      if (this._index == null || typeof this._index === "boolean") {
        this._index = {};
      } else if (typeof this._index === "string") {
        this._index = { type: this._index };
      }
      this._index.sparse = bool;
      return this;
    };
    SchemaType.prototype.immutable = function(bool) {
      this.$immutable = bool;
      handleImmutable2(this);
      return this;
    };
    SchemaType.prototype.transform = function(fn) {
      this.options.transform = fn;
      return this;
    };
    SchemaType.prototype.set = function(fn) {
      if (typeof fn !== "function") {
        throw new TypeError("A setter must be a function.");
      }
      this.setters.push(fn);
      return this;
    };
    SchemaType.prototype.get = function(fn) {
      if (typeof fn !== "function") {
        throw new TypeError("A getter must be a function.");
      }
      this.getters.push(fn);
      return this;
    };
    SchemaType.prototype.validateAll = function(validators) {
      for (let i = 0; i < validators.length; i++) {
        this.validate(validators[i]);
      }
      return this;
    };
    SchemaType.prototype.validate = function(obj, message, type2) {
      if (typeof obj === "function" || obj && utils2.getFunctionName(obj.constructor) === "RegExp") {
        let properties;
        if (typeof message === "function") {
          properties = { validator: obj, message };
          properties.type = type2 || "user defined";
        } else if (message instanceof Object && !type2) {
          properties = isSimpleValidator2(message) ? Object.assign({}, message) : clone(message);
          if (!properties.message) {
            properties.message = properties.msg;
          }
          properties.validator = obj;
          properties.type = properties.type || "user defined";
        } else {
          if (message == null) {
            message = MongooseError.messages.general.default;
          }
          if (!type2) {
            type2 = "user defined";
          }
          properties = { message, type: type2, validator: obj };
        }
        this.validators.push(properties);
        return this;
      }
      let i;
      let length;
      let arg;
      for (i = 0, length = arguments.length; i < length; i++) {
        arg = arguments[i];
        if (!utils2.isPOJO(arg)) {
          const msg = "Invalid validator. Received (" + typeof arg + ") " + arg + ". See https://mongoosejs.com/docs/api/schematype.html#SchemaType.prototype.validate()";
          throw new MongooseError(msg);
        }
        this.validate(arg.validator, arg);
      }
      return this;
    };
    SchemaType.prototype.required = function(required, message) {
      let customOptions = {};
      if (arguments.length > 0 && required == null) {
        this.validators = this.validators.filter(function(v) {
          return v.validator !== this.requiredValidator;
        }, this);
        this.isRequired = false;
        delete this.originalRequiredValue;
        return this;
      }
      if (typeof required === "object") {
        customOptions = required;
        message = customOptions.message || message;
        required = required.isRequired;
      }
      if (required === false) {
        this.validators = this.validators.filter(function(v) {
          return v.validator !== this.requiredValidator;
        }, this);
        this.isRequired = false;
        delete this.originalRequiredValue;
        return this;
      }
      const _this = this;
      this.isRequired = true;
      this.originalRequiredValue = required;
      if (typeof this.originalRequiredValue !== "function" && (utils2.hasUserDefinedProperty(this.options, "allowNull") || this.allowNullValidator != null)) {
        throw new MongooseError('Path "' + this.path + '" may not have `allowNull` specified when `required` is true');
      }
      this.requiredValidator = function(v) {
        const cachedRequired = this?.$__?.cachedRequired;
        if (cachedRequired != null && !this.$__isSelected(_this.path) && !this[documentIsModified](_this.path)) {
          return true;
        }
        if (cachedRequired != null && _this.path in cachedRequired) {
          const res = cachedRequired[_this.path] ? _this.checkRequired(v, this) : true;
          delete cachedRequired[_this.path];
          return res;
        } else if (typeof required === "function") {
          return required.apply(this) ? _this.checkRequired(v, this) : true;
        }
        return _this.checkRequired(v, this);
      };
      if (typeof required === "string") {
        message = required;
        required = void 0;
      }
      const msg = message || MongooseError.messages.general.required;
      this.validators.unshift(Object.assign({}, customOptions, {
        validator: this.requiredValidator,
        message: msg,
        type: "required"
      }));
      return this;
    };
    SchemaType.prototype.allowNull = function(allowNull) {
      if (arguments.length > 0 && this.isRequired && typeof this.originalRequiredValue !== "function") {
        throw new MongooseError('Path "' + this.path + '" may not have `allowNull` specified when `required` is true');
      }
      this.validators = this.validators.filter(function(v) {
        return v.validator !== this.allowNullValidator;
      }, this);
      if (allowNull !== false) {
        delete this.options.allowNull;
        delete this.allowNullValidator;
        return this;
      }
      this.options.allowNull = false;
      this.allowNullValidator = function(v) {
        return v !== null;
      };
      this.validators.push({
        validator: this.allowNullValidator,
        message: MongooseError.messages.general.allowNull,
        type: "allowNull"
      });
      return this;
    };
    SchemaType.prototype.ref = function(ref) {
      this.options.ref = ref;
      return this;
    };
    SchemaType.prototype.getDefault = function getDefault(parentDoc, init, options2) {
      let ret;
      if (this.defaultValue == null) {
        return this.defaultValue;
      }
      if (typeof this.defaultValue === "function") {
        const context = options2?.context ?? parentDoc;
        if (this.defaultValue === Date.now || this.defaultValue === Array || this.defaultValue.name === "ObjectId" || this.defaultValue.name === "ObjectID") {
          ret = this.defaultValue.call(context);
        } else {
          ret = this.defaultValue.call(context, context);
        }
      } else {
        ret = this.defaultValue;
      }
      if (ret != null) {
        if (typeof ret === "object" && ret != null && ret._bsontype !== "ObjectId" && !this.options?.shared) {
          ret = clone(ret);
        }
        if (options2?.skipCast) {
          return this._applySetters(ret, parentDoc);
        }
        const casted = this.applySetters(ret, parentDoc, init, void 0, setOptionsForDefaults);
        if (casted && !Array.isArray(casted) && casted.$isSingleNested) {
          casted.$__parent = parentDoc;
        }
        return casted;
      }
      return ret;
    };
    SchemaType.prototype._applySetters = function(value, scope, init, priorVal, options2) {
      let v = value;
      if (init) {
        return v;
      }
      const setters = this.setters;
      for (let i = setters.length - 1; i >= 0; i--) {
        v = setters[i].call(scope, v, priorVal, this, options2);
      }
      return v;
    };
    SchemaType.prototype._castNullish = function _castNullish(v) {
      return v;
    };
    SchemaType.prototype.applySetters = function(value, scope, init, priorVal, options2) {
      let v = this._applySetters(value, scope, init, priorVal, options2);
      if (v == null) {
        return this._castNullish(v);
      }
      v = this.cast(v, scope, init, priorVal, options2);
      return v;
    };
    SchemaType.prototype.applyGetters = function(value, scope) {
      let v = value;
      const getters = this.getters;
      const len = getters.length;
      if (len === 0) {
        return v;
      }
      for (let i = 0; i < len; ++i) {
        v = getters[i].call(scope, v, this);
      }
      return v;
    };
    SchemaType.prototype.select = function select(val) {
      this.selected = !!val;
      return this;
    };
    SchemaType.prototype.doValidate = async function doValidate(value, scope, options2) {
      let err = false;
      const path = this.path;
      const validators = this.validators.filter((v) => typeof v === "object" && v !== null);
      if (!validators.length) {
        return;
      }
      const promises = [];
      for (let i = 0, len = validators.length; i < len; ++i) {
        const v = validators[i];
        const validator2 = v.validator;
        let ok;
        const validatorProperties = isSimpleValidator2(v) ? Object.assign({}, v) : clone(v);
        validatorProperties.path = options2?.path || path;
        validatorProperties.fullPath = this.$fullPath;
        validatorProperties.value = value;
        if (typeof value === "string") {
          validatorProperties.length = value.length;
        }
        if (value === void 0 && validator2 !== this.requiredValidator) {
          continue;
        }
        if (validator2 instanceof RegExp) {
          ok = validator2.test(value);
          if (ok === false) {
            const ErrorConstructor = validatorProperties.ErrorConstructor || ValidatorError;
            err = new ErrorConstructor(validatorProperties, scope);
            err[validatorErrorSymbol] = true;
            throw err;
          }
          continue;
        } else if (typeof validator2 !== "function") {
          continue;
        }
        try {
          if (validatorProperties.propsParameter) {
            ok = validator2.call(scope, value, validatorProperties);
          } else {
            ok = validator2.call(scope, value);
          }
        } catch (error2) {
          ok = false;
          validatorProperties.reason = error2;
          if (error2.message) {
            validatorProperties.message = error2.message;
          }
        }
        if (typeof ok?.then === "function") {
          promises.push(
            ok.then(
              function(ok2) {
                if (ok2 === false) {
                  const ErrorConstructor = validatorProperties.ErrorConstructor || ValidatorError;
                  err = new ErrorConstructor(validatorProperties, scope);
                  err[validatorErrorSymbol] = true;
                  throw err;
                }
              },
              function(error2) {
                validatorProperties.reason = error2;
                validatorProperties.message = error2.message;
                ok = false;
                const ErrorConstructor = validatorProperties.ErrorConstructor || ValidatorError;
                err = new ErrorConstructor(validatorProperties, scope);
                err[validatorErrorSymbol] = true;
                throw err;
              }
            )
          );
        } else if (ok !== void 0 && !ok) {
          const ErrorConstructor = validatorProperties.ErrorConstructor || ValidatorError;
          err = new ErrorConstructor(validatorProperties, scope);
          err[validatorErrorSymbol] = true;
          throw err;
        }
      }
      await Promise.all(promises);
    };
    function _validate(ok, validatorProperties) {
      if (ok !== void 0 && !ok) {
        const ErrorConstructor = validatorProperties.ErrorConstructor || ValidatorError;
        const err = new ErrorConstructor(validatorProperties);
        err[validatorErrorSymbol] = true;
        return err;
      }
    }
    SchemaType.prototype.doValidateSync = function(value, scope, options2) {
      const path = this.path;
      const count = this.validators.length;
      if (!count) {
        return null;
      }
      let validators = this.validators;
      if (value === void 0) {
        if (this.validators.length !== 0 && this.validators[0].type === "required") {
          validators = [this.validators[0]];
        } else {
          return null;
        }
      }
      let err = null;
      let i = 0;
      const len = validators.length;
      for (i = 0; i < len; ++i) {
        const v = validators[i];
        if (v === null || typeof v !== "object") {
          continue;
        }
        const validator2 = v.validator;
        const validatorProperties = isSimpleValidator2(v) ? Object.assign({}, v) : clone(v);
        validatorProperties.path = options2?.path || path;
        validatorProperties.fullPath = this.$fullPath;
        validatorProperties.value = value;
        if (typeof value === "string") {
          validatorProperties.length = value.length;
        }
        let ok = false;
        if (isAsyncFunction2(validator2)) {
          continue;
        }
        if (validator2 instanceof RegExp) {
          err = _validate(validator2.test(value), validatorProperties);
          continue;
        }
        if (typeof validator2 !== "function") {
          continue;
        }
        try {
          if (validatorProperties.propsParameter) {
            ok = validator2.call(scope, value, validatorProperties);
          } else {
            ok = validator2.call(scope, value);
          }
        } catch (error2) {
          ok = false;
          validatorProperties.reason = error2;
        }
        if (typeof ok?.then === "function") {
          continue;
        }
        err = _validate(ok, validatorProperties);
        if (err) {
          break;
        }
      }
      return err;
    };
    SchemaType._isRef = function(self, value, doc, init) {
      let ref = init && (self.options?.ref || self.options?.refPath);
      if (!ref && doc?.$__ != null) {
        const path = doc.$__fullPath(self.path, true);
        const owner = doc.ownerDocument();
        ref = path != null && owner.$populated(path) || doc.$populated(self.path);
      }
      if (ref) {
        if (value == null) {
          return true;
        }
        if (!Buffer.isBuffer(value) && // buffers are objects too
        value._bsontype !== "Binary" && utils2.isObject(value)) {
          return true;
        }
        return init;
      }
      return false;
    };
    SchemaType.prototype._castRef = function _castRef(value, doc, init, options2) {
      if (value == null) {
        return value;
      }
      if (value.$__ != null) {
        value.$__.wasPopulated = value.$__.wasPopulated || { value: value._doc._id };
        return value;
      }
      if (Buffer.isBuffer(value) || !utils2.isObject(value)) {
        if (init) {
          return value;
        }
        throw new CastError(this.instance, value, this.path, null, this);
      }
      const path = doc.$__fullPath(this.path, true);
      const owner = doc.ownerDocument();
      const pop = owner.$populated(path, true);
      let ret = value;
      if (!doc.$__.populated || !doc.$__.populated[path] || !doc.$__.populated[path].options || !doc.$__.populated[path].options.options || !doc.$__.populated[path].options.options.lean) {
        const PopulatedModel = pop ? pop.options[populateModelSymbol] : doc.constructor.db.model(this.options.ref);
        ret = PopulatedModel.hydrate(value, null, options2);
        ret.$__.wasPopulated = { value: ret._doc._id, options: { [populateModelSymbol]: PopulatedModel } };
      }
      return ret;
    };
    function handleSingle(val, context) {
      return this.castForQuery(null, val, context);
    }
    function handleArray(val, context) {
      const _this = this;
      if (!Array.isArray(val)) {
        return [this.castForQuery(null, val, context)];
      }
      return val.map(function(m) {
        return _this.castForQuery(null, m, context);
      });
    }
    function handle$in(val, context) {
      const _this = this;
      if (!Array.isArray(val)) {
        return [this.castForQuery(null, val, context)];
      }
      return val.map(function(m) {
        if (Array.isArray(m) && m.length === 0) {
          return m;
        }
        return _this.castForQuery(null, m, context);
      });
    }
    SchemaType.prototype.$conditionalHandlers = {
      $all: handleArray,
      $eq: handleSingle,
      $in: handle$in,
      $ne: handleSingle,
      $nin: handle$in,
      $exists,
      $type
    };
    SchemaType.prototype.castForQuery = function($conditional, val, context) {
      let handler;
      if ($conditional != null) {
        handler = this.$conditionalHandlers[$conditional];
        if (!handler) {
          throw new MongooseError("Can't use " + $conditional);
        }
        return handler.call(this, val, context);
      }
      try {
        return this.applySetters(val, context);
      } catch (err) {
        if (err instanceof CastError && err.path === this.path && this.$fullPath != null) {
          err.path = this.$fullPath;
        }
        throw err;
      }
    };
    SchemaType.checkRequired = function(fn) {
      if (arguments.length !== 0) {
        this._checkRequired = fn;
      }
      return this._checkRequired;
    };
    SchemaType.prototype.checkRequired = function(val) {
      return val != null;
    };
    SchemaType.prototype.clone = function() {
      const options2 = Object.assign({}, this.options);
      const schematype = new this.constructor(this.path, options2, this.instance, this.parentSchema);
      schematype.validators = this.validators.slice();
      if (this.requiredValidator !== void 0) schematype.requiredValidator = this.requiredValidator;
      if (this.allowNullValidator !== void 0) schematype.allowNullValidator = this.allowNullValidator;
      if (this.defaultValue !== void 0) schematype.defaultValue = this.defaultValue;
      if (this.$immutable !== void 0 && this.options.immutable === void 0) {
        schematype.$immutable = this.$immutable;
        handleImmutable2(schematype);
      }
      if (this._index !== void 0) schematype._index = this._index;
      if (this.selected !== void 0) schematype.selected = this.selected;
      if (this.isRequired !== void 0) schematype.isRequired = this.isRequired;
      if (this.originalRequiredValue !== void 0) schematype.originalRequiredValue = this.originalRequiredValue;
      schematype.getters = this.getters.slice();
      schematype.setters = this.setters.slice();
      return schematype;
    };
    SchemaType.prototype.getEmbeddedSchemaType = function getEmbeddedSchemaType() {
      return this.embeddedSchemaType;
    };
    SchemaType.prototype._duplicateKeyErrorMessage = null;
    SchemaType.prototype.toJSONSchema = function toJSONSchema(_options) {
      throw new MongooseError(`Converting unsupported SchemaType to JSON Schema: ${this.instance} at path "${this.path}"`);
    };
    SchemaType.prototype.autoEncryptionType = function autoEncryptionType() {
      return null;
    };
    module.exports = exports = SchemaType;
    exports.CastError = CastError;
    exports.ValidatorError = ValidatorError;
  })(schemaType, schemaType.exports);
  return schemaType.exports;
}
var virtualOptions;
var hasRequiredVirtualOptions;
function requireVirtualOptions() {
  if (hasRequiredVirtualOptions) return virtualOptions;
  hasRequiredVirtualOptions = 1;
  const opts = requirePropertyOptions();
  class VirtualOptions {
    constructor(obj) {
      Object.assign(this, obj);
      if (obj?.options != null) {
        this.options = Object.assign({}, obj.options);
      }
    }
  }
  Object.defineProperty(VirtualOptions.prototype, "ref", opts);
  Object.defineProperty(VirtualOptions.prototype, "refPath", opts);
  Object.defineProperty(VirtualOptions.prototype, "localField", opts);
  Object.defineProperty(VirtualOptions.prototype, "foreignField", opts);
  Object.defineProperty(VirtualOptions.prototype, "justOne", opts);
  Object.defineProperty(VirtualOptions.prototype, "count", opts);
  Object.defineProperty(VirtualOptions.prototype, "match", opts);
  Object.defineProperty(VirtualOptions.prototype, "options", opts);
  Object.defineProperty(VirtualOptions.prototype, "skip", opts);
  Object.defineProperty(VirtualOptions.prototype, "limit", opts);
  Object.defineProperty(VirtualOptions.prototype, "perDocumentLimit", opts);
  virtualOptions = VirtualOptions;
  return virtualOptions;
}
var lookupLocalFields;
var hasRequiredLookupLocalFields;
function requireLookupLocalFields() {
  if (hasRequiredLookupLocalFields) return lookupLocalFields;
  hasRequiredLookupLocalFields = 1;
  lookupLocalFields = function lookupLocalFields2(cur, path, val) {
    if (cur == null) {
      return cur;
    }
    if (cur._doc != null) {
      cur = cur._doc;
    }
    if (arguments.length >= 3) {
      if (typeof cur !== "object") {
        return void 0;
      }
      if (val === void 0) {
        return void 0;
      }
      if (cur instanceof Map) {
        cur.set(path, val);
      } else {
        cur[path] = val;
      }
      return val;
    }
    if (path === "$*") {
      return cur instanceof Map ? Array.from(cur.values()) : Object.keys(cur).map((key) => cur[key]);
    }
    if (cur instanceof Map) {
      return cur.get(path);
    }
    return cur[path];
  };
  return lookupLocalFields;
}
var modelNamesFromRefPath;
var hasRequiredModelNamesFromRefPath;
function requireModelNamesFromRefPath() {
  if (hasRequiredModelNamesFromRefPath) return modelNamesFromRefPath;
  hasRequiredModelNamesFromRefPath = 1;
  const MongooseError = requireMongooseError();
  const isPathExcluded2 = requireIsPathExcluded();
  const lookupLocalFields2 = requireLookupLocalFields();
  const mpath = requireMpath();
  const util = require$$1;
  const utils2 = requireUtils();
  const hasNumericPropRE = /(\.\d+$|\.\d+\.)/g;
  modelNamesFromRefPath = function modelNamesFromRefPath2(refPath, doc, populatedPath, modelSchema, queryProjection) {
    if (refPath == null) {
      return [];
    }
    if (typeof refPath !== "string") {
      throw new MongooseError("`refPath` must be a string or a function that returns a string, got " + util.inspect(refPath));
    }
    if (queryProjection != null && isPathExcluded2(queryProjection, refPath)) {
      throw new MongooseError("refPath `" + refPath + "` must not be excluded in projection, got " + util.inspect(queryProjection));
    }
    if (hasNumericPropRE.test(populatedPath)) {
      const chunks = populatedPath.split(hasNumericPropRE);
      if (chunks[chunks.length - 1] === "") {
        throw new Error("Can't populate individual element in an array");
      }
      let _refPath = "";
      let _remaining = refPath;
      for (let i = 0; i < chunks.length; i += 2) {
        const chunk = chunks[i];
        if (_remaining.length >= chunk.length + 1 && _remaining.charAt(chunk.length) === "." && _remaining.slice(0, chunk.length) === chunk) {
          _refPath += _remaining.substring(0, chunk.length) + chunks[i + 1];
          _remaining = _remaining.substring(chunk.length + 1);
        } else if (i === chunks.length - 1) {
          _refPath += _remaining;
          _remaining = "";
          break;
        } else {
          throw new Error("Could not normalize ref path, chunk " + chunk + " not in populated path");
        }
      }
      const refValue2 = mpath.get(_refPath, doc, lookupLocalFields2);
      let modelNames2 = Array.isArray(refValue2) ? refValue2 : [refValue2];
      modelNames2 = utils2.array.flatten(modelNames2);
      return modelNames2;
    }
    const refValue = mpath.get(refPath, doc, lookupLocalFields2);
    let modelNames;
    if (modelSchema != null && Object.hasOwn(modelSchema.virtuals, refPath)) {
      modelNames = [modelSchema.virtuals[refPath].applyGetters(void 0, doc)];
    } else {
      modelNames = Array.isArray(refValue) ? refValue : [refValue];
    }
    return modelNames;
  };
  return modelNamesFromRefPath;
}
var virtualType;
var hasRequiredVirtualType;
function requireVirtualType() {
  if (hasRequiredVirtualType) return virtualType;
  hasRequiredVirtualType = 1;
  const modelNamesFromRefPath2 = requireModelNamesFromRefPath();
  const utils2 = requireUtils();
  const modelSymbol = requireSymbols$1().modelSymbol;
  function VirtualType(options2, name) {
    this.path = name;
    this.getters = [];
    this.setters = [];
    this.options = Object.assign({}, options2);
  }
  VirtualType.prototype._applyDefaultGetters = function() {
    if (this.getters.length > 0 || this.setters.length > 0) {
      return;
    }
    const path = this.path;
    const internalProperty = "$" + path;
    this.getters.push(function() {
      return this.$locals[internalProperty];
    });
    this.setters.push(function(v) {
      this.$locals[internalProperty] = v;
    });
  };
  VirtualType.prototype.clone = function() {
    const clone = new VirtualType(this.options, this.path);
    clone.getters = [].concat(this.getters);
    clone.setters = [].concat(this.setters);
    return clone;
  };
  VirtualType.prototype.get = function(fn) {
    this.getters.push(fn);
    return this;
  };
  VirtualType.prototype.set = function(fn) {
    this.setters.push(fn);
    return this;
  };
  VirtualType.prototype.applyGetters = function(value, doc) {
    if (utils2.hasUserDefinedProperty(this.options, ["ref", "refPath"]) && doc.$$populatedVirtuals && Object.hasOwn(doc.$$populatedVirtuals, this.path)) {
      value = doc.$$populatedVirtuals[this.path];
    }
    let v = value;
    for (const getter of this.getters) {
      v = getter.call(doc, v, this, doc);
    }
    return v;
  };
  VirtualType.prototype.applySetters = function(value, doc) {
    let v = value;
    for (const setter of this.setters) {
      v = setter.call(doc, v, this, doc);
    }
    return v;
  };
  VirtualType.prototype._getModelNamesForPopulate = function _getModelNamesForPopulate(doc) {
    if (this.options.refPath) {
      return modelNamesFromRefPath2(this.options.refPath, doc, this.path);
    }
    let normalizedRef = null;
    if (typeof this.options.ref === "function" && !this.options.ref[modelSymbol]) {
      normalizedRef = this.options.ref.call(doc, doc);
    } else {
      normalizedRef = this.options.ref;
    }
    if (normalizedRef != null && !Array.isArray(normalizedRef)) {
      return [normalizedRef];
    }
    return normalizedRef;
  };
  virtualType = VirtualType;
  return virtualType;
}
var addAutoId;
var hasRequiredAddAutoId;
function requireAddAutoId() {
  if (hasRequiredAddAutoId) return addAutoId;
  hasRequiredAddAutoId = 1;
  addAutoId = function addAutoId2(schema2) {
    const _obj = { _id: { auto: true } };
    _obj._id[schema2.options.typeKey] = "ObjectId";
    schema2.add(_obj);
  };
  return addAutoId;
}
var decorateDiscriminatorIndexOptions;
var hasRequiredDecorateDiscriminatorIndexOptions;
function requireDecorateDiscriminatorIndexOptions() {
  if (hasRequiredDecorateDiscriminatorIndexOptions) return decorateDiscriminatorIndexOptions;
  hasRequiredDecorateDiscriminatorIndexOptions = 1;
  decorateDiscriminatorIndexOptions = function decorateDiscriminatorIndexOptions2(schema2, indexOptions) {
    const discriminatorName = schema2.discriminatorMapping?.value;
    if (discriminatorName && !("sparse" in indexOptions)) {
      const discriminatorKey = schema2.options.discriminatorKey;
      indexOptions.partialFilterExpression = indexOptions.partialFilterExpression || {};
      indexOptions.partialFilterExpression[discriminatorKey] = discriminatorName;
    }
    return indexOptions;
  };
  return decorateDiscriminatorIndexOptions;
}
var getIndexes;
var hasRequiredGetIndexes;
function requireGetIndexes() {
  if (hasRequiredGetIndexes) return getIndexes;
  hasRequiredGetIndexes = 1;
  const get2 = requireGet();
  const helperIsObject = requireIsObject();
  const decorateDiscriminatorIndexOptions2 = requireDecorateDiscriminatorIndexOptions();
  getIndexes = function getIndexes2(schema2) {
    let indexes = [];
    const schemaStack = /* @__PURE__ */ new WeakMap();
    const indexTypes = schema2.constructor.indexTypes;
    const indexByName = /* @__PURE__ */ new Map();
    collectIndexes(schema2);
    return indexes;
    function collectIndexes(schema3, prefix, baseSchema) {
      if (schemaStack.has(schema3)) {
        return;
      }
      schemaStack.set(schema3, true);
      prefix = prefix || "";
      const keys = Object.keys(schema3.paths);
      for (const key of keys) {
        const path = schema3.paths[key];
        if (baseSchema != null && baseSchema.paths[key]) {
          continue;
        }
        if (path._duplicateKeyErrorMessage != null) {
          schema3._duplicateKeyErrorMessagesByPath = schema3._duplicateKeyErrorMessagesByPath || {};
          schema3._duplicateKeyErrorMessagesByPath[key] = path._duplicateKeyErrorMessage;
        }
        if (path.$isMongooseDocumentArray || path.$isSingleNested) {
          if (get2(path, "options.excludeIndexes") !== true && get2(path, "schemaOptions.excludeIndexes") !== true && get2(path, "schema.options.excludeIndexes") !== true) {
            collectIndexes(path.schema, prefix + key + ".");
          }
          if (path.schema.discriminators != null) {
            const discriminators = path.schema.discriminators;
            const discriminatorKeys = Object.keys(discriminators);
            for (const discriminatorKey of discriminatorKeys) {
              collectIndexes(
                discriminators[discriminatorKey],
                prefix + key + ".",
                path.schema
              );
            }
          }
          if (path.$isMongooseDocumentArray) {
            continue;
          }
        }
        const index = path._index || path.embeddedSchemaType && path.embeddedSchemaType._index;
        if (index !== false && index != null) {
          const field = {};
          const isObject2 = helperIsObject(index);
          const options2 = isObject2 ? { ...index } : {};
          const type2 = typeof index === "string" ? index : isObject2 ? index.type : false;
          if (type2 && indexTypes.indexOf(type2) !== -1) {
            field[prefix + key] = type2;
          } else if (options2.text) {
            field[prefix + key] = "text";
            delete options2.text;
          } else {
            let isDescendingIndex = false;
            if (index === "descending" || index === "desc") {
              isDescendingIndex = true;
            } else if (index === "ascending" || index === "asc") {
              isDescendingIndex = false;
            } else {
              isDescendingIndex = Number(index) === -1;
            }
            field[prefix + key] = isDescendingIndex ? -1 : 1;
          }
          delete options2.type;
          if (schema3.options.autoIndex != null) {
            options2._autoIndex = schema3.options.autoIndex;
          }
          const indexName = options2?.name;
          if (typeof indexName === "string") {
            if (indexByName.has(indexName)) {
              Object.assign(indexByName.get(indexName), field);
            } else {
              indexes.push([field, options2]);
              indexByName.set(indexName, field);
            }
          } else {
            indexes.push([field, options2]);
            indexByName.set(indexName, field);
          }
        }
      }
      schemaStack.delete(schema3);
      if (prefix) {
        fixSubIndexPaths(schema3, prefix);
      } else {
        schema3._indexes.forEach(function(index) {
          const options2 = index[1];
          decorateDiscriminatorIndexOptions2(schema3, options2);
        });
        indexes = indexes.concat(schema3._indexes);
      }
    }
    function fixSubIndexPaths(schema3, prefix) {
      const subindexes = schema3._indexes;
      const len = subindexes.length;
      for (let i = 0; i < len; ++i) {
        const indexObj = subindexes[i][0];
        const indexOptions = subindexes[i][1];
        const keys = Object.keys(indexObj);
        const klen = keys.length;
        const newindex = {};
        for (let j = 0; j < klen; ++j) {
          const key = keys[j];
          newindex[prefix + key] = indexObj[key];
        }
        const newIndexOptions = Object.assign({}, indexOptions);
        if (indexOptions?.partialFilterExpression != null) {
          newIndexOptions.partialFilterExpression = {};
          const partialFilterExpression = indexOptions.partialFilterExpression;
          for (const key of Object.keys(partialFilterExpression)) {
            newIndexOptions.partialFilterExpression[prefix + key] = partialFilterExpression[key];
          }
        }
        indexes.push([newindex, newIndexOptions]);
      }
    }
  };
  return getIndexes;
}
var handleReadPreferenceAliases;
var hasRequiredHandleReadPreferenceAliases;
function requireHandleReadPreferenceAliases() {
  if (hasRequiredHandleReadPreferenceAliases) return handleReadPreferenceAliases;
  hasRequiredHandleReadPreferenceAliases = 1;
  handleReadPreferenceAliases = function handleReadPreferenceAliases2(pref) {
    switch (pref) {
      case "p":
        pref = "primary";
        break;
      case "pp":
        pref = "primaryPreferred";
        break;
      case "s":
        pref = "secondary";
        break;
      case "sp":
        pref = "secondaryPreferred";
        break;
      case "n":
        pref = "nearest";
        break;
    }
    return pref;
  };
  return handleReadPreferenceAliases;
}
var idGetter_1;
var hasRequiredIdGetter;
function requireIdGetter() {
  if (hasRequiredIdGetter) return idGetter_1;
  hasRequiredIdGetter = 1;
  idGetter_1 = function addIdGetter(schema2) {
    const autoIdGetter = !schema2.paths["id"] && schema2.paths["_id"] && schema2.options.id;
    if (!autoIdGetter) {
      return schema2;
    }
    if (schema2.aliases?.id) {
      return schema2;
    }
    schema2.virtual("id").get(idGetter);
    return schema2;
  };
  function idGetter() {
    if (this._doc) {
      return this._doc._id?.toString() ?? null;
    }
    if (this._id != null) {
      return this._id.toString();
    }
    return null;
  }
  return idGetter_1;
}
var isIndexSpecEqual;
var hasRequiredIsIndexSpecEqual;
function requireIsIndexSpecEqual() {
  if (hasRequiredIsIndexSpecEqual) return isIndexSpecEqual;
  hasRequiredIsIndexSpecEqual = 1;
  isIndexSpecEqual = function isIndexSpecEqual2(spec1, spec2) {
    const spec1Keys = Object.keys(spec1);
    const spec2Keys = Object.keys(spec2);
    if (spec1Keys.length !== spec2Keys.length) {
      return false;
    }
    for (let i = 0; i < spec1Keys.length; i++) {
      const key = spec1Keys[i];
      if (key !== spec2Keys[i] || spec1[key] !== spec2[key]) {
        return false;
      }
    }
    return true;
  };
  return isIndexSpecEqual;
}
var setPopulatedVirtualValue;
var hasRequiredSetPopulatedVirtualValue;
function requireSetPopulatedVirtualValue() {
  if (hasRequiredSetPopulatedVirtualValue) return setPopulatedVirtualValue;
  hasRequiredSetPopulatedVirtualValue = 1;
  setPopulatedVirtualValue = function setPopulatedVirtualValue2(populatedVirtuals, name, v, options2) {
    if (options2.justOne || options2.count) {
      populatedVirtuals[name] = Array.isArray(v) ? v[0] : v;
      if (typeof populatedVirtuals[name] !== "object") {
        populatedVirtuals[name] = options2.count ? v : null;
      }
    } else {
      populatedVirtuals[name] = Array.isArray(v) ? v : v == null ? [] : [v];
      populatedVirtuals[name] = populatedVirtuals[name].filter(function(doc) {
        return doc && typeof doc === "object";
      });
    }
    return populatedVirtuals[name];
  };
  return setPopulatedVirtualValue;
}
var cleanPositionalOperators;
var hasRequiredCleanPositionalOperators;
function requireCleanPositionalOperators() {
  if (hasRequiredCleanPositionalOperators) return cleanPositionalOperators;
  hasRequiredCleanPositionalOperators = 1;
  cleanPositionalOperators = function cleanPositionalOperators2(path) {
    return path.replace(/\.\$(\[[^\]]*\])?(?=\.)/g, ".0").replace(/\.\$(\[[^\]]*\])?$/g, ".0");
  };
  return cleanPositionalOperators;
}
var handleTimestampOption_1;
var hasRequiredHandleTimestampOption;
function requireHandleTimestampOption() {
  if (hasRequiredHandleTimestampOption) return handleTimestampOption_1;
  hasRequiredHandleTimestampOption = 1;
  handleTimestampOption_1 = handleTimestampOption;
  function handleTimestampOption(arg, prop) {
    if (arg == null) {
      return null;
    }
    if (typeof arg === "boolean") {
      return prop;
    }
    if (typeof arg[prop] === "boolean") {
      return arg[prop] ? prop : null;
    }
    if (!(prop in arg)) {
      return prop;
    }
    return arg[prop];
  }
  return handleTimestampOption_1;
}
var applyTimestampsToChildren_1;
var hasRequiredApplyTimestampsToChildren;
function requireApplyTimestampsToChildren() {
  if (hasRequiredApplyTimestampsToChildren) return applyTimestampsToChildren_1;
  hasRequiredApplyTimestampsToChildren = 1;
  const cleanPositionalOperators2 = requireCleanPositionalOperators();
  const handleTimestampOption = requireHandleTimestampOption();
  applyTimestampsToChildren_1 = applyTimestampsToChildren;
  function applyTimestampsToChildren(now, update, schema2) {
    if (update == null) {
      return;
    }
    const keys = Object.keys(update);
    const hasDollarKey = keys.some((key) => key[0] === "$");
    if (hasDollarKey) {
      if (update.$push) {
        _applyTimestampToUpdateOperator(update.$push);
      }
      if (update.$addToSet) {
        _applyTimestampToUpdateOperator(update.$addToSet);
      }
      if (update.$set != null) {
        const keys2 = Object.keys(update.$set);
        for (const key of keys2) {
          applyTimestampsToUpdateKey(schema2, key, update.$set, now);
        }
      }
      if (update.$setOnInsert != null) {
        const keys2 = Object.keys(update.$setOnInsert);
        for (const key of keys2) {
          applyTimestampsToUpdateKey(schema2, key, update.$setOnInsert, now);
        }
      }
    }
    const updateKeys = Object.keys(update).filter((key) => key[0] !== "$");
    for (const key of updateKeys) {
      applyTimestampsToUpdateKey(schema2, key, update, now);
    }
    function _applyTimestampToUpdateOperator(op) {
      for (const key of Object.keys(op)) {
        const $path = schema2.path(key.replace(/\.\$\./i, ".").replace(/.\$$/, ""));
        if (op[key] && $path && $path.$isMongooseDocumentArray && $path.schema.options.timestamps) {
          const timestamps = $path.schema.options.timestamps;
          const createdAt = handleTimestampOption(timestamps, "createdAt");
          const updatedAt = handleTimestampOption(timestamps, "updatedAt");
          if (op[key].$each) {
            op[key].$each.forEach(function(subdoc) {
              if (updatedAt != null) {
                subdoc[updatedAt] = now;
              }
              if (createdAt != null) {
                subdoc[createdAt] = now;
              }
              applyTimestampsToChildren(now, subdoc, $path.schema);
            });
          } else {
            if (updatedAt != null) {
              op[key][updatedAt] = now;
            }
            if (createdAt != null) {
              op[key][createdAt] = now;
            }
            applyTimestampsToChildren(now, op[key], $path.schema);
          }
        }
      }
    }
  }
  function applyTimestampsToDocumentArray(arr, schematype, now) {
    const timestamps = schematype.schema.options.timestamps;
    const len = arr.length;
    if (!timestamps) {
      for (let i = 0; i < len; ++i) {
        applyTimestampsToChildren(now, arr[i], schematype.schema);
      }
      return;
    }
    const createdAt = handleTimestampOption(timestamps, "createdAt");
    const updatedAt = handleTimestampOption(timestamps, "updatedAt");
    for (let i = 0; i < len; ++i) {
      if (updatedAt != null) {
        arr[i][updatedAt] = now;
      }
      if (createdAt != null) {
        arr[i][createdAt] = now;
      }
      applyTimestampsToChildren(now, arr[i], schematype.schema);
    }
  }
  function applyTimestampsToSingleNested(subdoc, schematype, now) {
    const timestamps = schematype.schema.options.timestamps;
    if (!timestamps) {
      applyTimestampsToChildren(now, subdoc, schematype.schema);
      return;
    }
    const createdAt = handleTimestampOption(timestamps, "createdAt");
    const updatedAt = handleTimestampOption(timestamps, "updatedAt");
    if (updatedAt != null) {
      subdoc[updatedAt] = now;
    }
    if (createdAt != null) {
      subdoc[createdAt] = now;
    }
    applyTimestampsToChildren(now, subdoc, schematype.schema);
  }
  function applyTimestampsToUpdateKey(schema2, key, update, now) {
    const keyToSearch = cleanPositionalOperators2(key);
    const path = schema2.path(keyToSearch);
    if (!path) {
      return;
    }
    const parentSchemaTypes = [];
    const pieces = keyToSearch.split(".");
    for (let i = pieces.length - 1; i > 0; --i) {
      const s = schema2.path(pieces.slice(0, i).join("."));
      if (s != null && (s.$isMongooseDocumentArray || s.$isSingleNested)) {
        parentSchemaTypes.push({ parentPath: key.split(".").slice(0, i).join("."), parentSchemaType: s });
      }
    }
    if (Array.isArray(update[key]) && path.$isMongooseDocumentArray) {
      applyTimestampsToDocumentArray(update[key], path, now);
    } else if (update[key] && path.$isSingleNested) {
      applyTimestampsToSingleNested(update[key], path, now);
    } else if (parentSchemaTypes.length > 0) {
      for (const item of parentSchemaTypes) {
        const parentPath = item.parentPath;
        const parentSchemaType = item.parentSchemaType;
        const timestamps = parentSchemaType.schema.options.timestamps;
        const updatedAt = handleTimestampOption(timestamps, "updatedAt");
        if (!timestamps || updatedAt == null) {
          continue;
        }
        if (parentSchemaType.$isSingleNested) {
          update[parentPath + "." + updatedAt] = now;
        } else if (parentSchemaType.$isMongooseDocumentArray) {
          let childPath = key.substring(parentPath.length + 1);
          if (/^\d+$/.test(childPath)) {
            update[parentPath + "." + childPath][updatedAt] = now;
            continue;
          }
          const firstDot = childPath.indexOf(".");
          childPath = firstDot !== -1 ? childPath.substring(0, firstDot) : childPath;
          update[parentPath + "." + childPath + "." + updatedAt] = now;
        }
      }
    } else if (path.schema != null && path.schema != schema2 && update[key]) {
      const timestamps = path.schema.options.timestamps;
      const createdAt = handleTimestampOption(timestamps, "createdAt");
      const updatedAt = handleTimestampOption(timestamps, "updatedAt");
      if (!timestamps) {
        return;
      }
      if (updatedAt != null) {
        update[key][updatedAt] = now;
      }
      if (createdAt != null) {
        update[key][createdAt] = now;
      }
    }
  }
  return applyTimestampsToChildren_1;
}
var applyTimestampsToUpdate_1;
var hasRequiredApplyTimestampsToUpdate;
function requireApplyTimestampsToUpdate() {
  if (hasRequiredApplyTimestampsToUpdate) return applyTimestampsToUpdate_1;
  hasRequiredApplyTimestampsToUpdate = 1;
  const utils2 = requireUtils();
  applyTimestampsToUpdate_1 = applyTimestampsToUpdate;
  function applyTimestampsToUpdate(now, createdAt, updatedAt, currentUpdate, options2, upsert, isReplace) {
    const updates = currentUpdate;
    let _updates = updates;
    const timestamps = options2?.timestamps ?? true;
    if (!timestamps || updates == null) {
      return currentUpdate;
    }
    const skipCreatedAt = timestamps?.createdAt === false;
    const skipUpdatedAt = timestamps?.updatedAt === false;
    if (isReplace) {
      if (currentUpdate?.$set) {
        currentUpdate = currentUpdate.$set;
        updates.$set = {};
        _updates = updates.$set;
      }
      if (!skipUpdatedAt && updatedAt && !currentUpdate[updatedAt]) {
        _updates[updatedAt] = now;
      }
      if (!skipCreatedAt && createdAt && !currentUpdate[createdAt]) {
        _updates[createdAt] = now;
      }
      return updates;
    }
    currentUpdate = currentUpdate || {};
    if (Array.isArray(updates)) {
      if (updatedAt == null) {
        return updates;
      }
      updates.push({ $set: { [updatedAt]: now } });
      return updates;
    }
    updates.$set = updates.$set || {};
    if (!skipUpdatedAt && updatedAt && !currentUpdate.$currentDate?.[updatedAt]) {
      let timestampSet = false;
      if (updatedAt.indexOf(".") !== -1) {
        const pieces = updatedAt.split(".");
        for (let i = 1; i < pieces.length; ++i) {
          const remnant = pieces.slice(-i).join(".");
          const start = pieces.slice(0, -i).join(".");
          if (currentUpdate[start] != null) {
            currentUpdate[start][remnant] = now;
            timestampSet = true;
            break;
          } else if (currentUpdate.$set?.[start]) {
            currentUpdate.$set[start][remnant] = now;
            timestampSet = true;
            break;
          }
        }
      }
      if (!timestampSet) {
        updates.$set[updatedAt] = now;
      }
      if (Object.hasOwn(updates, updatedAt)) {
        delete updates[updatedAt];
      }
    }
    if (!skipCreatedAt && createdAt) {
      const overwriteImmutable = options2?.overwriteImmutable;
      const hasUserCreatedAt = currentUpdate[createdAt] != null || currentUpdate.$set?.[createdAt] != null || currentUpdate.$setOnInsert?.[createdAt] != null;
      if (overwriteImmutable && hasUserCreatedAt) {
        if (currentUpdate[createdAt] != null) {
          updates.$set[createdAt] = currentUpdate[createdAt];
          delete currentUpdate[createdAt];
        }
      } else {
        if (currentUpdate[createdAt]) {
          delete currentUpdate[createdAt];
        }
        if (currentUpdate.$set?.[createdAt]) {
          delete currentUpdate.$set[createdAt];
        }
        let timestampSet = false;
        if (createdAt.indexOf(".") !== -1) {
          const pieces = createdAt.split(".");
          for (let i = 1; i < pieces.length; ++i) {
            const remnant = pieces.slice(-i).join(".");
            const start = pieces.slice(0, -i).join(".");
            if (currentUpdate[start] != null) {
              currentUpdate[start][remnant] = now;
              timestampSet = true;
              break;
            } else if (currentUpdate.$set?.[start]) {
              currentUpdate.$set[start][remnant] = now;
              timestampSet = true;
              break;
            }
          }
        }
        if (!timestampSet && upsert) {
          updates.$setOnInsert = updates.$setOnInsert || {};
          updates.$setOnInsert[createdAt] = now;
        }
      }
    }
    if (utils2.hasOwnKeys(updates.$set) === false) {
      delete updates.$set;
    }
    return updates;
  }
  return applyTimestampsToUpdate_1;
}
var setDocumentTimestamps;
var hasRequiredSetDocumentTimestamps;
function requireSetDocumentTimestamps() {
  if (hasRequiredSetDocumentTimestamps) return setDocumentTimestamps;
  hasRequiredSetDocumentTimestamps = 1;
  setDocumentTimestamps = function setDocumentTimestamps2(doc, timestampOption, currentTime, createdAt, updatedAt) {
    const skipUpdatedAt = timestampOption?.updatedAt === false;
    const skipCreatedAt = timestampOption?.createdAt === false;
    const defaultTimestamp = currentTime != null ? currentTime() : doc.ownerDocument().constructor.base.now();
    if (!skipCreatedAt && (doc.isNew || doc.$isSubdocument) && createdAt && !doc.$__getValue(createdAt) && doc.$__isSelected(createdAt)) {
      doc.$set(createdAt, defaultTimestamp, void 0, { overwriteImmutable: true });
    }
    if (!skipUpdatedAt && updatedAt && (doc.isNew || doc.$isModified())) {
      let ts = defaultTimestamp;
      if (doc.isNew && createdAt != null) {
        ts = doc.$__getValue(createdAt);
      }
      doc.$set(updatedAt, ts);
    }
  };
  return setDocumentTimestamps;
}
var setupTimestamps;
var hasRequiredSetupTimestamps;
function requireSetupTimestamps() {
  if (hasRequiredSetupTimestamps) return setupTimestamps;
  hasRequiredSetupTimestamps = 1;
  const applyTimestampsToChildren = requireApplyTimestampsToChildren();
  const applyTimestampsToUpdate = requireApplyTimestampsToUpdate();
  const handleTimestampOption = requireHandleTimestampOption();
  const setDocumentTimestamps2 = requireSetDocumentTimestamps();
  const symbols2 = requireSymbols();
  const replaceOps = /* @__PURE__ */ new Set([
    "replaceOne",
    "findOneAndReplace"
  ]);
  setupTimestamps = function setupTimestamps2(schema2, timestamps) {
    const childHasTimestamp = schema2.childSchemas.find(withTimestamp);
    function withTimestamp(s) {
      const ts = s.schema.options.timestamps;
      return !!ts;
    }
    if (!timestamps && !childHasTimestamp) {
      return;
    }
    const createdAt = handleTimestampOption(timestamps, "createdAt");
    const updatedAt = handleTimestampOption(timestamps, "updatedAt");
    const currentTime = timestamps != null && Object.hasOwn(timestamps, "currentTime") ? timestamps.currentTime : null;
    const schemaAdditions = {};
    schema2.$timestamps = { createdAt, updatedAt };
    if (createdAt && !schema2.paths[createdAt]) {
      const baseImmutableCreatedAt = schema2.base?.get("timestamps.createdAt.immutable") ?? null;
      const immutable = baseImmutableCreatedAt ?? true;
      schemaAdditions[createdAt] = { [schema2.options.typeKey || "type"]: Date, immutable };
    }
    if (updatedAt && !schema2.paths[updatedAt]) {
      schemaAdditions[updatedAt] = Date;
    }
    schema2.add(schemaAdditions);
    schema2.pre("save", timestampsPreSave);
    schema2.methods.initializeTimestamps = function(timestampsOptions) {
      if (timestampsOptions === false) {
        return this;
      }
      const ts = currentTime != null ? currentTime() : this.constructor.base.now();
      const initTimestampsCreatedAt = timestampsOptions != null ? handleTimestampOption(timestampsOptions, "createdAt") : createdAt;
      const initTimestampsUpdatedAt = timestampsOptions != null ? handleTimestampOption(timestampsOptions, "updatedAt") : updatedAt;
      if (initTimestampsCreatedAt && !this.get(initTimestampsCreatedAt)) {
        this.$set(createdAt, ts);
      }
      if (initTimestampsUpdatedAt && !this.get(initTimestampsUpdatedAt)) {
        this.$set(updatedAt, ts);
      }
      if (this.$isSubdocument) {
        return this;
      }
      const subdocs = this.$getAllSubdocs();
      for (const subdoc of subdocs) {
        if (subdoc.initializeTimestamps) {
          subdoc.initializeTimestamps(timestampsOptions);
        }
      }
      return this;
    };
    const opts = { query: true, model: false };
    schema2.pre("findOneAndReplace", opts, _setTimestampsOnUpdate);
    schema2.pre("findOneAndUpdate", opts, _setTimestampsOnUpdate);
    schema2.pre("replaceOne", opts, _setTimestampsOnUpdate);
    schema2.pre("update", opts, _setTimestampsOnUpdate);
    schema2.pre("updateOne", opts, _setTimestampsOnUpdate);
    schema2.pre("updateMany", opts, _setTimestampsOnUpdate);
  };
  function timestampsPreSave() {
    const timestampOption = this.$__?.saveOptions?.timestamps;
    if (timestampOption === false) {
      return;
    }
    const schema2 = this.$__schema;
    const { createdAt, updatedAt } = schema2.$timestamps;
    const timestamps = schema2.options.timestamps;
    const currentTime = timestamps != null && Object.hasOwn(timestamps, "currentTime") ? timestamps.currentTime : null;
    setDocumentTimestamps2(this, timestampOption, currentTime, createdAt, updatedAt);
  }
  function _setTimestampsOnUpdate() {
    const schema2 = this.model.schema;
    const { createdAt, updatedAt } = schema2.$timestamps;
    const timestamps = schema2.options.timestamps;
    const currentTime = timestamps != null && Object.hasOwn(timestamps, "currentTime") ? timestamps.currentTime : null;
    const now = currentTime != null ? currentTime() : this.model.base.now();
    if (replaceOps.has(this.op) && this.getUpdate() == null) {
      this.setUpdate({});
    }
    applyTimestampsToUpdate(
      now,
      createdAt,
      updatedAt,
      this.getUpdate(),
      this._mongooseOptions,
      this.options.upsert,
      replaceOps.has(this.op)
    );
    applyTimestampsToChildren(now, this.getUpdate(), this.model.schema);
  }
  timestampsPreSave[symbols2.builtInMiddleware] = true;
  _setTimestampsOnUpdate[symbols2.builtInMiddleware] = true;
  return setupTimestamps;
}
var validateRef_1;
var hasRequiredValidateRef;
function requireValidateRef() {
  if (hasRequiredValidateRef) return validateRef_1;
  hasRequiredValidateRef = 1;
  const MongooseError = requireMongooseError();
  const util = require$$1;
  validateRef_1 = validateRef;
  function validateRef(ref, path) {
    if (typeof ref === "string") {
      return;
    }
    if (typeof ref === "function") {
      return;
    }
    throw new MongooseError('Invalid ref at path "' + path + '". Got ' + util.inspect(ref, { depth: 0 }));
  }
  return validateRef_1;
}
var constants = {};
var hasRequiredConstants;
function requireConstants() {
  if (hasRequiredConstants) return constants;
  hasRequiredConstants = 1;
  const queryOperations = Object.freeze([
    // Read
    "countDocuments",
    "distinct",
    "estimatedDocumentCount",
    "find",
    "findOne",
    // Update
    "findOneAndReplace",
    "findOneAndUpdate",
    "replaceOne",
    "updateMany",
    "updateOne",
    // Delete
    "deleteMany",
    "deleteOne",
    "findOneAndDelete"
  ]);
  constants.queryOperations = queryOperations;
  const queryMiddlewareFunctions = queryOperations.concat([
    "validate"
  ]);
  constants.queryMiddlewareFunctions = queryMiddlewareFunctions;
  const aggregateMiddlewareFunctions = [
    "aggregate"
  ];
  constants.aggregateMiddlewareFunctions = aggregateMiddlewareFunctions;
  const modelMiddlewareFunctions = [
    "bulkWrite",
    "createCollection",
    "insertMany"
  ];
  constants.modelMiddlewareFunctions = modelMiddlewareFunctions;
  const documentMiddlewareFunctions = [
    "validate",
    "save",
    "remove",
    "updateOne",
    "deleteOne",
    "init"
  ];
  constants.documentMiddlewareFunctions = documentMiddlewareFunctions;
  return constants;
}
var types = {};
var array$1 = { exports: {} };
var hasRequiredArray$1;
function requireArray$1() {
  if (hasRequiredArray$1) return array$1.exports;
  hasRequiredArray$1 = 1;
  (function(module, exports) {
    const mongooseArrayMethods = requireMethods$1();
    const arrayAtomicsSymbol = requireSymbols$1().arrayAtomicsSymbol;
    const arrayAtomicsBackupSymbol = requireSymbols$1().arrayAtomicsBackupSymbol;
    const arrayParentSymbol = requireSymbols$1().arrayParentSymbol;
    const arrayPathSymbol = requireSymbols$1().arrayPathSymbol;
    const arraySchemaSymbol = requireSymbols$1().arraySchemaSymbol;
    const _basePush = Array.prototype.push;
    const numberRE = /^\d+$/;
    function MongooseArray(values, path, doc, schematype) {
      let __array;
      if (Array.isArray(values)) {
        const len = values.length;
        if (len === 0) {
          __array = new Array();
        } else if (len === 1) {
          __array = new Array(1);
          __array[0] = values[0];
        } else if (len < 1e4) {
          __array = new Array();
          _basePush.apply(__array, values);
        } else {
          __array = new Array();
          for (let i = 0; i < len; ++i) {
            _basePush.call(__array, values[i]);
          }
        }
      } else {
        __array = [];
      }
      const internals = {
        [arrayAtomicsSymbol]: {},
        [arrayAtomicsBackupSymbol]: void 0,
        [arrayPathSymbol]: path,
        [arraySchemaSymbol]: schematype,
        [arrayParentSymbol]: void 0,
        isMongooseArray: true,
        isMongooseArrayProxy: true,
        __array
      };
      if (values && values[arrayAtomicsSymbol] != null) {
        internals[arrayAtomicsSymbol] = values[arrayAtomicsSymbol];
      }
      if (doc?.$__) {
        internals[arrayParentSymbol] = doc;
        internals[arraySchemaSymbol] = schematype || doc.schema.path(path);
      }
      const proxy = new Proxy(__array, {
        get: function(target, prop) {
          if (Object.hasOwn(internals, prop)) {
            return internals[prop];
          }
          if (Object.hasOwn(mongooseArrayMethods, prop)) {
            return mongooseArrayMethods[prop];
          }
          if (schematype?.virtuals && Object.hasOwn(schematype.virtuals, prop)) {
            return schematype.virtuals[prop].applyGetters(void 0, target);
          }
          if (typeof prop === "string" && numberRE.test(prop) && schematype?.embeddedSchemaType != null) {
            return schematype.embeddedSchemaType.applyGetters(__array[prop], doc);
          }
          return __array[prop];
        },
        set: function(target, prop, value) {
          if (typeof prop === "string" && numberRE.test(prop)) {
            mongooseArrayMethods.set.call(proxy, prop, value, false);
          } else if (Object.hasOwn(internals, prop)) {
            internals[prop] = value;
          } else if (schematype?.virtuals && Object.hasOwn(schematype.virtuals, prop)) {
            schematype.virtuals[prop].applySetters(value, target);
          } else {
            __array[prop] = value;
          }
          return true;
        }
      });
      return proxy;
    }
    module.exports = MongooseArray;
  })(array$1);
  return array$1.exports;
}
var double$2;
var hasRequiredDouble$2;
function requireDouble$2() {
  if (hasRequiredDouble$2) return double$2;
  hasRequiredDouble$2 = 1;
  double$2 = requireBson().Double;
  return double$2;
}
var map$1;
var hasRequiredMap$1;
function requireMap$1() {
  if (hasRequiredMap$1) return map$1;
  hasRequiredMap$1 = 1;
  const Mixed = requireMixed();
  const MongooseError = requireMongooseError();
  const clone = requireClone();
  const deepEqual = requireUtils().deepEqual;
  const getConstructorName2 = requireGetConstructorName();
  const handleSpreadDoc2 = requireHandleSpreadDoc();
  const util = require$$1;
  const specialProperties2 = requireSpecialProperties();
  const isBsonType = requireIsBsonType();
  const cleanModifiedSubpaths2 = requireCleanModifiedSubpaths();
  const populateModelSymbol = requireSymbols$1().populateModelSymbol;
  class MongooseMap extends Map {
    constructor(v, path, doc, schemaType2) {
      if (getConstructorName2(v) === "Object") {
        v = Object.keys(v).reduce((arr, key) => arr.concat([[key, v[key]]]), []);
      }
      super(v);
      this.$__parent = doc?.$__ != null ? doc : null;
      this.$__path = path;
      this.$__schemaType = schemaType2 == null ? new Mixed(path) : schemaType2;
      this.$__runDeferred();
    }
    $init(key, value) {
      checkValidKey(key);
      super.set(key, value);
      if (value?.$isSingleNested) {
        value.$basePath = this.$__path + "." + key;
      }
    }
    $__set(key, value) {
      super.set(key, value);
    }
    /**
     * Overwrites native Map's `get()` function to support Mongoose getters.
     *
     * @api public
     * @method get
     * @memberOf Map
     */
    get(key, options2) {
      if (isBsonType(key, "ObjectId")) {
        key = key.toString();
      }
      options2 = options2 || {};
      if (options2.getters === false) {
        return super.get(key);
      }
      return this.$__schemaType.applyGetters(super.get(key), this.$__parent);
    }
    /**
     * Overwrites native Map's `set()` function to support setters, `populate()`,
     * and change tracking. Note that Mongoose maps _only_ support strings and
     * ObjectIds as keys.
     *
     * Keys also cannot:
     * - be named after special properties `prototype`, `constructor`, and `__proto__`
     * - start with a dollar sign (`$`)
     * - contain any dots (`.`)
     *
     * #### Example:
     *
     *     doc.myMap.set('test', 42); // works
     *     doc.myMap.set({ obj: 42 }, 42); // Throws "Mongoose maps only support string keys"
     *     doc.myMap.set(10, 42); // Throws "Mongoose maps only support string keys"
     *     doc.myMap.set("$test", 42); // Throws "Mongoose maps do not support keys that start with "$", got "$test""
     *
     * @api public
     * @method set
     * @memberOf Map
     */
    set(key, value) {
      if (isBsonType(key, "ObjectId")) {
        key = key.toString();
      }
      checkValidKey(key);
      value = handleSpreadDoc2(value);
      if (this.$__schemaType == null) {
        this.$__deferred = this.$__deferred || [];
        this.$__deferred.push({ key, value });
        return;
      }
      let _fullPath;
      const parent = this.$__parent;
      const populated = parent?.$__?.populated ? parent.$populated(fullPath.call(this), true) || parent.$populated(this.$__path, true) : null;
      const priorVal = this.get(key);
      if (populated != null) {
        if (this.$__schemaType.$isSingleNested) {
          throw new MongooseError(
            `Cannot manually populate single nested subdoc underneath Map at path "${this.$__path}". Try using an array instead of a Map.`
          );
        }
        if (Array.isArray(value) && this.$__schemaType.$isMongooseArray) {
          value = value.map((v) => {
            if (v.$__ == null) {
              v = new populated.options[populateModelSymbol](v);
            }
            v.$__.wasPopulated = { value: v._doc._id };
            return v;
          });
        } else if (value != null) {
          if (value.$__ == null) {
            value = new populated.options[populateModelSymbol](value);
          }
          value.$__.wasPopulated = { value: value._doc._id };
        }
      } else {
        try {
          const options2 = this.$__schemaType.$isMongooseDocumentArray || this.$__schemaType.$isSingleNested || this.$__schemaType.$isMongooseArray || this.$__schemaType.$isSchemaMap ? { path: fullPath.call(this) } : null;
          value = this.$__schemaType.applySetters(
            value,
            this.$__parent,
            false,
            this.get(key),
            options2
          );
        } catch (error2) {
          if (this.$__parent?.$__ != null) {
            this.$__parent.invalidate(fullPath.call(this), error2);
            return;
          }
          throw error2;
        }
      }
      super.set(key, value);
      if (parent?.$__ != null && !deepEqual(value, priorVal)) {
        const path = fullPath.call(this);
        parent.markModified(path);
        if (this.$__schemaType.$isMongooseDocumentArray || this.$__schemaType.$isSingleNested) {
          cleanModifiedSubpaths2(parent, path);
        }
      }
      function fullPath() {
        if (_fullPath) {
          return _fullPath;
        }
        _fullPath = this.$__path + "." + key;
        return _fullPath;
      }
    }
    /**
     * Overwrites native Map's `clear()` function to support change tracking.
     *
     * @api public
     * @method clear
     * @memberOf Map
     */
    clear() {
      super.clear();
      const parent = this.$__parent;
      if (parent != null) {
        parent.markModified(this.$__path);
      }
    }
    /**
     * Overwrites native Map's `delete()` function to support change tracking.
     *
     * @api public
     * @method delete
     * @memberOf Map
     */
    delete(key) {
      if (isBsonType(key, "ObjectId")) {
        key = key.toString();
      }
      this.set(key, void 0);
      return super.delete(key);
    }
    /**
     * Converts this map to a native JavaScript Map so the MongoDB driver can serialize it.
     *
     * @api public
     * @method toBSON
     * @memberOf Map
     */
    toBSON() {
      return new Map(this);
    }
    toObject(options2) {
      if (options2?.flattenMaps) {
        const ret = {};
        const keys = this.keys();
        for (const key of keys) {
          ret[key] = clone(this.get(key), options2);
        }
        return ret;
      }
      return new Map(this);
    }
    $toObject() {
      return this.constructor.prototype.toObject.apply(this, arguments);
    }
    /**
     * Converts this map to a native JavaScript Map for `JSON.stringify()`. Set
     * the `flattenMaps` option to convert this map to a POJO instead.
     *
     * #### Example:
     *
     *     doc.myMap.toJSON() instanceof Map; // true
     *     doc.myMap.toJSON({ flattenMaps: true }) instanceof Map; // false
     *
     * @api public
     * @method toJSON
     * @param {object} [options]
     * @param {boolean} [options.flattenMaps=false] set to `true` to convert the map to a POJO rather than a native JavaScript map
     * @memberOf Map
     */
    toJSON(options2) {
      if (typeof options2?.flattenMaps === "boolean" ? options2.flattenMaps : true) {
        const ret = {};
        const keys = this.keys();
        for (const key of keys) {
          ret[key] = clone(this.get(key), options2);
        }
        return ret;
      }
      return new Map(this);
    }
    inspect() {
      return new Map(this);
    }
    $__runDeferred() {
      if (!this.$__deferred) {
        return;
      }
      for (const keyValueObject of this.$__deferred) {
        this.set(keyValueObject.key, keyValueObject.value);
      }
      this.$__deferred = null;
    }
  }
  if (util.inspect.custom) {
    Object.defineProperty(MongooseMap.prototype, util.inspect.custom, {
      enumerable: false,
      writable: false,
      configurable: false,
      value: MongooseMap.prototype.inspect
    });
  }
  Object.defineProperty(MongooseMap.prototype, "$__set", {
    enumerable: false,
    writable: true,
    configurable: false
  });
  Object.defineProperty(MongooseMap.prototype, "$__parent", {
    enumerable: false,
    writable: true,
    configurable: false
  });
  Object.defineProperty(MongooseMap.prototype, "$__path", {
    enumerable: false,
    writable: true,
    configurable: false
  });
  Object.defineProperty(MongooseMap.prototype, "$__schemaType", {
    enumerable: false,
    writable: true,
    configurable: false
  });
  Object.defineProperty(MongooseMap.prototype, "$isMongooseMap", {
    enumerable: false,
    writable: false,
    configurable: false,
    value: true
  });
  Object.defineProperty(MongooseMap.prototype, "$__deferredCalls", {
    enumerable: false,
    writable: false,
    configurable: false,
    value: true
  });
  function checkValidKey(key) {
    const keyType = typeof key;
    if (keyType !== "string") {
      throw new TypeError(`Mongoose maps only support string keys, got ${keyType}`);
    }
    if (key.startsWith("$")) {
      throw new Error(`Mongoose maps do not support keys that start with "$", got "${key}"`);
    }
    if (key.includes(".")) {
      throw new Error(`Mongoose maps do not support keys that contain ".", got "${key}"`);
    }
    if (specialProperties2.has(key)) {
      throw new Error(`Mongoose maps do not support reserved key name "${key}"`);
    }
  }
  map$1 = MongooseMap;
  return map$1;
}
var uuid$2;
var hasRequiredUuid$2;
function requireUuid$2() {
  if (hasRequiredUuid$2) return uuid$2;
  hasRequiredUuid$2 = 1;
  uuid$2 = requireBson().UUID;
  return uuid$2;
}
var hasRequiredTypes;
function requireTypes() {
  if (hasRequiredTypes) return types;
  hasRequiredTypes = 1;
  types.Array = requireArray$1();
  types.Buffer = requireBuffer$1();
  types.Document = // @deprecate
  types.Embedded = requireArraySubdocument();
  types.DocumentArray = requireDocumentArray$1();
  types.Double = requireDouble$2();
  types.Decimal128 = requireDecimal128$2();
  types.ObjectId = requireObjectid$1();
  types.Map = requireMap$1();
  types.Subdocument = requireSubdocument$1();
  types.UUID = requireUuid$2();
  return types;
}
var schema = {};
var schemaArrayOptions;
var hasRequiredSchemaArrayOptions;
function requireSchemaArrayOptions() {
  if (hasRequiredSchemaArrayOptions) return schemaArrayOptions;
  hasRequiredSchemaArrayOptions = 1;
  const SchemaTypeOptions = requireSchemaTypeOptions();
  class SchemaArrayOptions extends SchemaTypeOptions {
  }
  const opts = requirePropertyOptions();
  Object.defineProperty(SchemaArrayOptions.prototype, "enum", opts);
  Object.defineProperty(SchemaArrayOptions.prototype, "of", opts);
  Object.defineProperty(SchemaArrayOptions.prototype, "castNonArrays", opts);
  schemaArrayOptions = SchemaArrayOptions;
  return schemaArrayOptions;
}
var arrayDepth_1;
var hasRequiredArrayDepth;
function requireArrayDepth() {
  if (hasRequiredArrayDepth) return arrayDepth_1;
  hasRequiredArrayDepth = 1;
  arrayDepth_1 = arrayDepth;
  function arrayDepth(arr) {
    if (!Array.isArray(arr)) {
      return { min: 0, max: 0, containsNonArrayItem: true };
    }
    if (arr.length === 0) {
      return { min: 1, max: 1, containsNonArrayItem: false };
    }
    if (arr.length === 1 && !Array.isArray(arr[0])) {
      return { min: 1, max: 1, containsNonArrayItem: false };
    }
    const res = arrayDepth(arr[0]);
    for (let i = 1; i < arr.length; ++i) {
      const _res = arrayDepth(arr[i]);
      if (_res.min < res.min) {
        res.min = _res.min;
      }
      if (_res.max > res.max) {
        res.max = _res.max;
      }
      res.containsNonArrayItem = res.containsNonArrayItem || _res.containsNonArrayItem;
    }
    res.min = res.min + 1;
    res.max = res.max + 1;
    return res;
  }
  return arrayDepth_1;
}
var number$1;
var hasRequiredNumber$1;
function requireNumber$1() {
  if (hasRequiredNumber$1) return number$1;
  hasRequiredNumber$1 = 1;
  number$1 = function castNumber(val) {
    if (val == null) {
      return val;
    }
    if (val === "") {
      return null;
    }
    if (typeof val === "string" || typeof val === "boolean") {
      val = Number(val);
    }
    if (isNaN(val)) {
      throw new Error("Cast to Number failed: value is not a valid number");
    }
    if (val instanceof Number) {
      return val.valueOf();
    }
    if (typeof val === "number") {
      return val;
    }
    if (!Array.isArray(val) && typeof val.valueOf === "function") {
      return Number(val.valueOf());
    }
    if (val.toString && !Array.isArray(val) && val.toString() == Number(val)) {
      return Number(val);
    }
    throw new Error("Cast to Number failed: value is not a valid number");
  };
  return number$1;
}
var omitUndefined;
var hasRequiredOmitUndefined;
function requireOmitUndefined() {
  if (hasRequiredOmitUndefined) return omitUndefined;
  hasRequiredOmitUndefined = 1;
  omitUndefined = function omitUndefined2(val) {
    if (val == null || typeof val !== "object") {
      return val;
    }
    if (Array.isArray(val)) {
      for (let i = val.length - 1; i >= 0; --i) {
        if (val[i] === void 0) {
          val.splice(i, 1);
        }
      }
    }
    for (const key of Object.keys(val)) {
      if (val[key] === void 0) {
        delete val[key];
      }
    }
    return val;
  };
  return omitUndefined;
}
var cast$expr;
var hasRequiredCast$expr;
function requireCast$expr() {
  if (hasRequiredCast$expr) return cast$expr;
  hasRequiredCast$expr = 1;
  const CastError = requireCast$1();
  const StrictModeError = requireStrict();
  const castNumber = requireNumber$1();
  const omitUndefined2 = requireOmitUndefined();
  const booleanComparison = /* @__PURE__ */ new Set(["$and", "$or"]);
  const comparisonOperator = /* @__PURE__ */ new Set(["$cmp", "$eq", "$lt", "$lte", "$gt", "$gte"]);
  const arithmeticOperatorArray = /* @__PURE__ */ new Set([
    // avoid casting '$add' or '$subtract', because expressions can be either number or date,
    // and we don't have a good way of inferring which arguments should be numbers and which should
    // be dates.
    "$multiply",
    "$divide",
    "$log",
    "$mod",
    "$trunc",
    "$avg",
    "$max",
    "$min",
    "$stdDevPop",
    "$stdDevSamp",
    "$sum"
  ]);
  const arithmeticOperatorNumber = /* @__PURE__ */ new Set([
    "$abs",
    "$exp",
    "$ceil",
    "$floor",
    "$ln",
    "$log10",
    "$sqrt",
    "$sin",
    "$cos",
    "$tan",
    "$asin",
    "$acos",
    "$atan",
    "$atan2",
    "$asinh",
    "$acosh",
    "$atanh",
    "$sinh",
    "$cosh",
    "$tanh",
    "$degreesToRadians",
    "$radiansToDegrees"
  ]);
  const arrayElementOperators = /* @__PURE__ */ new Set([
    "$arrayElemAt",
    "$first",
    "$last"
  ]);
  const dateOperators = /* @__PURE__ */ new Set([
    "$year",
    "$month",
    "$week",
    "$dayOfMonth",
    "$dayOfYear",
    "$hour",
    "$minute",
    "$second",
    "$isoDayOfWeek",
    "$isoWeekYear",
    "$isoWeek",
    "$millisecond"
  ]);
  const expressionOperator = /* @__PURE__ */ new Set(["$not"]);
  cast$expr = function cast$expr2(val, schema2, strictQuery) {
    if (typeof val === "boolean") {
      return val;
    }
    if (typeof val !== "object" || val === null) {
      throw new Error("`$expr` must be an object or boolean literal");
    }
    return _castExpression(val, schema2, strictQuery);
  };
  function _castExpression(val, schema2, strictQuery) {
    if (isPath(val) || val === null) {
      return val;
    }
    if (val.$cond != null) {
      if (Array.isArray(val.$cond)) {
        val.$cond = val.$cond.map((expr) => _castExpression(expr, schema2, strictQuery));
      } else {
        val.$cond.if = _castExpression(val.$cond.if, schema2, strictQuery);
        val.$cond.then = _castExpression(val.$cond.then, schema2, strictQuery);
        val.$cond.else = _castExpression(val.$cond.else, schema2, strictQuery);
      }
    } else if (val.$ifNull != null) {
      val.$ifNull.map((v) => _castExpression(v, schema2, strictQuery));
    } else if (val.$switch != null) {
      if (Array.isArray(val.$switch.branches)) {
        val.$switch.branches = val.$switch.branches.map((v) => _castExpression(v, schema2, strictQuery));
      }
      if ("default" in val.$switch) {
        val.$switch.default = _castExpression(val.$switch.default, schema2, strictQuery);
      }
    }
    const keys = Object.keys(val);
    for (const key of keys) {
      if (booleanComparison.has(key)) {
        val[key] = val[key].map((v) => _castExpression(v, schema2, strictQuery));
      } else if (comparisonOperator.has(key)) {
        val[key] = castComparison(val[key], schema2, strictQuery);
      } else if (arithmeticOperatorArray.has(key)) {
        val[key] = castArithmetic(val[key]);
      } else if (arithmeticOperatorNumber.has(key)) {
        val[key] = castNumberOperator(val[key]);
      } else if (expressionOperator.has(key)) {
        val[key] = _castExpression(val[key], schema2, strictQuery);
      }
    }
    if (val.$in) {
      val.$in = castIn(val.$in, schema2, strictQuery);
    }
    if (val.$size) {
      val.$size = castNumberOperator(val.$size);
    }
    if (val.$round) {
      const $round = val.$round;
      if (!Array.isArray($round) || $round.length < 1 || $round.length > 2) {
        throw new CastError("Array", $round, "$round");
      }
      val.$round = $round.map((v) => castNumberOperator(v));
    }
    omitUndefined2(val);
    return val;
  }
  function castNumberOperator(val) {
    if (!isLiteral(val)) {
      return val;
    }
    try {
      return castNumber(val);
    } catch {
      throw new CastError("Number", val);
    }
  }
  function castIn(val, schema2, strictQuery) {
    const path = val[1];
    if (!isPath(path)) {
      return val;
    }
    const search = val[0];
    const schematype = schema2.path(path.slice(1));
    if (schematype === null) {
      if (strictQuery === false) {
        return val;
      } else if (strictQuery === "throw") {
        throw new StrictModeError("$in");
      }
      return void 0;
    }
    if (!schematype.$isMongooseArray) {
      throw new Error("Path must be an array for $in");
    }
    return [
      schematype.embeddedSchemaType.cast(search),
      path
    ];
  }
  function castArithmetic(val) {
    if (!Array.isArray(val)) {
      if (!isLiteral(val)) {
        return val;
      }
      try {
        return castNumber(val);
      } catch {
        throw new CastError("Number", val);
      }
    }
    return val.map((v) => {
      if (!isLiteral(v)) {
        return v;
      }
      try {
        return castNumber(v);
      } catch {
        throw new CastError("Number", v);
      }
    });
  }
  function castComparison(val, schema2, strictQuery) {
    if (!Array.isArray(val) || val.length !== 2) {
      throw new Error("Comparison operator must be an array of length 2");
    }
    val[0] = _castExpression(val[0], schema2, strictQuery);
    const lhs = val[0];
    if (isLiteral(val[1])) {
      let path = null;
      let schematype = null;
      let caster = null;
      if (isPath(lhs)) {
        path = lhs.slice(1);
        schematype = schema2.path(path);
      } else if (typeof lhs === "object" && lhs != null) {
        for (const key of Object.keys(lhs)) {
          if (dateOperators.has(key) && isPath(lhs[key])) {
            path = lhs[key].slice(1) + "." + key;
            caster = castNumber;
          } else if (arrayElementOperators.has(key) && isPath(lhs[key])) {
            path = lhs[key].slice(1) + "." + key;
            schematype = schema2.path(lhs[key].slice(1));
            if (schematype != null) {
              if (schematype.$isMongooseArray) {
                schematype = schematype.embeddedSchemaType;
              }
            }
          }
        }
      }
      const is$literal = typeof val[1] === "object" && val[1] != null && val[1].$literal != null;
      if (schematype != null) {
        if (is$literal) {
          val[1] = { $literal: schematype.cast(val[1].$literal) };
        } else {
          val[1] = schematype.cast(val[1]);
        }
      } else if (caster != null) {
        if (is$literal) {
          try {
            val[1] = { $literal: caster(val[1].$literal) };
          } catch {
            throw new CastError(caster.name.replace(/^cast/, ""), val[1], path + ".$literal");
          }
        } else {
          try {
            val[1] = caster(val[1]);
          } catch {
            throw new CastError(caster.name.replace(/^cast/, ""), val[1], path);
          }
        }
      } else if (path != null && strictQuery === true) {
        return void 0;
      } else if (path != null && strictQuery === "throw") {
        throw new StrictModeError(path);
      }
    } else {
      val[1] = _castExpression(val[1]);
    }
    return val;
  }
  function isPath(val) {
    return typeof val === "string" && val[0] === "$";
  }
  function isLiteral(val) {
    if (typeof val === "string" && val[0] === "$") {
      return false;
    }
    if (typeof val === "object" && val !== null && Object.keys(val).find((key) => key[0] === "$")) {
      return val.$literal != null;
    }
    return true;
  }
  return cast$expr;
}
var string$1;
var hasRequiredString$1;
function requireString$1() {
  if (hasRequiredString$1) return string$1;
  hasRequiredString$1 = 1;
  const CastError = requireCast$1();
  string$1 = function castString(value, path) {
    if (value == null) {
      return value;
    }
    if (typeof value?._id === "string") {
      return value._id;
    }
    if (value.toString && value.toString !== Object.prototype.toString && !Array.isArray(value)) {
      return value.toString();
    }
    throw new CastError("string", value, path);
  };
  return string$1;
}
var text;
var hasRequiredText;
function requireText() {
  if (hasRequiredText) return text;
  hasRequiredText = 1;
  const CastError = requireCast$1();
  const castBoolean = requireBoolean$1();
  const castString = requireString$1();
  text = function castTextSearch(val, path) {
    if (val == null || typeof val !== "object") {
      throw new CastError("$text", val, path);
    }
    if (val.$search != null) {
      val.$search = castString(val.$search, path + ".$search");
    }
    if (val.$language != null) {
      val.$language = castString(val.$language, path + ".$language");
    }
    if (val.$caseSensitive != null) {
      val.$caseSensitive = castBoolean(
        val.$caseSensitive,
        path + ".$caseSensitive"
      );
    }
    if (val.$diacriticSensitive != null) {
      val.$diacriticSensitive = castBoolean(
        val.$diacriticSensitive,
        path + ".$diacriticSensitive"
      );
    }
    return val;
  };
  return text;
}
var isOperator;
var hasRequiredIsOperator;
function requireIsOperator() {
  if (hasRequiredIsOperator) return isOperator;
  hasRequiredIsOperator = 1;
  const specialKeys = /* @__PURE__ */ new Set([
    "$ref",
    "$id",
    "$db"
  ]);
  isOperator = function isOperator2(path) {
    return path[0] === "$" && !specialKeys.has(path);
  };
  return isOperator;
}
var cast;
var hasRequiredCast;
function requireCast() {
  if (hasRequiredCast) return cast;
  hasRequiredCast = 1;
  const CastError = requireCast$1();
  const StrictModeError = requireStrict();
  const Types = requireSchema$1();
  const cast$expr2 = requireCast$expr();
  const castString = requireString$1();
  const castTextSearch = requireText();
  const get2 = requireGet();
  const getSchemaDiscriminatorByValue2 = requireGetSchemaDiscriminatorByValue();
  const isOperator2 = requireIsOperator();
  const util = require$$1;
  const isObject2 = requireIsObject();
  const isMongooseObject2 = requireIsMongooseObject();
  const utils2 = requireUtils();
  const ALLOWED_GEOWITHIN_GEOJSON_TYPES = ["Polygon", "MultiPolygon"];
  cast = function cast2(schema2, obj, options2, context) {
    if (Array.isArray(obj)) {
      throw new Error("Query filter must be an object, got an array " + util.inspect(obj));
    }
    if (obj == null) {
      return obj;
    }
    if (schema2?.discriminators != null && obj[schema2.options.discriminatorKey] != null) {
      schema2 = getSchemaDiscriminatorByValue2(schema2, obj[schema2.options.discriminatorKey]) || schema2;
    }
    const paths = Object.keys(obj);
    let i = paths.length;
    let _keys;
    let any$conditionals;
    let schematype;
    let nested;
    let path;
    let type2;
    let val;
    options2 = options2 || {};
    while (i--) {
      path = paths[i];
      val = obj[path];
      if (path === "$or" || path === "$nor" || path === "$and") {
        if (!Array.isArray(val)) {
          throw new CastError("Array", val, path);
        }
        for (let k = val.length - 1; k >= 0; k--) {
          if (val[k] == null || typeof val[k] !== "object") {
            throw new CastError("Object", val[k], path + "." + k);
          }
          const beforeCastKeysLength = Object.keys(val[k]).length;
          const discriminatorValue = val[k][schema2.options.discriminatorKey];
          if (discriminatorValue == null) {
            val[k] = cast2(schema2, val[k], options2, context);
          } else {
            const discriminatorSchema = getSchemaDiscriminatorByValue2(context.schema, discriminatorValue);
            val[k] = cast2(discriminatorSchema ? discriminatorSchema : schema2, val[k], options2, context);
          }
          if (utils2.hasOwnKeys(val[k]) === false && beforeCastKeysLength !== 0) {
            val.splice(k, 1);
          }
        }
        if (val.length === 0) {
          delete obj[path];
        }
      } else if (path === "$where") {
        type2 = typeof val;
        if (type2 !== "string" && type2 !== "function") {
          throw new Error("Must have a string or function for $where");
        }
        if (type2 === "function") {
          obj[path] = val.toString();
        }
        continue;
      } else if (path === "$expr") {
        val = cast$expr2(val, schema2);
        continue;
      } else if (path === "$elemMatch") {
        val = cast2(schema2, val, options2, context);
      } else if (path === "$text") {
        val = castTextSearch(val, path);
      } else if (path === "$comment" && !Object.hasOwn(schema2.paths, "$comment")) {
        val = castString(val, path);
        obj[path] = val;
      } else {
        if (!schema2) {
          continue;
        }
        schematype = schema2.path(path);
        if (!schematype) {
          const split = path.split(".");
          let j = split.length;
          while (j--) {
            const pathFirstHalf = split.slice(0, j).join(".");
            const pathLastHalf = split.slice(j).join(".");
            const _schematype = schema2.path(pathFirstHalf);
            const discriminatorKey = _schematype?.schema?.options?.discriminatorKey;
            if (_schematype?.schema?.discriminators != null && discriminatorKey != null && pathLastHalf !== discriminatorKey) {
              const discriminatorVal = get2(obj, pathFirstHalf + "." + discriminatorKey);
              const discriminators = _schematype.schema.discriminators;
              if (typeof discriminatorVal === "string" && discriminators[discriminatorVal] != null) {
                schematype = discriminators[discriminatorVal].path(pathLastHalf);
              } else if (discriminatorVal != null && Object.keys(discriminatorVal).length === 1 && Array.isArray(discriminatorVal.$in) && discriminatorVal.$in.length === 1 && typeof discriminatorVal.$in[0] === "string" && discriminators[discriminatorVal.$in[0]] != null) {
                schematype = discriminators[discriminatorVal.$in[0]].path(pathLastHalf);
              }
            }
          }
        }
        if (!schematype) {
          const split = path.split(".");
          let j = split.length;
          let pathFirstHalf;
          let pathLastHalf;
          let remainingConds;
          while (j--) {
            pathFirstHalf = split.slice(0, j).join(".");
            schematype = schema2.path(pathFirstHalf);
            if (schematype) {
              break;
            }
          }
          if (schematype) {
            if (schematype.schema) {
              remainingConds = {};
              pathLastHalf = split.slice(j).join(".");
              remainingConds[pathLastHalf] = val;
              const ret = cast2(schematype.schema, remainingConds, options2, context)[pathLastHalf];
              if (ret === void 0) {
                delete obj[path];
              } else {
                obj[path] = ret;
              }
            } else {
              obj[path] = val;
            }
            continue;
          }
          if (isObject2(val)) {
            let geo = "";
            if (val.$near) {
              geo = "$near";
            } else if (val.$nearSphere) {
              geo = "$nearSphere";
            } else if (val.$within) {
              geo = "$within";
            } else if (val.$geoIntersects) {
              geo = "$geoIntersects";
            } else if (val.$geoWithin) {
              geo = "$geoWithin";
            }
            if (geo) {
              const numbertype = new Types.Number("__QueryCasting__", null, null, schema2);
              let value = val[geo];
              if (val.$maxDistance != null) {
                val.$maxDistance = numbertype.castForQuery(
                  null,
                  val.$maxDistance,
                  context
                );
              }
              if (val.$minDistance != null) {
                val.$minDistance = numbertype.castForQuery(
                  null,
                  val.$minDistance,
                  context
                );
              }
              if (geo === "$within") {
                const withinType = value.$center || value.$centerSphere || value.$box || value.$polygon;
                if (!withinType) {
                  throw new Error("Bad $within parameter: " + JSON.stringify(val));
                }
                value = withinType;
              } else if (geo === "$near" && typeof value.type === "string" && Array.isArray(value.coordinates)) {
                value = value.coordinates;
              } else if ((geo === "$near" || geo === "$nearSphere" || geo === "$geoIntersects") && value.$geometry && typeof value.$geometry.type === "string" && Array.isArray(value.$geometry.coordinates)) {
                if (value.$maxDistance != null) {
                  value.$maxDistance = numbertype.castForQuery(
                    null,
                    value.$maxDistance,
                    context
                  );
                }
                if (value.$minDistance != null) {
                  value.$minDistance = numbertype.castForQuery(
                    null,
                    value.$minDistance,
                    context
                  );
                }
                if (isMongooseObject2(value.$geometry)) {
                  value.$geometry = value.$geometry.toObject({
                    transform: false,
                    virtuals: false
                  });
                }
                value = value.$geometry.coordinates;
              } else if (geo === "$geoWithin") {
                if (value.$geometry) {
                  if (isMongooseObject2(value.$geometry)) {
                    value.$geometry = value.$geometry.toObject({ virtuals: false });
                  }
                  const geoWithinType = value.$geometry.type;
                  if (ALLOWED_GEOWITHIN_GEOJSON_TYPES.indexOf(geoWithinType) === -1) {
                    throw new Error('Invalid geoJSON type for $geoWithin "' + geoWithinType + '", must be "Polygon" or "MultiPolygon"');
                  }
                  value = value.$geometry.coordinates;
                } else {
                  value = value.$box || value.$polygon || value.$center || value.$centerSphere;
                  if (isMongooseObject2(value)) {
                    value = value.toObject({ virtuals: false });
                  }
                }
              }
              _cast(value, numbertype, context);
              continue;
            }
          }
          if (schema2.nested[path]) {
            continue;
          }
          const strict2 = "strict" in options2 ? options2.strict : schema2.options.strict;
          const strictQuery = getStrictQuery(options2, schema2._userProvidedOptions, schema2.options, context);
          if (options2.upsert && strict2) {
            if (strict2 === "throw") {
              throw new StrictModeError(path);
            }
            throw new StrictModeError(path, 'Path "' + path + '" is not in schema, strict mode is `true`, and upsert is `true`.');
          }
          if (strictQuery === "throw") {
            throw new StrictModeError(path, 'Path "' + path + `" is not in schema and strictQuery is 'throw'.`);
          } else if (strictQuery) {
            delete obj[path];
          }
        } else if (val == null) {
          continue;
        } else if (utils2.isPOJO(val)) {
          any$conditionals = Object.keys(val).some(isOperator2);
          if (!any$conditionals) {
            obj[path] = schematype.castForQuery(
              null,
              val,
              context
            );
          } else {
            const ks = Object.keys(val);
            let $cond;
            let k = ks.length;
            while (k--) {
              $cond = ks[k];
              nested = val[$cond];
              if ($cond === "$elemMatch") {
                if (nested && schematype?.schema != null) {
                  cast2(schematype.schema, nested, options2, context);
                } else if (nested && schematype?.$isMongooseArray) {
                  if (utils2.isPOJO(nested) && nested.$not != null) {
                    cast2(schema2, nested, options2, context);
                  } else {
                    val[$cond] = schematype.castForQuery(
                      $cond,
                      nested,
                      context
                    );
                  }
                }
              } else if ($cond === "$not") {
                if (nested && schematype) {
                  _keys = Object.keys(nested);
                  if (_keys.length && isOperator2(_keys[0])) {
                    for (const key in nested) {
                      nested[key] = schematype.castForQuery(
                        key,
                        nested[key],
                        context
                      );
                    }
                  } else {
                    val[$cond] = schematype.castForQuery(
                      $cond,
                      nested,
                      context
                    );
                  }
                  continue;
                }
              } else {
                val[$cond] = schematype.castForQuery(
                  $cond,
                  nested,
                  context
                );
              }
            }
          }
        } else if (Array.isArray(val) && ["Buffer", "Array"].indexOf(schematype.instance) === -1 && !options2.sanitizeFilter) {
          const casted = [];
          const valuesArray = val;
          for (const _val of valuesArray) {
            casted.push(schematype.castForQuery(
              null,
              _val,
              context
            ));
          }
          obj[path] = { $in: casted };
        } else {
          obj[path] = schematype.castForQuery(
            null,
            val,
            context
          );
        }
      }
    }
    return obj;
  };
  function _cast(val, numbertype, context) {
    if (Array.isArray(val)) {
      val.forEach(function(item, i) {
        if (Array.isArray(item) || isObject2(item)) {
          return _cast(item, numbertype, context);
        }
        val[i] = numbertype.castForQuery(null, item, context);
      });
    } else {
      const nearKeys = Object.keys(val);
      let nearLen = nearKeys.length;
      while (nearLen--) {
        const nkey = nearKeys[nearLen];
        const item = val[nkey];
        if (Array.isArray(item) || isObject2(item)) {
          _cast(item, numbertype, context);
          val[nkey] = item;
        } else {
          val[nkey] = numbertype.castForQuery(null, item, context);
        }
      }
    }
  }
  function getStrictQuery(queryOptions, schemaUserProvidedOptions, schemaOptions, context) {
    if ("strictQuery" in queryOptions) {
      return queryOptions.strictQuery;
    }
    if ("strictQuery" in schemaUserProvidedOptions) {
      return schemaUserProvidedOptions.strictQuery;
    }
    const mongooseOptions = context?.mongooseCollection?.conn?.base?.options;
    if (mongooseOptions) {
      if ("strictQuery" in mongooseOptions) {
        return mongooseOptions.strictQuery;
      }
    }
    return schemaOptions.strictQuery;
  }
  return cast;
}
var helpers = {};
var schemaNumberOptions;
var hasRequiredSchemaNumberOptions;
function requireSchemaNumberOptions() {
  if (hasRequiredSchemaNumberOptions) return schemaNumberOptions;
  hasRequiredSchemaNumberOptions = 1;
  const SchemaTypeOptions = requireSchemaTypeOptions();
  class SchemaNumberOptions extends SchemaTypeOptions {
  }
  const opts = requirePropertyOptions();
  Object.defineProperty(SchemaNumberOptions.prototype, "min", opts);
  Object.defineProperty(SchemaNumberOptions.prototype, "max", opts);
  Object.defineProperty(SchemaNumberOptions.prototype, "enum", opts);
  Object.defineProperty(SchemaNumberOptions.prototype, "populate", opts);
  schemaNumberOptions = SchemaNumberOptions;
  return schemaNumberOptions;
}
var bitwise;
var hasRequiredBitwise;
function requireBitwise() {
  if (hasRequiredBitwise) return bitwise;
  hasRequiredBitwise = 1;
  const CastError = requireCast$1();
  function handleBitwiseOperator(val) {
    const _this = this;
    if (Array.isArray(val)) {
      return val.map(function(v) {
        return _castNumber(_this.path, v);
      });
    } else if (Buffer.isBuffer(val)) {
      return val;
    }
    return _castNumber(_this.path, val);
  }
  function _castNumber(path, num) {
    const v = Number(num);
    if (isNaN(v)) {
      throw new CastError("number", num, path);
    }
    return v;
  }
  bitwise = handleBitwiseOperator;
  return bitwise;
}
var number;
var hasRequiredNumber;
function requireNumber() {
  if (hasRequiredNumber) return number;
  hasRequiredNumber = 1;
  const MongooseError = requireError();
  const SchemaNumberOptions = requireSchemaNumberOptions();
  const SchemaType = requireSchemaType();
  const castNumber = requireNumber$1();
  const handleBitwiseOperator = requireBitwise();
  const utils2 = requireUtils();
  const CastError = SchemaType.CastError;
  function SchemaNumber(key, options2, _schemaOptions, parentSchema) {
    this.enumValues = [];
    SchemaType.call(this, key, options2, "Number", parentSchema);
  }
  SchemaNumber.get = SchemaType.get;
  SchemaNumber.set = SchemaType.set;
  SchemaNumber.setters = [];
  SchemaNumber._cast = castNumber;
  SchemaNumber.cast = function cast2(caster) {
    if (arguments.length === 0) {
      return this._cast;
    }
    if (caster === false) {
      caster = this._defaultCaster;
    }
    this._cast = caster;
    return this._cast;
  };
  SchemaNumber._defaultCaster = (v) => {
    if (typeof v !== "number") {
      throw new Error();
    }
    return v;
  };
  SchemaNumber.schemaName = "Number";
  SchemaNumber.defaultOptions = {};
  SchemaNumber.prototype = Object.create(SchemaType.prototype);
  SchemaNumber.prototype.constructor = SchemaNumber;
  SchemaNumber.prototype.OptionsConstructor = SchemaNumberOptions;
  SchemaNumber._checkRequired = (v) => typeof v === "number" || v instanceof Number;
  SchemaNumber.checkRequired = SchemaType.checkRequired;
  SchemaNumber.prototype.checkRequired = function checkRequired(value, doc) {
    if (typeof value === "object" && SchemaType._isRef(this, value, doc, true)) {
      return value != null;
    }
    const _checkRequired = typeof this.constructor.checkRequired === "function" ? this.constructor.checkRequired() : SchemaNumber.checkRequired();
    return _checkRequired(value);
  };
  SchemaNumber.prototype.min = function(value, message) {
    if (this.minValidator) {
      this.validators = this.validators.filter(function(v) {
        return v.validator !== this.minValidator;
      }, this);
    }
    if (value != null) {
      let msg = message || MongooseError.messages.Number.min;
      msg = msg.replace(/{MIN}/, value);
      this.validators.push({
        validator: this.minValidator = function(v) {
          return v == null || v >= value;
        },
        message: msg,
        type: "min",
        min: value
      });
    }
    return this;
  };
  SchemaNumber.prototype.max = function(value, message) {
    if (this.maxValidator) {
      this.validators = this.validators.filter(function(v) {
        return v.validator !== this.maxValidator;
      }, this);
    }
    if (value != null) {
      let msg = message || MongooseError.messages.Number.max;
      msg = msg.replace(/{MAX}/, value);
      this.validators.push({
        validator: this.maxValidator = function(v) {
          return v == null || v <= value;
        },
        message: msg,
        type: "max",
        max: value
      });
    }
    return this;
  };
  SchemaNumber.prototype.enum = function(values, message) {
    if (this.enumValidator) {
      this.validators = this.validators.filter(function(v) {
        return v.validator !== this.enumValidator;
      }, this);
      this.enumValidator = false;
    }
    if (values === void 0 || values === false) {
      return this;
    }
    if (!Array.isArray(values)) {
      const isObjectSyntax = utils2.isPOJO(values) && values.values != null;
      if (isObjectSyntax) {
        message = values.message;
        values = values.values;
      } else if (typeof values === "number") {
        values = Array.prototype.slice.call(arguments);
        message = null;
      }
      if (utils2.isPOJO(values)) {
        const keys = Object.keys(values).sort();
        const objVals = Object.values(values).sort();
        if (keys.length === objVals.length && keys.every((k, i) => k === String(objVals[i])) && objVals.filter((v) => typeof v === "number").length === Math.floor(objVals.length / 2)) {
          values = objVals.filter((v) => typeof v === "number");
        } else {
          values = Object.values(values);
        }
      }
      message = message || MongooseError.messages.Number.enum;
    }
    message = message == null ? MongooseError.messages.Number.enum : message;
    for (const value of values) {
      if (value !== void 0) {
        this.enumValues.push(this.cast(value));
      }
    }
    const vals = this.enumValues;
    this.enumValidator = (v) => v == null || vals.indexOf(v) !== -1;
    this.validators.push({
      validator: this.enumValidator,
      message,
      type: "enum",
      enumValues: vals
    });
    return this;
  };
  SchemaNumber.prototype.cast = function(value, doc, init, prev, options2) {
    if (typeof value !== "number" && SchemaType._isRef(this, value, doc, init)) {
      if (value == null || utils2.isNonBuiltinObject(value)) {
        return this._castRef(value, doc, init, options2);
      }
    }
    const val = value?._id !== void 0 ? value._id : (
      // documents
      value
    );
    let castNumber2;
    if (typeof this._castFunction === "function") {
      castNumber2 = this._castFunction;
    } else if (typeof this.constructor.cast === "function") {
      castNumber2 = this.constructor.cast();
    } else {
      castNumber2 = SchemaNumber.cast();
    }
    try {
      return castNumber2(val);
    } catch (err) {
      throw new CastError("Number", val, this.path, err, this);
    }
  };
  function handleSingle(val) {
    return this.cast(val);
  }
  function handleArray(val) {
    const _this = this;
    if (!Array.isArray(val)) {
      return [this.cast(val)];
    }
    return val.map(function(m) {
      return _this.cast(m);
    });
  }
  const $conditionalHandlers = {
    ...SchemaType.prototype.$conditionalHandlers,
    $bitsAllClear: handleBitwiseOperator,
    $bitsAnyClear: handleBitwiseOperator,
    $bitsAllSet: handleBitwiseOperator,
    $bitsAnySet: handleBitwiseOperator,
    $gt: handleSingle,
    $gte: handleSingle,
    $lt: handleSingle,
    $lte: handleSingle,
    $mod: handleArray
  };
  Object.defineProperty(SchemaNumber.prototype, "$conditionalHandlers", {
    enumerable: false,
    value: $conditionalHandlers
  });
  SchemaNumber.prototype.castForQuery = function($conditional, val, context) {
    let handler;
    if ($conditional != null) {
      handler = this.$conditionalHandlers[$conditional];
      if (!handler) {
        throw new MongooseError("Can't use " + $conditional + " with Number.");
      }
      return handler.call(this, val, context);
    }
    try {
      val = this.applySetters(val, context);
    } catch (err) {
      if (err instanceof CastError && err.path === this.path && this.$fullPath != null) {
        err.path = this.$fullPath;
      }
      throw err;
    }
    return val;
  };
  SchemaNumber.prototype.toJSONSchema = function toJSONSchema(options2) {
    return this._addJSONSchemaEnum(this._createJSONSchemaTypeDefinition("number", "number", options2));
  };
  number = SchemaNumber;
  return number;
}
var hasRequiredHelpers;
function requireHelpers() {
  if (hasRequiredHelpers) return helpers;
  hasRequiredHelpers = 1;
  const SchemaNumber = requireNumber();
  helpers.castToNumber = castToNumber;
  helpers.castArraysOfNumbers = castArraysOfNumbers;
  function castToNumber(val) {
    return SchemaNumber.cast()(val);
  }
  function castArraysOfNumbers(arr, self) {
    arr.forEach(function(v, i) {
      if (Array.isArray(v)) {
        castArraysOfNumbers(v, self);
      } else {
        arr[i] = castToNumber.call(self, v);
      }
    });
  }
  return helpers;
}
var geospatial = {};
var hasRequiredGeospatial;
function requireGeospatial() {
  if (hasRequiredGeospatial) return geospatial;
  hasRequiredGeospatial = 1;
  const castArraysOfNumbers = requireHelpers().castArraysOfNumbers;
  const castToNumber = requireHelpers().castToNumber;
  geospatial.cast$geoIntersects = cast$geoIntersects;
  geospatial.cast$near = cast$near;
  geospatial.cast$within = cast$within;
  function cast$near(val) {
    const SchemaArray = requireArray();
    if (Array.isArray(val)) {
      castArraysOfNumbers(val, this);
      return val;
    }
    _castMinMaxDistance(this, val);
    if (val?.$geometry) {
      return cast$geometry(val, this);
    }
    if (!Array.isArray(val)) {
      throw new TypeError("$near must be either an array or an object with a $geometry property");
    }
    return SchemaArray.prototype.castForQuery.call(this, null, val);
  }
  function cast$geometry(val, self) {
    switch (val.$geometry.type) {
      case "Polygon":
      case "LineString":
      case "Point":
        castArraysOfNumbers(val.$geometry.coordinates, self);
        break;
    }
    _castMinMaxDistance(self, val);
    return val;
  }
  function cast$within(val) {
    _castMinMaxDistance(this, val);
    if (val.$box || val.$polygon) {
      const type2 = val.$box ? "$box" : "$polygon";
      val[type2].forEach((arr) => {
        if (!Array.isArray(arr)) {
          const msg = "Invalid $within $box argument. Expected an array, received " + arr;
          throw new TypeError(msg);
        }
        arr.forEach((v, i) => {
          arr[i] = castToNumber.call(this, v);
        });
      });
    } else if (val.$center || val.$centerSphere) {
      const type2 = val.$center ? "$center" : "$centerSphere";
      val[type2].forEach((item, i) => {
        if (Array.isArray(item)) {
          item.forEach((v, j) => {
            item[j] = castToNumber.call(this, v);
          });
        } else {
          val[type2][i] = castToNumber.call(this, item);
        }
      });
    } else if (val.$geometry) {
      cast$geometry(val, this);
    }
    return val;
  }
  function cast$geoIntersects(val) {
    const geo = val.$geometry;
    if (!geo) {
      return;
    }
    cast$geometry(val, this);
    return val;
  }
  function _castMinMaxDistance(self, val) {
    if (val.$maxDistance) {
      val.$maxDistance = castToNumber.call(self, val.$maxDistance);
    }
    if (val.$minDistance) {
      val.$minDistance = castToNumber.call(self, val.$minDistance);
    }
  }
  return geospatial;
}
var array;
var hasRequiredArray;
function requireArray() {
  if (hasRequiredArray) return array;
  hasRequiredArray = 1;
  const $exists = requireExists();
  const $type = requireType();
  const MongooseError = requireMongooseError();
  const SchemaArrayOptions = requireSchemaArrayOptions();
  const SchemaType = requireSchemaType();
  const CastError = SchemaType.CastError;
  const Mixed = requireMixed();
  const VirtualOptions = requireVirtualOptions();
  const VirtualType = requireVirtualType();
  const arrayDepth = requireArrayDepth();
  const cast2 = requireCast();
  const clone = requireClone();
  const getConstructorName2 = requireGetConstructorName();
  const isOperator2 = requireIsOperator();
  const util = require$$1;
  const utils2 = requireUtils();
  const castToNumber = requireHelpers().castToNumber;
  const geospatial2 = requireGeospatial();
  const getDiscriminatorByValue2 = requireGetDiscriminatorByValue();
  let MongooseArray;
  let EmbeddedDoc;
  const emptyOpts = Object.freeze({});
  function SchemaArray(key, cast3, options2, schemaOptions, parentSchema) {
    EmbeddedDoc || (EmbeddedDoc = requireTypes().Embedded);
    let typeKey = "type";
    if (schemaOptions?.typeKey) {
      typeKey = schemaOptions.typeKey;
    }
    this.schemaOptions = schemaOptions;
    if (cast3) {
      let castOptions = {};
      if (utils2.isPOJO(cast3)) {
        if (cast3[typeKey]) {
          castOptions = clone(cast3);
          delete castOptions[typeKey];
          cast3 = cast3[typeKey];
        } else {
          cast3 = Mixed;
        }
      }
      if (options2?.ref != null && castOptions.ref == null) {
        castOptions.ref = options2.ref;
      }
      if (cast3 === Object) {
        cast3 = Mixed;
      }
      const name = typeof cast3 === "string" ? cast3 : utils2.getFunctionName(cast3);
      const Types = requireSchema$1();
      const schemaTypeDefinition = Object.hasOwn(Types, name) ? Types[name] : cast3;
      if (typeof schemaTypeDefinition === "function") {
        if (schemaTypeDefinition === SchemaArray) {
          this.embeddedSchemaType = new schemaTypeDefinition(key, castOptions, schemaOptions, null, parentSchema);
        } else {
          this.embeddedSchemaType = new schemaTypeDefinition(key, castOptions, schemaOptions, parentSchema);
        }
      } else if (schemaTypeDefinition instanceof SchemaType) {
        this.embeddedSchemaType = schemaTypeDefinition;
        if (!(this.embeddedSchemaType instanceof EmbeddedDoc)) {
          this.embeddedSchemaType.path = key;
        }
      }
    }
    this.$isMongooseArray = true;
    SchemaType.call(this, key, options2, "Array", parentSchema);
    let defaultArr;
    let fn;
    if (this.defaultValue != null) {
      defaultArr = this.defaultValue;
      fn = typeof defaultArr === "function";
    }
    if (!("defaultValue" in this) || this.defaultValue != null) {
      const defaultFn = function() {
        return fn ? defaultArr.call(this) : defaultArr != null ? [].concat(defaultArr) : [];
      };
      defaultFn.$runBeforeSetters = !fn;
      this.default(defaultFn);
    }
  }
  SchemaArray.schemaName = "Array";
  SchemaArray.options = { castNonArrays: true };
  SchemaArray.defaultOptions = {};
  SchemaArray.set = SchemaType.set;
  SchemaArray.setters = [];
  SchemaArray.get = SchemaType.get;
  SchemaArray.prototype = Object.create(SchemaType.prototype);
  SchemaArray.prototype.constructor = SchemaArray;
  SchemaArray.prototype.OptionsConstructor = SchemaArrayOptions;
  SchemaArray._checkRequired = SchemaType.prototype.checkRequired;
  SchemaArray.checkRequired = SchemaType.checkRequired;
  SchemaArray.prototype.virtuals = null;
  SchemaArray.prototype.checkRequired = function checkRequired(value, doc) {
    if (typeof value === "object" && SchemaType._isRef(this, value, doc, true)) {
      return !!value;
    }
    const _checkRequired = typeof this.constructor.checkRequired === "function" ? this.constructor.checkRequired() : SchemaArray.checkRequired();
    return _checkRequired(value);
  };
  SchemaArray.prototype.enum = function() {
    let arr = this;
    while (true) {
      const instance = arr?.embeddedSchemaType?.instance;
      if (instance === "Array") {
        arr = arr.embeddedSchemaType;
        continue;
      }
      if (instance !== "String" && instance !== "Number") {
        throw new Error("`enum` can only be set on an array of strings or numbers , not " + instance);
      }
      break;
    }
    let enumArray = arguments;
    if (!Array.isArray(arguments) && utils2.isObject(arguments)) {
      enumArray = utils2.object.vals(enumArray);
    }
    arr.embeddedSchemaType.enum.apply(arr.embeddedSchemaType, enumArray);
    return this;
  };
  SchemaArray.prototype.applyGetters = function(value, scope) {
    if (scope?.$__ != null && scope.$populated(this.path)) {
      return value;
    }
    const ret = SchemaType.prototype.applyGetters.call(this, value, scope);
    return ret;
  };
  SchemaArray.prototype._applySetters = function(value, scope, init, priorVal) {
    if (this.embeddedSchemaType.$isMongooseArray && SchemaArray.options.castNonArrays) {
      let depth = 0;
      let arr = this;
      while (arr != null && arr.$isMongooseArray && !arr.$isMongooseDocumentArray) {
        ++depth;
        arr = arr.embeddedSchemaType;
      }
      if (value != null && value.length !== 0) {
        const valueDepth = arrayDepth(value);
        if (valueDepth.min === valueDepth.max && valueDepth.max < depth && valueDepth.containsNonArrayItem) {
          for (let i = valueDepth.max; i < depth; ++i) {
            value = [value];
          }
        }
      }
    }
    return SchemaType.prototype._applySetters.call(this, value, scope, init, priorVal);
  };
  SchemaArray.prototype.cast = function(value, doc, init, prev, options2) {
    MongooseArray || (MongooseArray = requireTypes().Array);
    let i;
    let l;
    if (Array.isArray(value)) {
      const len = value.length;
      if (!len && doc) {
        const indexes = doc.schema.indexedPaths();
        const arrayPath = this.path;
        for (i = 0, l = indexes.length; i < l; ++i) {
          const pathIndex = indexes[i][0][arrayPath];
          if (pathIndex === "2dsphere" || pathIndex === "2d") {
            return;
          }
        }
        const arrayGeojsonPath = this.path.endsWith(".coordinates") ? this.path.substring(0, this.path.lastIndexOf(".")) : null;
        if (arrayGeojsonPath != null) {
          for (i = 0, l = indexes.length; i < l; ++i) {
            const pathIndex = indexes[i][0][arrayGeojsonPath];
            if (pathIndex === "2dsphere") {
              return;
            }
          }
        }
      }
      options2 = options2 || emptyOpts;
      let rawValue = utils2.isMongooseArray(value) ? value.__array : value;
      let path = options2.path || this.path;
      if (options2.arrayPathIndex != null) {
        path += "." + options2.arrayPathIndex;
      }
      value = MongooseArray(rawValue, path, doc, this);
      rawValue = value.__array;
      if (init && doc?.$__ != null && doc.$populated(this.path)) {
        return value;
      }
      const caster = this.embeddedSchemaType;
      const isMongooseArray2 = caster.$isMongooseArray;
      if (caster && this.embeddedSchemaType.constructor !== Mixed) {
        try {
          const len2 = rawValue.length;
          for (i = 0; i < len2; i++) {
            const opts = {};
            if (isMongooseArray2) {
              if (options2.arrayPath != null) {
                opts.arrayPathIndex = i;
              } else if (caster._arrayParentPath != null) {
                opts.arrayPathIndex = i;
              }
            }
            if (options2.hydratedPopulatedDocs) {
              opts.hydratedPopulatedDocs = options2.hydratedPopulatedDocs;
            }
            if (options2.virtuals) {
              opts.virtuals = options2.virtuals;
            }
            rawValue[i] = caster.applySetters(rawValue[i], doc, init, void 0, opts);
          }
        } catch (e) {
          throw new CastError("[" + e.kind + "]", util.inspect(value), this.path + "." + i, e, this);
        }
      }
      return value;
    }
    const castNonArraysOption = this.options.castNonArrays ?? SchemaArray.options.castNonArrays;
    if (init || castNonArraysOption) {
      if (doc && init) {
        doc.markModified(this.path);
      }
      return this.cast([value], doc, init);
    }
    throw new CastError("Array", util.inspect(value), this.path, null, this);
  };
  SchemaArray.prototype._castForPopulate = function _castForPopulate(value, doc) {
    MongooseArray || (MongooseArray = requireTypes().Array);
    if (Array.isArray(value)) {
      let i;
      const rawValue = value.__array ? value.__array : value;
      const len = rawValue.length;
      if (this.embeddedSchemaType && this.embeddedSchemaType.constructor !== Mixed) {
        try {
          for (i = 0; i < len; i++) {
            const opts = {};
            if (this.embeddedSchemaType.$isMongooseArray && this.embeddedSchemaType._arrayParentPath != null) {
              opts.arrayPathIndex = i;
            }
            rawValue[i] = this.embeddedSchemaType.cast(rawValue[i], doc, false, void 0, opts);
          }
        } catch (e) {
          throw new CastError("[" + e.kind + "]", util.inspect(value), this.path + "." + i, e, this);
        }
      }
      return value;
    }
    throw new CastError("Array", util.inspect(value), this.path, null, this);
  };
  SchemaArray.prototype.$toObject = SchemaArray.prototype.toObject;
  SchemaArray.prototype.discriminator = function(...args) {
    let arr = this;
    while (arr.$isMongooseArray && !arr.$isMongooseDocumentArray) {
      arr = arr.embeddedSchemaType;
    }
    if (!arr.$isMongooseDocumentArray) {
      throw new MongooseError("You can only add an embedded discriminator on a document array, " + this.path + " is a plain array");
    }
    return arr.discriminator(...args);
  };
  SchemaArray.prototype.clone = function() {
    const options2 = Object.assign({}, this.options);
    const schematype = new this.constructor(this.path, this.embeddedSchemaType, options2, this.schemaOptions, this.parentSchema);
    schematype.validators = this.validators.slice();
    if (this.requiredValidator !== void 0) {
      schematype.requiredValidator = this.requiredValidator;
    }
    return schematype;
  };
  SchemaArray.prototype._castForQuery = function(val, context) {
    let embeddedSchemaType = this.embeddedSchemaType;
    const discriminatorKey = embeddedSchemaType?.schema?.options?.discriminatorKey;
    const discriminators = embeddedSchemaType?.discriminators;
    if (val && discriminators && typeof discriminatorKey === "string") {
      if (discriminators[val[discriminatorKey]]) {
        embeddedSchemaType = discriminators[val[discriminatorKey]];
      } else {
        const constructorByValue = getDiscriminatorByValue2(discriminators, val[discriminatorKey]);
        if (constructorByValue) {
          embeddedSchemaType = constructorByValue;
        }
      }
    }
    if (Array.isArray(val)) {
      for (let i = this.setters.length - 1; i >= 0; i--) {
        val = this.setters[i].call(this, val, this);
      }
      val = val.map(function(v) {
        if (utils2.isObject(v) && v.$elemMatch) {
          return v;
        }
        return embeddedSchemaType.castForQuery(null, v, context);
      });
    } else {
      val = embeddedSchemaType.castForQuery(null, val, context);
    }
    return val;
  };
  SchemaArray.prototype.castForQuery = function($conditional, val, context) {
    let handler;
    if ($conditional != null) {
      handler = this.$conditionalHandlers[$conditional];
      if (!handler) {
        throw new Error("Can't use " + $conditional + " with Array.");
      }
      return handler.call(this, val, context);
    } else {
      return this._castForQuery(val, context);
    }
  };
  SchemaArray.prototype.virtual = function virtual(name, options2) {
    if (name instanceof VirtualType || getConstructorName2(name) === "VirtualType") {
      return this.virtual(name.path, name.options);
    }
    options2 = new VirtualOptions(options2);
    if (utils2.hasUserDefinedProperty(options2, ["ref", "refPath"])) {
      throw new MongooseError("Cannot set populate virtual as a property of an array");
    }
    const virtual2 = new VirtualType(options2, name);
    if (this.virtuals === null) {
      this.virtuals = {};
    }
    this.virtuals[name] = virtual2;
    return virtual2;
  };
  function cast$all(val, context) {
    if (!Array.isArray(val)) {
      val = [val];
    }
    val = val.map((v) => {
      if (!utils2.isObject(v)) {
        return v;
      }
      if (v.$elemMatch != null) {
        return { $elemMatch: cast2(this.embeddedSchemaType.schema, v.$elemMatch, null, this?.$$context) };
      }
      const o = {};
      o[this.path] = v;
      return cast2(this.embeddedSchemaType.schema, o, null, this?.$$context)[this.path];
    }, this);
    return this.castForQuery(null, val, context);
  }
  function cast$elemMatch(val, context) {
    const keys = Object.keys(val);
    const numKeys = keys.length;
    for (let i = 0; i < numKeys; ++i) {
      const key = keys[i];
      const value = val[key];
      if (isOperator2(key) && value != null) {
        val[key] = this.castForQuery(key, value, context);
      }
    }
    return val;
  }
  const handle = SchemaArray.prototype.$conditionalHandlers = {};
  handle.$all = cast$all;
  handle.$options = String;
  handle.$elemMatch = cast$elemMatch;
  handle.$geoIntersects = geospatial2.cast$geoIntersects;
  handle.$or = createLogicalQueryOperatorHandler("$or");
  handle.$and = createLogicalQueryOperatorHandler("$and");
  handle.$nor = createLogicalQueryOperatorHandler("$nor");
  function createLogicalQueryOperatorHandler(op) {
    return function logicalQueryOperatorHandler(val, context) {
      if (!Array.isArray(val)) {
        throw new TypeError("conditional " + op + " requires an array");
      }
      const ret = [];
      for (const obj of val) {
        ret.push(cast2(this.embeddedSchemaType.schema ?? context.schema, obj, null, this?.$$context));
      }
      return ret;
    };
  }
  handle.$near = handle.$nearSphere = geospatial2.cast$near;
  handle.$within = handle.$geoWithin = geospatial2.cast$within;
  handle.$size = handle.$minDistance = handle.$maxDistance = castToNumber;
  handle.$exists = $exists;
  handle.$type = $type;
  handle.$eq = handle.$gt = handle.$gte = handle.$lt = handle.$lte = handle.$not = handle.$regex = handle.$ne = SchemaArray.prototype._castForQuery;
  handle.$nin = SchemaType.prototype.$conditionalHandlers.$nin;
  handle.$in = SchemaType.prototype.$conditionalHandlers.$in;
  SchemaArray.prototype.toJSONSchema = function toJSONSchema(options2) {
    const embeddedSchemaType = this.getEmbeddedSchemaType();
    return {
      ...this._createJSONSchemaTypeDefinition("array", "array", options2),
      items: embeddedSchemaType.toJSONSchema(options2)
    };
  };
  SchemaArray.prototype.autoEncryptionType = function autoEncryptionType() {
    return "array";
  };
  array = SchemaArray;
  return array;
}
var bigint$1;
var hasRequiredBigint$1;
function requireBigint$1() {
  if (hasRequiredBigint$1) return bigint$1;
  hasRequiredBigint$1 = 1;
  const { Long } = requireBson();
  const MAX_BIGINT = 9223372036854775807n;
  const MIN_BIGINT = -9223372036854775808n;
  const ERROR_MESSAGE = `Mongoose only supports BigInts between ${MIN_BIGINT} and ${MAX_BIGINT} because MongoDB does not support arbitrary precision integers`;
  bigint$1 = function castBigInt(val) {
    if (val == null) {
      return val;
    }
    if (val === "") {
      return null;
    }
    if (typeof val === "bigint") {
      if (val > MAX_BIGINT || val < MIN_BIGINT) {
        throw new Error(ERROR_MESSAGE);
      }
      return val;
    }
    if (val instanceof Long) {
      return val.toBigInt();
    }
    if (typeof val === "string" || typeof val === "number") {
      val = BigInt(val);
      if (val > MAX_BIGINT || val < MIN_BIGINT) {
        throw new Error(ERROR_MESSAGE);
      }
      return val;
    }
    throw new Error(`Cannot convert value to BigInt: "${val}"`);
  };
  return bigint$1;
}
var bigint;
var hasRequiredBigint;
function requireBigint() {
  if (hasRequiredBigint) return bigint;
  hasRequiredBigint = 1;
  const CastError = requireCast$1();
  const SchemaType = requireSchemaType();
  const castBigInt = requireBigint$1();
  function SchemaBigInt(path, options2, _schemaOptions, parentSchema) {
    SchemaType.call(this, path, options2, "BigInt", parentSchema);
  }
  SchemaBigInt.schemaName = "BigInt";
  SchemaBigInt.defaultOptions = {};
  SchemaBigInt.prototype = Object.create(SchemaType.prototype);
  SchemaBigInt.prototype.constructor = SchemaBigInt;
  SchemaBigInt._cast = castBigInt;
  SchemaBigInt.set = SchemaType.set;
  SchemaBigInt.setters = [];
  SchemaBigInt.get = SchemaType.get;
  SchemaBigInt.cast = function cast2(caster) {
    if (arguments.length === 0) {
      return this._cast;
    }
    if (caster === false) {
      caster = this._defaultCaster;
    }
    this._cast = caster;
    return this._cast;
  };
  SchemaBigInt._checkRequired = (v) => v != null;
  SchemaBigInt.checkRequired = SchemaType.checkRequired;
  SchemaBigInt.prototype.checkRequired = function(value) {
    return this.constructor._checkRequired(value);
  };
  SchemaBigInt.prototype.cast = function(value) {
    let castBigInt2;
    if (typeof this._castFunction === "function") {
      castBigInt2 = this._castFunction;
    } else if (typeof this.constructor.cast === "function") {
      castBigInt2 = this.constructor.cast();
    } else {
      castBigInt2 = SchemaBigInt.cast();
    }
    try {
      return castBigInt2(value);
    } catch (error2) {
      throw new CastError("BigInt", value, this.path, error2, this);
    }
  };
  const $conditionalHandlers = {
    ...SchemaType.prototype.$conditionalHandlers,
    $gt: handleSingle,
    $gte: handleSingle,
    $lt: handleSingle,
    $lte: handleSingle
  };
  Object.defineProperty(SchemaBigInt.prototype, "$conditionalHandlers", {
    enumerable: false,
    value: $conditionalHandlers
  });
  function handleSingle(val, context) {
    return this.castForQuery(null, val, context);
  }
  SchemaBigInt.prototype.castForQuery = function($conditional, val, context) {
    let handler;
    if ($conditional != null) {
      handler = this.$conditionalHandlers[$conditional];
      if (handler) {
        return handler.call(this, val);
      }
      return this.applySetters(val, context);
    }
    try {
      return this.applySetters(val, context);
    } catch (err) {
      if (err instanceof CastError && err.path === this.path && this.$fullPath != null) {
        err.path = this.$fullPath;
      }
      throw err;
    }
  };
  SchemaBigInt.prototype._castNullish = function _castNullish(v) {
    if (typeof v === "undefined") {
      return v;
    }
    const castBigInt2 = typeof this.constructor.cast === "function" ? this.constructor.cast() : SchemaBigInt.cast();
    if (castBigInt2 == null) {
      return v;
    }
    return v;
  };
  SchemaBigInt.prototype.toJSONSchema = function toJSONSchema(options2) {
    return this._createJSONSchemaTypeDefinition("string", "long", options2);
  };
  SchemaBigInt.prototype.autoEncryptionType = function autoEncryptionType() {
    return "long";
  };
  bigint = SchemaBigInt;
  return bigint;
}
var boolean;
var hasRequiredBoolean;
function requireBoolean() {
  if (hasRequiredBoolean) return boolean;
  hasRequiredBoolean = 1;
  const CastError = requireCast$1();
  const SchemaType = requireSchemaType();
  const castBoolean = requireBoolean$1();
  function SchemaBoolean(path, options2, _schemaOptions, parentSchema) {
    SchemaType.call(this, path, options2, "Boolean", parentSchema);
  }
  SchemaBoolean.schemaName = "Boolean";
  SchemaBoolean.defaultOptions = {};
  SchemaBoolean.prototype = Object.create(SchemaType.prototype);
  SchemaBoolean.prototype.constructor = SchemaBoolean;
  SchemaBoolean._cast = castBoolean;
  SchemaBoolean.set = SchemaType.set;
  SchemaBoolean.setters = [];
  SchemaBoolean.get = SchemaType.get;
  SchemaBoolean.cast = function cast2(caster) {
    if (arguments.length === 0) {
      return this._cast;
    }
    if (caster === false) {
      caster = this._defaultCaster;
    }
    this._cast = caster;
    return this._cast;
  };
  SchemaBoolean._defaultCaster = (v) => {
    if (v != null && typeof v !== "boolean") {
      throw new Error();
    }
    return v;
  };
  SchemaBoolean._checkRequired = (v) => v === true || v === false;
  SchemaBoolean.checkRequired = SchemaType.checkRequired;
  SchemaBoolean.prototype.checkRequired = function(value) {
    return this.constructor._checkRequired(value);
  };
  Object.defineProperty(SchemaBoolean, "convertToTrue", {
    get: () => castBoolean.convertToTrue,
    set: (v) => {
      castBoolean.convertToTrue = v;
    }
  });
  Object.defineProperty(SchemaBoolean, "convertToFalse", {
    get: () => castBoolean.convertToFalse,
    set: (v) => {
      castBoolean.convertToFalse = v;
    }
  });
  SchemaBoolean.prototype.cast = function(value) {
    let castBoolean2;
    if (typeof this._castFunction === "function") {
      castBoolean2 = this._castFunction;
    } else if (typeof this.constructor.cast === "function") {
      castBoolean2 = this.constructor.cast();
    } else {
      castBoolean2 = SchemaBoolean.cast();
    }
    try {
      return castBoolean2(value);
    } catch (error2) {
      throw new CastError("Boolean", value, this.path, error2, this);
    }
  };
  const $conditionalHandlers = { ...SchemaType.prototype.$conditionalHandlers };
  Object.defineProperty(SchemaBoolean.prototype, "$conditionalHandlers", {
    enumerable: false,
    value: $conditionalHandlers
  });
  SchemaBoolean.prototype.castForQuery = function($conditional, val, context) {
    let handler;
    if ($conditional != null) {
      handler = this.$conditionalHandlers[$conditional];
      if (handler) {
        return handler.call(this, val);
      }
      return this.applySetters(val, context);
    }
    try {
      return this.applySetters(val, context);
    } catch (err) {
      if (err instanceof CastError && err.path === this.path && this.$fullPath != null) {
        err.path = this.$fullPath;
      }
      throw err;
    }
  };
  SchemaBoolean.prototype._castNullish = function _castNullish(v) {
    if (typeof v === "undefined") {
      return v;
    }
    const castBoolean2 = typeof this.constructor.cast === "function" ? this.constructor.cast() : SchemaBoolean.cast();
    if (castBoolean2 == null) {
      return v;
    }
    if (castBoolean2.convertToFalse instanceof Set && castBoolean2.convertToFalse.has(v)) {
      return false;
    }
    if (castBoolean2.convertToTrue instanceof Set && castBoolean2.convertToTrue.has(v)) {
      return true;
    }
    return v;
  };
  SchemaBoolean.prototype.toJSONSchema = function toJSONSchema(options2) {
    return this._createJSONSchemaTypeDefinition("boolean", "bool", options2);
  };
  SchemaBoolean.prototype.autoEncryptionType = function autoEncryptionType() {
    return "bool";
  };
  boolean = SchemaBoolean;
  return boolean;
}
var schemaBufferOptions;
var hasRequiredSchemaBufferOptions;
function requireSchemaBufferOptions() {
  if (hasRequiredSchemaBufferOptions) return schemaBufferOptions;
  hasRequiredSchemaBufferOptions = 1;
  const SchemaTypeOptions = requireSchemaTypeOptions();
  class SchemaBufferOptions extends SchemaTypeOptions {
  }
  const opts = requirePropertyOptions();
  Object.defineProperty(SchemaBufferOptions.prototype, "subtype", opts);
  schemaBufferOptions = SchemaBufferOptions;
  return schemaBufferOptions;
}
var buffer;
var hasRequiredBuffer;
function requireBuffer() {
  if (hasRequiredBuffer) return buffer;
  hasRequiredBuffer = 1;
  const MongooseBuffer = requireBuffer$1();
  const SchemaBufferOptions = requireSchemaBufferOptions();
  const SchemaType = requireSchemaType();
  const handleBitwiseOperator = requireBitwise();
  const utils2 = requireUtils();
  const Binary = MongooseBuffer.Binary;
  const CastError = SchemaType.CastError;
  function SchemaBuffer(key, options2, _schemaOptions, parentSchema) {
    SchemaType.call(this, key, options2, "Buffer", parentSchema);
  }
  SchemaBuffer.schemaName = "Buffer";
  SchemaBuffer.defaultOptions = {};
  SchemaBuffer.prototype = Object.create(SchemaType.prototype);
  SchemaBuffer.prototype.constructor = SchemaBuffer;
  SchemaBuffer.prototype.OptionsConstructor = SchemaBufferOptions;
  SchemaBuffer._checkRequired = (v) => !!v?.length;
  SchemaBuffer.set = SchemaType.set;
  SchemaBuffer.setters = [];
  SchemaBuffer.get = SchemaType.get;
  SchemaBuffer.checkRequired = SchemaType.checkRequired;
  SchemaBuffer.prototype.checkRequired = function(value, doc) {
    if (SchemaType._isRef(this, value, doc, true)) {
      return !!value;
    }
    return this.constructor._checkRequired(value);
  };
  SchemaBuffer.prototype.cast = function(value, doc, init, prev, options2) {
    let ret;
    if (SchemaType._isRef(this, value, doc, init)) {
      if (value?.isMongooseBuffer) {
        return value;
      }
      if (Buffer.isBuffer(value)) {
        if (!value?.isMongooseBuffer) {
          value = new MongooseBuffer(value, [this.path, doc]);
          if (this.options.subtype != null) {
            value._subtype = this.options.subtype;
          }
        }
        return value;
      }
      if (value instanceof Binary) {
        ret = new MongooseBuffer(value.value(true), [this.path, doc]);
        if (typeof value.sub_type !== "number") {
          throw new CastError("Buffer", value, this.path, null, this);
        }
        ret._subtype = value.sub_type;
        return ret;
      }
      if (value == null || utils2.isNonBuiltinObject(value)) {
        return this._castRef(value, doc, init, options2);
      }
    }
    if (value?._id) {
      value = value._id;
    }
    if (value?.isMongooseBuffer) {
      return value;
    }
    if (Buffer.isBuffer(value)) {
      if (!value?.isMongooseBuffer) {
        value = new MongooseBuffer(value, [this.path, doc]);
        if (this.options.subtype != null) {
          value._subtype = this.options.subtype;
        }
      }
      return value;
    }
    if (value instanceof Binary) {
      ret = new MongooseBuffer(value.value(true), [this.path, doc]);
      if (typeof value.sub_type !== "number") {
        throw new CastError("Buffer", value, this.path, null, this);
      }
      ret._subtype = value.sub_type;
      return ret;
    }
    if (value === null) {
      return value;
    }
    const type2 = typeof value;
    if (type2 === "string" || type2 === "number" || Array.isArray(value) || type2 === "object" && value.type === "Buffer" && Array.isArray(value.data)) {
      if (type2 === "number") {
        value = [value];
      }
      ret = new MongooseBuffer(value, [this.path, doc]);
      if (this.options.subtype != null) {
        ret._subtype = this.options.subtype;
      }
      return ret;
    }
    if (utils2.isPOJO(value) && (value.$binary instanceof Binary || typeof value.$binary === "string")) {
      const buf = this.cast(Buffer.from(value.$binary, "base64"));
      if (value.$type != null) {
        buf._subtype = value.$type;
        return buf;
      }
    }
    throw new CastError("Buffer", value, this.path, null, this);
  };
  SchemaBuffer.prototype.subtype = function(subtype) {
    this.options.subtype = subtype;
    return this;
  };
  function handleSingle(val, context) {
    return this.castForQuery(null, val, context);
  }
  const $conditionalHandlers = {
    ...SchemaType.prototype.$conditionalHandlers,
    $bitsAllClear: handleBitwiseOperator,
    $bitsAnyClear: handleBitwiseOperator,
    $bitsAllSet: handleBitwiseOperator,
    $bitsAnySet: handleBitwiseOperator,
    $gt: handleSingle,
    $gte: handleSingle,
    $lt: handleSingle,
    $lte: handleSingle
  };
  Object.defineProperty(SchemaBuffer.prototype, "$conditionalHandlers", {
    enumerable: false,
    value: $conditionalHandlers
  });
  SchemaBuffer.prototype.castForQuery = function($conditional, val, context) {
    let handler;
    if ($conditional != null) {
      handler = this.$conditionalHandlers[$conditional];
      if (!handler) {
        throw new Error("Can't use " + $conditional + " with Buffer.");
      }
      return handler.call(this, val);
    }
    let casted;
    try {
      casted = this.applySetters(val, context);
    } catch (err) {
      if (err instanceof CastError && err.path === this.path && this.$fullPath != null) {
        err.path = this.$fullPath;
      }
      throw err;
    }
    return casted ? casted.toObject({ transform: false, virtuals: false }) : casted;
  };
  SchemaBuffer.prototype.toJSONSchema = function toJSONSchema(options2) {
    return this._createJSONSchemaTypeDefinition("string", "binData", options2);
  };
  SchemaBuffer.prototype.autoEncryptionType = function autoEncryptionType() {
    return "binData";
  };
  buffer = SchemaBuffer;
  return buffer;
}
var schemaDateOptions;
var hasRequiredSchemaDateOptions;
function requireSchemaDateOptions() {
  if (hasRequiredSchemaDateOptions) return schemaDateOptions;
  hasRequiredSchemaDateOptions = 1;
  const SchemaTypeOptions = requireSchemaTypeOptions();
  class SchemaDateOptions extends SchemaTypeOptions {
  }
  const opts = requirePropertyOptions();
  Object.defineProperty(SchemaDateOptions.prototype, "min", opts);
  Object.defineProperty(SchemaDateOptions.prototype, "max", opts);
  Object.defineProperty(SchemaDateOptions.prototype, "expires", opts);
  schemaDateOptions = SchemaDateOptions;
  return schemaDateOptions;
}
var date$1;
var hasRequiredDate$1;
function requireDate$1() {
  if (hasRequiredDate$1) return date$1;
  hasRequiredDate$1 = 1;
  const assert = require$$0$2;
  date$1 = function castDate(value) {
    if (value == null || value === "") {
      return null;
    }
    if (value instanceof Date) {
      assert.ok(!isNaN(value.valueOf()));
      return value;
    }
    let date2;
    assert.ok(typeof value !== "boolean");
    if (value instanceof Number || typeof value === "number") {
      date2 = new Date(value);
    } else if (typeof value === "string" && !isNaN(Number(value)) && (Number(value) >= 275761 || Number(value) < -271820)) {
      date2 = new Date(Number(value));
    } else if (typeof value.valueOf === "function") {
      date2 = new Date(value.valueOf());
    } else {
      date2 = new Date(value);
    }
    if (!isNaN(date2.valueOf())) {
      return date2;
    }
    assert.ok(false);
  };
  return date$1;
}
var date;
var hasRequiredDate;
function requireDate() {
  if (hasRequiredDate) return date;
  hasRequiredDate = 1;
  const MongooseError = requireError();
  const SchemaDateOptions = requireSchemaDateOptions();
  const SchemaType = requireSchemaType();
  const castDate = requireDate$1();
  const getConstructorName2 = requireGetConstructorName();
  const utils2 = requireUtils();
  const CastError = SchemaType.CastError;
  function SchemaDate(key, options2, _schemaOptions, parentSchema) {
    SchemaType.call(this, key, options2, "Date", parentSchema);
  }
  SchemaDate.schemaName = "Date";
  SchemaDate.defaultOptions = {};
  SchemaDate.prototype = Object.create(SchemaType.prototype);
  SchemaDate.prototype.constructor = SchemaDate;
  SchemaDate.prototype.OptionsConstructor = SchemaDateOptions;
  SchemaDate._cast = castDate;
  SchemaDate.set = SchemaType.set;
  SchemaDate.setters = [];
  SchemaDate.get = SchemaType.get;
  SchemaDate.cast = function cast2(caster) {
    if (arguments.length === 0) {
      return this._cast;
    }
    if (caster === false) {
      caster = this._defaultCaster;
    }
    this._cast = caster;
    return this._cast;
  };
  SchemaDate._defaultCaster = (v) => {
    if (v != null && !(v instanceof Date)) {
      throw new Error();
    }
    return v;
  };
  SchemaDate.prototype.expires = function(when) {
    if (getConstructorName2(this._index) !== "Object") {
      this._index = {};
    }
    this._index.expires = when;
    utils2.expires(this._index);
    return this;
  };
  SchemaDate._checkRequired = (v) => v instanceof Date;
  SchemaDate.checkRequired = SchemaType.checkRequired;
  SchemaDate.prototype.checkRequired = function(value, doc) {
    if (typeof value === "object" && SchemaType._isRef(this, value, doc, true)) {
      return value != null;
    }
    const _checkRequired = typeof this.constructor.checkRequired === "function" ? this.constructor.checkRequired() : SchemaDate.checkRequired();
    return _checkRequired(value);
  };
  SchemaDate.prototype.min = function(value, message) {
    if (this.minValidator) {
      this.validators = this.validators.filter(function(v) {
        return v.validator !== this.minValidator;
      }, this);
    }
    if (value) {
      let msg = message || MongooseError.messages.Date.min;
      if (typeof msg === "string") {
        msg = msg.replace(/{MIN}/, value === Date.now ? "Date.now()" : value.toString());
      }
      const _this = this;
      this.validators.push({
        validator: this.minValidator = function(val) {
          let _value = value;
          if (typeof value === "function" && value !== Date.now) {
            _value = _value.call(this);
          }
          const min = _value === Date.now ? _value() : _this.cast(_value);
          return val === null || val.valueOf() >= min.valueOf();
        },
        message: msg,
        type: "min",
        min: value
      });
    }
    return this;
  };
  SchemaDate.prototype.max = function(value, message) {
    if (this.maxValidator) {
      this.validators = this.validators.filter(function(v) {
        return v.validator !== this.maxValidator;
      }, this);
    }
    if (value) {
      let msg = message || MongooseError.messages.Date.max;
      if (typeof msg === "string") {
        msg = msg.replace(/{MAX}/, value === Date.now ? "Date.now()" : value.toString());
      }
      const _this = this;
      this.validators.push({
        validator: this.maxValidator = function(val) {
          let _value = value;
          if (typeof _value === "function" && _value !== Date.now) {
            _value = _value.call(this);
          }
          const max = _value === Date.now ? _value() : _this.cast(_value);
          return val === null || val.valueOf() <= max.valueOf();
        },
        message: msg,
        type: "max",
        max: value
      });
    }
    return this;
  };
  SchemaDate.prototype.cast = function(value) {
    let castDate2;
    if (typeof this._castFunction === "function") {
      castDate2 = this._castFunction;
    } else if (typeof this.constructor.cast === "function") {
      castDate2 = this.constructor.cast();
    } else {
      castDate2 = SchemaDate.cast();
    }
    try {
      return castDate2(value);
    } catch (error2) {
      throw new CastError("date", value, this.path, error2, this);
    }
  };
  function handleSingle(val) {
    return this.cast(val);
  }
  const $conditionalHandlers = {
    ...SchemaType.prototype.$conditionalHandlers,
    $gt: handleSingle,
    $gte: handleSingle,
    $lt: handleSingle,
    $lte: handleSingle
  };
  Object.defineProperty(SchemaDate.prototype, "$conditionalHandlers", {
    enumerable: false,
    value: $conditionalHandlers
  });
  SchemaDate.prototype.castForQuery = function($conditional, val, context) {
    if ($conditional == null) {
      try {
        return this.applySetters(val, context);
      } catch (err) {
        if (err instanceof CastError && err.path === this.path && this.$fullPath != null) {
          err.path = this.$fullPath;
        }
        throw err;
      }
    }
    const handler = this.$conditionalHandlers[$conditional];
    if (!handler) {
      throw new Error("Can't use " + $conditional + " with Date.");
    }
    return handler.call(this, val);
  };
  SchemaDate.prototype.toJSONSchema = function toJSONSchema(options2) {
    return this._createJSONSchemaTypeDefinition("string", "date", options2);
  };
  SchemaDate.prototype.autoEncryptionType = function autoEncryptionType() {
    return "date";
  };
  date = SchemaDate;
  return date;
}
var decimal128$1;
var hasRequiredDecimal128$1;
function requireDecimal128$1() {
  if (hasRequiredDecimal128$1) return decimal128$1;
  hasRequiredDecimal128$1 = 1;
  const Decimal128Type = requireDecimal128$2();
  const assert = require$$0$2;
  decimal128$1 = function castDecimal128(value) {
    if (value == null) {
      return value;
    }
    if (typeof value === "object" && typeof value.$numberDecimal === "string") {
      return Decimal128Type.fromString(value.$numberDecimal);
    }
    if (value instanceof Decimal128Type) {
      return value;
    }
    if (typeof value === "string") {
      return Decimal128Type.fromString(value);
    }
    if (typeof Buffer === "function" && Buffer.isBuffer(value)) {
      return new Decimal128Type(value);
    }
    if (typeof Uint8Array === "function" && value instanceof Uint8Array) {
      return new Decimal128Type(value);
    }
    if (typeof value === "number") {
      return Decimal128Type.fromString(String(value));
    }
    if (typeof value.valueOf === "function" && typeof value.valueOf() === "string") {
      return Decimal128Type.fromString(value.valueOf());
    }
    assert.ok(false);
  };
  return decimal128$1;
}
var decimal128;
var hasRequiredDecimal128;
function requireDecimal128() {
  if (hasRequiredDecimal128) return decimal128;
  hasRequiredDecimal128 = 1;
  const SchemaType = requireSchemaType();
  const CastError = SchemaType.CastError;
  const castDecimal128 = requireDecimal128$1();
  const isBsonType = requireIsBsonType();
  function SchemaDecimal128(key, options2, _schemaOptions, parentSchema) {
    SchemaType.call(this, key, options2, "Decimal128", parentSchema);
  }
  SchemaDecimal128.schemaName = "Decimal128";
  SchemaDecimal128.defaultOptions = {};
  SchemaDecimal128.prototype = Object.create(SchemaType.prototype);
  SchemaDecimal128.prototype.constructor = SchemaDecimal128;
  SchemaDecimal128._cast = castDecimal128;
  SchemaDecimal128.set = SchemaType.set;
  SchemaDecimal128.setters = [];
  SchemaDecimal128.get = SchemaType.get;
  SchemaDecimal128.cast = function cast2(caster) {
    if (arguments.length === 0) {
      return this._cast;
    }
    if (caster === false) {
      caster = this._defaultCaster;
    }
    this._cast = caster;
    return this._cast;
  };
  SchemaDecimal128._defaultCaster = (v) => {
    if (v != null && !isBsonType(v, "Decimal128")) {
      throw new Error();
    }
    return v;
  };
  SchemaDecimal128._checkRequired = (v) => isBsonType(v, "Decimal128");
  SchemaDecimal128.checkRequired = SchemaType.checkRequired;
  SchemaDecimal128.prototype.checkRequired = function checkRequired(value, doc) {
    if (SchemaType._isRef(this, value, doc, true)) {
      return !!value;
    }
    const _checkRequired = typeof this.constructor.checkRequired === "function" ? this.constructor.checkRequired() : SchemaDecimal128.checkRequired();
    return _checkRequired(value);
  };
  SchemaDecimal128.prototype.cast = function(value, doc, init, prev, options2) {
    if (SchemaType._isRef(this, value, doc, init)) {
      if (isBsonType(value, "Decimal128")) {
        return value;
      }
      return this._castRef(value, doc, init, options2);
    }
    let castDecimal1282;
    if (typeof this._castFunction === "function") {
      castDecimal1282 = this._castFunction;
    } else if (typeof this.constructor.cast === "function") {
      castDecimal1282 = this.constructor.cast();
    } else {
      castDecimal1282 = SchemaDecimal128.cast();
    }
    try {
      return castDecimal1282(value);
    } catch (error2) {
      throw new CastError("Decimal128", value, this.path, error2, this);
    }
  };
  function handleSingle(val) {
    return this.cast(val);
  }
  const $conditionalHandlers = {
    ...SchemaType.prototype.$conditionalHandlers,
    $gt: handleSingle,
    $gte: handleSingle,
    $lt: handleSingle,
    $lte: handleSingle
  };
  Object.defineProperty(SchemaDecimal128.prototype, "$conditionalHandlers", {
    enumerable: false,
    value: $conditionalHandlers
  });
  SchemaDecimal128.prototype.toJSONSchema = function toJSONSchema(options2) {
    return this._createJSONSchemaTypeDefinition("string", "decimal", options2);
  };
  SchemaDecimal128.prototype.autoEncryptionType = function autoEncryptionType() {
    return "decimal";
  };
  decimal128 = SchemaDecimal128;
  return decimal128;
}
var schemaSubdocumentOptions;
var hasRequiredSchemaSubdocumentOptions;
function requireSchemaSubdocumentOptions() {
  if (hasRequiredSchemaSubdocumentOptions) return schemaSubdocumentOptions;
  hasRequiredSchemaSubdocumentOptions = 1;
  const SchemaTypeOptions = requireSchemaTypeOptions();
  class SchemaSubdocumentOptions extends SchemaTypeOptions {
  }
  const opts = requirePropertyOptions();
  Object.defineProperty(SchemaSubdocumentOptions.prototype, "_id", opts);
  Object.defineProperty(SchemaSubdocumentOptions.prototype, "minimize", opts);
  Object.defineProperty(SchemaSubdocumentOptions.prototype, "discriminators", opts);
  schemaSubdocumentOptions = SchemaSubdocumentOptions;
  return schemaSubdocumentOptions;
}
var plugins = {};
var saveSubdocs;
var hasRequiredSaveSubdocs;
function requireSaveSubdocs() {
  if (hasRequiredSaveSubdocs) return saveSubdocs;
  hasRequiredSaveSubdocs = 1;
  const symbols2 = requireSymbols();
  saveSubdocs = function saveSubdocs2(schema2) {
    const unshift = true;
    schema2.s.hooks.pre("save", false, saveSubdocsPreSave, null, unshift);
    schema2.s.hooks.post("save", saveSubdocsPostSave, null, unshift);
    schema2.s.hooks.pre("save", saveSubdocsPreDeleteOne);
    schema2.s.hooks.post("save", saveSubdocsPostDeleteOne);
  };
  function saveSubdocsPreSave() {
    if (this.$isSubdocument) {
      return;
    }
    const subdocs = this.$getAllSubdocs({ useCache: true });
    if (!subdocs.length) {
      return;
    }
    const options2 = this.$__.saveOptions;
    return Promise.all(subdocs.map((subdoc) => subdoc._execDocumentPreHooks("save", options2, [options2]))).then(() => {
      if (this.$__.saveOptions) {
        this.$__.saveOptions.__subdocs = null;
      }
    });
  }
  function saveSubdocsPostSave() {
    if (this.$isSubdocument) {
      return;
    }
    const subdocs = this.$getAllSubdocs({ useCache: true });
    if (!subdocs.length) {
      return;
    }
    const options2 = this.$__.saveOptions;
    const promises = [];
    for (const subdoc of subdocs) {
      promises.push(subdoc._execDocumentPostHooks("save", options2));
    }
    return Promise.all(promises);
  }
  function saveSubdocsPreDeleteOne() {
    const removedSubdocs = this.$__.removedSubdocs;
    if (!removedSubdocs?.length) {
      return;
    }
    const options2 = this.$__.saveOptions;
    const promises = [];
    for (const subdoc of removedSubdocs) {
      promises.push(subdoc._execDocumentPreHooks("deleteOne", options2));
    }
    return Promise.all(promises);
  }
  function saveSubdocsPostDeleteOne() {
    const removedSubdocs = this.$__.removedSubdocs;
    if (!removedSubdocs?.length) {
      return;
    }
    const options2 = this.$__.saveOptions;
    const promises = [];
    for (const subdoc of removedSubdocs) {
      promises.push(subdoc._execDocumentPostHooks("deleteOne", options2));
    }
    this.$__.removedSubdocs = null;
    return Promise.all(promises);
  }
  saveSubdocsPreSave[symbols2.builtInMiddleware] = true;
  saveSubdocsPostSave[symbols2.builtInMiddleware] = true;
  saveSubdocsPreDeleteOne[symbols2.builtInMiddleware] = true;
  saveSubdocsPostDeleteOne[symbols2.builtInMiddleware] = true;
  return saveSubdocs;
}
var sharding = { exports: {} };
var hasRequiredSharding;
function requireSharding() {
  if (hasRequiredSharding) return sharding.exports;
  hasRequiredSharding = 1;
  const objectIdSymbol = requireSymbols$1().objectIdSymbol;
  const symbols2 = requireSymbols();
  const utils2 = requireUtils();
  sharding.exports = function shardingPlugin(schema2) {
    schema2.post("init", shardingPluginPostInit);
    schema2.pre("save", shardingPluginPreSave);
    schema2.post("save", shardingPluginPostSave);
    schema2.pre("deleteOne", { document: true, query: false }, shardingPluginPreDeleteOne);
    schema2.pre("updateOne", { document: true, query: false }, shardingPluginPreUpdateOne);
  };
  function shardingPluginPostInit() {
    storeShard.call(this);
    return this;
  }
  function shardingPluginPreSave() {
    applyWhere.call(this);
  }
  function shardingPluginPostSave() {
    storeShard.call(this);
  }
  function shardingPluginPreDeleteOne() {
    applyWhere.call(this);
  }
  function shardingPluginPreUpdateOne() {
    applyWhere.call(this);
  }
  function applyWhere() {
    let paths;
    let len;
    if (this.$__.shardval) {
      paths = Object.keys(this.$__.shardval);
      len = paths.length;
      this.$where = this.$where || {};
      for (let i = 0; i < len; ++i) {
        this.$where[paths[i]] = this.$__.shardval[paths[i]];
      }
    }
  }
  sharding.exports.storeShard = storeShard;
  function storeShard() {
    const key = this.$__schema.options.shardKey || this.$__schema.options.shardkey;
    if (!utils2.isPOJO(key)) {
      return;
    }
    const orig = this.$__.shardval = {};
    const paths = Object.keys(key);
    const len = paths.length;
    let val;
    for (let i = 0; i < len; ++i) {
      val = this.$__getValue(paths[i]);
      if (val == null) {
        orig[paths[i]] = val;
      } else if (utils2.isMongooseObject(val)) {
        orig[paths[i]] = val.toObject({ depopulate: true, _isNested: true });
      } else if (val instanceof Date || val[objectIdSymbol]) {
        orig[paths[i]] = val;
      } else if (typeof val.valueOf === "function") {
        orig[paths[i]] = val.valueOf();
      } else {
        orig[paths[i]] = val;
      }
    }
  }
  shardingPluginPostInit[symbols2.builtInMiddleware] = true;
  shardingPluginPreSave[symbols2.builtInMiddleware] = true;
  shardingPluginPostSave[symbols2.builtInMiddleware] = true;
  shardingPluginPreDeleteOne[symbols2.builtInMiddleware] = true;
  shardingPluginPreUpdateOne[symbols2.builtInMiddleware] = true;
  return sharding.exports;
}
var trackTransaction;
var hasRequiredTrackTransaction;
function requireTrackTransaction() {
  if (hasRequiredTrackTransaction) return trackTransaction;
  hasRequiredTrackTransaction = 1;
  const arrayAtomicsSymbol = requireSymbols$1().arrayAtomicsSymbol;
  const sessionNewDocuments = requireSymbols$1().sessionNewDocuments;
  const symbols2 = requireSymbols();
  const utils2 = requireUtils();
  trackTransaction = function trackTransaction2(schema2) {
    schema2.pre("save", trackTransactionPreSave);
    schema2.pre("deleteOne", { document: true, query: false }, trackTransactionPreDeleteOne);
  };
  function trackTransactionPreSave() {
    _getInitialState(this);
  }
  function trackTransactionPreDeleteOne() {
    const initialState = _getInitialState(this);
    if (initialState != null && !Object.hasOwn(initialState, "isDeleted")) {
      initialState.isDeleted = this.$isDeleted();
    }
  }
  function _getInitialState(doc) {
    const session = doc.$session();
    if (session == null) {
      return null;
    }
    if (session.transaction == null || session[sessionNewDocuments] == null) {
      return null;
    }
    if (!session[sessionNewDocuments].has(doc)) {
      const initialState = {};
      if (doc.isNew) {
        initialState.isNew = true;
      }
      if (doc.$__schema.options.versionKey) {
        initialState.versionKey = doc.get(doc.$__schema.options.versionKey);
      }
      initialState.modifiedPaths = new Set(Object.keys(doc.$__.activePaths.getStatePaths("modify")));
      initialState.atomics = _getAtomics(doc);
      session[sessionNewDocuments].set(doc, initialState);
    }
    return session[sessionNewDocuments].get(doc);
  }
  function _getAtomics(doc, previous) {
    const pathToAtomics = /* @__PURE__ */ new Map();
    previous = previous || /* @__PURE__ */ new Map();
    const pathsToCheck = Object.keys(doc.$__.activePaths.init).concat(Object.keys(doc.$__.activePaths.modify));
    for (const path of pathsToCheck) {
      const val = doc.$__getValue(path);
      if (Array.isArray(val) && utils2.isMongooseDocumentArray(val) && val.length && val[arrayAtomicsSymbol] != null && utils2.hasOwnKeys(val[arrayAtomicsSymbol])) {
        const existing = previous.get(path) || {};
        pathToAtomics.set(path, mergeAtomics(existing, val[arrayAtomicsSymbol]));
      }
    }
    const dirty = doc.$__dirty();
    for (const dirt of dirty) {
      const path = dirt.path;
      const val = dirt.value;
      if (val?.[arrayAtomicsSymbol] != null && utils2.hasOwnKeys(val[arrayAtomicsSymbol])) {
        const existing = previous.get(path) || {};
        pathToAtomics.set(path, mergeAtomics(existing, val[arrayAtomicsSymbol]));
      }
    }
    return pathToAtomics;
  }
  function mergeAtomics(destination, source) {
    destination = destination || {};
    if (source.$pullAll != null) {
      destination.$pullAll = (destination.$pullAll || []).concat(source.$pullAll);
    }
    if (source.$push != null) {
      destination.$push = destination.$push || {};
      destination.$push.$each = (destination.$push.$each || []).concat(source.$push.$each);
    }
    if (source.$addToSet != null) {
      destination.$addToSet = (destination.$addToSet || []).concat(source.$addToSet);
    }
    if (source.$set != null) {
      destination.$set = Array.isArray(source.$set) ? [...source.$set] : Object.assign({}, source.$set);
    }
    return destination;
  }
  trackTransactionPreSave[symbols2.builtInMiddleware] = true;
  trackTransactionPreDeleteOne[symbols2.builtInMiddleware] = true;
  return trackTransaction;
}
var hasRequiredPlugins;
function requirePlugins() {
  if (hasRequiredPlugins) return plugins;
  hasRequiredPlugins = 1;
  plugins.saveSubdocs = requireSaveSubdocs();
  plugins.sharding = requireSharding();
  plugins.trackTransaction = requireTrackTransaction();
  return plugins;
}
var applyBuiltinPlugins;
var hasRequiredApplyBuiltinPlugins;
function requireApplyBuiltinPlugins() {
  if (hasRequiredApplyBuiltinPlugins) return applyBuiltinPlugins;
  hasRequiredApplyBuiltinPlugins = 1;
  const builtinPlugins = requirePlugins();
  applyBuiltinPlugins = function applyBuiltinPlugins2(schema2) {
    for (const plugin of Object.values(builtinPlugins)) {
      plugin(schema2, { deduplicate: true });
    }
    schema2.plugins = Object.values(builtinPlugins).map((fn) => ({ fn, opts: { deduplicate: true } })).concat(schema2.plugins);
  };
  return applyBuiltinPlugins;
}
var mergeDiscriminatorSchema;
var hasRequiredMergeDiscriminatorSchema;
function requireMergeDiscriminatorSchema() {
  if (hasRequiredMergeDiscriminatorSchema) return mergeDiscriminatorSchema;
  hasRequiredMergeDiscriminatorSchema = 1;
  const schemaMerge = requireMerge();
  const specialProperties2 = requireSpecialProperties();
  const isBsonType = requireIsBsonType();
  const ObjectId = requireObjectid$1();
  const SchemaType = requireSchemaType();
  const isObject2 = requireIsObject();
  mergeDiscriminatorSchema = function mergeDiscriminatorSchema2(to, from, path, seen) {
    const keys = Object.keys(from);
    let i = 0;
    const len = keys.length;
    let key;
    path = path || "";
    seen = seen || /* @__PURE__ */ new WeakSet();
    if (seen.has(from)) {
      return;
    }
    seen.add(from);
    while (i < len) {
      key = keys[i++];
      if (!path) {
        if (key === "discriminators" || key === "base" || key === "_applyDiscriminators" || key === "_userProvidedOptions" || key === "options" || key === "tree") {
          continue;
        }
      }
      if (path === "tree" && from?.instanceOfSchema) {
        continue;
      }
      if (specialProperties2.has(key)) {
        continue;
      }
      if (key === "parentSchema") {
        continue;
      }
      if (to[key] == null) {
        to[key] = from[key];
      } else if (isObject2(from[key])) {
        if (!isObject2(to[key])) {
          to[key] = {};
        }
        if (from[key] != null) {
          if (from[key].$isSingleNested && to[key].$isMongooseDocumentArray || from[key].$isMongooseDocumentArray && to[key].$isSingleNested || from[key].$isMongooseDocumentArrayElement && to[key].$isMongooseDocumentArrayElement) {
            continue;
          } else if (from[key].instanceOfSchema) {
            if (to[key].instanceOfSchema) {
              schemaMerge(to[key], from[key].clone(), true);
            } else {
              to[key] = from[key].clone();
            }
            continue;
          } else if (isBsonType(from[key], "ObjectId")) {
            to[key] = new ObjectId(from[key]);
            continue;
          } else if (from[key] instanceof SchemaType) {
            if (to[key] == null) {
              to[key] = from[key].clone();
            }
            if (!from[key].$isMongooseDocumentArray && !from[key].$isSingleNested) {
              continue;
            }
          }
        }
        mergeDiscriminatorSchema2(to[key], from[key], path ? path + "." + key : key, seen);
      }
    }
    if (from?.instanceOfSchema) {
      to.tree = Object.assign({}, from.tree, to.tree);
    }
  };
  return mergeDiscriminatorSchema;
}
var discriminator;
var hasRequiredDiscriminator;
function requireDiscriminator() {
  if (hasRequiredDiscriminator) return discriminator;
  hasRequiredDiscriminator = 1;
  const Mixed = requireMixed();
  const applyBuiltinPlugins2 = requireApplyBuiltinPlugins();
  const clone = requireClone();
  const defineKey = requireCompile().defineKey;
  const get2 = requireGet();
  const utils2 = requireUtils();
  const mergeDiscriminatorSchema2 = requireMergeDiscriminatorSchema();
  const CUSTOMIZABLE_DISCRIMINATOR_OPTIONS = {
    toJSON: true,
    toObject: true,
    _id: true,
    id: true,
    virtuals: true,
    methods: true,
    statics: true
  };
  function validateDiscriminatorSchemasForEncryption(parentSchema, childSchema) {
    if (parentSchema.encryptionType() == null && childSchema.encryptionType() == null) return;
    const allSharedNestedPaths = setIntersection(
      allNestedPaths(parentSchema),
      allNestedPaths(childSchema)
    );
    for (const path of allSharedNestedPaths) {
      if (parentSchema._hasEncryptedField(path) && childSchema._hasEncryptedField(path)) {
        throw new Error(`encrypted fields cannot be declared on both the base schema and the child schema in a discriminator. path=${path}`);
      }
      if (parentSchema._hasEncryptedField(path) || childSchema._hasEncryptedField(path)) {
        throw new Error(`encrypted fields cannot have the same path as a non-encrypted field for discriminators. path=${path}`);
      }
    }
    function allNestedPaths(schema2) {
      return [...Object.keys(schema2.paths), ...Object.keys(schema2.singleNestedPaths)];
    }
    function* setIntersection(i1, i2) {
      const s1 = new Set(i1);
      for (const item of i2) {
        if (s1.has(item)) {
          yield item;
        }
      }
    }
  }
  discriminator = function discriminator2(model2, name, schema2, tiedValue, applyPlugins2, mergeHooks, overwriteExisting) {
    if (!schema2?.instanceOfSchema) {
      throw new Error("You must pass a valid discriminator Schema");
    }
    mergeHooks = mergeHooks == null ? true : mergeHooks;
    if (model2.schema.discriminatorMapping && !model2.schema.discriminatorMapping.isRoot) {
      throw new Error('Discriminator "' + name + '" can only be a discriminator of the root model');
    }
    if (applyPlugins2) {
      const applyPluginsToDiscriminators = get2(
        model2.base,
        "options.applyPluginsToDiscriminators",
        false
      ) || !mergeHooks;
      model2.base._applyPlugins(schema2, {
        skipTopLevel: !applyPluginsToDiscriminators
      });
    } else if (!mergeHooks) {
      applyBuiltinPlugins2(schema2);
    }
    const key = model2.schema.options.discriminatorKey;
    const existingPath = model2.schema.path(key);
    if (existingPath != null) {
      if (!utils2.hasUserDefinedProperty(existingPath.options, "select")) {
        existingPath.options.select = true;
      }
      existingPath.options.$skipDiscriminatorCheck = true;
    } else {
      const baseSchemaAddition = {};
      baseSchemaAddition[key] = {
        default: void 0,
        select: true,
        $skipDiscriminatorCheck: true
      };
      baseSchemaAddition[key][model2.schema.options.typeKey] = String;
      model2.schema.add(baseSchemaAddition);
      defineKey({
        prop: key,
        prototype: model2.prototype,
        options: model2.schema.options
      });
    }
    if (schema2.path(key) && schema2.path(key).options.$skipDiscriminatorCheck !== true) {
      throw new Error('Discriminator "' + name + '" cannot have field with name "' + key + '"');
    }
    let value = name;
    if (typeof tiedValue === "string" && tiedValue.length || tiedValue != null) {
      value = tiedValue;
    }
    validateDiscriminatorSchemasForEncryption(model2.schema, schema2);
    function merge2(schema3, baseSchema) {
      schema3._baseSchema = baseSchema;
      if (baseSchema.paths._id && baseSchema.paths._id.options && !baseSchema.paths._id.options.auto) {
        schema3.remove("_id");
      }
      const baseSchemaPaths = Object.keys(baseSchema.paths);
      const conflictingPaths = [];
      for (const path of baseSchemaPaths) {
        if (schema3.nested[path]) {
          conflictingPaths.push(path);
          continue;
        }
        if (path.indexOf(".") === -1) {
          continue;
        }
        const sp = path.split(".").slice(0, -1);
        let cur = "";
        for (const piece of sp) {
          cur += (cur.length ? "." : "") + piece;
          if (schema3.paths[cur] instanceof Mixed || schema3.singleNestedPaths[cur] instanceof Mixed) {
            conflictingPaths.push(path);
          }
        }
      }
      schema3.obj = { ...schema3.obj };
      mergeDiscriminatorSchema2(schema3, baseSchema);
      schema3._gatherChildSchemas();
      for (const conflictingPath of conflictingPaths) {
        delete schema3.paths[conflictingPath];
      }
      schema3.childSchemas.forEach((obj2) => {
        obj2.model.prototype.$__setSchema(obj2.schema);
      });
      const obj = {};
      obj[key] = {
        default: value,
        select: true,
        set: function(newName) {
          if (newName === value || Array.isArray(value) && utils2.deepEqual(newName, value)) {
            return value;
          }
          throw new Error(`Can't set discriminator key "` + key + '"');
        },
        $skipDiscriminatorCheck: true
      };
      obj[key][schema3.options.typeKey] = existingPath ? existingPath.options[schema3.options.typeKey] : String;
      schema3.add(obj);
      schema3.discriminatorMapping = { key, value, isRoot: false };
      if (baseSchema.options.collection) {
        schema3.options.collection = baseSchema.options.collection;
      }
      const toJSON = schema3.options.toJSON;
      const toObject = schema3.options.toObject;
      const _id = schema3.options._id;
      const id = schema3.options.id;
      const keys = Object.keys(schema3.options);
      schema3.options.discriminatorKey = baseSchema.options.discriminatorKey;
      const userProvidedOptions = schema3._userProvidedOptions;
      for (const _key of keys) {
        if (!CUSTOMIZABLE_DISCRIMINATOR_OPTIONS[_key]) {
          if (_key in userProvidedOptions && !utils2.deepEqual(schema3.options[_key], baseSchema.options[_key])) {
            throw new Error("Can't customize discriminator option " + _key + " (can only modify " + Object.keys(CUSTOMIZABLE_DISCRIMINATOR_OPTIONS).join(", ") + ")");
          }
        }
      }
      schema3.options = clone(baseSchema.options);
      for (const _key of Object.keys(userProvidedOptions)) {
        schema3.options[_key] = userProvidedOptions[_key];
      }
      if (toJSON) schema3.options.toJSON = toJSON;
      if (toObject) schema3.options.toObject = toObject;
      if (typeof _id !== "undefined") {
        schema3.options._id = _id;
      }
      schema3.options.id = id;
      if (mergeHooks) {
        schema3.s.hooks = model2.schema.s.hooks.merge(schema3.s.hooks);
      }
      if (applyPlugins2) {
        schema3.plugins = Array.prototype.slice.call(baseSchema.plugins);
      }
      schema3.callQueue = baseSchema.callQueue.concat(schema3.callQueue);
      delete schema3._requiredpaths;
    }
    merge2(schema2, model2.schema);
    if (!model2.discriminators) {
      model2.discriminators = {};
    }
    if (!model2.schema.discriminatorMapping) {
      model2.schema.discriminatorMapping = { key, value: null, isRoot: true };
    }
    if (!model2.schema.discriminators) {
      model2.schema.discriminators = {};
    }
    model2.schema.discriminators[name] = schema2;
    if (model2.discriminators[name] && !schema2.options.overwriteModels && !overwriteExisting) {
      throw new Error('Discriminator with name "' + name + '" already exists');
    }
    return schema2;
  };
  return discriminator;
}
var getConstructor;
var hasRequiredGetConstructor;
function requireGetConstructor() {
  if (hasRequiredGetConstructor) return getConstructor;
  hasRequiredGetConstructor = 1;
  const getDiscriminatorByValue2 = requireGetDiscriminatorByValue();
  getConstructor = function getConstructor2(Constructor, value, defaultDiscriminatorValue) {
    const discriminatorKey = Constructor.schema.options.discriminatorKey;
    let discriminatorValue = value?.[discriminatorKey];
    if (discriminatorValue == null) {
      discriminatorValue = defaultDiscriminatorValue;
    }
    if (Constructor.discriminators && discriminatorValue != null) {
      if (Constructor.discriminators[discriminatorValue]) {
        Constructor = Constructor.discriminators[discriminatorValue];
      } else {
        const constructorByValue = getDiscriminatorByValue2(Constructor.discriminators, discriminatorValue);
        if (constructorByValue) {
          Constructor = constructorByValue;
        }
      }
    }
    return Constructor;
  };
  return getConstructor;
}
var handleIdOption;
var hasRequiredHandleIdOption;
function requireHandleIdOption() {
  if (hasRequiredHandleIdOption) return handleIdOption;
  hasRequiredHandleIdOption = 1;
  const addAutoId2 = requireAddAutoId();
  handleIdOption = function handleIdOption2(schema2, options2) {
    if (options2?._id == null) {
      return schema2;
    }
    schema2 = schema2.clone();
    if (!options2._id) {
      schema2.remove("_id");
      schema2.options._id = false;
    } else if (!schema2.paths["_id"]) {
      addAutoId2(schema2);
      schema2.options._id = true;
    }
    return schema2;
  };
  return handleIdOption;
}
var invalidSchemaOption;
var hasRequiredInvalidSchemaOption;
function requireInvalidSchemaOption() {
  if (hasRequiredInvalidSchemaOption) return invalidSchemaOption;
  hasRequiredInvalidSchemaOption = 1;
  const MongooseError = requireMongooseError();
  class InvalidSchemaOptionError extends MongooseError {
    constructor(name, option) {
      const msg = `Cannot create use schema for property "${name}" because the schema has the ${option} option enabled.`;
      super(msg);
    }
  }
  Object.defineProperty(InvalidSchemaOptionError.prototype, "name", {
    value: "InvalidSchemaOptionError"
  });
  invalidSchemaOption = InvalidSchemaOptionError;
  return invalidSchemaOption;
}
var subdocument;
var hasRequiredSubdocument;
function requireSubdocument() {
  if (hasRequiredSubdocument) return subdocument;
  hasRequiredSubdocument = 1;
  const CastError = requireCast$1();
  const EventEmitter = require$$0.EventEmitter;
  const ObjectExpectedError = requireObjectExpected();
  const SchemaSubdocumentOptions = requireSchemaSubdocumentOptions();
  const SchemaType = requireSchemaType();
  const applyDefaults2 = requireApplyDefaults();
  const $exists = requireExists();
  const castToNumber = requireHelpers().castToNumber;
  const discriminator2 = requireDiscriminator();
  const geospatial2 = requireGeospatial();
  const getConstructor2 = requireGetConstructor();
  const handleIdOption2 = requireHandleIdOption();
  const internalToObjectOptions = requireOptions().internalToObjectOptions;
  const isExclusive2 = requireIsExclusive();
  const utils2 = requireUtils();
  const InvalidSchemaOptionError = requireInvalidSchemaOption();
  let SubdocumentType;
  subdocument = SchemaSubdocument;
  function SchemaSubdocument(schema2, path, options2, parentSchema) {
    if (schema2.options.timeseries) {
      throw new InvalidSchemaOptionError(path, "timeseries");
    }
    const schemaTypeIdOption = SchemaSubdocument.defaultOptions?._id;
    if (schemaTypeIdOption != null) {
      options2 = options2 || {};
      options2._id = schemaTypeIdOption;
    }
    schema2 = handleIdOption2(schema2, options2);
    this.Constructor = _createConstructor(schema2, null, options2);
    this.Constructor.path = path;
    this.Constructor.prototype.$basePath = path;
    this.schema = schema2;
    this.$isSingleNested = true;
    this.base = schema2.base;
    SchemaType.call(this, path, options2, "Embedded", parentSchema);
  }
  SchemaSubdocument.prototype = Object.create(SchemaType.prototype);
  SchemaSubdocument.prototype.constructor = SchemaSubdocument;
  SchemaSubdocument.prototype.OptionsConstructor = SchemaSubdocumentOptions;
  function _createConstructor(schema2, baseClass, options2) {
    SubdocumentType || (SubdocumentType = requireSubdocument$1());
    const _embedded = function SingleNested(value, path, parent) {
      this.$__parent = parent;
      SubdocumentType.apply(this, arguments);
      if (parent == null) {
        return;
      }
      this.$session(parent.$session());
    };
    schema2._preCompile();
    const proto = baseClass?.prototype ?? SubdocumentType.prototype;
    _embedded.prototype = Object.create(proto);
    _embedded.prototype.$__setSchema(schema2);
    _embedded.prototype.constructor = _embedded;
    _embedded.prototype.$__schemaTypeOptions = options2;
    _embedded.$__required = options2?.required;
    _embedded.base = schema2.base;
    _embedded.schema = schema2;
    _embedded.$isSingleNested = true;
    _embedded.events = new EventEmitter();
    _embedded.prototype.toBSON = function() {
      return this.toObject(internalToObjectOptions);
    };
    for (const i in schema2.methods) {
      _embedded.prototype[i] = schema2.methods[i];
    }
    for (const i in schema2.statics) {
      _embedded[i] = schema2.statics[i];
    }
    for (const i in EventEmitter.prototype) {
      _embedded[i] = EventEmitter.prototype[i];
    }
    return _embedded;
  }
  const $conditionalHandlers = { ...SchemaType.prototype.$conditionalHandlers };
  $conditionalHandlers.$geoWithin = function handle$geoWithin(val, context) {
    return { $geometry: this.castForQuery(null, val.$geometry, context) };
  };
  $conditionalHandlers.$near = $conditionalHandlers.$nearSphere = geospatial2.cast$near;
  $conditionalHandlers.$within = $conditionalHandlers.$geoWithin = geospatial2.cast$within;
  $conditionalHandlers.$geoIntersects = geospatial2.cast$geoIntersects;
  $conditionalHandlers.$minDistance = castToNumber;
  $conditionalHandlers.$maxDistance = castToNumber;
  $conditionalHandlers.$exists = $exists;
  Object.defineProperty(SchemaSubdocument.prototype, "$conditionalHandlers", {
    enumerable: false,
    value: $conditionalHandlers
  });
  SchemaSubdocument.prototype.cast = function(val, doc, init, priorVal, options2) {
    if (val?.$isSingleNested && val.parent === doc) {
      return val;
    }
    if (!init && val != null && (typeof val !== "object" || Array.isArray(val))) {
      throw new ObjectExpectedError(this.path, val);
    }
    const discriminatorKeyPath = this.schema.path(this.schema.options.discriminatorKey);
    const defaultDiscriminatorValue = discriminatorKeyPath == null ? null : discriminatorKeyPath.getDefault(doc);
    const Constructor = getConstructor2(this.Constructor, val, defaultDiscriminatorValue);
    let subdoc;
    const parentSelected = doc?.$__?.selected;
    const path = this.path;
    const selected = parentSelected == null ? null : Object.keys(parentSelected).reduce((obj, key) => {
      if (key.startsWith(path + ".")) {
        obj = obj || {};
        obj[key.substring(path.length + 1)] = parentSelected[key];
      }
      return obj;
    }, null);
    if (init) {
      subdoc = new Constructor(void 0, selected, doc, { defaults: false });
      delete subdoc.$__.defaults;
      if (options2.path != null) {
        options2 = { ...options2 };
        delete options2.path;
      }
      subdoc.$init(val, options2);
      const exclude = isExclusive2(selected);
      applyDefaults2(subdoc, selected, exclude);
    } else {
      options2 = Object.assign({}, options2, { priorDoc: priorVal });
      if (utils2.hasOwnKeys(val) === false) {
        return new Constructor({}, selected, doc, options2);
      }
      return new Constructor(val, selected, doc, options2);
    }
    return subdoc;
  };
  SchemaSubdocument.prototype.castForQuery = function($conditional, val, context, options2) {
    let handler;
    if ($conditional != null) {
      handler = this.$conditionalHandlers[$conditional];
      if (!handler) {
        throw new Error("Can't use " + $conditional);
      }
      return handler.call(this, val);
    }
    if (val == null) {
      return val;
    }
    const Constructor = getConstructor2(this.Constructor, val);
    if (val instanceof Constructor) {
      return val;
    }
    if (this.options.runSetters) {
      val = this._applySetters(val, context);
    }
    const overrideStrict = options2?.strict ?? void 0;
    try {
      val = new Constructor(val, overrideStrict);
    } catch (error2) {
      if (!(error2 instanceof CastError)) {
        throw new CastError("Embedded", val, this.path, error2, this);
      }
      throw error2;
    }
    return val;
  };
  SchemaSubdocument.prototype.doValidate = async function doValidate(value, scope, options2) {
    const Constructor = getConstructor2(this.Constructor, value);
    if (value && !(value instanceof Constructor)) {
      value = new Constructor(value, null, scope?.$__ != null ? scope : null);
    }
    if (options2?.skipSchemaValidators) {
      if (!value) {
        return;
      }
      return value.validate();
    }
    await SchemaType.prototype.doValidate.call(this, value, scope, options2);
    if (value != null) {
      await value.validate();
    }
  };
  SchemaSubdocument.prototype.doValidateSync = function(value, scope, options2) {
    if (!options2?.skipSchemaValidators) {
      const schemaTypeError = SchemaType.prototype.doValidateSync.call(this, value, scope);
      if (schemaTypeError) {
        return schemaTypeError;
      }
    }
    if (!value) {
      return;
    }
    return value.$__validateSync();
  };
  SchemaSubdocument.prototype.discriminator = function(name, schema2, options2) {
    options2 = options2 || {};
    const value = utils2.isPOJO(options2) ? options2.value : options2;
    const clone = typeof options2.clone === "boolean" ? options2.clone : true;
    if (schema2.instanceOfSchema && clone) {
      schema2 = schema2.clone();
    }
    schema2 = discriminator2(this.Constructor, name, schema2, value, null, null, options2.overwriteExisting);
    this.Constructor.discriminators[name] = _createConstructor(schema2, this.Constructor);
    return this.Constructor.discriminators[name];
  };
  SchemaSubdocument.defaultOptions = {};
  SchemaSubdocument.set = SchemaType.set;
  SchemaSubdocument.setters = [];
  SchemaSubdocument.get = SchemaType.get;
  SchemaSubdocument.prototype.toJSON = function toJSON() {
    return { path: this.path, options: this.options };
  };
  SchemaSubdocument.prototype.clone = function() {
    const schematype = new this.constructor(
      this.schema,
      this.path,
      { ...this.options, _skipApplyDiscriminators: true },
      this.parentSchema
    );
    schematype.validators = this.validators.slice();
    if (this.requiredValidator !== void 0) {
      schematype.requiredValidator = this.requiredValidator;
    }
    schematype.Constructor.discriminators = Object.assign({}, this.Constructor.discriminators);
    schematype._appliedDiscriminators = this._appliedDiscriminators;
    return schematype;
  };
  SchemaSubdocument.prototype.toJSONSchema = function toJSONSchema(options2) {
    return {
      ...this.schema.toJSONSchema(options2),
      ...this._createJSONSchemaTypeDefinition("object", "object", options2)
    };
  };
  return subdocument;
}
var documentArrayElement;
var hasRequiredDocumentArrayElement;
function requireDocumentArrayElement() {
  if (hasRequiredDocumentArrayElement) return documentArrayElement;
  hasRequiredDocumentArrayElement = 1;
  const MongooseError = requireMongooseError();
  const SchemaType = requireSchemaType();
  const SchemaSubdocument = requireSubdocument();
  const getConstructor2 = requireGetConstructor();
  function SchemaDocumentArrayElement(path, schema2, options2, parentSchema) {
    this.$parentSchemaType = options2?.$parentSchemaType;
    if (!this.$parentSchemaType) {
      throw new MongooseError("Cannot create DocumentArrayElement schematype without a parent");
    }
    delete options2.$parentSchemaType;
    SchemaType.call(this, path, options2, "DocumentArrayElement", parentSchema);
    this.$isMongooseDocumentArrayElement = true;
    this.Constructor = options2?.Constructor;
    this.schema = schema2;
  }
  SchemaDocumentArrayElement.schemaName = "DocumentArrayElement";
  SchemaDocumentArrayElement.defaultOptions = {};
  SchemaDocumentArrayElement.set = SchemaType.set;
  SchemaDocumentArrayElement.get = SchemaType.get;
  SchemaDocumentArrayElement.prototype = Object.create(SchemaType.prototype);
  SchemaDocumentArrayElement.prototype.constructor = SchemaDocumentArrayElement;
  SchemaDocumentArrayElement.prototype.cast = function(...args) {
    return this.$parentSchemaType.cast(...args)[0];
  };
  SchemaDocumentArrayElement.prototype.doValidate = async function doValidate(value, scope, options2) {
    const Constructor = getConstructor2(this.Constructor, value);
    if (value && !(value instanceof Constructor)) {
      value = new Constructor(value, scope, null, null, options2?.index ?? null);
    }
    return SchemaSubdocument.prototype.doValidate.call(this, value, scope, options2);
  };
  SchemaDocumentArrayElement.prototype.clone = function() {
    this.options.$parentSchemaType = this.$parentSchemaType;
    const ret = SchemaType.prototype.clone.apply(this, arguments);
    delete this.options.$parentSchemaType;
    ret.Constructor = this.Constructor;
    ret.schema = this.schema;
    return ret;
  };
  documentArrayElement = SchemaDocumentArrayElement;
  return documentArrayElement;
}
var schemaDocumentArrayOptions;
var hasRequiredSchemaDocumentArrayOptions;
function requireSchemaDocumentArrayOptions() {
  if (hasRequiredSchemaDocumentArrayOptions) return schemaDocumentArrayOptions;
  hasRequiredSchemaDocumentArrayOptions = 1;
  const SchemaTypeOptions = requireSchemaTypeOptions();
  class SchemaDocumentArrayOptions extends SchemaTypeOptions {
  }
  const opts = requirePropertyOptions();
  Object.defineProperty(SchemaDocumentArrayOptions.prototype, "excludeIndexes", opts);
  Object.defineProperty(SchemaDocumentArrayOptions.prototype, "_id", opts);
  Object.defineProperty(SchemaDocumentArrayOptions.prototype, "discriminators", opts);
  schemaDocumentArrayOptions = SchemaDocumentArrayOptions;
  return schemaDocumentArrayOptions;
}
var documentArray;
var hasRequiredDocumentArray;
function requireDocumentArray() {
  if (hasRequiredDocumentArray) return documentArray;
  hasRequiredDocumentArray = 1;
  const CastError = requireCast$1();
  const DocumentArrayElement = requireDocumentArrayElement();
  const EventEmitter = require$$0.EventEmitter;
  const SchemaArray = requireArray();
  const SchemaDocumentArrayOptions = requireSchemaDocumentArrayOptions();
  const SchemaType = requireSchemaType();
  const cast2 = requireCast();
  const discriminator2 = requireDiscriminator();
  const handleIdOption2 = requireHandleIdOption();
  const handleSpreadDoc2 = requireHandleSpreadDoc();
  const isOperator2 = requireIsOperator();
  const utils2 = requireUtils();
  const getConstructor2 = requireGetConstructor();
  const InvalidSchemaOptionError = requireInvalidSchemaOption();
  const arrayAtomicsSymbol = requireSymbols$1().arrayAtomicsSymbol;
  const arrayPathSymbol = requireSymbols$1().arrayPathSymbol;
  const documentArrayParent = requireSymbols$1().documentArrayParent;
  let MongooseDocumentArray;
  let Subdocument;
  function SchemaDocumentArray(key, schema2, options2, schemaOptions, parentSchema) {
    if (schema2.options?.timeseries) {
      throw new InvalidSchemaOptionError(key, "timeseries");
    }
    const schemaTypeIdOption = SchemaDocumentArray.defaultOptions?._id;
    if (schemaTypeIdOption != null) {
      schemaOptions = schemaOptions || {};
      schemaOptions._id = schemaTypeIdOption;
    }
    if (schemaOptions?._id != null) {
      schema2 = handleIdOption2(schema2, schemaOptions);
    } else if (options2?._id != null) {
      schema2 = handleIdOption2(schema2, options2);
    }
    const Constructor = _createConstructor(schema2, options2);
    Constructor.prototype.$basePath = key;
    Constructor.path = key;
    const $parentSchemaType = this;
    const embeddedSchemaType = new DocumentArrayElement(key + ".$", schema2, {
      ...schemaOptions || {},
      $parentSchemaType,
      Constructor
    });
    SchemaArray.call(this, key, embeddedSchemaType, options2, null, parentSchema);
    this.schema = schema2;
    this.schemaOptions = schemaOptions || {};
    this.$isMongooseDocumentArray = true;
    this.Constructor = Constructor;
    Constructor.base = schema2.base;
    const fn = this.defaultValue;
    if (!("defaultValue" in this) || fn != null) {
      this.default(function() {
        let arr = fn.call(this);
        if (arr != null && !Array.isArray(arr)) {
          arr = [arr];
        }
        return arr;
      });
    }
  }
  SchemaDocumentArray.schemaName = "DocumentArray";
  SchemaDocumentArray.options = { castNonArrays: true };
  SchemaDocumentArray.prototype = Object.create(SchemaArray.prototype);
  SchemaDocumentArray.prototype.constructor = SchemaDocumentArray;
  SchemaDocumentArray.prototype.OptionsConstructor = SchemaDocumentArrayOptions;
  Object.defineProperty(SchemaDocumentArray.prototype, "$conditionalHandlers", {
    enumerable: false,
    value: { ...SchemaArray.prototype.$conditionalHandlers }
  });
  function _createConstructor(schema2, options2, baseClass) {
    Subdocument || (Subdocument = requireArraySubdocument());
    function EmbeddedDocument() {
      Subdocument.apply(this, arguments);
      if (this.__parentArray == null || this.__parentArray.getArrayParent() == null) {
        return;
      }
      this.$session(this.__parentArray.getArrayParent().$session());
    }
    schema2._preCompile();
    const proto = baseClass?.prototype ?? Subdocument.prototype;
    EmbeddedDocument.prototype = Object.create(proto);
    EmbeddedDocument.prototype.$__setSchema(schema2);
    EmbeddedDocument.schema = schema2;
    EmbeddedDocument.prototype.constructor = EmbeddedDocument;
    EmbeddedDocument.$isArraySubdocument = true;
    EmbeddedDocument.events = new EventEmitter();
    EmbeddedDocument.base = schema2.base;
    for (const i in schema2.methods) {
      EmbeddedDocument.prototype[i] = schema2.methods[i];
    }
    for (const i in schema2.statics) {
      EmbeddedDocument[i] = schema2.statics[i];
    }
    for (const i in EventEmitter.prototype) {
      EmbeddedDocument[i] = EventEmitter.prototype[i];
    }
    EmbeddedDocument.options = options2;
    return EmbeddedDocument;
  }
  SchemaDocumentArray.prototype.discriminator = function(name, schema2, options2) {
    if (typeof name === "function") {
      name = utils2.getFunctionName(name);
    }
    options2 = options2 || {};
    const tiedValue = utils2.isPOJO(options2) ? options2.value : options2;
    const clone = typeof options2.clone === "boolean" ? options2.clone : true;
    if (schema2.instanceOfSchema && clone) {
      schema2 = schema2.clone();
    }
    schema2 = discriminator2(this.Constructor, name, schema2, tiedValue, null, null, options2?.overwriteExisting);
    const EmbeddedDocument = _createConstructor(schema2, null, this.Constructor);
    EmbeddedDocument.baseCasterConstructor = this.Constructor;
    Object.defineProperty(EmbeddedDocument, "name", {
      value: name
    });
    this.Constructor.discriminators[name] = EmbeddedDocument;
    return this.Constructor.discriminators[name];
  };
  SchemaDocumentArray.prototype.doValidate = async function doValidate(array2, scope, options2) {
    MongooseDocumentArray || (MongooseDocumentArray = requireDocumentArray$1());
    await SchemaType.prototype.doValidate.call(this, array2, scope);
    if (options2?.updateValidator) {
      return;
    }
    if (!utils2.isMongooseDocumentArray(array2)) {
      array2 = new MongooseDocumentArray(array2, this.path, scope);
    }
    const promises = [];
    for (let i = 0; i < array2.length; ++i) {
      let doc = array2[i];
      if (doc == null) {
        continue;
      }
      if (!(doc instanceof Subdocument)) {
        const Constructor = getConstructor2(this.Constructor, array2[i]);
        doc = array2[i] = new Constructor(doc, array2, void 0, void 0, i);
      }
      if (options2?.validateModifiedOnly && !doc.$isModified()) {
        continue;
      }
      promises.push(doc.validate(null, options2));
    }
    await Promise.all(promises);
  };
  SchemaDocumentArray.prototype.doValidateSync = function(array2, scope, options2) {
    const schemaTypeError = SchemaType.prototype.doValidateSync.call(this, array2, scope);
    if (schemaTypeError != null) {
      return schemaTypeError;
    }
    const count = array2?.length;
    let resultError = null;
    if (!count) {
      return;
    }
    for (let i = 0, len = count; i < len; ++i) {
      let doc = array2[i];
      if (!doc) {
        continue;
      }
      if (!(doc instanceof Subdocument)) {
        const Constructor = getConstructor2(this.Constructor, array2[i]);
        doc = array2[i] = new Constructor(doc, array2, void 0, void 0, i);
      }
      if (options2?.validateModifiedOnly && !doc.$isModified()) {
        continue;
      }
      const subdocValidateError = doc.$__validateSync(options2);
      if (subdocValidateError && resultError == null) {
        resultError = subdocValidateError;
      }
    }
    return resultError;
  };
  SchemaDocumentArray.prototype.getDefault = function(scope, init, options2) {
    let ret = typeof this.defaultValue === "function" ? this.defaultValue.call(scope) : this.defaultValue;
    if (ret == null) {
      return ret;
    }
    if (options2?.skipCast) {
      return ret;
    }
    MongooseDocumentArray || (MongooseDocumentArray = requireDocumentArray$1());
    if (!Array.isArray(ret)) {
      ret = [ret];
    }
    ret = new MongooseDocumentArray(ret, this.path, scope);
    for (let i = 0; i < ret.length; ++i) {
      const Constructor = getConstructor2(this.Constructor, ret[i]);
      const _subdoc = new Constructor(
        {},
        ret,
        void 0,
        void 0,
        i
      );
      _subdoc.$init(ret[i]);
      _subdoc.isNew = true;
      Object.assign(_subdoc.$__.activePaths.default, _subdoc.$__.activePaths.init);
      _subdoc.$__.activePaths.init = {};
      ret[i] = _subdoc;
    }
    return ret;
  };
  const _toObjectOptions = Object.freeze({ transform: false, virtuals: false });
  const initDocumentOptions = Object.freeze({ skipId: false, willInit: true });
  SchemaDocumentArray.prototype.cast = function(value, doc, init, prev, options2) {
    MongooseDocumentArray || (MongooseDocumentArray = requireDocumentArray$1());
    if (value?.[arrayPathSymbol] != null && value === prev) {
      return value;
    }
    let selected;
    let subdoc;
    options2 = options2 || {};
    const path = options2.path || this.path;
    if (!Array.isArray(value)) {
      if (!init && !SchemaDocumentArray.options.castNonArrays) {
        throw new CastError("DocumentArray", value, this.path, null, this);
      }
      if (!!doc && init) {
        doc.markModified(path);
      }
      return this.cast([value], doc, init, prev, options2);
    }
    if (!options2.skipDocumentArrayCast || utils2.isMongooseDocumentArray(value)) {
      value = new MongooseDocumentArray(value, path, doc, this);
    }
    if (prev != null) {
      value[arrayAtomicsSymbol] = prev[arrayAtomicsSymbol] || {};
    }
    if (options2.arrayPathIndex != null) {
      value[arrayPathSymbol] = path + "." + options2.arrayPathIndex;
    }
    const rawArray = utils2.isMongooseDocumentArray(value) ? value.__array : value;
    const len = rawArray.length;
    for (let i = 0; i < len; ++i) {
      if (!rawArray[i]) {
        continue;
      }
      const Constructor = getConstructor2(this.Constructor, rawArray[i]);
      const spreadDoc = handleSpreadDoc2(rawArray[i], true);
      if (rawArray[i] !== spreadDoc) {
        rawArray[i] = spreadDoc;
      }
      if (rawArray[i] instanceof Subdocument) {
        if (rawArray[i][documentArrayParent] !== doc || rawArray[i].__index != null && rawArray[i].__index !== i) {
          if (init) {
            const subdoc2 = new Constructor(null, value, initDocumentOptions, selected, i);
            rawArray[i] = subdoc2.$init(rawArray[i]);
          } else {
            const subdoc2 = new Constructor(rawArray[i], value, void 0, void 0, i);
            rawArray[i] = subdoc2;
          }
        }
        if (rawArray[i].__index == null) {
          rawArray[i].$setIndex(i);
        }
      } else if (rawArray[i] != null) {
        if (init) {
          if (doc) {
            selected || (selected = scopePaths(this, doc.$__.selected, init));
          } else {
            selected = true;
          }
          subdoc = new Constructor(null, value, initDocumentOptions, selected, i);
          if (options2.path != null) {
            options2 = { ...options2 };
            delete options2.path;
          }
          rawArray[i] = subdoc.$init(rawArray[i], options2);
        } else {
          if (typeof prev?.id === "function") {
            subdoc = prev.id(rawArray[i]._id);
          }
          if (prev && subdoc && utils2.deepEqual(subdoc.toObject(_toObjectOptions), rawArray[i])) {
            subdoc.set(rawArray[i]);
            rawArray[i] = subdoc;
          } else {
            try {
              subdoc = new Constructor(
                rawArray[i],
                value,
                void 0,
                void 0,
                i
              );
              rawArray[i] = subdoc;
            } catch (error2) {
              throw new CastError(
                "embedded",
                rawArray[i],
                value[arrayPathSymbol],
                error2,
                this
              );
            }
          }
        }
      }
    }
    return value;
  };
  SchemaDocumentArray.prototype.clone = function() {
    const options2 = Object.assign({}, this.options);
    const schematype = new this.constructor(
      this.path,
      this.schema,
      options2,
      this.schemaOptions,
      this.parentSchema
    );
    schematype.validators = this.validators.slice();
    if (this.requiredValidator !== void 0) {
      schematype.requiredValidator = this.requiredValidator;
    }
    schematype.Constructor.discriminators = Object.assign(
      {},
      this.Constructor.discriminators
    );
    schematype._appliedDiscriminators = this._appliedDiscriminators;
    return schematype;
  };
  SchemaDocumentArray.prototype.applyGetters = function(value, scope) {
    return SchemaType.prototype.applyGetters.call(this, value, scope);
  };
  function scopePaths(array2, fields, init) {
    if (!(init && fields)) {
      return void 0;
    }
    const path = array2.path + ".";
    const keys = Object.keys(fields);
    let i = keys.length;
    const selected = {};
    let hasKeys;
    let key;
    let sub;
    while (i--) {
      key = keys[i];
      if (key.startsWith(path)) {
        sub = key.substring(path.length);
        if (sub === "$") {
          continue;
        }
        if (sub.startsWith("$.")) {
          sub = sub.substring(2);
        }
        hasKeys || (hasKeys = true);
        selected[sub] = fields[key];
      }
    }
    return hasKeys && selected || void 0;
  }
  SchemaDocumentArray.defaultOptions = {};
  SchemaDocumentArray.set = SchemaType.set;
  SchemaDocumentArray.setters = [];
  SchemaDocumentArray.get = SchemaType.get;
  SchemaDocumentArray.prototype.$conditionalHandlers.$elemMatch = cast$elemMatch;
  function cast$elemMatch(val, context) {
    const keys = Object.keys(val);
    const numKeys = keys.length;
    for (let i = 0; i < numKeys; ++i) {
      const key = keys[i];
      const value = val[key];
      if (isOperator2(key) && value != null) {
        val[key] = this.castForQuery(key, value, context);
      }
    }
    const discriminatorKey = this?.Constructor?.schema?.options?.discriminatorKey;
    const discriminators = this?.Constructor?.schema?.discriminators || {};
    if (discriminatorKey != null && val[discriminatorKey] != null && discriminators[val[discriminatorKey]] != null) {
      return cast2(discriminators[val[discriminatorKey]], val, null, this?.$$context);
    }
    const schema2 = this.Constructor.schema ?? context.schema;
    return cast2(schema2, val, null, this?.$$context);
  }
  SchemaDocumentArray.prototype.toJSONSchema = function toJSONSchema(options2) {
    const itemsTypeDefinition = this._createJSONSchemaTypeDefinition("object", "object", { ...options2, _overrideRequired: false });
    return {
      ...this._createJSONSchemaTypeDefinition("array", "array", options2),
      items: { ...itemsTypeDefinition, ...this.schema.toJSONSchema(options2) }
    };
  };
  documentArray = SchemaDocumentArray;
  return documentArray;
}
var double$1;
var hasRequiredDouble$1;
function requireDouble$1() {
  if (hasRequiredDouble$1) return double$1;
  hasRequiredDouble$1 = 1;
  const assert = require$$0$2;
  const BSON = requireBson();
  const isBsonType = requireIsBsonType();
  double$1 = function castDouble(val) {
    if (val == null || val === "") {
      return null;
    }
    let coercedVal;
    if (isBsonType(val, "Long")) {
      coercedVal = val.toNumber();
    } else if (typeof val === "string") {
      try {
        coercedVal = BSON.Double.fromString(val);
        return coercedVal;
      } catch {
        assert.ok(false);
      }
    } else if (typeof val === "object") {
      const tempVal = val.valueOf() ?? val.toString();
      if (typeof tempVal === "string") {
        try {
          coercedVal = BSON.Double.fromString(tempVal);
          return coercedVal;
        } catch {
          assert.ok(false);
        }
      } else {
        coercedVal = Number(tempVal);
      }
    } else {
      coercedVal = Number(val);
    }
    return new BSON.Double(coercedVal);
  };
  return double$1;
}
var double;
var hasRequiredDouble;
function requireDouble() {
  if (hasRequiredDouble) return double;
  hasRequiredDouble = 1;
  const CastError = requireCast$1();
  const SchemaType = requireSchemaType();
  const castDouble = requireDouble$1();
  function SchemaDouble(path, options2, _schemaOptions, parentSchema) {
    SchemaType.call(this, path, options2, "Double", parentSchema);
  }
  SchemaDouble.schemaName = "Double";
  SchemaDouble.defaultOptions = {};
  SchemaDouble.prototype = Object.create(SchemaType.prototype);
  SchemaDouble.prototype.constructor = SchemaDouble;
  SchemaDouble._cast = castDouble;
  SchemaDouble.set = SchemaType.set;
  SchemaDouble.setters = [];
  SchemaDouble.get = SchemaType.get;
  SchemaDouble._defaultCaster = (v) => {
    if (v != null) {
      if (v._bsontype !== "Double") {
        throw new Error();
      }
    }
    return v;
  };
  SchemaDouble.cast = function cast2(caster) {
    if (arguments.length === 0) {
      return this._cast;
    }
    if (caster === false) {
      caster = this._defaultCaster;
    }
    this._cast = caster;
    return this._cast;
  };
  SchemaDouble._checkRequired = (v) => v != null;
  SchemaDouble.checkRequired = SchemaType.checkRequired;
  SchemaDouble.prototype.checkRequired = function(value) {
    return this.constructor._checkRequired(value);
  };
  SchemaDouble.prototype.cast = function(value) {
    let castDouble2;
    if (typeof this._castFunction === "function") {
      castDouble2 = this._castFunction;
    } else if (typeof this.constructor.cast === "function") {
      castDouble2 = this.constructor.cast();
    } else {
      castDouble2 = SchemaDouble.cast();
    }
    try {
      return castDouble2(value);
    } catch (error2) {
      throw new CastError("Double", value, this.path, error2, this);
    }
  };
  function handleSingle(val) {
    return this.cast(val);
  }
  const $conditionalHandlers = {
    ...SchemaType.prototype.$conditionalHandlers,
    $gt: handleSingle,
    $gte: handleSingle,
    $lt: handleSingle,
    $lte: handleSingle
  };
  Object.defineProperty(SchemaDouble.prototype, "$conditionalHandlers", {
    enumerable: false,
    value: $conditionalHandlers
  });
  SchemaDouble.prototype.toJSONSchema = function toJSONSchema(options2) {
    return this._createJSONSchemaTypeDefinition("number", "double", options2);
  };
  SchemaDouble.prototype.autoEncryptionType = function autoEncryptionType() {
    return "double";
  };
  double = SchemaDouble;
  return double;
}
var int32$1;
var hasRequiredInt32$1;
function requireInt32$1() {
  if (hasRequiredInt32$1) return int32$1;
  hasRequiredInt32$1 = 1;
  const isBsonType = requireIsBsonType();
  const assert = require$$0$2;
  int32$1 = function castInt32(val) {
    if (val == null) {
      return val;
    }
    if (val === "") {
      return null;
    }
    const coercedVal = isBsonType(val, "Long") ? val.toNumber() : Number(val);
    const INT32_MAX = 2147483647;
    const INT32_MIN = -2147483648;
    if (coercedVal === (coercedVal | 0) && coercedVal >= INT32_MIN && coercedVal <= INT32_MAX) {
      return coercedVal;
    }
    assert.ok(false);
  };
  return int32$1;
}
var int32;
var hasRequiredInt32;
function requireInt32() {
  if (hasRequiredInt32) return int32;
  hasRequiredInt32 = 1;
  const CastError = requireCast$1();
  const SchemaType = requireSchemaType();
  const castInt32 = requireInt32$1();
  const handleBitwiseOperator = requireBitwise();
  function SchemaInt32(path, options2, _schemaOptions, parentSchema) {
    SchemaType.call(this, path, options2, "Int32", parentSchema);
  }
  SchemaInt32.schemaName = "Int32";
  SchemaInt32.defaultOptions = {};
  SchemaInt32.prototype = Object.create(SchemaType.prototype);
  SchemaInt32.prototype.constructor = SchemaInt32;
  SchemaInt32._cast = castInt32;
  SchemaInt32.set = SchemaType.set;
  SchemaInt32.setters = [];
  SchemaInt32.get = SchemaType.get;
  SchemaInt32._defaultCaster = (v) => {
    const INT32_MAX = 2147483647;
    const INT32_MIN = -2147483648;
    if (v != null) {
      if (typeof v !== "number" || v !== (v | 0) || v < INT32_MIN || v > INT32_MAX) {
        throw new Error();
      }
    }
    return v;
  };
  SchemaInt32.cast = function cast2(caster) {
    if (arguments.length === 0) {
      return this._cast;
    }
    if (caster === false) {
      caster = this._defaultCaster;
    }
    this._cast = caster;
    return this._cast;
  };
  SchemaInt32._checkRequired = (v) => v != null;
  SchemaInt32.checkRequired = SchemaType.checkRequired;
  SchemaInt32.prototype.checkRequired = function(value) {
    return this.constructor._checkRequired(value);
  };
  SchemaInt32.prototype.cast = function(value) {
    let castInt322;
    if (typeof this._castFunction === "function") {
      castInt322 = this._castFunction;
    } else if (typeof this.constructor.cast === "function") {
      castInt322 = this.constructor.cast();
    } else {
      castInt322 = SchemaInt32.cast();
    }
    try {
      return castInt322(value);
    } catch (error2) {
      throw new CastError("Int32", value, this.path, error2, this);
    }
  };
  const $conditionalHandlers = {
    ...SchemaType.prototype.$conditionalHandlers,
    $gt: handleSingle,
    $gte: handleSingle,
    $lt: handleSingle,
    $lte: handleSingle,
    $bitsAllClear: handleBitwiseOperator,
    $bitsAnyClear: handleBitwiseOperator,
    $bitsAllSet: handleBitwiseOperator,
    $bitsAnySet: handleBitwiseOperator
  };
  Object.defineProperty(SchemaInt32.prototype, "$conditionalHandlers", {
    enumerable: false,
    value: $conditionalHandlers
  });
  function handleSingle(val, context) {
    return this.castForQuery(null, val, context);
  }
  SchemaInt32.prototype.castForQuery = function($conditional, val, context) {
    let handler;
    if ($conditional != null) {
      handler = this.$conditionalHandlers[$conditional];
      if (handler) {
        return handler.call(this, val);
      }
      return this.applySetters(val, context);
    }
    try {
      return this.applySetters(val, context);
    } catch (err) {
      if (err instanceof CastError && err.path === this.path && this.$fullPath != null) {
        err.path = this.$fullPath;
      }
      throw err;
    }
  };
  SchemaInt32.prototype.toJSONSchema = function toJSONSchema(options2) {
    return this._createJSONSchemaTypeDefinition("number", "int", options2);
  };
  SchemaInt32.prototype.autoEncryptionType = function autoEncryptionType() {
    return "int";
  };
  int32 = SchemaInt32;
  return int32;
}
var schemaMapOptions;
var hasRequiredSchemaMapOptions;
function requireSchemaMapOptions() {
  if (hasRequiredSchemaMapOptions) return schemaMapOptions;
  hasRequiredSchemaMapOptions = 1;
  const SchemaTypeOptions = requireSchemaTypeOptions();
  class SchemaMapOptions extends SchemaTypeOptions {
  }
  const opts = requirePropertyOptions();
  Object.defineProperty(SchemaMapOptions.prototype, "of", opts);
  schemaMapOptions = SchemaMapOptions;
  return schemaMapOptions;
}
var map;
var hasRequiredMap;
function requireMap() {
  if (hasRequiredMap) return map;
  hasRequiredMap = 1;
  const MongooseMap = requireMap$1();
  const SchemaMapOptions = requireSchemaMapOptions();
  const SchemaType = requireSchemaType();
  const MongooseError = requireMongooseError();
  const Schema = requireSchema();
  const utils2 = requireUtils();
  class SchemaMap extends SchemaType {
    /**
     * Map SchemaType constructor.
     *
     * @param {string} path
     * @param {object} options
     * @param {object} schemaOptions
     * @param {Schema} parentSchema
     * @inherits SchemaType
     * @api public
     */
    constructor(key, options2, schemaOptions, parentSchema) {
      super(key, options2, "Map", parentSchema);
      this.$isSchemaMap = true;
      this._createNestedSchemaType(parentSchema, key, options2, schemaOptions);
    }
    /**
     * Sets a default option for all Map instances.
     *
     * @param {string} option The option you'd like to set the value for
     * @param {any} value value for option
     * @return {undefined}
     * @function set
     * @api public
     */
    set(option, value) {
      return SchemaType.set(option, value);
    }
    /**
     * Casts to Map
     *
     * @param {object} value
     * @param {object} model this value is optional
     * @api private
     */
    cast(val, doc, init, prev, options2) {
      if (val instanceof MongooseMap) {
        return val;
      }
      const path = options2?.path ?? this.path;
      if (init) {
        const map2 = new MongooseMap({}, path, doc, this.$__schemaType);
        if (val instanceof commonjsGlobal.Map) {
          for (const key of val.keys()) {
            let _val = val.get(key);
            if (_val == null) {
              _val = map2.$__schemaType._castNullish(_val);
            } else {
              _val = map2.$__schemaType.cast(_val, doc, true, null, { ...options2, path: path + "." + key });
            }
            map2.$init(key, _val);
          }
        } else {
          for (const key of Object.keys(val)) {
            let _val = val[key];
            if (_val == null) {
              _val = map2.$__schemaType._castNullish(_val);
            } else {
              _val = map2.$__schemaType.cast(_val, doc, true, null, { ...options2, path: path + "." + key });
            }
            map2.$init(key, _val);
          }
        }
        return map2;
      }
      return new MongooseMap(val, path, doc, this.$__schemaType);
    }
    /**
     * Creates a copy of this map schema type.
     *
     * @api private
     */
    clone() {
      const schematype = super.clone();
      if (this.$__schemaType != null) {
        schematype.$__schemaType = this.$__schemaType.clone();
      }
      return schematype;
    }
    /**
     * Returns the embedded schema type (i.e. the `.$*` path)
     *
     * @api public
     */
    getEmbeddedSchemaType() {
      return this.$__schemaType;
    }
    /**
     * Returns this schema type's representation in a JSON schema.
     *
     * @param {object} [options]
     * @param {boolean} [options.useBsonType=false] If true, return a representation with `bsonType` for use with MongoDB's `$jsonSchema`.
     * @returns {object} JSON schema properties
     */
    toJSONSchema(options2) {
      const embeddedSchemaType = this.getEmbeddedSchemaType();
      const result = this._createJSONSchemaTypeDefinition("object", "object", options2);
      result.additionalProperties = embeddedSchemaType.toJSONSchema(options2);
      return result;
    }
    /**
     * Returns the auto encryption type for this schema type.
     *
     * @api public
     */
    autoEncryptionType() {
      return "object";
    }
  }
  SchemaMap.schemaName = "Map";
  SchemaMap.prototype.OptionsConstructor = SchemaMapOptions;
  SchemaMap.defaultOptions = {};
  SchemaMap.prototype._createNestedSchemaType = function _createNestedSchemaType(schema2, path, obj, options2) {
    const mapPath = path + ".$*";
    let _mapType = { type: {} };
    if (utils2.hasUserDefinedProperty(obj, "of")) {
      const isInlineSchema = utils2.isPOJO(obj.of) && utils2.hasOwnKeys(obj.of) && !utils2.hasUserDefinedProperty(obj.of, schema2.options.typeKey);
      if (isInlineSchema) {
        _mapType = { [schema2.options.typeKey]: new Schema(obj.of) };
      } else if (utils2.isPOJO(obj.of)) {
        _mapType = Object.assign({}, obj.of);
      } else {
        _mapType = { [schema2.options.typeKey]: obj.of };
      }
      if (_mapType[schema2.options.typeKey] && _mapType[schema2.options.typeKey].instanceOfSchema) {
        const subdocumentSchema = _mapType[schema2.options.typeKey];
        subdocumentSchema.eachPath((subpath, type2) => {
          if (type2.options.select === true || type2.options.select === false) {
            throw new MongooseError('Cannot use schema-level projections (`select: true` or `select: false`) within maps at path "' + path + "." + subpath + '"');
          }
        });
      }
      if (utils2.hasUserDefinedProperty(obj, "ref")) {
        _mapType.ref = obj.ref;
      }
    }
    this.$__schemaType = schema2.interpretAsType(mapPath, _mapType, options2);
  };
  map = SchemaMap;
  return map;
}
var schemaObjectIdOptions;
var hasRequiredSchemaObjectIdOptions;
function requireSchemaObjectIdOptions() {
  if (hasRequiredSchemaObjectIdOptions) return schemaObjectIdOptions;
  hasRequiredSchemaObjectIdOptions = 1;
  const SchemaTypeOptions = requireSchemaTypeOptions();
  class SchemaObjectIdOptions extends SchemaTypeOptions {
  }
  const opts = requirePropertyOptions();
  Object.defineProperty(SchemaObjectIdOptions.prototype, "auto", opts);
  Object.defineProperty(SchemaObjectIdOptions.prototype, "populate", opts);
  schemaObjectIdOptions = SchemaObjectIdOptions;
  return schemaObjectIdOptions;
}
var objectid;
var hasRequiredObjectid;
function requireObjectid() {
  if (hasRequiredObjectid) return objectid;
  hasRequiredObjectid = 1;
  const isBsonType = requireIsBsonType();
  const ObjectId = requireObjectid$1();
  objectid = function castObjectId(value) {
    if (value == null) {
      return value;
    }
    if (isBsonType(value, "ObjectId")) {
      return value;
    }
    if (value._id) {
      if (isBsonType(value._id, "ObjectId")) {
        return value._id;
      }
      if (value._id.toString instanceof Function) {
        return new ObjectId(value._id.toString());
      }
    }
    if (value.toString instanceof Function) {
      return new ObjectId(value.toString());
    }
    return new ObjectId(value);
  };
  return objectid;
}
var objectId;
var hasRequiredObjectId;
function requireObjectId() {
  if (hasRequiredObjectId) return objectId;
  hasRequiredObjectId = 1;
  const SchemaObjectIdOptions = requireSchemaObjectIdOptions();
  const SchemaType = requireSchemaType();
  const castObjectId = requireObjectid();
  const getConstructorName2 = requireGetConstructorName();
  const oid = requireObjectid$1();
  const isBsonType = requireIsBsonType();
  const utils2 = requireUtils();
  const CastError = SchemaType.CastError;
  let Document;
  function SchemaObjectId(key, options2, _schemaOptions, parentSchema) {
    const isKeyHexStr = typeof key === "string" && key.length === 24 && /^[a-f0-9]+$/i.test(key);
    const suppressWarning = options2?.suppressWarning;
    if ((isKeyHexStr || typeof key === "undefined") && !suppressWarning) {
      utils2.warn("mongoose: To create a new ObjectId please try `Mongoose.Types.ObjectId` instead of using `Mongoose.Schema.Types.ObjectId`. Set the `suppressWarning` option if you're trying to create a hex char path in your schema.");
    }
    SchemaType.call(this, key, options2, "ObjectId", parentSchema);
  }
  SchemaObjectId.schemaName = "ObjectId";
  SchemaObjectId.defaultOptions = {};
  SchemaObjectId.prototype = Object.create(SchemaType.prototype);
  SchemaObjectId.prototype.constructor = SchemaObjectId;
  SchemaObjectId.prototype.OptionsConstructor = SchemaObjectIdOptions;
  SchemaObjectId.get = SchemaType.get;
  SchemaObjectId.set = SchemaType.set;
  SchemaObjectId.setters = [];
  SchemaObjectId.prototype.auto = function(turnOn) {
    if (turnOn) {
      this.default(defaultId);
      this.set(resetId);
    }
    return this;
  };
  SchemaObjectId._checkRequired = (v) => isBsonType(v, "ObjectId");
  SchemaObjectId._cast = castObjectId;
  SchemaObjectId.cast = function cast2(caster) {
    if (arguments.length === 0) {
      return this._cast;
    }
    if (caster === false) {
      caster = this._defaultCaster;
    }
    this._cast = caster;
    return this._cast;
  };
  SchemaObjectId._defaultCaster = (v) => {
    if (!isBsonType(v, "ObjectId")) {
      throw new Error(v + " is not an instance of ObjectId");
    }
    return v;
  };
  SchemaObjectId.checkRequired = SchemaType.checkRequired;
  SchemaObjectId.prototype.checkRequired = function checkRequired(value, doc) {
    if (SchemaType._isRef(this, value, doc, true)) {
      return !!value;
    }
    const _checkRequired = typeof this.constructor.checkRequired === "function" ? this.constructor.checkRequired() : SchemaObjectId.checkRequired();
    return _checkRequired(value);
  };
  SchemaObjectId.prototype.cast = function(value, doc, init, prev, options2) {
    if (!isBsonType(value, "ObjectId") && SchemaType._isRef(this, value, doc, init)) {
      if ((getConstructorName2(value) || "").toLowerCase() === "objectid") {
        return new oid(value.toHexString());
      }
      if (value == null || utils2.isNonBuiltinObject(value)) {
        return this._castRef(value, doc, init, options2);
      }
    }
    let castObjectId2;
    if (typeof this._castFunction === "function") {
      castObjectId2 = this._castFunction;
    } else if (typeof this.constructor.cast === "function") {
      castObjectId2 = this.constructor.cast();
    } else {
      castObjectId2 = SchemaObjectId.cast();
    }
    try {
      return castObjectId2(value);
    } catch (error2) {
      throw new CastError("ObjectId", value, this.path, error2, this);
    }
  };
  function handleSingle(val) {
    return this.cast(val);
  }
  const $conditionalHandlers = {
    ...SchemaType.prototype.$conditionalHandlers,
    $gt: handleSingle,
    $gte: handleSingle,
    $lt: handleSingle,
    $lte: handleSingle
  };
  Object.defineProperty(SchemaObjectId.prototype, "$conditionalHandlers", {
    enumerable: false,
    value: $conditionalHandlers
  });
  function defaultId() {
    return new oid();
  }
  defaultId.$runBeforeSetters = true;
  function resetId(v) {
    Document || (Document = requireDocument());
    if (this instanceof Document) {
      if (v === void 0) {
        const _v = new oid();
        return _v;
      }
    }
    return v;
  }
  SchemaObjectId.prototype.toJSONSchema = function toJSONSchema(options2) {
    const jsonSchema = this._createJSONSchemaTypeDefinition("string", "objectId", options2);
    if (!options2?.useBsonType) {
      jsonSchema.pattern = "^[A-Fa-f0-9]{24}$";
    }
    return jsonSchema;
  };
  SchemaObjectId.prototype.autoEncryptionType = function autoEncryptionType() {
    return "objectId";
  };
  objectId = SchemaObjectId;
  return objectId;
}
var schemaStringOptions;
var hasRequiredSchemaStringOptions;
function requireSchemaStringOptions() {
  if (hasRequiredSchemaStringOptions) return schemaStringOptions;
  hasRequiredSchemaStringOptions = 1;
  const SchemaTypeOptions = requireSchemaTypeOptions();
  class SchemaStringOptions extends SchemaTypeOptions {
  }
  const opts = requirePropertyOptions();
  Object.defineProperty(SchemaStringOptions.prototype, "enum", opts);
  Object.defineProperty(SchemaStringOptions.prototype, "match", opts);
  Object.defineProperty(SchemaStringOptions.prototype, "lowercase", opts);
  Object.defineProperty(SchemaStringOptions.prototype, "trim", opts);
  Object.defineProperty(SchemaStringOptions.prototype, "uppercase", opts);
  Object.defineProperty(SchemaStringOptions.prototype, "minLength", opts);
  Object.defineProperty(SchemaStringOptions.prototype, "minlength", opts);
  Object.defineProperty(SchemaStringOptions.prototype, "maxLength", opts);
  Object.defineProperty(SchemaStringOptions.prototype, "maxlength", opts);
  Object.defineProperty(SchemaStringOptions.prototype, "populate", opts);
  schemaStringOptions = SchemaStringOptions;
  return schemaStringOptions;
}
var string;
var hasRequiredString;
function requireString() {
  if (hasRequiredString) return string;
  hasRequiredString = 1;
  const SchemaType = requireSchemaType();
  const MongooseError = requireError();
  const SchemaStringOptions = requireSchemaStringOptions();
  const castString = requireString$1();
  const utils2 = requireUtils();
  const isBsonType = requireIsBsonType();
  const CastError = SchemaType.CastError;
  function SchemaString(key, options2, _schemaOptions, parentSchema) {
    this.enumValues = [];
    this.regExp = null;
    SchemaType.call(this, key, options2, "String", parentSchema);
  }
  SchemaString.schemaName = "String";
  SchemaString.defaultOptions = {};
  SchemaString.prototype = Object.create(SchemaType.prototype);
  SchemaString.prototype.constructor = SchemaString;
  Object.defineProperty(SchemaString.prototype, "OptionsConstructor", {
    configurable: false,
    enumerable: false,
    writable: false,
    value: SchemaStringOptions
  });
  SchemaString._cast = castString;
  SchemaString.cast = function cast2(caster) {
    if (arguments.length === 0) {
      return this._cast;
    }
    if (caster === false) {
      caster = this._defaultCaster;
    }
    this._cast = caster;
    return this._cast;
  };
  SchemaString._defaultCaster = (v) => {
    if (v != null && typeof v !== "string") {
      throw new Error();
    }
    return v;
  };
  SchemaString.get = SchemaType.get;
  SchemaString.set = SchemaType.set;
  SchemaString.setters = [];
  SchemaString._checkRequired = (v) => (v instanceof String || typeof v === "string") && v.length;
  SchemaString.checkRequired = SchemaType.checkRequired;
  SchemaString.prototype.enum = function() {
    if (this.enumValidator) {
      this.validators = this.validators.filter(function(v) {
        return v.validator !== this.enumValidator;
      }, this);
      this.enumValidator = false;
    }
    if (arguments[0] === void 0 || arguments[0] === false) {
      return this;
    }
    let values;
    let errorMessage;
    if (utils2.isObject(arguments[0])) {
      if (Array.isArray(arguments[0].values)) {
        values = arguments[0].values;
        errorMessage = arguments[0].message;
      } else {
        values = utils2.object.vals(arguments[0]);
        errorMessage = MongooseError.messages.String.enum;
      }
    } else {
      values = arguments;
      errorMessage = MongooseError.messages.String.enum;
    }
    for (const value of values) {
      if (value !== void 0) {
        this.enumValues.push(this.cast(value));
      }
    }
    const vals = this.enumValues;
    this.enumValidator = function(v) {
      return null == v || ~vals.indexOf(v);
    };
    this.validators.push({
      validator: this.enumValidator,
      message: errorMessage,
      type: "enum",
      enumValues: vals
    });
    return this;
  };
  SchemaString.prototype.lowercase = function(shouldApply) {
    if (arguments.length > 0 && !shouldApply) {
      return this;
    }
    return this.set((v) => {
      if (typeof v !== "string") {
        v = this.cast(v);
      }
      if (v) {
        return v.toLowerCase();
      }
      return v;
    });
  };
  SchemaString.prototype.uppercase = function(shouldApply) {
    if (arguments.length > 0 && !shouldApply) {
      return this;
    }
    return this.set((v) => {
      if (typeof v !== "string") {
        v = this.cast(v);
      }
      if (v) {
        return v.toUpperCase();
      }
      return v;
    });
  };
  SchemaString.prototype.trim = function(shouldTrim) {
    if (arguments.length > 0 && !shouldTrim) {
      return this;
    }
    return this.set((v) => {
      if (typeof v !== "string") {
        v = this.cast(v);
      }
      if (v) {
        return v.trim();
      }
      return v;
    });
  };
  SchemaString.prototype.minlength = function(value, message) {
    if (this.minlengthValidator) {
      this.validators = this.validators.filter(function(v) {
        return v.validator !== this.minlengthValidator;
      }, this);
    }
    if (value != null) {
      let msg = message || MongooseError.messages.String.minlength;
      msg = msg.replace(/{MINLENGTH}/, value);
      this.validators.push({
        validator: this.minlengthValidator = function(v) {
          return v === null || v.length >= value;
        },
        message: msg,
        type: "minlength",
        minlength: value
      });
    }
    return this;
  };
  SchemaString.prototype.minLength = SchemaString.prototype.minlength;
  SchemaString.prototype.maxlength = function(value, message) {
    if (this.maxlengthValidator) {
      this.validators = this.validators.filter(function(v) {
        return v.validator !== this.maxlengthValidator;
      }, this);
    }
    if (value != null) {
      let msg = message || MongooseError.messages.String.maxlength;
      if (typeof msg !== "function") {
        msg = msg.replace(/{MAXLENGTH}/, value);
      }
      this.validators.push({
        validator: this.maxlengthValidator = function(v) {
          return v === null || v.length <= value;
        },
        message: formatMaxLengthValidatorMessage(msg),
        type: "maxlength",
        maxlength: value
      });
    }
    return this;
  };
  SchemaString.prototype.maxLength = SchemaString.prototype.maxlength;
  SchemaString.prototype.match = function match(regExp, message) {
    const msg = message || MongooseError.messages.String.match;
    const matchValidator = function(v) {
      if (!regExp) {
        return false;
      }
      regExp.lastIndex = 0;
      const ret = v != null && v !== "" ? regExp.test(v) : true;
      return ret;
    };
    this.validators.push({
      validator: matchValidator,
      message: msg,
      type: "regexp",
      regexp: regExp
    });
    return this;
  };
  SchemaString.prototype.checkRequired = function checkRequired(value, doc) {
    if (typeof value === "object" && SchemaType._isRef(this, value, doc, true)) {
      return value != null;
    }
    const _checkRequired = typeof this.constructor.checkRequired === "function" ? this.constructor.checkRequired() : SchemaString.checkRequired();
    return _checkRequired(value);
  };
  SchemaString.prototype.cast = function(value, doc, init, prev, options2) {
    if (typeof value !== "string" && SchemaType._isRef(this, value, doc, init)) {
      return this._castRef(value, doc, init, options2);
    }
    let castString2;
    if (typeof this._castFunction === "function") {
      castString2 = this._castFunction;
    } else if (typeof this.constructor.cast === "function") {
      castString2 = this.constructor.cast();
    } else {
      castString2 = SchemaString.cast();
    }
    try {
      return castString2(value);
    } catch {
      throw new CastError("string", value, this.path, null, this);
    }
  };
  function handleSingle(val, context) {
    return this.castForQuery(null, val, context);
  }
  function handleArray(val, context) {
    const _this = this;
    if (!Array.isArray(val)) {
      return [this.castForQuery(null, val, context)];
    }
    return val.map(function(m) {
      return _this.castForQuery(null, m, context);
    });
  }
  function handleSingleNoSetters(val) {
    if (val == null) {
      return this._castNullish(val);
    }
    return this.cast(val, this);
  }
  const $conditionalHandlers = {
    ...SchemaType.prototype.$conditionalHandlers,
    $all: handleArray,
    $gt: handleSingle,
    $gte: handleSingle,
    $lt: handleSingle,
    $lte: handleSingle,
    $options: handleSingleNoSetters,
    $regex: function handle$regex(val) {
      if (Object.prototype.toString.call(val) === "[object RegExp]") {
        return val;
      }
      return handleSingleNoSetters.call(this, val);
    },
    $not: handleSingle
  };
  Object.defineProperty(SchemaString.prototype, "$conditionalHandlers", {
    enumerable: false,
    value: $conditionalHandlers
  });
  SchemaString.prototype.castForQuery = function($conditional, val, context) {
    let handler;
    if ($conditional != null) {
      handler = this.$conditionalHandlers[$conditional];
      if (!handler) {
        throw new Error("Can't use " + $conditional + " with String.");
      }
      return handler.call(this, val, context);
    }
    if (Object.prototype.toString.call(val) === "[object RegExp]" || isBsonType(val, "BSONRegExp")) {
      return val;
    }
    try {
      return this.applySetters(val, context);
    } catch (err) {
      if (err instanceof CastError && err.path === this.path && this.$fullPath != null) {
        err.path = this.$fullPath;
      }
      throw err;
    }
  };
  SchemaString.prototype.toJSONSchema = function toJSONSchema(options2) {
    return this._addJSONSchemaEnum(this._createJSONSchemaTypeDefinition("string", "string", options2));
  };
  SchemaString.prototype.autoEncryptionType = function autoEncryptionType() {
    return "string";
  };
  string = SchemaString;
  function formatMaxLengthValidatorMessage(msg) {
    return function(props, doc) {
      if (typeof msg === "function") {
        return MongooseError.ValidatorError.prototype.formatMessage(msg, props, doc);
      }
      if (typeof msg === "string" && typeof props.value === "string" && props.value.length > 30) {
        props = Object.assign({}, props, {
          value: props.value.slice(0, 30) + "..."
        });
      }
      return MongooseError.ValidatorError.prototype.formatMessage(msg, props, doc);
    };
  }
  return string;
}
var uuid$1 = { exports: {} };
var hasRequiredUuid$1;
function requireUuid$1() {
  if (hasRequiredUuid$1) return uuid$1.exports;
  hasRequiredUuid$1 = 1;
  const UUID = requireBson().UUID;
  const UUID_FORMAT = /[0-9a-f]{8}-[0-9a-f]{4}-[0-9][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}/i;
  uuid$1.exports = function castUUID(value) {
    if (value == null) {
      return value;
    }
    if (value instanceof UUID) {
      return value;
    }
    if (typeof value === "string") {
      if (UUID_FORMAT.test(value)) {
        return new UUID(value);
      } else {
        throw new Error(`"${value}" is not a valid UUID string`);
      }
    }
    if (value.toString && value.toString !== Object.prototype.toString) {
      if (UUID_FORMAT.test(value.toString())) {
        return new UUID(value.toString());
      }
    }
    throw new Error(`"${value}" cannot be casted to a UUID`);
  };
  uuid$1.exports.UUID_FORMAT = UUID_FORMAT;
  return uuid$1.exports;
}
var uuid;
var hasRequiredUuid;
function requireUuid() {
  if (hasRequiredUuid) return uuid;
  hasRequiredUuid = 1;
  const SchemaType = requireSchemaType();
  const CastError = SchemaType.CastError;
  const castUUID = requireUuid$1();
  const utils2 = requireUtils();
  const handleBitwiseOperator = requireBitwise();
  const UUID_FORMAT = castUUID.UUID_FORMAT;
  function binaryToString(uuidBin) {
    let hex;
    if (typeof uuidBin !== "string" && uuidBin != null) {
      hex = uuidBin.toString("hex");
      const uuidStr = hex.substring(0, 8) + "-" + hex.substring(8, 8 + 4) + "-" + hex.substring(12, 12 + 4) + "-" + hex.substring(16, 16 + 4) + "-" + hex.substring(20, 20 + 12);
      return uuidStr;
    }
    return uuidBin;
  }
  function SchemaUUID(key, options2, _schemaOptions, parentSchema) {
    SchemaType.call(this, key, options2, "UUID", parentSchema);
  }
  SchemaUUID.schemaName = "UUID";
  SchemaUUID.defaultOptions = {};
  SchemaUUID.prototype = Object.create(SchemaType.prototype);
  SchemaUUID.prototype.constructor = SchemaUUID;
  SchemaUUID._cast = castUUID;
  SchemaUUID.get = SchemaType.get;
  SchemaUUID.set = SchemaType.set;
  SchemaUUID.setters = [];
  SchemaUUID.cast = function cast2(caster) {
    if (arguments.length === 0) {
      return this._cast;
    }
    if (caster === false) {
      caster = this._defaultCaster;
    }
    this._cast = caster;
    return this._cast;
  };
  SchemaUUID._checkRequired = (v) => v != null;
  SchemaUUID.checkRequired = SchemaType.checkRequired;
  SchemaUUID.prototype.checkRequired = function checkRequired(value) {
    if (Buffer.isBuffer(value)) {
      value = binaryToString(value);
    }
    return value != null && UUID_FORMAT.test(value);
  };
  SchemaUUID.prototype.cast = function(value, doc, init, prev, options2) {
    if (utils2.isNonBuiltinObject(value) && SchemaType._isRef(this, value, doc, init)) {
      return this._castRef(value, doc, init, options2);
    }
    let castFn;
    if (typeof this._castFunction === "function") {
      castFn = this._castFunction;
    } else if (typeof this.constructor.cast === "function") {
      castFn = this.constructor.cast();
    } else {
      castFn = SchemaUUID.cast();
    }
    try {
      return castFn(value);
    } catch (error2) {
      throw new CastError(SchemaUUID.schemaName, value, this.path, error2, this);
    }
  };
  function handleSingle(val) {
    return this.cast(val);
  }
  function handleArray(val) {
    return val.map((m) => {
      return this.cast(m);
    });
  }
  const $conditionalHandlers = {
    ...SchemaType.prototype.$conditionalHandlers,
    $bitsAllClear: handleBitwiseOperator,
    $bitsAnyClear: handleBitwiseOperator,
    $bitsAllSet: handleBitwiseOperator,
    $bitsAnySet: handleBitwiseOperator,
    $all: handleArray,
    $in: handleArray,
    $ne: handleSingle,
    $nin: handleArray
  };
  Object.defineProperty(SchemaUUID.prototype, "$conditionalHandlers", {
    enumerable: false,
    value: $conditionalHandlers
  });
  SchemaUUID.prototype.castForQuery = function($conditional, val, context) {
    let handler;
    if ($conditional != null) {
      handler = this.$conditionalHandlers[$conditional];
      if (!handler)
        throw new Error("Can't use " + $conditional + " with UUID.");
      return handler.call(this, val, context);
    }
    try {
      return this.applySetters(val, context);
    } catch (err) {
      if (err instanceof CastError && err.path === this.path && this.$fullPath != null) {
        err.path = this.$fullPath;
      }
      throw err;
    }
  };
  SchemaUUID.prototype.toJSONSchema = function toJSONSchema(options2) {
    return this._createJSONSchemaTypeDefinition("string", "binData", options2);
  };
  SchemaUUID.prototype.autoEncryptionType = function autoEncryptionType() {
    return "binData";
  };
  uuid = SchemaUUID;
  return uuid;
}
var schemaUnionOptions;
var hasRequiredSchemaUnionOptions;
function requireSchemaUnionOptions() {
  if (hasRequiredSchemaUnionOptions) return schemaUnionOptions;
  hasRequiredSchemaUnionOptions = 1;
  const SchemaTypeOptions = requireSchemaTypeOptions();
  class SchemaUnionOptions extends SchemaTypeOptions {
  }
  const opts = requirePropertyOptions();
  Object.defineProperty(SchemaUnionOptions.prototype, "of", opts);
  schemaUnionOptions = SchemaUnionOptions;
  return schemaUnionOptions;
}
var union;
var hasRequiredUnion;
function requireUnion() {
  if (hasRequiredUnion) return union;
  hasRequiredUnion = 1;
  const SchemaUnionOptions = requireSchemaUnionOptions();
  const SchemaType = requireSchemaType();
  const firstValueSymbol = /* @__PURE__ */ Symbol("firstValue");
  class Union extends SchemaType {
    /**
     * Create a Union schema type.
     *
     * @param {string} key the path in the schema for this schema type
     * @param {object} options SchemaType-specific options (must have 'of' as array)
     * @param {object} schemaOptions additional options from the schema this schematype belongs to
     * @param {Schema} parentSchema the schema this schematype belongs to
     */
    constructor(key, options2, schemaOptions, parentSchema) {
      super(key, options2, "Union", parentSchema);
      if (!Array.isArray(options2?.of) || options2.of.length === 0) {
        throw new Error("Union schema type requires an array of types");
      }
      this.schemaTypes = options2.of.map((obj) => parentSchema.interpretAsType(key, obj, schemaOptions));
      this.$isSchemaUnion = true;
    }
    cast(val, doc, init, prev, options2) {
      let firstValue = firstValueSymbol;
      let lastError;
      for (let i = 0; i < this.schemaTypes.length; ++i) {
        try {
          const casted = this.schemaTypes[i].cast(val, doc, init, prev, options2);
          if (casted === val) {
            return casted;
          }
          if (firstValue === firstValueSymbol) {
            firstValue = casted;
          }
        } catch (error2) {
          lastError = error2;
        }
      }
      if (firstValue !== firstValueSymbol) {
        return firstValue;
      }
      throw lastError;
    }
    // Setters also need to be aware of casting - we need to apply the setters of the entry in the union we choose.
    applySetters(val, doc, init, prev, options2) {
      let firstValue = firstValueSymbol;
      let lastError;
      for (let i = 0; i < this.schemaTypes.length; ++i) {
        try {
          let castedVal = this.schemaTypes[i]._applySetters(val, doc, init, prev, options2);
          if (castedVal == null) {
            castedVal = this.schemaTypes[i]._castNullish(castedVal);
          } else {
            castedVal = this.schemaTypes[i].cast(castedVal, doc, init, prev, options2);
          }
          if (castedVal === val) {
            return castedVal;
          }
          if (firstValue === firstValueSymbol) {
            firstValue = castedVal;
          }
        } catch (error2) {
          lastError = error2;
        }
      }
      if (firstValue !== firstValueSymbol) {
        return firstValue;
      }
      throw lastError;
    }
    async doValidate(value, scope, options2) {
      if (options2 && options2.skipSchemaValidators) {
        if (value != null && typeof value.validate === "function") {
          return value.validate();
        }
        return;
      }
      await super.doValidate(value, scope, options2);
      if (value != null && typeof value.validate === "function") {
        await value.validate();
      }
    }
    doValidateSync(value, scope, options2) {
      if (!options2 || !options2.skipSchemaValidators) {
        const schemaTypeError = super.doValidateSync(value, scope, options2);
        if (schemaTypeError) {
          return schemaTypeError;
        }
      }
      if (value != null && typeof value.$__validateSync === "function") {
        return value.$__validateSync();
      }
    }
    clone() {
      const schematype = super.clone();
      schematype.schemaTypes = this.schemaTypes.map((schemaType2) => schemaType2.clone());
      return schematype;
    }
    /**
     * Returns this schema type's representation in a JSON schema.
     *
     * @param {object} [options]
     * @param {boolean} [options.useBsonType=false] If true, return a representation with `bsonType` for use with MongoDB's `$jsonSchema`.
     * @returns {object} JSON schema properties
     */
    toJSONSchema(options2) {
      const isRequired = this.options.required && typeof this.options.required !== "function";
      const childOptions = { ...options2, _overrideRequired: true };
      const jsonSchemas = this.schemaTypes.map((schemaType2) => schemaType2.toJSONSchema(childOptions));
      if (isRequired) {
        return { anyOf: jsonSchemas };
      }
      return {
        anyOf: [
          options2?.useBsonType ? { bsonType: "null" } : { type: "null" },
          ...jsonSchemas
        ]
      };
    }
  }
  Union.schemaName = "Union";
  Union.defaultOptions = {};
  Union.prototype.OptionsConstructor = SchemaUnionOptions;
  union = Union;
  return union;
}
var hasRequiredSchema$1;
function requireSchema$1() {
  if (hasRequiredSchema$1) return schema;
  hasRequiredSchema$1 = 1;
  (function(exports) {
    exports.Array = requireArray();
    exports.BigInt = requireBigint();
    exports.Boolean = requireBoolean();
    exports.Buffer = requireBuffer();
    exports.Date = requireDate();
    exports.Decimal128 = exports.Decimal = requireDecimal128();
    exports.DocumentArray = requireDocumentArray();
    exports.DocumentArrayElement = requireDocumentArrayElement();
    exports.Double = requireDouble();
    exports.Int32 = requireInt32();
    exports.Map = requireMap();
    exports.Mixed = requireMixed();
    exports.Number = requireNumber();
    exports.ObjectId = requireObjectId();
    exports.String = requireString();
    exports.Subdocument = requireSubdocument();
    exports.UUID = requireUuid();
    exports.Union = requireUnion();
    exports.Oid = exports.ObjectId;
    exports.Object = exports.Mixed;
    exports.Bool = exports.Boolean;
    exports.ObjectID = exports.ObjectId;
  })(schema);
  return schema;
}
var hasRequiredSchema;
function requireSchema() {
  if (hasRequiredSchema) return schema$1.exports;
  hasRequiredSchema = 1;
  (function(module, exports) {
    const EventEmitter = require$$0.EventEmitter;
    const Kareem = requireKareem();
    const MongooseError = requireMongooseError();
    const PathTrie = requirePathTrie();
    const SchemaType = requireSchemaType();
    const SchemaTypeOptions = requireSchemaTypeOptions();
    const VirtualOptions = requireVirtualOptions();
    const VirtualType = requireVirtualType();
    const addAutoId2 = requireAddAutoId();
    const clone = requireClone();
    const get2 = requireGet();
    const getConstructorName2 = requireGetConstructorName();
    const getIndexes2 = requireGetIndexes();
    const handleReadPreferenceAliases2 = requireHandleReadPreferenceAliases();
    const idGetter = requireIdGetter();
    const isIndexSpecEqual2 = requireIsIndexSpecEqual();
    const merge2 = requireMerge();
    const mpath = requireMpath();
    const setPopulatedVirtualValue2 = requireSetPopulatedVirtualValue();
    const setupTimestamps2 = requireSetupTimestamps();
    const symbols2 = requireSymbols();
    const utils2 = requireUtils();
    const validateRef = requireValidateRef();
    const hasNumericSubpathRegex = /\.\d+(\.|$)/;
    let MongooseTypes;
    const queryHooks = requireConstants().queryMiddlewareFunctions;
    const documentHooks = requireConstants().documentMiddlewareFunctions;
    const hookNames = queryHooks.concat(documentHooks).reduce((s, hook) => s.add(hook), /* @__PURE__ */ new Set());
    const isPOJO2 = utils2.isPOJO;
    let id = 0;
    const numberRE = /^\d+$/;
    function Schema(obj, options2) {
      if (!(this instanceof Schema)) {
        return new Schema(obj, options2);
      }
      this.obj = obj;
      this.paths = {};
      this.aliases = {};
      this.subpaths = {};
      this.virtuals = {};
      this.singleNestedPaths = {};
      this.nested = {};
      this.inherits = {};
      this.callQueue = [];
      this._indexes = [];
      this._searchIndexes = [];
      this.methods = options2?.methods || {};
      this.methodOptions = {};
      this.statics = options2?.statics || {};
      this.tree = {};
      this.query = options2?.query || {};
      this.childSchemas = [];
      this.plugins = [];
      this.$id = ++id;
      this.mapPaths = [];
      this.encryptedFields = {};
      this.s = {
        hooks: new Kareem()
      };
      this.options = this.defaultOptions(options2);
      if (Array.isArray(obj)) {
        for (const definition of obj) {
          this.add(definition);
        }
      } else if (obj) {
        this.add(obj);
      }
      if (options2?.virtuals) {
        const virtuals = options2.virtuals;
        const pathNames = Object.keys(virtuals);
        for (const pathName of pathNames) {
          const pathOptions = virtuals[pathName].options ? virtuals[pathName].options : void 0;
          const virtual = this.virtual(pathName, pathOptions);
          if (virtuals[pathName].get) {
            virtual.get(virtuals[pathName].get);
          }
          if (virtuals[pathName].set) {
            virtual.set(virtuals[pathName].set);
          }
        }
      }
      const _idSubDoc = obj?._id && utils2.isObject(obj._id);
      const auto_id = !this.paths["_id"] && this.options._id && !_idSubDoc;
      if (auto_id) {
        addAutoId2(this);
      }
      this.setupTimestamp(this.options.timestamps);
    }
    function aliasFields(schema2, paths) {
      for (const path of Object.keys(paths)) {
        let alias = null;
        if (paths[path] != null) {
          alias = paths[path];
        } else {
          const options2 = get2(schema2.paths[path], "options");
          if (options2 == null) {
            continue;
          }
          alias = options2.alias;
        }
        if (!alias) {
          continue;
        }
        const prop = schema2.paths[path].path;
        if (Array.isArray(alias)) {
          for (const a of alias) {
            if (typeof a !== "string") {
              throw new MongooseError("Invalid value for alias option on " + prop + ", got " + a);
            }
            schema2.aliases[a] = prop;
            schema2.virtual(a).get(/* @__PURE__ */ (function(p) {
              return function() {
                if (typeof this.get === "function") {
                  return this.get(p);
                }
                return this[p];
              };
            })(prop)).set(/* @__PURE__ */ (function(p) {
              return function(v) {
                return this.$set(p, v);
              };
            })(prop));
          }
          continue;
        }
        if (typeof alias !== "string") {
          throw new MongooseError("Invalid value for alias option on " + prop + ", got " + alias);
        }
        schema2.aliases[alias] = prop;
        schema2.virtual(alias).get(/* @__PURE__ */ (function(p) {
          return function() {
            if (typeof this.get === "function") {
              return this.get(p);
            }
            return this[p];
          };
        })(prop)).set(/* @__PURE__ */ (function(p) {
          return function(v) {
            return this.$set(p, v);
          };
        })(prop));
      }
    }
    Schema.prototype = Object.create(EventEmitter.prototype);
    Schema.prototype.constructor = Schema;
    Schema.prototype.instanceOfSchema = true;
    Object.defineProperty(Schema.prototype, "$schemaType", {
      configurable: false,
      enumerable: false,
      writable: true
    });
    Object.defineProperty(Schema.prototype, "childSchemas", {
      configurable: false,
      enumerable: true,
      writable: true
    });
    Object.defineProperty(Schema.prototype, "virtuals", {
      configurable: false,
      enumerable: true,
      writable: true
    });
    Schema.prototype.obj;
    Schema.prototype.paths;
    Schema.prototype.tree;
    Schema.create = function create(definition, options2) {
      return new Schema(definition, options2);
    };
    Schema.prototype.clone = function() {
      const s = this._clone();
      s.on("init", (v) => this.emit("init", v));
      return s;
    };
    Schema.prototype._clone = function _clone(Constructor) {
      Constructor = Constructor || (this.base == null ? Schema : this.base.Schema);
      const s = new Constructor({}, this._userProvidedOptions);
      s.base = this.base;
      s.obj = this.obj;
      s.options = clone(this.options);
      s.callQueue = this.callQueue.map(function(f) {
        return f;
      });
      s.methods = clone(this.methods);
      s.methodOptions = clone(this.methodOptions);
      s.statics = clone(this.statics);
      s.query = clone(this.query);
      s.plugins = Array.prototype.slice.call(this.plugins);
      s._indexes = clone(this._indexes);
      s._searchIndexes = clone(this._searchIndexes);
      s.s.hooks = this.s.hooks.clone();
      s.tree = clone(this.tree);
      s.paths = Object.fromEntries(
        Object.entries(this.paths).map(([key, value]) => [key, value.clone()])
      );
      s.nested = clone(this.nested);
      s.subpaths = clone(this.subpaths);
      for (const schemaType2 of Object.values(s.paths)) {
        if (schemaType2.$isSingleNested) {
          const path = schemaType2.path;
          for (const key of Object.keys(schemaType2.schema.paths)) {
            s.singleNestedPaths[path + "." + key] = schemaType2.schema.paths[key];
          }
          for (const key of Object.keys(schemaType2.schema.singleNestedPaths)) {
            s.singleNestedPaths[path + "." + key] = schemaType2.schema.singleNestedPaths[key];
          }
          for (const key of Object.keys(schemaType2.schema.subpaths)) {
            s.singleNestedPaths[path + "." + key] = schemaType2.schema.subpaths[key];
          }
          for (const key of Object.keys(schemaType2.schema.nested)) {
            s.singleNestedPaths[path + "." + key] = "nested";
          }
        }
      }
      s._gatherChildSchemas();
      s.virtuals = clone(this.virtuals);
      s.$globalPluginsApplied = this.$globalPluginsApplied;
      s.$isRootDiscriminator = this.$isRootDiscriminator;
      s.$implicitlyCreated = this.$implicitlyCreated;
      s.$id = ++id;
      s.$originalSchemaId = this.$id;
      s.mapPaths = [].concat(this.mapPaths);
      if (this.discriminatorMapping != null) {
        s.discriminatorMapping = Object.assign({}, this.discriminatorMapping);
      }
      if (this.discriminators != null) {
        s.discriminators = Object.assign({}, this.discriminators);
      }
      if (this._applyDiscriminators != null) {
        s._applyDiscriminators = new Map(this._applyDiscriminators);
      }
      s.aliases = Object.assign({}, this.aliases);
      s.encryptedFields = clone(this.encryptedFields);
      return s;
    };
    Schema.prototype.pick = function(paths, options2) {
      const newSchema = new Schema({}, options2 || this.options);
      if (!Array.isArray(paths)) {
        throw new MongooseError('Schema#pick() only accepts an array argument, got "' + typeof paths + '"');
      }
      for (const path of paths) {
        if (this._hasEncryptedField(path)) {
          const encrypt = this.encryptedFields[path];
          const schemaType2 = this.path(path);
          newSchema.add({
            [path]: {
              encrypt,
              [this.options.typeKey]: schemaType2
            }
          });
        } else if (this.nested[path]) {
          newSchema.add({ [path]: get2(this.tree, path) });
        } else {
          const schematype = this.path(path);
          if (schematype == null) {
            throw new MongooseError("Path `" + path + "` is not in the schema");
          }
          newSchema.add({ [path]: schematype });
        }
      }
      if (!this._hasEncryptedFields()) {
        newSchema.options.encryptionType = null;
      }
      return newSchema;
    };
    Schema.prototype.omit = function(paths, options2) {
      const newSchema = new Schema(this, options2 || this.options);
      if (!Array.isArray(paths)) {
        throw new MongooseError(
          'Schema#omit() only accepts an array argument, got "' + typeof paths + '"'
        );
      }
      newSchema.remove(paths);
      for (const nested in newSchema.singleNestedPaths) {
        if (paths.includes(nested)) {
          delete newSchema.singleNestedPaths[nested];
        }
      }
      return newSchema;
    };
    Schema.prototype.defaultOptions = function(options2) {
      this._userProvidedOptions = options2 == null ? {} : clone(options2);
      const baseOptions = this.base?.options || {};
      const defaultStrict = baseOptions.strict ?? true;
      const defaultStrictQuery = baseOptions.strictQuery ?? false;
      const defaultStrictRead = baseOptions.strictRead ?? false;
      const defaultId = baseOptions.id ?? true;
      options2 = {
        strict: defaultStrict,
        strictQuery: defaultStrictQuery,
        strictRead: defaultStrictRead,
        bufferCommands: true,
        capped: false,
        // { size, max, autoIndexId }
        versionKey: "__v",
        optimisticConcurrency: false,
        minimize: true,
        autoIndex: null,
        discriminatorKey: "__t",
        shardKey: null,
        read: null,
        validateBeforeSave: true,
        validateModifiedOnly: false,
        // the following are only applied at construction time
        _id: true,
        id: defaultId,
        typeKey: "type",
        ...options2
      };
      options2.strict ??= defaultStrict;
      options2.strictQuery ??= defaultStrictQuery;
      options2.id ??= defaultId;
      if (options2.versionKey && typeof options2.versionKey !== "string") {
        throw new MongooseError("`versionKey` must be falsy or string, got `" + typeof options2.versionKey + "`");
      }
      if (typeof options2.read === "string") {
        options2.read = handleReadPreferenceAliases2(options2.read);
      } else if (Array.isArray(options2.read) && typeof options2.read[0] === "string") {
        options2.read = {
          mode: handleReadPreferenceAliases2(options2.read[0]),
          tags: options2.read[1]
        };
      }
      if (options2.optimisticConcurrency && !options2.versionKey) {
        throw new MongooseError("Must set `versionKey` if using `optimisticConcurrency`");
      }
      return options2;
    };
    Schema.prototype.discriminator = function(name, schema2, options2) {
      this._applyDiscriminators = this._applyDiscriminators || /* @__PURE__ */ new Map();
      this._applyDiscriminators.set(name, { schema: schema2, options: options2 });
      return this;
    };
    Schema.prototype._getDocumentMiddleware = function _getDocumentMiddleware() {
      return this.s.hooks.filter((hook) => {
        if (hook.name === "updateOne" || hook.name === "deleteOne") {
          return !!hook["document"];
        }
        if (hook.name === "remove" || hook.name === "init") {
          return hook["document"] == null || !!hook["document"];
        }
        if (hook.query != null || hook.document != null) {
          return hook.document !== false;
        }
        return true;
      }).filter((hook) => {
        if (this.methods[hook.name]) {
          return !hook.fn[symbols2.builtInMiddleware];
        }
        return true;
      });
    };
    Schema.prototype._defaultToObjectOptions = function(json) {
      const path = json ? "toJSON" : "toObject";
      if (this._defaultToObjectOptionsMap && path in this._defaultToObjectOptionsMap) {
        return this._defaultToObjectOptionsMap[path];
      }
      const baseOptions = this.base?.options?.[path] || {};
      const schemaOptions = this.options[path] || {};
      const defaultOptions = Object.assign({}, baseOptions, schemaOptions);
      this._defaultToObjectOptionsMap = this._defaultToObjectOptionsMap || {};
      this._defaultToObjectOptionsMap[path] = utils2.hasOwnKeys(defaultOptions) ? defaultOptions : null;
      return defaultOptions;
    };
    Schema.prototype.encryptionType = function encryptionType(encryptionType) {
      if (arguments.length === 0) {
        return this.options.encryptionType;
      }
      if (!(typeof encryptionType === "string" || encryptionType === null)) {
        throw new MongooseError(`invalid \`encryptionType\`: ${encryptionType}`);
      }
      this.options.encryptionType = encryptionType;
    };
    Schema.prototype.add = function add(obj, prefix) {
      if (obj instanceof Schema || obj?.instanceOfSchema) {
        merge2(this, obj);
        return this;
      }
      if (obj._id === false && prefix == null) {
        this.options._id = false;
      }
      prefix = prefix || "";
      if (prefix === "__proto__." || prefix === "constructor." || prefix === "prototype.") {
        return this;
      }
      const keys = Object.keys(obj);
      const typeKey = this.options.typeKey;
      for (const key of keys) {
        if (utils2.specialProperties.has(key)) {
          continue;
        }
        const fullPath = prefix + key;
        const val = obj[key];
        if (val == null) {
          throw new TypeError("Invalid value for schema path `" + fullPath + '`, got value "' + val + '"');
        }
        if (key === "_id" && val === false) {
          continue;
        }
        let isMongooseTypeString = false;
        if (typeof val === "string") {
          const MongooseTypes2 = this.base?.Schema.Types ?? Schema.Types;
          const upperVal = val.charAt(0).toUpperCase() + val.substring(1);
          isMongooseTypeString = MongooseTypes2[upperVal] != null;
        }
        if (key !== "_id" && (typeof val !== "object" && typeof val !== "function" && !isMongooseTypeString || val == null)) {
          throw new TypeError(`Invalid schema configuration: \`${val}\` is not a valid type at path \`${key}\`. See https://bit.ly/mongoose-schematypes for a list of valid schema types.`);
        }
        if (val instanceof VirtualType || (val.constructor?.name ?? null) === "VirtualType") {
          this.virtual(val);
          continue;
        }
        if (Array.isArray(val) && val.length === 1 && val[0] == null) {
          throw new TypeError("Invalid value for schema Array path `" + fullPath + '`, got value "' + val[0] + '"');
        }
        if (!(isPOJO2(val) || val instanceof SchemaTypeOptions)) {
          if (prefix) {
            this.nested[prefix.substring(0, prefix.length - 1)] = true;
          }
          this.path(prefix + key, val);
          if (!val[0]?.instanceOfSchema && utils2.isPOJO(val[0]?.discriminators)) {
            const schemaType2 = this.path(prefix + key);
            for (const key2 in val[0].discriminators) {
              schemaType2.discriminator(key2, val[0].discriminators[key2]);
            }
          }
        } else if (utils2.hasOwnKeys(val) === false) {
          if (prefix) {
            this.nested[prefix.substring(0, prefix.length - 1)] = true;
          }
          this.path(fullPath, val);
        } else if (!val[typeKey] || typeKey === "type" && isPOJO2(val.type) && val.type.type) {
          this.nested[fullPath] = true;
          this.add(val, fullPath + ".");
        } else {
          const _typeDef = val[typeKey];
          if (isPOJO2(_typeDef) && utils2.hasOwnKeys(_typeDef)) {
            if (prefix) {
              this.nested[prefix.substring(0, prefix.length - 1)] = true;
            }
            const childSchemaOptions = {};
            if (this._userProvidedOptions.typeKey) {
              childSchemaOptions.typeKey = this._userProvidedOptions.typeKey;
            }
            if (this._userProvidedOptions.strict != null) {
              childSchemaOptions.strict = this._userProvidedOptions.strict;
            }
            if (this._userProvidedOptions.toObject != null) {
              childSchemaOptions.toObject = utils2.omit(this._userProvidedOptions.toObject, ["transform"]);
            }
            if (this._userProvidedOptions.toJSON != null) {
              childSchemaOptions.toJSON = utils2.omit(this._userProvidedOptions.toJSON, ["transform"]);
            }
            const _schema = new Schema(_typeDef, childSchemaOptions);
            _schema.$implicitlyCreated = true;
            const schemaWrappedPath = Object.assign({}, val, { [typeKey]: _schema });
            this.path(prefix + key, schemaWrappedPath);
          } else {
            if (prefix) {
              this.nested[prefix.substring(0, prefix.length - 1)] = true;
            }
            this.path(prefix + key, val);
            if (!val?.instanceOfSchema && utils2.isPOJO(val?.discriminators)) {
              const schemaType2 = this.path(prefix + key);
              for (const key2 in val.discriminators) {
                schemaType2.discriminator(key2, val.discriminators[key2]);
              }
            }
          }
        }
        if (val.instanceOfSchema && val.encryptionType() != null) {
          if (this.encryptionType() != val.encryptionType()) {
            throw new MongooseError("encryptionType of a nested schema must match the encryption type of the parent schema.");
          }
          for (const [encryptedField, encryptedFieldConfig] of Object.entries(val.encryptedFields)) {
            const path = fullPath + "." + encryptedField;
            this._addEncryptedField(path, encryptedFieldConfig);
          }
        } else if (typeof val === "object" && "encrypt" in val) {
          const { encrypt } = val;
          if (this.encryptionType() == null) {
            throw new MongooseError("encryptionType must be provided");
          }
          this._addEncryptedField(fullPath, encrypt);
        } else {
          this._removeEncryptedField(fullPath);
        }
      }
      const aliasObj = Object.fromEntries(
        Object.entries(obj).map(([key]) => [prefix + key, null])
      );
      aliasFields(this, aliasObj);
      return this;
    };
    Schema.prototype._addEncryptedField = function _addEncryptedField(path, fieldConfig) {
      const type2 = this.path(path).autoEncryptionType();
      if (type2 == null) {
        throw new MongooseError(`Invalid BSON type for FLE field: '${path}'`);
      }
      this.encryptedFields[path] = clone(fieldConfig);
    };
    Schema.prototype._removeEncryptedField = function _removeEncryptedField(path) {
      delete this.encryptedFields[path];
    };
    Schema.prototype._hasEncryptedFields = function _hasEncryptedFields() {
      return utils2.hasOwnKeys(this.encryptedFields);
    };
    Schema.prototype._hasEncryptedField = function _hasEncryptedField(path) {
      return path in this.encryptedFields;
    };
    Schema.prototype._buildEncryptedFields = function() {
      const fields = Object.entries(this.encryptedFields).map(
        ([path, config]) => {
          const bsonType = this.path(path).autoEncryptionType();
          return { path, bsonType, ...config };
        }
      );
      return { fields };
    };
    Schema.prototype._buildSchemaMap = function() {
      function buildNestedPath(path, object, value) {
        let i = 0, component = path[i];
        for (; i < path.length - 1; ++i, component = path[i]) {
          object[component] = object[component] == null ? {
            bsonType: "object",
            properties: {}
          } : object[component];
          object = object[component].properties;
        }
        object[component] = value;
      }
      const schemaMapPropertyReducer = (accum, [path, propertyConfig]) => {
        const bsonType = this.path(path).autoEncryptionType();
        const pathComponents = path.split(".");
        const configuration = { encrypt: { ...propertyConfig, bsonType } };
        buildNestedPath(pathComponents, accum, configuration);
        return accum;
      };
      const properties = Object.entries(this.encryptedFields).reduce(
        schemaMapPropertyReducer,
        {}
      );
      return {
        bsonType: "object",
        properties
      };
    };
    Schema.prototype.alias = function alias(path, alias) {
      aliasFields(this, { [path]: alias });
      return this;
    };
    Schema.prototype.removeIndex = function removeIndex(index) {
      if (arguments.length > 1) {
        throw new MongooseError("removeIndex() takes only 1 argument");
      }
      if (typeof index !== "object" && typeof index !== "string") {
        throw new MongooseError("removeIndex() may only take either an object or a string as an argument");
      }
      if (typeof index === "object") {
        for (let i = this._indexes.length - 1; i >= 0; --i) {
          if (isIndexSpecEqual2(this._indexes[i][0], index)) {
            this._indexes.splice(i, 1);
          }
        }
      } else {
        for (let i = this._indexes.length - 1; i >= 0; --i) {
          if (this._indexes[i][1] != null && this._indexes[i][1].name === index) {
            this._indexes.splice(i, 1);
          }
        }
      }
      return this;
    };
    Schema.prototype.clearIndexes = function clearIndexes() {
      this._indexes.length = 0;
      return this;
    };
    Schema.prototype.searchIndex = function searchIndex(description) {
      this._searchIndexes.push(description);
      return this;
    };
    Schema.reserved = /* @__PURE__ */ Object.create(null);
    Schema.prototype.reserved = Schema.reserved;
    const reserved = Schema.reserved;
    reserved["prototype"] = // EventEmitter
    reserved.emit = reserved.listeners = reserved.removeListener = // document properties and functions
    reserved.collection = reserved.errors = reserved.get = reserved.init = reserved.isModified = reserved.isNew = reserved.populated = reserved.remove = reserved.save = reserved.toObject = reserved.validate = 1;
    reserved.collection = 1;
    Schema.prototype.path = function(path, obj) {
      if (obj === void 0) {
        if (Object.hasOwn(this.paths, path)) {
          return this.paths[path];
        }
        const cleanPath = _pathToPositionalSyntax(path);
        let schematype = _getPath(this, path, cleanPath);
        if (schematype != null) {
          return schematype;
        }
        const mapPath = getMapPath(this, path);
        if (mapPath != null) {
          return mapPath;
        }
        schematype = this.hasMixedParent(cleanPath);
        if (schematype != null) {
          return schematype;
        }
        return hasNumericSubpathRegex.test(path) ? getPositionalPath(this, path, cleanPath) : void 0;
      }
      const subpaths = path.indexOf(".") === -1 ? [path] : path.split(".");
      const last = subpaths.pop();
      if (utils2.specialProperties.has(last)) {
        throw new MongooseError("Cannot set special property `" + last + "` on a schema");
      }
      const firstPieceOfPath = subpaths.length === 0 ? last : subpaths[0];
      if (Object.hasOwn(reserved, firstPieceOfPath) && !this.options.suppressReservedKeysWarning) {
        const errorMessage = `\`${firstPieceOfPath}\` is a reserved schema pathname and may break some functionality. You are allowed to use it, but use at your own risk. To disable this warning pass \`suppressReservedKeysWarning\` as a schema option.`;
        utils2.warn(errorMessage);
      }
      if (typeof obj === "object" && utils2.hasUserDefinedProperty(obj, "ref")) {
        validateRef(obj.ref, path);
      }
      let branch = this.tree;
      let fullPath = "";
      for (const sub of subpaths) {
        if (utils2.specialProperties.has(sub)) {
          throw new MongooseError("Cannot set special property `" + sub + "` on a schema");
        }
        fullPath = fullPath += (fullPath.length > 0 ? "." : "") + sub;
        if (!branch[sub]) {
          this.nested[fullPath] = true;
          branch[sub] = {};
        }
        if (typeof branch[sub] !== "object") {
          const msg = "Cannot set nested path `" + path + "`. Parent path `" + fullPath + "` already set to type " + branch[sub].name + ".";
          throw new MongooseError(msg);
        }
        branch = branch[sub];
      }
      branch[last] = clone(obj);
      this.paths[path] = this.interpretAsType(path, obj, this.options);
      const schemaType2 = this.paths[path];
      this.childSchemas = this.childSchemas.filter((childSchema) => childSchema.path !== path);
      if (schemaType2.$isSchemaMap) {
        const mapPath = path + ".$*";
        this.paths[mapPath] = schemaType2.$__schemaType;
        this.mapPaths.push(this.paths[mapPath]);
        if (schemaType2.$__schemaType.$isSingleNested) {
          this.childSchemas.push({
            schema: schemaType2.$__schemaType.schema,
            model: schemaType2.$__schemaType.Constructor,
            path
          });
        }
      }
      if (schemaType2.$isSingleNested) {
        for (const key of Object.keys(schemaType2.schema.paths)) {
          this.singleNestedPaths[path + "." + key] = schemaType2.schema.paths[key];
        }
        for (const key of Object.keys(schemaType2.schema.singleNestedPaths)) {
          this.singleNestedPaths[path + "." + key] = schemaType2.schema.singleNestedPaths[key];
        }
        for (const key of Object.keys(schemaType2.schema.subpaths)) {
          this.singleNestedPaths[path + "." + key] = schemaType2.schema.subpaths[key];
        }
        for (const key of Object.keys(schemaType2.schema.nested)) {
          this.singleNestedPaths[path + "." + key] = "nested";
        }
        Object.defineProperty(schemaType2.schema, "base", {
          configurable: true,
          enumerable: false,
          writable: false,
          value: this.base
        });
        schemaType2.Constructor.base = this.base;
        this.childSchemas.push({
          schema: schemaType2.schema,
          model: schemaType2.Constructor,
          path
        });
      } else if (schemaType2.$isMongooseDocumentArray) {
        Object.defineProperty(schemaType2.schema, "base", {
          configurable: true,
          enumerable: false,
          writable: false,
          value: this.base
        });
        schemaType2.Constructor.base = this.base;
        this.childSchemas.push({
          schema: schemaType2.schema,
          model: schemaType2.Constructor,
          path
        });
      }
      if (schemaType2.$isMongooseArray && !schemaType2.$isMongooseDocumentArray) {
        let arrayPath = path;
        let _schemaType = schemaType2;
        const toAdd = [];
        while (_schemaType.$isMongooseArray) {
          arrayPath = arrayPath + ".$";
          _schemaType.embeddedSchemaType._arrayPath = arrayPath;
          _schemaType.embeddedSchemaType._arrayParentPath = path;
          _schemaType = _schemaType.embeddedSchemaType;
          this.subpaths[arrayPath] = _schemaType;
        }
        for (const _schemaType2 of toAdd) {
          this.subpaths[_schemaType2.path] = _schemaType2;
        }
      }
      if (schemaType2.$isMongooseDocumentArray) {
        for (const key of Object.keys(schemaType2.schema.paths)) {
          const _schemaType = schemaType2.schema.paths[key];
          this.subpaths[path + "." + key] = _schemaType;
          if (typeof _schemaType === "object" && _schemaType != null && _schemaType.$parentSchemaDocArray == null) {
            _schemaType.$parentSchemaDocArray = schemaType2;
          }
        }
        for (const key of Object.keys(schemaType2.schema.subpaths)) {
          const _schemaType = schemaType2.schema.subpaths[key];
          this.subpaths[path + "." + key] = _schemaType;
          if (typeof _schemaType === "object" && _schemaType != null && _schemaType.$parentSchemaDocArray == null) {
            _schemaType.$parentSchemaDocArray = schemaType2;
          }
        }
        for (const key of Object.keys(schemaType2.schema.singleNestedPaths)) {
          const _schemaType = schemaType2.schema.singleNestedPaths[key];
          this.subpaths[path + "." + key] = _schemaType;
          if (typeof _schemaType === "object" && _schemaType != null && _schemaType.$parentSchemaDocArray == null) {
            _schemaType.$parentSchemaDocArray = schemaType2;
          }
        }
      }
      return this;
    };
    Schema.prototype._gatherChildSchemas = function _gatherChildSchemas() {
      const childSchemas = [];
      for (const path of Object.keys(this.paths)) {
        if (typeof path !== "string") {
          continue;
        }
        const schematype = this.paths[path];
        if (schematype.$isMongooseDocumentArray || schematype.$isSingleNested) {
          childSchemas.push({
            schema: schematype.schema,
            model: schematype.Constructor,
            path
          });
        } else if (schematype.$isSchemaMap && schematype.$__schemaType.$isSingleNested) {
          childSchemas.push({
            schema: schematype.$__schemaType.schema,
            model: schematype.$__schemaType.Constructor,
            path
          });
        }
      }
      this.childSchemas = childSchemas;
      return childSchemas;
    };
    function _getPath(schema2, path, cleanPath) {
      if (Object.hasOwn(schema2.paths, path)) {
        return schema2.paths[path];
      }
      if (Object.hasOwn(schema2.subpaths, cleanPath)) {
        const subpath = schema2.subpaths[cleanPath];
        if (subpath === "nested") {
          return void 0;
        }
        return subpath;
      }
      if (Object.hasOwn(schema2.singleNestedPaths, cleanPath) && typeof schema2.singleNestedPaths[cleanPath] === "object") {
        const singleNestedPath = schema2.singleNestedPaths[cleanPath];
        if (singleNestedPath === "nested") {
          return void 0;
        }
        return singleNestedPath;
      }
      return null;
    }
    function _pathToPositionalSyntax(path) {
      if (!/\.\d+/.test(path)) {
        return path;
      }
      return path.replace(/\.\d+\./g, ".$.").replace(/\.\d+$/, ".$");
    }
    function getMapPath(schema2, path) {
      if (schema2.mapPaths.length === 0) {
        return null;
      }
      for (const val of schema2.mapPaths) {
        const cleanPath = val.path.replace(/\.\$\*/g, "");
        if (path === cleanPath || path.startsWith(cleanPath + ".") && path.slice(cleanPath.length + 1).indexOf(".") === -1) {
          return val;
        } else if (val.schema && path.startsWith(cleanPath + ".")) {
          let remnant = path.slice(cleanPath.length + 1);
          remnant = remnant.slice(remnant.indexOf(".") + 1);
          return val.schema.path(remnant);
        } else if (val.$isSchemaMap && path.startsWith(cleanPath + ".")) {
          let remnant = path.slice(cleanPath.length + 1);
          remnant = remnant.slice(remnant.indexOf(".") + 1);
          const presplitPath = val.$__schemaType._presplitPath;
          if (remnant.indexOf(".") === -1 && presplitPath[presplitPath.length - 1] === "$*") {
            return val.$__schemaType;
          } else if (remnant.indexOf(".") !== -1 && val.$__schemaType.schema && presplitPath[presplitPath.length - 1] === "$*") {
            return val.$__schemaType.schema.path(remnant.slice(remnant.indexOf(".") + 1));
          }
        }
      }
      return null;
    }
    Object.defineProperty(Schema.prototype, "base", {
      configurable: true,
      enumerable: false,
      writable: true,
      value: null
    });
    Schema.prototype.interpretAsType = function(path, obj, options2) {
      if (obj instanceof SchemaType) {
        if (obj.path === path) {
          return obj;
        }
        const clone2 = obj.clone();
        clone2.path = path;
        return clone2;
      }
      const MongooseTypes2 = this.base?.Schema.Types ?? Schema.Types;
      const Types = this.base?.Types ?? requireTypes();
      if (!utils2.isPOJO(obj) && !(obj instanceof SchemaTypeOptions)) {
        const constructorName = utils2.getFunctionName(obj.constructor);
        if (constructorName !== "Object") {
          const oldObj = obj;
          obj = {};
          obj[options2.typeKey] = oldObj;
        }
      }
      let type2 = obj[options2.typeKey] && (obj[options2.typeKey] instanceof Function || options2.typeKey !== "type" || !obj.type.type) ? obj[options2.typeKey] : {};
      if (type2 instanceof SchemaType) {
        if (type2.path === path) {
          return type2;
        }
        const clone2 = type2.clone();
        clone2.path = path;
        return clone2;
      }
      let name;
      if (utils2.isPOJO(type2) || type2 === "mixed") {
        return new MongooseTypes2.Mixed(path, obj, null, this);
      }
      if (Array.isArray(type2) || type2 === Array || type2 === "array" || type2 === MongooseTypes2.Array) {
        let cast2 = type2 === Array || type2 === "array" ? obj.cast || obj.of : type2[0];
        if (cast2?.instanceOfSchema) {
          if (!(cast2 instanceof Schema)) {
            if (this.options._isMerging) {
              cast2 = new Schema(cast2);
            } else {
              throw new TypeError("Schema for array path `" + path + `\` is from a different copy of the Mongoose module. Please make sure you're using the same version of Mongoose everywhere with \`npm list mongoose\`. If you are still getting this error, please add \`new Schema()\` around the path: ${path}: new Schema(...)`);
            }
          }
          return new MongooseTypes2.DocumentArray(path, cast2, obj, null, this);
        }
        if (cast2 && cast2[options2.typeKey] && cast2[options2.typeKey].instanceOfSchema) {
          if (!(cast2[options2.typeKey] instanceof Schema)) {
            if (this.options._isMerging) {
              cast2[options2.typeKey] = new Schema(cast2[options2.typeKey]);
            } else {
              throw new TypeError("Schema for array path `" + path + `\` is from a different copy of the Mongoose module. Please make sure you're using the same version of Mongoose everywhere with \`npm list mongoose\`. If you are still getting this error, please add \`new Schema()\` around the path: ${path}: new Schema(...)`);
            }
          }
          return new MongooseTypes2.DocumentArray(path, cast2[options2.typeKey], obj, cast2, this);
        }
        if (typeof cast2 !== "undefined") {
          if (Array.isArray(cast2) || cast2.type === Array || cast2.type == "Array") {
            if (cast2?.type == "Array") {
              cast2.type = Array;
            }
            return new MongooseTypes2.Array(path, this.interpretAsType(path, cast2, options2), obj, null, this);
          }
        }
        const castFromTypeKey = cast2 != null && cast2[options2.typeKey] && (options2.typeKey !== "type" || !cast2.type.type) ? cast2[options2.typeKey] : cast2;
        if (typeof cast2 === "string") {
          cast2 = MongooseTypes2[cast2.charAt(0).toUpperCase() + cast2.substring(1)];
        } else if (utils2.isPOJO(castFromTypeKey)) {
          if (utils2.hasOwnKeys(castFromTypeKey)) {
            const childSchemaOptions = { minimize: options2.minimize };
            if (options2.typeKey) {
              childSchemaOptions.typeKey = options2.typeKey;
            }
            if (Object.hasOwn(options2, "strict")) {
              childSchemaOptions.strict = options2.strict;
            }
            if (Object.hasOwn(options2, "strictQuery")) {
              childSchemaOptions.strictQuery = options2.strictQuery;
            }
            if (Object.hasOwn(options2, "toObject")) {
              childSchemaOptions.toObject = utils2.omit(options2.toObject, ["transform"]);
            }
            if (Object.hasOwn(options2, "toJSON")) {
              childSchemaOptions.toJSON = utils2.omit(options2.toJSON, ["transform"]);
            }
            if (Object.hasOwn(this._userProvidedOptions, "_id")) {
              childSchemaOptions._id = this._userProvidedOptions._id;
            } else if (Schema.Types.DocumentArray.defaultOptions._id != null) {
              childSchemaOptions._id = Schema.Types.DocumentArray.defaultOptions._id;
            }
            const childSchema = new Schema(castFromTypeKey, childSchemaOptions);
            childSchema.$implicitlyCreated = true;
            return new MongooseTypes2.DocumentArray(path, childSchema, obj, null, this);
          } else {
            return new MongooseTypes2.Array(path, MongooseTypes2.Mixed, obj, null, this);
          }
        }
        if (cast2) {
          type2 = cast2[options2.typeKey] && (options2.typeKey !== "type" || !cast2.type.type) ? cast2[options2.typeKey] : cast2;
          if (Array.isArray(type2)) {
            return new MongooseTypes2.Array(path, this.interpretAsType(path, type2, options2), obj, null, this);
          }
          name = typeof type2 === "string" ? type2 : type2.schemaName || utils2.getFunctionName(type2);
          if (name === "ClockDate") {
            name = "Date";
          }
          if (name === void 0) {
            throw new TypeError(`Invalid schema configuration: Could not determine the embedded type for array \`${path}\`. See https://mongoosejs.com/docs/guide.html#definition for more info on supported schema syntaxes.`);
          }
          if (!Object.hasOwn(MongooseTypes2, name)) {
            throw new TypeError(`Invalid schema configuration: \`${name}\` is not a valid type within the array \`${path}\`.See https://bit.ly/mongoose-schematypes for a list of valid schema types.`);
          }
          if (name === "Union" && typeof cast2 === "object") {
            cast2.parentSchema = this;
          }
        }
        return new MongooseTypes2.Array(path, cast2 || MongooseTypes2.Mixed, obj, options2, this);
      }
      if (type2?.instanceOfSchema) {
        return new MongooseTypes2.Subdocument(type2, path, obj, this);
      }
      if (Buffer.isBuffer(type2)) {
        name = "Buffer";
      } else if (typeof type2 === "function" || typeof type2 === "object") {
        name = type2.schemaName || utils2.getFunctionName(type2);
      } else if (type2 === Types.ObjectId) {
        name = "ObjectId";
      } else if (type2 === Types.Decimal128) {
        name = "Decimal128";
      } else {
        name = type2 == null ? "" + type2 : type2.toString();
      }
      if (name) {
        name = name.charAt(0).toUpperCase() + name.substring(1);
      }
      if (name === "ObjectID") {
        name = "ObjectId";
      }
      if (name === "ClockDate") {
        name = "Date";
      }
      if (name === void 0) {
        throw new TypeError(`Invalid schema configuration: \`${path}\` schematype definition is invalid. See https://mongoosejs.com/docs/guide.html#definition for more info on supported schema syntaxes.`);
      }
      if (MongooseTypes2[name] == null) {
        throw new TypeError(`Invalid schema configuration: \`${name}\` is not a valid type at path \`${path}\`. See https://bit.ly/mongoose-schematypes for a list of valid schema types.`);
      }
      const schemaType2 = new MongooseTypes2[name](path, obj, options2, this);
      return schemaType2;
    };
    Schema.prototype.eachPath = function(fn) {
      const keys = Object.keys(this.paths);
      const len = keys.length;
      for (let i = 0; i < len; ++i) {
        fn(keys[i], this.paths[keys[i]]);
      }
      return this;
    };
    Schema.prototype.requiredPaths = function requiredPaths(invalidate) {
      if (this._requiredpaths && !invalidate) {
        return this._requiredpaths;
      }
      const paths = Object.keys(this.paths);
      let i = paths.length;
      const ret = [];
      while (i--) {
        const path = paths[i];
        if (this.paths[path].isRequired) {
          ret.push(path);
        }
      }
      this._requiredpaths = ret;
      return this._requiredpaths;
    };
    Schema.prototype.pathsWithTransforms = function pathsWithTransforms(invalidate) {
      if (this._pathsWithTransforms !== void 0 && !invalidate) {
        return this._pathsWithTransforms;
      }
      const paths = Object.keys(this.paths);
      const ret = [];
      for (const path of paths) {
        const schematype = this.paths[path];
        const topLevelTransformFunction = schematype.options.transform ?? schematype.constructor?.defaultOptions?.transform;
        const embeddedSchemaTypeTransformFunction = schematype.embeddedSchemaType?.options?.transform ?? schematype.embeddedSchemaType?.constructor?.defaultOptions?.transform;
        if (topLevelTransformFunction || embeddedSchemaTypeTransformFunction) {
          ret.push(path);
        }
      }
      this._pathsWithTransforms = ret.length ? ret : null;
      return this._pathsWithTransforms;
    };
    Schema.prototype.indexedPaths = function indexedPaths() {
      if (this._indexedpaths) {
        return this._indexedpaths;
      }
      this._indexedpaths = this.indexes();
      return this._indexedpaths;
    };
    Schema.prototype.pathType = function(path) {
      if (Object.hasOwn(this.paths, path)) {
        return "real";
      }
      if (Object.hasOwn(this.virtuals, path)) {
        return "virtual";
      }
      if (Object.hasOwn(this.nested, path)) {
        return "nested";
      }
      const cleanPath = _pathToPositionalSyntax(path);
      if (Object.hasOwn(this.subpaths, cleanPath) || Object.hasOwn(this.subpaths, path)) {
        return "real";
      }
      const singleNestedPath = Object.hasOwn(this.singleNestedPaths, cleanPath) || Object.hasOwn(this.singleNestedPaths, path);
      if (singleNestedPath) {
        return singleNestedPath === "nested" ? "nested" : "real";
      }
      const mapPath = getMapPath(this, path);
      if (mapPath != null) {
        return "real";
      }
      if (/\.\d+\.|\.\d+$/.test(path)) {
        return getPositionalPathType(this, path, cleanPath);
      }
      return "adhocOrUndefined";
    };
    Schema.prototype.hasMixedParent = function(path) {
      const subpaths = path.split(/\./g);
      path = "";
      for (let i = 0; i < subpaths.length; ++i) {
        path = i > 0 ? path + "." + subpaths[i] : subpaths[i];
        if (Object.hasOwn(this.paths, path) && this.paths[path] instanceof MongooseTypes.Mixed) {
          return this.paths[path];
        }
      }
      return null;
    };
    Schema.prototype.setupTimestamp = function(timestamps) {
      return setupTimestamps2(this, timestamps);
    };
    function getPositionalPathType(self, path, cleanPath) {
      const subpaths = path.split(/\.(\d+)\.|\.(\d+)$/).filter(Boolean);
      if (subpaths.length < 2) {
        return Object.hasOwn(self.paths, subpaths[0]) ? self.paths[subpaths[0]] : "adhocOrUndefined";
      }
      let val = self.path(subpaths[0]);
      let isNested = false;
      if (!val) {
        return "adhocOrUndefined";
      }
      const last = subpaths.length - 1;
      for (let i = 1; i < subpaths.length; ++i) {
        isNested = false;
        const subpath = subpaths[i];
        if (i === last && val && !/\D/.test(subpath)) {
          if (val.$isMongooseDocumentArray) {
            val = val.embeddedSchemaType;
          } else if (val instanceof MongooseTypes.Array) {
            val = val.embeddedSchemaType;
          } else {
            val = void 0;
          }
          break;
        }
        if (!/\D/.test(subpath)) {
          if (val instanceof MongooseTypes.Array && i !== last) {
            val = val.embeddedSchemaType;
          }
          continue;
        }
        if (!val?.schema) {
          val = void 0;
          break;
        }
        const type2 = val.schema.pathType(subpath);
        isNested = type2 === "nested";
        val = val.schema.path(subpath);
      }
      self.subpaths[cleanPath] = val;
      if (val) {
        return "real";
      }
      if (isNested) {
        return "nested";
      }
      return "adhocOrUndefined";
    }
    function getPositionalPath(self, path, cleanPath) {
      getPositionalPathType(self, path, cleanPath);
      return self.subpaths[cleanPath];
    }
    Schema.prototype.queue = function(name, args) {
      this.callQueue.push([name, args]);
      return this;
    };
    Schema.prototype.pre = function(name) {
      if (name instanceof RegExp) {
        const remainingArgs = Array.prototype.slice.call(arguments, 1);
        for (const fn of hookNames) {
          if (name.test(fn)) {
            this.pre.apply(this, [fn].concat(remainingArgs));
          }
        }
        return this;
      }
      if (Array.isArray(name)) {
        const remainingArgs = Array.prototype.slice.call(arguments, 1);
        for (const el of name) {
          this.pre.apply(this, [el].concat(remainingArgs));
        }
        return this;
      }
      this.s.hooks.pre.apply(this.s.hooks, arguments);
      return this;
    };
    Schema.prototype.post = function(name) {
      if (name instanceof RegExp) {
        const remainingArgs = Array.prototype.slice.call(arguments, 1);
        for (const fn of hookNames) {
          if (name.test(fn)) {
            this.post.apply(this, [fn].concat(remainingArgs));
          }
        }
        return this;
      }
      if (Array.isArray(name)) {
        const remainingArgs = Array.prototype.slice.call(arguments, 1);
        for (const el of name) {
          this.post.apply(this, [el].concat(remainingArgs));
        }
        return this;
      }
      this.s.hooks.post.apply(this.s.hooks, arguments);
      return this;
    };
    Schema.prototype.plugin = function(fn, opts) {
      if (typeof fn !== "function") {
        throw new MongooseError('First param to `schema.plugin()` must be a function, got "' + typeof fn + '"');
      }
      if (opts?.deduplicate) {
        for (const plugin of this.plugins) {
          if (plugin.fn === fn) {
            return this;
          }
        }
      }
      this.plugins.push({ fn, opts });
      fn(this, opts);
      return this;
    };
    Schema.prototype.method = function(name, fn, options2) {
      if (typeof name !== "string") {
        for (const i in name) {
          this.methods[i] = name[i];
          this.methodOptions[i] = clone(options2);
        }
      } else {
        this.methods[name] = fn;
        this.methodOptions[name] = clone(options2);
      }
      return this;
    };
    Schema.prototype.static = function(name, fn) {
      if (typeof name !== "string") {
        for (const i in name) {
          this.statics[i] = name[i];
        }
      } else {
        this.statics[name] = fn;
      }
      return this;
    };
    Schema.prototype.index = function(fields, options2) {
      fields || (fields = {});
      options2 || (options2 = {});
      if (options2.expires) {
        utils2.expires(options2);
      }
      for (const key in fields) {
        if (this.aliases[key]) {
          fields = utils2.renameObjKey(fields, key, this.aliases[key]);
        }
      }
      for (const field of Object.keys(fields)) {
        if (fields[field] === "ascending" || fields[field] === "asc") {
          fields[field] = 1;
        } else if (fields[field] === "descending" || fields[field] === "desc") {
          fields[field] = -1;
        }
      }
      this._indexes.push([fields, options2]);
      return this;
    };
    Schema.prototype.set = function(key, value, tags) {
      if (arguments.length === 1) {
        return this.options[key];
      }
      switch (key) {
        case "read":
          if (typeof value === "string") {
            this.options[key] = { mode: handleReadPreferenceAliases2(value), tags };
          } else if (Array.isArray(value) && typeof value[0] === "string") {
            this.options[key] = {
              mode: handleReadPreferenceAliases2(value[0]),
              tags: value[1]
            };
          } else {
            this.options[key] = value;
          }
          this._userProvidedOptions[key] = this.options[key];
          break;
        case "timestamps":
          this.setupTimestamp(value);
          this.options[key] = value;
          this._userProvidedOptions[key] = this.options[key];
          break;
        case "_id":
          this.options[key] = value;
          this._userProvidedOptions[key] = this.options[key];
          if (value && !this.paths["_id"]) {
            addAutoId2(this);
          } else if (!value && this.paths["_id"] != null && this.paths["_id"].auto) {
            this.remove("_id");
          }
          break;
        default:
          this.options[key] = value;
          this._userProvidedOptions[key] = this.options[key];
          break;
      }
      if (key === "strict") {
        _propagateOptionsToImplicitlyCreatedSchemas(this, { strict: value });
      }
      if (key === "strictQuery") {
        _propagateOptionsToImplicitlyCreatedSchemas(this, { strictQuery: value });
      }
      if (key === "toObject") {
        value = { ...value };
        delete value.transform;
        _propagateOptionsToImplicitlyCreatedSchemas(this, { toObject: value });
      }
      if (key === "toJSON") {
        value = { ...value };
        delete value.transform;
        _propagateOptionsToImplicitlyCreatedSchemas(this, { toJSON: value });
      }
      return this;
    };
    function _propagateOptionsToImplicitlyCreatedSchemas(baseSchema, options2) {
      for (const { schema: schema2 } of baseSchema.childSchemas) {
        if (!schema2.$implicitlyCreated) {
          continue;
        }
        Object.assign(schema2.options, options2);
        _propagateOptionsToImplicitlyCreatedSchemas(schema2, options2);
      }
    }
    Schema.prototype.get = function(key) {
      return this.options[key];
    };
    const indexTypes = "2d 2dsphere hashed text".split(" ");
    Object.defineProperty(Schema, "indexTypes", {
      get: function() {
        return indexTypes;
      },
      set: function() {
        throw new MongooseError("Cannot overwrite Schema.indexTypes");
      }
    });
    Schema.prototype.indexes = function() {
      return getIndexes2(this);
    };
    Schema.prototype.virtual = function(name, options2) {
      if (name instanceof VirtualType || getConstructorName2(name) === "VirtualType") {
        return this.virtual(name.path, name.options);
      }
      options2 = new VirtualOptions(options2);
      if (utils2.hasUserDefinedProperty(options2, ["ref", "refPath"])) {
        if (options2.localField == null) {
          throw new MongooseError("Reference virtuals require `localField` option");
        }
        if (options2.foreignField == null) {
          throw new MongooseError("Reference virtuals require `foreignField` option");
        }
        const virtual = this.virtual(name);
        virtual.options = options2;
        this.pre("init", function virtualPreInit(obj, opts) {
          if (mpath.has(name, obj)) {
            const _v = mpath.get(name, obj);
            if (!this.$$populatedVirtuals) {
              this.$$populatedVirtuals = {};
            }
            if (options2.justOne || options2.count) {
              this.$$populatedVirtuals[name] = Array.isArray(_v) ? _v[0] : _v;
            } else {
              this.$$populatedVirtuals[name] = Array.isArray(_v) ? _v : _v == null ? [] : [_v];
            }
            if (opts?.hydratedPopulatedDocs && !options2.count) {
              const modelNames = virtual._getModelNamesForPopulate(this);
              const populatedVal = this.$$populatedVirtuals[name];
              if (!Array.isArray(populatedVal) && !populatedVal.$__ && modelNames?.length === 1) {
                const PopulateModel = this.db.model(modelNames[0]);
                this.$$populatedVirtuals[name] = PopulateModel.hydrate(populatedVal);
              } else if (Array.isArray(populatedVal) && modelNames?.length === 1) {
                const PopulateModel = this.db.model(modelNames[0]);
                for (let i = 0; i < populatedVal.length; ++i) {
                  if (!populatedVal[i].$__) {
                    populatedVal[i] = PopulateModel.hydrate(populatedVal[i], null, { hydratedPopulatedDocs: true });
                  }
                }
                const foreignField = options2.foreignField;
                this.$populated(
                  name,
                  populatedVal.map((doc) => doc == null ? doc : doc.get(typeof foreignField === "function" ? foreignField.call(doc, doc) : foreignField)),
                  { populateModelSymbol: PopulateModel }
                );
              }
            }
            mpath.unset(name, obj);
          }
        });
        virtual.set(function(v) {
          if (!this.$$populatedVirtuals) {
            this.$$populatedVirtuals = {};
          }
          return setPopulatedVirtualValue2(
            this.$$populatedVirtuals,
            name,
            v,
            options2
          );
        });
        if (typeof options2.get === "function") {
          virtual.get(options2.get);
        }
        const parts2 = name.split(".");
        let cur = parts2[0];
        for (let i = 0; i < parts2.length - 1; ++i) {
          if (this.paths[cur] == null) {
            continue;
          }
          if (this.paths[cur].$isMongooseDocumentArray || this.paths[cur].$isSingleNested) {
            const remnant = parts2.slice(i + 1).join(".");
            this.paths[cur].schema.virtual(remnant, options2);
            break;
          } else if (this.paths[cur].$isSchemaMap) {
            const remnant = parts2.slice(i + 2).join(".");
            this.paths[cur].$__schemaType.schema.virtual(remnant, options2);
            break;
          }
          cur += "." + parts2[i + 1];
        }
        return virtual;
      }
      const virtuals = this.virtuals;
      const parts = name.split(".");
      if (this.pathType(name) === "real") {
        throw new MongooseError('Virtual path "' + name + '" conflicts with a real path in the schema');
      }
      virtuals[name] = parts.reduce(function(mem, part, i) {
        mem[part] || (mem[part] = i === parts.length - 1 ? new VirtualType(options2, name) : {});
        return mem[part];
      }, this.tree);
      if (options2?.applyToArray && parts.length > 1) {
        const path = this.path(parts.slice(0, -1).join("."));
        if (path?.$isMongooseArray) {
          return path.virtual(parts[parts.length - 1], options2);
        } else {
          throw new MongooseError(`Path "${path}" is not an array`);
        }
      }
      return virtuals[name];
    };
    Schema.prototype.virtualpath = function(name) {
      return Object.hasOwn(this.virtuals, name) ? this.virtuals[name] : null;
    };
    Schema.prototype.remove = function(path) {
      if (typeof path === "string") {
        path = [path];
      }
      if (Array.isArray(path)) {
        path.forEach(function(name) {
          if (this.path(name) == null && !this.nested[name]) {
            return;
          }
          if (this.nested[name]) {
            const allKeys = Object.keys(this.paths).concat(Object.keys(this.nested));
            for (const path2 of allKeys) {
              if (path2.startsWith(name + ".")) {
                delete this.paths[path2];
                delete this.nested[path2];
                _deletePath(this, path2);
              }
            }
            delete this.nested[name];
            _deletePath(this, name);
            return;
          }
          delete this.paths[name];
          _deletePath(this, name);
          this._removeEncryptedField(name);
        }, this);
      }
      return this;
    };
    function _deletePath(schema2, name) {
      const pieces = name.split(".");
      const last = pieces.pop();
      let branch = schema2.tree;
      for (const piece of pieces) {
        branch = branch[piece];
      }
      delete branch[last];
    }
    Schema.prototype.removeVirtual = function(path) {
      if (typeof path === "string") {
        path = [path];
      }
      if (Array.isArray(path)) {
        for (const virtual of path) {
          if (this.virtuals[virtual] == null) {
            throw new MongooseError(`Attempting to remove virtual "${virtual}" that does not exist.`);
          }
        }
        for (const virtual of path) {
          delete this.paths[virtual];
          delete this.virtuals[virtual];
          if (virtual.indexOf(".") !== -1) {
            mpath.unset(virtual, this.tree);
          } else {
            delete this.tree[virtual];
          }
        }
      }
      return this;
    };
    Schema.prototype.loadClass = function(model2, virtualsOnly) {
      if (model2 === Object.prototype || model2 === Function.prototype || Object.hasOwn(model2.prototype, "$isMongooseModelPrototype") || Object.hasOwn(model2.prototype, "$isMongooseDocumentPrototype")) {
        return this;
      }
      this.loadClass(Object.getPrototypeOf(model2), virtualsOnly);
      if (!virtualsOnly) {
        Object.getOwnPropertyNames(model2).forEach(function(name) {
          if (name.match(/^(length|name|prototype|constructor|__proto__)$/)) {
            return;
          }
          const prop = Object.getOwnPropertyDescriptor(model2, name);
          if (Object.hasOwn(prop, "value")) {
            this.static(name, prop.value);
          }
        }, this);
      }
      Object.getOwnPropertyNames(model2.prototype).forEach(function(name) {
        if (name.match(/^(constructor)$/)) {
          return;
        }
        const method = Object.getOwnPropertyDescriptor(model2.prototype, name);
        if (!virtualsOnly) {
          if (typeof method.value === "function") {
            this.method(name, method.value);
          }
        }
        if (typeof method.get === "function") {
          if (this.virtuals[name]) {
            this.virtuals[name].getters = [];
          }
          this.virtual(name).get(method.get);
        }
        if (typeof method.set === "function") {
          if (this.virtuals[name]) {
            this.virtuals[name].setters = [];
          }
          this.virtual(name).set(method.set);
        }
      }, this);
      return this;
    };
    Schema.prototype._getSchema = function(path) {
      const _this = this;
      const pathschema = _this.path(path);
      const resultPath = [];
      if (pathschema) {
        pathschema.$fullPath = path;
        return pathschema;
      }
      function search(parts2, schema2) {
        let p = parts2.length + 1;
        let foundschema;
        let trypath;
        while (p--) {
          trypath = parts2.slice(0, p).join(".");
          foundschema = schema2.path(trypath);
          if (foundschema) {
            resultPath.push(trypath);
            if (foundschema.embeddedSchemaType || foundschema.Constructor) {
              if (foundschema.embeddedSchemaType instanceof MongooseTypes.Mixed) {
                foundschema.embeddedSchemaType.$fullPath = resultPath.join(".");
                return foundschema.embeddedSchemaType;
              }
              if (p !== parts2.length) {
                if (p + 1 === parts2.length && foundschema.embeddedSchemaType && (parts2[p] === "$" || isArrayFilter(parts2[p]))) {
                  return foundschema.embeddedSchemaType;
                }
                if (foundschema.schema) {
                  let ret;
                  if (parts2[p] === "$" || isArrayFilter(parts2[p])) {
                    if (p + 1 === parts2.length) {
                      return foundschema.embeddedSchemaType;
                    }
                    ret = search(parts2.slice(p + 1), foundschema.schema);
                    if (ret) {
                      ret.$parentSchemaDocArray = ret.$parentSchemaDocArray || (foundschema.schema.$isSingleNested ? null : foundschema);
                    }
                    return ret;
                  }
                  ret = search(parts2.slice(p), foundschema.schema);
                  if (ret) {
                    ret.$parentSchemaDocArray = ret.$parentSchemaDocArray || (foundschema.schema.$isSingleNested ? null : foundschema);
                  }
                  return ret;
                }
              }
            } else if (foundschema.$isSchemaMap) {
              if (p >= parts2.length) {
                return foundschema;
              }
              if (p + 1 >= parts2.length) {
                return foundschema.$__schemaType;
              }
              if (foundschema.$__schemaType instanceof MongooseTypes.Mixed) {
                return foundschema.$__schemaType;
              }
              if (foundschema.$__schemaType.schema != null) {
                const ret = search(parts2.slice(p + 1), foundschema.$__schemaType.schema);
                return ret;
              }
            }
            foundschema.$fullPath = resultPath.join(".");
            return foundschema;
          }
        }
      }
      const parts = path.split(".");
      for (let i = 0; i < parts.length; ++i) {
        if (parts[i] === "$" || isArrayFilter(parts[i])) {
          parts[i] = "0";
        }
        if (numberRE.test(parts[i])) {
          parts[i] = "$";
        }
      }
      return search(parts, _this);
    };
    Schema.prototype._getPathType = function(path) {
      const _this = this;
      const pathschema = _this.path(path);
      if (pathschema) {
        return "real";
      }
      function search(parts, schema2) {
        let p = parts.length + 1, foundschema, trypath;
        while (p--) {
          trypath = parts.slice(0, p).join(".");
          foundschema = schema2.path(trypath);
          if (foundschema) {
            if (foundschema.embeddedSchemaType || foundschema.Constructor) {
              if (foundschema.embeddedSchemaType instanceof MongooseTypes.Mixed) {
                return { schema: foundschema, pathType: "mixed" };
              }
              if (p !== parts.length && foundschema.schema) {
                if (parts[p] === "$" || isArrayFilter(parts[p])) {
                  if (p === parts.length - 1) {
                    return { schema: foundschema, pathType: "nested" };
                  }
                  return search(parts.slice(p + 1), foundschema.schema);
                }
                return search(parts.slice(p), foundschema.schema);
              }
              return {
                schema: foundschema,
                pathType: foundschema.$isSingleNested ? "nested" : "array"
              };
            }
            return { schema: foundschema, pathType: "real" };
          } else if (p === parts.length && Object.hasOwn(schema2.nested, trypath)) {
            return { schema: schema2, pathType: "nested" };
          }
        }
        return { schema: foundschema || schema2, pathType: "undefined" };
      }
      return search(path.split("."), _this);
    };
    Schema.prototype._transformDuplicateKeyError = function _transformDuplicateKeyError(error2) {
      if (!this._duplicateKeyErrorMessagesByPath) {
        return error2;
      }
      if (error2.code !== 11e3 && error2.code !== 11001) {
        return error2;
      }
      if (error2.keyPattern != null) {
        const keyPattern = error2.keyPattern;
        const keys = Object.keys(keyPattern);
        if (keys.length !== 1) {
          return error2;
        }
        const firstKey2 = keys[0];
        if (!Object.hasOwn(this._duplicateKeyErrorMessagesByPath, firstKey2)) {
          return error2;
        }
        return new MongooseError(this._duplicateKeyErrorMessagesByPath[firstKey2], { cause: error2 });
      }
      return error2;
    };
    function isArrayFilter(piece) {
      return piece.startsWith("$[") && piece.endsWith("]");
    }
    Schema.prototype._preCompile = function _preCompile() {
      this.plugin(idGetter, { deduplicate: true });
      _precomputeOptimisticConcurrency(this);
      this.pathsWithTransforms();
    };
    function _precomputeOptimisticConcurrency(schema2) {
      const opt = schema2.options.optimisticConcurrency;
      if (!opt || opt === true) {
        return;
      }
      if (Array.isArray(opt)) {
        schema2.options._optimisticConcurrencyTrie = new PathTrie(opt);
      } else if (Array.isArray(opt.exclude)) {
        schema2.options._optimisticConcurrencyExcludeTrie = new PathTrie(opt.exclude);
      }
    }
    Schema.prototype.toJSONSchema = function toJSONSchema(options2) {
      const useBsonType = options2?.useBsonType ?? false;
      const result = useBsonType ? { required: [], properties: {} } : { type: "object", required: [], properties: {} };
      for (const path of Object.keys(this.paths)) {
        const schemaType2 = this.paths[path];
        if (schemaType2._presplitPath.indexOf("$*") !== -1) {
          continue;
        }
        const isNested = schemaType2._presplitPath.length > 1;
        let jsonSchemaForPath = result;
        if (isNested) {
          for (let i = 0; i < schemaType2._presplitPath.length - 1; ++i) {
            const subpath = schemaType2._presplitPath[i];
            if (jsonSchemaForPath.properties[subpath] == null) {
              jsonSchemaForPath.properties[subpath] = useBsonType ? {
                bsonType: ["object", "null"],
                properties: {}
              } : {
                type: ["object", "null"],
                properties: {}
              };
            }
            jsonSchemaForPath = jsonSchemaForPath.properties[subpath];
          }
        }
        const lastSubpath = schemaType2._presplitPath[schemaType2._presplitPath.length - 1];
        if (path === "_id") {
          if (!jsonSchemaForPath.required) {
            jsonSchemaForPath.required = [];
          }
          jsonSchemaForPath.required.push("_id");
        } else if (schemaType2.options.required && typeof schemaType2.options.required !== "function") {
          if (!jsonSchemaForPath.required) {
            jsonSchemaForPath.required = [];
          }
          jsonSchemaForPath.required.push(lastSubpath);
        }
        jsonSchemaForPath.properties[lastSubpath] = schemaType2.toJSONSchema(options2);
      }
      if (result.required.length === 0) {
        delete result.required;
      }
      return result;
    };
    module.exports = exports = Schema;
    Schema.Types = MongooseTypes = requireSchema$1();
    exports.ObjectId = MongooseTypes.ObjectId;
  })(schema$1, schema$1.exports);
  return schema$1.exports;
}
var bulkWriteError;
var hasRequiredBulkWriteError;
function requireBulkWriteError() {
  if (hasRequiredBulkWriteError) return bulkWriteError;
  hasRequiredBulkWriteError = 1;
  const MongooseError = requireError();
  class MongooseBulkWriteError extends MongooseError {
    constructor(validationErrors, results, rawResult, operation) {
      let preview = validationErrors.map((e) => e.message).join(", ");
      if (preview.length > 200) {
        preview = preview.slice(0, 200) + "...";
      }
      super(`${operation} failed with ${validationErrors.length} Mongoose validation errors: ${preview}`);
      this.validationErrors = validationErrors;
      this.results = results;
      this.rawResult = rawResult;
      this.operation = operation;
    }
  }
  Object.defineProperty(MongooseBulkWriteError.prototype, "name", {
    value: "MongooseBulkWriteError"
  });
  bulkWriteError = MongooseBulkWriteError;
  return bulkWriteError;
}
var syncIndexes;
var hasRequiredSyncIndexes;
function requireSyncIndexes() {
  if (hasRequiredSyncIndexes) return syncIndexes;
  hasRequiredSyncIndexes = 1;
  const MongooseError = requireMongooseError();
  class SyncIndexesError extends MongooseError {
    constructor(message, errorsMap) {
      super(message);
      this.errors = errorsMap;
    }
  }
  Object.defineProperty(SyncIndexesError.prototype, "name", {
    value: "SyncIndexesError"
  });
  syncIndexes = SyncIndexesError;
  return syncIndexes;
}
var applyPlugins;
var hasRequiredApplyPlugins;
function requireApplyPlugins() {
  if (hasRequiredApplyPlugins) return applyPlugins;
  hasRequiredApplyPlugins = 1;
  applyPlugins = function applyPlugins2(schema2, plugins2, options2, cacheKey) {
    if (schema2[cacheKey]) {
      return;
    }
    schema2[cacheKey] = true;
    if (!options2?.skipTopLevel) {
      let pluginTags = null;
      for (const plugin of plugins2) {
        const tags = plugin[1] == null ? null : plugin[1].tags;
        if (!Array.isArray(tags)) {
          schema2.plugin(plugin[0], plugin[1]);
          continue;
        }
        pluginTags = pluginTags || new Set(schema2.options.pluginTags || []);
        if (!tags.find((tag) => pluginTags.has(tag))) {
          continue;
        }
        schema2.plugin(plugin[0], plugin[1]);
      }
    }
    options2 = Object.assign({}, options2);
    delete options2.skipTopLevel;
    if (options2.applyPluginsToChildSchemas !== false) {
      for (const path of Object.keys(schema2.paths)) {
        const type2 = schema2.paths[path];
        if (type2.schema != null) {
          applyPlugins2(type2.schema, plugins2, options2, cacheKey);
          type2.Constructor.prototype.$__setSchema(type2.schema);
        }
      }
    }
    const discriminators = schema2.discriminators;
    if (discriminators == null) {
      return;
    }
    const applyPluginsToDiscriminators = options2.applyPluginsToDiscriminators;
    const keys = Object.keys(discriminators);
    for (const discriminatorKey of keys) {
      const discriminatorSchema = discriminators[discriminatorKey];
      applyPlugins2(
        discriminatorSchema,
        plugins2,
        { skipTopLevel: !applyPluginsToDiscriminators },
        cacheKey
      );
    }
  };
  return applyPlugins;
}
var driver = {};
var hasRequiredDriver;
function requireDriver() {
  if (hasRequiredDriver) return driver;
  hasRequiredDriver = 1;
  let driver$1 = null;
  driver.get = function() {
    return driver$1;
  };
  driver.set = function(v) {
    driver$1 = v;
  };
  return driver;
}
var getDefaultBulkwriteResult_1;
var hasRequiredGetDefaultBulkwriteResult;
function requireGetDefaultBulkwriteResult() {
  if (hasRequiredGetDefaultBulkwriteResult) return getDefaultBulkwriteResult_1;
  hasRequiredGetDefaultBulkwriteResult = 1;
  function getDefaultBulkwriteResult() {
    return {
      ok: 1,
      nInserted: 0,
      nUpserted: 0,
      nMatched: 0,
      nModified: 0,
      nRemoved: 0,
      upserted: [],
      writeErrors: [],
      insertedIds: [],
      writeConcernErrors: []
    };
  }
  getDefaultBulkwriteResult_1 = getDefaultBulkwriteResult;
  return getDefaultBulkwriteResult_1;
}
var createCollectionsError;
var hasRequiredCreateCollectionsError;
function requireCreateCollectionsError() {
  if (hasRequiredCreateCollectionsError) return createCollectionsError;
  hasRequiredCreateCollectionsError = 1;
  const MongooseError = requireMongooseError();
  class CreateCollectionsError extends MongooseError {
    constructor(message, errorsMap) {
      super(message);
      this.errors = errorsMap;
    }
  }
  Object.defineProperty(CreateCollectionsError.prototype, "name", {
    value: "CreateCollectionsError"
  });
  createCollectionsError = CreateCollectionsError;
  return createCollectionsError;
}
var castBulkWrite = { exports: {} };
var modifiedPaths;
var hasRequiredModifiedPaths;
function requireModifiedPaths() {
  if (hasRequiredModifiedPaths) return modifiedPaths;
  hasRequiredModifiedPaths = 1;
  const _modifiedPaths = requireCommon().modifiedPaths;
  modifiedPaths = function modifiedPaths2(update) {
    const keys = Object.keys(update);
    const res = {};
    const withoutDollarKeys = {};
    for (const key of keys) {
      if (key.startsWith("$")) {
        _modifiedPaths(update[key], "", res);
        continue;
      }
      withoutDollarKeys[key] = update[key];
    }
    _modifiedPaths(withoutDollarKeys, "", res);
    return res;
  };
  return modifiedPaths;
}
var updatedPathsByArrayFilter;
var hasRequiredUpdatedPathsByArrayFilter;
function requireUpdatedPathsByArrayFilter() {
  if (hasRequiredUpdatedPathsByArrayFilter) return updatedPathsByArrayFilter;
  hasRequiredUpdatedPathsByArrayFilter = 1;
  const modifiedPaths2 = requireModifiedPaths();
  updatedPathsByArrayFilter = function updatedPathsByArrayFilter2(update) {
    if (update == null) {
      return {};
    }
    const updatedPaths = modifiedPaths2(update);
    return Object.keys(updatedPaths).reduce((cur, path) => {
      const matches = path.match(/\$\[[^\]]+\]/g);
      if (matches == null) {
        return cur;
      }
      for (const match of matches) {
        const firstMatch = path.indexOf(match);
        if (firstMatch !== path.lastIndexOf(match)) {
          throw new Error(`Path '${path}' contains the same array filter multiple times`);
        }
        cur[match.substring(2, match.length - 1)] = path.substring(0, firstMatch - 1).replace(/\$\[[^\]]+\]/g, "0");
      }
      return cur;
    }, {});
  };
  return updatedPathsByArrayFilter;
}
var getEmbeddedDiscriminatorPath;
var hasRequiredGetEmbeddedDiscriminatorPath;
function requireGetEmbeddedDiscriminatorPath() {
  if (hasRequiredGetEmbeddedDiscriminatorPath) return getEmbeddedDiscriminatorPath;
  hasRequiredGetEmbeddedDiscriminatorPath = 1;
  const cleanPositionalOperators2 = requireCleanPositionalOperators();
  const get2 = requireGet();
  const getDiscriminatorByValue2 = requireGetDiscriminatorByValue();
  const updatedPathsByArrayFilter2 = requireUpdatedPathsByArrayFilter();
  getEmbeddedDiscriminatorPath = function getEmbeddedDiscriminatorPath2(schema2, update, filter, path, options2) {
    const parts = path.indexOf(".") === -1 ? [path] : path.split(".");
    let schematype = null;
    let type2 = "adhocOrUndefined";
    filter = filter || {};
    update = update || {};
    const arrayFilters = Array.isArray(options2?.arrayFilters) ? options2.arrayFilters : [];
    const updatedPathsByFilter = updatedPathsByArrayFilter2(update);
    let startIndex = 0;
    for (let i = 0; i < parts.length; ++i) {
      const originalSubpath = parts.slice(startIndex, i + 1).join(".");
      const subpath = cleanPositionalOperators2(originalSubpath);
      schematype = schema2.path(subpath);
      if (schematype == null) {
        continue;
      }
      type2 = schema2.pathType(subpath);
      if ((schematype.$isSingleNested || schematype.$isMongooseDocumentArrayElement) && schematype.schema.discriminators != null) {
        const key = get2(schematype, "schema.options.discriminatorKey");
        const discriminatorValuePath = subpath + "." + key;
        const discriminatorFilterPath = discriminatorValuePath.replace(/\.\d+\./, ".");
        let discriminatorKey = null;
        if (discriminatorValuePath in filter) {
          discriminatorKey = filter[discriminatorValuePath];
        }
        if (discriminatorFilterPath in filter) {
          discriminatorKey = filter[discriminatorFilterPath];
        }
        const wrapperPath = subpath.replace(/\.\d+$/, "");
        if (schematype.$isMongooseDocumentArrayElement && get2(filter[wrapperPath], "$elemMatch." + key) != null) {
          discriminatorKey = filter[wrapperPath].$elemMatch[key];
        }
        const discriminatorKeyUpdatePath = originalSubpath + "." + key;
        if (discriminatorKeyUpdatePath in update) {
          discriminatorKey = update[discriminatorKeyUpdatePath];
        }
        if (discriminatorValuePath in update) {
          discriminatorKey = update[discriminatorValuePath];
        }
        for (const filterKey of Object.keys(updatedPathsByFilter)) {
          const schemaKey = updatedPathsByFilter[filterKey] + "." + key;
          const arrayFilterKey = filterKey + "." + key;
          if (schemaKey === discriminatorFilterPath) {
            const filter2 = arrayFilters.find((filter3) => Object.hasOwn(filter3, arrayFilterKey));
            if (filter2 != null) {
              discriminatorKey = filter2[arrayFilterKey];
            }
          }
        }
        if (discriminatorKey == null) {
          continue;
        }
        const discriminator2 = getDiscriminatorByValue2(schematype.Constructor.discriminators, discriminatorKey);
        const discriminatorSchema = discriminator2?.schema;
        if (discriminatorSchema == null) {
          continue;
        }
        const rest = parts.slice(i + 1).join(".");
        schematype = discriminatorSchema._getSchema(rest);
        schema2 = discriminatorSchema;
        startIndex = i + 1;
        if (schematype != null) {
          type2 = discriminatorSchema._getPathType(rest);
          break;
        }
      }
    }
    return { type: type2, schematype };
  };
  return getEmbeddedDiscriminatorPath;
}
var handleImmutable;
var hasRequiredHandleImmutable;
function requireHandleImmutable() {
  if (hasRequiredHandleImmutable) return handleImmutable;
  hasRequiredHandleImmutable = 1;
  const StrictModeError = requireStrict();
  handleImmutable = function handleImmutable2(schematype, strict2, obj, key, fullPath, options2, ctx) {
    if (!schematype?.options?.immutable) {
      return false;
    }
    let immutable = schematype.options.immutable;
    if (typeof immutable === "function") {
      immutable = immutable.call(ctx, ctx);
    }
    if (!immutable) {
      return false;
    }
    if (options2?.overwriteImmutable) {
      return false;
    }
    if (strict2 === false) {
      return false;
    }
    if (strict2 === "throw") {
      throw new StrictModeError(
        null,
        `Field ${fullPath} is immutable and strict = 'throw'`
      );
    }
    delete obj[key];
    return true;
  };
  return handleImmutable;
}
var moveImmutableProperties;
var hasRequiredMoveImmutableProperties;
function requireMoveImmutableProperties() {
  if (hasRequiredMoveImmutableProperties) return moveImmutableProperties;
  hasRequiredMoveImmutableProperties = 1;
  const get2 = requireGet();
  moveImmutableProperties = function moveImmutableProperties2(schema2, update, ctx) {
    if (update == null) {
      return;
    }
    const keys = Object.keys(update);
    for (const key of keys) {
      const isDollarKey = key.startsWith("$");
      if (key === "$set") {
        const updatedPaths = Object.keys(update[key]);
        for (const path of updatedPaths) {
          _walkUpdatePath(schema2, update[key], path, update, ctx);
        }
      } else if (!isDollarKey) {
        _walkUpdatePath(schema2, update, key, update, ctx);
      }
    }
  };
  function _walkUpdatePath(schema2, op, path, update, ctx) {
    const schematype = schema2.path(path);
    if (schematype == null) {
      return;
    }
    let immutable = get2(schematype, "options.immutable", null);
    if (immutable == null) {
      return;
    }
    if (typeof immutable === "function") {
      immutable = immutable.call(ctx, ctx);
    }
    if (!immutable) {
      return;
    }
    update.$setOnInsert = update.$setOnInsert || {};
    update.$setOnInsert[path] = op[path];
    delete op[path];
  }
  return moveImmutableProperties;
}
var setDottedPath;
var hasRequiredSetDottedPath;
function requireSetDottedPath() {
  if (hasRequiredSetDottedPath) return setDottedPath;
  hasRequiredSetDottedPath = 1;
  const specialProperties2 = requireSpecialProperties();
  setDottedPath = function setDottedPath2(obj, path, val) {
    if (path.indexOf(".") === -1) {
      if (specialProperties2.has(path)) {
        return;
      }
      obj[path] = val;
      return;
    }
    const parts = path.split(".");
    const last = parts.pop();
    let cur = obj;
    for (const part of parts) {
      if (specialProperties2.has(part)) {
        continue;
      }
      if (cur[part] == null) {
        cur[part] = {};
      }
      cur = cur[part];
    }
    if (!specialProperties2.has(last)) {
      cur[last] = val;
    }
  };
  return setDottedPath;
}
var castUpdate;
var hasRequiredCastUpdate;
function requireCastUpdate() {
  if (hasRequiredCastUpdate) return castUpdate;
  hasRequiredCastUpdate = 1;
  const CastError = requireCast$1();
  const MongooseError = requireMongooseError();
  const SchemaString = requireString();
  const StrictModeError = requireStrict();
  const ValidationError = requireValidation();
  const castNumber = requireNumber$1();
  const cast2 = requireCast();
  const getConstructorName2 = requireGetConstructorName();
  const getDiscriminatorByValue2 = requireGetDiscriminatorByValue();
  const getEmbeddedDiscriminatorPath2 = requireGetEmbeddedDiscriminatorPath();
  const handleImmutable2 = requireHandleImmutable();
  const isOperator2 = requireIsOperator();
  const moveImmutableProperties2 = requireMoveImmutableProperties();
  const schemaMixedSymbol = requireSymbols().schemaMixedSymbol;
  const setDottedPath2 = requireSetDottedPath();
  const utils2 = requireUtils();
  const { internalToObjectOptions } = requireOptions();
  const mongodbUpdateOperators = /* @__PURE__ */ new Set([
    "$currentDate",
    "$inc",
    "$min",
    "$max",
    "$mul",
    "$rename",
    "$set",
    "$setOnInsert",
    "$unset",
    "$addToSet",
    "$pop",
    "$pull",
    "$push",
    "$pullAll",
    "$bit"
  ]);
  castUpdate = function castUpdate2(schema2, obj, options2, context, filter) {
    if (obj == null) {
      return void 0;
    }
    options2 = options2 || {};
    if (Array.isArray(obj)) {
      const len = obj.length;
      for (let i2 = 0; i2 < len; ++i2) {
        const ops2 = Object.keys(obj[i2]);
        for (const op of ops2) {
          obj[i2][op] = castPipelineOperator(op, obj[i2][op]);
        }
      }
      return obj;
    }
    if (schema2 != null && filter != null && utils2.hasUserDefinedProperty(filter, schema2.options.discriminatorKey) && typeof filter[schema2.options.discriminatorKey] !== "object" && schema2.discriminators != null) {
      const discriminatorValue = filter[schema2.options.discriminatorKey];
      const byValue = getDiscriminatorByValue2(context.model.discriminators, discriminatorValue);
      schema2 = schema2.discriminators[discriminatorValue] || byValue?.schema || schema2;
    } else if (schema2 != null && options2.overwriteDiscriminatorKey && utils2.hasUserDefinedProperty(obj, schema2.options.discriminatorKey) && schema2.discriminators != null) {
      const discriminatorValue = obj[schema2.options.discriminatorKey];
      const byValue = getDiscriminatorByValue2(context.model.discriminators, discriminatorValue);
      schema2 = schema2.discriminators[discriminatorValue] || byValue?.schema || schema2;
    } else if (schema2 != null && options2.overwriteDiscriminatorKey && obj.$set != null && utils2.hasUserDefinedProperty(obj.$set, schema2.options.discriminatorKey) && schema2.discriminators != null) {
      const discriminatorValue = obj.$set[schema2.options.discriminatorKey];
      const byValue = getDiscriminatorByValue2(context.model.discriminators, discriminatorValue);
      schema2 = schema2.discriminators[discriminatorValue] || byValue?.schema || schema2;
    }
    if (options2.upsert) {
      moveImmutableProperties2(schema2, obj, context);
    }
    if (obj?.$__ && typeof obj.toObject === "function") {
      obj = obj.toObject(internalToObjectOptions);
    }
    const ops = Object.keys(obj);
    let i = ops.length;
    const ret = {};
    let val;
    let hasDollarKey = false;
    filter = filter || {};
    while (i--) {
      const op = ops[i];
      if (!mongodbUpdateOperators.has(op)) {
        if (!ret.$set) {
          if (obj.$set) {
            ret.$set = obj.$set;
          } else {
            ret.$set = {};
          }
        }
        ret.$set[op] = obj[op];
        ops.splice(i, 1);
        if (!~ops.indexOf("$set")) ops.push("$set");
      } else if (op === "$set") {
        if (!ret.$set) {
          ret[op] = obj[op];
        }
      } else {
        ret[op] = obj[op];
      }
    }
    i = ops.length;
    while (i--) {
      const op = ops[i];
      val = ret[op];
      hasDollarKey = hasDollarKey || op.startsWith("$");
      if (val?.$__) {
        val = val.toObject(internalToObjectOptions);
        ret[op] = val;
      }
      if (val && typeof val === "object" && !Buffer.isBuffer(val) && mongodbUpdateOperators.has(op)) {
        walkUpdatePath(schema2, val, op, options2, context, filter);
      } else {
        const msg = "Invalid atomic update value for " + op + ". Expected an object, received " + typeof val;
        throw new Error(msg);
      }
      if (op.startsWith("$") && utils2.isEmptyObject(val)) {
        delete ret[op];
      }
    }
    if (utils2.hasOwnKeys(ret) === false && options2.upsert && utils2.hasOwnKeys(filter)) {
      return { $setOnInsert: { ...filter } };
    }
    return ret;
  };
  function castPipelineOperator(op, val) {
    if (op === "$unset") {
      if (typeof val !== "string" && (!Array.isArray(val) || val.find((v) => typeof v !== "string"))) {
        throw new MongooseError("Invalid $unset in pipeline, must be  a string or an array of strings");
      }
      return val;
    }
    if (op === "$project") {
      if (val == null || typeof val !== "object") {
        throw new MongooseError("Invalid $project in pipeline, must be an object");
      }
      return val;
    }
    if (op === "$addFields" || op === "$set") {
      if (val == null || typeof val !== "object") {
        throw new MongooseError("Invalid " + op + " in pipeline, must be an object");
      }
      return val;
    } else if (op === "$replaceRoot" || op === "$replaceWith") {
      if (val == null || typeof val !== "object") {
        throw new MongooseError("Invalid " + op + " in pipeline, must be an object");
      }
      return val;
    }
    throw new MongooseError('Invalid update pipeline operator: "' + op + '"');
  }
  function walkUpdatePath(schema2, obj, op, options2, context, filter, prefix) {
    const strict2 = options2.strict;
    prefix = prefix ? prefix + "." : "";
    const keys = Object.keys(obj);
    let i = keys.length;
    let hasKeys = false;
    let schematype;
    let key;
    let val;
    let aggregatedError = null;
    const strictMode = strict2 ?? schema2.options.strict;
    while (i--) {
      key = keys[i];
      val = obj[key];
      const fullPath = prefix + key;
      const isTopLevelOperator = !prefix && isOperator2(key);
      let fullPathSchema = isTopLevelOperator ? schema2._getSchema(fullPath) : null;
      const isTopLevelNestedDollarPath = isTopLevelOperator && Object.hasOwn(schema2.nested, key);
      if (isTopLevelOperator && fullPathSchema == null && !isTopLevelNestedDollarPath) {
        throw new MongooseError('Invalid update: Unexpected modifier "' + key + '" as a key in operator "' + op + '". Did you mean something like { ' + op + ": { fieldName: { " + key + ": [...] } } }? Modifiers must appear under a valid field path.");
      }
      if (op === "$pull") {
        if (!isTopLevelOperator) {
          fullPathSchema = schema2._getSchema(fullPath);
        }
        schematype = fullPathSchema;
        if (schematype == null) {
          const _res = getEmbeddedDiscriminatorPath2(schema2, obj, filter, prefix + key, options2);
          if (_res.schematype != null) {
            schematype = _res.schematype;
          }
        }
        if (schematype?.schema != null) {
          obj[key] = cast2(schematype.schema, obj[key], options2, context);
          hasKeys = true;
          continue;
        }
      }
      const discriminatorKey = prefix ? prefix + key : key;
      if (schema2.discriminatorMapping != null && discriminatorKey === schema2.options.discriminatorKey && schema2.discriminatorMapping.value !== obj[key] && !options2.overwriteDiscriminatorKey) {
        if (strictMode === "throw") {
          const err = new Error(`Can't modify discriminator key "` + discriminatorKey + '" on discriminator model');
          aggregatedError = _appendError(err, context, discriminatorKey, aggregatedError);
          continue;
        } else if (strictMode) {
          delete obj[key];
          continue;
        }
      }
      if (!isTopLevelOperator && op !== "$pull") {
        fullPathSchema = schema2._getSchema(fullPath);
      }
      schematype = fullPathSchema;
      if (getConstructorName2(val) === "Object") {
        if (schematype == null) {
          const _res = getEmbeddedDiscriminatorPath2(schema2, obj, filter, prefix + key, options2);
          if (_res.schematype != null) {
            schematype = _res.schematype;
          }
        }
        if (op !== "$setOnInsert" && handleImmutable2(schematype, strict2, obj, key, prefix + key, options2, context)) {
          continue;
        }
        if (schematype && (schematype.embeddedSchemaType || schematype.Constructor) && op in castOps) {
          if ("$each" in val) {
            hasKeys = true;
            try {
              obj[key] = {
                $each: castUpdateVal(schematype, val.$each, op, key, context, prefix + key)
              };
            } catch (error2) {
              aggregatedError = _appendError(error2, context, key, aggregatedError);
            }
            if (val.$slice != null) {
              obj[key].$slice = val.$slice | 0;
            }
            if (val.$sort) {
              obj[key].$sort = val.$sort;
            }
            if (val.$position != null) {
              obj[key].$position = castNumber(val.$position);
            }
          } else {
            if (schematype?.$isSingleNested) {
              const _strict = strict2 == null ? schematype.schema.options.strict : strict2;
              try {
                obj[key] = schematype.castForQuery(null, val, context, { strict: _strict });
              } catch (error2) {
                aggregatedError = _appendError(error2, context, key, aggregatedError);
              }
            } else {
              try {
                obj[key] = castUpdateVal(schematype, val, op, key, context, prefix + key);
              } catch (error2) {
                aggregatedError = _appendError(error2, context, key, aggregatedError);
              }
            }
            if (obj[key] === void 0) {
              delete obj[key];
              continue;
            }
            hasKeys = true;
          }
        } else if (op === "$currentDate" || op in castOps && schematype) {
          try {
            obj[key] = castUpdateVal(schematype, val, op, key, context, prefix + key);
          } catch (error2) {
            aggregatedError = _appendError(error2, context, key, aggregatedError);
          }
          if (obj[key] === void 0) {
            delete obj[key];
            continue;
          }
          hasKeys = true;
        } else if (op === "$rename") {
          const schematype2 = new SchemaString(`${prefix}${key}.$rename`);
          try {
            obj[key] = castUpdateVal(schematype2, val, op, key, context, prefix + key);
          } catch (error2) {
            aggregatedError = _appendError(error2, context, key, aggregatedError);
          }
          if (obj[key] === void 0) {
            delete obj[key];
            continue;
          }
          hasKeys = true;
        } else {
          const pathToCheck = prefix + key;
          const v = schema2._getPathType(pathToCheck);
          let _strict = strict2;
          if (v?.schema && _strict == null) {
            _strict = v.schema.options.strict;
          }
          if (v.pathType === "undefined") {
            if (_strict === "throw") {
              throw new StrictModeError(pathToCheck);
            } else if (_strict) {
              delete obj[key];
              continue;
            }
          }
          hasKeys |= walkUpdatePath(schema2, val, op, options2, context, filter, prefix + key) || utils2.isObject(val) && utils2.hasOwnKeys(val) === false;
        }
      } else {
        const isModifier = !isTopLevelNestedDollarPath && schematype == null && (key === "$each" || key === "$or" || key === "$and" || key === "$in");
        const checkPath = isModifier ? prefix : fullPath;
        if (isModifier) {
          schematype = schema2._getSchema(checkPath);
        }
        if (op !== "$setOnInsert" && handleImmutable2(schematype, strict2, obj, key, prefix + key, options2, context)) {
          continue;
        }
        let pathDetails = schema2._getPathType(checkPath);
        if (schematype == null) {
          const _res = getEmbeddedDiscriminatorPath2(schema2, obj, filter, checkPath, options2);
          if (_res.schematype != null) {
            schematype = _res.schematype;
            pathDetails = _res.type;
          }
        }
        let isStrict = strict2;
        if (pathDetails?.schema && strict2 == null) {
          isStrict = pathDetails.schema.options.strict;
        }
        const skip = isStrict && !schematype && !/real|nested/.test(pathDetails.pathType);
        if (skip) {
          if (isStrict === "throw" && schema2.virtuals[checkPath] == null) {
            throw new StrictModeError(prefix + key);
          } else {
            delete obj[key];
          }
        } else {
          if (op === "$rename") {
            if (obj[key] == null) {
              throw new CastError("String", obj[key], `${prefix}${key}.$rename`);
            }
            const schematype2 = new SchemaString(`${prefix}${key}.$rename`, null, null, schema2);
            obj[key] = schematype2.castForQuery(null, obj[key], context);
            continue;
          }
          try {
            if (prefix.length === 0 || key.indexOf(".") === -1) {
              obj[key] = castUpdateVal(schematype, val, op, key, context, prefix + key);
            } else if (isStrict !== false || schematype != null) {
              setDottedPath2(obj, key, castUpdateVal(schematype, val, op, key, context, prefix + key));
              delete obj[key];
            }
          } catch (error2) {
            aggregatedError = _appendError(error2, context, key, aggregatedError);
          }
          if (Array.isArray(obj[key]) && (op === "$addToSet" || op === "$push") && key !== "$each") {
            if (schematype && schematype.embeddedSchemaType && !schematype.embeddedSchemaType.$isMongooseArray && !schematype.embeddedSchemaType[schemaMixedSymbol]) {
              obj[key] = { $each: obj[key] };
            }
          }
          if (obj[key] === void 0) {
            delete obj[key];
            continue;
          }
          hasKeys = true;
        }
      }
    }
    if (aggregatedError != null) {
      throw aggregatedError;
    }
    return hasKeys;
  }
  function _appendError(error2, query2, key, aggregatedError) {
    if (typeof query2 !== "object" || !query2.options.multipleCastError) {
      throw error2;
    }
    aggregatedError = aggregatedError || new ValidationError();
    aggregatedError.addError(key, error2);
    return aggregatedError;
  }
  const numberOps = {
    $pop: 1,
    $inc: 1
  };
  const noCastOps = {
    $unset: 1
  };
  const castOps = {
    $push: 1,
    $addToSet: 1,
    $set: 1,
    $setOnInsert: 1
  };
  const overwriteOps = {
    $set: 1,
    $setOnInsert: 1
  };
  function castUpdateVal(schema2, val, op, $conditional, context, path) {
    if (!schema2) {
      if (op in numberOps) {
        try {
          return castNumber(val);
        } catch {
          throw new CastError("number", val, path);
        }
      }
      return val;
    }
    const cond = schema2.$isMongooseArray && op in castOps && (utils2.isObject(val) || Array.isArray(val));
    if (cond && !overwriteOps[op]) {
      let schemaArrayDepth = 0;
      let cur = schema2;
      while (cur.$isMongooseArray) {
        ++schemaArrayDepth;
        cur = cur.embeddedSchemaType;
      }
      let arrayDepth = 0;
      let _val = val;
      while (Array.isArray(_val)) {
        ++arrayDepth;
        _val = _val[0];
      }
      const additionalNesting = schemaArrayDepth - arrayDepth;
      while (arrayDepth < schemaArrayDepth) {
        val = [val];
        ++arrayDepth;
      }
      let tmp = schema2.applySetters(Array.isArray(val) ? val : [val], context);
      for (let i = 0; i < additionalNesting; ++i) {
        tmp = tmp[0];
      }
      return tmp;
    }
    if (op in noCastOps) {
      return val;
    }
    if (op in numberOps) {
      if (val == null) {
        throw new CastError("number", val, schema2.path);
      }
      if (op === "$inc") {
        return schema2.castForQuery(
          null,
          val,
          context
        );
      }
      try {
        return castNumber(val);
      } catch {
        throw new CastError("number", val, schema2.path);
      }
    }
    if (op === "$currentDate") {
      if (typeof val === "object") {
        return { $type: val.$type };
      }
      return Boolean(val);
    }
    if (mongodbUpdateOperators.has($conditional)) {
      return schema2.castForQuery(
        $conditional,
        val,
        context
      );
    }
    if (overwriteOps[op]) {
      const skipQueryCastForUpdate = val != null && schema2.$isMongooseArray && schema2.$fullPath != null && !schema2.$fullPath.match(/\d+$/);
      const applySetters = schema2[schemaMixedSymbol] != null;
      if (skipQueryCastForUpdate || applySetters) {
        return schema2.applySetters(val, context);
      }
      return schema2.castForQuery(
        null,
        val,
        context
      );
    }
    return schema2.castForQuery(null, val, context);
  }
  return castUpdate;
}
var decorateUpdateWithVersionKey;
var hasRequiredDecorateUpdateWithVersionKey;
function requireDecorateUpdateWithVersionKey() {
  if (hasRequiredDecorateUpdateWithVersionKey) return decorateUpdateWithVersionKey;
  hasRequiredDecorateUpdateWithVersionKey = 1;
  decorateUpdateWithVersionKey = function decorateUpdateWithVersionKey2(update, options2, versionKey) {
    if (!versionKey || !options2?.upsert) {
      return;
    }
    if (options2.overwrite) {
      if (!hasKey(update, versionKey)) {
        update[versionKey] = 0;
      }
    } else if (!hasKey(update, versionKey) && !hasKey(update?.$set, versionKey) && !hasKey(update?.$inc, versionKey) && !hasKey(update?.$setOnInsert, versionKey)) {
      if (!update.$setOnInsert) {
        update.$setOnInsert = {};
      }
      update.$setOnInsert[versionKey] = 0;
    }
  };
  function hasKey(obj, key) {
    if (obj == null || typeof obj !== "object") {
      return false;
    }
    return Object.hasOwn(obj, key);
  }
  return decorateUpdateWithVersionKey;
}
var setDefaultsOnInsert;
var hasRequiredSetDefaultsOnInsert;
function requireSetDefaultsOnInsert() {
  if (hasRequiredSetDefaultsOnInsert) return setDefaultsOnInsert;
  hasRequiredSetDefaultsOnInsert = 1;
  const get2 = requireGet();
  setDefaultsOnInsert = function(filter, schema2, castedDoc, options2, queryMongooseOptions, context) {
    const shouldSetDefaultsOnInsert = options2?.setDefaultsOnInsert ?? queryMongooseOptions?.setDefaultsOnInsert ?? schema2.base.options.setDefaultsOnInsert;
    if (!options2.upsert || shouldSetDefaultsOnInsert === false) {
      return castedDoc;
    }
    const keys = Object.keys(castedDoc || {});
    const updatedKeys = /* @__PURE__ */ Object.create(null);
    const numKeys = keys.length;
    let hasDollarUpdate = false;
    for (let i = 0; i < numKeys; ++i) {
      if (keys[i].charAt(0) === "$") {
        hasDollarUpdate = true;
        break;
      }
    }
    const paths = Object.keys(filter);
    const numPaths = paths.length;
    for (let i = 0; i < numPaths; ++i) {
      const path = paths[i];
      const condition = filter[path];
      if (condition && typeof condition === "object") {
        const conditionKeys = Object.keys(condition);
        const numConditionKeys = conditionKeys.length;
        let hasDollarKey = false;
        for (let j = 0; j < numConditionKeys; ++j) {
          if (conditionKeys[j].charAt(0) === "$") {
            hasDollarKey = true;
            break;
          }
        }
        if (hasDollarKey) {
          continue;
        }
      }
      updatedKeys[path] = true;
      if (path.indexOf(".") === -1) {
        continue;
      }
      const pieces = schema2.paths[path] ? (
        // If the SchemaType already split for us, use that to avoid the extra overhead
        schema2.paths[path].splitPath()
      ) : path.split(".");
      let cur = pieces[0];
      for (let j = 1; j < pieces.length; ++j) {
        updatedKeys[cur] = true;
        cur += "." + pieces[j];
      }
    }
    if (options2?.overwrite && !hasDollarUpdate) {
      return castedDoc;
    }
    schema2.eachPath(function(path, schemaType2) {
      if (schemaType2.path === "_id" && schemaType2.options.auto) {
        return;
      }
      const def = schemaType2.getDefault(null, true, { context });
      if (typeof def === "undefined") {
        return;
      }
      const pathPieces = schemaType2.splitPath();
      if (pathPieces.includes("$*")) {
        return;
      }
      if (isModified(castedDoc, updatedKeys, path, pathPieces, hasDollarUpdate)) {
        return;
      }
      castedDoc = castedDoc || {};
      castedDoc.$setOnInsert = castedDoc.$setOnInsert || {};
      if (get2(castedDoc, path) == null) {
        castedDoc.$setOnInsert[path] = def;
      }
    });
    return castedDoc;
  };
  function isModified(castedDoc, updatedKeys, path, pathPieces, hasDollarUpdate) {
    if (updatedKeys[path]) {
      return true;
    }
    let cur = pathPieces[0];
    for (let i = 1; i < pathPieces.length; ++i) {
      if (updatedKeys[cur]) {
        return true;
      }
      cur += "." + pathPieces[i];
    }
    if (hasDollarUpdate) {
      for (const key in castedDoc) {
        if (key.charAt(0) === "$") {
          if (pathExistsInUpdate(castedDoc[key], path, pathPieces)) {
            return true;
          }
        }
      }
    } else {
      if (pathExistsInUpdate(castedDoc, path, pathPieces)) {
        return true;
      }
    }
    return false;
  }
  function pathExistsInUpdate(update, targetPath, pathPieces) {
    if (update == null || typeof update !== "object") {
      return false;
    }
    if (Object.hasOwn(update, targetPath)) {
      return true;
    }
    let cur = pathPieces[0];
    for (let i = 1; i < pathPieces.length; ++i) {
      if (Object.hasOwn(update, cur)) {
        return true;
      }
      cur += "." + pathPieces[i];
    }
    const prefix = targetPath + ".";
    for (const key in update) {
      if (key.startsWith(prefix)) {
        return true;
      }
    }
    return false;
  }
  return setDefaultsOnInsert;
}
var hasRequiredCastBulkWrite;
function requireCastBulkWrite() {
  if (hasRequiredCastBulkWrite) return castBulkWrite.exports;
  hasRequiredCastBulkWrite = 1;
  (function(module) {
    const MongooseError = requireMongooseError();
    const getDiscriminatorByValue2 = requireGetDiscriminatorByValue();
    const applyTimestampsToChildren = requireApplyTimestampsToChildren();
    const applyTimestampsToUpdate = requireApplyTimestampsToUpdate();
    const cast2 = requireCast();
    const castUpdate2 = requireCastUpdate();
    const clone = requireClone();
    const decorateUpdateWithVersionKey2 = requireDecorateUpdateWithVersionKey();
    const { inspect } = require$$1;
    const setDefaultsOnInsert2 = requireSetDefaultsOnInsert();
    module.exports = function castBulkWrite2(originalModel, op, options2) {
      let now;
      const timestampOpts = originalModel.schema.get("timestamps");
      if (typeof timestampOpts?.currentTime === "function") {
        now = timestampOpts.currentTime();
      } else {
        now = originalModel.base.now();
      }
      if (op["insertOne"]) {
        return (callback) => module.exports.castInsertOne(originalModel, op["insertOne"], options2).then(() => callback(null), (err) => callback(err));
      } else if (op["updateOne"]) {
        return (callback) => {
          try {
            module.exports.castUpdateOne(originalModel, op["updateOne"], options2, now);
            callback(null);
          } catch (err) {
            callback(err);
          }
        };
      } else if (op["updateMany"]) {
        return (callback) => {
          try {
            module.exports.castUpdateMany(originalModel, op["updateMany"], options2, now);
            callback(null);
          } catch (err) {
            callback(err);
          }
        };
      } else if (op["replaceOne"]) {
        return (callback) => {
          module.exports.castReplaceOne(originalModel, op["replaceOne"], options2).then(() => callback(null), (err) => callback(err));
        };
      } else if (op["deleteOne"]) {
        return (callback) => {
          try {
            module.exports.castDeleteOne(originalModel, op["deleteOne"]);
            callback(null);
          } catch (err) {
            callback(err);
          }
        };
      } else if (op["deleteMany"]) {
        return (callback) => {
          try {
            module.exports.castDeleteMany(originalModel, op["deleteMany"]);
            callback(null);
          } catch (err) {
            callback(err);
          }
        };
      } else {
        return (callback) => {
          const error2 = new MongooseError(`Invalid op passed to \`bulkWrite()\`: ${inspect(op)}`);
          callback(error2, null);
        };
      }
    };
    module.exports.castInsertOne = async function castInsertOne(originalModel, insertOne, options2) {
      const model2 = decideModelByObject(originalModel, insertOne["document"]);
      const doc = new model2(insertOne["document"]);
      if (model2.schema.options.timestamps && getTimestampsOpt(insertOne, options2)) {
        doc.initializeTimestamps();
      }
      if (options2.session != null) {
        doc.$session(options2.session);
      }
      const versionKey = model2?.schema?.options?.versionKey;
      if (versionKey && doc[versionKey] == null) {
        doc[versionKey] = 0;
      }
      insertOne["document"] = doc;
      if (options2.skipValidation || insertOne.skipValidation) {
        return insertOne;
      }
      await insertOne["document"].$validate();
      return insertOne;
    };
    module.exports.castUpdateOne = function castUpdateOne(originalModel, updateOne, options2, now) {
      if (!updateOne["filter"]) {
        throw new Error("Must provide a filter object.");
      }
      if (!updateOne["update"]) {
        throw new Error("Must provide an update object.");
      }
      const model2 = decideModelByObject(originalModel, updateOne["filter"]);
      const schema2 = model2.schema;
      const strict2 = options2.strict ?? model2.schema.options.strict;
      const update = clone(updateOne["update"]);
      _addDiscriminatorToObject(schema2, updateOne["filter"]);
      const doInitTimestamps = getTimestampsOpt(updateOne, options2);
      if (model2.schema.$timestamps != null && doInitTimestamps) {
        const createdAt = model2.schema.$timestamps.createdAt;
        const updatedAt = model2.schema.$timestamps.updatedAt;
        applyTimestampsToUpdate(now, createdAt, updatedAt, update, {
          timestamps: updateOne.timestamps,
          overwriteImmutable: updateOne.overwriteImmutable
        }, updateOne.upsert);
      }
      if (doInitTimestamps) {
        applyTimestampsToChildren(now, update, model2.schema);
      }
      const globalSetDefaultsOnInsert = originalModel.base.options.setDefaultsOnInsert;
      const shouldSetDefaultsOnInsert = updateOne.setDefaultsOnInsert ?? globalSetDefaultsOnInsert;
      if (shouldSetDefaultsOnInsert !== false) {
        setDefaultsOnInsert2(updateOne["filter"], model2.schema, update, {
          setDefaultsOnInsert: true,
          upsert: updateOne.upsert
        });
      }
      decorateUpdateWithVersionKey2(
        update,
        updateOne,
        model2.schema.options.versionKey
      );
      updateOne["filter"] = cast2(model2.schema, updateOne["filter"], {
        strict: strict2,
        upsert: updateOne.upsert
      });
      updateOne["update"] = castUpdate2(model2.schema, update, {
        strict: strict2,
        upsert: updateOne.upsert,
        arrayFilters: updateOne.arrayFilters,
        overwriteDiscriminatorKey: updateOne.overwriteDiscriminatorKey,
        overwriteImmutable: updateOne.overwriteImmutable
      }, model2, updateOne["filter"]);
      return updateOne;
    };
    module.exports.castUpdateMany = function castUpdateMany(originalModel, updateMany, options2, now) {
      if (!updateMany["filter"]) {
        throw new Error("Must provide a filter object.");
      }
      if (!updateMany["update"]) {
        throw new Error("Must provide an update object.");
      }
      const model2 = decideModelByObject(originalModel, updateMany["filter"]);
      const schema2 = model2.schema;
      const strict2 = options2.strict ?? model2.schema.options.strict;
      const globalSetDefaultsOnInsert = originalModel.base.options.setDefaultsOnInsert;
      const shouldSetDefaultsOnInsert = updateMany.setDefaultsOnInsert ?? globalSetDefaultsOnInsert;
      if (shouldSetDefaultsOnInsert !== false) {
        setDefaultsOnInsert2(updateMany["filter"], model2.schema, updateMany["update"], {
          setDefaultsOnInsert: true,
          upsert: updateMany.upsert
        });
      }
      const doInitTimestamps = getTimestampsOpt(updateMany, options2);
      if (model2.schema.$timestamps != null && doInitTimestamps) {
        const createdAt = model2.schema.$timestamps.createdAt;
        const updatedAt = model2.schema.$timestamps.updatedAt;
        applyTimestampsToUpdate(now, createdAt, updatedAt, updateMany["update"], {
          timestamps: updateMany.timestamps,
          overwriteImmutable: updateMany.overwriteImmutable
        }, updateMany.upsert);
      }
      if (doInitTimestamps) {
        applyTimestampsToChildren(now, updateMany["update"], model2.schema);
      }
      _addDiscriminatorToObject(schema2, updateMany["filter"]);
      decorateUpdateWithVersionKey2(
        updateMany["update"],
        updateMany,
        model2.schema.options.versionKey
      );
      updateMany["filter"] = cast2(model2.schema, updateMany["filter"], {
        strict: strict2,
        upsert: updateMany.upsert
      });
      updateMany["update"] = castUpdate2(model2.schema, updateMany["update"], {
        strict: strict2,
        upsert: updateMany.upsert,
        arrayFilters: updateMany.arrayFilters,
        overwriteDiscriminatorKey: updateMany.overwriteDiscriminatorKey,
        overwriteImmutable: updateMany.overwriteImmutable
      }, model2, updateMany["filter"]);
    };
    module.exports.castReplaceOne = async function castReplaceOne(originalModel, replaceOne, options2) {
      const model2 = decideModelByObject(originalModel, replaceOne["filter"]);
      const schema2 = model2.schema;
      const strict2 = options2.strict ?? model2.schema.options.strict;
      _addDiscriminatorToObject(schema2, replaceOne["filter"]);
      replaceOne["filter"] = cast2(model2.schema, replaceOne["filter"], {
        strict: strict2,
        upsert: replaceOne.upsert
      });
      const doc = new model2(replaceOne["replacement"], strict2, { skipId: true });
      if (model2.schema.options.timestamps && getTimestampsOpt(replaceOne, options2)) {
        doc.initializeTimestamps();
      }
      if (options2.session != null) {
        doc.$session(options2.session);
      }
      const versionKey = model2?.schema?.options?.versionKey;
      if (versionKey && doc[versionKey] == null) {
        doc[versionKey] = 0;
      }
      replaceOne["replacement"] = doc;
      if (options2.skipValidation || replaceOne.skipValidation) {
        replaceOne["replacement"] = replaceOne["replacement"].toBSON();
        return;
      }
      await replaceOne["replacement"].$validate();
      replaceOne["replacement"] = replaceOne["replacement"].toBSON();
    };
    module.exports.castDeleteOne = function castDeleteOne(originalModel, deleteOne) {
      const model2 = decideModelByObject(originalModel, deleteOne["filter"]);
      const schema2 = model2.schema;
      _addDiscriminatorToObject(schema2, deleteOne["filter"]);
      deleteOne["filter"] = cast2(model2.schema, deleteOne["filter"]);
    };
    module.exports.castDeleteMany = function castDeleteMany(originalModel, deleteMany) {
      const model2 = decideModelByObject(originalModel, deleteMany["filter"]);
      const schema2 = model2.schema;
      _addDiscriminatorToObject(schema2, deleteMany["filter"]);
      deleteMany["filter"] = cast2(model2.schema, deleteMany["filter"]);
    };
    module.exports.cast = {
      insertOne: module.exports.castInsertOne,
      updateOne: module.exports.castUpdateOne,
      updateMany: module.exports.castUpdateMany,
      replaceOne: module.exports.castReplaceOne,
      deleteOne: module.exports.castDeleteOne,
      deleteMany: module.exports.castDeleteMany
    };
    function _addDiscriminatorToObject(schema2, obj) {
      if (schema2 == null) {
        return;
      }
      if (schema2.discriminatorMapping && !schema2.discriminatorMapping.isRoot) {
        obj[schema2.discriminatorMapping.key] = schema2.discriminatorMapping.value;
      }
    }
    function decideModelByObject(model2, object) {
      const discriminatorKey = model2.schema.options.discriminatorKey;
      if (object != null && Object.hasOwn(object, discriminatorKey)) {
        model2 = getDiscriminatorByValue2(model2.discriminators, object[discriminatorKey]) || model2;
      }
      return model2;
    }
    function getTimestampsOpt(opCommand, options2) {
      const opLevelOpt = opCommand.timestamps;
      const bulkLevelOpt = options2.timestamps;
      if (opLevelOpt != null) {
        return opLevelOpt;
      } else if (bulkLevelOpt != null) {
        return bulkLevelOpt;
      }
      return true;
    }
  })(castBulkWrite);
  return castBulkWrite.exports;
}
var decorateBulkWriteResult;
var hasRequiredDecorateBulkWriteResult;
function requireDecorateBulkWriteResult() {
  if (hasRequiredDecorateBulkWriteResult) return decorateBulkWriteResult;
  hasRequiredDecorateBulkWriteResult = 1;
  decorateBulkWriteResult = function decorateBulkWriteResult2(resultOrError, validationErrors, results) {
    resultOrError.mongoose = resultOrError.mongoose || {};
    resultOrError.mongoose.validationErrors = validationErrors;
    resultOrError.mongoose.results = results;
    return resultOrError;
  };
  return decorateBulkWriteResult;
}
var connection$1;
var hasRequiredConnection$1;
function requireConnection$1() {
  if (hasRequiredConnection$1) return connection$1;
  hasRequiredConnection$1 = 1;
  const ChangeStream = requireChangeStream();
  const EventEmitter = require$$0.EventEmitter;
  const Schema = requireSchema();
  const STATES = requireConnectionState();
  const MongooseBulkWriteError = requireBulkWriteError();
  const MongooseError = requireError();
  const ServerSelectionError = requireServerSelection();
  const SyncIndexesError = requireSyncIndexes();
  const applyPlugins2 = requireApplyPlugins();
  const clone = requireClone();
  const driver2 = requireDriver();
  const get2 = requireGet();
  const getDefaultBulkwriteResult = requireGetDefaultBulkwriteResult();
  const immediate2 = requireImmediate();
  const utils2 = requireUtils();
  const CreateCollectionsError = requireCreateCollectionsError();
  const castBulkWrite2 = requireCastBulkWrite();
  const { modelSymbol } = requireSymbols$1();
  const isPromise = requireIsPromise();
  const decorateBulkWriteResult2 = requireDecorateBulkWriteResult();
  const arrayAtomicsSymbol = requireSymbols$1().arrayAtomicsSymbol;
  const sessionNewDocuments = requireSymbols$1().sessionNewDocuments;
  const Date2 = globalThis.Date;
  const noPasswordAuthMechanisms = [
    "MONGODB-X509"
  ];
  function Connection(base) {
    this.base = base;
    this.collections = {};
    this.models = {};
    this.config = {};
    this.replica = false;
    this.options = null;
    this.otherDbs = [];
    this.relatedDbs = {};
    this.states = STATES;
    this._readyState = STATES.disconnected;
    this._closeCalled = false;
    this._hasOpened = false;
    this.plugins = [];
    if (typeof base === "undefined" || !base.connections.length) {
      this.id = 0;
    } else {
      this.id = base.nextConnectionId;
    }
    this._queue = [];
  }
  Object.setPrototypeOf(Connection.prototype, EventEmitter.prototype);
  Object.defineProperty(Connection.prototype, "readyState", {
    get: function() {
      if (this._readyState === STATES.connected && this._lastHeartbeatAt != null && // LoadBalanced topology (behind haproxy, including Atlas serverless instances) don't use heartbeats,
      // so we can't use this check in that case.
      this.client?.topology?.s?.description?.type !== "LoadBalanced" && typeof this.client?.topology?.s?.description?.heartbeatFrequencyMS === "number" && Date2.now() - this._lastHeartbeatAt >= this.client.topology.s.description.heartbeatFrequencyMS * 2) {
        return STATES.disconnected;
      }
      return this._readyState;
    },
    set: function(val) {
      if (!(val in STATES)) {
        throw new MongooseError("Invalid connection state: " + val);
      }
      if (this._readyState !== val) {
        this._readyState = val;
        for (const db of this.otherDbs) {
          db.readyState = val;
        }
        if (STATES.connected === val) {
          this._hasOpened = true;
        }
        this.emit(STATES[val]);
      }
    }
  });
  Connection.prototype.get = function getOption(key) {
    if (Object.hasOwn(this.config, key)) {
      return this.config[key];
    }
    return get2(this.options, key);
  };
  Connection.prototype.set = function setOption(key, val) {
    if (Object.hasOwn(this.config, key)) {
      this.config[key] = val;
      return val;
    }
    this.options = this.options || {};
    this.options[key] = val;
    return val;
  };
  Connection.prototype.collections;
  Connection.prototype.name;
  Connection.prototype.models;
  Connection.prototype.id;
  Object.defineProperty(Connection.prototype, "plugins", {
    configurable: false,
    enumerable: true,
    writable: true
  });
  Object.defineProperty(Connection.prototype, "host", {
    configurable: true,
    enumerable: true,
    writable: true
  });
  Object.defineProperty(Connection.prototype, "port", {
    configurable: true,
    enumerable: true,
    writable: true
  });
  Object.defineProperty(Connection.prototype, "user", {
    configurable: true,
    enumerable: true,
    writable: true
  });
  Object.defineProperty(Connection.prototype, "pass", {
    configurable: true,
    enumerable: true,
    writable: true
  });
  Connection.prototype.db;
  Connection.prototype.client;
  Connection.prototype.config;
  Connection.prototype.createCollection = async function createCollection(collection2, options2) {
    if (typeof options2 === "function" || arguments.length >= 3 && typeof arguments[2] === "function") {
      throw new MongooseError("Connection.prototype.createCollection() no longer accepts a callback");
    }
    await this._waitForConnect();
    return this.db.createCollection(collection2, options2);
  };
  Connection.prototype.bulkWrite = async function bulkWrite(ops, options2) {
    await this._waitForConnect();
    options2 = options2 || {};
    const ordered = options2.ordered == null ? true : options2.ordered;
    const asyncLocalStorage = this.base.transactionAsyncLocalStorage?.getStore();
    if ((!options2 || !Object.hasOwn(options2, "session")) && asyncLocalStorage?.session != null) {
      options2 = { ...options2, session: asyncLocalStorage.session };
    }
    const now = this.base.now();
    let res = null;
    if (ordered) {
      const opsToSend = [];
      for (const op of ops) {
        if (typeof op.model !== "string" && !op.model?.[modelSymbol]) {
          throw new MongooseError("Must specify model in Connection.prototype.bulkWrite() operations");
        }
        const Model = op.model[modelSymbol] ? op.model : this.model(op.model);
        if (op.name == null) {
          throw new MongooseError("Must specify operation name in Connection.prototype.bulkWrite()");
        }
        if (!Object.hasOwn(castBulkWrite2.cast, op.name)) {
          throw new MongooseError(`Unrecognized bulkWrite() operation name ${op.name}`);
        }
        await castBulkWrite2.cast[op.name](Model, op, options2, now);
        opsToSend.push({ ...op, namespace: Model.namespace() });
      }
      res = await this.client.bulkWrite(opsToSend, options2);
    } else {
      const validOps = [];
      const validOpIndexes = [];
      let validationErrors = [];
      const asyncValidations = [];
      const results = [];
      for (let i = 0; i < ops.length; ++i) {
        const op = ops[i];
        if (typeof op.model !== "string" && !op.model?.[modelSymbol]) {
          const error3 = new MongooseError("Must specify model in Connection.prototype.bulkWrite() operations");
          validationErrors.push({ index: i, error: error3 });
          results[i] = error3;
          continue;
        }
        let Model;
        try {
          Model = op.model[modelSymbol] ? op.model : this.model(op.model);
        } catch (error3) {
          validationErrors.push({ index: i, error: error3 });
          continue;
        }
        if (op.name == null) {
          const error3 = new MongooseError("Must specify operation name in Connection.prototype.bulkWrite()");
          validationErrors.push({ index: i, error: error3 });
          results[i] = error3;
          continue;
        }
        if (!Object.hasOwn(castBulkWrite2.cast, op.name)) {
          const error3 = new MongooseError(`Unrecognized bulkWrite() operation name ${op.name}`);
          validationErrors.push({ index: i, error: error3 });
          results[i] = error3;
          continue;
        }
        let maybePromise = null;
        try {
          maybePromise = castBulkWrite2.cast[op.name](Model, op, options2, now);
        } catch (error3) {
          validationErrors.push({ index: i, error: error3 });
          results[i] = error3;
          continue;
        }
        if (isPromise(maybePromise)) {
          asyncValidations.push(
            maybePromise.then(
              () => {
                validOps.push({ ...op, namespace: Model.namespace() });
                validOpIndexes.push(i);
              },
              (error3) => {
                validationErrors.push({ index: i, error: error3 });
                results[i] = error3;
              }
            )
          );
        } else {
          validOps.push({ ...op, namespace: Model.namespace() });
          validOpIndexes.push(i);
        }
      }
      if (asyncValidations.length > 0) {
        await Promise.all(asyncValidations);
      }
      validationErrors = validationErrors.sort((v1, v2) => v1.index - v2.index).map((v) => v.error);
      if (validOps.length === 0) {
        if (options2.throwOnValidationError && validationErrors.length) {
          throw new MongooseBulkWriteError(
            validationErrors,
            results,
            res2,
            "bulkWrite"
          );
        }
        const BulkWriteResult = this.base.driver.get().BulkWriteResult;
        const res2 = new BulkWriteResult(getDefaultBulkwriteResult(), false);
        return decorateBulkWriteResult2(res2, validationErrors, results);
      }
      let error2;
      [res, error2] = await this.client.bulkWrite(validOps, options2).then((res2) => [res2, null]).catch((err) => [null, err]);
      for (let i = 0; i < validOpIndexes.length; ++i) {
        results[validOpIndexes[i]] = null;
      }
      if (error2) {
        if (validationErrors.length > 0) {
          decorateBulkWriteResult2(error2, validationErrors, results);
          error2.mongoose = error2.mongoose || {};
          error2.mongoose.validationErrors = validationErrors;
        }
      }
      if (validationErrors.length > 0) {
        if (options2.throwOnValidationError) {
          throw new MongooseBulkWriteError(
            validationErrors,
            results,
            res,
            "bulkWrite"
          );
        } else {
          decorateBulkWriteResult2(res, validationErrors, results);
        }
      }
    }
    return res;
  };
  Connection.prototype.createCollections = async function createCollections(options2 = {}) {
    const result = {};
    const errorsMap = {};
    const { continueOnError } = options2;
    delete options2.continueOnError;
    for (const model2 of Object.values(this.models)) {
      try {
        result[model2.modelName] = await model2.createCollection({});
      } catch (err) {
        if (!continueOnError) {
          errorsMap[model2.modelName] = err;
          break;
        } else {
          result[model2.modelName] = err;
        }
      }
    }
    if (!continueOnError && utils2.hasOwnKeys(errorsMap)) {
      const message = Object.entries(errorsMap).map(([modelName, err]) => `${modelName}: ${err.message}`).join(", ");
      const createCollectionsError2 = new CreateCollectionsError(message, errorsMap);
      throw createCollectionsError2;
    }
    return result;
  };
  Connection.prototype.withSession = async function withSession(executor) {
    if (arguments.length === 0) {
      throw new MongooseError("Please provide an executor function");
    }
    return await this.client.withSession(executor);
  };
  Connection.prototype.startSession = async function startSession(options2) {
    if (arguments.length >= 2 && typeof arguments[1] === "function") {
      throw new MongooseError("Connection.prototype.startSession() no longer accepts a callback");
    }
    await this._waitForConnect();
    const session = this.client.startSession(options2);
    return session;
  };
  Connection.prototype.transaction = function transaction(fn, options2) {
    return this.startSession().then((session) => {
      session[sessionNewDocuments] = /* @__PURE__ */ new Map();
      return session.withTransaction(() => _wrapUserTransaction(fn, session, this.base), options2).then((res) => {
        delete session[sessionNewDocuments];
        return res;
      }).catch((err) => {
        delete session[sessionNewDocuments];
        throw err;
      }).finally(() => {
        session.endSession().catch(() => {
        });
      });
    });
  };
  async function _wrapUserTransaction(fn, session, mongoose2) {
    try {
      const res = mongoose2.transactionAsyncLocalStorage == null ? await fn(session) : await new Promise((resolve) => {
        mongoose2.transactionAsyncLocalStorage.run(
          { session },
          () => resolve(fn(session))
        );
      });
      return res;
    } catch (err) {
      _resetSessionDocuments(session);
      throw err;
    }
  }
  function _resetSessionDocuments(session) {
    for (const doc of session[sessionNewDocuments].keys()) {
      const state = session[sessionNewDocuments].get(doc);
      if (Object.hasOwn(state, "isNew")) {
        doc.$isNew = state.isNew;
      }
      if (Object.hasOwn(state, "versionKey")) {
        doc.set(doc.schema.options.versionKey, state.versionKey);
      }
      if (Object.hasOwn(state, "isDeleted")) {
        doc.$isDeleted(state.isDeleted);
      }
      for (const path of state.modifiedPaths) {
        doc.$__.activePaths.modify(path);
      }
      for (const path of state.atomics.keys()) {
        const val = doc.$__getValue(path);
        if (val == null) {
          continue;
        }
        val[arrayAtomicsSymbol] = state.atomics.get(path);
      }
    }
  }
  Connection.prototype.dropCollection = async function dropCollection(collection2) {
    if (arguments.length >= 2 && typeof arguments[1] === "function") {
      throw new MongooseError("Connection.prototype.dropCollection() no longer accepts a callback");
    }
    await this._waitForConnect();
    return this.db.dropCollection(collection2);
  };
  Connection.prototype._waitForConnect = async function _waitForConnect(noTimeout) {
    if ((this.readyState === STATES.connecting || this.readyState === STATES.disconnected) && this._shouldBufferCommands()) {
      const bufferTimeoutMS = this._getBufferTimeoutMS();
      let timeout = null;
      let timedOut = false;
      const queueElement = {};
      const waitForConnectPromise = new Promise((resolve) => {
        queueElement.fn = resolve;
        this._queue.push(queueElement);
      });
      if (noTimeout) {
        await waitForConnectPromise;
      } else {
        await Promise.race([
          waitForConnectPromise,
          new Promise((resolve) => {
            timeout = setTimeout(
              () => {
                timedOut = true;
                resolve();
              },
              bufferTimeoutMS
            );
          })
        ]);
      }
      if (timedOut) {
        const index = this._queue.indexOf(queueElement);
        if (index !== -1) {
          this._queue.splice(index, 1);
        }
        const message = "Connection operation buffering timed out after " + bufferTimeoutMS + "ms";
        throw new MongooseError(message);
      } else if (timeout != null) {
        clearTimeout(timeout);
      }
    }
  };
  Connection.prototype._getBufferTimeoutMS = function _getBufferTimeoutMS() {
    if (this.config.bufferTimeoutMS != null) {
      return this.config.bufferTimeoutMS;
    }
    if (this.base?.get("bufferTimeoutMS") != null) {
      return this.base.get("bufferTimeoutMS");
    }
    return 1e4;
  };
  Connection.prototype.listCollections = async function listCollections() {
    await this._waitForConnect();
    const cursor = this.db.listCollections();
    return await cursor.toArray();
  };
  Connection.prototype.listDatabases = async function listDatabases() {
    throw new MongooseError("listDatabases() not implemented by driver");
  };
  Connection.prototype.dropDatabase = async function dropDatabase() {
    if (arguments.length >= 1 && typeof arguments[0] === "function") {
      throw new MongooseError("Connection.prototype.dropDatabase() no longer accepts a callback");
    }
    await this._waitForConnect();
    for (const model2 of Object.values(this.models)) {
      delete model2.$init;
    }
    return this.db.dropDatabase();
  };
  Connection.prototype._shouldBufferCommands = function _shouldBufferCommands() {
    if (this.config.bufferCommands != null) {
      return this.config.bufferCommands;
    }
    if (this.base.get("bufferCommands") != null) {
      return this.base.get("bufferCommands");
    }
    return true;
  };
  Connection.prototype.error = function error2(err, callback) {
    if (callback) {
      callback(err);
      return null;
    }
    if (this.listeners("error").length > 0) {
      this.emit("error", err);
    }
    return Promise.reject(err);
  };
  Connection.prototype.onOpen = function() {
    this.readyState = STATES.connected;
    this._flushQueue();
    for (const i in this.collections) {
      if (Object.hasOwn(this.collections, i)) {
        this.collections[i].onOpen();
      }
    }
    this.emit("open");
  };
  Connection.prototype._flushQueue = function _flushQueue() {
    const queue = this._queue;
    this._queue = [];
    for (const d of queue) {
      d.fn.apply(d.ctx, d.args);
    }
  };
  Connection.prototype.openUri = async function openUri(uri, options2) {
    if (this.readyState === STATES.connecting || this.readyState === STATES.connected) {
      if (this._connectionString === uri) {
        return this;
      }
    }
    this._closeCalled = false;
    let _fireAndForget = false;
    if (options2 && "_fireAndForget" in options2) {
      _fireAndForget = options2._fireAndForget;
      delete options2._fireAndForget;
    }
    try {
      _validateArgs.apply(arguments);
    } catch (err) {
      if (_fireAndForget) {
        throw err;
      }
      this.$initialConnection = Promise.reject(err);
      throw err;
    }
    this.$initialConnection = this.createClient(uri, options2).then(() => this).catch((err) => {
      this.readyState = STATES.disconnected;
      if (this.listeners("error").length > 0) {
        immediate2(() => this.emit("error", err));
      }
      throw err;
    });
    for (const model2 of Object.values(this.models)) {
      model2.init().catch(function $modelInitNoop() {
      });
    }
    if (_fireAndForget) {
      return this;
    }
    try {
      await this.$initialConnection;
    } catch (err) {
      throw _handleConnectionErrors(err);
    }
    return this;
  };
  Connection.prototype.on = function on(event, callback) {
    if (event === "error" && this.$initialConnection) {
      this.$initialConnection.catch(() => {
      });
    }
    return EventEmitter.prototype.on.call(this, event, callback);
  };
  Connection.prototype.once = function on(event, callback) {
    if (event === "error" && this.$initialConnection) {
      this.$initialConnection.catch(() => {
      });
    }
    return EventEmitter.prototype.once.call(this, event, callback);
  };
  function _validateArgs(uri, options2, callback) {
    if (typeof options2 === "function" && callback == null) {
      throw new MongooseError("Connection.prototype.openUri() no longer accepts a callback");
    } else if (typeof callback === "function") {
      throw new MongooseError("Connection.prototype.openUri() no longer accepts a callback");
    }
  }
  function _handleConnectionErrors(err) {
    if (err?.name === "MongoServerSelectionError") {
      const originalError = err;
      err = new ServerSelectionError();
      err.assimilateError(originalError);
    }
    return err;
  }
  Connection.prototype.destroy = async function destroy(force) {
    if (typeof force === "function" || arguments.length === 2 && typeof arguments[1] === "function") {
      throw new MongooseError("Connection.prototype.destroy() no longer accepts a callback");
    }
    if (force != null && typeof force === "object") {
      this.$wasForceClosed = !!force.force;
    } else {
      this.$wasForceClosed = !!force;
    }
    return this._close(force, true);
  };
  Connection.prototype.close = async function close(force) {
    if (typeof force === "function" || arguments.length === 2 && typeof arguments[1] === "function") {
      throw new MongooseError("Connection.prototype.close() no longer accepts a callback");
    }
    if (force != null && typeof force === "object") {
      this.$wasForceClosed = !!force.force;
    } else {
      this.$wasForceClosed = !!force;
    }
    if (this._lastHeartbeatAt != null) {
      this._lastHeartbeatAt = null;
    }
    for (const model2 of Object.values(this.models)) {
      delete model2.$init;
    }
    return this._close(force, false);
  };
  Connection.prototype._close = async function _close(force, destroy) {
    const _this = this;
    const closeCalled = this._closeCalled;
    this._closeCalled = true;
    this._destroyCalled = destroy;
    if (this.client != null) {
      this.client._closeCalled = true;
      this.client._destroyCalled = destroy;
    }
    const conn = this;
    switch (this.readyState) {
      case STATES.disconnected:
        if (destroy) {
          const index = this.base.connections.indexOf(conn);
          if (index !== -1) {
            this.base.connections.splice(index, 1);
          }
        }
        if (!closeCalled) {
          await this.doClose(force);
          this.onClose(force);
        }
        break;
      case STATES.connected:
        this.readyState = STATES.disconnecting;
        await this.doClose(force);
        if (destroy) {
          const index = _this.base.connections.indexOf(conn);
          if (index !== -1) {
            this.base.connections.splice(index, 1);
          }
        }
        this.onClose(force);
        break;
      case STATES.connecting:
        return new Promise((resolve, reject) => {
          const _rerunClose = () => {
            this.removeListener("open", _rerunClose);
            this.removeListener("error", _rerunClose);
            if (destroy) {
              this.destroy(force).then(resolve, reject);
            } else {
              this.close(force).then(resolve, reject);
            }
          };
          this.once("open", _rerunClose);
          this.once("error", _rerunClose);
        });
      case STATES.disconnecting:
        return new Promise((resolve) => {
          this.once("close", () => {
            if (destroy && this.base.connections.indexOf(conn) !== -1) {
              this.base.connections.splice(this.base.connections.indexOf(conn), 1);
            }
            resolve();
          });
        });
    }
    return this;
  };
  Connection.prototype.doClose = function doClose() {
    throw new MongooseError("Connection#doClose unimplemented by driver");
  };
  Connection.prototype.onClose = function onClose(force) {
    this.readyState = STATES.disconnected;
    for (const i in this.collections) {
      if (Object.hasOwn(this.collections, i)) {
        this.collections[i].onClose(force);
      }
    }
    this.emit("close", force);
    const wasForceClosed = typeof force === "object" && force !== null ? force.force : force;
    for (const db of this.otherDbs) {
      this._destroyCalled ? db.destroy({ force: wasForceClosed, skipCloseClient: true }) : db.close({ force: wasForceClosed, skipCloseClient: true });
    }
  };
  Connection.prototype.collection = function(name, options2) {
    const defaultOptions = {
      autoIndex: this.config.autoIndex ?? this.base.options.autoIndex,
      autoCreate: this.config.autoCreate ?? this.base.options.autoCreate,
      autoSearchIndex: this.config.autoSearchIndex ?? this.base.options.autoSearchIndex
    };
    options2 = Object.assign({}, defaultOptions, options2 ? clone(options2) : {});
    options2.$wasForceClosed = this.$wasForceClosed;
    const Collection = this.base?.__driver?.Collection || driver2.get().Collection;
    if (!(name in this.collections)) {
      this.collections[name] = new Collection(name, this, options2);
    }
    return this.collections[name];
  };
  Connection.prototype.plugin = function(fn, opts) {
    this.plugins.push([fn, opts]);
    return this;
  };
  Connection.prototype.model = function model2(name, schema2, collection2, options2) {
    if (!(this instanceof Connection)) {
      throw new MongooseError("`connection.model()` should not be run with `new`. If you are doing `new db.model(foo)(bar)`, use `db.model(foo)(bar)` instead");
    }
    let fn;
    if (typeof name === "function") {
      fn = name;
      name = fn.name;
    }
    if (typeof schema2 === "string") {
      collection2 = schema2;
      schema2 = false;
    }
    if (utils2.isObject(schema2)) {
      if (!schema2.instanceOfSchema) {
        schema2 = new Schema(schema2);
      } else if (!(schema2 instanceof this.base.Schema)) {
        schema2 = schema2._clone(this.base.Schema);
      }
    }
    if (schema2 && !schema2.instanceOfSchema) {
      throw new MongooseError("The 2nd parameter to `mongoose.model()` should be a schema or a POJO");
    }
    const defaultOptions = { cache: false, overwriteModels: this.base.options.overwriteModels };
    const opts = Object.assign(defaultOptions, options2, { connection: this });
    if (this.models[name] && !collection2 && opts.overwriteModels !== true) {
      if (schema2?.instanceOfSchema && schema2 !== this.models[name].schema) {
        throw new MongooseError.OverwriteModelError(name);
      }
      return this.models[name];
    }
    let model3;
    if (schema2?.instanceOfSchema) {
      applyPlugins2(schema2, this.plugins, null, "$connectionPluginsApplied");
      model3 = this.base._model(fn || name, schema2, collection2, opts);
      if (!this.models[name]) {
        this.models[name] = model3;
      }
      model3.init().catch(function $modelInitNoop() {
      });
      return model3;
    }
    if (this.models[name] && collection2) {
      model3 = this.models[name];
      schema2 = model3.prototype.schema;
      const sub = model3.__subclass(this, schema2, collection2);
      return sub;
    }
    if (arguments.length === 1) {
      model3 = this.models[name];
      if (!model3) {
        throw new MongooseError.MissingSchemaError(name);
      }
      return model3;
    }
    if (!model3) {
      throw new MongooseError.MissingSchemaError(name);
    }
    if (this === model3.prototype.db && (!collection2 || collection2 === model3.collection.name)) {
      if (!this.models[name]) {
        this.models[name] = model3;
      }
      return model3;
    }
    this.models[name] = model3.__subclass(this, schema2, collection2);
    return this.models[name];
  };
  Connection.prototype.deleteModel = function deleteModel(name) {
    if (typeof name === "string") {
      const model2 = this.model(name);
      if (model2 == null) {
        return this;
      }
      const collectionName = model2.collection.name;
      delete this.models[name];
      delete this.collections[collectionName];
      this.emit("deleteModel", model2);
    } else if (name instanceof RegExp) {
      const pattern = name;
      const names = this.modelNames();
      for (const name2 of names) {
        if (pattern.test(name2)) {
          this.deleteModel(name2);
        }
      }
    } else {
      throw new MongooseError('First parameter to `deleteModel()` must be a string or regexp, got "' + name + '"');
    }
    return this;
  };
  Connection.prototype.watch = function watch(pipeline, options2) {
    const changeStreamPromise = this._waitForConnect().then(() => this.db.watch(pipeline, options2));
    return new ChangeStream(changeStreamPromise, pipeline, options2);
  };
  Connection.prototype.asPromise = async function asPromise() {
    try {
      await this.$initialConnection;
      return this;
    } catch (err) {
      throw _handleConnectionErrors(err);
    }
  };
  Connection.prototype.modelNames = function modelNames() {
    return Object.keys(this.models);
  };
  Connection.prototype.shouldAuthenticate = function shouldAuthenticate() {
    return this.user != null && (this.pass != null || this.authMechanismDoesNotRequirePassword());
  };
  Connection.prototype.authMechanismDoesNotRequirePassword = function authMechanismDoesNotRequirePassword() {
    if (this.options?.auth) {
      return noPasswordAuthMechanisms.indexOf(this.options.auth.authMechanism) >= 0;
    }
    return true;
  };
  Connection.prototype.optionsProvideAuthenticationData = function optionsProvideAuthenticationData(options2) {
    return options2 && options2.user && (options2.pass || this.authMechanismDoesNotRequirePassword());
  };
  Connection.prototype.getClient = function getClient() {
    return this.client;
  };
  Connection.prototype.setClient = function setClient() {
    throw new MongooseError("Connection#setClient not implemented by driver");
  };
  Connection.prototype.createClient = function createClient() {
    throw new MongooseError("Connection#createClient not implemented by driver");
  };
  Connection.prototype.syncIndexes = async function syncIndexes2(options2 = {}) {
    const result = {};
    const errorsMap = {};
    const { continueOnError } = options2;
    delete options2.continueOnError;
    for (const model2 of Object.values(this.models)) {
      try {
        result[model2.modelName] = await model2.syncIndexes(options2);
      } catch (err) {
        if (!continueOnError) {
          errorsMap[model2.modelName] = err;
          break;
        } else {
          result[model2.modelName] = err;
        }
      }
    }
    if (!continueOnError && utils2.hasOwnKeys(errorsMap)) {
      const message = Object.entries(errorsMap).map(([modelName, err]) => `${modelName}: ${err.message}`).join(", ");
      const syncIndexesError = new SyncIndexesError(message, errorsMap);
      throw syncIndexesError;
    }
    return result;
  };
  Connection.STATES = STATES;
  connection$1 = Connection;
  return connection$1;
}
const version = "9.9.2";
const require$$17 = {
  version
};
var processConnectionOptions_1;
var hasRequiredProcessConnectionOptions;
function requireProcessConnectionOptions() {
  if (hasRequiredProcessConnectionOptions) return processConnectionOptions_1;
  hasRequiredProcessConnectionOptions = 1;
  const clone = requireClone();
  const MongooseError = requireError();
  function processConnectionOptions(uri, options2) {
    const opts = options2 ? options2 : {};
    const readPreference = opts.readPreference ? opts.readPreference : getUriReadPreference(uri);
    const clonedOpts = clone(opts);
    const resolvedOpts = readPreference && readPreference !== "primary" && readPreference !== "primaryPreferred" ? resolveOptsConflicts(readPreference, clonedOpts) : clonedOpts;
    return resolvedOpts;
  }
  function resolveOptsConflicts(pref, opts) {
    if (setsIndexOptions(opts) && setsSecondaryRead(pref)) {
      throwReadPreferenceError();
    } else {
      return defaultIndexOptsToFalse(opts);
    }
  }
  function setsIndexOptions(opts) {
    const configIdx = opts.config?.autoIndex;
    const { autoCreate, autoIndex } = opts;
    return !!(configIdx || autoCreate || autoIndex);
  }
  function setsSecondaryRead(prefString) {
    return !!(prefString === "secondary" || prefString === "secondaryPreferred");
  }
  function getUriReadPreference(connectionString) {
    const exp = /(?:&|\?)readPreference=(\w+)(?:&|$)/;
    const match = exp.exec(connectionString);
    return match ? match[1] : null;
  }
  function defaultIndexOptsToFalse(opts) {
    opts.config = { autoIndex: false };
    opts.autoCreate = false;
    opts.autoIndex = false;
    return opts;
  }
  function throwReadPreferenceError() {
    throw new MongooseError(
      'MongoDB prohibits index creation on connections that read from non-primary replicas.  Connections that set "readPreference" to "secondary" or "secondaryPreferred" may not opt-in to the following connection options: autoCreate, autoIndex'
    );
  }
  processConnectionOptions_1 = processConnectionOptions;
  return processConnectionOptions_1;
}
var timers = {};
var hasRequiredTimers;
function requireTimers() {
  if (hasRequiredTimers) return timers;
  hasRequiredTimers = 1;
  timers.setTimeout = setTimeout;
  return timers;
}
var connection;
var hasRequiredConnection;
function requireConnection() {
  if (hasRequiredConnection) return connection;
  hasRequiredConnection = 1;
  const MongooseConnection = requireConnection$1();
  const MongooseError = requireError();
  const STATES = requireConnectionState();
  const mongodb = requireLib$1();
  const pkg = require$$17;
  const processConnectionOptions = requireProcessConnectionOptions();
  const setTimeout2 = requireTimers().setTimeout;
  const utils2 = requireUtils();
  const Schema = requireSchema();
  const Date2 = globalThis.Date;
  function NativeConnection() {
    MongooseConnection.apply(this, arguments);
    this._listening = false;
    this._lastHeartbeatAt = null;
  }
  NativeConnection.STATES = STATES;
  Object.setPrototypeOf(NativeConnection.prototype, MongooseConnection.prototype);
  NativeConnection.prototype.useDb = function(name, options2) {
    options2 = options2 || {};
    if (options2.useCache && this.relatedDbs[name]) {
      return this.relatedDbs[name];
    }
    const newConn = new this.constructor();
    newConn.name = name;
    newConn.base = this.base;
    newConn.collections = {};
    newConn.models = {};
    newConn.replica = this.replica;
    newConn.config = Object.assign({}, this.config, newConn.config);
    newConn.name = this.name;
    newConn.options = this.options;
    newConn._readyState = this._readyState;
    newConn._closeCalled = this._closeCalled;
    newConn._hasOpened = this._hasOpened;
    newConn._listening = false;
    newConn._parent = this;
    newConn.host = this.host;
    newConn.port = this.port;
    newConn.user = this.user;
    newConn.pass = this.pass;
    const _this = this;
    newConn.client = _this.client;
    if (this.db && this._readyState === STATES.connected) {
      wireup();
    } else {
      this._queue.push({ fn: wireup });
    }
    function wireup() {
      newConn.client = _this.client;
      newConn.db = _this.client.db(name);
      newConn._lastHeartbeatAt = _this._lastHeartbeatAt;
      newConn.onOpen();
    }
    newConn.name = name;
    this.otherDbs.push(newConn);
    newConn.otherDbs.push(this);
    if (options2?.useCache) {
      this.relatedDbs[newConn.name] = newConn;
      newConn.relatedDbs = this.relatedDbs;
    }
    return newConn;
  };
  NativeConnection.prototype.aggregate = function aggregate2(pipeline, options2) {
    return new this.base.Aggregate(null, this).append(pipeline).option(options2 ?? {});
  };
  NativeConnection.prototype.removeDb = function removeDb(name) {
    const dbs = this.otherDbs.filter((db) => db.name === name);
    if (!dbs.length) {
      throw new MongooseError(`No connections to database "${name}" found`);
    }
    for (const db of dbs) {
      db._closeCalled = true;
      db._destroyCalled = true;
      db._readyState = STATES.disconnected;
      db.$wasForceClosed = true;
    }
    delete this.relatedDbs[name];
    this.otherDbs = this.otherDbs.filter((db) => db.name !== name);
  };
  NativeConnection.prototype.doClose = async function doClose(force) {
    if (this.client == null) {
      return this;
    }
    let skipCloseClient = false;
    if (force != null && typeof force === "object") {
      skipCloseClient = force.skipCloseClient;
      force = force.force;
    }
    if (skipCloseClient) {
      return this;
    }
    await this.client.close(force);
    await new Promise((resolve) => setTimeout2(resolve, 1));
    return this;
  };
  NativeConnection.prototype.listDatabases = async function listDatabases() {
    await this._waitForConnect();
    return await this.db.admin().listDatabases();
  };
  NativeConnection.prototype.createClient = async function createClient(uri, options2) {
    if (typeof uri !== "string") {
      throw new MongooseError(`The \`uri\` parameter to \`openUri()\` must be a string, got "${typeof uri}". Make sure the first parameter to \`mongoose.connect()\` or \`mongoose.createConnection()\` is a string.`);
    }
    if (this._destroyCalled) {
      throw new MongooseError(
        "Connection has been closed and destroyed, and cannot be used for re-opening the connection. Please create a new connection with `mongoose.createConnection()` or `mongoose.connect()`."
      );
    }
    if (this.readyState === STATES.connecting || this.readyState === STATES.connected) {
      if (this._connectionString !== uri) {
        throw new MongooseError("Can't call `openUri()` on an active connection with different connection strings. Make sure you aren't calling `mongoose.connect()` multiple times. See: https://mongoosejs.com/docs/connections.html#multiple_connections");
      }
    }
    options2 = processConnectionOptions(uri, options2);
    if (options2) {
      const autoIndex = options2.config?.autoIndex ?? options2.autoIndex;
      if (autoIndex != null) {
        this.config.autoIndex = autoIndex !== false;
        delete options2.config;
        delete options2.autoIndex;
      }
      if ("autoCreate" in options2) {
        this.config.autoCreate = !!options2.autoCreate;
        delete options2.autoCreate;
      }
      if ("sanitizeFilter" in options2) {
        this.config.sanitizeFilter = options2.sanitizeFilter;
        delete options2.sanitizeFilter;
      }
      if ("autoSearchIndex" in options2) {
        this.config.autoSearchIndex = options2.autoSearchIndex;
        delete options2.autoSearchIndex;
      }
      if ("bufferTimeoutMS" in options2) {
        this.config.bufferTimeoutMS = options2.bufferTimeoutMS;
        delete options2.bufferTimeoutMS;
      }
      if (options2.user || options2.pass) {
        options2.auth = options2.auth || {};
        options2.auth.username = options2.user;
        options2.auth.password = options2.pass;
        this.user = options2.user;
        this.pass = options2.pass;
      }
      delete options2.user;
      delete options2.pass;
      if (options2.bufferCommands != null) {
        this.config.bufferCommands = options2.bufferCommands;
        delete options2.bufferCommands;
      }
    } else {
      options2 = {};
    }
    this._connectionOptions = options2;
    const dbName = options2.dbName;
    if (dbName != null) {
      this.$dbName = dbName;
    }
    delete options2.dbName;
    if (!utils2.hasUserDefinedProperty(options2, "driverInfo")) {
      options2.driverInfo = {
        name: "Mongoose",
        version: pkg.version
      };
    }
    const { schemaMap, encryptedFieldsMap } = this._buildEncryptionSchemas();
    if ((utils2.hasOwnKeys(schemaMap) || utils2.hasOwnKeys(encryptedFieldsMap)) && !options2.autoEncryption) {
      throw new Error("Must provide `autoEncryption` when connecting with encrypted schemas.");
    }
    if (utils2.hasOwnKeys(schemaMap)) {
      options2.autoEncryption.schemaMap = schemaMap;
    }
    if (utils2.hasOwnKeys(encryptedFieldsMap)) {
      options2.autoEncryption.encryptedFieldsMap = encryptedFieldsMap;
    }
    this.readyState = STATES.connecting;
    this._connectionString = uri;
    let client;
    try {
      client = new mongodb.MongoClient(uri, options2);
    } catch (error2) {
      this.readyState = STATES.disconnected;
      throw error2;
    }
    this.client = client;
    client.setMaxListeners(0);
    await client.connect();
    _setClient(this, client, options2, dbName);
    for (const db of this.otherDbs) {
      _setClient(db, client, {}, db.name);
    }
    return this;
  };
  NativeConnection.prototype._buildEncryptionSchemas = function() {
    const qeMappings = {};
    const csfleMappings = {};
    const encryptedModels = Object.values(this.models).filter((model2) => model2.schema._hasEncryptedFields());
    for (const model2 of encryptedModels) {
      const { schema: schema2, collection: { collectionName } } = model2;
      const namespace = `${this.$dbName}.${collectionName}`;
      const mappings = schema2.encryptionType() === "csfle" ? csfleMappings : qeMappings;
      mappings[namespace] ??= new Schema({}, { encryptionType: schema2.encryptionType() });
      const isNonRootDiscriminator = schema2.discriminatorMapping && !schema2.discriminatorMapping.isRoot;
      if (isNonRootDiscriminator) {
        const rootSchema = schema2._baseSchema;
        schema2.eachPath((pathname) => {
          if (rootSchema.path(pathname)) return;
          if (!mappings[namespace]._hasEncryptedField(pathname)) return;
          throw new Error(`Cannot have duplicate keys in discriminators with encryption. key=${pathname}`);
        });
      }
      mappings[namespace].add(schema2);
    }
    const schemaMap = Object.fromEntries(Object.entries(csfleMappings).map(
      ([namespace, schema2]) => [namespace, schema2._buildSchemaMap()]
    ));
    const encryptedFieldsMap = Object.fromEntries(Object.entries(qeMappings).map(
      ([namespace, schema2]) => [namespace, schema2._buildEncryptedFields()]
    ));
    return {
      schemaMap,
      encryptedFieldsMap
    };
  };
  NativeConnection.prototype.setClient = function setClient(client) {
    if (!(client instanceof mongodb.MongoClient)) {
      throw new MongooseError("Must call `setClient()` with an instance of MongoClient");
    }
    if (this.readyState !== STATES.disconnected) {
      throw new MongooseError("Cannot call `setClient()` on a connection that is already connected.");
    }
    if (client.topology == null) {
      throw new MongooseError("Cannot call `setClient()` with a MongoClient that you have not called `connect()` on yet.");
    }
    this._connectionString = client.s.url;
    _setClient(this, client, {}, client.s.options.dbName);
    for (const model2 of Object.values(this.models)) {
      model2.init().catch(function $modelInitNoop() {
      });
    }
    return this;
  };
  function _setClient(conn, client, options2, dbName) {
    const db = dbName != null ? client.db(dbName) : client.db();
    conn.db = db;
    conn.client = client;
    conn.host = client?.s?.options?.hosts?.[0]?.host;
    conn.port = client?.s?.options?.hosts?.[0]?.port;
    conn.name = dbName != null ? dbName : db.databaseName;
    conn._closeCalled = client._closeCalled;
    const _handleReconnect = () => {
      if (conn.readyState !== STATES.connected) {
        conn.readyState = STATES.connected;
        conn.emit("reconnect");
        conn.emit("reconnected");
        conn.onOpen();
      }
    };
    const type2 = client?.topology?.description?.type || "";
    if (type2 === "Single") {
      client.on("serverDescriptionChanged", (ev) => {
        const newDescription = ev.newDescription;
        if (newDescription.type === "Unknown") {
          conn.readyState = STATES.disconnected;
        } else {
          _handleReconnect();
        }
      });
    } else if (type2.startsWith("ReplicaSet")) {
      client.on("topologyDescriptionChanged", (ev) => {
        const description = ev.newDescription;
        if (conn.readyState === STATES.connected && description.type !== "ReplicaSetWithPrimary") {
          conn.readyState = STATES.disconnected;
        } else if (conn.readyState === STATES.disconnected && description.type === "ReplicaSetWithPrimary") {
          _handleReconnect();
        }
      });
    }
    conn._lastHeartbeatAt = null;
    client.on("serverHeartbeatSucceeded", () => {
      conn._lastHeartbeatAt = Date2.now();
      for (const otherDb of conn.otherDbs) {
        otherDb._lastHeartbeatAt = conn._lastHeartbeatAt;
      }
      if (conn._queue.length > 0 && conn.readyState === STATES.connected) {
        conn._flushQueue();
      }
      for (const otherDb of conn.otherDbs) {
        if (otherDb._queue.length > 0 && otherDb.readyState === STATES.connected) {
          otherDb._flushQueue();
        }
      }
    });
    if (options2.monitorCommands) {
      client.on("commandStarted", (data) => conn.emit("commandStarted", data));
      client.on("commandFailed", (data) => conn.emit("commandFailed", data));
      client.on("commandSucceeded", (data) => conn.emit("commandSucceeded", data));
    }
    conn.onOpen();
    for (const i in conn.collections) {
      if (Object.hasOwn(conn.collections, i)) {
        conn.collections[i].onOpen();
      }
    }
  }
  connection = NativeConnection;
  return connection;
}
var hasRequiredNodeMongodbNative;
function requireNodeMongodbNative() {
  if (hasRequiredNodeMongodbNative) return nodeMongodbNative;
  hasRequiredNodeMongodbNative = 1;
  nodeMongodbNative.BulkWriteResult = requireBulkWriteResult();
  nodeMongodbNative.Collection = requireCollection();
  nodeMongodbNative.Connection = requireConnection();
  nodeMongodbNative.ClientEncryption = requireLib$1().ClientEncryption;
  return nodeMongodbNative;
}
var mongoose$1 = { exports: {} };
var validOptions;
var hasRequiredValidOptions;
function requireValidOptions() {
  if (hasRequiredValidOptions) return validOptions;
  hasRequiredValidOptions = 1;
  const VALID_OPTIONS = Object.freeze([
    "allowDiskUse",
    "applyPluginsToChildSchemas",
    "applyPluginsToDiscriminators",
    "autoCreate",
    "autoIndex",
    "autoSearchIndex",
    "bufferCommands",
    "bufferTimeoutMS",
    "cloneSchemas",
    "createInitialConnection",
    "debug",
    "forceRepopulate",
    "id",
    "maxTimeMS",
    "objectIdGetter",
    "overwriteModels",
    "returnDocument",
    "returnOriginal",
    "runValidators",
    "sanitizeFilter",
    "sanitizeProjection",
    "selectPopulatedPaths",
    "setDefaultsOnInsert",
    "strict",
    "strictPopulate",
    "strictQuery",
    "strictRead",
    "timestamps.createdAt.immutable",
    "toJSON",
    "toObject",
    "transactionAsyncLocalStorage",
    "translateAliases",
    "updatePipeline"
  ]);
  validOptions = VALID_OPTIONS;
  return validOptions;
}
var eachAsyncMultiError;
var hasRequiredEachAsyncMultiError;
function requireEachAsyncMultiError() {
  if (hasRequiredEachAsyncMultiError) return eachAsyncMultiError;
  hasRequiredEachAsyncMultiError = 1;
  const MongooseError = requireMongooseError();
  class EachAsyncMultiError extends MongooseError {
    /**
     * @param {string} connectionString
     */
    constructor(errors) {
      let preview = errors.map((e) => e.message).join(", ");
      if (preview.length > 50) {
        preview = preview.slice(0, 50) + "...";
      }
      super(`eachAsync() finished with ${errors.length} errors: ${preview}`);
      this.errors = errors;
    }
  }
  Object.defineProperty(EachAsyncMultiError.prototype, "name", {
    value: "EachAsyncMultiError"
  });
  eachAsyncMultiError = EachAsyncMultiError;
  return eachAsyncMultiError;
}
var eachAsync;
var hasRequiredEachAsync;
function requireEachAsync() {
  if (hasRequiredEachAsync) return eachAsync;
  hasRequiredEachAsync = 1;
  const EachAsyncMultiError = requireEachAsyncMultiError();
  const immediate2 = requireImmediate();
  eachAsync = async function eachAsync2(next, fn, options2) {
    const parallel = options2.parallel || 1;
    const batchSize = options2.batchSize;
    const signal = options2.signal;
    const continueOnError = options2.continueOnError;
    const aggregatedErrors = [];
    const enqueue = asyncQueue();
    let aborted = false;
    return new Promise((resolve, reject) => {
      if (signal != null) {
        if (signal.aborted) {
          return resolve(null);
        }
        signal.addEventListener("abort", () => {
          aborted = true;
          return resolve(null);
        }, { once: true });
      }
      if (batchSize != null) {
        if (typeof batchSize !== "number") {
          throw new TypeError("batchSize must be a number");
        } else if (!Number.isInteger(batchSize)) {
          throw new TypeError("batchSize must be an integer");
        } else if (batchSize < 1) {
          throw new TypeError("batchSize must be at least 1");
        }
      }
      iterate((err, res) => {
        if (err != null) {
          return reject(err);
        }
        resolve(res);
      });
    });
    function iterate(finalCallback) {
      let handleResultsInProgress = 0;
      let currentDocumentIndex = 0;
      let error2 = null;
      for (let i = 0; i < parallel; ++i) {
        enqueue(createFetch());
      }
      function createFetch() {
        let documentsBatch = [];
        let drained = false;
        return fetch;
        function fetch(done) {
          if (drained || aborted) {
            return done();
          } else if (error2) {
            return done();
          }
          next(function(err, doc) {
            if (error2 != null) {
              return done();
            }
            if (err != null) {
              if (err.name === "MongoCursorExhaustedError") {
                doc = null;
              } else if (continueOnError) {
                aggregatedErrors.push(err);
              } else {
                error2 = err;
                finalCallback(err);
                return done();
              }
            }
            if (doc == null) {
              drained = true;
              if (handleResultsInProgress <= 0) {
                const finalErr = continueOnError ? createEachAsyncMultiError(aggregatedErrors) : error2;
                finalCallback(finalErr);
              } else if (batchSize && documentsBatch.length) {
                handleNextResult(documentsBatch, currentDocumentIndex++, handleNextResultCallBack);
              }
              return done();
            }
            ++handleResultsInProgress;
            immediate2(() => done());
            if (batchSize) {
              documentsBatch.push(doc);
            }
            if (batchSize && documentsBatch.length !== batchSize) {
              immediate2(() => enqueue(fetch));
              return;
            }
            const docsToProcess = batchSize ? documentsBatch : doc;
            function handleNextResultCallBack(err2) {
              if (batchSize) {
                handleResultsInProgress -= documentsBatch.length;
                documentsBatch = [];
              } else {
                --handleResultsInProgress;
              }
              if (err2 != null) {
                if (continueOnError) {
                  aggregatedErrors.push(err2);
                } else {
                  error2 = err2;
                  return finalCallback(err2);
                }
              }
              if ((drained || aborted) && handleResultsInProgress <= 0) {
                const finalErr = continueOnError ? createEachAsyncMultiError(aggregatedErrors) : error2;
                return finalCallback(finalErr);
              }
              immediate2(() => enqueue(fetch));
            }
            handleNextResult(docsToProcess, currentDocumentIndex++, handleNextResultCallBack);
          });
        }
      }
    }
    function handleNextResult(doc, i, callback) {
      let maybePromise;
      try {
        maybePromise = fn(doc, i);
      } catch (err) {
        return callback(err);
      }
      if (typeof maybePromise?.then === "function") {
        maybePromise.then(
          function() {
            callback(null);
          },
          function(error2) {
            callback(error2 || new Error("`eachAsync()` promise rejected without error"));
          }
        );
      } else {
        callback(null);
      }
    }
  };
  function asyncQueue() {
    const _queue = [];
    let inProgress = null;
    let id = 0;
    return function enqueue(fn) {
      if (inProgress === null && _queue.length === 0) {
        inProgress = id++;
        return fn(_step);
      }
      _queue.push(fn);
    };
    function _step() {
      if (_queue.length !== 0) {
        inProgress = id++;
        const fn = _queue.shift();
        fn(_step);
      } else {
        inProgress = null;
      }
    }
  }
  function createEachAsyncMultiError(aggregatedErrors) {
    if (aggregatedErrors.length === 0) {
      return null;
    }
    return new EachAsyncMultiError(aggregatedErrors);
  }
  return eachAsync;
}
var tracing;
var hasRequiredTracing;
function requireTracing() {
  if (hasRequiredTracing) return tracing;
  hasRequiredTracing = 1;
  let dc;
  try {
    dc = typeof process !== "undefined" && "getBuiltinModule" in process ? process.getBuiltinModule("node:diagnostics_channel") : require("node:diagnostics_channel");
  } catch {
  }
  const hasTracingChannel = !!dc && typeof dc.tracingChannel === "function";
  function shouldTrace(channel) {
    return !!channel && channel.hasSubscribers !== false;
  }
  function createTracedChannel(name) {
    const ch = hasTracingChannel ? dc.tracingChannel(name) : void 0;
    function trace(fn, contextFactory) {
      if (!shouldTrace(ch)) {
        return fn();
      }
      const traced = ch.tracePromise(fn, contextFactory());
      return traced;
    }
    return { channel: ch, trace };
  }
  tracing = {
    createTracedChannel,
    cursorNextChannel: createTracedChannel("mongoose:cursor:next")
  };
  return tracing;
}
var queryCursor;
var hasRequiredQueryCursor;
function requireQueryCursor() {
  if (hasRequiredQueryCursor) return queryCursor;
  hasRequiredQueryCursor = 1;
  const MongooseError = requireMongooseError();
  const Readable = require$$0$1.Readable;
  const eachAsync2 = requireEachAsync();
  const helpers2 = requireQueryHelpers();
  const kareem = requireKareem();
  const immediate2 = requireImmediate();
  const { once } = require$$0;
  const util = require$$1;
  const { cursorNextChannel } = requireTracing();
  function QueryCursor(query2) {
    Readable.call(this, { autoDestroy: true, objectMode: true });
    this.cursor = null;
    this.skipped = false;
    this.query = query2;
    this._closed = false;
    const model2 = query2.model;
    this._mongooseOptions = {};
    this._transforms = [];
    this.model = model2;
    this.options = {};
    const onPreComplete = (err) => {
      if (err != null) {
        if (err instanceof kareem.skipWrappedFunction) {
          const resultValue = err.args[0];
          if (resultValue != null && (!Array.isArray(resultValue) || resultValue.length)) {
            const err2 = new MongooseError(
              'Cannot `skipMiddlewareFunction()` with a value when using `.find().cursor()`, value must be nullish or empty array, got "' + util.inspect(resultValue) + '".'
            );
            this._markError(err2);
            this.listeners("error").length > 0 && this.emit("error", err2);
            return;
          }
          this.skipped = true;
          this.emit("cursor", null);
          return;
        }
        this._markError(err);
        this.listeners("error").length > 0 && this.emit("error", err);
        return;
      }
      Object.assign(this.options, query2._optionsForExec());
      this._transforms = this._transforms.concat(query2._transforms.slice());
      if (this.options.transform) {
        this._transforms.push(this.options.transform);
      }
      if (this.options.batchSize) {
        this.options._populateBatchSize = Math.min(this.options.batchSize, 5e3);
      }
      if (query2._mongooseOptions._asyncIterator) {
        this._mongooseOptions._asyncIterator = true;
      }
      if (model2.collection._shouldBufferCommands() && model2.collection.buffer) {
        model2.collection.queue.push([
          () => _getRawCursor(query2, this)
        ]);
      } else {
        _getRawCursor(query2, this);
      }
    };
    model2.hooks.execPre("find", query2).then(() => onPreComplete(null), (err) => onPreComplete(err));
  }
  util.inherits(QueryCursor, Readable);
  function _getRawCursor(query2, queryCursor2) {
    try {
      const cursor = query2.model.collection.find(query2._conditions, queryCursor2.options);
      queryCursor2.cursor = cursor;
      queryCursor2.emit("cursor", cursor);
    } catch (err) {
      queryCursor2._markError(err);
      queryCursor2.listeners("error").length > 0 && queryCursor2.emit("error", queryCursor2._error);
    }
  }
  QueryCursor.prototype._read = function() {
    _next(this, (error2, doc) => {
      if (error2) {
        return this.emit("error", error2);
      }
      if (!doc) {
        this.push(null);
        this.cursor.close((error3) => {
          if (error3) {
            return this.emit("error", error3);
          }
        });
        return;
      }
      this.push(doc);
    });
  };
  QueryCursor.prototype.getDriverCursor = async function getDriverCursor() {
    if (this.cursor) {
      return this.cursor;
    }
    await once(this, "cursor");
    return this.cursor;
  };
  Object.defineProperty(QueryCursor.prototype, "map", {
    value: function(fn) {
      this._transforms.push(fn);
      return this;
    },
    enumerable: true,
    configurable: true,
    writable: true
  });
  QueryCursor.prototype._markError = function(error2) {
    this._error = error2;
    return this;
  };
  QueryCursor.prototype.close = async function close() {
    if (typeof arguments[0] === "function") {
      throw new MongooseError("QueryCursor.prototype.close() no longer accepts a callback");
    }
    try {
      await this.cursor.close();
      this._closed = true;
      this.emit("close");
    } catch (error2) {
      this.listeners("error").length > 0 && this.emit("error", error2);
      throw error2;
    }
  };
  QueryCursor.prototype._destroy = function _destroy(_err, callback) {
    let waitForCursor = null;
    if (!this.cursor) {
      waitForCursor = new Promise((resolve) => {
        this.once("cursor", resolve);
      });
    } else {
      waitForCursor = Promise.resolve();
    }
    waitForCursor.then(() => {
      this.cursor.close();
    }).then(() => {
      this._closed = true;
      callback();
    }).catch((error2) => {
      callback(error2);
    });
    return this;
  };
  QueryCursor.prototype.rewind = function() {
    _waitForCursor(this, () => {
      this.cursor.rewind();
    });
    return this;
  };
  QueryCursor.prototype.next = async function next() {
    if (typeof arguments[0] === "function") {
      throw new MongooseError("QueryCursor.prototype.next() no longer accepts a callback");
    }
    if (this._closed) {
      throw new MongooseError("Cannot call `next()` on a closed cursor");
    }
    const _this = this;
    return cursorNextChannel.trace(function maybeTracedQueryCursorNext() {
      return new Promise((resolve, reject) => {
        _next(_this, function(error2, doc) {
          if (error2) {
            return reject(error2);
          }
          resolve(doc);
        });
      });
    }, () => ({
      operation: _this.query.op || "find",
      collection: _this.query.mongooseCollection.name,
      database: _this.model.db?.name,
      serverAddress: _this.model.db?.host,
      serverPort: _this.model.db?.port,
      batchSize: _this.options.batchSize || _this.query.options?.batchSize,
      tailable: _this.options.tailable || _this.query.options?.tailable || false,
      args: {
        filter: _this.query.getFilter(),
        options: _this.query._mongooseOptions
      }
    }));
  };
  QueryCursor.prototype.eachAsync = function(fn, opts) {
    if (typeof arguments[2] === "function") {
      throw new MongooseError("QueryCursor.prototype.eachAsync() no longer accepts a callback");
    }
    if (typeof opts === "function") {
      opts = {};
    }
    opts = opts || {};
    return eachAsync2((cb) => _next(this, cb), fn, opts);
  };
  QueryCursor.prototype.options;
  QueryCursor.prototype.addCursorFlag = function(flag, value) {
    _waitForCursor(this, () => {
      this.cursor.addCursorFlag(flag, value);
    });
    return this;
  };
  if (Symbol.asyncIterator != null) {
    QueryCursor.prototype[Symbol.asyncIterator] = function queryCursorAsyncIterator() {
      this._mongooseOptions._asyncIterator = true;
      return this;
    };
  }
  function _next(ctx, cb) {
    let callback = cb;
    callback = function(err, doc) {
      if (err) {
        return cb(err);
      }
      if (doc === null) {
        if (ctx._mongooseOptions._asyncIterator) {
          return cb(null, { done: true });
        } else {
          return cb(null, null);
        }
      }
      if (ctx._transforms.length && doc !== null) {
        doc = ctx._transforms.reduce(function(doc2, fn) {
          return fn.call(ctx, doc2);
        }, doc);
      }
      if (ctx._mongooseOptions._asyncIterator) {
        return cb(null, { value: doc, done: false });
      }
      return cb(null, doc);
    };
    if (ctx._error) {
      return immediate2(function() {
        callback(ctx._error);
      });
    }
    if (ctx.skipped) {
      return immediate2(() => callback(null, null));
    }
    if (ctx.cursor) {
      if (ctx.query._mongooseOptions.populate && !ctx._pop) {
        ctx._pop = helpers2.preparePopulationOptionsMQ(
          ctx.query,
          ctx.query._mongooseOptions
        );
        ctx._pop.__noPromise = true;
      }
      if (ctx.query._mongooseOptions.populate && ctx.options._populateBatchSize > 1) {
        if (ctx._batchDocs && ctx._batchDocs.length) {
          return _nextDoc(ctx, ctx._batchDocs.shift(), ctx._pop, callback);
        } else if (ctx._batchExhausted) {
          return callback(null, null);
        } else {
          ctx._batchDocs = [];
          ctx.cursor.next().then(
            (res) => {
              _onNext.call({ ctx, callback }, null, res);
            },
            (err) => {
              _onNext.call({ ctx, callback }, err);
            }
          );
          return;
        }
      } else {
        return ctx.cursor.next().then(
          (doc) => {
            if (!doc) {
              callback(null, null);
              return;
            }
            if (!ctx.query._mongooseOptions.populate) {
              return _nextDoc(ctx, doc, null, callback);
            }
            _nextDoc(ctx, doc, ctx._pop, (err, doc2) => {
              if (err != null) {
                return callback(err);
              }
              ctx.query.model.populate(doc2, ctx._pop).then(
                (doc3) => callback(null, doc3),
                (err2) => callback(err2)
              );
            });
          },
          (error2) => {
            callback(error2);
          }
        );
      }
    } else {
      ctx.once("error", cb);
      ctx.once("cursor", function(cursor) {
        ctx.removeListener("error", cb);
        if (cursor == null) {
          if (ctx.skipped) {
            return cb(null, null);
          }
          return;
        }
        _next(ctx, cb);
      });
    }
  }
  function _onNext(error2, doc) {
    if (error2) {
      return this.callback(error2);
    }
    if (!doc) {
      this.ctx._batchExhausted = true;
      return _populateBatch.call(this);
    }
    this.ctx._batchDocs.push(doc);
    if (this.ctx._batchDocs.length < this.ctx.options._populateBatchSize) {
      immediate2(() => this.ctx.cursor.next().then(
        (res) => {
          _onNext.call(this, null, res);
        },
        (err) => {
          _onNext.call(this, err);
        }
      ));
    } else {
      _populateBatch.call(this);
    }
  }
  function _populateBatch() {
    if (!this.ctx._batchDocs.length) {
      return this.callback(null, null);
    }
    this.ctx.query.model.populate(this.ctx._batchDocs, this.ctx._pop).then(
      () => {
        _nextDoc(this.ctx, this.ctx._batchDocs.shift(), this.ctx._pop, this.callback);
      },
      (err) => {
        this.callback(err);
      }
    );
  }
  function _nextDoc(ctx, doc, pop, callback) {
    if (ctx.query._mongooseOptions.lean) {
      return ctx.model.hooks.execPost("find", ctx.query, [[doc]]).then(() => callback(null, doc), (err) => callback(err));
    }
    const { model: model2, _fields, _userProvidedFields, options: options2 } = ctx.query;
    helpers2.createModelAndInit(model2, doc, _fields, _userProvidedFields, options2, pop, (err, doc2) => {
      if (err != null) {
        return callback(err);
      }
      if (options2.session != null) {
        doc2.$session(options2.session);
      }
      ctx.model.hooks.execPost("find", ctx.query, [[doc2]]).then(() => callback(null, doc2), (err2) => callback(err2));
    });
  }
  function _waitForCursor(ctx, cb) {
    if (ctx.cursor) {
      return cb();
    }
    ctx.once("cursor", function(cursor) {
      if (cursor == null) {
        return;
      }
      cb();
    });
  }
  queryCursor = QueryCursor;
  return queryCursor;
}
var applyGlobalOption_1;
var hasRequiredApplyGlobalOption;
function requireApplyGlobalOption() {
  if (hasRequiredApplyGlobalOption) return applyGlobalOption_1;
  hasRequiredApplyGlobalOption = 1;
  const utils2 = requireUtils();
  function applyGlobalMaxTimeMS(options2, connectionOptions, baseOptions) {
    applyGlobalOption(options2, connectionOptions, baseOptions, "maxTimeMS");
  }
  function applyGlobalDiskUse(options2, connectionOptions, baseOptions) {
    applyGlobalOption(options2, connectionOptions, baseOptions, "allowDiskUse");
  }
  applyGlobalOption_1 = {
    applyGlobalMaxTimeMS,
    applyGlobalDiskUse
  };
  function applyGlobalOption(options2, connectionOptions, baseOptions, optionName) {
    if (utils2.hasUserDefinedProperty(options2, optionName)) {
      return;
    }
    if (utils2.hasUserDefinedProperty(connectionOptions, optionName)) {
      options2[optionName] = connectionOptions[optionName];
    } else if (utils2.hasUserDefinedProperty(baseOptions, optionName)) {
      options2[optionName] = baseOptions[optionName];
    }
  }
  return applyGlobalOption_1;
}
var applyReadConcern;
var hasRequiredApplyReadConcern;
function requireApplyReadConcern() {
  if (hasRequiredApplyReadConcern) return applyReadConcern;
  hasRequiredApplyReadConcern = 1;
  applyReadConcern = function applyReadConcern2(schema2, options2) {
    if (options2.readConcern !== void 0) {
      return;
    }
    if (options2?.session?.transaction) {
      return;
    }
    const level = schema2.options?.readConcern?.level;
    if (level != null) {
      options2.readConcern = { level };
    }
  };
  return applyReadConcern;
}
var applyWriteConcern;
var hasRequiredApplyWriteConcern;
function requireApplyWriteConcern() {
  if (hasRequiredApplyWriteConcern) return applyWriteConcern;
  hasRequiredApplyWriteConcern = 1;
  const utils2 = requireUtils();
  applyWriteConcern = function applyWriteConcern2(schema2, options2) {
    if (options2.writeConcern != null) {
      return;
    }
    if (options2?.session?.transaction) {
      return;
    }
    const writeConcern = schema2.options.writeConcern ?? {};
    if (utils2.hasOwnKeys(writeConcern)) {
      options2.writeConcern = {};
      if (!("w" in options2) && writeConcern.w != null) {
        options2.writeConcern.w = writeConcern.w;
      }
      if (!("j" in options2) && writeConcern.j != null) {
        options2.writeConcern.j = writeConcern.j;
      }
      if (!("wtimeout" in options2) && writeConcern.wtimeout != null) {
        options2.writeConcern.wtimeout = writeConcern.wtimeout;
      }
    } else {
      if (!("w" in options2) && writeConcern.w != null) {
        options2.w = writeConcern.w;
      }
      if (!("j" in options2) && writeConcern.j != null) {
        options2.j = writeConcern.j;
      }
      if (!("wtimeout" in options2) && writeConcern.wtimeout != null) {
        options2.wtimeout = writeConcern.wtimeout;
      }
    }
  };
  return applyWriteConcern;
}
var castFilterPath;
var hasRequiredCastFilterPath;
function requireCastFilterPath() {
  if (hasRequiredCastFilterPath) return castFilterPath;
  hasRequiredCastFilterPath = 1;
  const isOperator2 = requireIsOperator();
  castFilterPath = function castFilterPath2(ctx, schematype, val) {
    const any$conditionals = Object.keys(val).some(isOperator2);
    if (!any$conditionals) {
      return schematype.castForQuery(
        null,
        val,
        ctx
      );
    }
    const ks = Object.keys(val);
    let k = ks.length;
    while (k--) {
      const $cond = ks[k];
      const nested = val[$cond];
      if ($cond === "$not") {
        if (nested && schematype && !schematype.embeddedSchemaType && !schematype.Constructor) {
          const _keys = Object.keys(nested);
          if (_keys.length && isOperator2(_keys[0])) {
            for (const key of Object.keys(nested)) {
              nested[key] = schematype.castForQuery(
                key,
                nested[key],
                ctx
              );
            }
          } else {
            val[$cond] = schematype.castForQuery(
              $cond,
              nested,
              ctx
            );
          }
          continue;
        }
      } else {
        val[$cond] = schematype.castForQuery(
          $cond,
          nested,
          ctx
        );
      }
    }
    return val;
  };
  return castFilterPath;
}
var getPath;
var hasRequiredGetPath;
function requireGetPath() {
  if (hasRequiredGetPath) return getPath;
  hasRequiredGetPath = 1;
  const numberRE = /^\d+$/;
  getPath = function getPath2(schema2, path, discriminatorValueMap) {
    let schematype = schema2.path(path);
    if (schematype != null) {
      return schematype;
    }
    const pieces = path.split(".");
    let cur = "";
    let isArray = false;
    for (const piece of pieces) {
      if (isArray && numberRE.test(piece)) {
        continue;
      }
      cur = cur.length === 0 ? piece : cur + "." + piece;
      schematype = schema2.path(cur);
      if (schematype?.schema) {
        schema2 = schematype.schema;
        if (!isArray && schematype.$isMongooseDocumentArray) {
          isArray = true;
        }
        if (discriminatorValueMap && discriminatorValueMap[cur]) {
          schema2 = schema2.discriminators[discriminatorValueMap[cur]] ?? schema2;
        }
        cur = "";
      } else if (schematype?.instance === "Mixed") {
        break;
      }
    }
    return schematype;
  };
  return getPath;
}
var castArrayFilters;
var hasRequiredCastArrayFilters;
function requireCastArrayFilters() {
  if (hasRequiredCastArrayFilters) return castArrayFilters;
  hasRequiredCastArrayFilters = 1;
  const castFilterPath2 = requireCastFilterPath();
  const cleanPositionalOperators2 = requireCleanPositionalOperators();
  const getPath2 = requireGetPath();
  const updatedPathsByArrayFilter2 = requireUpdatedPathsByArrayFilter();
  const utils2 = requireUtils();
  castArrayFilters = function castArrayFilters2(query2) {
    const arrayFilters = query2.options.arrayFilters;
    if (!Array.isArray(arrayFilters)) {
      return;
    }
    const update = query2.getUpdate();
    const schema2 = query2.schema;
    const updatedPathsByFilter = updatedPathsByArrayFilter2(update);
    let strictQuery = schema2.options.strict;
    if (query2._mongooseOptions.strict != null) {
      strictQuery = query2._mongooseOptions.strict;
    }
    if (query2.model?.base.options.strictQuery != null) {
      strictQuery = query2.model.base.options.strictQuery;
    }
    if (schema2._userProvidedOptions.strictQuery != null) {
      strictQuery = schema2._userProvidedOptions.strictQuery;
    }
    if (query2._mongooseOptions.strictQuery != null) {
      strictQuery = query2._mongooseOptions.strictQuery;
    }
    _castArrayFilters(arrayFilters, schema2, strictQuery, updatedPathsByFilter, query2);
  };
  function _castArrayFilters(arrayFilters, schema2, strictQuery, updatedPathsByFilter, query2) {
    const discriminatorValueMap = {};
    for (const filter of arrayFilters) {
      if (filter == null) {
        throw new Error(`Got null array filter in ${arrayFilters}`);
      }
      const keys = Object.keys(filter).filter((key) => filter[key] != null);
      if (keys.length === 0) {
        continue;
      }
      const firstKey2 = keys[0];
      if (firstKey2 === "$and" || firstKey2 === "$or") {
        for (const key of keys) {
          _castArrayFilters(filter[key], schema2, strictQuery, updatedPathsByFilter, query2);
        }
        continue;
      }
      const dot = firstKey2.indexOf(".");
      const filterWildcardPath = dot === -1 ? firstKey2 : firstKey2.substring(0, dot);
      if (updatedPathsByFilter[filterWildcardPath] == null) {
        continue;
      }
      const baseFilterPath = cleanPositionalOperators2(
        updatedPathsByFilter[filterWildcardPath]
      );
      const baseSchematype = getPath2(schema2, baseFilterPath, discriminatorValueMap);
      let filterBaseSchema = baseSchematype != null ? baseSchematype.schema : null;
      if (filterBaseSchema?.discriminators != null && filter[filterWildcardPath + "." + filterBaseSchema.options.discriminatorKey]) {
        filterBaseSchema = filterBaseSchema.discriminators[filter[filterWildcardPath + "." + filterBaseSchema.options.discriminatorKey]] || filterBaseSchema;
        discriminatorValueMap[baseFilterPath] = filter[filterWildcardPath + "." + filterBaseSchema.options.discriminatorKey];
      }
      for (const key of keys) {
        if (updatedPathsByFilter[key] === null) {
          continue;
        }
        if (utils2.hasOwnKeys(updatedPathsByFilter) === false) {
          continue;
        }
        const dot2 = key.indexOf(".");
        let filterPathRelativeToBase = dot2 === -1 ? null : key.substring(dot2);
        let schematype;
        if (filterPathRelativeToBase == null || filterBaseSchema == null) {
          schematype = baseSchematype;
        } else {
          filterPathRelativeToBase = cleanPositionalOperators2(filterPathRelativeToBase);
          schematype = getPath2(filterBaseSchema, filterPathRelativeToBase, discriminatorValueMap);
        }
        if (schematype == null) {
          if (!strictQuery) {
            return;
          }
          const filterPath = filterPathRelativeToBase == null ? baseFilterPath + ".0" : baseFilterPath + ".0" + filterPathRelativeToBase;
          throw new Error(`Could not find path "${filterPath}" in schema`);
        }
        if (typeof filter[key] === "object") {
          filter[key] = castFilterPath2(query2, schematype, filter[key]);
        } else {
          filter[key] = schematype.castForQuery(null, filter[key]);
        }
      }
    }
  }
  return castArrayFilters;
}
var isInclusive;
var hasRequiredIsInclusive;
function requireIsInclusive() {
  if (hasRequiredIsInclusive) return isInclusive;
  hasRequiredIsInclusive = 1;
  const isDefiningProjection2 = requireIsDefiningProjection();
  const isPOJO2 = requireIsPOJO();
  isInclusive = function isInclusive2(projection) {
    if (projection == null) {
      return false;
    }
    const props = Object.keys(projection);
    const numProps = props.length;
    if (numProps === 0) {
      return false;
    }
    for (let i = 0; i < numProps; ++i) {
      const prop = props[i];
      if (prop.startsWith("+")) {
        continue;
      }
      if (isDefiningProjection2(projection[prop]) && !!projection[prop]) {
        if (isPOJO2(projection[prop])) {
          return isInclusive2(projection[prop]);
        } else {
          return !!projection[prop];
        }
      }
    }
    return false;
  };
  return isInclusive;
}
var isSubpath;
var hasRequiredIsSubpath;
function requireIsSubpath() {
  if (hasRequiredIsSubpath) return isSubpath;
  hasRequiredIsSubpath = 1;
  isSubpath = function isSubpath2(path1, path2) {
    return path1 === path2 || path2.startsWith(path1 + ".");
  };
  return isSubpath;
}
var parseProjection;
var hasRequiredParseProjection;
function requireParseProjection() {
  if (hasRequiredParseProjection) return parseProjection;
  hasRequiredParseProjection = 1;
  parseProjection = function parseProjection2(v, retainMinusPaths) {
    const type2 = typeof v;
    if (type2 === "string") {
      v = v.split(/\s+/);
    }
    if (!Array.isArray(v) && Object.prototype.toString.call(v) !== "[object Arguments]") {
      return v;
    }
    const len = v.length;
    const ret = {};
    for (let i = 0; i < len; ++i) {
      let field = v[i];
      if (!field) {
        continue;
      }
      const include = field.charAt(0) === "-" ? 0 : 1;
      if (!retainMinusPaths && include === 0) {
        field = field.slice(1);
      }
      ret[field] = include;
    }
    return ret;
  };
  return parseProjection;
}
var removeUnusedArrayFilters;
var hasRequiredRemoveUnusedArrayFilters;
function requireRemoveUnusedArrayFilters() {
  if (hasRequiredRemoveUnusedArrayFilters) return removeUnusedArrayFilters;
  hasRequiredRemoveUnusedArrayFilters = 1;
  removeUnusedArrayFilters = function removeUnusedArrayFilters2(update, options2) {
    const arrayFilters = options2?.arrayFilters;
    if (!Array.isArray(arrayFilters)) {
      return;
    }
    const updateKeys = Object.keys(update).map((key) => Object.keys(update[key])).reduce((cur, arr) => cur.concat(arr), []);
    options2.arrayFilters = options2.arrayFilters.filter((obj) => {
      return _checkSingleFilterKey(obj, updateKeys);
    });
  };
  function _checkSingleFilterKey(arrayFilter, updateKeys) {
    const firstKey2 = Object.keys(arrayFilter)[0];
    if (firstKey2 === "$and" || firstKey2 === "$or") {
      if (!Array.isArray(arrayFilter[firstKey2])) {
        return false;
      }
      return arrayFilter[firstKey2].find((filter) => _checkSingleFilterKey(filter, updateKeys)) != null;
    }
    const firstDot = firstKey2.indexOf(".");
    const arrayFilterKey = firstDot === -1 ? firstKey2 : firstKey2.slice(0, firstDot);
    return updateKeys.find((key) => key.includes("$[" + arrayFilterKey + "]")) != null;
  }
  return removeUnusedArrayFilters;
}
var hasDollarKeys;
var hasRequiredHasDollarKeys;
function requireHasDollarKeys() {
  if (hasRequiredHasDollarKeys) return hasDollarKeys;
  hasRequiredHasDollarKeys = 1;
  hasDollarKeys = function hasDollarKeys2(obj) {
    if (typeof obj !== "object" || obj === null) {
      return false;
    }
    const keys = Object.keys(obj);
    const len = keys.length;
    for (let i = 0; i < len; ++i) {
      if (keys[i][0] === "$") {
        return true;
      }
    }
    return false;
  };
  return hasDollarKeys;
}
var sanitizeFilter;
var hasRequiredSanitizeFilter;
function requireSanitizeFilter() {
  if (hasRequiredSanitizeFilter) return sanitizeFilter;
  hasRequiredSanitizeFilter = 1;
  const MongooseError = requireMongooseError();
  const hasDollarKeys2 = requireHasDollarKeys();
  const { trustedSymbol } = requireTrusted();
  sanitizeFilter = function sanitizeFilter2(filter) {
    if (filter == null || typeof filter !== "object") {
      return filter;
    }
    if (Array.isArray(filter)) {
      for (const subfilter of filter) {
        sanitizeFilter2(subfilter);
      }
      return filter;
    }
    const filterKeys = Object.keys(filter);
    for (const key of filterKeys) {
      const value = filter[key];
      if (value?.[trustedSymbol]) {
        continue;
      }
      if (key === "$and" || key === "$or" || key === "$nor") {
        sanitizeFilter2(value);
        continue;
      } else if (key === "$jsonSchema" || key === "$where" || key === "$expr" || key === "$text") {
        throw new MongooseError(key + " is not allowed with sanitizeFilter");
      }
      if (hasDollarKeys2(value)) {
        const keys = Object.keys(value);
        if (keys.length === 1 && keys[0] === "$eq") {
          continue;
        }
        filter[key] = { $eq: filter[key] };
      }
    }
    return filter;
  };
  return sanitizeFilter;
}
var sanitizeProjection;
var hasRequiredSanitizeProjection;
function requireSanitizeProjection() {
  if (hasRequiredSanitizeProjection) return sanitizeProjection;
  hasRequiredSanitizeProjection = 1;
  sanitizeProjection = function sanitizeProjection2(projection) {
    if (projection == null) {
      return;
    }
    const keys = Object.keys(projection);
    for (let i = 0; i < keys.length; ++i) {
      if (typeof projection[keys[i]] === "string") {
        projection[keys[i]] = 1;
      }
    }
  };
  return sanitizeProjection;
}
var selectPopulatedFields;
var hasRequiredSelectPopulatedFields;
function requireSelectPopulatedFields() {
  if (hasRequiredSelectPopulatedFields) return selectPopulatedFields;
  hasRequiredSelectPopulatedFields = 1;
  const isExclusive2 = requireIsExclusive();
  const isInclusive2 = requireIsInclusive();
  selectPopulatedFields = function selectPopulatedFields2(fields, userProvidedFields, populateOptions2) {
    if (populateOptions2 == null) {
      return;
    }
    const paths = Object.keys(populateOptions2);
    userProvidedFields = userProvidedFields || {};
    if (isInclusive2(fields)) {
      for (const path of paths) {
        if (!isPathInFields(userProvidedFields, path)) {
          fields[path] = 1;
        } else if (userProvidedFields[path] === 0) {
          delete fields[path];
        }
        const refPath = populateOptions2[path]?.refPath;
        if (typeof refPath === "string") {
          if (!isPathInFields(userProvidedFields, refPath)) {
            fields[refPath] = 1;
          } else if (userProvidedFields[refPath] === 0) {
            delete fields[refPath];
          }
        }
      }
    } else if (isExclusive2(fields)) {
      for (const path of paths) {
        if (userProvidedFields[path] == null) {
          delete fields[path];
        }
        const refPath = populateOptions2[path]?.refPath;
        if (typeof refPath === "string" && userProvidedFields[refPath] == null) {
          delete fields[refPath];
        }
      }
    }
  };
  function isPathInFields(userProvidedFields, path) {
    const pieces = path.split(".");
    const len = pieces.length;
    let cur = pieces[0];
    for (let i = 1; i < len; ++i) {
      if (userProvidedFields[cur] != null || userProvidedFields[cur + ".$"] != null) {
        return true;
      }
      cur += "." + pieces[i];
    }
    return userProvidedFields[cur] != null || userProvidedFields[cur + ".$"] != null;
  }
  return selectPopulatedFields;
}
var updateValidators;
var hasRequiredUpdateValidators;
function requireUpdateValidators() {
  if (hasRequiredUpdateValidators) return updateValidators;
  hasRequiredUpdateValidators = 1;
  const ValidationError = requireValidation();
  const cleanPositionalOperators2 = requireCleanPositionalOperators();
  const flatten = requireCommon().flatten;
  updateValidators = async function updateValidators2(query2, schema2, castedDoc, options2) {
    const keys = Object.keys(castedDoc || {});
    let updatedKeys = {};
    let updatedValues = {};
    const isPull = {};
    const arrayAtomicUpdates = {};
    const numKeys = keys.length;
    let hasDollarUpdate = false;
    let currentUpdate;
    let key;
    for (let i = 0; i < numKeys; ++i) {
      if (keys[i].startsWith("$")) {
        hasDollarUpdate = true;
        if (keys[i] === "$push" || keys[i] === "$addToSet") {
          const _keys = Object.keys(castedDoc[keys[i]]);
          for (let ii = 0; ii < _keys.length; ++ii) {
            currentUpdate = castedDoc[keys[i]][_keys[ii]];
            if (currentUpdate?.$each) {
              arrayAtomicUpdates[_keys[ii]] = (arrayAtomicUpdates[_keys[ii]] || []).concat(currentUpdate.$each);
            } else {
              arrayAtomicUpdates[_keys[ii]] = (arrayAtomicUpdates[_keys[ii]] || []).concat([currentUpdate]);
            }
          }
          continue;
        }
        const flat = flatten(castedDoc[keys[i]], null, null, schema2);
        const paths = Object.keys(flat);
        const numPaths = paths.length;
        for (let j = 0; j < numPaths; ++j) {
          const updatedPath = cleanPositionalOperators2(paths[j]);
          key = keys[i];
          if (updatedPath.includes("$")) {
            continue;
          }
          if (key === "$set" || key === "$setOnInsert" || key === "$pull" || key === "$pullAll") {
            updatedValues[updatedPath] = flat[paths[j]];
            isPull[updatedPath] = key === "$pull" || key === "$pullAll";
          } else if (key === "$unset") {
            updatedValues[updatedPath] = void 0;
          }
          updatedKeys[updatedPath] = true;
        }
      }
    }
    if (!hasDollarUpdate) {
      updatedValues = flatten(castedDoc, null, null, schema2);
      updatedKeys = Object.keys(updatedValues);
    }
    const updates = Object.keys(updatedValues);
    const numUpdates = updates.length;
    const validatorsToExecute = [];
    const validationErrors = [];
    const alreadyValidated = [];
    const context = query2;
    for (let i = 0; i < numUpdates; ++i) {
      const v = updatedValues[updates[i]];
      const schemaPath = schema2._getSchema(updates[i]);
      if (schemaPath == null) {
        continue;
      }
      if (schemaPath.instance === "Mixed" && schemaPath.path !== updates[i]) {
        continue;
      }
      if (v && Array.isArray(v.$in)) {
        v.$in.forEach((v2, i2) => {
          validatorsToExecute.push(
            schemaPath.doValidate(v2, context, { updateValidator: true }).catch((err) => {
              err.path = updates[i2] + ".$in." + i2;
              validationErrors.push(err);
            })
          );
        });
      } else {
        if (isPull[updates[i]] && schemaPath.$isMongooseArray) {
          continue;
        }
        if (schemaPath.$isMongooseDocumentArrayElement && v?.$__ != null) {
          alreadyValidated.push(updates[i]);
          validatorsToExecute.push(
            schemaPath.doValidate(v, context, { updateValidator: true }).catch((err) => {
              if (err.errors) {
                for (const key2 of Object.keys(err.errors)) {
                  const _err = err.errors[key2];
                  _err.path = updates[i] + "." + key2;
                  validationErrors.push(_err);
                }
              } else {
                err.path = updates[i];
                validationErrors.push(err);
              }
            })
          );
        } else {
          const isAlreadyValidated = alreadyValidated.find((path) => updates[i].startsWith(path + "."));
          if (isAlreadyValidated) {
            continue;
          }
          if (schemaPath.$isSingleNested) {
            alreadyValidated.push(updates[i]);
          }
          validatorsToExecute.push(
            schemaPath.doValidate(v, context, { updateValidator: true }).catch((err) => {
              if (schemaPath.schema != null && schemaPath.schema.options.storeSubdocValidationError === false && err instanceof ValidationError) {
                return;
              }
              if (err) {
                err.path = updates[i];
                validationErrors.push(err);
              }
            })
          );
        }
      }
    }
    const arrayUpdates = Object.keys(arrayAtomicUpdates);
    for (const arrayUpdate of arrayUpdates) {
      let schemaPath = schema2._getSchema(arrayUpdate);
      if (schemaPath && schemaPath.$isMongooseDocumentArray) {
        validatorsToExecute.push(
          schemaPath.doValidate(
            arrayAtomicUpdates[arrayUpdate],
            options2?.context === "query" ? query2 : null
          ).catch((err) => {
            err.path = arrayUpdate;
            validationErrors.push(err);
          })
        );
      } else {
        schemaPath = schema2._getSchema(arrayUpdate + ".0");
        for (const atomicUpdate of arrayAtomicUpdates[arrayUpdate]) {
          validatorsToExecute.push(
            schemaPath.doValidate(
              atomicUpdate,
              options2?.context === "query" ? query2 : null,
              { updateValidator: true }
            ).catch((err) => {
              err.path = arrayUpdate;
              validationErrors.push(err);
            })
          );
        }
      }
    }
    await Promise.all(validatorsToExecute);
    return validationErrors;
  };
  return updateValidators;
}
var query;
var hasRequiredQuery;
function requireQuery() {
  if (hasRequiredQuery) return query;
  hasRequiredQuery = 1;
  const CastError = requireCast$1();
  const DocumentNotFoundError = requireNotFound();
  const Kareem = requireKareem();
  const MongooseError = requireMongooseError();
  const ObjectParameterError = requireObjectParameter();
  const QueryCursor = requireQueryCursor();
  const ValidationError = requireValidation();
  const { applyGlobalMaxTimeMS, applyGlobalDiskUse } = requireApplyGlobalOption();
  const handleReadPreferenceAliases2 = requireHandleReadPreferenceAliases();
  const applyReadConcern2 = requireApplyReadConcern();
  const applyWriteConcern2 = requireApplyWriteConcern();
  const cast2 = requireCast();
  const castArrayFilters2 = requireCastArrayFilters();
  const castNumber = requireNumber$1();
  const castUpdate2 = requireCastUpdate();
  const clone = requireClone();
  const decorateUpdateWithVersionKey2 = requireDecorateUpdateWithVersionKey();
  const getDiscriminatorByValue2 = requireGetDiscriminatorByValue();
  const helpers2 = requireQueryHelpers();
  const internalToObjectOptions = requireOptions().internalToObjectOptions;
  const isExclusive2 = requireIsExclusive();
  const isInclusive2 = requireIsInclusive();
  const isPathSelectedInclusive2 = requireIsPathSelectedInclusive();
  const isSubpath2 = requireIsSubpath();
  const mpath = requireMpath();
  const mquery = requireMquery();
  const parseProjection2 = requireParseProjection();
  const removeUnusedArrayFilters2 = requireRemoveUnusedArrayFilters();
  const sanitizeFilter2 = requireSanitizeFilter();
  const sanitizeProjection2 = requireSanitizeProjection();
  const { buildMiddlewareFilter } = requireBuildMiddlewareFilter();
  const selectPopulatedFields2 = requireSelectPopulatedFields();
  const setDefaultsOnInsert2 = requireSetDefaultsOnInsert();
  const specialProperties2 = requireSpecialProperties();
  const updateValidators2 = requireUpdateValidators();
  const util = require$$1;
  const utils2 = requireUtils();
  const queryMiddlewareFunctions = requireConstants().queryMiddlewareFunctions;
  const { createTracedChannel } = requireTracing();
  const { trace: traceQuery } = createTracedChannel("mongoose:query");
  const queryOptionMethods = /* @__PURE__ */ new Set([
    "allowDiskUse",
    "batchSize",
    "collation",
    "comment",
    "explain",
    "hint",
    "j",
    "lean",
    "limit",
    "maxTimeMS",
    "populate",
    "projection",
    "read",
    "select",
    "skip",
    "slice",
    "sort",
    "tailable",
    "w",
    "writeConcern",
    "wtimeout"
  ]);
  const opToThunk = /* @__PURE__ */ new Map([
    ["countDocuments", "_countDocuments"],
    ["distinct", "__distinct"],
    ["estimatedDocumentCount", "_estimatedDocumentCount"],
    ["find", "_find"],
    ["findOne", "_findOne"],
    ["findOneAndReplace", "_findOneAndReplace"],
    ["findOneAndUpdate", "_findOneAndUpdate"],
    ["replaceOne", "_replaceOne"],
    ["updateMany", "_updateMany"],
    ["updateOne", "_updateOne"],
    ["deleteMany", "_deleteMany"],
    ["deleteOne", "_deleteOne"],
    ["findOneAndDelete", "_findOneAndDelete"]
  ]);
  const queryUpdateSymbol = /* @__PURE__ */ Symbol("mongoose#Query#update");
  function Query(conditions, options2, model2, collection2) {
    if (!this._mongooseOptions) {
      this._mongooseOptions = {};
    }
    options2 = options2 || {};
    this._transforms = [];
    this._hooks = new Kareem();
    this._execCount = 0;
    const keys = Object.keys(options2);
    for (const key of keys) {
      this._mongooseOptions[key] = options2[key];
    }
    if (collection2) {
      this.mongooseCollection = collection2;
    }
    if (model2) {
      this.model = model2;
      this.schema = model2.schema;
    }
    if (this.model?._mapreduce) {
      this.lean();
    }
    mquery.call(this, null, options2);
    if (collection2) {
      this.collection(collection2);
    }
    if (conditions) {
      this.find(conditions);
    }
    this.options = this.options || {};
    this.$useProjection = true;
    const collation = this?.schema?.options?.collation || null;
    if (collation != null) {
      this.options.collation = collation;
    }
  }
  function isEmptyFilter(obj) {
    if (obj == null) return true;
    if (typeof obj !== "object") return true;
    if (utils2.hasOwnKeys(obj) === false) return true;
    for (const key of ["$and", "$or", "$nor"]) {
      if (Array.isArray(obj[key])) {
        if (obj[key].length === 0 || obj[key].every((item) => isEmptyFilter(item))) {
          return true;
        }
      }
    }
    return false;
  }
  function checkRequireFilter(filter, options2) {
    if (options2?.requireFilter && isEmptyFilter(filter)) {
      throw new MongooseError("Empty or invalid filter not allowed with requireFilter enabled");
    }
  }
  Query.prototype = new mquery();
  Query.prototype.constructor = Query;
  Object.defineProperty(Query.prototype, "_update", {
    configurable: true,
    enumerable: true,
    get: function() {
      _cloneUpdateIfShared(this);
      return this[queryUpdateSymbol];
    },
    set: function(v) {
      this[queryUpdateSymbol] = v;
    }
  });
  Query.prototype.count = void 0;
  Query.prototype.findOneAndRemove = void 0;
  Query.base = mquery.prototype;
  Object.defineProperty(Query.prototype, "_distinct", {
    configurable: true,
    writable: true,
    enumerable: true,
    value: void 0
  });
  Query.use$geoWithin = mquery.use$geoWithin;
  Query.prototype.toConstructor = function toConstructor() {
    const model2 = this.model;
    const coll = this.mongooseCollection;
    const CustomQuery = function(criteria, options3) {
      if (!(this instanceof CustomQuery)) {
        return new CustomQuery(criteria, options3);
      }
      this._mongooseOptions = clone(p._mongooseOptions);
      Query.call(this, criteria, options3 || null, model2, coll);
    };
    util.inherits(CustomQuery, model2.Query);
    const p = CustomQuery.prototype;
    p.options = {};
    const options2 = Object.assign({}, this.options);
    if (options2.sort != null) {
      p.sort(options2.sort);
      delete options2.sort;
    }
    p.setOptions(options2);
    p.op = this.op;
    p._conditions = clone(this._conditions);
    p._fields = clone(this._fields);
    p[queryUpdateSymbol] = clone(this[queryUpdateSymbol], {
      flattenDecimals: false
    });
    p._path = this._path;
    p._distinct = this._distinct;
    p._collection = this._collection;
    p._mongooseOptions = this._mongooseOptions;
    return CustomQuery;
  };
  Query.prototype.clone = function() {
    const model2 = this.model;
    const collection2 = this.mongooseCollection;
    const q = new this.model.Query({}, {}, model2, collection2);
    const options2 = Object.assign({}, this.options);
    if (options2.sort != null) {
      q.sort(options2.sort);
      delete options2.sort;
    }
    q.setOptions(options2);
    q.op = this.op;
    q._conditions = clone(this._conditions);
    q._fields = clone(this._fields);
    q[queryUpdateSymbol] = clone(this[queryUpdateSymbol], {
      flattenDecimals: false
    });
    q._path = this._path;
    q._distinct = this._distinct;
    q._collection = this._collection;
    q._mongooseOptions = this._mongooseOptions;
    return q;
  };
  Query.prototype.slice = function(...args) {
    if (args.length === 0) {
      return this;
    }
    this._validate("slice");
    let path;
    let val;
    if (args.length === 1) {
      const arg = args[0];
      if (typeof arg === "object" && !Array.isArray(arg)) {
        const keys = Object.keys(arg);
        const numKeys = keys.length;
        for (let i = 0; i < numKeys; ++i) {
          this.slice(keys[i], arg[keys[i]]);
        }
        return this;
      }
      this._ensurePath("slice");
      path = this._path;
      val = args[0];
    } else if (args.length === 2) {
      if ("number" === typeof args[0]) {
        this._ensurePath("slice");
        path = this._path;
        val = [args[0], args[1]];
      } else {
        path = args[0];
        val = args[1];
      }
    } else if (args.length === 3) {
      path = args[0];
      val = [args[1], args[2]];
    }
    const p = {};
    p[path] = { $slice: val };
    this.select(p);
    return this;
  };
  const validOpsSet = new Set(queryMiddlewareFunctions);
  Query.prototype._validateOp = function() {
    if (this.op != null && !validOpsSet.has(this.op)) {
      this.error(new Error('Query has invalid `op`: "' + this.op + '"'));
    }
    if (this.op !== "estimatedDocumentCount" && this._conditions == null) {
      throw new ObjectParameterError(this._conditions, "filter", this.op);
    }
  };
  Query.prototype.mod = function() {
    let val;
    let path;
    if (arguments.length === 1) {
      this._ensurePath("mod");
      val = arguments[0];
      path = this._path;
    } else if (arguments.length === 2 && !Array.isArray(arguments[1])) {
      this._ensurePath("mod");
      val = [arguments[0], arguments[1]];
      path = this._path;
    } else if (arguments.length === 3) {
      val = [arguments[1], arguments[2]];
      path = arguments[0];
    } else {
      val = arguments[1];
      path = arguments[0];
    }
    const conds = this._conditions[path] || (this._conditions[path] = {});
    conds.$mod = val;
    return this;
  };
  Query.prototype.limit = function limit(v) {
    this._validate("limit");
    if (typeof v === "string") {
      try {
        v = castNumber(v);
      } catch {
        throw new CastError("Number", v, "limit");
      }
    }
    this.options.limit = v;
    return this;
  };
  Query.prototype.skip = function skip(v) {
    this._validate("skip");
    if (typeof v === "string") {
      try {
        v = castNumber(v);
      } catch {
        throw new CastError("Number", v, "skip");
      }
    }
    this.options.skip = v;
    return this;
  };
  Query.prototype.projection = function(arg) {
    if (arguments.length === 0) {
      return this._fields;
    }
    this._fields = {};
    this._userProvidedFields = {};
    this.select(arg);
    return this._fields;
  };
  Query.prototype.select = function select() {
    let arg = arguments[0];
    if (!arg) return this;
    if (arguments.length !== 1) {
      throw new MongooseError("Invalid select: select only takes 1 argument");
    }
    this._validate("select");
    const fields = this._fields || (this._fields = {});
    const userProvidedFields = this._userProvidedFields || (this._userProvidedFields = {});
    let sanitizeProjection3 = void 0;
    if (this.model != null && utils2.hasUserDefinedProperty(this.model.db.options, "sanitizeProjection")) {
      sanitizeProjection3 = this.model.db.options.sanitizeProjection;
    } else if (this.model != null && utils2.hasUserDefinedProperty(this.model.base.options, "sanitizeProjection")) {
      sanitizeProjection3 = this.model.base.options.sanitizeProjection;
    } else {
      sanitizeProjection3 = this._mongooseOptions.sanitizeProjection;
    }
    function sanitizeValue(value) {
      return typeof value === "string" && sanitizeProjection3 ? value = 1 : value;
    }
    arg = parseProjection2(arg, true);
    if (utils2.isObject(arg)) {
      if (this.selectedInclusively()) {
        Object.entries(arg).forEach(([key, value]) => {
          if (value) {
            if (fields["-" + key] != null) {
              delete fields["-" + key];
            }
            fields[key] = userProvidedFields[key] = sanitizeValue(value);
          } else {
            Object.keys(userProvidedFields).forEach((field) => {
              if (isSubpath2(key, field)) {
                delete fields[field];
                delete userProvidedFields[field];
              }
            });
          }
        });
      } else if (this.selectedExclusively()) {
        Object.entries(arg).forEach(([key, value]) => {
          if (!value) {
            if (fields["+" + key] != null) {
              delete fields["+" + key];
            }
            fields[key] = userProvidedFields[key] = sanitizeValue(value);
          } else {
            Object.keys(userProvidedFields).forEach((field) => {
              if (isSubpath2(key, field)) {
                delete fields[field];
                delete userProvidedFields[field];
              }
            });
          }
        });
      } else {
        const keys = Object.keys(arg);
        for (let i = 0; i < keys.length; ++i) {
          const value = arg[keys[i]];
          const key = keys[i];
          fields[key] = sanitizeValue(value);
          userProvidedFields[key] = sanitizeValue(value);
        }
      }
      return this;
    }
    throw new TypeError("Invalid select() argument. Must be string or object.");
  };
  Query.prototype.schemaLevelProjections = function schemaLevelProjections(value) {
    this._mongooseOptions.schemaLevelProjections = value;
    return this;
  };
  Query.prototype.sanitizeProjection = function sanitizeProjection3(value) {
    this._mongooseOptions.sanitizeProjection = value;
    return this;
  };
  Query.prototype.read = function read(mode, tags) {
    if (typeof mode === "string") {
      mode = handleReadPreferenceAliases2(mode);
      this.options.readPreference = { mode, tags };
    } else {
      this.options.readPreference = mode;
    }
    return this;
  };
  Query.prototype.toString = function toString() {
    if (this.op === "count" || this.op === "countDocuments" || this.op === "find" || this.op === "findOne" || this.op === "deleteMany" || this.op === "deleteOne" || this.op === "findOneAndDelete" || this.op === "remove") {
      return `${this.model.modelName}.${this.op}(${util.inspect(this._conditions)})`;
    }
    if (this.op === "distinct") {
      return `${this.model.modelName}.distinct('${this._distinct}', ${util.inspect(this._conditions)})`;
    }
    if (this.op === "findOneAndReplace" || this.op === "findOneAndUpdate" || this.op === "replaceOne" || this.op === "update" || this.op === "updateMany" || this.op === "updateOne") {
      return `${this.model.modelName}.${this.op}(${util.inspect(this._conditions)}, ${util.inspect(this[queryUpdateSymbol])})`;
    }
    return `${this.model.modelName}.${this.op}()`;
  };
  Query.prototype.session = function session(v) {
    if (v == null) {
      delete this.options.session;
    }
    this.options.session = v;
    return this;
  };
  Query.prototype.writeConcern = function writeConcern(val) {
    if (val == null) {
      delete this.options.writeConcern;
      return this;
    }
    this.options.writeConcern = val;
    return this;
  };
  Query.prototype.w = function w(val) {
    if (val == null) {
      delete this.options.w;
    }
    if (this.options.writeConcern != null) {
      this.options.writeConcern.w = val;
    } else {
      this.options.w = val;
    }
    return this;
  };
  Query.prototype.j = function j(val) {
    if (val == null) {
      delete this.options.j;
    }
    if (this.options.writeConcern != null) {
      this.options.writeConcern.j = val;
    } else {
      this.options.j = val;
    }
    return this;
  };
  Query.prototype.wtimeout = function wtimeout(ms) {
    if (ms == null) {
      delete this.options.wtimeout;
    }
    if (this.options.writeConcern != null) {
      this.options.writeConcern.wtimeout = ms;
    } else {
      this.options.wtimeout = ms;
    }
    return this;
  };
  Query.prototype.getOptions = function() {
    return this.options;
  };
  Query.prototype.setOptions = function(options2, overwrite) {
    if (overwrite) {
      this._mongooseOptions = options2 && clone(options2) || {};
      this.options = options2 || {};
      if ("populate" in options2) {
        this.populate(this._mongooseOptions);
      }
      return this;
    }
    if (options2 == null) {
      return this;
    }
    if (typeof options2 !== "object") {
      throw new MongooseError('Options must be an object, got "' + options2 + '"');
    }
    options2 = Object.assign({}, options2);
    if (Array.isArray(options2.populate)) {
      const populate = options2.populate;
      delete options2.populate;
      const _numPopulate = populate.length;
      for (let i = 0; i < _numPopulate; ++i) {
        this.populate(populate[i]);
      }
    }
    if ("setDefaultsOnInsert" in options2) {
      this._mongooseOptions.setDefaultsOnInsert = options2.setDefaultsOnInsert;
      delete options2.setDefaultsOnInsert;
    }
    if ("overwriteDiscriminatorKey" in options2) {
      this._mongooseOptions.overwriteDiscriminatorKey = options2.overwriteDiscriminatorKey;
      delete options2.overwriteDiscriminatorKey;
    }
    if ("overwriteImmutable" in options2) {
      this._mongooseOptions.overwriteImmutable = options2.overwriteImmutable;
      delete options2.overwriteImmutable;
    }
    if ("updatePipeline" in options2) {
      this._mongooseOptions.updatePipeline = options2.updatePipeline;
      delete options2.updatePipeline;
    }
    if ("cloneUpdate" in options2) {
      this._mongooseOptions.cloneUpdate = options2.cloneUpdate;
      delete options2.cloneUpdate;
    }
    if ("sanitizeProjection" in options2) {
      if (options2.sanitizeProjection && !this._mongooseOptions.sanitizeProjection) {
        sanitizeProjection2(this._fields);
      }
      this._mongooseOptions.sanitizeProjection = options2.sanitizeProjection;
      delete options2.sanitizeProjection;
    }
    if ("sanitizeFilter" in options2) {
      this._mongooseOptions.sanitizeFilter = options2.sanitizeFilter;
      delete options2.sanitizeFilter;
    }
    if ("timestamps" in options2) {
      this._mongooseOptions.timestamps = options2.timestamps;
      delete options2.timestamps;
    }
    if ("defaults" in options2) {
      this._mongooseOptions.defaults = options2.defaults;
    }
    if ("translateAliases" in options2) {
      this._mongooseOptions.translateAliases = options2.translateAliases;
      delete options2.translateAliases;
    }
    if ("schemaLevelProjections" in options2) {
      this._mongooseOptions.schemaLevelProjections = options2.schemaLevelProjections;
      delete options2.schemaLevelProjections;
    }
    if (options2.lean == null && this.schema && "lean" in this.schema.options) {
      this._mongooseOptions.lean = this.schema.options.lean;
    }
    if (typeof options2.limit === "string") {
      try {
        options2.limit = castNumber(options2.limit);
      } catch {
        throw new CastError("Number", options2.limit, "limit");
      }
    }
    if (typeof options2.skip === "string") {
      try {
        options2.skip = castNumber(options2.skip);
      } catch {
        throw new CastError("Number", options2.skip, "skip");
      }
    }
    for (const key of Object.keys(options2)) {
      if (queryOptionMethods.has(key)) {
        const args = Array.isArray(options2[key]) ? options2[key] : [options2[key]];
        this[key].apply(this, args);
      } else {
        this.options[key] = options2[key];
      }
    }
    return this;
  };
  Query.prototype.explain = function explain(verbose) {
    if (arguments.length === 0) {
      this.options.explain = true;
    } else if (verbose === false) {
      delete this.options.explain;
    } else {
      this.options.explain = verbose;
    }
    return this;
  };
  Query.prototype.allowDiskUse = function(v) {
    if (arguments.length === 0) {
      this.options.allowDiskUse = true;
    } else if (v === false) {
      delete this.options.allowDiskUse;
    } else {
      this.options.allowDiskUse = v;
    }
    return this;
  };
  Query.prototype.maxTimeMS = function(ms) {
    this.options.maxTimeMS = ms;
    return this;
  };
  Query.prototype.getFilter = function() {
    return this._conditions;
  };
  Query.prototype.getQuery = function() {
    return this._conditions;
  };
  Query.prototype.setQuery = function(val) {
    this._conditions = val;
  };
  Query.prototype.getUpdate = function() {
    return this._update;
  };
  Query.prototype.setUpdate = function(val, cloneUpdate) {
    this[queryUpdateSymbol] = cloneUpdate === false ? val : clone(val);
    if (cloneUpdate != null) {
      this._mongooseOptions.cloneUpdate = cloneUpdate;
    }
    this._updateIsShared = false;
  };
  Query.prototype._fieldsForExec = function() {
    if (this._fields == null) {
      return null;
    }
    if (utils2.hasOwnKeys(this._fields) === false) {
      return null;
    }
    return clone(this._fields);
  };
  Query.prototype._updateForExec = function() {
    const update = clone(this[queryUpdateSymbol], {
      transform: false,
      depopulate: true
    });
    const ops = Object.keys(update);
    let i = ops.length;
    const ret = {};
    while (i--) {
      const op = ops[i];
      if ("$" !== op[0]) {
        if (!ret.$set) {
          if (update.$set) {
            ret.$set = update.$set;
          } else {
            ret.$set = {};
          }
        }
        ret.$set[op] = update[op];
        ops.splice(i, 1);
        if (!~ops.indexOf("$set")) ops.push("$set");
      } else if ("$set" === op) {
        if (!ret.$set) {
          ret[op] = update[op];
        }
      } else {
        ret[op] = update[op];
      }
    }
    return ret;
  };
  Query.prototype._optionsForExec = function(model2) {
    const options2 = clone(this.options);
    delete options2.populate;
    model2 = model2 || this.model;
    if (!model2) {
      return options2;
    }
    applyReadConcern2(model2.schema, options2);
    applyWriteConcern2(model2.schema, options2);
    const asyncLocalStorage = this.model?.db?.base.transactionAsyncLocalStorage?.getStore();
    if (!Object.hasOwn(this.options, "session") && asyncLocalStorage?.session != null) {
      options2.session = asyncLocalStorage.session;
    }
    const readPreference = model2?.schema?.options?.read;
    if (!("readPreference" in options2) && readPreference) {
      options2.readPreference = readPreference;
    }
    if (options2.upsert !== void 0) {
      options2.upsert = !!options2.upsert;
    }
    if (options2.writeConcern) {
      if (options2.j) {
        options2.writeConcern.j = options2.j;
        delete options2.j;
      }
      if (options2.w) {
        options2.writeConcern.w = options2.w;
        delete options2.w;
      }
      if (options2.wtimeout) {
        options2.writeConcern.wtimeout = options2.wtimeout;
        delete options2.wtimeout;
      }
    }
    this._applyPaths();
    if (this._fields != null) {
      this._fields = this._castFields(this._fields);
      const projection = this._fieldsForExec();
      if (projection != null) {
        options2.projection = projection;
      }
    }
    if (this._mongooseOptions.populate) {
      if (options2.readPreference) {
        for (const pop of Object.values(this._mongooseOptions.populate)) {
          if (pop.options?.readPreference === void 0) {
            if (!pop.options) {
              pop.options = {};
            }
            pop.options.readPreference = options2.readPreference;
          }
        }
      }
      if (options2.readConcern) {
        for (const pop of Object.values(this._mongooseOptions.populate)) {
          if (pop.options?.readConcern === void 0) {
            if (!pop.options) {
              pop.options = {};
            }
            pop.options.readConcern = options2.readConcern;
          }
        }
      }
    }
    return options2;
  };
  Query.prototype.lean = function(v) {
    this._mongooseOptions.lean = arguments.length ? v : true;
    return this;
  };
  Query.prototype.set = function(path, val) {
    _cloneUpdateIfShared(this);
    if (typeof path === "object") {
      const keys = Object.keys(path);
      for (const key of keys) {
        this.set(key, path[key]);
      }
      return this;
    }
    let update = this[queryUpdateSymbol];
    if (update == null) {
      update = {};
      this[queryUpdateSymbol] = update;
    }
    if (path in update) {
      delete update[path];
    }
    update.$set = update.$set || {};
    update.$set[path] = val;
    return this;
  };
  Query.prototype.get = function get2(path) {
    const update = this[queryUpdateSymbol];
    if (update == null) {
      return void 0;
    }
    const $set = update.$set;
    if ($set == null) {
      return update[path];
    }
    if (utils2.hasUserDefinedProperty(update, path)) {
      return update[path];
    }
    if (utils2.hasUserDefinedProperty($set, path)) {
      return $set[path];
    }
    return void 0;
  };
  Query.prototype.error = function error2(err) {
    if (arguments.length === 0) {
      return this._error;
    }
    this._error = err;
    return this;
  };
  Query.prototype._unsetCastError = function _unsetCastError() {
    if (this._error == null || !(this._error instanceof CastError)) {
      return;
    }
    return this.error(null);
  };
  Query.prototype.mongooseOptions = function(v) {
    if (arguments.length > 0) {
      this._mongooseOptions = v;
    }
    return this._mongooseOptions;
  };
  Query.prototype._castConditions = function() {
    let sanitizeFilterOpt = void 0;
    if (this.model?.db.options?.sanitizeFilter != null) {
      sanitizeFilterOpt = this.model.db.options.sanitizeFilter;
    } else if (this.model?.base.options?.sanitizeFilter != null) {
      sanitizeFilterOpt = this.model.base.options.sanitizeFilter;
    } else {
      sanitizeFilterOpt = this._mongooseOptions.sanitizeFilter;
    }
    if (sanitizeFilterOpt) {
      sanitizeFilter2(this._conditions);
    }
    try {
      this.cast(this.model);
      this._unsetCastError();
    } catch (err) {
      this.error(err);
    }
  };
  function _castArrayFilters(query2) {
    try {
      castArrayFilters2(query2);
    } catch (err) {
      query2.error(err);
    }
  }
  Query.prototype._find = async function _find() {
    this._applyTranslateAliases();
    this._castConditions();
    if (this.error() != null) {
      throw this.error();
    }
    const mongooseOptions = this._mongooseOptions;
    const userProvidedFields = this._userProvidedFields || {};
    const dbOptions = this.model.db.options;
    const baseOptions = this.model.base.options;
    applyGlobalMaxTimeMS(this.options, dbOptions, baseOptions);
    applyGlobalDiskUse(this.options, dbOptions, baseOptions);
    const completeManyOptions = {
      session: this?.options?.session || null,
      lean: mongooseOptions.lean || null
    };
    if (mongooseOptions.defaults != null) {
      completeManyOptions.defaults = mongooseOptions.defaults;
    }
    const options2 = this._optionsForExec();
    const filter = this._conditions;
    const fields = options2.projection;
    const cursor = await this.mongooseCollection.find(filter, options2);
    if (options2.explain) {
      return cursor.explain();
    }
    let docs = await cursor.toArray();
    if (docs.length === 0) {
      return docs;
    }
    if (!mongooseOptions.populate) {
      const versionKey = this.schema.options.versionKey;
      if (mongooseOptions.lean?.versionKey === false && versionKey) {
        docs.forEach((doc) => {
          if (versionKey in doc) {
            delete doc[versionKey];
          }
        });
      }
      return mongooseOptions.lean ? _completeManyLean(this.model.schema, docs, null, completeManyOptions) : this._completeMany(docs, fields, userProvidedFields, completeManyOptions);
    }
    const pop = helpers2.preparePopulationOptionsMQ(this, mongooseOptions);
    if (this.options?._deferPopulate) {
      docs = mongooseOptions.lean ? docs : await this._completeMany(docs, fields, userProvidedFields, completeManyOptions);
      this._deferredPopulate = pop;
      return docs;
    }
    if (mongooseOptions.lean) {
      return this.model.populate(docs, pop);
    }
    docs = await this._completeMany(docs, fields, userProvidedFields, completeManyOptions);
    await this.model.populate(docs, pop);
    return docs;
  };
  Query.prototype.find = function(conditions) {
    if (typeof conditions === "function" || typeof arguments[1] === "function") {
      throw new MongooseError("Query.prototype.find() no longer accepts a callback");
    }
    this.op = "find";
    if (canMerge(conditions)) {
      this.merge(conditions);
      prepareDiscriminatorCriteria(this);
    } else if (conditions != null) {
      this.error(new ObjectParameterError(conditions, "filter", "find"));
    }
    return this;
  };
  Query.prototype.merge = function(source) {
    if (!source) {
      if (source === null) {
        this._conditions = null;
      }
      return this;
    }
    this._conditions = this._conditions ?? {};
    const opts = { overwrite: true };
    if (source instanceof Query) {
      if (source._conditions) {
        opts.omit = {};
        if (this._conditions && this._conditions.$and && source._conditions.$and) {
          opts.omit["$and"] = true;
          this._conditions.$and = this._conditions.$and.concat(source._conditions.$and);
        }
        if (this._conditions && this._conditions.$or && source._conditions.$or) {
          opts.omit["$or"] = true;
          this._conditions.$or = this._conditions.$or.concat(source._conditions.$or);
        }
        utils2.merge(this._conditions, source._conditions, opts);
      }
      if (source._fields) {
        this._fields || (this._fields = {});
        utils2.merge(this._fields, source._fields, opts);
      }
      if (source.options) {
        this.options || (this.options = {});
        utils2.merge(this.options, source.options, opts);
      }
      if (source[queryUpdateSymbol] != null) {
        _cloneUpdateIfShared(this);
        this[queryUpdateSymbol] || (this[queryUpdateSymbol] = {});
        utils2.mergeClone(this[queryUpdateSymbol], source[queryUpdateSymbol]);
      }
      if (source._distinct) {
        this._distinct = source._distinct;
      }
      utils2.merge(this._mongooseOptions, source._mongooseOptions);
      return this;
    } else if (this.model != null && source instanceof this.model.base.Types.ObjectId) {
      utils2.merge(this._conditions, { _id: source }, opts);
      return this;
    } else if (source?.$__) {
      source = source.toObject(internalToObjectOptions);
    }
    opts.omit = {};
    if (Array.isArray(source.$and)) {
      opts.omit["$and"] = true;
      if (!this._conditions) {
        this._conditions = {};
      }
      this._conditions.$and = (this._conditions.$and || []).concat(
        source.$and.map((el) => utils2.isPOJO(el) ? utils2.merge({}, el) : el)
      );
    }
    if (Array.isArray(source.$or)) {
      opts.omit["$or"] = true;
      if (!this._conditions) {
        this._conditions = {};
      }
      this._conditions.$or = (this._conditions.$or || []).concat(
        source.$or.map((el) => utils2.isPOJO(el) ? utils2.merge({}, el) : el)
      );
    }
    utils2.merge(this._conditions, source, opts);
    return this;
  };
  Query.prototype.collation = function(value) {
    if (this.options == null) {
      this.options = {};
    }
    this.options.collation = value;
    return this;
  };
  Query.prototype._completeOne = function(doc, res, projection, callback) {
    if (!doc && !this.options.includeResultMetadata) {
      return callback(null, null);
    }
    const model2 = this.model;
    const userProvidedFields = this._userProvidedFields || {};
    const mongooseOptions = this._mongooseOptions;
    const options2 = this.options;
    if (!options2.lean && mongooseOptions.lean) {
      options2.lean = mongooseOptions.lean;
    }
    if (options2.explain) {
      return callback(null, doc);
    }
    if (!mongooseOptions.populate) {
      const versionKey = this.schema.options.versionKey;
      if (mongooseOptions.lean?.versionKey === false && versionKey) {
        if (versionKey in doc) {
          delete doc[versionKey];
        }
      }
      return mongooseOptions.lean ? _completeOneLean(model2.schema, doc, null, res, options2, callback) : completeOne(
        model2,
        doc,
        res,
        options2,
        projection,
        userProvidedFields,
        null,
        callback
      );
    }
    const pop = helpers2.preparePopulationOptionsMQ(this, this._mongooseOptions);
    if (mongooseOptions.lean) {
      return model2.populate(doc, pop).then(
        (doc2) => {
          _completeOneLean(model2.schema, doc2, null, res, options2, callback);
        },
        (error2) => {
          callback(error2);
        }
      );
    }
    completeOne(model2, doc, res, options2, projection, userProvidedFields, [], (err, doc2) => {
      if (err != null) {
        return callback(err);
      }
      model2.populate(doc2, pop).then((res2) => {
        callback(null, res2);
      }, (err2) => {
        callback(err2);
      });
    });
  };
  Query.prototype._completeMany = async function _completeMany(docs, fields, userProvidedFields, opts) {
    const model2 = this.model;
    return Promise.all(docs.map((doc) => new Promise((resolve, reject) => {
      const rawDoc = doc;
      doc = helpers2.createModel(model2, doc, fields, userProvidedFields, opts);
      if (opts.session != null) {
        doc.$session(opts.session);
      }
      doc.$init(rawDoc, opts, (err) => {
        if (err != null) {
          return reject(err);
        }
        resolve(doc);
      });
    })));
  };
  Query.prototype._findOne = async function _findOne() {
    this._applyTranslateAliases();
    this._castConditions();
    if (this.error()) {
      const err = this.error();
      throw err;
    }
    const dbOptions = this.model.db.options;
    const baseOptions = this.model.base.options;
    applyGlobalMaxTimeMS(this.options, dbOptions, baseOptions);
    applyGlobalDiskUse(this.options, dbOptions, baseOptions);
    const options2 = this._optionsForExec();
    const doc = await this.mongooseCollection.findOne(this._conditions, options2);
    return new Promise((resolve, reject) => {
      this._completeOne(doc, null, options2.projection, (err, res) => {
        if (err) {
          return reject(err);
        }
        resolve(res);
      });
    });
  };
  Query.prototype.findOne = function(conditions, projection, options2) {
    if (typeof conditions === "function" || typeof projection === "function" || typeof options2 === "function" || typeof arguments[3] === "function") {
      throw new MongooseError("Query.prototype.findOne() no longer accepts a callback");
    }
    this.op = "findOne";
    if (options2) {
      this.setOptions(options2);
    }
    if (projection) {
      this.select(projection);
    }
    if (canMerge(conditions)) {
      this.merge(conditions);
      prepareDiscriminatorCriteria(this);
    } else if (conditions != null) {
      this.error(new ObjectParameterError(conditions, "filter", "findOne"));
    }
    return this;
  };
  Query.prototype._countDocuments = async function _countDocuments() {
    this._applyTranslateAliases();
    this._castConditions();
    if (this.error()) {
      throw this.error();
    }
    const dbOptions = this.model.db.options;
    const baseOptions = this.model.base.options;
    applyGlobalMaxTimeMS(this.options, dbOptions, baseOptions);
    applyGlobalDiskUse(this.options, dbOptions, baseOptions);
    const options2 = this._optionsForExec();
    const conds = this._conditions;
    return this.mongooseCollection.countDocuments(conds, options2);
  };
  Query.prototype._applyTranslateAliases = function _applyTranslateAliases() {
    let applyTranslateAliases = false;
    if ("translateAliases" in this._mongooseOptions) {
      applyTranslateAliases = this._mongooseOptions.translateAliases;
    } else if (this.model?.schema?._userProvidedOptions?.translateAliases != null) {
      applyTranslateAliases = this.model.schema._userProvidedOptions.translateAliases;
    } else if (this.model?.base?.options?.translateAliases != null) {
      applyTranslateAliases = this.model.base.options.translateAliases;
    }
    if (!applyTranslateAliases) {
      return;
    }
    if (this.model?.schema?.aliases && utils2.hasOwnKeys(this.model.schema.aliases)) {
      _cloneUpdateIfShared(this);
      this.model.translateAliases(this._conditions, true);
      this.model.translateAliases(this._fields, true);
      this.model.translateAliases(this[queryUpdateSymbol], true);
      if (this._distinct != null && this.model.schema.aliases[this._distinct] != null) {
        this._distinct = this.model.schema.aliases[this._distinct];
      }
    }
  };
  Query.prototype._estimatedDocumentCount = async function _estimatedDocumentCount() {
    if (this.error()) {
      throw this.error();
    }
    const options2 = this._optionsForExec();
    return this.mongooseCollection.estimatedDocumentCount(options2);
  };
  Query.prototype.estimatedDocumentCount = function(options2) {
    if (typeof options2 === "function" || typeof arguments[1] === "function") {
      throw new MongooseError("Query.prototype.estimatedDocumentCount() no longer accepts a callback");
    }
    this.op = "estimatedDocumentCount";
    if (options2 != null) {
      this.setOptions(options2);
    }
    return this;
  };
  Query.prototype.countDocuments = function(conditions, options2) {
    if (typeof conditions === "function" || typeof options2 === "function" || typeof arguments[2] === "function") {
      throw new MongooseError("Query.prototype.countDocuments() no longer accepts a callback");
    }
    this.op = "countDocuments";
    if (canMerge(conditions)) {
      this.merge(conditions);
    }
    if (options2 != null) {
      this.setOptions(options2);
    }
    return this;
  };
  Query.prototype.__distinct = async function __distinct() {
    this._applyTranslateAliases();
    this._castConditions();
    if (this.error()) {
      throw this.error();
    }
    const dbOptions = this.model.db.options;
    const baseOptions = this.model.base.options;
    applyGlobalMaxTimeMS(this.options, dbOptions, baseOptions);
    applyGlobalDiskUse(this.options, dbOptions, baseOptions);
    const options2 = this._optionsForExec();
    return this.mongooseCollection.distinct(this._distinct, this._conditions, options2);
  };
  Query.prototype.distinct = function(field, conditions, options2) {
    if (typeof field === "function" || typeof conditions === "function" || typeof options2 === "function" || typeof arguments[3] === "function") {
      throw new MongooseError("Query.prototype.distinct() no longer accepts a callback");
    }
    this.op = "distinct";
    if (canMerge(conditions)) {
      this.merge(conditions);
      prepareDiscriminatorCriteria(this);
    } else if (conditions != null) {
      this.error(new ObjectParameterError(conditions, "filter", "distinct"));
    }
    if (field != null) {
      this._distinct = field;
    }
    if (options2 != null) {
      this.setOptions(options2);
    }
    return this;
  };
  Query.prototype.sort = function(arg, options2) {
    if (arguments.length > 2) {
      throw new MongooseError("sort() takes at most 2 arguments");
    }
    if (options2 != null && typeof options2 !== "object") {
      throw new MongooseError("sort() options argument must be an object or nullish");
    }
    if (this.options.sort == null) {
      this.options.sort = {};
    }
    if (options2?.override) {
      this.options.sort = {};
    }
    const sort = this.options.sort;
    if (typeof arg === "string") {
      const properties = arg.indexOf(" ") === -1 ? [arg] : arg.split(" ");
      for (let property of properties) {
        const ascend = "-" == property[0] ? -1 : 1;
        if (ascend === -1) {
          property = property.slice(1);
        }
        if (specialProperties2.has(property)) {
          continue;
        }
        sort[property] = ascend;
      }
    } else if (Array.isArray(arg)) {
      for (const pair of arg) {
        if (!Array.isArray(pair)) {
          throw new TypeError("Invalid sort() argument, must be array of arrays");
        }
        const key = "" + pair[0];
        if (specialProperties2.has(key)) {
          continue;
        }
        sort[key] = _handleSortValue(pair[1], key);
      }
    } else if (typeof arg === "object" && arg != null && !(arg instanceof Map)) {
      for (const key of Object.keys(arg)) {
        if (specialProperties2.has(key)) {
          continue;
        }
        sort[key] = _handleSortValue(arg[key], key);
      }
    } else if (arg instanceof Map) {
      for (let key of arg.keys()) {
        key = "" + key;
        if (specialProperties2.has(key)) {
          continue;
        }
        sort[key] = _handleSortValue(arg.get(key), key);
      }
    } else if (arg != null) {
      throw new TypeError("Invalid sort() argument. Must be a string, object, array, or map.");
    }
    return this;
  };
  function _handleSortValue(val, key) {
    if (val === 1 || val === "asc" || val === "ascending") {
      return 1;
    }
    if (val === -1 || val === "desc" || val === "descending") {
      return -1;
    }
    if (val?.$meta != null) {
      return { $meta: val.$meta };
    }
    throw new TypeError("Invalid sort value: { " + key + ": " + val + " }");
  }
  Query.prototype.deleteOne = function deleteOne(filter, options2) {
    if (typeof filter === "function" || typeof options2 === "function" || typeof arguments[2] === "function") {
      throw new MongooseError("Query.prototype.deleteOne() no longer accepts a callback");
    }
    this.op = "deleteOne";
    this.setOptions(options2);
    if (canMerge(filter)) {
      this.merge(filter);
      prepareDiscriminatorCriteria(this);
    } else if (filter != null) {
      this.error(new ObjectParameterError(filter, "filter", "deleteOne"));
    }
    return this;
  };
  Query.prototype._deleteOne = async function _deleteOne() {
    this._applyTranslateAliases();
    this._castConditions();
    checkRequireFilter(this._conditions, this.options);
    if (this.error() != null) {
      throw this.error();
    }
    const options2 = this._optionsForExec();
    return this.mongooseCollection.deleteOne(this._conditions, options2);
  };
  Query.prototype.deleteMany = function(filter, options2) {
    if (typeof filter === "function" || typeof options2 === "function" || typeof arguments[2] === "function") {
      throw new MongooseError("Query.prototype.deleteMany() no longer accepts a callback");
    }
    this.setOptions(options2);
    this.op = "deleteMany";
    if (canMerge(filter)) {
      this.merge(filter);
      prepareDiscriminatorCriteria(this);
    } else if (filter != null) {
      this.error(new ObjectParameterError(filter, "filter", "deleteMany"));
    }
    return this;
  };
  Query.prototype._deleteMany = async function _deleteMany() {
    this._applyTranslateAliases();
    this._castConditions();
    checkRequireFilter(this._conditions, this.options);
    if (this.error() != null) {
      throw this.error();
    }
    const options2 = this._optionsForExec();
    return this.mongooseCollection.deleteMany(this._conditions, options2);
  };
  function completeOne(model2, doc, res, options2, fields, userProvidedFields, pop, callback) {
    if (options2.includeResultMetadata && doc == null) {
      _init(null);
      return null;
    }
    helpers2.createModelAndInit(model2, doc, fields, userProvidedFields, options2, pop, _init);
    function _init(err, casted) {
      if (err) {
        return callback(err);
      }
      if (options2.includeResultMetadata) {
        if (doc && casted) {
          if (options2.session != null) {
            casted.$session(options2.session);
          }
          res.value = casted;
        } else {
          res.value = null;
        }
        return callback(null, res);
      }
      if (options2.session != null) {
        casted.$session(options2.session);
      }
      callback(null, casted);
    }
  }
  function prepareDiscriminatorCriteria(query2) {
    if (!query2?.model?.schema) {
      return;
    }
    const schema2 = query2.model.schema;
    if (schema2?.discriminatorMapping && !schema2.discriminatorMapping.isRoot) {
      query2._conditions[schema2.discriminatorMapping.key] = schema2.discriminatorMapping.value;
    }
  }
  Query.prototype.findOneAndUpdate = function(filter, update, options2) {
    if (typeof filter === "function" || typeof update === "function" || typeof options2 === "function" || typeof arguments[3] === "function") {
      throw new MongooseError("Query.prototype.findOneAndUpdate() no longer accepts a callback");
    }
    this.op = "findOneAndUpdate";
    this._validate();
    switch (arguments.length) {
      case 2:
        options2 = void 0;
        break;
      case 1:
        update = filter;
        filter = options2 = void 0;
        break;
    }
    if (canMerge(filter)) {
      this.merge(filter);
    } else if (filter != null) {
      this.error(
        new ObjectParameterError(filter, "filter", "findOneAndUpdate")
      );
    }
    options2 = options2 ? clone(options2) : {};
    if (options2.projection) {
      this.select(options2.projection);
      delete options2.projection;
    }
    if (options2.fields) {
      this.select(options2.fields);
      delete options2.fields;
    }
    const globalReturnDocument = this?.model?.base?.options?.returnDocument;
    const globalReturnOriginal = this?.model?.base?.options?.returnOriginal;
    if (options2.new == null && options2.returnDocument == null && options2.returnOriginal == null) {
      if (globalReturnDocument != null) {
        options2.returnDocument = globalReturnDocument;
      } else if (globalReturnOriginal != null) {
        options2.returnOriginal = globalReturnOriginal;
      }
    }
    const updatePipeline = this?.model?.base?.options?.updatePipeline;
    if (options2.updatePipeline == null && updatePipeline != null) {
      options2.updatePipeline = updatePipeline;
    }
    if (!options2.updatePipeline && Array.isArray(update)) {
      throw new MongooseError("Cannot pass an array to query updates unless the `updatePipeline` option is set.");
    }
    this.setOptions(options2);
    if (update) {
      if (this[queryUpdateSymbol] == null || utils2.isEmptyObject(this[queryUpdateSymbol])) {
        this[queryUpdateSymbol] = update;
        this._updateIsShared = true;
      } else {
        this._mergeUpdate(update);
      }
    }
    return this;
  };
  Query.prototype._findOneAndUpdate = async function _findOneAndUpdate() {
    this._applyTranslateAliases();
    this._castConditions();
    checkRequireFilter(this._conditions, this.options);
    _castArrayFilters(this);
    if (this.error()) {
      throw this.error();
    }
    const dbOptions = this.model.db.options;
    const baseOptions = this.model.base.options;
    applyGlobalMaxTimeMS(this.options, dbOptions, baseOptions);
    applyGlobalDiskUse(this.options, dbOptions, baseOptions);
    if ("strict" in this.options) {
      this._mongooseOptions.strict = this.options.strict;
    }
    const options2 = this._optionsForExec(this.model);
    convertNewToReturnDocument(options2);
    this[queryUpdateSymbol] = this._castUpdate(this[queryUpdateSymbol]);
    this._updateIsShared = false;
    decorateUpdateWithVersionKey2(this[queryUpdateSymbol], options2, this.schema.options.versionKey);
    this[queryUpdateSymbol] = setDefaultsOnInsert2(
      this._conditions,
      this.model.schema,
      this[queryUpdateSymbol],
      options2,
      this._mongooseOptions,
      this
    );
    if (!this[queryUpdateSymbol] || utils2.hasOwnKeys(this[queryUpdateSymbol]) === false) {
      if (options2.upsert) {
        const $set = clone(this[queryUpdateSymbol]);
        delete $set._id;
        this[queryUpdateSymbol] = { $set };
      } else {
        this._execCount = 0;
        const res2 = await this._findOne();
        return res2;
      }
    } else if (this[queryUpdateSymbol] instanceof Error) {
      throw this[queryUpdateSymbol];
    } else {
      if (this[queryUpdateSymbol].$set && utils2.hasOwnKeys(this[queryUpdateSymbol].$set) === false) {
        delete this[queryUpdateSymbol].$set;
      }
    }
    const runValidators = _getOption(this, "runValidators", false);
    if (runValidators) {
      await this.validate(this[queryUpdateSymbol], options2, false);
    }
    if (typeof this[queryUpdateSymbol].toBSON === "function") {
      this[queryUpdateSymbol] = this[queryUpdateSymbol].toBSON();
    }
    let res = await this.mongooseCollection.findOneAndUpdate(this._conditions, this[queryUpdateSymbol], options2);
    for (const fn of this._transforms) {
      res = fn(res);
    }
    const doc = !options2.includeResultMetadata ? res : res.value;
    return new Promise((resolve, reject) => {
      this._completeOne(doc, res, options2.projection, (err, res2) => {
        if (err) {
          return reject(err);
        }
        resolve(res2);
      });
    });
  };
  Query.prototype.findOneAndDelete = function(filter, options2) {
    if (typeof filter === "function" || typeof options2 === "function" || typeof arguments[2] === "function") {
      throw new MongooseError("Query.prototype.findOneAndDelete() no longer accepts a callback");
    }
    this.op = "findOneAndDelete";
    this._validate();
    if (canMerge(filter)) {
      this.merge(filter);
    }
    options2 && this.setOptions(options2);
    return this;
  };
  Query.prototype._findOneAndDelete = async function _findOneAndDelete() {
    this._applyTranslateAliases();
    this._castConditions();
    checkRequireFilter(this._conditions, this.options);
    if (this.error() != null) {
      throw this.error();
    }
    const includeResultMetadata = this.options.includeResultMetadata;
    const filter = this._conditions;
    const options2 = this._optionsForExec(this.model);
    let res = await this.mongooseCollection.findOneAndDelete(filter, options2);
    for (const fn of this._transforms) {
      res = fn(res);
    }
    const doc = !includeResultMetadata ? res : res.value;
    return new Promise((resolve, reject) => {
      this._completeOne(doc, res, options2.projection, (err, res2) => {
        if (err) {
          return reject(err);
        }
        resolve(res2);
      });
    });
  };
  Query.prototype.findOneAndReplace = function(filter, replacement, options2) {
    if (typeof filter === "function" || typeof replacement === "function" || typeof options2 === "function" || typeof arguments[4] === "function") {
      throw new MongooseError("Query.prototype.findOneAndReplace() no longer accepts a callback");
    }
    this.op = "findOneAndReplace";
    this._validate();
    if (canMerge(filter)) {
      this.merge(filter);
    } else if (filter != null) {
      this.error(
        new ObjectParameterError(filter, "filter", "findOneAndReplace")
      );
    }
    if (replacement != null) {
      this._mergeUpdate(replacement);
    }
    options2 = options2 || {};
    const globalReturnDocument = this?.model?.base?.options?.returnDocument;
    const globalReturnOriginal = this?.model?.base?.options?.returnOriginal;
    if (options2.new == null && options2.returnDocument == null && options2.returnOriginal == null) {
      if (globalReturnDocument != null) {
        options2.returnDocument = globalReturnDocument;
      } else if (globalReturnOriginal != null) {
        options2.returnOriginal = globalReturnOriginal;
      }
    }
    this.setOptions(options2);
    return this;
  };
  Query.prototype._findOneAndReplace = async function _findOneAndReplace() {
    this._applyTranslateAliases();
    this._castConditions();
    checkRequireFilter(this._conditions, this.options);
    if (this.error() != null) {
      throw this.error();
    }
    if ("strict" in this.options) {
      this._mongooseOptions.strict = this.options.strict;
      delete this.options.strict;
    }
    const filter = this._conditions;
    const options2 = this._optionsForExec();
    convertNewToReturnDocument(options2);
    const includeResultMetadata = this.options.includeResultMetadata;
    const modelOpts = { skipId: true };
    if ("strict" in this._mongooseOptions) {
      modelOpts.strict = this._mongooseOptions.strict;
    }
    const runValidators = _getOption(this, "runValidators", false);
    try {
      const update = new this.model(this[queryUpdateSymbol], null, modelOpts);
      if (runValidators) {
        await update.validate();
      } else if (update.$__.validationError) {
        throw update.$__.validationError;
      }
      this[queryUpdateSymbol] = update.toBSON();
    } catch (err) {
      if (err instanceof ValidationError) {
        throw err;
      }
      const validationError = new ValidationError();
      validationError.errors[err.path] = err;
      throw validationError;
    }
    let res = await this.mongooseCollection.findOneAndReplace(filter, this[queryUpdateSymbol], options2);
    for (const fn of this._transforms) {
      res = fn(res);
    }
    const doc = !includeResultMetadata ? res : res.value;
    return new Promise((resolve, reject) => {
      this._completeOne(doc, res, options2.projection, (err, res2) => {
        if (err) {
          return reject(err);
        }
        resolve(res2);
      });
    });
  };
  Query.prototype.findById = function(id, projection, options2) {
    return this.findOne({ _id: id }, projection, options2);
  };
  Query.prototype.findByIdAndUpdate = function(id, update, options2) {
    return this.findOneAndUpdate({ _id: id }, update, options2);
  };
  Query.prototype.findByIdAndDelete = function(id, options2) {
    return this.findOneAndDelete({ _id: id }, options2);
  };
  function convertNewToReturnDocument(options2) {
    if ("new" in options2) {
      const replacement = options2["new"] ? "'after'" : "'before'";
      utils2.warn(
        "mongoose: the `new` option for `findOneAndUpdate()` and `findOneAndReplace()` is deprecated. Use `returnDocument: " + replacement + "` instead."
      );
      options2.returnDocument = options2["new"] ? "after" : "before";
      delete options2["new"];
    }
    if ("returnOriginal" in options2) {
      const replacement = options2["returnOriginal"] ? "'before'" : "'after'";
      utils2.warn(
        "mongoose: the `returnOriginal` option for `findOneAndUpdate()` and `findOneAndReplace()` is deprecated. Use `returnDocument: " + replacement + "` instead."
      );
      options2.returnDocument = options2["returnOriginal"] ? "before" : "after";
      delete options2["returnOriginal"];
    }
    if (typeof options2.returnDocument === "string") {
      options2.returnOriginal = options2.returnDocument === "before";
    }
  }
  function _getOption(query2, option, def) {
    const opts = query2._optionsForExec(query2.model);
    if (option in opts) {
      return opts[option];
    }
    if (option in query2.model.base.options) {
      return query2.model.base.options[option];
    }
    return def;
  }
  function _completeOneLean(schema2, doc, path, res, opts, callback) {
    if (opts.lean && typeof opts.lean.transform === "function") {
      opts.lean.transform(doc);
      for (let i = 0; i < schema2.childSchemas.length; i++) {
        const childPath = path ? path + "." + schema2.childSchemas[i].model.path : schema2.childSchemas[i].model.path;
        const _schema = schema2.childSchemas[i].schema;
        const obj = mpath.get(childPath, doc);
        if (obj == null) {
          continue;
        }
        if (Array.isArray(obj)) {
          for (let i2 = 0; i2 < obj.length; i2++) {
            opts.lean.transform(obj[i2]);
          }
        } else {
          opts.lean.transform(obj);
        }
        _completeOneLean(_schema, obj, childPath, res, opts);
      }
      if (callback) {
        return callback(null, doc);
      } else {
        return;
      }
    }
    if (opts.includeResultMetadata) {
      return callback(null, res);
    }
    return callback(null, doc);
  }
  function _completeManyLean(schema2, docs, path, opts) {
    if (opts.lean && typeof opts.lean.transform === "function") {
      for (const doc of docs) {
        opts.lean.transform(doc);
      }
      for (let i = 0; i < schema2.childSchemas.length; i++) {
        const childPath = path ? path + "." + schema2.childSchemas[i].model.path : schema2.childSchemas[i].model.path;
        const _schema = schema2.childSchemas[i].schema;
        let doc = mpath.get(childPath, docs);
        if (doc == null) {
          continue;
        }
        doc = doc.flat();
        for (let i2 = 0; i2 < doc.length; i2++) {
          opts.lean.transform(doc[i2]);
        }
        _completeManyLean(_schema, doc, childPath, opts);
      }
    }
    return docs;
  }
  Query.prototype._mergeUpdate = function(update) {
    _cloneUpdateIfShared(this);
    const updatePipeline = this._mongooseOptions.updatePipeline;
    if (!updatePipeline && Array.isArray(update)) {
      throw new MongooseError("Cannot pass an array to query updates unless the `updatePipeline` option is set.");
    }
    if (!this[queryUpdateSymbol]) {
      this[queryUpdateSymbol] = Array.isArray(update) ? [] : {};
    }
    if (update == null || typeof update === "object" && utils2.hasOwnKeys(update) === false) {
      return;
    }
    if (update instanceof Query) {
      if (Array.isArray(this[queryUpdateSymbol])) {
        throw new MongooseError(`Cannot mix array and object updates (current: ${_previewUpdate(this[queryUpdateSymbol])}, incoming: ${_previewUpdate(update[queryUpdateSymbol])})`);
      }
      if (update[queryUpdateSymbol]) {
        utils2.mergeClone(this[queryUpdateSymbol], update[queryUpdateSymbol]);
      }
    } else if (Array.isArray(update)) {
      if (!Array.isArray(this[queryUpdateSymbol])) {
        if (this[queryUpdateSymbol] == null || utils2.isEmptyObject(this[queryUpdateSymbol])) {
          this[queryUpdateSymbol] = [];
        } else {
          throw new MongooseError(`Cannot mix array and object updates (current: ${_previewUpdate(this[queryUpdateSymbol])}, incoming: ${_previewUpdate(update)})`);
        }
      }
      this[queryUpdateSymbol] = this[queryUpdateSymbol].concat(update);
    } else {
      if (Array.isArray(this[queryUpdateSymbol])) {
        throw new MongooseError(`Cannot mix array and object updates (current: ${_previewUpdate(this[queryUpdateSymbol])}, incoming: ${_previewUpdate(update)})`);
      }
      utils2.mergeClone(this[queryUpdateSymbol], update);
    }
  };
  function _previewUpdate(update) {
    const preview = util.inspect(update, {
      depth: 2,
      maxArrayLength: 5,
      breakLength: 80,
      compact: true
    });
    if (preview.length > 200) {
      return `${preview.slice(0, 197)}...`;
    }
    return preview;
  }
  Query.prototype.validate = async function validate(castedDoc, options2, isOverwriting) {
    if (typeof arguments[3] === "function") {
      throw new MongooseError("Query.prototype.validate() no longer accepts a callback");
    }
    await _executePreHooks(this, "validate");
    if (isOverwriting) {
      await castedDoc.$validate();
    } else {
      const validationErrors = await updateValidators2(this, this.model.schema, castedDoc, options2);
      if (validationErrors.length > 0) {
        const err = new ValidationError(null);
        for (const validationError of validationErrors) {
          err.addError(validationError.path, validationError);
        }
        throw err;
      }
    }
    await _executePostHooks(this, null, null, "validate");
  };
  Query.prototype._updateMany = async function _updateMany() {
    this._applyTranslateAliases();
    this._castConditions();
    checkRequireFilter(this._conditions, this.options);
    _castArrayFilters(this);
    if (this.error() != null) {
      throw this.error();
    }
    const options2 = this._optionsForExec(this.model);
    _cloneUpdateIfShared(this);
    this[queryUpdateSymbol] = this._castUpdate(this[queryUpdateSymbol]);
    if (this[queryUpdateSymbol] == null || utils2.hasOwnKeys(this[queryUpdateSymbol]) === false) {
      return { acknowledged: false };
    }
    removeUnusedArrayFilters2(this[queryUpdateSymbol], options2);
    this[queryUpdateSymbol] = setDefaultsOnInsert2(
      this._conditions,
      this.model.schema,
      this[queryUpdateSymbol],
      options2,
      this._mongooseOptions,
      this
    );
    if (_getOption(this, "runValidators", false)) {
      await this.validate(this[queryUpdateSymbol], options2, false);
    }
    if (typeof this[queryUpdateSymbol].toBSON === "function") {
      this[queryUpdateSymbol] = this[queryUpdateSymbol].toBSON();
    }
    return this.mongooseCollection.updateMany(this._conditions, this[queryUpdateSymbol], options2);
  };
  Query.prototype._updateOne = async function _updateOne() {
    this._applyTranslateAliases();
    this._castConditions();
    checkRequireFilter(this._conditions, this.options);
    _castArrayFilters(this);
    if (this.error() != null) {
      throw this.error();
    }
    const options2 = this._optionsForExec(this.model);
    _cloneUpdateIfShared(this);
    this[queryUpdateSymbol] = this._castUpdate(this[queryUpdateSymbol]);
    if (this[queryUpdateSymbol] == null || utils2.hasOwnKeys(this[queryUpdateSymbol]) === false) {
      return { acknowledged: false };
    }
    removeUnusedArrayFilters2(this[queryUpdateSymbol], options2);
    this[queryUpdateSymbol] = setDefaultsOnInsert2(
      this._conditions,
      this.model.schema,
      this[queryUpdateSymbol],
      options2,
      this._mongooseOptions,
      this
    );
    if (_getOption(this, "runValidators", false)) {
      await this.validate(this[queryUpdateSymbol], options2, false);
    }
    if (typeof this[queryUpdateSymbol].toBSON === "function") {
      this[queryUpdateSymbol] = this[queryUpdateSymbol].toBSON();
    }
    return this.mongooseCollection.updateOne(this._conditions, this[queryUpdateSymbol], options2);
  };
  Query.prototype._replaceOne = async function _replaceOne() {
    this._applyTranslateAliases();
    this._castConditions();
    checkRequireFilter(this._conditions, this.options);
    _castArrayFilters(this);
    if (this.error() != null) {
      throw this.error();
    }
    const options2 = this._optionsForExec(this.model);
    this[queryUpdateSymbol] = new this.model(this[queryUpdateSymbol], null, { skipId: true });
    removeUnusedArrayFilters2(this[queryUpdateSymbol], options2);
    if (_getOption(this, "runValidators", false)) {
      await this.validate(this[queryUpdateSymbol], options2, true);
    }
    if (typeof this[queryUpdateSymbol].toBSON === "function") {
      this[queryUpdateSymbol] = this[queryUpdateSymbol].toBSON();
    }
    return this.mongooseCollection.replaceOne(this._conditions, this[queryUpdateSymbol], options2);
  };
  Query.prototype.updateMany = function(conditions, doc, options2, callback) {
    if (typeof options2 === "function") {
      callback = options2;
      options2 = void 0;
    } else if (typeof doc === "function") {
      callback = doc;
      doc = conditions;
      conditions = {};
      options2 = void 0;
    } else if (typeof conditions === "function") {
      callback = conditions;
      conditions = void 0;
      doc = void 0;
      options2 = void 0;
    } else if (typeof conditions === "object" && !doc && !options2 && !callback) {
      doc = conditions;
      conditions = void 0;
      options2 = void 0;
      callback = void 0;
    }
    return _update(this, "updateMany", conditions, doc, options2, callback);
  };
  Query.prototype.updateOne = function(conditions, doc, options2, callback) {
    if (typeof options2 === "function") {
      callback = options2;
      options2 = void 0;
    } else if (typeof doc === "function") {
      callback = doc;
      doc = conditions;
      conditions = {};
      options2 = void 0;
    } else if (typeof conditions === "function") {
      callback = conditions;
      conditions = void 0;
      doc = void 0;
      options2 = void 0;
    } else if (typeof conditions === "object" && !doc && !options2 && !callback) {
      doc = conditions;
      conditions = void 0;
      options2 = void 0;
      callback = void 0;
    }
    return _update(this, "updateOne", conditions, doc, options2, callback);
  };
  Query.prototype.replaceOne = function(conditions, doc, options2, callback) {
    if (typeof options2 === "function") {
      callback = options2;
      options2 = void 0;
    } else if (typeof doc === "function") {
      callback = doc;
      doc = conditions;
      conditions = {};
      options2 = void 0;
    } else if (typeof conditions === "function") {
      callback = conditions;
      conditions = void 0;
      doc = void 0;
      options2 = void 0;
    } else if (typeof conditions === "object" && !doc && !options2 && !callback) {
      doc = conditions;
      conditions = void 0;
      options2 = void 0;
      callback = void 0;
    }
    return _update(this, "replaceOne", conditions, doc, options2, callback);
  };
  function _update(query2, op, filter, doc, options2, callback) {
    query2.op = op;
    doc = doc || {};
    if (options2 != null) {
      if ("strict" in options2) {
        query2._mongooseOptions.strict = options2.strict;
      }
    }
    if (!(filter instanceof Query) && filter != null && filter.toString() !== "[object Object]") {
      query2.error(new ObjectParameterError(filter, "filter", op));
    } else {
      query2.merge(filter);
    }
    const updatePipeline = query2?.model?.base?.options?.updatePipeline;
    if (updatePipeline != null && options2?.updatePipeline == null) {
      options2 = options2 || {};
      options2.updatePipeline = updatePipeline;
    }
    if (!options2?.updatePipeline && Array.isArray(doc)) {
      throw new MongooseError("Cannot pass an array to query updates unless the `updatePipeline` option is set.");
    }
    if (utils2.isObject(options2)) {
      query2.setOptions(options2);
    }
    if (query2[queryUpdateSymbol] == null || utils2.isEmptyObject(query2[queryUpdateSymbol])) {
      query2[queryUpdateSymbol] = doc;
      query2._updateIsShared = true;
    } else {
      query2._mergeUpdate(doc);
    }
    if (callback) {
      query2.exec(callback);
      return query2;
    }
    return query2;
  }
  Query.prototype.transform = function(fn) {
    this._transforms.push(fn);
    return this;
  };
  Query.prototype.orFail = function(err) {
    this.transform((res) => {
      switch (this.op) {
        case "find":
          if (res.length === 0) {
            throw _orFailError(err, this);
          }
          break;
        case "findOne":
          if (res == null) {
            throw _orFailError(err, this);
          }
          break;
        case "replaceOne":
        case "updateMany":
        case "updateOne":
          if (res?.matchedCount === 0) {
            throw _orFailError(err, this);
          }
          break;
        case "findOneAndDelete":
        case "findOneAndUpdate":
        case "findOneAndReplace":
          if (this.options.includeResultMetadata && res != null && res.value == null) {
            throw _orFailError(err, this);
          }
          if (!this.options.includeResultMetadata && res == null) {
            throw _orFailError(err, this);
          }
          break;
        case "deleteMany":
        case "deleteOne":
          if (res.deletedCount === 0) {
            throw _orFailError(err, this);
          }
          break;
      }
      return res;
    });
    return this;
  };
  function _orFailError(err, query2) {
    if (typeof err === "function") {
      err = err.call(query2);
    }
    if (err == null) {
      err = new DocumentNotFoundError(query2.getQuery(), query2.model.modelName);
    }
    return err;
  }
  Query.prototype.isPathSelectedInclusive = function(path) {
    return isPathSelectedInclusive2(this._fields, path);
  };
  Query.prototype.exec = async function exec(op) {
    if (typeof op === "function" || arguments.length >= 2 && typeof arguments[1] === "function") {
      throw new MongooseError("Query.prototype.exec() no longer accepts a callback");
    }
    this._validateOp();
    if (typeof op === "string") {
      this.op = op;
    }
    if (this.op == null) {
      throw new MongooseError("Query must have `op` before executing");
    }
    if (this.model == null) {
      throw new MongooseError("Query must have an associated model before executing");
    }
    const thunk = opToThunk.get(this.op);
    if (!thunk) {
      throw new MongooseError('Query has invalid `op`: "' + this.op + '"');
    }
    if (this.options?.sort && typeof this.options.sort === "object" && Object.hasOwn(this.options.sort, "")) {
      throw new MongooseError('Invalid field "" passed to sort()');
    }
    if (this._execCount > 0) {
      let str = this.toString();
      if (str.length > 60) {
        str = str.slice(0, 60) + "...";
      }
      throw new MongooseError("Query was already executed: " + str);
    }
    this._execCount++;
    const _this = this;
    return traceQuery(async function maybeTracedQueryExec() {
      let skipWrappedFunction = null;
      try {
        await _this._hooks.execPre("exec", _this, []);
      } catch (err) {
        if (err instanceof Kareem.skipWrappedFunction) {
          skipWrappedFunction = err;
        } else {
          throw err;
        }
      }
      let res;
      let error2 = null;
      try {
        await _executePreHooks(_this);
        res = skipWrappedFunction ? skipWrappedFunction.args[0] : await _this[thunk]();
        for (const fn of _this._transforms) {
          res = fn(res);
        }
      } catch (err) {
        if (err instanceof Kareem.skipWrappedFunction) {
          res = err.args[0];
        } else {
          error2 = err;
        }
        error2 = _this.model.schema._transformDuplicateKeyError(error2);
      }
      res = await _executePostHooks(_this, res, error2);
      await _this._hooks.execPost("exec", _this, []);
      return res;
    }, () => ({
      operation: _this.op,
      collection: _this.mongooseCollection.name,
      database: _this.model.db?.name,
      serverAddress: _this.model.db?.host,
      serverPort: _this.model.db?.port,
      args: {
        filter: _this.getFilter(),
        fields: _this._fields,
        options: _this._mongooseOptions
      }
    }));
  };
  async function _executePostHooks(query2, res, error2, op) {
    if (query2._queryMiddleware == null) {
      if (error2 != null) {
        throw error2;
      }
      return res;
    }
    const filter = buildMiddlewareFilter(query2.options, "post");
    const opts = { filter };
    if (error2) {
      opts.error = error2;
    }
    const [result] = await query2._queryMiddleware.execPost(
      op || query2.op,
      query2,
      [res],
      opts
    );
    return result;
  }
  function _executePreHooks(query2, op) {
    if (query2._queryMiddleware == null) {
      return;
    }
    const filter = buildMiddlewareFilter(query2.options, "pre");
    return query2._queryMiddleware.execPre(
      op || query2.op,
      query2,
      [],
      { filter }
    );
  }
  function _cloneUpdateIfShared(query2) {
    if (!query2._updateIsShared) {
      return;
    }
    if (query2.mongooseOptions().cloneUpdate === false) {
      return;
    }
    query2[queryUpdateSymbol] = clone(query2[queryUpdateSymbol], {
      flattenDecimals: false
    });
    query2._updateIsShared = false;
  }
  Query.prototype.then = function(resolve, reject) {
    return this.exec().then(resolve, reject);
  };
  Query.prototype.catch = function(reject) {
    return this.exec().then(null, reject);
  };
  Query.prototype.finally = function(onFinally) {
    return this.exec().finally(onFinally);
  };
  Query.prototype[Symbol.toStringTag] = function toString() {
    return `Query { ${this.op} }`;
  };
  Query.prototype.pre = function(fn) {
    this._hooks.pre("exec", fn);
    return this;
  };
  Query.prototype.post = function(fn) {
    this._hooks.post("exec", fn);
    return this;
  };
  Query.prototype._castUpdate = function _castUpdate(obj) {
    let schema2 = this.schema;
    const discriminatorKey = schema2.options.discriminatorKey;
    const baseSchema = schema2._baseSchema || schema2;
    if (this._mongooseOptions.overwriteDiscriminatorKey && obj[discriminatorKey] != null && baseSchema.discriminators) {
      const _schema = Object.values(baseSchema.discriminators).find(
        (discriminator2) => discriminator2.discriminatorMapping.value === obj[discriminatorKey]
      );
      if (_schema != null) {
        schema2 = _schema;
      }
    }
    let upsert;
    if ("upsert" in this.options) {
      upsert = this.options.upsert;
    }
    return castUpdate2(schema2, obj, {
      strict: this._mongooseOptions.strict,
      upsert,
      arrayFilters: this.options.arrayFilters,
      overwriteDiscriminatorKey: this._mongooseOptions.overwriteDiscriminatorKey,
      overwriteImmutable: this._mongooseOptions.overwriteImmutable
    }, this, this._conditions);
  };
  Query.prototype.populate = function(...args) {
    if (!args.some(Boolean)) {
      return this;
    }
    const res = utils2.populate.apply(null, args);
    const opts = this._mongooseOptions;
    if (opts.lean != null) {
      const lean = opts.lean;
      for (const populateOptions2 of res) {
        if (populateOptions2?.options?.lean == null) {
          populateOptions2.options = populateOptions2.options || {};
          populateOptions2.options.lean = lean;
        }
      }
    }
    if (!utils2.isObject(opts.populate)) {
      opts.populate = {};
    }
    const pop = opts.populate;
    for (const populateOptions2 of res) {
      const path = populateOptions2.path;
      if (pop[path]?.populate && populateOptions2.populate) {
        populateOptions2.populate = pop[path].populate.concat(populateOptions2.populate);
      }
      pop[populateOptions2.path] = populateOptions2;
    }
    return this;
  };
  Query.prototype.getPopulatedPaths = function getPopulatedPaths() {
    const obj = this._mongooseOptions.populate || {};
    const ret = Object.keys(obj);
    for (const path of Object.keys(obj)) {
      const pop = obj[path];
      if (!Array.isArray(pop.populate)) {
        continue;
      }
      _getPopulatedPaths(ret, pop.populate, path + ".");
    }
    return ret;
  };
  function _getPopulatedPaths(list, arr, prefix) {
    for (const pop of arr) {
      list.push(prefix + pop.path);
      if (!Array.isArray(pop.populate)) {
        continue;
      }
      _getPopulatedPaths(list, pop.populate, prefix + pop.path + ".");
    }
  }
  Query.prototype.cast = function(model2, obj) {
    obj || (obj = this._conditions);
    model2 = model2 || this.model;
    const discriminatorKey = model2.schema.options.discriminatorKey;
    if (obj != null && Object.hasOwn(obj, discriminatorKey)) {
      model2 = getDiscriminatorByValue2(model2.discriminators, obj[discriminatorKey]) || model2;
    }
    const opts = { upsert: this.options?.upsert };
    if (this.options) {
      if ("strict" in this.options) {
        opts.strict = this.options.strict;
      }
      if ("strictQuery" in this.options) {
        opts.strictQuery = this.options.strictQuery;
      }
    }
    if ("sanitizeFilter" in this._mongooseOptions) {
      opts.sanitizeFilter = this._mongooseOptions.sanitizeFilter;
    }
    try {
      return cast2(model2.schema, obj, opts, this);
    } catch (err) {
      if (typeof err.setModel === "function") {
        err.setModel(model2);
      }
      throw err;
    }
  };
  Query.prototype._castFields = function _castFields(fields) {
    let selected, elemMatchKeys, keys, key, out;
    if (fields) {
      keys = Object.keys(fields);
      elemMatchKeys = [];
      for (let i = 0; i < keys.length; ++i) {
        key = keys[i];
        if (fields[key].$elemMatch) {
          selected || (selected = {});
          selected[key] = fields[key];
          elemMatchKeys.push(key);
        }
      }
    }
    if (selected) {
      try {
        out = this.cast(this.model, selected);
      } catch (err) {
        return err;
      }
      for (let i = 0; i < elemMatchKeys.length; ++i) {
        key = elemMatchKeys[i];
        fields[key] = out[key];
      }
    }
    return fields;
  };
  Query.prototype._applyPaths = function applyPaths() {
    if (!this.model) {
      return;
    }
    this._fields = this._fields || {};
    let sanitizeProjection3 = void 0;
    if (this.model != null && utils2.hasUserDefinedProperty(this.model.db.options, "sanitizeProjection")) {
      sanitizeProjection3 = this.model.db.options.sanitizeProjection;
    } else if (this.model != null && utils2.hasUserDefinedProperty(this.model.base.options, "sanitizeProjection")) {
      sanitizeProjection3 = this.model.base.options.sanitizeProjection;
    } else {
      sanitizeProjection3 = this._mongooseOptions.sanitizeProjection;
    }
    const schemaLevelProjections = this._mongooseOptions.schemaLevelProjections ?? true;
    if (schemaLevelProjections) {
      helpers2.applyPaths(this._fields, this.model.schema, sanitizeProjection3);
    }
    let _selectPopulatedPaths = true;
    if ("selectPopulatedPaths" in this.model.base.options) {
      _selectPopulatedPaths = this.model.base.options.selectPopulatedPaths;
    }
    if ("selectPopulatedPaths" in this.model.schema.options) {
      _selectPopulatedPaths = this.model.schema.options.selectPopulatedPaths;
    }
    if (_selectPopulatedPaths) {
      selectPopulatedFields2(this._fields, this._userProvidedFields, this._mongooseOptions.populate);
    }
  };
  Query.prototype.cursor = function cursor(opts) {
    if (opts) {
      this.setOptions(opts);
    }
    try {
      this._castConditions();
    } catch (err) {
      return new QueryCursor(this)._markError(err);
    }
    if (this.error()) {
      return new QueryCursor(this)._markError(this.error());
    }
    return new QueryCursor(this);
  };
  Query.prototype.tailable = function(val, opts) {
    if (typeof val?.constructor === "function" && val.constructor.name === "Object") {
      opts = val;
      val = true;
    }
    if (val === void 0) {
      val = true;
    }
    if (opts && typeof opts === "object") {
      for (const key of Object.keys(opts)) {
        if (key === "awaitData" || key === "awaitdata") {
          this.options["awaitData"] = !!opts[key];
        } else {
          this.options[key] = opts[key];
        }
      }
    }
    this.options.tailable = arguments.length ? !!val : true;
    return this;
  };
  Query.prototype.near = function() {
    const params = [];
    const sphere = this._mongooseOptions.nearSphere;
    if (arguments.length === 1) {
      if (Array.isArray(arguments[0])) {
        params.push({ center: arguments[0], spherical: sphere });
      } else if (typeof arguments[0] === "string") {
        params.push(arguments[0]);
      } else if (utils2.isObject(arguments[0])) {
        if (typeof arguments[0].spherical !== "boolean") {
          arguments[0].spherical = sphere;
        }
        params.push(arguments[0]);
      } else {
        throw new TypeError("invalid argument");
      }
    } else if (arguments.length === 2) {
      if (typeof arguments[0] === "number" && typeof arguments[1] === "number") {
        params.push({ center: [arguments[0], arguments[1]], spherical: sphere });
      } else if (typeof arguments[0] === "string" && Array.isArray(arguments[1])) {
        params.push(arguments[0]);
        params.push({ center: arguments[1], spherical: sphere });
      } else if (typeof arguments[0] === "string" && utils2.isObject(arguments[1])) {
        params.push(arguments[0]);
        if (typeof arguments[1].spherical !== "boolean") {
          arguments[1].spherical = sphere;
        }
        params.push(arguments[1]);
      } else {
        throw new TypeError("invalid argument");
      }
    } else if (arguments.length === 3) {
      if (typeof arguments[0] === "string" && typeof arguments[1] === "number" && typeof arguments[2] === "number") {
        params.push(arguments[0]);
        params.push({ center: [arguments[1], arguments[2]], spherical: sphere });
      } else {
        throw new TypeError("invalid argument");
      }
    } else {
      throw new TypeError("invalid argument");
    }
    return Query.base.near.apply(this, params);
  };
  Query.prototype.nearSphere = function() {
    this._mongooseOptions.nearSphere = true;
    this.near.apply(this, arguments);
    return this;
  };
  Query.prototype[Symbol.asyncIterator] = function queryAsyncIterator() {
    this._mongooseOptions._asyncIterator = true;
    return this.cursor();
  };
  Query.prototype.box = function(ll, ur) {
    if (!Array.isArray(ll) && utils2.isObject(ll)) {
      ur = ll.ur;
      ll = ll.ll;
    }
    return Query.base.box.call(this, ll, ur);
  };
  Query.prototype.center = Query.base.circle;
  Query.prototype.centerSphere = function() {
    if (typeof arguments[0]?.constructor === "function" && arguments[0].constructor.name === "Object") {
      arguments[0].spherical = true;
    }
    if (typeof arguments[1]?.constructor === "function" && arguments[1].constructor.name === "Object") {
      arguments[1].spherical = true;
    }
    Query.base.circle.apply(this, arguments);
  };
  Query.prototype.selectedInclusively = function selectedInclusively() {
    return isInclusive2(this._fields);
  };
  Query.prototype.selectedExclusively = function selectedExclusively() {
    return isExclusive2(this._fields);
  };
  Query.prototype.model;
  function canMerge(value) {
    return value instanceof Query || utils2.isObject(value) || value === null;
  }
  query = Query;
  return query;
}
var model = { exports: {} };
var aggregationCursor;
var hasRequiredAggregationCursor;
function requireAggregationCursor() {
  if (hasRequiredAggregationCursor) return aggregationCursor;
  hasRequiredAggregationCursor = 1;
  const MongooseError = requireMongooseError();
  const Readable = require$$0$1.Readable;
  const eachAsync2 = requireEachAsync();
  const immediate2 = requireImmediate();
  const kareem = requireKareem();
  const util = require$$1;
  const { cursorNextChannel } = requireTracing();
  function AggregationCursor(agg) {
    Readable.call(this, { autoDestroy: true, objectMode: true });
    this.cursor = null;
    this.agg = agg;
    this._transforms = [];
    const connection2 = agg._connection;
    const model2 = agg._model;
    delete agg.options.cursor.useMongooseAggCursor;
    this._mongooseOptions = {};
    if (connection2) {
      this.cursor = connection2.db.aggregate(agg._pipeline, agg.options || {});
      setImmediate(() => this.emit("cursor", this.cursor));
    } else {
      _init(model2, this, agg);
    }
  }
  util.inherits(AggregationCursor, Readable);
  function _init(model2, c, agg) {
    model2.hooks.execPre("aggregate", agg).then(() => onPreComplete(null), (err) => onPreComplete(err));
    function onPreComplete(err) {
      if (err != null) {
        _handlePreHookError(c, err);
        return;
      }
      if (typeof agg.options?.cursor?.transform === "function") {
        c._transforms.push(agg.options.cursor.transform);
      }
      if (model2.collection._shouldBufferCommands() && model2.collection.buffer) {
        model2.collection.queue.push([
          () => _getRawCursor(model2, c, agg)
        ]);
      } else {
        _getRawCursor(model2, c, agg);
      }
    }
  }
  function _getRawCursor(model2, aggregationCursor2, agg) {
    try {
      const cursor = model2.collection.aggregate(agg._pipeline, agg.options || {});
      aggregationCursor2.cursor = cursor;
      aggregationCursor2.emit("cursor", cursor);
    } catch (err) {
      aggregationCursor2._markError(err);
      aggregationCursor2.listeners("error").length > 0 && aggregationCursor2.emit("error", aggregationCursor2._error);
    }
  }
  function _handlePreHookError(queryCursor2, err) {
    if (err instanceof kareem.skipWrappedFunction) {
      const resultValue = err.args[0];
      if (resultValue != null && (!Array.isArray(resultValue) || resultValue.length)) {
        const err2 = new MongooseError(
          'Cannot `skipMiddlewareFunction()` with a value when using `.aggregate().cursor()`, value must be nullish or empty array, got "' + util.inspect(resultValue) + '".'
        );
        queryCursor2._markError(err2);
        queryCursor2.listeners("error").length > 0 && queryCursor2.emit("error", err2);
        return;
      }
      queryCursor2.emit("cursor", null);
      return;
    }
    queryCursor2._markError(err);
    queryCursor2.listeners("error").length > 0 && queryCursor2.emit("error", err);
  }
  AggregationCursor.prototype._read = function() {
    const _this = this;
    _next(this, function(error2, doc) {
      if (error2) {
        return _this.emit("error", error2);
      }
      if (!doc) {
        _this.push(null);
        _this.cursor.close(function(error3) {
          if (error3) {
            return _this.emit("error", error3);
          }
        });
        return;
      }
      _this.push(doc);
    });
  };
  if (Symbol.asyncIterator != null) {
    const msg = "Mongoose does not support using async iterators with an existing aggregation cursor. See https://bit.ly/mongoose-async-iterate-aggregation";
    AggregationCursor.prototype[Symbol.asyncIterator] = function() {
      throw new MongooseError(msg);
    };
  }
  Object.defineProperty(AggregationCursor.prototype, "map", {
    value: function(fn) {
      this._transforms.push(fn);
      return this;
    },
    enumerable: true,
    configurable: true,
    writable: true
  });
  AggregationCursor.prototype._markError = function(error2) {
    this._error = error2;
    return this;
  };
  AggregationCursor.prototype.close = async function close() {
    if (typeof arguments[0] === "function") {
      throw new MongooseError("AggregationCursor.prototype.close() no longer accepts a callback");
    }
    try {
      await this.cursor.close();
    } catch (error2) {
      this.listeners("error").length > 0 && this.emit("error", error2);
      throw error2;
    }
    this.emit("close");
  };
  AggregationCursor.prototype._destroy = function _destroy(_err, callback) {
    let waitForCursor = null;
    if (!this.cursor) {
      waitForCursor = new Promise((resolve) => {
        this.once("cursor", resolve);
      });
    } else {
      waitForCursor = Promise.resolve();
    }
    waitForCursor.then(() => this.cursor.close()).then(() => {
      this._closed = true;
      callback();
    }).catch((error2) => {
      callback(error2);
    });
    return this;
  };
  AggregationCursor.prototype.next = async function next() {
    if (typeof arguments[0] === "function") {
      throw new MongooseError("AggregationCursor.prototype.next() no longer accepts a callback");
    }
    const _this = this;
    const model2 = this.agg._model;
    return cursorNextChannel.trace(function maybeTracedAggCursorNext() {
      return new Promise((resolve, reject) => {
        _next(_this, (err, res) => {
          if (err != null) {
            return reject(err);
          }
          resolve(res);
        });
      });
    }, () => ({
      operation: "aggregate",
      collection: model2?.collection?.name,
      database: model2?.db?.name,
      serverAddress: model2?.db?.host,
      serverPort: model2?.db?.port,
      batchSize: _this.agg.options?.cursor?.batchSize,
      tailable: false,
      args: {
        pipeline: _this.agg._pipeline,
        options: _this.agg.options
      }
    }));
  };
  AggregationCursor.prototype.eachAsync = function(fn, opts) {
    if (typeof arguments[2] === "function") {
      throw new MongooseError("AggregationCursor.prototype.eachAsync() no longer accepts a callback");
    }
    const _this = this;
    if (typeof opts === "function") {
      opts = {};
    }
    opts = opts || {};
    return eachAsync2(function(cb) {
      return _next(_this, cb);
    }, fn, opts);
  };
  if (Symbol.asyncIterator != null) {
    AggregationCursor.prototype[Symbol.asyncIterator] = function() {
      return this.transformNull()._transformForAsyncIterator();
    };
  }
  AggregationCursor.prototype._transformForAsyncIterator = function() {
    if (this._transforms.indexOf(_transformForAsyncIterator) === -1) {
      this.map(_transformForAsyncIterator);
    }
    return this;
  };
  AggregationCursor.prototype.transformNull = function(val) {
    if (arguments.length === 0) {
      val = true;
    }
    this._mongooseOptions.transformNull = val;
    return this;
  };
  function _transformForAsyncIterator(doc) {
    return doc == null ? { done: true } : { value: doc, done: false };
  }
  AggregationCursor.prototype.addCursorFlag = function(flag, value) {
    const _this = this;
    _waitForCursor(this, function() {
      _this.cursor.addCursorFlag(flag, value);
    });
    return this;
  };
  function _waitForCursor(ctx, cb) {
    if (ctx.cursor) {
      return cb();
    }
    ctx.once("cursor", function() {
      cb();
    });
  }
  function _next(ctx, cb) {
    let callback = cb;
    if (ctx._transforms.length) {
      callback = function(err, doc) {
        if (err || doc === null && !ctx._mongooseOptions.transformNull) {
          return cb(err, doc);
        }
        cb(err, ctx._transforms.reduce(function(doc2, fn) {
          return fn(doc2);
        }, doc));
      };
    }
    if (ctx._error) {
      return immediate2(function() {
        callback(ctx._error);
      });
    }
    if (ctx.cursor) {
      return ctx.cursor.next().then(
        (doc) => {
          if (!doc) {
            return callback(null, null);
          }
          callback(null, doc);
        },
        (err) => callback(err)
      );
    } else {
      ctx.once("error", cb);
      ctx.once("cursor", function() {
        _next(ctx, cb);
      });
    }
  }
  aggregationCursor = AggregationCursor;
  return aggregationCursor;
}
var prepareDiscriminatorPipeline;
var hasRequiredPrepareDiscriminatorPipeline;
function requirePrepareDiscriminatorPipeline() {
  if (hasRequiredPrepareDiscriminatorPipeline) return prepareDiscriminatorPipeline;
  hasRequiredPrepareDiscriminatorPipeline = 1;
  prepareDiscriminatorPipeline = function prepareDiscriminatorPipeline2(pipeline, schema2, prefix) {
    const discriminatorMapping = schema2?.discriminatorMapping;
    prefix = prefix || "";
    if (discriminatorMapping && !discriminatorMapping.isRoot) {
      const originalPipeline = pipeline;
      const filterKey = (prefix.length > 0 ? prefix + "." : prefix) + discriminatorMapping.key;
      const discriminatorValue = discriminatorMapping.value;
      if (originalPipeline[0] != null && originalPipeline[0].$match && (originalPipeline[0].$match[filterKey] === void 0 || originalPipeline[0].$match[filterKey] === discriminatorValue)) {
        originalPipeline[0].$match[filterKey] = discriminatorValue;
      } else if (originalPipeline[0]?.$geoNear) {
        originalPipeline[0].$geoNear.query = originalPipeline[0].$geoNear.query || {};
        originalPipeline[0].$geoNear.query[filterKey] = discriminatorValue;
      } else if (originalPipeline[0]?.$search) {
        if (originalPipeline[1]?.$match != null) {
          originalPipeline[1].$match[filterKey] = originalPipeline[1].$match[filterKey] || discriminatorValue;
        } else {
          const match = {};
          match[filterKey] = discriminatorValue;
          originalPipeline.splice(1, 0, { $match: match });
        }
      } else {
        const match = {};
        match[filterKey] = discriminatorValue;
        originalPipeline.unshift({ $match: match });
      }
    }
  };
  return prepareDiscriminatorPipeline;
}
var stringifyFunctionOperators;
var hasRequiredStringifyFunctionOperators;
function requireStringifyFunctionOperators() {
  if (hasRequiredStringifyFunctionOperators) return stringifyFunctionOperators;
  hasRequiredStringifyFunctionOperators = 1;
  stringifyFunctionOperators = function stringifyFunctionOperators2(pipeline) {
    if (!Array.isArray(pipeline)) {
      return;
    }
    for (const stage of pipeline) {
      if (stage == null) {
        continue;
      }
      const canHaveAccumulator = stage.$group || stage.$bucket || stage.$bucketAuto;
      if (canHaveAccumulator != null) {
        for (const key of Object.keys(canHaveAccumulator)) {
          handleAccumulator(canHaveAccumulator[key]);
        }
      }
      const stageType = Object.keys(stage)[0];
      if (stageType && typeof stage[stageType] === "object") {
        const stageOptions = stage[stageType];
        for (const key of Object.keys(stageOptions)) {
          if (stageOptions[key] != null && stageOptions[key].$function != null && typeof stageOptions[key].$function.body === "function") {
            stageOptions[key].$function.body = stageOptions[key].$function.body.toString();
          }
        }
      }
      if (stage.$facet != null) {
        for (const key of Object.keys(stage.$facet)) {
          stringifyFunctionOperators2(stage.$facet[key]);
        }
      }
    }
  };
  function handleAccumulator(operator) {
    if (operator == null || operator.$accumulator == null) {
      return;
    }
    for (const key of ["init", "accumulate", "merge", "finalize"]) {
      if (typeof operator.$accumulator[key] === "function") {
        operator.$accumulator[key] = String(operator.$accumulator[key]);
      }
    }
  }
  return stringifyFunctionOperators;
}
var aggregate;
var hasRequiredAggregate;
function requireAggregate() {
  if (hasRequiredAggregate) return aggregate;
  hasRequiredAggregate = 1;
  const AggregationCursor = requireAggregationCursor();
  const MongooseError = requireMongooseError();
  const Query = requireQuery();
  const { applyGlobalMaxTimeMS, applyGlobalDiskUse } = requireApplyGlobalOption();
  const clone = requireClone();
  const getConstructorName2 = requireGetConstructorName();
  const prepareDiscriminatorPipeline2 = requirePrepareDiscriminatorPipeline();
  const { buildMiddlewareFilter } = requireBuildMiddlewareFilter();
  const stringifyFunctionOperators2 = requireStringifyFunctionOperators();
  const utils2 = requireUtils();
  const { modelSymbol } = requireSymbols$1();
  const { createTracedChannel } = requireTracing();
  const { trace: traceAggregate } = createTracedChannel("mongoose:aggregate");
  const read = Query.prototype.read;
  const readConcern = Query.prototype.readConcern;
  const validRedactStringValues = /* @__PURE__ */ new Set(["$$DESCEND", "$$PRUNE", "$$KEEP"]);
  function Aggregate(pipeline, modelOrConn) {
    this._pipeline = [];
    if (modelOrConn == null || modelOrConn[modelSymbol]) {
      this._model = modelOrConn;
    } else {
      this._connection = modelOrConn;
    }
    this.options = {};
    if (arguments.length === 1 && Array.isArray(pipeline)) {
      this.append.apply(this, pipeline);
    }
  }
  Aggregate.prototype.options;
  Aggregate.prototype._optionsForExec = function() {
    const options2 = this.options || {};
    const asyncLocalStorage = this.model()?.db?.base.transactionAsyncLocalStorage?.getStore();
    if (!Object.hasOwn(options2, "session") && asyncLocalStorage?.session != null) {
      options2.session = asyncLocalStorage.session;
    }
    return options2;
  };
  Aggregate.prototype.model = function(model2) {
    if (arguments.length === 0) {
      return this._model;
    }
    this._model = model2;
    if (model2.schema != null) {
      if (this.options.readPreference == null && model2.schema.options.read != null) {
        this.options.readPreference = model2.schema.options.read;
      }
      if (this.options.collation == null && model2.schema.options.collation != null) {
        this.options.collation = model2.schema.options.collation;
      }
    }
    return model2;
  };
  Aggregate.prototype.append = function() {
    const args = arguments.length === 1 && Array.isArray(arguments[0]) ? arguments[0] : [...arguments];
    if (!args.every(isOperator2)) {
      throw new MongooseError("Arguments must be aggregate pipeline operators");
    }
    this._pipeline = this._pipeline.concat(args);
    return this;
  };
  Aggregate.prototype.addFields = function(arg) {
    if (typeof arg !== "object" || arg === null || Array.isArray(arg)) {
      throw new MongooseError("Invalid addFields() argument. Must be an object");
    }
    return this.append({ $addFields: Object.assign({}, arg) });
  };
  Aggregate.prototype.project = function(arg) {
    const fields = {};
    if (typeof arg === "object" && !Array.isArray(arg)) {
      Object.keys(arg).forEach(function(field) {
        fields[field] = arg[field];
      });
    } else if (arguments.length === 1 && typeof arg === "string") {
      arg.split(/\s+/).forEach(function(field) {
        if (!field) {
          return;
        }
        const include = field[0] === "-" ? 0 : 1;
        if (include === 0) {
          field = field.substring(1);
        }
        fields[field] = include;
      });
    } else {
      throw new MongooseError("Invalid project() argument. Must be string or object");
    }
    return this.append({ $project: fields });
  };
  Aggregate.prototype.near = function(arg) {
    if (arg == null) {
      throw new MongooseError("Aggregate `near()` must be called with non-nullish argument");
    }
    if (arg.near == null) {
      throw new MongooseError("Aggregate `near()` argument must have a `near` property");
    }
    const coordinates = Array.isArray(arg.near) ? arg.near : arg.near.coordinates;
    if (typeof arg.near === "object" && (!Array.isArray(coordinates) || coordinates.length < 2 || coordinates.find((c) => typeof c !== "number"))) {
      throw new MongooseError(`Aggregate \`near()\` argument has invalid coordinates, got "${coordinates}"`);
    }
    const op = {};
    op.$geoNear = arg;
    return this.append(op);
  };
  "group match skip limit out densify fill".split(" ").forEach(function($operator) {
    Aggregate.prototype[$operator] = function(arg) {
      const op = {};
      op["$" + $operator] = arg;
      return this.append(op);
    };
  });
  Aggregate.prototype.unwind = function() {
    const args = [...arguments];
    const res = [];
    for (const arg of args) {
      if (arg && typeof arg === "object") {
        res.push({ $unwind: arg });
      } else if (typeof arg === "string") {
        res.push({
          $unwind: arg[0] === "$" ? arg : "$" + arg
        });
      } else {
        throw new MongooseError('Invalid arg "' + arg + '" to unwind(), must be string or object');
      }
    }
    return this.append.apply(this, res);
  };
  Aggregate.prototype.replaceRoot = function(newRoot) {
    let ret;
    if (typeof newRoot === "string") {
      ret = newRoot.startsWith("$") ? newRoot : "$" + newRoot;
    } else {
      ret = newRoot;
    }
    return this.append({
      $replaceRoot: {
        newRoot: ret
      }
    });
  };
  Aggregate.prototype.count = function(fieldName) {
    return this.append({ $count: fieldName });
  };
  Aggregate.prototype.sortByCount = function(arg) {
    if (arg && typeof arg === "object") {
      return this.append({ $sortByCount: arg });
    } else if (typeof arg === "string") {
      return this.append({
        $sortByCount: arg[0] === "$" ? arg : "$" + arg
      });
    } else {
      throw new TypeError('Invalid arg "' + arg + '" to sortByCount(), must be string or object');
    }
  };
  Aggregate.prototype.lookup = function(options2) {
    return this.append({ $lookup: options2 });
  };
  Aggregate.prototype.graphLookup = function(options2) {
    const cloneOptions = {};
    if (options2) {
      if (!utils2.isObject(options2)) {
        throw new TypeError("Invalid graphLookup() argument. Must be an object.");
      }
      utils2.mergeClone(cloneOptions, options2);
      const startWith = cloneOptions.startWith;
      if (startWith && typeof startWith === "string") {
        cloneOptions.startWith = cloneOptions.startWith.startsWith("$") ? cloneOptions.startWith : "$" + cloneOptions.startWith;
      }
    }
    return this.append({ $graphLookup: cloneOptions });
  };
  Aggregate.prototype.sample = function(size) {
    return this.append({ $sample: { size } });
  };
  Aggregate.prototype.sort = function(arg) {
    const sort = {};
    if (getConstructorName2(arg) === "Object") {
      const desc = ["desc", "descending", -1];
      Object.keys(arg).forEach(function(field) {
        if (arg[field] instanceof Object && arg[field].$meta) {
          sort[field] = arg[field];
          return;
        }
        sort[field] = desc.indexOf(arg[field]) === -1 ? 1 : -1;
      });
    } else if (arguments.length === 1 && typeof arg === "string") {
      arg.split(/\s+/).forEach(function(field) {
        if (!field) {
          return;
        }
        const ascend = field[0] === "-" ? -1 : 1;
        if (ascend === -1) {
          field = field.substring(1);
        }
        sort[field] = ascend;
      });
    } else {
      throw new TypeError("Invalid sort() argument. Must be a string or object.");
    }
    return this.append({ $sort: sort });
  };
  Aggregate.prototype.unionWith = function(options2) {
    return this.append({ $unionWith: options2 });
  };
  Aggregate.prototype.read = function(pref, tags) {
    read.call(this, pref, tags);
    return this;
  };
  Aggregate.prototype.readConcern = function(level) {
    readConcern.call(this, level);
    return this;
  };
  Aggregate.prototype.redact = function(expression, thenExpr, elseExpr) {
    if (arguments.length === 3) {
      if (typeof thenExpr === "string" && !validRedactStringValues.has(thenExpr) || typeof elseExpr === "string" && !validRedactStringValues.has(elseExpr)) {
        throw new MongooseError("If thenExpr or elseExpr is string, it must be either $$DESCEND, $$PRUNE or $$KEEP");
      }
      expression = {
        $cond: {
          if: expression,
          then: thenExpr,
          else: elseExpr
        }
      };
    } else if (arguments.length !== 1) {
      throw new TypeError("Invalid arguments");
    }
    return this.append({ $redact: expression });
  };
  Aggregate.prototype.explain = async function explain(verbosity) {
    if (typeof verbosity === "function" || typeof arguments[1] === "function") {
      throw new MongooseError("Aggregate.prototype.explain() no longer accepts a callback");
    }
    const model2 = this._model;
    if (!this._pipeline.length) {
      throw new MongooseError("Aggregate has empty pipeline");
    }
    prepareDiscriminatorPipeline2(this._pipeline, this._model.schema);
    const preFilter = buildMiddlewareFilter(this.options, "pre");
    const postFilter = buildMiddlewareFilter(this.options, "post");
    const options2 = this.options != null ? { ...this.options } : {};
    delete options2.middleware;
    try {
      await model2.hooks.execPre("aggregate", this, [], { filter: preFilter });
    } catch (error2) {
      return await model2.hooks.execPost("aggregate", this, [null], { error: error2, filter: postFilter });
    }
    const cursor = await model2.collection.aggregate(this._pipeline, options2);
    if (verbosity == null) {
      verbosity = true;
    }
    let result = null;
    try {
      result = await cursor.explain(verbosity);
    } catch (error2) {
      return await model2.hooks.execPost("aggregate", this, [null], { error: error2, filter: postFilter });
    }
    await model2.hooks.execPost("aggregate", this, [result], { error: null, filter: postFilter });
    return result;
  };
  Aggregate.prototype.allowDiskUse = function(value) {
    this.options.allowDiskUse = value;
    return this;
  };
  Aggregate.prototype.hint = function(value) {
    this.options.hint = value;
    return this;
  };
  Aggregate.prototype.session = function(session) {
    if (session == null) {
      delete this.options.session;
    } else {
      this.options.session = session;
    }
    return this;
  };
  Aggregate.prototype.option = function(value) {
    for (const key in value) {
      this.options[key] = value[key];
    }
    return this;
  };
  Aggregate.prototype.cursor = function(options2) {
    this._optionsForExec();
    this.options.cursor = options2 || {};
    return new AggregationCursor(this);
  };
  Aggregate.prototype.collation = function(collation) {
    this.options.collation = collation;
    return this;
  };
  Aggregate.prototype.facet = function(options2) {
    return this.append({ $facet: options2 });
  };
  Aggregate.prototype.search = function(options2) {
    return this.append({ $search: options2 });
  };
  Aggregate.prototype.pipeline = function() {
    return this._pipeline;
  };
  Aggregate.prototype.pipelineForUnionWith = function pipelineForUnionWith() {
    for (const stage of this._pipeline) {
      if (stage?.$out != null || stage?.$merge != null) {
        throw new MongooseError("Aggregate pipeline for $unionWith cannot include `$out` or `$merge` stages");
      }
    }
    return this._pipeline;
  };
  Aggregate.prototype.exec = async function exec() {
    if (!this._model && !this._connection) {
      throw new MongooseError("Aggregate not bound to any Model");
    }
    if (typeof arguments[0] === "function") {
      throw new MongooseError("Aggregate.prototype.exec() no longer accepts a callback");
    }
    if (this._connection) {
      if (!this._pipeline.length) {
        throw new MongooseError("Aggregate has empty pipeline");
      }
      this._optionsForExec();
      const _this2 = this;
      return traceAggregate(async function maybeTracedConnectionAggregate() {
        const cursor = await _this2._connection.client.db().aggregate(_this2._pipeline, _this2.options);
        return await cursor.toArray();
      }, () => ({
        operation: "aggregate",
        database: _this2._connection.name,
        serverAddress: _this2._connection.host,
        serverPort: _this2._connection.port,
        args: {
          pipeline: _this2._pipeline,
          options: _this2.options
        }
      }));
    }
    const model2 = this._model;
    const collection2 = this._model.collection;
    applyGlobalMaxTimeMS(this.options, model2.db.options, model2.base.options);
    applyGlobalDiskUse(this.options, model2.db.options, model2.base.options);
    this._optionsForExec();
    if (this.options?.cursor) {
      return new AggregationCursor(this);
    }
    prepareDiscriminatorPipeline2(this._pipeline, this._model.schema);
    stringifyFunctionOperators2(this._pipeline);
    const _this = this;
    return traceAggregate(async function maybeTracedAggregateExec() {
      const preFilter = buildMiddlewareFilter(_this.options, "pre");
      const postFilter = buildMiddlewareFilter(_this.options, "post");
      try {
        await model2.hooks.execPre("aggregate", _this, [], { filter: preFilter });
      } catch (error2) {
        return await model2.hooks.execPost("aggregate", _this, [null], { error: error2, filter: postFilter });
      }
      if (!_this._pipeline.length) {
        throw new MongooseError("Aggregate has empty pipeline");
      }
      const options2 = clone(_this.options || {});
      delete options2.middleware;
      let result;
      try {
        const cursor = await collection2.aggregate(_this._pipeline, options2);
        result = await cursor.toArray();
      } catch (error2) {
        return await model2.hooks.execPost("aggregate", _this, [null], { error: error2, filter: postFilter });
      }
      await model2.hooks.execPost("aggregate", _this, [result], { error: null, filter: postFilter });
      return result;
    }, () => ({
      operation: "aggregate",
      collection: collection2.name,
      database: model2.db?.name,
      serverAddress: model2.db?.host,
      serverPort: model2.db?.port,
      args: {
        pipeline: _this._pipeline,
        options: _this.options
      }
    }));
  };
  Aggregate.prototype.then = function(resolve, reject) {
    return this.exec().then(resolve, reject);
  };
  Aggregate.prototype.catch = function(reject) {
    return this.exec().then(null, reject);
  };
  Aggregate.prototype.finally = function(onFinally) {
    return this.exec().finally(onFinally);
  };
  Aggregate.prototype[Symbol.asyncIterator] = function() {
    return this.cursor({ useMongooseAggCursor: true }).transformNull()._transformForAsyncIterator();
  };
  function isOperator2(obj) {
    if (typeof obj !== "object" || obj === null) {
      return false;
    }
    const k = Object.keys(obj);
    return k.length === 1 && k[0][0] === "$";
  }
  Aggregate._prepareDiscriminatorPipeline = prepareDiscriminatorPipeline2;
  aggregate = Aggregate;
  return aggregate;
}
var saveOptions;
var hasRequiredSaveOptions;
function requireSaveOptions() {
  if (hasRequiredSaveOptions) return saveOptions;
  hasRequiredSaveOptions = 1;
  const clone = requireClone();
  class SaveOptions {
    constructor(obj) {
      if (obj == null) {
        return;
      }
      Object.assign(this, clone(obj));
    }
  }
  SaveOptions.prototype.__subdocs = null;
  saveOptions = SaveOptions;
  return saveOptions;
}
var applyDefaultsToPOJO;
var hasRequiredApplyDefaultsToPOJO;
function requireApplyDefaultsToPOJO() {
  if (hasRequiredApplyDefaultsToPOJO) return applyDefaultsToPOJO;
  hasRequiredApplyDefaultsToPOJO = 1;
  applyDefaultsToPOJO = function applyDefaultsToPOJO2(doc, schema2) {
    const paths = Object.keys(schema2.paths);
    const plen = paths.length;
    for (let i = 0; i < plen; ++i) {
      let curPath = "";
      const p = paths[i];
      const type2 = schema2.paths[p];
      const path = type2.splitPath();
      const len = path.length;
      let doc_ = doc;
      for (let j = 0; j < len; ++j) {
        if (doc_ == null) {
          break;
        }
        const piece = path[j];
        curPath += (!curPath.length ? "" : ".") + piece;
        if (j === len - 1) {
          if (typeof doc_[piece] !== "undefined") {
            if (type2.$isSingleNested) {
              applyDefaultsToPOJO2(doc_[piece], type2.schema);
            } else if (type2.$isMongooseDocumentArray && Array.isArray(doc_[piece])) {
              doc_[piece].forEach((el) => applyDefaultsToPOJO2(el, type2.schema));
            }
            break;
          }
          const def = type2.getDefault(doc, false, { skipCast: true });
          if (typeof def !== "undefined") {
            doc_[piece] = def;
            if (type2.$isSingleNested) {
              applyDefaultsToPOJO2(def, type2.schema);
            } else if (type2.$isMongooseDocumentArray && Array.isArray(def)) {
              def.forEach((el) => applyDefaultsToPOJO2(el, type2.schema));
            }
          }
        } else {
          if (doc_[piece] == null) {
            doc_[piece] = {};
          }
          doc_ = doc_[piece];
        }
      }
    }
  };
  return applyDefaultsToPOJO;
}
var applyEmbeddedDiscriminators_1;
var hasRequiredApplyEmbeddedDiscriminators;
function requireApplyEmbeddedDiscriminators() {
  if (hasRequiredApplyEmbeddedDiscriminators) return applyEmbeddedDiscriminators_1;
  hasRequiredApplyEmbeddedDiscriminators = 1;
  applyEmbeddedDiscriminators_1 = applyEmbeddedDiscriminators;
  function applyEmbeddedDiscriminators(schema2, seen = /* @__PURE__ */ new WeakSet(), overwriteExisting = false) {
    if (seen.has(schema2)) {
      return;
    }
    seen.add(schema2);
    for (const path of Object.keys(schema2.paths)) {
      const schemaType2 = schema2.paths[path];
      if (!schemaType2.schema) {
        continue;
      }
      applyEmbeddedDiscriminators(schemaType2.schema, seen);
      if (!schemaType2.schema._applyDiscriminators) {
        continue;
      }
      if (schemaType2._appliedDiscriminators && !overwriteExisting) {
        continue;
      }
      for (const discriminatorKey of schemaType2.schema._applyDiscriminators.keys()) {
        const {
          schema: discriminatorSchema,
          options: options2
        } = schemaType2.schema._applyDiscriminators.get(discriminatorKey);
        applyEmbeddedDiscriminators(discriminatorSchema, seen);
        schemaType2.discriminator(
          discriminatorKey,
          discriminatorSchema,
          overwriteExisting ? { ...options2, overwriteExisting: true } : options2
        );
      }
      schemaType2._appliedDiscriminators = true;
    }
  }
  return applyEmbeddedDiscriminators_1;
}
var applyHooks_1;
var hasRequiredApplyHooks;
function requireApplyHooks() {
  if (hasRequiredApplyHooks) return applyHooks_1;
  hasRequiredApplyHooks = 1;
  const { buildMiddlewareFilter } = requireBuildMiddlewareFilter();
  applyHooks_1 = applyHooks;
  applyHooks.middlewareFunctions = [
    "deleteOne",
    "remove",
    "save",
    "updateOne",
    "validate",
    "init"
  ];
  const alreadyHookedFunctions = new Set(applyHooks.middlewareFunctions.flatMap((fn) => [fn, `$__${fn}`]));
  function applyHooks(model2, schema2, options2) {
    options2 = options2 || {};
    const kareemOptions = {
      useErrorHandlers: true,
      numCallbackParams: 1,
      nullResultByDefault: true,
      contextParameter: true
    };
    const objToDecorate = options2.decorateDoc ? model2 : model2.prototype;
    model2.$appliedHooks = true;
    for (const key of Object.keys(schema2.paths)) {
      let type2 = schema2.paths[key];
      let childModel = null;
      const result = findChildModel(type2);
      if (result) {
        childModel = result.childModel;
        type2 = result.type;
      } else {
        continue;
      }
      if (childModel.$appliedHooks) {
        continue;
      }
      applyHooks(childModel, type2.schema, {
        ...options2,
        decorateDoc: false,
        isChildSchema: true
      });
      if (childModel.discriminators != null) {
        const keys = Object.keys(childModel.discriminators);
        for (const key2 of keys) {
          applyHooks(
            childModel.discriminators[key2],
            childModel.discriminators[key2].schema,
            options2
          );
        }
      }
    }
    const middleware = schema2._getDocumentMiddleware();
    model2._middleware = middleware;
    objToDecorate.$__init = middleware.createWrapperSync("init", objToDecorate.$__init, null, {
      ...kareemOptions,
      getOptions: (args) => {
        const opts = args[1];
        return {
          pre: { filter: buildMiddlewareFilter(opts, "pre") },
          post: { filter: buildMiddlewareFilter(opts, "post") }
        };
      }
    });
    const customMethods = Object.keys(schema2.methods);
    const customMethodOptions = Object.assign({}, kareemOptions, {
      // Only use `checkForPromise` for custom methods, because mongoose
      // query thunks are not as consistent as I would like about returning
      // a nullish value rather than the query. If a query thunk returns
      // a query, `checkForPromise` causes infinite recursion
      checkForPromise: true
    });
    for (const method of customMethods) {
      if (alreadyHookedFunctions.has(method)) {
        continue;
      }
      if (!middleware.hasHooks(method)) {
        continue;
      }
      const originalMethod = objToDecorate[method];
      objToDecorate[`$__${method}`] = objToDecorate[method];
      objToDecorate[method] = middleware.createWrapper(method, originalMethod, null, customMethodOptions);
    }
  }
  function findChildModel(curType) {
    if (curType.$isSingleNested || curType.$isMongooseDocumentArray) {
      return { childModel: curType.Constructor, type: curType };
    }
    if (curType.instance === "Array") {
      const embedded = curType.getEmbeddedSchemaType();
      if (embedded) {
        return findChildModel(embedded);
      }
    }
    if (curType.instance === "Map") {
      const mapType = curType.getEmbeddedSchemaType();
      if (mapType) {
        return findChildModel(mapType);
      }
    }
    return null;
  }
  return applyHooks_1;
}
var applyMethods;
var hasRequiredApplyMethods;
function requireApplyMethods() {
  if (hasRequiredApplyMethods) return applyMethods;
  hasRequiredApplyMethods = 1;
  const get2 = requireGet();
  const utils2 = requireUtils();
  applyMethods = function applyMethods2(model2, schema2) {
    const Model = requireModel();
    function apply(method, schema3) {
      Object.defineProperty(model2.prototype, method, {
        get: function() {
          const h = {};
          for (const k in schema3.methods[method]) {
            h[k] = schema3.methods[method][k].bind(this);
          }
          return h;
        },
        configurable: true
      });
    }
    for (const method of Object.keys(schema2.methods)) {
      const fn = schema2.methods[method];
      if (Object.hasOwn(schema2.tree, method)) {
        throw new Error('You have a method and a property in your schema both named "' + method + '"');
      }
      if (typeof fn === "function" && Model.prototype[method] === fn) {
        delete schema2.methods[method];
        continue;
      }
      if (schema2.reserved[method] && !get2(schema2, `methodOptions.${method}.suppressWarning`, false)) {
        utils2.warn(`mongoose: the method name "${method}" is used by mongoose internally, overwriting it may cause bugs. If you're sure you know what you're doing, you can suppress this error by using \`schema.method('${method}', fn, { suppressWarning: true })\`.`);
      }
      if (typeof fn === "function") {
        model2.prototype[method] = fn;
      } else {
        apply(method, schema2);
      }
    }
    model2.$appliedMethods = true;
    for (const key of Object.keys(schema2.paths)) {
      const type2 = schema2.paths[key];
      if (type2.$isSingleNested && !type2.Constructor.$appliedMethods) {
        applyMethods2(type2.Constructor, type2.schema);
      }
      if (type2.$isMongooseDocumentArray && !type2.Constructor.$appliedMethods) {
        applyMethods2(type2.Constructor, type2.schema);
      }
    }
  };
  return applyMethods;
}
var applyProjection;
var hasRequiredApplyProjection;
function requireApplyProjection() {
  if (hasRequiredApplyProjection) return applyProjection;
  hasRequiredApplyProjection = 1;
  const hasIncludedChildren2 = requireHasIncludedChildren();
  const isExclusive2 = requireIsExclusive();
  const isInclusive2 = requireIsInclusive();
  const isPOJO2 = requireUtils().isPOJO;
  applyProjection = function applyProjection2(doc, projection, _hasIncludedChildren) {
    if (projection == null) {
      return doc;
    }
    if (doc == null) {
      return doc;
    }
    let exclude = null;
    if (isInclusive2(projection)) {
      exclude = false;
    } else if (isExclusive2(projection)) {
      exclude = true;
    }
    if (exclude == null) {
      return doc;
    } else if (exclude) {
      _hasIncludedChildren = _hasIncludedChildren || hasIncludedChildren2(projection);
      return applyExclusiveProjection(doc, projection, _hasIncludedChildren);
    } else {
      _hasIncludedChildren = _hasIncludedChildren || hasIncludedChildren2(projection);
      return applyInclusiveProjection(doc, projection, _hasIncludedChildren);
    }
  };
  function applyExclusiveProjection(doc, projection, hasIncludedChildren3, projectionLimb, prefix) {
    if (doc == null || typeof doc !== "object") {
      return doc;
    }
    if (Array.isArray(doc)) {
      return doc.map((el) => applyExclusiveProjection(el, projection, hasIncludedChildren3, projectionLimb, prefix));
    }
    const ret = { ...doc };
    projectionLimb = prefix ? projectionLimb || {} : projection;
    for (const key of Object.keys(ret)) {
      const fullPath = prefix ? prefix + "." + key : key;
      if (Object.hasOwn(projection, fullPath) || Object.hasOwn(projectionLimb, key)) {
        if (isPOJO2(projection[fullPath]) || isPOJO2(projectionLimb[key])) {
          ret[key] = applyExclusiveProjection(ret[key], projection, hasIncludedChildren3, projectionLimb[key], fullPath);
        } else {
          delete ret[key];
        }
      } else if (hasIncludedChildren3[fullPath]) {
        ret[key] = applyExclusiveProjection(ret[key], projection, hasIncludedChildren3, projectionLimb[key], fullPath);
      }
    }
    return ret;
  }
  function applyInclusiveProjection(doc, projection, hasIncludedChildren3, projectionLimb, prefix) {
    if (doc == null || typeof doc !== "object") {
      return doc;
    }
    if (Array.isArray(doc)) {
      return doc.map((el) => applyInclusiveProjection(el, projection, hasIncludedChildren3, projectionLimb, prefix));
    }
    const ret = { ...doc };
    projectionLimb = prefix ? projectionLimb || {} : projection;
    for (const key of Object.keys(ret)) {
      const fullPath = prefix ? prefix + "." + key : key;
      if (Object.hasOwn(projection, fullPath) || Object.hasOwn(projectionLimb, key)) {
        if (isPOJO2(projection[fullPath]) || isPOJO2(projectionLimb[key])) {
          ret[key] = applyInclusiveProjection(ret[key], projection, hasIncludedChildren3, projectionLimb[key], fullPath);
        }
        continue;
      } else if (hasIncludedChildren3[fullPath]) {
        ret[key] = applyInclusiveProjection(ret[key], projection, hasIncludedChildren3, projectionLimb[key], fullPath);
      } else {
        delete ret[key];
      }
    }
    return ret;
  }
  return applyProjection;
}
var isTextIndex;
var hasRequiredIsTextIndex;
function requireIsTextIndex() {
  if (hasRequiredIsTextIndex) return isTextIndex;
  hasRequiredIsTextIndex = 1;
  isTextIndex = function isTextIndex2(indexKeys) {
    let isTextIndex3 = false;
    for (const key of Object.keys(indexKeys)) {
      if (indexKeys[key] === "text") {
        isTextIndex3 = true;
      }
    }
    return isTextIndex3;
  };
  return isTextIndex;
}
var applySchemaCollation;
var hasRequiredApplySchemaCollation;
function requireApplySchemaCollation() {
  if (hasRequiredApplySchemaCollation) return applySchemaCollation;
  hasRequiredApplySchemaCollation = 1;
  const isTextIndex2 = requireIsTextIndex();
  applySchemaCollation = function applySchemaCollation2(indexKeys, indexOptions, schemaOptions) {
    if (isTextIndex2(indexKeys)) {
      return;
    }
    if (Object.hasOwn(schemaOptions, "collation") && !Object.hasOwn(indexOptions, "collation")) {
      indexOptions.collation = schemaOptions.collation;
    }
  };
  return applySchemaCollation;
}
var applyStaticHooks;
var hasRequiredApplyStaticHooks;
function requireApplyStaticHooks() {
  if (hasRequiredApplyStaticHooks) return applyStaticHooks;
  hasRequiredApplyStaticHooks = 1;
  const { queryMiddlewareFunctions, aggregateMiddlewareFunctions, modelMiddlewareFunctions, documentMiddlewareFunctions } = requireConstants();
  const middlewareFunctions = Array.from(
    /* @__PURE__ */ new Set([
      ...queryMiddlewareFunctions,
      ...aggregateMiddlewareFunctions,
      ...modelMiddlewareFunctions,
      ...documentMiddlewareFunctions
    ])
  );
  applyStaticHooks = function applyStaticHooks2(model2, hooks, statics) {
    hooks = hooks.filter((hook) => {
      if (middlewareFunctions.indexOf(hook.name) !== -1) {
        return !!hook.model;
      }
      return hook.model !== false;
    });
    for (const key of Object.keys(statics)) {
      if (hooks.hasHooks(key)) {
        const original = model2[key];
        model2[key] = hooks.createWrapper(key, original);
      }
    }
  };
  return applyStaticHooks;
}
var applyStatics;
var hasRequiredApplyStatics;
function requireApplyStatics() {
  if (hasRequiredApplyStatics) return applyStatics;
  hasRequiredApplyStatics = 1;
  applyStatics = function applyStatics2(model2, schema2) {
    for (const i in schema2.statics) {
      model2[i] = schema2.statics[i];
    }
  };
  return applyStatics;
}
var applyTimestamps_1;
var hasRequiredApplyTimestamps;
function requireApplyTimestamps() {
  if (hasRequiredApplyTimestamps) return applyTimestamps_1;
  hasRequiredApplyTimestamps = 1;
  const handleTimestampOption = requireHandleTimestampOption();
  const mpath = requireMpath();
  const utils2 = requireUtils();
  applyTimestamps_1 = applyTimestamps;
  function applyTimestamps(schema2, obj, options2) {
    if (obj == null) {
      return obj;
    }
    applyTimestampsToChildren(schema2, obj, options2);
    return applyTimestampsToDoc(schema2, obj, options2);
  }
  function applyTimestampsToChildren(schema2, res, options2) {
    for (const childSchema of schema2.childSchemas) {
      const _path = childSchema.model.path;
      const _schema = childSchema.schema;
      if (!_path) {
        continue;
      }
      const _obj = mpath.get(_path, res);
      if (_obj == null || Array.isArray(_obj) && _obj.flat(Infinity).length === 0) {
        continue;
      }
      applyTimestamps(_schema, _obj, options2);
    }
  }
  function applyTimestampsToDoc(schema2, obj, options2) {
    if (obj == null || typeof obj !== "object") {
      return;
    }
    if (Array.isArray(obj)) {
      for (const el of obj) {
        applyTimestampsToDoc(schema2, el, options2);
      }
      return;
    }
    if (schema2.discriminators && utils2.hasOwnKeys(schema2.discriminators)) {
      for (const discriminatorKey of Object.keys(schema2.discriminators)) {
        const discriminator2 = schema2.discriminators[discriminatorKey];
        const key = discriminator2.discriminatorMapping.key;
        const value = discriminator2.discriminatorMapping.value;
        if (obj[key] == value) {
          schema2 = discriminator2;
          break;
        }
      }
    }
    const createdAt = handleTimestampOption(schema2.options.timestamps, "createdAt");
    const updatedAt = handleTimestampOption(schema2.options.timestamps, "updatedAt");
    const currentTime = options2?.currentTime;
    let ts = null;
    if (currentTime != null) {
      ts = currentTime();
    } else if (schema2.base?.now) {
      ts = schema2.base.now();
    } else {
      ts = /* @__PURE__ */ new Date();
    }
    if (createdAt && obj[createdAt] == null && !options2?.isUpdate) {
      obj[createdAt] = ts;
    }
    if (updatedAt) {
      obj[updatedAt] = ts;
    }
  }
  return applyTimestamps_1;
}
var applyVirtuals_1;
var hasRequiredApplyVirtuals;
function requireApplyVirtuals() {
  if (hasRequiredApplyVirtuals) return applyVirtuals_1;
  hasRequiredApplyVirtuals = 1;
  const mpath = requireMpath();
  const utils2 = requireUtils();
  applyVirtuals_1 = applyVirtuals;
  function applyVirtuals(schema2, obj, virtuals) {
    if (obj == null) {
      return obj;
    }
    let virtualsForChildren = virtuals;
    let toApply = null;
    if (Array.isArray(virtuals)) {
      virtualsForChildren = [];
      toApply = [];
      for (const virtual of virtuals) {
        if (virtual.length === 1) {
          toApply.push(virtual[0]);
        } else {
          virtualsForChildren.push(virtual);
        }
      }
    }
    applyVirtualsToChildren(schema2, obj, virtualsForChildren);
    return applyVirtualsToDoc(schema2, obj, toApply);
  }
  function applyVirtualsToChildren(schema2, res, virtuals) {
    let attachedVirtuals = false;
    for (const childSchema of schema2.childSchemas) {
      const _path = childSchema.model.path;
      const _schema = childSchema.schema;
      if (!_path) {
        continue;
      }
      const _obj = mpath.get(_path, res);
      if (_obj == null || Array.isArray(_obj) && _obj.flat(Infinity).length === 0) {
        continue;
      }
      let virtualsForChild = null;
      if (Array.isArray(virtuals)) {
        virtualsForChild = [];
        for (const virtual of virtuals) {
          if (virtual[0] == _path) {
            virtualsForChild.push(virtual.slice(1));
          }
        }
        if (virtualsForChild.length === 0) {
          continue;
        }
      }
      applyVirtuals(_schema, _obj, virtualsForChild);
      attachedVirtuals = true;
    }
    if (virtuals?.length && !attachedVirtuals) {
      applyVirtualsToDoc(schema2, res, virtuals);
    }
  }
  function applyVirtualsToDoc(schema2, obj, virtuals) {
    if (obj == null || typeof obj !== "object") {
      return;
    }
    if (Array.isArray(obj)) {
      for (const el of obj) {
        applyVirtualsToDoc(schema2, el, virtuals);
      }
      return;
    }
    if (schema2.discriminators && utils2.hasOwnKeys(schema2.discriminators)) {
      for (const discriminatorKey of Object.keys(schema2.discriminators)) {
        const discriminator2 = schema2.discriminators[discriminatorKey];
        const key = discriminator2.discriminatorMapping.key;
        const value = discriminator2.discriminatorMapping.value;
        if (obj[key] == value) {
          schema2 = discriminator2;
          break;
        }
      }
    }
    if (virtuals == null) {
      virtuals = Object.keys(schema2.virtuals);
    }
    for (const virtual of virtuals) {
      if (schema2.virtuals[virtual] == null) {
        continue;
      }
      const virtualType2 = schema2.virtuals[virtual];
      const sp = Array.isArray(virtual) ? virtual : virtual.indexOf(".") === -1 ? [virtual] : virtual.split(".");
      let cur = obj;
      for (let i = 0; i < sp.length - 1; ++i) {
        cur[sp[i]] = sp[i] in cur ? cur[sp[i]] : {};
        cur = cur[sp[i]];
      }
      let val = virtualType2.applyGetters(cur[sp[sp.length - 1]], obj);
      const isPopulateVirtual = virtualType2.options?.ref || virtualType2.options?.refPath;
      if (isPopulateVirtual && val === void 0) {
        if (virtualType2.options.justOne) {
          val = null;
        } else {
          val = [];
        }
      }
      cur[sp[sp.length - 1]] = val;
    }
  }
  return applyVirtuals_1;
}
var skipPopulateValue;
var hasRequiredSkipPopulateValue;
function requireSkipPopulateValue() {
  if (hasRequiredSkipPopulateValue) return skipPopulateValue;
  hasRequiredSkipPopulateValue = 1;
  skipPopulateValue = function SkipPopulateValue(val) {
    if (!(this instanceof SkipPopulateValue)) {
      return new SkipPopulateValue(val);
    }
    this.val = val;
    return this;
  };
  return skipPopulateValue;
}
var leanPopulateMap;
var hasRequiredLeanPopulateMap;
function requireLeanPopulateMap() {
  if (hasRequiredLeanPopulateMap) return leanPopulateMap;
  hasRequiredLeanPopulateMap = 1;
  leanPopulateMap = /* @__PURE__ */ new WeakMap();
  return leanPopulateMap;
}
var assignRawDocsToIdStructure_1;
var hasRequiredAssignRawDocsToIdStructure;
function requireAssignRawDocsToIdStructure() {
  if (hasRequiredAssignRawDocsToIdStructure) return assignRawDocsToIdStructure_1;
  hasRequiredAssignRawDocsToIdStructure = 1;
  const clone = requireClone();
  const leanPopulateMap2 = requireLeanPopulateMap();
  const modelSymbol = requireSymbols$1().modelSymbol;
  const utils2 = requireUtils();
  assignRawDocsToIdStructure_1 = assignRawDocsToIdStructure;
  const kHasArray = /* @__PURE__ */ Symbol("mongoose#assignRawDocsToIdStructure#hasArray");
  function assignRawDocsToIdStructure(rawIds, resultDocs, resultOrder, options2, recursed) {
    const newOrder = [];
    const sorting = options2.isVirtual && options2.justOne && rawIds.length > 1 ? false : options2.sort && rawIds.length > 1;
    const nullIfNotFound = options2.$nullIfNotFound;
    let doc;
    let sid;
    let id;
    if (utils2.isMongooseArray(rawIds)) {
      rawIds = rawIds.__array;
    }
    let i = 0;
    const len = rawIds.length;
    if (sorting && recursed && options2[kHasArray] === void 0) {
      options2[kHasArray] = false;
      for (const key in resultOrder) {
        if (Array.isArray(resultOrder[key])) {
          options2[kHasArray] = true;
          break;
        }
      }
    }
    for (i = 0; i < len; ++i) {
      id = rawIds[i];
      if (Array.isArray(id)) {
        assignRawDocsToIdStructure(id, resultDocs, resultOrder, options2, true);
        newOrder.push(id);
        continue;
      }
      if (id === null && sorting === false) {
        newOrder.push(id);
        continue;
      }
      sid = String(id);
      doc = resultDocs[sid];
      if (options2.clone && doc != null) {
        if (options2.lean) {
          const _model = leanPopulateMap2.get(doc);
          doc = clone(doc);
          leanPopulateMap2.set(doc, _model);
        } else {
          doc = doc.constructor.hydrate(doc._doc);
        }
      }
      if (recursed) {
        if (doc) {
          if (sorting) {
            const _resultOrder = resultOrder[sid];
            if (options2[kHasArray]) {
              newOrder.push(doc);
            } else {
              newOrder[_resultOrder] = doc;
            }
          } else {
            newOrder.push(doc);
          }
        } else if (id?.[modelSymbol] != null) {
          newOrder.push(id);
        } else {
          newOrder.push(options2.retainNullValues || nullIfNotFound ? null : id);
        }
      } else {
        newOrder[i] = doc || null;
      }
    }
    rawIds.length = 0;
    if (newOrder.length) {
      newOrder.forEach(function(doc2, i2) {
        rawIds[i2] = doc2;
      });
    }
  }
  return assignRawDocsToIdStructure_1;
}
var getVirtual_1;
var hasRequiredGetVirtual;
function requireGetVirtual() {
  if (hasRequiredGetVirtual) return getVirtual_1;
  hasRequiredGetVirtual = 1;
  getVirtual_1 = getVirtual;
  function getVirtual(schema2, name) {
    if (schema2.virtuals[name]) {
      return { virtual: schema2.virtuals[name], path: void 0 };
    }
    const parts = name.split(".");
    let cur = "";
    let nestedSchemaPath = "";
    for (let i = 0; i < parts.length; ++i) {
      cur += (cur.length > 0 ? "." : "") + parts[i];
      if (schema2.virtuals[cur]) {
        if (i === parts.length - 1) {
          return { virtual: schema2.virtuals[cur], path: nestedSchemaPath };
        }
        continue;
      }
      if (schema2.nested[cur]) {
        continue;
      }
      if (schema2.paths[cur] && schema2.paths[cur].schema) {
        schema2 = schema2.paths[cur].schema;
        const rest = parts.slice(i + 1).join(".");
        if (schema2.virtuals[rest]) {
          if (i === parts.length - 2) {
            return {
              virtual: schema2.virtuals[rest],
              nestedSchemaPath: [nestedSchemaPath, cur].filter((v) => !!v).join(".")
            };
          }
          continue;
        }
        if (i + 1 < parts.length && schema2.discriminators) {
          for (const key of Object.keys(schema2.discriminators)) {
            const res = getVirtual(schema2.discriminators[key], rest);
            if (res != null) {
              const _path = [nestedSchemaPath, cur, res.nestedSchemaPath].filter((v) => !!v).join(".");
              return {
                virtual: res.virtual,
                nestedSchemaPath: _path
              };
            }
          }
        }
        nestedSchemaPath += (nestedSchemaPath.length > 0 ? "." : "") + cur;
        cur = "";
        continue;
      } else if (schema2.paths[cur]?.$isSchemaMap && schema2.paths[cur].$__schemaType?.schema) {
        schema2 = schema2.paths[cur].$__schemaType.schema;
        ++i;
        const rest = parts.slice(i + 1).join(".");
        if (schema2.virtuals[rest]) {
          if (i === parts.length - 2) {
            return {
              virtual: schema2.virtuals[rest],
              nestedSchemaPath: [nestedSchemaPath, cur, "$*"].filter((v) => !!v).join(".")
            };
          }
          continue;
        }
        if (i + 1 < parts.length && schema2.discriminators) {
          for (const key of Object.keys(schema2.discriminators)) {
            const res = getVirtual(schema2.discriminators[key], rest);
            if (res != null) {
              const _path = [nestedSchemaPath, cur, res.nestedSchemaPath, "$*"].filter((v) => !!v).join(".");
              return {
                virtual: res.virtual,
                nestedSchemaPath: _path
              };
            }
          }
        }
        nestedSchemaPath += (nestedSchemaPath.length > 0 ? "." : "") + "$*" + cur;
        cur = "";
      }
      if (schema2.discriminators) {
        for (const discriminatorKey of Object.keys(schema2.discriminators)) {
          const virtualFromDiscriminator = getVirtual(schema2.discriminators[discriminatorKey], name);
          if (virtualFromDiscriminator) return virtualFromDiscriminator;
        }
      }
      return null;
    }
  }
  return getVirtual_1;
}
var assignVals;
var hasRequiredAssignVals;
function requireAssignVals() {
  if (hasRequiredAssignVals) return assignVals;
  hasRequiredAssignVals = 1;
  const MongooseMap = requireMap$1();
  const SkipPopulateValue = requireSkipPopulateValue();
  const assignRawDocsToIdStructure = requireAssignRawDocsToIdStructure();
  const get2 = requireGet();
  const getVirtual = requireGetVirtual();
  const leanPopulateMap2 = requireLeanPopulateMap();
  const lookupLocalFields2 = requireLookupLocalFields();
  const markArraySubdocsPopulated2 = requireMarkArraySubdocsPopulated();
  const mpath = requireMpath();
  const sift = require$$9.default;
  const utils2 = requireUtils();
  const { populateModelSymbol } = requireSymbols$1();
  assignVals = function assignVals2(o) {
    const userOptions = Object.assign({}, get2(o, "allOptions.options.options"), get2(o, "allOptions.options"));
    const populateOptions2 = Object.assign({}, o.options, userOptions, {
      justOne: o.justOne,
      isVirtual: o.isVirtual
    });
    populateOptions2.$nullIfNotFound = o.isVirtual;
    const populatedModel = o.populatedModel;
    const originalIds = [].concat(o.rawIds);
    o.allIds = [].concat(o.allIds);
    assignRawDocsToIdStructure(o.rawIds, o.rawDocs, o.rawOrder, populateOptions2);
    const docs = o.docs;
    const rawIds = o.rawIds;
    const options2 = o.options;
    const count = o.count && o.isVirtual;
    let i;
    let setValueIndex = 0;
    function setValue(val) {
      ++setValueIndex;
      if (count) {
        return val;
      }
      if (val instanceof SkipPopulateValue) {
        return val.val;
      }
      if (val === void 0) {
        return val;
      }
      const _allIds = o.allIds[i];
      if (o.path.endsWith(".$*")) {
        return valueFilter(val, options2, populateOptions2, _allIds);
      }
      if (o.justOne === true && Array.isArray(val)) {
        const ret = [];
        for (const doc of val) {
          const _docPopulatedModel = leanPopulateMap2.get(doc);
          if (_docPopulatedModel == null || _docPopulatedModel === populatedModel) {
            ret.push(doc);
          }
        }
        while (val.length > ret.length) {
          Array.prototype.pop.apply(val, []);
        }
        for (let i2 = 0; i2 < ret.length; ++i2) {
          val[i2] = ret[i2];
        }
        return valueFilter(val[0], options2, populateOptions2, _allIds);
      } else if (o.justOne === false && !Array.isArray(val)) {
        return valueFilter([val], options2, populateOptions2, _allIds);
      } else if (o.justOne === true && !Array.isArray(val) && Array.isArray(_allIds)) {
        return valueFilter(val, options2, populateOptions2, val == null ? val : _allIds[setValueIndex - 1]);
      }
      return valueFilter(val, options2, populateOptions2, _allIds);
    }
    for (i = 0; i < docs.length; ++i) {
      setValueIndex = 0;
      const _path = o.path.endsWith(".$*") ? o.path.slice(0, -3) : o.path;
      const existingVal = mpath.get(_path, docs[i], lookupLocalFields2);
      if (existingVal == null && !getVirtual(o.originalModel.schema, _path)) {
        continue;
      }
      let valueToSet;
      if (count) {
        valueToSet = numDocs(rawIds[i]);
      } else if (Array.isArray(o.match)) {
        valueToSet = Array.isArray(rawIds[i]) ? rawIds[i].filter((v) => v == null || sift(o.match[i])(v)) : [rawIds[i]].filter((v) => v == null || sift(o.match[i])(v))[0];
      } else {
        valueToSet = rawIds[i];
      }
      const originalSchema = o.originalModel.schema;
      const isDoc = get2(docs[i], "$__", null) != null;
      let isMap = isDoc ? existingVal instanceof Map : utils2.isPOJO(existingVal);
      isMap = isMap && get2(originalSchema._getSchema(_path), "$isSchemaMap");
      if (!o.isVirtual && isMap) {
        const _keys = existingVal instanceof Map ? Array.from(existingVal.keys()) : Object.keys(existingVal);
        valueToSet = valueToSet.reduce((cur2, v, i2) => {
          cur2.set(_keys[i2], v);
          return cur2;
        }, /* @__PURE__ */ new Map());
      }
      if (isDoc && Array.isArray(valueToSet)) {
        for (const val of valueToSet) {
          if (val?.$__ != null) {
            val.$__.parent = docs[i];
          }
        }
      } else if (isDoc && valueToSet?.$__ != null) {
        valueToSet.$__.parent = docs[i];
      }
      if (o.isVirtual && isDoc) {
        docs[i].$populated(_path, o.justOne ? originalIds[0] : originalIds, o.allOptions);
        if (Array.isArray(valueToSet)) {
          valueToSet = valueToSet.map((v) => v == null ? void 0 : v);
        }
        mpath.set(
          _path,
          valueToSet,
          docs[i],
          // Handle setting paths underneath maps using $* by converting arrays into maps of values
          function lookup(obj, part, val) {
            if (arguments.length >= 3) {
              obj[part] = val;
              return obj[part];
            }
            if (obj instanceof Map && part === "$*") {
              return [...obj.values()];
            }
            return obj[part];
          },
          setValue,
          false
        );
        continue;
      }
      const parts = _path.split(".");
      let cur = docs[i];
      for (let j = 0; j < parts.length - 1; ++j) {
        if (Array.isArray(cur) && !utils2.isArrayIndex(parts[j])) {
          break;
        }
        if (parts[j] === "$*") {
          break;
        }
        if (cur[parts[j]] == null) {
          const curPath = parts.slice(0, j + 1).join(".");
          const schematype = originalSchema._getSchema(curPath);
          if (valueToSet == null && schematype?.$isMongooseArray) {
            break;
          }
          cur[parts[j]] = {};
        }
        cur = cur[parts[j]];
        if (typeof cur !== "object") {
          break;
        }
      }
      if (docs[i].$__) {
        o.allOptions.options[populateModelSymbol] = o.allOptions.model;
        docs[i].$populated(_path, o.unpopulatedValues[i], o.allOptions.options);
        if (valueToSet?.$__ != null) {
          valueToSet.$__.wasPopulated = { value: o.unpopulatedValues[i] };
        }
        if (valueToSet instanceof Map && !valueToSet.$isMongooseMap) {
          valueToSet = new MongooseMap(valueToSet, _path, docs[i], docs[i].schema.path(_path).$__schemaType);
        }
      }
      mpath.set(_path, valueToSet, docs[i], lookupLocalFields2, setValue, false);
      if (docs[i].$__) {
        markArraySubdocsPopulated2(docs[i], [o.allOptions.options]);
      }
    }
  };
  function numDocs(v) {
    if (Array.isArray(v)) {
      if (v.some((el) => Array.isArray(el) || el === null)) {
        return v.map((el) => {
          if (el == null) {
            return 0;
          }
          if (Array.isArray(el)) {
            return el.filter((el2) => el2 != null).length;
          }
          return 1;
        });
      }
      return v.filter((el) => el != null).length;
    }
    return v == null ? 0 : 1;
  }
  function valueFilter(val, assignmentOpts, populateOptions2, allIds) {
    const userSpecifiedTransform = typeof populateOptions2.transform === "function";
    const transform = userSpecifiedTransform ? populateOptions2.transform : (v) => v;
    if (Array.isArray(val)) {
      const ret = [];
      const numValues = val.length;
      for (let i2 = 0; i2 < numValues; ++i2) {
        let subdoc = val[i2];
        const _allIds = Array.isArray(allIds) ? allIds[i2] : allIds;
        if (!isPopulatedObject(subdoc) && (!populateOptions2.retainNullValues || subdoc != null) && !userSpecifiedTransform) {
          continue;
        } else if (!populateOptions2.retainNullValues && subdoc == null) {
          continue;
        } else if (userSpecifiedTransform) {
          subdoc = transform(isPopulatedObject(subdoc) ? subdoc : null, _allIds);
        }
        maybeRemoveId(subdoc, assignmentOpts);
        ret.push(subdoc);
        if (assignmentOpts.originalLimit && ret.length >= assignmentOpts.originalLimit) {
          break;
        }
      }
      const rLen = ret.length;
      while (val.length > rLen) {
        Array.prototype.pop.apply(val, []);
      }
      let i = 0;
      if (utils2.isMongooseArray(val)) {
        for (i = 0; i < rLen; ++i) {
          val.set(i, ret[i], true);
        }
      } else {
        for (i = 0; i < rLen; ++i) {
          val[i] = ret[i];
        }
      }
      return val;
    }
    if (isPopulatedObject(val) || utils2.isPOJO(val)) {
      maybeRemoveId(val, assignmentOpts);
      return transform(val, allIds);
    }
    if (val instanceof Map) {
      return val;
    }
    if (populateOptions2.justOne === false) {
      return [];
    }
    return val == null ? transform(val, allIds) : transform(null, allIds);
  }
  function maybeRemoveId(subdoc, assignmentOpts) {
    if (subdoc != null && assignmentOpts.excludeId) {
      if (typeof subdoc.$__setValue === "function") {
        delete subdoc._doc._id;
      } else {
        delete subdoc._id;
      }
    }
  }
  function isPopulatedObject(obj) {
    if (obj == null) {
      return false;
    }
    return Array.isArray(obj) || obj.$isMongooseMap || obj.$__ != null || leanPopulateMap2.has(obj);
  }
  return assignVals;
}
var convertErrorToIssues;
var hasRequiredConvertErrorToIssues;
function requireConvertErrorToIssues() {
  if (hasRequiredConvertErrorToIssues) return convertErrorToIssues;
  hasRequiredConvertErrorToIssues = 1;
  const ValidationError = requireValidation();
  convertErrorToIssues = function convertErrorToIssues2(error2) {
    if (error2 instanceof ValidationError) {
      return Object.keys(error2.errors).map((path) => {
        const err = error2.errors[path];
        return {
          message: err.message,
          path: path.split(".").map((part) => {
            const num = +part;
            return Number.isInteger(num) && String(num) === part ? num : part;
          })
        };
      });
    }
    return [{ message: error2.message }];
  };
  return convertErrorToIssues;
}
var createPopulateQueryFilter;
var hasRequiredCreatePopulateQueryFilter;
function requireCreatePopulateQueryFilter() {
  if (hasRequiredCreatePopulateQueryFilter) return createPopulateQueryFilter;
  hasRequiredCreatePopulateQueryFilter = 1;
  const SkipPopulateValue = requireSkipPopulateValue();
  const parentPaths2 = requireParentPaths();
  const { trusted: trusted2 } = requireTrusted();
  const hasDollarKeys2 = requireHasDollarKeys();
  createPopulateQueryFilter = function createPopulateQueryFilter2(ids, _match, _foreignField, model2, skipInvalidIds) {
    const match = _formatMatch(_match);
    if (_foreignField.size === 1) {
      const foreignField = Array.from(_foreignField)[0];
      const foreignSchemaType = model2.schema.path(foreignField);
      if (foreignField !== "_id" || !match["_id"]) {
        ids = _filterInvalidIds(ids, foreignSchemaType, skipInvalidIds);
        match[foreignField] = trusted2({ $in: ids });
      } else if (foreignField === "_id" && match["_id"]) {
        const userSpecifiedMatch = hasDollarKeys2(match[foreignField]) ? match[foreignField] : { $eq: match[foreignField] };
        match[foreignField] = { ...trusted2({ $in: ids }), ...userSpecifiedMatch };
      }
      const _parentPaths = parentPaths2(foreignField);
      for (let i = 0; i < _parentPaths.length - 1; ++i) {
        const cur = _parentPaths[i];
        if (match[cur] != null && match[cur].$elemMatch != null) {
          match[cur] = {
            ...match[cur],
            $elemMatch: {
              ...match[cur].$elemMatch,
              [foreignField.slice(cur.length + 1)]: trusted2({ $in: ids })
            }
          };
          delete match[foreignField];
          break;
        }
      }
    } else {
      const $or = [];
      if (Array.isArray(match.$or)) {
        match.$and = [{ $or: match.$or }, { $or }];
        delete match.$or;
      } else {
        match.$or = $or;
      }
      for (const foreignField of _foreignField) {
        if (foreignField !== "_id" || !match["_id"]) {
          const foreignSchemaType = model2.schema.path(foreignField);
          ids = _filterInvalidIds(ids, foreignSchemaType, skipInvalidIds);
          $or.push({ [foreignField]: { $in: ids } });
        } else if (foreignField === "_id" && match["_id"]) {
          const userSpecifiedMatch = hasDollarKeys2(match[foreignField]) ? match[foreignField] : { $eq: match[foreignField] };
          match[foreignField] = { ...trusted2({ $in: ids }), ...userSpecifiedMatch };
        }
      }
    }
    return match;
  };
  function _filterInvalidIds(ids, foreignSchemaType, skipInvalidIds) {
    ids = ids.filter((v) => !(v instanceof SkipPopulateValue));
    if (!skipInvalidIds) {
      return ids;
    }
    return ids.filter((id) => {
      try {
        foreignSchemaType.cast(id);
        return true;
      } catch {
        return false;
      }
    });
  }
  function _formatMatch(match) {
    if (Array.isArray(match)) {
      if (match.length > 1) {
        return { $or: [].concat(match.map((m) => Object.assign({}, m))) };
      }
      return Object.assign({}, match[0]);
    }
    return Object.assign({}, match);
  }
  return createPopulateQueryFilter;
}
var each;
var hasRequiredEach;
function requireEach() {
  if (hasRequiredEach) return each;
  hasRequiredEach = 1;
  each = function each2(arr, cb, done) {
    if (arr.length === 0) {
      return done();
    }
    let remaining = arr.length;
    let err = null;
    for (const v of arr) {
      cb(v, function(_err) {
        if (err != null) {
          return;
        }
        if (_err != null) {
          err = _err;
          return done(err);
        }
        if (--remaining <= 0) {
          return done();
        }
      });
    }
  };
  return each;
}
var getSchemaTypes;
var hasRequiredGetSchemaTypes;
function requireGetSchemaTypes() {
  if (hasRequiredGetSchemaTypes) return getSchemaTypes;
  hasRequiredGetSchemaTypes = 1;
  const Mixed = requireMixed();
  const get2 = requireGet();
  const getDiscriminatorByValue2 = requireGetDiscriminatorByValue();
  const leanPopulateMap2 = requireLeanPopulateMap();
  const mpath = requireMpath();
  const populateModelSymbol = requireSymbols$1().populateModelSymbol;
  getSchemaTypes = function getSchemaTypes2(model2, schema2, doc, path, parts) {
    const pathschema = schema2.path(path);
    const topLevelDoc = doc;
    if (pathschema) {
      return pathschema;
    }
    const discriminatorKey = schema2.discriminatorMapping?.key;
    if (discriminatorKey && model2 != null) {
      if (doc?.[discriminatorKey] != null) {
        const discriminator2 = getDiscriminatorByValue2(model2.discriminators, doc[discriminatorKey]);
        schema2 = discriminator2?.schema || schema2;
      } else if (model2.discriminators != null) {
        return Object.keys(model2.discriminators).reduce((arr, name) => {
          const disc = model2.discriminators[name];
          return arr.concat(getSchemaTypes2(disc, disc.schema, null, path));
        }, []);
      }
    }
    function search(parts2, schema3, subdoc, nestedPath) {
      let p = parts2.length + 1;
      let foundschema;
      let trypath;
      while (p--) {
        trypath = parts2.slice(0, p).join(".");
        foundschema = schema3.path(trypath);
        if (foundschema == null) {
          continue;
        }
        if (foundschema.embeddedSchemaType || foundschema.schema) {
          if (foundschema.embeddedSchemaType instanceof Mixed) {
            return foundschema.embeddedSchemaType;
          }
          let schemas = null;
          if (foundschema.schema?.discriminators != null) {
            const discriminators = foundschema.schema.discriminators;
            const discriminatorKeyPath = trypath + "." + foundschema.schema.options.discriminatorKey;
            const keys = subdoc ? mpath.get(discriminatorKeyPath, subdoc) || [] : [];
            schemas = Object.keys(discriminators).reduce(function(cur, discriminator2) {
              const tiedValue = discriminators[discriminator2].discriminatorMapping.value;
              if (doc == null || keys.indexOf(discriminator2) !== -1 || keys.indexOf(tiedValue) !== -1) {
                cur.push(discriminators[discriminator2]);
              }
              return cur;
            }, []);
          }
          if (p !== parts2.length && foundschema.schema) {
            let ret;
            if (parts2[p] === "$") {
              if (p + 1 === parts2.length) {
                return foundschema;
              }
              ret = search(
                parts2.slice(p + 1),
                schema3,
                subdoc ? mpath.get(trypath, subdoc) : null,
                nestedPath.concat(parts2.slice(0, p))
              );
              if (ret) {
                ret.$parentSchemaDocArray = ret.$parentSchemaDocArray || (foundschema.schema.$isSingleNested ? null : foundschema);
              }
              return ret;
            }
            if (schemas != null && schemas.length > 0) {
              ret = [];
              for (const schema4 of schemas) {
                const _ret = search(
                  parts2.slice(p),
                  schema4,
                  subdoc ? mpath.get(trypath, subdoc) : null,
                  nestedPath.concat(parts2.slice(0, p))
                );
                if (_ret != null) {
                  _ret.$parentSchemaDocArray = _ret.$parentSchemaDocArray || (foundschema.schema.$isSingleNested ? null : foundschema);
                  if (_ret.$parentSchemaDocArray) {
                    ret.$parentSchemaDocArray = _ret.$parentSchemaDocArray;
                  }
                  ret.push(_ret);
                }
              }
              return ret;
            } else {
              ret = search(
                parts2.slice(p),
                foundschema.schema,
                subdoc ? mpath.get(trypath, subdoc) : null,
                nestedPath.concat(parts2.slice(0, p))
              );
              if (ret) {
                ret.$parentSchemaDocArray = ret.$parentSchemaDocArray || (foundschema.schema.$isSingleNested ? null : foundschema);
              }
              return ret;
            }
          } else if (p !== parts2.length && foundschema.$isMongooseArray && foundschema.embeddedSchemaType.$isMongooseArray) {
            let type2 = foundschema;
            while (type2.$isMongooseArray && !type2.$isMongooseDocumentArray) {
              type2 = type2.embeddedSchemaType;
            }
            const ret = search(
              parts2.slice(p),
              type2.schema,
              null,
              nestedPath.concat(parts2.slice(0, p))
            );
            if (ret != null) {
              return ret;
            }
            if (type2.schema.discriminators) {
              const discriminatorPaths = [];
              for (const discriminatorName of Object.keys(type2.schema.discriminators)) {
                const _schema = type2.schema.discriminators[discriminatorName] || type2.schema;
                const ret2 = search(parts2.slice(p), _schema, null, nestedPath.concat(parts2.slice(0, p)));
                if (ret2 != null) {
                  discriminatorPaths.push(ret2);
                }
              }
              if (discriminatorPaths.length > 0) {
                return discriminatorPaths;
              }
            }
          }
        } else if (foundschema.$isSchemaMap && foundschema.$__schemaType instanceof Mixed) {
          return foundschema.$__schemaType;
        }
        const fullPath = nestedPath.concat([trypath]).join(".");
        if (topLevelDoc?.$__ && topLevelDoc.$populated(fullPath, true) && p < parts2.length) {
          const model3 = topLevelDoc.$populated(fullPath, true).options[populateModelSymbol];
          if (model3 != null) {
            const ret = search(
              parts2.slice(p),
              model3.schema,
              subdoc ? mpath.get(trypath, subdoc) : null,
              nestedPath.concat(parts2.slice(0, p))
            );
            return ret;
          }
        }
        const _val = get2(topLevelDoc, trypath);
        if (_val != null) {
          const model3 = Array.isArray(_val) && _val.length > 0 ? leanPopulateMap2.get(_val[0]) : leanPopulateMap2.get(_val);
          const schema4 = model3?.schema ?? null;
          if (schema4 != null) {
            const ret = search(
              parts2.slice(p),
              schema4,
              subdoc ? mpath.get(trypath, subdoc) : null,
              nestedPath.concat(parts2.slice(0, p))
            );
            if (ret != null) {
              ret.$parentSchemaDocArray = ret.$parentSchemaDocArray || (schema4.$isSingleNested ? null : schema4);
              return ret;
            }
          }
        }
        return foundschema;
      }
    }
    parts = parts || path.split(".");
    for (let i = 0; i < parts.length; ++i) {
      if (parts[i] === "$") {
        parts[i] = "0";
      }
    }
    return search(parts, schema2, doc, []);
  };
  return getSchemaTypes;
}
var getModelsMapForPopulate;
var hasRequiredGetModelsMapForPopulate;
function requireGetModelsMapForPopulate() {
  if (hasRequiredGetModelsMapForPopulate) return getModelsMapForPopulate;
  hasRequiredGetModelsMapForPopulate = 1;
  const MongooseError = requireError();
  const SkipPopulateValue = requireSkipPopulateValue();
  const clone = requireClone();
  const get2 = requireGet();
  const getDiscriminatorByValue2 = requireGetDiscriminatorByValue();
  const getConstructorName2 = requireGetConstructorName();
  const getSchemaTypes2 = requireGetSchemaTypes();
  const getVirtual = requireGetVirtual();
  const lookupLocalFields2 = requireLookupLocalFields();
  const mpath = requireMpath();
  const modelNamesFromRefPath2 = requireModelNamesFromRefPath();
  const utils2 = requireUtils();
  const modelSymbol = requireSymbols$1().modelSymbol;
  const populateModelSymbol = requireSymbols$1().populateModelSymbol;
  const schemaMixedSymbol = requireSymbols().schemaMixedSymbol;
  const StrictPopulate = requireStrictPopulate();
  const numericPathSegmentPattern = "\\.\\d+(?=\\.|$)";
  const hasNumericPathSegmentRE = new RegExp(numericPathSegmentPattern);
  const numericPathSegmentRE = new RegExp(numericPathSegmentPattern, "g");
  getModelsMapForPopulate = function getModelsMapForPopulate2(model2, docs, options2) {
    let doc;
    const len = docs.length;
    const map2 = [];
    const modelNameFromQuery = options2.model?.modelName || options2.model;
    let schema2;
    let refPath;
    let modelNames;
    let modelNamesSet;
    const available = {};
    const modelSchema = model2.schema;
    if (options2._localModel != null && options2._localModel.schema.nested[options2.path]) {
      return [];
    }
    const _virtualRes = getVirtual(model2.schema, options2.path);
    const virtual = _virtualRes == null ? null : _virtualRes.virtual;
    if (virtual != null) {
      return _virtualPopulate(model2, docs, options2, _virtualRes);
    }
    const parts = modelSchema.paths[options2.path]?.splitPath() ?? options2.path.split(".");
    let allSchemaTypes = getSchemaTypes2(model2, modelSchema, null, options2.path, parts);
    allSchemaTypes = Array.isArray(allSchemaTypes) ? allSchemaTypes : [allSchemaTypes].filter((v) => v != null);
    const isStrictPopulateDisabled = options2.strictPopulate === false || options2.options?.strictPopulate === false;
    if (!isStrictPopulateDisabled && allSchemaTypes.length === 0 && options2._localModel != null) {
      return new StrictPopulate(options2._fullPath || options2.path);
    }
    for (let i = 0; i < len; i++) {
      doc = docs[i];
      let justOne = null;
      if (doc.$__ != null && doc.populated(options2.path)) {
        const forceRepopulate = options2.forceRepopulate ?? doc.constructor.base.options.forceRepopulate;
        if (forceRepopulate === false) {
          continue;
        }
      }
      const docSchema = doc?.$__ != null ? doc.$__schema : modelSchema;
      schema2 = getSchemaTypes2(model2, docSchema, doc, options2.path, parts);
      if (schema2 != null && schema2.$isMongooseDocumentArray && schema2.options.ref == null && schema2.options.refPath == null) {
        continue;
      }
      const isUnderneathDocArray = schema2?.$parentSchemaDocArray;
      if (isUnderneathDocArray && get2(options2, "options.sort") != null) {
        return new MongooseError("Cannot populate with `sort` on path " + options2.path + " because it is a subproperty of a document array");
      }
      modelNames = null;
      let isRefPath = false;
      let normalizedRefPath = null;
      let schemaOptions = null;
      let modelNamesInOrder = null;
      if (schema2?.instance === "Embedded") {
        if (schema2.options.ref) {
          const data2 = {
            localField: options2.path + "._id",
            foreignField: "_id",
            justOne: true
          };
          const res = _getModelNames(doc, schema2, modelNameFromQuery, model2);
          const unpopulatedValue = mpath.get(options2.path, doc);
          const id2 = mpath.get("_id", unpopulatedValue);
          addModelNamesToMap(model2, map2, available, res.modelNames, options2, data2, id2, doc, schemaOptions, unpopulatedValue);
        }
        continue;
      }
      if (Array.isArray(schema2)) {
        const schemasArray = schema2;
        for (const _schema of schemasArray) {
          let _modelNames;
          let res;
          try {
            res = _getModelNames(doc, _schema, modelNameFromQuery, model2);
            _modelNames = res.modelNames;
            isRefPath = isRefPath || res.isRefPath;
            normalizedRefPath = normalizedRefPath || res.refPath;
            justOne = res.justOne;
          } catch (error2) {
            return error2;
          }
          if (isRefPath && !res.isRefPath) {
            continue;
          }
          if (!_modelNames) {
            continue;
          }
          modelNames = modelNames || [];
          modelNamesSet = modelNamesSet || /* @__PURE__ */ new Set();
          for (const modelName of _modelNames) {
            if (modelNamesSet.has(modelName) === false) {
              modelNamesSet.add(modelName);
              modelNames.push(modelName);
            }
          }
        }
      } else {
        try {
          const res = _getModelNames(doc, schema2, modelNameFromQuery, model2);
          modelNames = res.modelNames;
          isRefPath = res.isRefPath;
          normalizedRefPath = normalizedRefPath || res.refPath;
          justOne = res.justOne;
          schemaOptions = get2(schema2, "options.populate", null);
          if (isRefPath) {
            modelNamesInOrder = modelNames;
            modelNames = Array.from(new Set(modelNames));
          }
        } catch (error2) {
          return error2;
        }
        if (!modelNames) {
          continue;
        }
      }
      const data = {};
      const localField = options2.path;
      const foreignField = "_id";
      if ("justOne" in options2 && options2.justOne !== void 0) {
        justOne = options2.justOne;
      } else if (schema2 && !schema2[schemaMixedSymbol]) {
        if (options2.path.endsWith("." + schema2.path) || options2.path === schema2.path) {
          justOne = Array.isArray(schema2) ? schema2.every((schema3) => !schema3.$isMongooseArray) : !schema2.$isMongooseArray;
        }
      }
      if (!modelNames) {
        continue;
      }
      data.isVirtual = false;
      data.justOne = justOne;
      data.localField = localField;
      data.foreignField = foreignField;
      const ret = _getLocalFieldValues(doc, localField, model2, options2, null, schema2);
      const id = String(utils2.getValue(foreignField, doc));
      options2._docs[id] = Array.isArray(ret) ? ret.slice() : ret;
      let match = get2(options2, "match", null);
      const hasMatchFunction = typeof match === "function";
      if (hasMatchFunction) {
        match = match.call(doc, doc);
      }
      throwOn$where(match);
      data.match = match;
      data.hasMatchFunction = hasMatchFunction;
      data.isRefPath = isRefPath;
      data.modelNamesInOrder = modelNamesInOrder;
      if (isRefPath) {
        const normalizedRefPathForDiscriminators = typeof normalizedRefPath === "string" ? normalizedRefPath.replace(numericPathSegmentRE, "") : normalizedRefPath;
        const embeddedDiscriminatorModelNames = _findRefPathForDiscriminators(
          doc,
          modelSchema,
          data,
          options2,
          normalizedRefPathForDiscriminators,
          ret
        );
        modelNames = embeddedDiscriminatorModelNames || modelNames;
      }
      try {
        addModelNamesToMap(model2, map2, available, modelNames, options2, data, ret, doc, schemaOptions);
      } catch (err) {
        return err;
      }
    }
    return map2;
    function _getModelNames(doc2, schemaType2, modelNameFromQuery2, model3) {
      let modelNames2;
      let isRefPath = false;
      let justOne = null;
      const originalSchema = schemaType2;
      if (schemaType2?.instance === "Array") {
        schemaType2 = schemaType2.embeddedSchemaType;
      }
      if (schemaType2?.$isSchemaMap) {
        schemaType2 = schemaType2.$__schemaType;
      }
      const ref = schemaType2?.options?.ref;
      refPath = schemaType2?.options?.refPath;
      if (schemaType2 != null && schemaType2[schemaMixedSymbol] && !ref && !refPath && !modelNameFromQuery2) {
        return { modelNames: null };
      }
      if (modelNameFromQuery2) {
        modelNames2 = [modelNameFromQuery2];
      } else if (refPath != null) {
        if (typeof refPath === "function") {
          const res = _getModelNamesFromFunctionRefPath(refPath, doc2, schemaType2, options2.path, modelSchema, options2._queryProjection);
          modelNames2 = res.modelNames;
          refPath = res.refPath;
        } else {
          modelNames2 = modelNamesFromRefPath2(refPath, doc2, options2.path, modelSchema, options2._queryProjection);
        }
        isRefPath = true;
      } else {
        let ref2;
        let refPath2;
        let schemaForCurrentDoc;
        let discriminatorValue;
        let modelForCurrentDoc = model3;
        const discriminatorKey = model3.schema.options.discriminatorKey;
        if (!schemaType2 && discriminatorKey && (discriminatorValue = utils2.getValue(discriminatorKey, doc2))) {
          const discriminatorModel = getDiscriminatorByValue2(model3.discriminators, discriminatorValue) || model3;
          if (discriminatorModel != null) {
            modelForCurrentDoc = discriminatorModel;
          } else {
            try {
              modelForCurrentDoc = _getModelFromConn(model3.db, discriminatorValue);
            } catch (error2) {
              return error2;
            }
          }
          schemaForCurrentDoc = modelForCurrentDoc.schema._getSchema(options2.path);
          if (schemaForCurrentDoc?.embeddedSchemaType) {
            schemaForCurrentDoc = schemaForCurrentDoc.embeddedSchemaType;
          }
        } else {
          schemaForCurrentDoc = schemaType2;
        }
        if (originalSchema && originalSchema.path.endsWith(".$*")) {
          justOne = !originalSchema.$isMongooseArray && !originalSchema._arrayPath;
        } else if (schemaForCurrentDoc != null) {
          justOne = !schemaForCurrentDoc.$isMongooseArray && !schemaForCurrentDoc._arrayPath;
        }
        if ((ref2 = get2(schemaForCurrentDoc, "options.ref")) != null) {
          if (schemaForCurrentDoc != null && typeof ref2 === "function" && options2.path.endsWith("." + schemaForCurrentDoc.path)) {
            modelNames2 = /* @__PURE__ */ new Set();
            const subdocPath = options2.path.slice(0, options2.path.length - schemaForCurrentDoc.path.length - 1);
            const vals = mpath.get(subdocPath, doc2, lookupLocalFields2);
            const subdocsBeingPopulated = Array.isArray(vals) ? utils2.array.flatten(vals) : vals ? [vals] : [];
            for (const subdoc of subdocsBeingPopulated) {
              modelNames2.add(handleRefFunction(ref2, subdoc));
            }
            if (subdocsBeingPopulated.length === 0) {
              modelNames2 = [handleRefFunction(ref2, doc2)];
            } else {
              modelNames2 = Array.from(modelNames2);
            }
          } else {
            ref2 = handleRefFunction(ref2, doc2);
            modelNames2 = [ref2];
          }
        } else if ((refPath2 = get2(schemaForCurrentDoc, "options.refPath")) != null) {
          isRefPath = true;
          if (typeof refPath2 === "function") {
            const res = _getModelNamesFromFunctionRefPath(refPath2, doc2, schemaForCurrentDoc, options2.path, modelSchema, options2._queryProjection);
            modelNames2 = res.modelNames;
            refPath2 = res.refPath;
          } else {
            modelNames2 = modelNamesFromRefPath2(refPath2, doc2, options2.path, modelSchema, options2._queryProjection);
          }
        }
      }
      if (!modelNames2) {
        if (options2._localModel == null) {
          modelNames2 = [model3.modelName];
        } else {
          return { modelNames: modelNames2, justOne, isRefPath, refPath };
        }
      }
      if (!Array.isArray(modelNames2)) {
        modelNames2 = [modelNames2];
      }
      return { modelNames: modelNames2, justOne, isRefPath, refPath };
    }
  };
  function _getModelNamesFromFunctionRefPath(refPath, doc, schemaType2, populatePath, modelSchema, queryProjection) {
    const modelNames = [];
    let normalizedRefPath = null;
    const schemaPath = schemaType2?.path;
    if (schemaPath != null && populatePath.length > schemaPath.length + 1 && populatePath.charAt(populatePath.length - schemaPath.length - 1) === "." && populatePath.slice(populatePath.length - schemaPath.length) === schemaPath) {
      let walkSubdocs = function(currentSubdoc, indexedPathPrefix, segmentIndex) {
        if (currentSubdoc == null) {
          return;
        }
        if (segmentIndex >= segments.length) {
          hasSubdoc = true;
          const indexedPath = indexedPathPrefix + "." + schemaPath;
          const subdocRefPath = refPath.call(currentSubdoc, currentSubdoc, indexedPath);
          normalizedRefPath = normalizedRefPath || subdocRefPath;
          modelNames.push(..._getModelNamesFromRefPath(subdocRefPath, doc, populatePath, indexedPath, modelSchema, queryProjection));
          return;
        }
        const segment = segments[segmentIndex];
        const source = currentSubdoc?._doc ?? currentSubdoc;
        if (segment === "$*") {
          if (source instanceof Map) {
            for (const [key, value] of source.entries()) {
              if (value != null) {
                const valuePath = indexedPathPrefix.length === 0 ? key : indexedPathPrefix + "." + key;
                walkSubdocs(value, valuePath, segmentIndex + 1);
              }
            }
            return;
          }
          if (source != null && typeof source === "object") {
            for (const key of Object.keys(source)) {
              const value = source[key];
              if (value != null) {
                const valuePath = indexedPathPrefix.length === 0 ? key : indexedPathPrefix + "." + key;
                walkSubdocs(value, valuePath, segmentIndex + 1);
              }
            }
          }
          return;
        }
        const child = source[segment];
        const childPath = indexedPathPrefix.length === 0 ? segment : indexedPathPrefix + "." + segment;
        if (Array.isArray(child)) {
          for (let i = 0; i < child.length; ++i) {
            if (child[i] != null) {
              walkSubdocs(child[i], childPath + "." + i, segmentIndex + 1);
            }
          }
        } else if (child != null) {
          walkSubdocs(child, childPath, segmentIndex + 1);
        }
      };
      const subdocPath = populatePath.slice(0, populatePath.length - schemaPath.length - 1);
      const segments = subdocPath.indexOf(".") === -1 ? [subdocPath] : subdocPath.split(".");
      let hasSubdoc = false;
      walkSubdocs(doc, "", 0);
      if (hasSubdoc) {
        return { modelNames, refPath: normalizedRefPath };
      }
    }
    const topLevelRefPath = refPath.call(doc, doc, populatePath);
    normalizedRefPath = normalizedRefPath || topLevelRefPath;
    modelNames.push(...modelNamesFromRefPath2(topLevelRefPath, doc, populatePath, modelSchema, queryProjection));
    return { modelNames, refPath: normalizedRefPath };
  }
  function _getModelNamesFromRefPath(refPath, doc, populatePath, indexedPath, modelSchema, queryProjection) {
    const populatedPath = typeof refPath === "string" && hasNumericPathSegmentRE.test(refPath) ? populatePath : indexedPath;
    try {
      return modelNamesFromRefPath2(refPath, doc, populatedPath, modelSchema, queryProjection);
    } catch (error2) {
      if (populatedPath === indexedPath && typeof error2?.message === "string" && error2.message.startsWith("Could not normalize ref path")) {
        return modelNamesFromRefPath2(refPath, doc, populatePath, modelSchema, queryProjection);
      }
      throw error2;
    }
  }
  function _virtualPopulate(model2, docs, options2, _virtualRes) {
    const map2 = [];
    const available = {};
    const virtual = _virtualRes.virtual;
    for (const doc of docs) {
      let modelNames = null;
      const data = {};
      let localField;
      const virtualPrefix = _virtualRes.nestedSchemaPath ? _virtualRes.nestedSchemaPath + "." : "";
      if (typeof options2.localField === "string") {
        localField = options2.localField;
      } else if (typeof virtual.options.localField === "function") {
        localField = virtualPrefix + virtual.options.localField.call(doc, doc);
      } else if (Array.isArray(virtual.options.localField)) {
        localField = virtual.options.localField.map((field) => virtualPrefix + field);
      } else {
        localField = virtualPrefix + virtual.options.localField;
      }
      data.count = virtual.options.count;
      if (virtual.options.skip != null && !Object.hasOwn(options2, "skip")) {
        options2.skip = virtual.options.skip;
      }
      if (virtual.options.limit != null && !Object.hasOwn(options2, "limit")) {
        options2.limit = virtual.options.limit;
      }
      if (virtual.options.perDocumentLimit != null && !Object.hasOwn(options2, "perDocumentLimit")) {
        options2.perDocumentLimit = virtual.options.perDocumentLimit;
      }
      let foreignField = virtual.options.foreignField;
      if (!localField || !foreignField) {
        return new MongooseError(`Cannot populate virtual \`${options2.path}\` on model \`${model2.modelName}\`, because options \`localField\` and / or \`foreignField\` are missing`);
      }
      if (typeof localField === "function") {
        localField = localField.call(doc, doc);
      }
      if (typeof foreignField === "function") {
        foreignField = foreignField.call(doc, doc);
      }
      data.isRefPath = false;
      let justOne = null;
      if ("justOne" in options2 && options2.justOne !== void 0) {
        justOne = options2.justOne;
      }
      if (_virtualRes.nestedSchemaPath && typeof virtual.options.ref === "function") {
        const subdocs = utils2.getValue(_virtualRes.nestedSchemaPath, doc);
        modelNames = Array.isArray(subdocs) ? subdocs.flatMap((subdoc) => virtual._getModelNamesForPopulate(subdoc)) : virtual._getModelNamesForPopulate(subdocs);
      } else {
        modelNames = virtual._getModelNamesForPopulate(doc);
      }
      if (virtual.options.refPath) {
        justOne = !!virtual.options.justOne;
        data.isRefPath = true;
      } else if (virtual.options.ref) {
        justOne = !!virtual.options.justOne;
      }
      data.isVirtual = true;
      data.virtual = virtual;
      data.justOne = justOne;
      const baseMatch = get2(data, "virtual.options.match", null) || get2(data, "virtual.options.options.match", null);
      let match = get2(options2, "match", null) || baseMatch;
      let hasMatchFunction = typeof match === "function";
      if (hasMatchFunction) {
        match = match.call(doc, doc, data.virtual);
      }
      if (Array.isArray(localField) && Array.isArray(foreignField) && localField.length === foreignField.length) {
        match = Object.assign({}, match);
        for (let i = 1; i < localField.length; ++i) {
          match[foreignField[i]] = convertTo_id(mpath.get(localField[i], doc, lookupLocalFields2), model2.schema);
          hasMatchFunction = true;
        }
        localField = localField[0];
        foreignField = foreignField[0];
      }
      data.localField = localField;
      data.foreignField = foreignField;
      data.match = match;
      data.hasMatchFunction = hasMatchFunction;
      throwOn$where(match);
      const ret = _getLocalFieldValues(doc, localField, model2, options2, virtual);
      try {
        addModelNamesToMap(model2, map2, available, modelNames, options2, data, ret, doc);
      } catch (err) {
        return err;
      }
    }
    return map2;
  }
  function addModelNamesToMap(model2, map2, available, modelNames, options2, data, ret, doc, schemaOptions, unpopulatedValue) {
    const connection2 = options2.connection ?? model2.db;
    unpopulatedValue = unpopulatedValue === void 0 ? ret : unpopulatedValue;
    if (Array.isArray(unpopulatedValue)) {
      unpopulatedValue = utils2.cloneArrays(unpopulatedValue);
    }
    if (modelNames == null) {
      return;
    }
    const flatModelNames = utils2.array.flatten(modelNames);
    let k = flatModelNames.length;
    while (k--) {
      let modelName = flatModelNames[k];
      if (modelName == null) {
        continue;
      }
      let Model;
      if (options2.model && options2.model[modelSymbol]) {
        Model = options2.model;
      } else if (modelName[modelSymbol]) {
        Model = modelName;
        modelName = Model.modelName;
      } else {
        try {
          Model = _getModelFromConn(connection2, modelName);
        } catch (err) {
          if (ret !== void 0) {
            throw err;
          }
          Model = null;
        }
      }
      let ids = ret;
      const modelNamesForRefPath = data.modelNamesInOrder || modelNames;
      if (data.isRefPath && Array.isArray(ret) && ret.length === modelNamesForRefPath.length) {
        ids = matchIdsToRefPaths(ret, modelNamesForRefPath, modelName);
      }
      if (!available[modelName]) {
        const currentOptions = {
          model: Model
        };
        if (data.isVirtual && get2(data.virtual, "options.options")) {
          currentOptions.options = clone(data.virtual.options.options);
        } else if (schemaOptions != null) {
          currentOptions.options = Object.assign({}, schemaOptions);
        }
        utils2.merge(currentOptions, options2);
        options2[populateModelSymbol] = Model;
        currentOptions[populateModelSymbol] = Model;
        available[modelName] = {
          model: Model,
          options: currentOptions,
          match: data.hasMatchFunction ? [data.match] : data.match,
          docs: [doc],
          ids: [ids],
          allIds: [ret],
          unpopulatedValues: [unpopulatedValue],
          localField: /* @__PURE__ */ new Set([data.localField]),
          foreignField: /* @__PURE__ */ new Set([data.foreignField]),
          justOne: data.justOne,
          isVirtual: data.isVirtual,
          virtual: data.virtual,
          count: data.count,
          isRefPath: !!data.isRefPath,
          [populateModelSymbol]: Model
        };
        map2.push(available[modelName]);
      } else {
        available[modelName].localField.add(data.localField);
        available[modelName].foreignField.add(data.foreignField);
        available[modelName].docs.push(doc);
        available[modelName].ids.push(ids);
        available[modelName].allIds.push(ret);
        available[modelName].unpopulatedValues.push(unpopulatedValue);
        available[modelName].isRefPath = available[modelName].isRefPath || !!data.isRefPath;
        if (data.hasMatchFunction) {
          available[modelName].match.push(data.match);
        }
      }
    }
  }
  function _getModelFromConn(conn, modelName) {
    if (conn.models[modelName] == null && conn._parent != null) {
      return _getModelFromConn(conn._parent, modelName);
    }
    return conn.model(modelName);
  }
  function matchIdsToRefPaths(ids, refPaths, refPathToFind) {
    if (!Array.isArray(refPaths)) {
      return refPaths === refPathToFind ? Array.isArray(ids) ? utils2.array.flatten(ids) : [ids] : [];
    }
    if (Array.isArray(ids) && Array.isArray(refPaths)) {
      return ids.flatMap((id, index) => matchIdsToRefPaths(id, refPaths[index], refPathToFind));
    }
    return [];
  }
  function handleRefFunction(ref, doc) {
    if (typeof ref === "function" && !ref[modelSymbol]) {
      return ref.call(doc, doc);
    }
    return ref;
  }
  function _getLocalFieldValues(doc, localField, model2, options2, virtual, schema2) {
    const localFieldPathType = model2.schema._getPathType(localField);
    const localFieldPath = localFieldPathType === "real" ? model2.schema.path(localField) : localFieldPathType.schema;
    const localFieldGetters = localFieldPath?.getters || [];
    localField = localFieldPath?.instance === "Embedded" ? localField + "._id" : localField;
    const _populateOptions = get2(options2, "options", {});
    const getters = "getters" in _populateOptions ? _populateOptions.getters : get2(virtual, "options.getters", false);
    if (localFieldGetters.length !== 0 && getters) {
      const hydratedDoc = doc.$__ != null ? doc : model2.hydrate(doc);
      const localFieldValue = utils2.getValue(localField, doc);
      if (Array.isArray(localFieldValue)) {
        const localFieldHydratedValue = utils2.getValue(localField.split(".").slice(0, -1), hydratedDoc);
        return localFieldValue.map((localFieldArrVal, localFieldArrIndex) => localFieldPath.applyGetters(localFieldArrVal, localFieldHydratedValue[localFieldArrIndex]));
      } else {
        return localFieldPath.applyGetters(localFieldValue, hydratedDoc);
      }
    } else {
      return convertTo_id(mpath.get(localField, doc, lookupLocalFields2), schema2);
    }
  }
  function convertTo_id(val, schema2) {
    if (val?.$__ != null) {
      return val._doc._id;
    }
    if (val?._id != null && !schema2?.$isSchemaMap) {
      return val._id;
    }
    if (Array.isArray(val)) {
      const rawVal = val.__array ?? val;
      for (let i = 0; i < rawVal.length; ++i) {
        if (rawVal[i]?.$__ != null) {
          rawVal[i] = rawVal[i]._doc._id;
        }
      }
      if (utils2.isMongooseArray(val) && val.$schema()) {
        return val.$schema()._castForPopulate(val, val.$parent());
      }
      return [].concat(val);
    }
    if (getConstructorName2(val) === "Object" && // The intent here is we should only flatten the object if we expect
    // to get a Map in the end. Avoid doing this for mixed types.
    schema2?.[schemaMixedSymbol] == null) {
      const ret = [];
      for (const key of Object.keys(val)) {
        ret.push(val[key]);
      }
      return ret;
    }
    if (val instanceof Map) {
      return Array.from(val.values());
    }
    return val;
  }
  function _findRefPathForDiscriminators(doc, modelSchema, data, options2, normalizedRefPath, ret) {
    if (!data.isRefPath || normalizedRefPath == null) {
      return;
    }
    const pieces = normalizedRefPath.split(".");
    let cur = "";
    let modelNames = void 0;
    for (let i = 0; i < pieces.length; ++i) {
      const piece = pieces[i];
      cur = cur + (cur.length === 0 ? "" : ".") + piece;
      const schematype = modelSchema.path(cur);
      if (schematype != null && schematype.$isMongooseDocumentArray && schematype.Constructor.discriminators != null && utils2.hasOwnKeys(schematype.Constructor.discriminators)) {
        const subdocs = utils2.getValue(cur, doc);
        const remnant = options2.path.substring(cur.length + 1);
        const discriminatorKey = schematype.Constructor.schema.options.discriminatorKey;
        modelNames = [];
        for (const subdoc of subdocs) {
          const discriminatorName = utils2.getValue(discriminatorKey, subdoc);
          const discriminator2 = schematype.Constructor.discriminators[discriminatorName];
          const discriminatorSchema = discriminator2?.schema;
          if (discriminatorSchema == null) {
            continue;
          }
          const _path = discriminatorSchema.path(remnant);
          if (_path == null || _path.options.refPath == null) {
            const docValue = utils2.getValue(data.localField.substring(cur.length + 1), subdoc);
            ret.forEach((v, i2) => {
              if (v === docValue) {
                ret[i2] = SkipPopulateValue(v);
              }
            });
            continue;
          }
          const modelName = utils2.getValue(pieces.slice(i + 1).join("."), subdoc);
          modelNames.push(modelName);
        }
      }
    }
    return modelNames;
  }
  function throwOn$where(match) {
    if (match == null) {
      return;
    }
    if (typeof match !== "object") {
      return;
    }
    for (const key of Object.keys(match)) {
      if (key === "$where") {
        throw new MongooseError("Cannot use $where filter with populate() match");
      }
      if (match[key] != null && typeof match[key] === "object") {
        throwOn$where(match[key]);
      }
    }
  }
  return getModelsMapForPopulate;
}
var isDefaultIdIndex;
var hasRequiredIsDefaultIdIndex;
function requireIsDefaultIdIndex() {
  if (hasRequiredIsDefaultIdIndex) return isDefaultIdIndex;
  hasRequiredIsDefaultIdIndex = 1;
  const get2 = requireGet();
  isDefaultIdIndex = function isDefaultIdIndex2(index) {
    if (Array.isArray(index)) {
      const keys = Object.keys(index[0]);
      return keys.length === 1 && keys[0] === "_id" && index[0]._id !== "hashed";
    }
    if (typeof index !== "object") {
      return false;
    }
    const key = get2(index, "key", {});
    return Object.keys(key).length === 1 && Object.hasOwn(key, "_id");
  };
  return isDefaultIdIndex;
}
var isIndexEqual;
var hasRequiredIsIndexEqual;
function requireIsIndexEqual() {
  if (hasRequiredIsIndexEqual) return isIndexEqual;
  hasRequiredIsIndexEqual = 1;
  const get2 = requireGet();
  const utils2 = requireUtils();
  isIndexEqual = function isIndexEqual2(schemaIndexKeysObject, options2, dbIndex) {
    if (dbIndex.textIndexVersion != null) {
      delete dbIndex.key._fts;
      delete dbIndex.key._ftsx;
      const weights = { ...dbIndex.weights, ...dbIndex.key };
      if (Object.keys(weights).length !== Object.keys(schemaIndexKeysObject).length) {
        return false;
      }
      for (const prop of Object.keys(weights)) {
        if (!(prop in schemaIndexKeysObject)) {
          return false;
        }
        const weight = weights[prop];
        if (weight !== get2(options2, "weights." + prop) && !(weight === 1 && get2(options2, "weights." + prop) == null)) {
          return false;
        }
      }
      if (options2["default_language"] !== dbIndex["default_language"]) {
        return dbIndex["default_language"] === "english" && options2["default_language"] == null;
      }
      return true;
    }
    const optionKeys = [
      "unique",
      "partialFilterExpression",
      "sparse",
      "expireAfterSeconds",
      "collation"
    ];
    for (const key of optionKeys) {
      if (!(key in options2) && !(key in dbIndex)) {
        continue;
      }
      if (key === "collation") {
        if (options2[key] == null || dbIndex[key] == null) {
          return options2[key] == null && dbIndex[key] == null;
        }
        const definedKeys = Object.keys(options2.collation);
        const schemaCollation = options2.collation;
        const dbCollation = dbIndex.collation;
        for (const opt of definedKeys) {
          if (get2(schemaCollation, opt) !== get2(dbCollation, opt)) {
            return false;
          }
        }
      } else if (!utils2.deepEqual(options2[key], dbIndex[key])) {
        return false;
      }
    }
    const schemaIndexKeys = Object.keys(schemaIndexKeysObject);
    const dbIndexKeys = Object.keys(dbIndex.key);
    if (schemaIndexKeys.length !== dbIndexKeys.length) {
      return false;
    }
    for (let i = 0; i < schemaIndexKeys.length; ++i) {
      if (schemaIndexKeys[i] !== dbIndexKeys[i]) {
        return false;
      }
      if (!utils2.deepEqual(schemaIndexKeysObject[schemaIndexKeys[i]], dbIndex.key[dbIndexKeys[i]])) {
        return false;
      }
    }
    return true;
  };
  return isIndexEqual;
}
var isTimeseriesIndex;
var hasRequiredIsTimeseriesIndex;
function requireIsTimeseriesIndex() {
  if (hasRequiredIsTimeseriesIndex) return isTimeseriesIndex;
  hasRequiredIsTimeseriesIndex = 1;
  isTimeseriesIndex = function isTimeseriesIndex2(dbIndex, schemaOptions) {
    if (schemaOptions.timeseries == null) {
      return false;
    }
    const { timeField, metaField } = schemaOptions.timeseries;
    if (typeof timeField !== "string" || typeof metaField !== "string") {
      return false;
    }
    return Object.keys(dbIndex.key).length === 2 && dbIndex.key[timeField] === 1 && dbIndex.key[metaField] === 1;
  };
  return isTimeseriesIndex;
}
var getRelatedIndexes_1;
var hasRequiredGetRelatedIndexes;
function requireGetRelatedIndexes() {
  if (hasRequiredGetRelatedIndexes) return getRelatedIndexes_1;
  hasRequiredGetRelatedIndexes = 1;
  const hasDollarKeys2 = requireHasDollarKeys();
  function getRelatedSchemaIndexes(model2, schemaIndexes) {
    return getRelatedIndexes({
      baseModelName: model2.baseModelName,
      discriminatorMapping: model2.schema.discriminatorMapping,
      indexes: schemaIndexes,
      indexesType: "schema"
    });
  }
  function getRelatedDBIndexes(model2, dbIndexes) {
    return getRelatedIndexes({
      baseModelName: model2.baseModelName,
      discriminatorMapping: model2.schema.discriminatorMapping,
      indexes: dbIndexes,
      indexesType: "db"
    });
  }
  getRelatedIndexes_1 = {
    getRelatedSchemaIndexes,
    getRelatedDBIndexes
  };
  function getRelatedIndexes({
    baseModelName,
    discriminatorMapping,
    indexes,
    indexesType
  }) {
    const discriminatorKey = discriminatorMapping?.key;
    const discriminatorValue = discriminatorMapping?.value;
    if (!discriminatorKey) {
      return indexes;
    }
    const isChildDiscriminatorModel = Boolean(baseModelName);
    if (isChildDiscriminatorModel) {
      return indexes.filter((index) => {
        const partialFilterExpression = getPartialFilterExpression(index, indexesType);
        return partialFilterExpression && partialFilterExpression[discriminatorKey] === discriminatorValue;
      });
    }
    return indexes.filter((index) => {
      const partialFilterExpression = getPartialFilterExpression(index, indexesType);
      return !partialFilterExpression || !partialFilterExpression[discriminatorKey] || hasDollarKeys2(partialFilterExpression[discriminatorKey]) && !("$eq" in partialFilterExpression[discriminatorKey]);
    });
  }
  function getPartialFilterExpression(index, indexesType) {
    if (indexesType === "schema") {
      const options2 = index[1];
      return options2?.partialFilterExpression;
    }
    return index.partialFilterExpression;
  }
  return getRelatedIndexes_1;
}
var parallelLimit_1;
var hasRequiredParallelLimit;
function requireParallelLimit() {
  if (hasRequiredParallelLimit) return parallelLimit_1;
  hasRequiredParallelLimit = 1;
  parallelLimit_1 = parallelLimit;
  async function parallelLimit(params, fn, limit) {
    if (limit <= 0) {
      throw new Error("Limit must be positive");
    }
    const length = params.length;
    if (length === 0) {
      return [];
    }
    const results = new Array(length);
    if (limit >= length) {
      for (let i = 0; i < length; ++i) {
        results[i] = fn(params[i], i);
      }
      return Promise.all(results);
    }
    let nextIndex = 0;
    let firstError = null;
    function worker() {
      if (nextIndex >= length || firstError !== null) {
        return void 0;
      }
      const index = nextIndex++;
      return Promise.resolve(fn(params[index], index)).then(
        (val) => {
          results[index] = val;
          return worker();
        },
        (err) => {
          if (firstError === null) {
            firstError = err;
          }
        }
      );
    }
    const workers = new Array(Math.min(limit, length));
    for (let i = 0; i < workers.length; ++i) {
      workers[i] = worker();
    }
    await Promise.all(workers);
    if (firstError !== null) {
      throw firstError;
    }
    return results;
  }
  return parallelLimit_1;
}
var pushNestedArrayPaths;
var hasRequiredPushNestedArrayPaths;
function requirePushNestedArrayPaths() {
  if (hasRequiredPushNestedArrayPaths) return pushNestedArrayPaths;
  hasRequiredPushNestedArrayPaths = 1;
  pushNestedArrayPaths = function pushNestedArrayPaths2(paths, nestedArray, path) {
    if (nestedArray == null) {
      return;
    }
    for (let i = 0; i < nestedArray.length; ++i) {
      if (Array.isArray(nestedArray[i])) {
        pushNestedArrayPaths2(paths, nestedArray[i], path + "." + i);
      } else {
        paths.push(path + "." + i);
      }
    }
  };
  return pushNestedArrayPaths;
}
var removeDeselectedForeignField;
var hasRequiredRemoveDeselectedForeignField;
function requireRemoveDeselectedForeignField() {
  if (hasRequiredRemoveDeselectedForeignField) return removeDeselectedForeignField;
  hasRequiredRemoveDeselectedForeignField = 1;
  const get2 = requireGet();
  const mpath = requireMpath();
  const parseProjection2 = requireParseProjection();
  removeDeselectedForeignField = function removeDeselectedForeignField2(foreignFields, options2, docs) {
    const projection = parseProjection2(get2(options2, "select", null), true) || parseProjection2(get2(options2, "options.select", null), true);
    if (projection == null) {
      return;
    }
    for (const foreignField of foreignFields) {
      if (!Object.hasOwn(projection, "-" + foreignField)) {
        continue;
      }
      for (const val of docs) {
        if (val.$__ != null) {
          mpath.unset(foreignField, val._doc);
        } else {
          mpath.unset(foreignField, val);
        }
      }
    }
  };
  return removeDeselectedForeignField;
}
var splitPopulateQuery_1;
var hasRequiredSplitPopulateQuery;
function requireSplitPopulateQuery() {
  if (hasRequiredSplitPopulateQuery) return splitPopulateQuery_1;
  hasRequiredSplitPopulateQuery = 1;
  const createPopulateQueryFilter2 = requireCreatePopulateQueryFilter();
  const get2 = requireGet();
  const utils2 = requireUtils();
  splitPopulateQuery_1 = splitPopulateQuery;
  splitPopulateQuery.maxInFilterLength = 5e4;
  function splitPopulateQuery(mod, ids, select, assignmentOpts) {
    if (mod.docs.length <= 1) {
      return null;
    }
    const perDocumentLimit = mod.options.perDocumentLimit == null ? get2(mod.options, "options.perDocumentLimit", null) : mod.options.perDocumentLimit;
    const numInFilterElements = ids.length * mod.foreignField.size;
    if (perDocumentLimit == null && (numInFilterElements <= splitPopulateQuery.maxInFilterLength || mod.isRefPath)) {
      return null;
    }
    return mod.docs.map((doc, i) => {
      const subMod = {
        ...mod,
        docs: [doc],
        ids: [mod.ids[i]],
        allIds: [mod.allIds[i]],
        unpopulatedValues: [mod.unpopulatedValues[i]],
        match: Array.isArray(mod.match) ? [mod.match[i]] : mod.match,
        _assignFromOwnResults: !mod.isRefPath
      };
      let subIds = utils2.array.flatten(subMod.ids, flatten);
      subIds = utils2.array.unique(subIds);
      const match = subIds.length === 0 || subIds.every(utils2.isNullOrUndefined) ? null : createPopulateQueryFilter2(subIds, subMod.match, subMod.foreignField, subMod.model, subMod.options.skipInvalidIds);
      return [subMod, match, select, assignmentOpts];
    });
  }
  function flatten(item) {
    return void 0 !== item;
  }
  return splitPopulateQuery_1;
}
var hasRequiredModel;
function requireModel() {
  if (hasRequiredModel) return model.exports;
  hasRequiredModel = 1;
  (function(module, exports) {
    const Aggregate = requireAggregate();
    const ChangeStream = requireChangeStream();
    const Document = requireDocument();
    const { createTracedChannel } = requireTracing();
    const { trace: traceSave } = createTracedChannel("mongoose:model:save");
    const { trace: traceInsertMany } = createTracedChannel("mongoose:model:insertMany");
    const { trace: traceBulkWrite } = createTracedChannel("mongoose:model:bulkWrite");
    const DocumentNotFoundError = requireNotFound();
    const EventEmitter = require$$0.EventEmitter;
    const Kareem = requireKareem();
    const MongooseBulkWriteError = requireBulkWriteError();
    const MongooseError = requireError();
    const ObjectParameterError = requireObjectParameter();
    const OverwriteModelError = requireOverwriteModel();
    const Query = requireQuery();
    const SaveOptions = requireSaveOptions();
    const Schema = requireSchema();
    const ValidationError = requireValidation();
    const VersionError = requireVersion();
    const ParallelSaveError = requireParallelSave();
    const applyDefaultsHelper = requireApplyDefaults();
    const isInPathsToSave2 = requireIsInPathsToSave();
    const applyDefaultsToPOJO2 = requireApplyDefaultsToPOJO();
    const applyEmbeddedDiscriminators = requireApplyEmbeddedDiscriminators();
    const applyHooks = requireApplyHooks();
    const applyMethods2 = requireApplyMethods();
    const applyProjection2 = requireApplyProjection();
    const applyReadConcern2 = requireApplyReadConcern();
    const applySchemaCollation2 = requireApplySchemaCollation();
    const applyStaticHooks2 = requireApplyStaticHooks();
    const applyStatics2 = requireApplyStatics();
    const applyTimestampsHelper = requireApplyTimestamps();
    const applyWriteConcern2 = requireApplyWriteConcern();
    const applyVirtualsHelper = requireApplyVirtuals();
    const assignVals2 = requireAssignVals();
    const castBulkWrite2 = requireCastBulkWrite();
    const clone = requireClone();
    const convertErrorToStandardSchemaIssues = requireConvertErrorToIssues();
    const createPopulateQueryFilter2 = requireCreatePopulateQueryFilter();
    const decorateUpdateWithVersionKey2 = requireDecorateUpdateWithVersionKey();
    const getDefaultBulkwriteResult = requireGetDefaultBulkwriteResult();
    const getSchemaDiscriminatorByValue2 = requireGetSchemaDiscriminatorByValue();
    const discriminator2 = requireDiscriminator();
    const each2 = requireEach();
    const get2 = requireGet();
    const getConstructorName2 = requireGetConstructorName();
    const getDiscriminatorByValue2 = requireGetDiscriminatorByValue();
    const getModelsMapForPopulate2 = requireGetModelsMapForPopulate();
    const immediate2 = requireImmediate();
    const internalToObjectOptions = requireOptions().internalToObjectOptions;
    const isDefaultIdIndex2 = requireIsDefaultIdIndex();
    const isIndexEqual2 = requireIsIndexEqual();
    const isIndexSpecEqual2 = requireIsIndexSpecEqual();
    const isTimeseriesIndex2 = requireIsTimeseriesIndex();
    const {
      getRelatedDBIndexes,
      getRelatedSchemaIndexes
    } = requireGetRelatedIndexes();
    const decorateDiscriminatorIndexOptions2 = requireDecorateDiscriminatorIndexOptions();
    const isPathSelectedInclusive2 = requireIsPathSelectedInclusive();
    const leanPopulateMap2 = requireLeanPopulateMap();
    const parallelLimit = requireParallelLimit();
    const parseProjection2 = requireParseProjection();
    const prepareDiscriminatorPipeline2 = requirePrepareDiscriminatorPipeline();
    const pushNestedArrayPaths2 = requirePushNestedArrayPaths();
    const removeDeselectedForeignField2 = requireRemoveDeselectedForeignField();
    const setDottedPath2 = requireSetDottedPath();
    const splitPopulateQuery = requireSplitPopulateQuery();
    const { buildMiddlewareFilter } = requireBuildMiddlewareFilter();
    const util = require$$1;
    const utils2 = requireUtils();
    const minimize = requireMinimize();
    const MongooseBulkSaveIncompleteError = requireBulkSaveIncompleteError();
    const ObjectExpectedError = requireObjectExpected();
    const decorateBulkWriteResult2 = requireDecorateBulkWriteResult();
    const modelCollectionSymbol = /* @__PURE__ */ Symbol("mongoose#Model#collection");
    const modelDbSymbol = /* @__PURE__ */ Symbol("mongoose#Model#db");
    const {
      arrayAtomicsBackupSymbol,
      arrayAtomicsSymbol,
      modelSymbol
    } = requireSymbols$1();
    const subclassedSymbol = /* @__PURE__ */ Symbol("mongoose#Model#subclassed");
    const { VERSION_INC, VERSION_WHERE, VERSION_ALL } = Document;
    const saveToObjectOptions = Object.assign({}, internalToObjectOptions, {
      bson: true
    });
    function Model(doc, fields, options2) {
      if (fields instanceof Schema) {
        throw new TypeError("2nd argument to `Model` constructor must be a POJO or string, **not** a schema. Make sure you're calling `mongoose.model()`, not `mongoose.Model()`.");
      }
      if (typeof doc === "string") {
        throw new TypeError("First argument to `Model` constructor must be an object, **not** a string. Make sure you're calling `mongoose.model()`, not `mongoose.Model()`.");
      }
      Document.call(this, doc, fields, options2);
    }
    Object.setPrototypeOf(Model.prototype, Document.prototype);
    Model.prototype.$isMongooseModelPrototype = true;
    Model.prototype.db;
    Model.useConnection = function useConnection(connection2) {
      if (typeof connection2?.model !== "function" || typeof connection2.collection !== "function" || typeof connection2.base?.version !== "string") {
        throw new MongooseError("`useConnection()` requires a Mongoose connection.");
      }
      if (this.db?.base?.version && this.db?.base?.version !== connection2.base?.version) {
        throw new MongooseError(`The connection passed to \`useConnection()\` has a different version of Mongoose (${connection2.base?.version}) than the model you are using (${this.db?.base?.version}).`);
      }
      if (this.db) {
        delete this.db.models[this.modelName];
        delete this.prototype.db;
        delete this.prototype[modelDbSymbol];
        delete this.prototype.collection;
        delete this.prototype.$collection;
        delete this.prototype[modelCollectionSymbol];
      }
      this.db = connection2;
      const collection2 = connection2.collection(this.collection.collectionName, connection2.options);
      this.prototype.collection = collection2;
      this.prototype.$collection = collection2;
      this.prototype[modelCollectionSymbol] = collection2;
      this.prototype.db = connection2;
      this.prototype[modelDbSymbol] = connection2;
      this.collection = collection2;
      this.$__collection = collection2;
      connection2.models[this.modelName] = this;
      return this;
    };
    Model.prototype.collection;
    Model.prototype.$__collection;
    Model.prototype.modelName;
    Model.prototype.$where;
    Model.prototype.baseModelName;
    Model.events;
    Model._middleware;
    function _applyCustomWhere(doc, where) {
      if (doc.$where == null) {
        return;
      }
      for (const key of Object.keys(doc.$where)) {
        where[key] = doc.$where[key];
      }
    }
    function _createSaveOptions(doc, options2) {
      const saveOptions2 = {};
      applyWriteConcern2(doc.$__schema, options2);
      if (typeof options2.writeConcern !== "undefined") {
        saveOptions2.writeConcern = {};
        if ("w" in options2.writeConcern) {
          saveOptions2.writeConcern.w = options2.writeConcern.w;
        }
        if ("j" in options2.writeConcern) {
          saveOptions2.writeConcern.j = options2.writeConcern.j;
        }
        if ("wtimeout" in options2.writeConcern) {
          saveOptions2.writeConcern.wtimeout = options2.writeConcern.wtimeout;
        }
      } else {
        if ("w" in options2) {
          saveOptions2.w = options2.w;
        }
        if ("j" in options2) {
          saveOptions2.j = options2.j;
        }
        if ("wtimeout" in options2) {
          saveOptions2.wtimeout = options2.wtimeout;
        }
      }
      if ("checkKeys" in options2) {
        saveOptions2.checkKeys = options2.checkKeys;
      }
      const session = doc.$session();
      const asyncLocalStorage = doc[modelDbSymbol].base.transactionAsyncLocalStorage?.getStore();
      if (session != null) {
        saveOptions2.session = session;
      } else if (!Object.hasOwn(options2, "session") && asyncLocalStorage?.session != null) {
        saveOptions2.session = asyncLocalStorage.session;
      }
      return saveOptions2;
    }
    Model.prototype.$__save = async function $__save(options2) {
      try {
        const hasValidateBeforeSaveOption = options2 && typeof options2 === "object" && "validateBeforeSave" in options2;
        const shouldValidateBeforeSave = hasValidateBeforeSaveOption ? !!options2.validateBeforeSave : this.$__schema.options.validateBeforeSave;
        if (shouldValidateBeforeSave) {
          const hasValidateModifiedOnlyOption = options2 != null && typeof options2 === "object" && Object.hasOwn(options2, "validateModifiedOnly");
          const validateOptions = hasValidateModifiedOnlyOption ? { validateModifiedOnly: options2.validateModifiedOnly } : null;
          await this.$validate(validateOptions);
          this.$op = "save";
        }
        await this._execDocumentPreHooks("save", options2, [options2]);
      } catch (error2) {
        await this._execDocumentPostHooks("save", options2, error2);
        return;
      }
      let result = null;
      let where = null;
      try {
        const saveOptions2 = _createSaveOptions(this, options2);
        if (this.$isNew) {
          const hasOnlyPrimitiveValues = this.$__hasOnlyPrimitiveValues();
          const obj = hasOnlyPrimitiveValues ? this.$__toObjectShallow() : this.toObject(saveToObjectOptions);
          if ((obj || {})._id === void 0) {
            throw new MongooseError("document must have an _id before saving");
          }
          this.$__version(true, obj);
          this.$__reset();
          _setIsNew(this, false, hasOnlyPrimitiveValues);
          this.$__.inserting = true;
          result = await this[modelCollectionSymbol].insertOne(obj, saveOptions2).catch((err) => {
            _setIsNew(this, true, hasOnlyPrimitiveValues);
            throw err;
          });
        } else {
          this.$__.inserting = false;
          const pathsToSave = Array.isArray(options2.pathsToSave) ? options2.pathsToSave : null;
          const pathsToSaveSet = pathsToSave != null ? new Set(pathsToSave) : null;
          const delta = this.$__delta(pathsToSave, pathsToSaveSet);
          const unsavedDirty = pathsToSave != null ? delta != null ? delta[2] : this.$__dirty() : null;
          const unsavedDefaultPaths = pathsToSave != null ? Object.keys(this.$__.activePaths.getStatePaths("default")).filter((path) => !isInPathsToSave2(path, pathsToSaveSet, pathsToSave)) : null;
          if (delta) {
            where = this.$__where(delta[0]);
            _applyCustomWhere(this, where);
            const update = delta[1];
            if (this.$__schema.options.minimize) {
              for (const updateOp of Object.values(update)) {
                if (updateOp == null) {
                  continue;
                }
                for (const key of Object.keys(updateOp)) {
                  if (updateOp[key] == null || typeof updateOp[key] !== "object") {
                    continue;
                  }
                  if (!utils2.isPOJO(updateOp[key])) {
                    continue;
                  }
                  minimize(updateOp[key]);
                  if (utils2.hasOwnKeys(updateOp[key]) === false) {
                    delete updateOp[key];
                    update.$unset = update.$unset || {};
                    update.$unset[key] = 1;
                  }
                }
              }
            }
            this.$__.modifiedPaths = this.modifiedPaths();
            this.$__reset();
            restoreUnsavedState(this, unsavedDirty, unsavedDefaultPaths);
            _setIsNew(this, false);
            result = await this[modelCollectionSymbol].updateOne(where, update, saveOptions2).catch((err) => {
              this.$__undoReset();
              throw err;
            });
          } else {
            where = this.$__where();
            _applyCustomWhere(this, where);
            if (this.$__.version) {
              this.$__version(where, delta);
            }
            applyReadConcern2(this.$__schema, saveOptions2);
            result = await this.constructor.collection.findOne(where, { ...saveOptions2, projection: { _id: 1 } }).then((documentExists) => ({ matchedCount: !documentExists ? 0 : 1 }));
          }
        }
      } catch (err) {
        const error2 = this.$__schema._transformDuplicateKeyError(err);
        await this._execDocumentPostHooks("save", options2, error2);
        return;
      }
      let numAffected = 0;
      const writeConcern = options2 != null ? options2.writeConcern != null ? options2.writeConcern.w : options2.w : 0;
      if (writeConcern !== 0) {
        if (result != null) {
          if (Array.isArray(result)) {
            numAffected = result.length;
          } else if (result.matchedCount != null) {
            numAffected = result.matchedCount;
          } else {
            numAffected = result;
          }
        }
        const versionBump = this.$__.version;
        if (versionBump && !this.$__.inserting) {
          const doIncrement = VERSION_INC === (VERSION_INC & this.$__.version);
          this.$__.version = void 0;
          const key = this.$__schema.options.versionKey;
          const version2 = this.$__getValue(key) || 0;
          if (numAffected <= 0) {
            this.$__undoReset();
            const err = this.$__.$versionError || new VersionError(this, version2, this.$__.modifiedPaths);
            await this._execDocumentPostHooks("save", options2, err);
            return;
          }
          if (doIncrement) {
            this.$__setValue(key, version2 + 1);
          }
        }
        if (result != null && numAffected <= 0) {
          this.$__undoReset();
          const error2 = new DocumentNotFoundError(where, this.constructor.modelName, numAffected, result);
          await this._execDocumentPostHooks("save", options2, error2);
          return;
        }
      }
      this.$__.saving = void 0;
      this.$__.savedState = {};
      this.$emit("save", this, numAffected);
      this.constructor.emit("save", this, numAffected);
      await this._execDocumentPostHooks("save", options2);
    };
    function restoreUnsavedState(doc, unsavedDirty, unsavedDefaultPaths) {
      if (unsavedDirty == null) {
        return;
      }
      for (const dirty of unsavedDirty) {
        doc.$__.activePaths.modify(dirty.path);
        if (dirty.value?.[arrayAtomicsBackupSymbol]) {
          dirty.value[arrayAtomicsSymbol] = dirty.value[arrayAtomicsBackupSymbol];
          dirty.value[arrayAtomicsBackupSymbol] = null;
        }
      }
      for (const path of unsavedDefaultPaths) {
        doc.$__.activePaths.default(path);
      }
    }
    function generateVersionError(doc, modifiedPaths2, defaultPaths) {
      const key = doc.$__schema.options.versionKey;
      if (!key) {
        return null;
      }
      const version2 = doc.$__getValue(key) || 0;
      return new VersionError(doc, version2, modifiedPaths2.concat(defaultPaths));
    }
    Model.prototype.save = async function save(options2) {
      if (typeof options2 === "function" || typeof arguments[1] === "function") {
        throw new MongooseError("Model.prototype.save() no longer accepts a callback");
      }
      let parallelSave2;
      this.$op = "save";
      if (this.$__.saving) {
        parallelSave2 = new ParallelSaveError(this);
      } else {
        this.$__.saving = true;
      }
      options2 = new SaveOptions(options2);
      if (Object.hasOwn(options2, "session")) {
        this.$session(options2.session);
      }
      if (this.$__.timestamps != null) {
        options2.timestamps = this.$__.timestamps;
      }
      if (!this.$isNew) {
        this.$__.$versionError = generateVersionError(
          this,
          this.modifiedPaths(),
          Object.keys(this.$__.activePaths.getStatePaths("default"))
        );
      }
      if (parallelSave2) {
        this.$__handleReject(parallelSave2);
        throw parallelSave2;
      }
      this.$__.saveOptions = options2;
      const _this = this;
      return traceSave(async function maybeTracedSave() {
        try {
          await _this.$__save(options2);
        } catch (error2) {
          _this.$__handleReject(error2);
          throw error2;
        } finally {
          _this.$__.saving = null;
          _this.$__.saveOptions = null;
          _this.$__.$versionError = null;
          _this.$op = null;
        }
        return _this;
      }, () => ({
        operation: "save",
        collection: _this.constructor.collection.name,
        database: _this.constructor.db?.name,
        serverAddress: _this.constructor.db?.host,
        serverPort: _this.constructor.db?.port,
        args: { options: options2 }
      }));
    };
    Model.prototype.$save = Model.prototype.save;
    Model.prototype.$__version = function(where, delta) {
      const key = this.$__schema.options.versionKey;
      if (where === true) {
        if (key) {
          setDottedPath2(delta, key, 0);
          this.$__setValue(key, 0);
        }
        return;
      }
      if (key === false) {
        return;
      }
      if (!this.$__isSelected(key)) {
        return;
      }
      if (VERSION_WHERE === (VERSION_WHERE & this.$__.version)) {
        const value = this.$__getValue(key);
        if (value != null) where[key] = value;
      }
      if (VERSION_INC === (VERSION_INC & this.$__.version)) {
        if (get2(delta.$set, key, null) != null) {
          ++delta.$set[key];
        } else {
          delta.$inc = delta.$inc || {};
          delta.$inc[key] = 1;
        }
      }
    };
    Model.prototype.increment = function increment() {
      this.$__.version = VERSION_ALL;
      return this;
    };
    Model.prototype.$__where = function _where(where) {
      where || (where = {});
      if (!where._id) {
        where._id = this._doc._id;
      }
      if (this._doc._id === void 0) {
        throw new MongooseError("No _id found on document!");
      }
      return where;
    };
    Model.prototype.deleteOne = function deleteOne(options2) {
      if (typeof options2 === "function" || typeof arguments[1] === "function") {
        throw new MongooseError("Model.prototype.deleteOne() no longer accepts a callback");
      }
      if (!options2) {
        options2 = {};
      }
      if (Object.hasOwn(options2, "session")) {
        this.$session(options2.session);
      }
      const self = this;
      const where = this.$__where();
      const query2 = self.constructor.deleteOne();
      if (this.$session() != null) {
        if (!("session" in query2.options)) {
          query2.options.session = this.$session();
        }
      }
      const preFilter = buildMiddlewareFilter(options2, "pre");
      const postFilter = buildMiddlewareFilter(options2, "post");
      query2.pre(async function queryPreDeleteOne() {
        const res = await self.constructor._middleware.execPre("deleteOne", self, [self, options2], { filter: preFilter });
        if (res[0] !== self || res[1] !== options2) {
          throw new MongooseError("Document deleteOne pre hooks cannot overwrite arguments");
        }
        query2.deleteOne(where, options2);
        if (self.$where != null) {
          this.where(self.$where);
        }
        return res;
      });
      query2.pre(function callSubdocPreHooks() {
        return Promise.all(self.$getAllSubdocs().map((subdoc) => subdoc.constructor._middleware.execPre("deleteOne", subdoc, [subdoc], { filter: preFilter })));
      });
      query2.pre(function skipIfAlreadyDeleted() {
        if (self.$__.isDeleted) {
          throw new Kareem.skipWrappedFunction();
        }
      });
      query2.post(function callSubdocPostHooks() {
        return Promise.all(self.$getAllSubdocs().map((subdoc) => subdoc.constructor._middleware.execPost("deleteOne", subdoc, [subdoc], { filter: postFilter })));
      });
      query2.post(function queryPostDeleteOne() {
        return self.constructor._middleware.execPost("deleteOne", self, [self], { filter: postFilter });
      });
      query2.transform(function setIsDeleted(result) {
        if (result?.deletedCount > 0) {
          self.$isDeleted(true);
        }
        return result;
      });
      return query2;
    };
    Model.prototype.$model = function $model(name) {
      if (arguments.length === 0) {
        return this.constructor;
      }
      return this[modelDbSymbol].model(name);
    };
    Model.prototype.model = Model.prototype.$model;
    Model.exists = function exists2(filter, options2) {
      _checkContext(this, "exists");
      if (typeof arguments[2] === "function") {
        throw new MongooseError("Model.exists() no longer accepts a callback");
      }
      const query2 = this.findOne(filter).select({ _id: 1 }).lean().setOptions(options2);
      return query2;
    };
    Model.discriminator = function(name, schema2, options2) {
      let model2;
      if (typeof name === "function") {
        model2 = name;
        name = utils2.getFunctionName(model2);
        if (!(model2.prototype instanceof Model)) {
          throw new MongooseError("The provided class " + name + " must extend Model");
        }
      }
      options2 = options2 || {};
      const value = utils2.isPOJO(options2) ? options2.value : options2;
      const clone2 = typeof options2.clone === "boolean" ? options2.clone : true;
      const mergePlugins = typeof options2.mergePlugins === "boolean" ? options2.mergePlugins : true;
      const overwriteModels = typeof options2.overwriteModels === "boolean" ? options2.overwriteModels : false;
      _checkContext(this, "discriminator");
      if (utils2.isObject(schema2) && !schema2.instanceOfSchema) {
        schema2 = new Schema(schema2);
      }
      if (schema2 instanceof Schema && clone2) {
        schema2 = schema2.clone();
      }
      schema2 = discriminator2(this, name, schema2, value, mergePlugins, options2.mergeHooks, overwriteModels);
      if (this.db.models[name] && !schema2.options.overwriteModels && !overwriteModels) {
        throw new OverwriteModelError(name);
      }
      schema2.$isRootDiscriminator = true;
      schema2.$globalPluginsApplied = true;
      model2 = this.db.model(model2 || name, schema2, this.$__collection.name);
      this.discriminators[name] = model2;
      const d = this.discriminators[name];
      Object.setPrototypeOf(d.prototype, this.prototype);
      Object.defineProperty(d, "baseModelName", {
        value: this.modelName,
        configurable: true,
        writable: false
      });
      applyMethods2(d, schema2);
      applyStatics2(d, schema2);
      if (this[subclassedSymbol] != null) {
        for (const submodel of this[subclassedSymbol]) {
          submodel.discriminators = submodel.discriminators || {};
          submodel.discriminators[name] = model2.__subclass(model2.db, schema2, submodel.collection.name);
        }
      }
      return d;
    };
    function _checkContext(ctx, fnName) {
      if (ctx == null || ctx === commonjsGlobal) {
        throw new MongooseError("`Model." + fnName + "()` cannot run without a model as `this`. Make sure you are calling `MyModel." + fnName + "()` where `MyModel` is a Mongoose model.");
      } else if (ctx[modelSymbol] == null) {
        throw new MongooseError("`Model." + fnName + "()` cannot run without a model as `this`. Make sure you are not calling `new Model." + fnName + "()`");
      }
    }
    for (const i in EventEmitter.prototype) {
      Model[i] = EventEmitter.prototype[i];
    }
    Model.init = function init() {
      _checkContext(this, "init");
      if (typeof arguments[0] === "function") {
        throw new MongooseError("Model.init() no longer accepts a callback");
      }
      this.schema.emit("init", this);
      if (this.$init != null) {
        return this.$init;
      }
      const conn = this.db;
      const _ensureIndexes2 = async () => {
        const autoIndex = utils2.getOption(
          "autoIndex",
          this.schema.options,
          conn.config,
          conn.base.options
        );
        if (!autoIndex) {
          return;
        }
        return await this.ensureIndexes({ _automatic: true });
      };
      const _createSearchIndexes = async () => {
        const autoSearchIndex = utils2.getOption(
          "autoSearchIndex",
          this.schema.options,
          conn.config,
          conn.base.options
        );
        if (!autoSearchIndex) {
          return;
        }
        return await this.createSearchIndexes();
      };
      const _createCollection = async () => {
        let autoCreate = utils2.getOption(
          "autoCreate",
          this.schema.options,
          conn.config
          // No base.options here because we don't want to take the base value if the connection hasn't
          // set it yet
        );
        if (autoCreate == null) {
          await conn._waitForConnect(true);
          autoCreate = utils2.getOption(
            "autoCreate",
            this.schema.options,
            conn.config,
            conn.base.options
          );
        }
        if (!autoCreate) {
          return;
        }
        return await this.createCollection();
      };
      this.$init = _createCollection().then(() => _ensureIndexes2()).then(() => _createSearchIndexes());
      const _catch = this.$init.catch;
      const _this = this;
      this.$init.catch = function() {
        _this.$caught = true;
        return _catch.apply(_this.$init, arguments);
      };
      return this.$init;
    };
    Model.createCollection = async function createCollection(options2) {
      _checkContext(this, "createCollection");
      if (typeof arguments[0] === "function" || typeof arguments[1] === "function") {
        throw new MongooseError("Model.createCollection() no longer accepts a callback");
      }
      const preFilter = buildMiddlewareFilter(options2, "pre");
      const postFilter = buildMiddlewareFilter(options2, "post");
      if (options2?.middleware != null) {
        options2 = { ...options2 };
        delete options2.middleware;
      }
      [options2] = await this.hooks.execPre("createCollection", this, [options2], { filter: preFilter }).catch((err) => {
        if (err instanceof Kareem.skipWrappedFunction) {
          return [err];
        }
        throw err;
      });
      const collectionOptions = this?.schema?.options?.collectionOptions;
      if (collectionOptions != null) {
        options2 = Object.assign({}, collectionOptions, options2);
      }
      const schemaCollation = this?.schema?.options?.collation;
      if (schemaCollation != null) {
        options2 = Object.assign({ collation: schemaCollation }, options2);
      }
      const capped = this?.schema?.options?.capped;
      if (capped != null) {
        if (typeof capped === "number") {
          options2 = Object.assign({ capped: true, size: capped }, options2);
        } else if (typeof capped === "object") {
          options2 = Object.assign({ capped: true }, capped, options2);
        }
      }
      const timeseries = this?.schema?.options?.timeseries;
      if (timeseries != null) {
        options2 = Object.assign({ timeseries }, options2);
        if (options2.expireAfterSeconds != null) ;
        else if (options2.expires != null) {
          utils2.expires(options2);
        } else if (this.schema.options.expireAfterSeconds != null) {
          options2.expireAfterSeconds = this.schema.options.expireAfterSeconds;
        } else if (this.schema.options.expires != null) {
          options2.expires = this.schema.options.expires;
          utils2.expires(options2);
        }
      }
      const clusteredIndex = this?.schema?.options?.clusteredIndex;
      if (clusteredIndex != null) {
        options2 = Object.assign({ clusteredIndex: { ...clusteredIndex, unique: true } }, options2);
      }
      try {
        if (!(options2 instanceof Kareem.skipWrappedFunction)) {
          await this.db.createCollection(this.$__collection.collectionName, options2);
        }
      } catch (err) {
        if (err != null && (err.name !== "MongoServerError" || err.code !== 48)) {
          await this.hooks.execPost("createCollection", this, [null], { error: err, filter: postFilter });
        }
      }
      await this.hooks.execPost("createCollection", this, [this.$__collection], { filter: postFilter });
      return this.$__collection;
    };
    Model.syncIndexes = async function syncIndexes2(options2) {
      _checkContext(this, "syncIndexes");
      if (typeof arguments[0] === "function" || typeof arguments[1] === "function") {
        throw new MongooseError("Model.syncIndexes() no longer accepts a callback");
      }
      const autoCreate = options2?.autoCreate ?? this.schema.options?.autoCreate ?? this.db.config.autoCreate ?? this.db.base?.options?.autoCreate ?? true;
      if (autoCreate) {
        try {
          await this.createCollection();
        } catch (err) {
          if (err != null && (err.name !== "MongoServerError" || err.code !== 48)) {
            throw err;
          }
        }
      }
      const diffIndexesResult = await this.diffIndexes({ indexOptionsToCreate: true });
      const dropped = await this.cleanIndexes({ ...options2, toDrop: diffIndexesResult.toDrop });
      await this.createIndexes({ ...options2, toCreate: diffIndexesResult.toCreate });
      return dropped;
    };
    Model.createSearchIndex = async function createSearchIndex(description) {
      _checkContext(this, "createSearchIndex");
      return await this.$__collection.createSearchIndex(description);
    };
    Model.updateSearchIndex = async function updateSearchIndex(name, definition) {
      _checkContext(this, "updateSearchIndex");
      return await this.$__collection.updateSearchIndex(name, definition);
    };
    Model.dropSearchIndex = async function dropSearchIndex(name) {
      _checkContext(this, "dropSearchIndex");
      return await this.$__collection.dropSearchIndex(name);
    };
    Model.listSearchIndexes = async function listSearchIndexes(options2) {
      _checkContext(this, "listSearchIndexes");
      const cursor = await this.$__collection.listSearchIndexes(options2);
      return await cursor.toArray();
    };
    Model.diffIndexes = async function diffIndexes(options2) {
      if (typeof arguments[0] === "function" || typeof arguments[1] === "function") {
        throw new MongooseError("Model.syncIndexes() no longer accepts a callback");
      }
      const model2 = this;
      let dbIndexes = await model2.listIndexes().catch((err) => {
        if (err.codeName == "NamespaceNotFound") {
          return void 0;
        }
        throw err;
      });
      if (dbIndexes === void 0) {
        dbIndexes = [];
      }
      dbIndexes = getRelatedDBIndexes(model2, dbIndexes);
      const schema2 = model2.schema;
      const schemaIndexes = getRelatedSchemaIndexes(model2, schema2.indexes());
      const toDrop = getIndexesToDrop(schema2, schemaIndexes, dbIndexes);
      const toCreate = getIndexesToCreate(schema2, schemaIndexes, dbIndexes, toDrop, options2);
      return { toDrop, toCreate };
    };
    function getIndexesToCreate(schema2, schemaIndexes, dbIndexes, toDrop, options2) {
      const toCreate = [];
      const indexOptionsToCreate = options2?.indexOptionsToCreate ?? false;
      for (const [schemaIndexKeysObject, schemaIndexOptions] of schemaIndexes) {
        let found = false;
        const options3 = decorateDiscriminatorIndexOptions2(schema2, clone(schemaIndexOptions));
        for (const index of dbIndexes) {
          if (isDefaultIdIndex2(index)) {
            continue;
          }
          if (isIndexEqual2(schemaIndexKeysObject, options3, index) && !toDrop.includes(index.name)) {
            found = true;
            break;
          }
        }
        if (!found) {
          if (indexOptionsToCreate) {
            toCreate.push([schemaIndexKeysObject, schemaIndexOptions]);
          } else {
            toCreate.push(schemaIndexKeysObject);
          }
        }
      }
      return toCreate;
    }
    function getIndexesToDrop(schema2, schemaIndexes, dbIndexes) {
      const toDrop = [];
      for (const dbIndex of dbIndexes) {
        let found = false;
        if (isDefaultIdIndex2(dbIndex)) {
          continue;
        }
        if (isTimeseriesIndex2(dbIndex, schema2.options)) {
          continue;
        }
        for (const [schemaIndexKeysObject, schemaIndexOptions] of schemaIndexes) {
          const options2 = decorateDiscriminatorIndexOptions2(schema2, clone(schemaIndexOptions));
          applySchemaCollation2(schemaIndexKeysObject, options2, schema2.options);
          if (isIndexEqual2(schemaIndexKeysObject, options2, dbIndex)) {
            found = true;
            break;
          }
        }
        if (found) {
          continue;
        }
        toDrop.push(dbIndex.name);
      }
      return toDrop;
    }
    Model.cleanIndexes = async function cleanIndexes(options2) {
      _checkContext(this, "cleanIndexes");
      if (typeof arguments[0] === "function" || typeof arguments[1] === "function") {
        throw new MongooseError("Model.cleanIndexes() no longer accepts a callback");
      }
      const model2 = this;
      if (Array.isArray(options2?.toDrop)) {
        const res2 = await _dropIndexes(options2.toDrop, model2, options2);
        return res2;
      }
      const res = await model2.diffIndexes();
      return await _dropIndexes(res.toDrop, model2, options2);
    };
    async function _dropIndexes(toDrop, model2, options2) {
      if (toDrop.length === 0) {
        return [];
      }
      const collection2 = model2.$__collection;
      if (options2?.hideIndexes) {
        await Promise.all(toDrop.map((indexName) => {
          return model2.db.db.command({
            collMod: collection2.collectionName,
            index: { name: indexName, hidden: true }
          });
        }));
      } else {
        await Promise.all(toDrop.map((indexName) => collection2.dropIndex(indexName)));
      }
      return toDrop;
    }
    Model.listIndexes = async function listIndexes() {
      _checkContext(this, "listIndexes");
      if (typeof arguments[0] === "function") {
        throw new MongooseError("Model.listIndexes() no longer accepts a callback");
      }
      if (this.$__collection.buffer) {
        await new Promise((resolve) => {
          this.$__collection.addQueue(resolve);
        });
      }
      return this.$__collection.listIndexes().toArray();
    };
    Model.ensureIndexes = async function ensureIndexes(options2) {
      _checkContext(this, "ensureIndexes");
      if (typeof arguments[0] === "function" || typeof arguments[1] === "function") {
        throw new MongooseError("Model.ensureIndexes() no longer accepts a callback");
      }
      await new Promise((resolve, reject) => {
        _ensureIndexes(this, options2, (err) => {
          if (err != null) {
            return reject(err);
          }
          resolve();
        });
      });
    };
    Model.createIndexes = async function createIndexes(options2) {
      _checkContext(this, "createIndexes");
      if (typeof arguments[0] === "function" || typeof arguments[1] === "function") {
        throw new MongooseError("Model.createIndexes() no longer accepts a callback");
      }
      return this.ensureIndexes(options2);
    };
    function _ensureIndexes(model2, options2, callback) {
      const indexes = Array.isArray(options2?.toCreate) ? options2.toCreate : model2.schema.indexes();
      let indexError;
      options2 = options2 || {};
      const done = function(err) {
        model2.emit("index", indexError);
        callback && callback(indexError);
      };
      for (const index of indexes) {
        if (isDefaultIdIndex2(index)) {
          utils2.warn('mongoose: Cannot specify a custom index on `_id` for model name "' + model2.modelName + '", MongoDB does not allow overwriting the default `_id` index. See https://bit.ly/mongodb-id-index');
        }
      }
      const seenIndexes = [];
      for (const index of indexes) {
        const fields = index[0];
        const indexOptions = index[1];
        if (indexOptions.name == null) {
          for (const existingIndex of seenIndexes) {
            if (existingIndex[1].name == null && isIndexSpecEqual2(existingIndex[0], fields)) {
              utils2.warn("mongoose: Duplicate schema index on " + JSON.stringify(fields) + ' for model "' + model2.modelName + '". This is often due to declaring an index using both "index: true" and "schema.index()". Please remove the duplicate index definition.');
              break;
            }
          }
        }
        seenIndexes.push(index);
      }
      if (!indexes.length) {
        immediate2(function() {
          done();
        });
        return;
      }
      const indexSingleDone = function(err, fields, options3, name) {
        model2.emit("index-single-done", err, fields, options3, name);
      };
      const indexSingleStart = function(fields, options3) {
        model2.emit("index-single-start", fields, options3);
      };
      const baseSchema = model2.schema._baseSchema;
      const baseSchemaIndexes = baseSchema ? baseSchema.indexes() : [];
      immediate2(function() {
        if (options2._automatic && !model2.collection.collection) {
          model2.collection.addQueue(create, []);
        } else {
          create();
        }
      });
      function create() {
        if (options2._automatic) {
          if (model2.schema.options.autoIndex === false || model2.schema.options.autoIndex == null && model2.db.config.autoIndex === false) {
            return done();
          }
        }
        const index = indexes.shift();
        if (!index) {
          return done();
        }
        if (options2._automatic && index[1]._autoIndex === false) {
          return create();
        }
        if (baseSchemaIndexes.find((i) => utils2.deepEqual(i, index))) {
          return create();
        }
        const indexFields = clone(index[0]);
        const indexOptions = clone(index[1]);
        delete indexOptions._autoIndex;
        decorateDiscriminatorIndexOptions2(model2.schema, indexOptions);
        applyWriteConcern2(model2.schema, indexOptions);
        applySchemaCollation2(indexFields, indexOptions, model2.schema.options);
        indexSingleStart(indexFields, options2);
        let promise = null;
        try {
          promise = model2.collection.createIndex(indexFields, indexOptions);
        } catch (err) {
          if (!indexError) {
            indexError = err;
          }
          if (!model2.$caught) {
            model2.emit("error", err);
          }
          indexSingleDone(err, indexFields, indexOptions);
          create();
          return;
        }
        promise.then(
          (name) => {
            indexSingleDone(null, indexFields, indexOptions, name);
            create();
          },
          (err) => {
            if (!indexError) {
              indexError = err;
            }
            if (!model2.$caught) {
              model2.emit("error", err);
            }
            indexSingleDone(err, indexFields, indexOptions);
            create();
          }
        );
      }
    }
    Model.createSearchIndexes = async function createSearchIndexes() {
      _checkContext(this, "createSearchIndexes");
      const results = [];
      for (const searchIndex of this.schema._searchIndexes) {
        results.push(await this.createSearchIndex(searchIndex));
      }
      return results;
    };
    Model.schema;
    Model.db;
    Model.collection;
    Model.$__collection;
    Model.base;
    Model.discriminators;
    Model.translateAliases = function translateAliases(fields, errorOnDuplicates) {
      _checkContext(this, "translateAliases");
      const translate = (key, value) => {
        let alias;
        const translated = [];
        const fieldKeys = key.split(".");
        let currentSchema = this.schema;
        for (const i in fieldKeys) {
          const name = fieldKeys[i];
          if (currentSchema?.aliases[name]) {
            alias = currentSchema.aliases[name];
            if (errorOnDuplicates && alias in fields) {
              throw new MongooseError(`Provided object has both field "${name}" and its alias "${alias}"`);
            }
            translated.push(alias);
          } else {
            alias = name;
            translated.push(name);
          }
          if (currentSchema?.paths[alias]) {
            currentSchema = currentSchema.paths[alias].schema;
          } else
            currentSchema = null;
        }
        const translatedKey = translated.join(".");
        if (fields instanceof Map)
          fields.set(translatedKey, value);
        else
          fields[translatedKey] = value;
        if (translatedKey !== key) {
          if (fields instanceof Map) {
            fields.delete(key);
          } else {
            delete fields[key];
          }
        }
        return fields;
      };
      if (typeof fields === "object") {
        if (fields instanceof Map) {
          for (const field of new Map(fields)) {
            fields = translate(field[0], field[1]);
          }
        } else {
          for (const key of Object.keys(fields)) {
            fields = translate(key, fields[key]);
            if (key[0] === "$") {
              if (Array.isArray(fields[key])) {
                for (const i in fields[key]) {
                  fields[key][i] = this.translateAliases(fields[key][i]);
                }
              } else {
                this.translateAliases(fields[key]);
              }
            }
          }
        }
        return fields;
      } else {
        return fields;
      }
    };
    Model.deleteOne = function deleteOne(conditions, options2) {
      _checkContext(this, "deleteOne");
      if (typeof arguments[0] === "function" || typeof arguments[1] === "function" || typeof arguments[2] === "function") {
        throw new MongooseError("Model.prototype.deleteOne() no longer accepts a callback");
      }
      const mq = new this.Query({}, {}, this, this.$__collection);
      mq.setOptions(options2);
      return mq.deleteOne(conditions);
    };
    Model.deleteMany = function deleteMany(conditions, options2) {
      _checkContext(this, "deleteMany");
      if (typeof arguments[0] === "function" || typeof arguments[1] === "function" || typeof arguments[2] === "function") {
        throw new MongooseError("Model.deleteMany() no longer accepts a callback");
      }
      const mq = new this.Query({}, {}, this, this.$__collection);
      mq.setOptions(options2);
      return mq.deleteMany(conditions);
    };
    Model.find = function find(conditions, projection, options2) {
      _checkContext(this, "find");
      if (typeof arguments[0] === "function" || typeof arguments[1] === "function" || typeof arguments[2] === "function" || typeof arguments[3] === "function") {
        throw new MongooseError("Model.find() no longer accepts a callback");
      }
      const mq = new this.Query({}, {}, this, this.$__collection);
      mq.select(projection);
      mq.setOptions(options2);
      return mq.find(conditions);
    };
    Model.findById = function findById(id, projection, options2) {
      _checkContext(this, "findById");
      if (typeof arguments[0] === "function" || typeof arguments[1] === "function" || typeof arguments[2] === "function") {
        throw new MongooseError("Model.findById() no longer accepts a callback");
      }
      return this.findOne({ _id: id }, projection, options2);
    };
    Model.findOne = function findOne(conditions, projection, options2) {
      _checkContext(this, "findOne");
      if (typeof arguments[0] === "function" || typeof arguments[1] === "function" || typeof arguments[2] === "function") {
        throw new MongooseError("Model.findOne() no longer accepts a callback");
      }
      const mq = new this.Query({}, {}, this, this.$__collection);
      mq.select(projection);
      mq.setOptions(options2);
      return mq.findOne(conditions);
    };
    Model.estimatedDocumentCount = function estimatedDocumentCount(options2) {
      _checkContext(this, "estimatedDocumentCount");
      const mq = new this.Query({}, {}, this, this.$__collection);
      return mq.estimatedDocumentCount(options2);
    };
    Model.countDocuments = function countDocuments(conditions, options2) {
      _checkContext(this, "countDocuments");
      if (typeof arguments[0] === "function" || typeof arguments[1] === "function" || typeof arguments[2] === "function") {
        throw new MongooseError("Model.countDocuments() no longer accepts a callback");
      }
      const mq = new this.Query({}, {}, this, this.$__collection);
      if (options2 != null) {
        mq.setOptions(options2);
      }
      return mq.countDocuments(conditions);
    };
    Model.distinct = function distinct(field, conditions, options2) {
      _checkContext(this, "distinct");
      if (typeof arguments[0] === "function" || typeof arguments[1] === "function" || typeof arguments[2] === "function") {
        throw new MongooseError("Model.distinct() no longer accepts a callback");
      }
      const mq = new this.Query({}, {}, this, this.$__collection);
      if (options2 != null) {
        mq.setOptions(options2);
      }
      return mq.distinct(field, conditions);
    };
    Model.where = function where(path, val) {
      _checkContext(this, "where");
      const mq = new this.Query({}, {}, this, this.$__collection).find({});
      return mq.where.apply(mq, arguments);
    };
    Model.$where = function $where() {
      _checkContext(this, "$where");
      const mq = new this.Query({}, {}, this, this.$__collection).find({});
      return mq.$where.apply(mq, arguments);
    };
    Model.findOneAndUpdate = function(conditions, update, options2) {
      _checkContext(this, "findOneAndUpdate");
      if (typeof arguments[0] === "function" || typeof arguments[1] === "function" || typeof arguments[2] === "function" || typeof arguments[3] === "function") {
        throw new MongooseError("Model.findOneAndUpdate() no longer accepts a callback");
      }
      let fields;
      if (options2) {
        fields = options2.fields || options2.projection;
      }
      const mq = new this.Query({}, {}, this, this.$__collection);
      mq.select(fields);
      return mq.findOneAndUpdate(conditions, update, options2);
    };
    Model.findByIdAndUpdate = function(id, update, options2) {
      _checkContext(this, "findByIdAndUpdate");
      if (typeof arguments[0] === "function" || typeof arguments[1] === "function" || typeof arguments[2] === "function" || typeof arguments[3] === "function") {
        throw new MongooseError("Model.findByIdAndUpdate() no longer accepts a callback");
      }
      if (id instanceof Document) {
        id = id._doc._id;
      }
      return this.findOneAndUpdate.call(this, { _id: id }, update, options2);
    };
    Model.findOneAndDelete = function(conditions, options2) {
      _checkContext(this, "findOneAndDelete");
      if (typeof arguments[0] === "function" || typeof arguments[1] === "function" || typeof arguments[2] === "function") {
        throw new MongooseError("Model.findOneAndDelete() no longer accepts a callback");
      }
      let fields;
      if (options2) {
        fields = options2.select;
        options2.select = void 0;
      }
      const mq = new this.Query({}, {}, this, this.$__collection);
      mq.select(fields);
      return mq.findOneAndDelete(conditions, options2);
    };
    Model.findByIdAndDelete = function(id, options2) {
      _checkContext(this, "findByIdAndDelete");
      if (typeof arguments[0] === "function" || typeof arguments[1] === "function" || typeof arguments[2] === "function") {
        throw new MongooseError("Model.findByIdAndDelete() no longer accepts a callback");
      }
      return this.findOneAndDelete({ _id: id }, options2);
    };
    Model.findOneAndReplace = function(filter, replacement, options2) {
      _checkContext(this, "findOneAndReplace");
      if (typeof arguments[0] === "function" || typeof arguments[1] === "function" || typeof arguments[2] === "function" || typeof arguments[3] === "function") {
        throw new MongooseError("Model.findOneAndReplace() no longer accepts a callback");
      }
      let fields;
      if (options2) {
        fields = options2.select;
        options2.select = void 0;
      }
      const mq = new this.Query({}, {}, this, this.$__collection);
      mq.select(fields);
      return mq.findOneAndReplace(filter, replacement, options2);
    };
    Model.create = async function create(doc, options2) {
      if (typeof options2 === "function" || typeof arguments[2] === "function") {
        throw new MongooseError("Model.create() no longer accepts a callback");
      }
      _checkContext(this, "create");
      let args;
      const discriminatorKey = this.schema.options.discriminatorKey;
      if (Array.isArray(doc)) {
        args = doc;
        options2 = options2 != null && typeof options2 === "object" ? options2 : {};
      } else {
        const last = arguments[arguments.length - 1];
        options2 = {};
        const hasCallback = typeof last === "function" || typeof options2 === "function" || typeof arguments[2] === "function";
        if (hasCallback) {
          throw new MongooseError("Model.create() no longer accepts a callback");
        } else {
          args = [...arguments];
          if (args.length > 1 && !last) {
            args.pop();
          }
        }
        if (args.length === 2 && args[0] != null && args[1] != null && args[0].session == null && last && getConstructorName2(last.session) === "ClientSession" && !this.schema.path("session")) {
          utils2.warn("WARNING: to pass a `session` to `Model.create()` in Mongoose, you **must** pass an array as the first argument. See: https://mongoosejs.com/docs/api/model.html#Model.create()");
        }
      }
      if (args.length === 0) {
        return Array.isArray(doc) ? [] : null;
      }
      let res = [];
      const immediateError = typeof options2.aggregateErrors === "boolean" ? !options2.aggregateErrors : true;
      delete options2.aggregateErrors;
      if (options2.session && !options2.ordered && args.length > 1) {
        throw new MongooseError("Cannot call `create()` with a session and multiple documents unless `ordered: true` is set");
      }
      if (!Array.isArray(doc) && args.length === 1) {
        let toSave = doc;
        const Model2 = this.discriminators && doc[discriminatorKey] != null ? this.discriminators[doc[discriminatorKey]] || getDiscriminatorByValue2(this.discriminators, doc[discriminatorKey]) : this;
        if (Model2 == null) {
          throw new MongooseError(`Discriminator "${doc[discriminatorKey]}" not found for model "${this.modelName}"`);
        }
        if (!(toSave instanceof Model2)) {
          toSave = new Model2(toSave);
        }
        await toSave.$save(options2);
        return toSave;
      }
      if (options2.ordered) {
        for (let i = 0; i < args.length; i++) {
          try {
            const doc2 = args[i];
            const Model2 = this.discriminators && doc2[discriminatorKey] != null ? this.discriminators[doc2[discriminatorKey]] || getDiscriminatorByValue2(this.discriminators, doc2[discriminatorKey]) : this;
            if (Model2 == null) {
              throw new MongooseError(`Discriminator "${doc2[discriminatorKey]}" not found for model "${this.modelName}"`);
            }
            let toSave = doc2;
            if (!(toSave instanceof Model2)) {
              toSave = new Model2(toSave);
            }
            await toSave.$save(options2);
            res.push(toSave);
          } catch (err) {
            if (!immediateError) {
              res.push(err);
            } else {
              throw err;
            }
          }
        }
        return res;
      } else if (!immediateError) {
        res = await Promise.allSettled(args.map(async (doc2) => {
          const Model2 = this.discriminators && doc2[discriminatorKey] != null ? this.discriminators[doc2[discriminatorKey]] || getDiscriminatorByValue2(this.discriminators, doc2[discriminatorKey]) : this;
          if (Model2 == null) {
            throw new MongooseError(`Discriminator "${doc2[discriminatorKey]}" not found for model "${this.modelName}"`);
          }
          let toSave = doc2;
          if (!(toSave instanceof Model2)) {
            toSave = new Model2(toSave);
          }
          await toSave.$save(options2);
          return toSave;
        }));
        res = res.map((result) => result.status === "fulfilled" ? result.value : result.reason);
      } else {
        let firstError = null;
        res = await Promise.all(args.map(async (doc2) => {
          const Model2 = this.discriminators && doc2[discriminatorKey] != null ? this.discriminators[doc2[discriminatorKey]] || getDiscriminatorByValue2(this.discriminators, doc2[discriminatorKey]) : this;
          if (Model2 == null) {
            throw new MongooseError(`Discriminator "${doc2[discriminatorKey]}" not found for model "${this.modelName}"`);
          }
          try {
            let toSave = doc2;
            if (!(toSave instanceof Model2)) {
              toSave = new Model2(toSave);
            }
            await toSave.$save(options2);
            return toSave;
          } catch (err) {
            if (!firstError) {
              firstError = err;
            }
          }
        }));
        if (firstError) {
          throw firstError;
        }
      }
      return res;
    };
    Model.insertOne = async function insertOne(doc, options2) {
      _checkContext(this, "insertOne");
      if (doc == null || typeof doc !== "object") {
        throw new ObjectParameterError(doc, "doc", "insertOne");
      }
      const discriminatorKey = this.schema.options.discriminatorKey;
      const Model2 = this.discriminators && doc[discriminatorKey] != null ? this.discriminators[doc[discriminatorKey]] || getDiscriminatorByValue2(this.discriminators, doc[discriminatorKey]) : this;
      if (Model2 == null) {
        throw new MongooseError(
          `Discriminator "${doc[discriminatorKey]}" not found for model "${this.modelName}"`
        );
      }
      if (!(doc instanceof Model2)) {
        doc = new Model2(doc);
      }
      return await doc.$save(options2);
    };
    Model.watch = function(pipeline, options2) {
      _checkContext(this, "watch");
      options2 = options2 || {};
      const watchOptions = options2?.hydrate !== void 0 ? utils2.omit(options2, ["hydrate"]) : { ...options2 };
      options2.model = this;
      pipeline = pipeline || [];
      prepareDiscriminatorPipeline2(pipeline, this.schema, "fullDocument");
      const changeStreamPromise = this.db._waitForConnect().then(
        () => this.$__collection.watch(pipeline, watchOptions)
      );
      return new ChangeStream(changeStreamPromise, pipeline, options2);
    };
    Model.startSession = function() {
      _checkContext(this, "startSession");
      return this.db.startSession.apply(this.db, arguments);
    };
    Model.insertMany = async function insertMany(arr, options2) {
      _checkContext(this, "insertMany");
      if (typeof options2 === "function" || typeof arguments[2] === "function") {
        throw new MongooseError("Model.insertMany() no longer accepts a callback");
      }
      const ThisModel = this;
      return traceInsertMany(function maybeTracedInsertMany() {
        return _insertMany.call(ThisModel, arr, options2);
      }, () => ({
        operation: "insertMany",
        collection: ThisModel.collection.name,
        database: ThisModel.db?.name,
        serverAddress: ThisModel.db?.host,
        serverPort: ThisModel.db?.port,
        args: { docs: arr, options: options2 }
      }));
    };
    async function _insertMany(arr, options2) {
      options2 = options2 || {};
      const hasInsertManyHooks = this._middleware.hasHooks("insertMany");
      const preFilter = hasInsertManyHooks ? buildMiddlewareFilter(options2, "pre") : null;
      const postFilter = hasInsertManyHooks ? buildMiddlewareFilter(options2, "post") : null;
      if (hasInsertManyHooks) {
        try {
          [arr] = await this._middleware.execPre("insertMany", this, [arr], { filter: preFilter });
        } catch (error2) {
          await this._middleware.execPost("insertMany", this, [arr], { error: error2, filter: postFilter });
        }
      }
      const ThisModel = this;
      const limit = options2.limit || 1e3;
      const rawResult = !!options2.rawResult;
      const ordered = options2.ordered == null ? true : !!options2.ordered;
      const throwOnValidationError = !!options2.throwOnValidationError;
      const lean = !!options2.lean;
      const asyncLocalStorage = this.db.base.transactionAsyncLocalStorage?.getStore();
      if (!Object.hasOwn(options2, "session") && asyncLocalStorage?.session != null) {
        options2 = { ...options2, session: asyncLocalStorage.session };
      }
      if (!Array.isArray(arr)) {
        arr = [arr];
      }
      const validationErrors = [];
      const results = ordered ? null : new Array(arr.length);
      const session = options2.session;
      function validateDoc(doc, index) {
        if (lean) {
          return doc;
        }
        let createdNewDoc = false;
        if (!(doc instanceof ThisModel)) {
          if (doc != null && typeof doc !== "object") {
            return Promise.reject(new ObjectParameterError(doc, "arr." + index, "insertMany"));
          }
          doc = new ThisModel(doc);
          createdNewDoc = true;
        }
        if (session != null) {
          doc.$session(session);
        }
        return doc.$validate(createdNewDoc ? { _skipParallelValidateCheck: true } : null).then(() => doc).catch((error2) => {
          if (ordered === false) {
            error2.index = index;
            validationErrors.push(error2);
            results[index] = error2;
            return;
          }
          throw error2;
        });
      }
      const docs = await parallelLimit(arr, validateDoc, limit);
      const validDocIndexToOriginalIndex = {};
      const docAttributes = [];
      const docObjects = lean ? docAttributes : [];
      const versionKeyPath = ThisModel.schema.options.versionKey ? ThisModel.schema.paths[ThisModel.schema.options.versionKey]?.splitPath() : null;
      const timestamps = options2.timestamps;
      for (let i = 0; i < docs.length; ++i) {
        const doc = docs[i];
        if (doc == null) {
          continue;
        }
        validDocIndexToOriginalIndex[docAttributes.length] = i;
        docAttributes.push(doc);
        if (lean) {
          continue;
        }
        if (versionKeyPath) {
          doc.$__setValue(versionKeyPath, 0);
        }
        const shouldSetTimestamps = timestamps !== false && doc.initializeTimestamps && (!doc.$__ || doc.$__.timestamps !== false);
        if (shouldSetTimestamps) {
          doc.initializeTimestamps(timestamps);
        }
        docObjects.push(doc.$__hasOnlyPrimitiveValues() ? doc.$__toObjectShallow() : doc.toObject(internalToObjectOptions));
      }
      if (validationErrors.length > 0 && ordered === false) {
        validationErrors.sort((err1, err2) => {
          return err1.index - err2.index;
        });
      }
      if (docAttributes.length === 0) {
        if (throwOnValidationError) {
          throw new MongooseBulkWriteError(
            validationErrors,
            results,
            null,
            "insertMany"
          );
        }
        if (rawResult) {
          const res2 = {
            acknowledged: true,
            insertedCount: 0,
            insertedIds: {}
          };
          decorateBulkWriteResult2(res2, validationErrors, validationErrors);
          return res2;
        }
        return [];
      }
      let res;
      try {
        res = await this.$__collection.insertMany(docObjects, options2);
      } catch (error2) {
        if (error2.writeErrors == null && error2.result?.result?.writeErrors != null) {
          error2.writeErrors = error2.result.result.writeErrors;
        }
        const hasWriteErrors = error2?.writeErrors;
        const erroredIndexes = new Set((error2?.writeErrors || []).map((err) => err.index));
        if (error2.writeErrors != null) {
          for (let i = 0; i < error2.writeErrors.length; ++i) {
            const originalIndex = validDocIndexToOriginalIndex[error2.writeErrors[i].index];
            error2.writeErrors[i] = { ...error2.writeErrors[i], index: originalIndex };
            if (!ordered) {
              results[originalIndex] = error2.writeErrors[i];
            }
          }
        }
        if (!ordered) {
          for (let i = 0; i < results.length; ++i) {
            if (results[i] === void 0) {
              results[i] = docs[i];
            }
          }
          error2.results = results;
        }
        let firstErroredIndex = -1;
        error2.insertedDocs = docAttributes.filter((doc, i) => {
          const isErrored = !hasWriteErrors || erroredIndexes.has(i);
          if (ordered) {
            if (firstErroredIndex > -1) {
              return i < firstErroredIndex;
            }
            if (isErrored) {
              firstErroredIndex = i;
            }
          }
          return !isErrored;
        }).map(function setIsNewForInsertedDoc(doc) {
          if (lean) {
            return doc;
          }
          doc.$__reset(true);
          _setIsNew(doc, false);
          return doc;
        });
        if (rawResult && ordered === false) {
          decorateBulkWriteResult2(error2, validationErrors, results);
        }
        if (hasInsertManyHooks) {
          await this._middleware.execPost("insertMany", this, [arr], { error: error2, filter: postFilter });
        } else {
          throw error2;
        }
      }
      if (!lean) {
        for (const attribute of docAttributes) {
          const hasOnlyPrimitiveValues = attribute.$__hasOnlyPrimitiveValues();
          attribute.$__reset(true, hasOnlyPrimitiveValues);
          _setIsNew(attribute, false, hasOnlyPrimitiveValues);
        }
      }
      if (ordered === false && throwOnValidationError && validationErrors.length > 0) {
        for (let i = 0; i < results.length; ++i) {
          if (results[i] === void 0) {
            results[i] = docs[i];
          }
        }
        throw new MongooseBulkWriteError(
          validationErrors,
          results,
          res,
          "insertMany"
        );
      }
      if (rawResult) {
        if (ordered === false) {
          for (let i = 0; i < results.length; ++i) {
            if (results[i] === void 0) {
              results[i] = docs[i];
            }
          }
          decorateBulkWriteResult2(res, validationErrors, results);
        }
        return res;
      }
      if (options2.populate != null) {
        return this.populate(docAttributes, options2.populate).catch((err) => {
          if (err != null) {
            err.insertedDocs = docAttributes;
          }
          throw err;
        });
      }
      if (hasInsertManyHooks) {
        const [result] = await this._middleware.execPost("insertMany", this, [docAttributes], { filter: postFilter });
        return result;
      }
      return docAttributes;
    }
    function _setIsNew(doc, val, hasOnlyPrimitiveValues) {
      doc.$isNew = val;
      doc.$emit("isNew", val);
      doc.constructor.emit("isNew", val);
      if (hasOnlyPrimitiveValues) {
        return;
      }
      const subdocs = doc.$getAllSubdocs({ useCache: true });
      for (const subdoc of subdocs) {
        subdoc.$isNew = val;
        subdoc.$emit("isNew", val);
      }
    }
    Model.bulkWrite = async function bulkWrite(ops, options2) {
      _checkContext(this, "bulkWrite");
      if (typeof options2 === "function" || typeof arguments[2] === "function") {
        throw new MongooseError("Model.bulkWrite() no longer accepts a callback");
      }
      const ThisModel = this;
      return traceBulkWrite(function maybeTracedBulkWrite() {
        return _bulkWrite.call(ThisModel, ops, options2);
      }, () => ({
        operation: "bulkWrite",
        collection: ThisModel.collection.name,
        database: ThisModel.db?.name,
        serverAddress: ThisModel.db?.host,
        serverPort: ThisModel.db?.port,
        args: { ops, options: options2 }
      }));
    };
    async function _bulkWrite(ops, options2) {
      options2 = options2 || {};
      const preFilter = buildMiddlewareFilter(options2, "pre");
      const postFilter = buildMiddlewareFilter(options2, "post");
      try {
        [ops, options2] = await this.hooks.execPre("bulkWrite", this, [ops, options2], { filter: preFilter });
      } catch (err) {
        if (err instanceof Kareem.skipWrappedFunction) {
          ops = err;
        } else {
          await this.hooks.execPost("bulkWrite", this, [null], { error: err, filter: postFilter });
        }
      }
      if (ops instanceof Kareem.skipWrappedFunction) {
        return ops.args[0];
      }
      const ordered = options2.ordered == null ? true : options2.ordered;
      if (ops.length === 0) {
        const BulkWriteResult = this.base.driver.get().BulkWriteResult;
        const bulkWriteResult2 = new BulkWriteResult(getDefaultBulkwriteResult(), false);
        bulkWriteResult2.n = 0;
        decorateBulkWriteResult2(bulkWriteResult2, [], []);
        return bulkWriteResult2;
      }
      const validations = options2?._skipCastBulkWrite ? [] : ops.map((op) => castBulkWrite2(this, op, options2));
      const asyncLocalStorage = this.db.base.transactionAsyncLocalStorage?.getStore();
      if ((!options2 || !Object.hasOwn(options2, "session")) && asyncLocalStorage?.session != null) {
        options2 = { ...options2, session: asyncLocalStorage.session };
      }
      let res = null;
      if (ordered) {
        await new Promise((resolve, reject) => {
          each2(validations, (fn, cb) => fn(cb), (error2) => {
            if (error2) {
              return reject(error2);
            }
            resolve();
          });
        });
        try {
          res = await this.$__collection.bulkWrite(ops, options2);
        } catch (error2) {
          await this.hooks.execPost("bulkWrite", this, [null], { error: error2, filter: postFilter });
        }
      } else {
        let validOpIndexes = [];
        let validationErrors = [];
        const results = [];
        if (validations.length > 0) {
          validOpIndexes = await Promise.all(ops.map((op, i) => {
            if (i >= validations.length) {
              return i;
            }
            return new Promise((resolve) => {
              validations[i]((err) => {
                if (err == null) {
                  resolve(i);
                } else {
                  validationErrors.push({ index: i, error: err });
                  results[i] = err;
                }
                resolve();
              });
            });
          }));
          validOpIndexes = validOpIndexes.filter((index) => index != null);
        } else {
          validOpIndexes = ops.map((op, i) => i);
        }
        validationErrors = validationErrors.sort((v1, v2) => v1.index - v2.index).map((v) => v.error);
        const validOps = validOpIndexes.sort((a, b) => a - b).map((index) => ops[index]);
        if (validOps.length === 0) {
          if (options2.throwOnValidationError && validationErrors.length) {
            throw new MongooseBulkWriteError(
              validationErrors,
              results,
              res,
              "bulkWrite"
            );
          }
          const BulkWriteResult = this.base.driver.get().BulkWriteResult;
          const bulkWriteResult2 = new BulkWriteResult(getDefaultBulkwriteResult(), false);
          bulkWriteResult2.result = getDefaultBulkwriteResult();
          decorateBulkWriteResult2(bulkWriteResult2, validationErrors, results);
          return bulkWriteResult2;
        }
        let error2;
        [res, error2] = await this.$__collection.bulkWrite(validOps, options2).then((res2) => [res2, null]).catch((error3) => [null, error3]);
        const writeErrorsByIndex = {};
        if (error2?.writeErrors) {
          for (const writeError of error2.writeErrors) {
            writeErrorsByIndex[writeError.err.index] = writeError;
          }
        }
        for (let i = 0; i < validOpIndexes.length; ++i) {
          results[validOpIndexes[i]] = writeErrorsByIndex[i] ?? null;
        }
        if (error2) {
          if (validationErrors.length > 0) {
            decorateBulkWriteResult2(error2, validationErrors, results);
          }
          await this.hooks.execPost("bulkWrite", this, [null], { error: error2, filter: postFilter });
        }
        if (validationErrors.length > 0) {
          if (options2.throwOnValidationError) {
            throw new MongooseBulkWriteError(
              validationErrors,
              results,
              res,
              "bulkWrite"
            );
          } else {
            decorateBulkWriteResult2(res, validationErrors, results);
          }
        }
      }
      await this.hooks.execPost("bulkWrite", this, [res], { filter: postFilter });
      return res;
    }
    Model.bulkSave = async function bulkSave(documents, options2) {
      options2 = options2 || {};
      if (options2.timestamps != null) {
        for (const document2 of documents) {
          document2.$__.saveOptions = document2.$__.saveOptions || {};
          document2.$__.saveOptions.timestamps = options2.timestamps;
        }
      } else {
        for (const document2 of documents) {
          if (document2.$__.timestamps != null) {
            document2.$__.saveOptions = document2.$__.saveOptions || {};
            document2.$__.saveOptions.timestamps = document2.$__.timestamps;
          }
        }
      }
      if (Object.hasOwn(options2, "session")) {
        for (const document2 of documents) {
          document2.$session(options2.session);
        }
      }
      await Promise.all(documents.map((doc) => buildPreSavePromise(doc, options2)));
      const writeOperations = this.buildBulkWriteOperations(documents, options2);
      const opts = { skipValidation: true, _skipCastBulkWrite: true, ...options2 };
      const { bulkWriteResult: bulkWriteResult2, bulkWriteError: bulkWriteError2 } = await this.bulkWrite(writeOperations, opts).then(
        (res) => ({ bulkWriteResult: res, bulkWriteError: null }),
        (err) => ({ bulkWriteResult: null, bulkWriteError: err })
      );
      if (bulkWriteError2 != null && bulkWriteError2.name !== "MongoBulkWriteError") {
        throw bulkWriteError2;
      }
      const matchedCount = bulkWriteResult2?.matchedCount ?? 0;
      const insertedCount = bulkWriteResult2?.insertedCount ?? 0;
      if (writeOperations.length > 0 && matchedCount + insertedCount < writeOperations.length && !bulkWriteError2) {
        throw new MongooseBulkSaveIncompleteError(
          this.modelName,
          documents,
          bulkWriteResult2
        );
      }
      const successfulDocuments = [];
      for (let i = 0; i < documents.length; i++) {
        const document2 = documents[i];
        const documentError = bulkWriteError2?.writeErrors.find((writeError) => {
          const writeErrorDocumentId = writeError.err.op._id || writeError.err.op.q._id;
          return writeErrorDocumentId.toString() === document2._doc._id.toString();
        });
        if (documentError == null) {
          successfulDocuments.push(document2);
        }
      }
      await Promise.all(successfulDocuments.map((document2) => handleSuccessfulWrite(document2, options2)));
      if (bulkWriteError2 != null) {
        throw bulkWriteError2;
      }
      return bulkWriteResult2;
    };
    async function buildPreSavePromise(document2, options2) {
      const preFilter = buildMiddlewareFilter(options2, "pre");
      const [newOptions] = await document2.schema.s.hooks.execPre("save", document2, [options2], { filter: preFilter });
      if (newOptions !== options2) {
        throw new MongooseError('Cannot overwrite options in pre("save") hook on bulkSave()');
      }
    }
    async function handleSuccessfulWrite(document2, options2) {
      const wasNew = document2.$isNew;
      if (wasNew) {
        _setIsNew(document2, false);
      }
      document2.$__reset();
      if (!wasNew) {
        document2._applyVersionIncrement();
      }
      const postFilter = buildMiddlewareFilter(options2, "post");
      return document2.schema.s.hooks.execPost("save", document2, [document2], { filter: postFilter });
    }
    Model.applyDefaults = function applyDefaults2(doc) {
      if (doc == null) {
        return doc;
      }
      if (doc.$__ != null) {
        applyDefaultsHelper(doc, doc.$__.fields, doc.$__.exclude);
        for (const subdoc of doc.$getAllSubdocs()) {
          applyDefaults2(subdoc, subdoc.$__.fields, subdoc.$__.exclude);
        }
        return doc;
      }
      applyDefaultsToPOJO2(doc, this.schema);
      return doc;
    };
    Model.applyVirtuals = function applyVirtuals(obj, virtualsToApply) {
      if (obj == null) {
        return obj;
      }
      if (obj.$__ != null) {
        return obj;
      }
      applyVirtualsHelper(this.schema, obj, virtualsToApply);
      return obj;
    };
    Model.applyTimestamps = function applyTimestamps(obj, options2) {
      if (obj == null) {
        return obj;
      }
      if (obj.$__ != null) {
        return obj;
      }
      applyTimestampsHelper(this.schema, obj, options2);
      return obj;
    };
    Model.castObject = function castObject(obj, options2) {
      options2 = options2 || {};
      const ret = {};
      let schema2 = this.schema;
      const discriminatorKey = schema2.options.discriminatorKey;
      if (schema2.discriminators != null && obj != null && obj[discriminatorKey] != null) {
        schema2 = getSchemaDiscriminatorByValue2(schema2, obj[discriminatorKey]) || schema2;
      }
      const paths = Object.keys(schema2.paths);
      for (const path of paths) {
        const schemaType2 = schema2.path(path);
        if (!schemaType2?.$isMongooseArray) {
          continue;
        }
        const val = get2(obj, path);
        pushNestedArrayPaths2(paths, val, path);
      }
      let error2 = null;
      for (const path of paths) {
        const schemaType2 = schema2.path(path);
        if (schemaType2 == null) {
          continue;
        }
        let val = get2(obj, path, void 0);
        if (val == null) {
          continue;
        }
        const pieces = path.indexOf(".") === -1 ? [path] : path.split(".");
        let cur = ret;
        for (let i = 0; i < pieces.length - 1; ++i) {
          if (cur[pieces[i]] == null) {
            cur[pieces[i]] = isNaN(pieces[i + 1]) ? {} : [];
          }
          cur = cur[pieces[i]];
        }
        if (schemaType2.$isMongooseDocumentArray) {
          const castNonArraysOption = schemaType2.options?.castNonArrays ?? schemaType2.constructor.options.castNonArrays;
          if (!Array.isArray(val)) {
            if (!castNonArraysOption) {
              if (!options2.ignoreCastErrors) {
                error2 = error2 || new ValidationError();
                error2.addError(path, new ObjectExpectedError(path, val));
              }
            } else {
              cur[pieces[pieces.length - 1]] = [
                Model.castObject.call(schemaType2.Constructor, val)
              ];
            }
            continue;
          }
        }
        if (schemaType2.$isSingleNested || schemaType2.$isMongooseDocumentArrayElement) {
          try {
            val = Model.castObject.call(schemaType2.Constructor, val);
          } catch (err) {
            if (!options2.ignoreCastErrors) {
              error2 = error2 || new ValidationError();
              error2.addError(path, err);
            }
            continue;
          }
          cur[pieces[pieces.length - 1]] = val;
          continue;
        }
        try {
          val = schemaType2.cast(val);
          cur[pieces[pieces.length - 1]] = val;
        } catch (err) {
          if (!options2.ignoreCastErrors) {
            error2 = error2 || new ValidationError();
            error2.addError(path, err);
          }
          continue;
        }
      }
      if (error2 != null) {
        throw error2;
      }
      return ret;
    };
    Model.buildBulkWriteOperations = function buildBulkWriteOperations(documents, options2) {
      if (!Array.isArray(documents)) {
        throw new MongooseError(`bulkSave expects an array of documents to be passed, received \`${documents}\` instead`);
      }
      setDefaultOptions();
      const writeOperations = documents.map((document2, i) => {
        if (!options2.skipValidation) {
          if (!(document2 instanceof Document)) {
            throw new MongooseError(`documents.${i} was not a mongoose document, documents must be an array of mongoose documents (instanceof mongoose.Document).`);
          }
          if (options2.validateBeforeSave == null || options2.validateBeforeSave) {
            const err = document2.$__validateSync();
            if (err != null) {
              throw err;
            }
          }
        }
        const isANewDocument = document2.isNew;
        if (isANewDocument) {
          const versionKey = document2.$__schema.options.versionKey;
          if (versionKey) {
            document2.$__setValue(versionKey, 0);
          }
          const writeOperation = { insertOne: { document: document2 } };
          utils2.injectTimestampsOption(writeOperation.insertOne, options2.timestamps);
          return writeOperation;
        }
        const delta = document2.$__delta();
        const isDocumentWithChanges = delta != null && !utils2.isEmptyObject(delta[0]);
        if (isDocumentWithChanges) {
          const where = document2.$__where(delta[0]);
          const changes = delta[1];
          _applyCustomWhere(document2, where);
          const shardKey = this.schema.options.shardKey;
          if (shardKey) {
            const paths = Object.keys(shardKey);
            const len = paths.length;
            for (let i2 = 0; i2 < len; ++i2) {
              where[paths[i2]] = document2[paths[i2]];
            }
          }
          document2.$__version(where, delta);
          const writeOperation = { updateOne: { filter: where, update: changes } };
          utils2.injectTimestampsOption(writeOperation.updateOne, options2.timestamps);
          return writeOperation;
        }
        return null;
      }).filter((op) => op !== null);
      return writeOperations;
      function setDefaultOptions() {
        options2 = options2 || {};
        if (options2.skipValidation == null) {
          options2.skipValidation = false;
        }
      }
    };
    Model.hydrate = function hydrate(obj, projection, options2) {
      _checkContext(this, "hydrate");
      if (options2?.virtuals && options2?.hydratedPopulatedDocs === false) {
        throw new MongooseError("Cannot set `hydratedPopulatedDocs` option to false if `virtuals` option is truthy because `virtuals: true` also sets populated virtuals");
      }
      if (projection != null) {
        if (obj?.$__ != null) {
          obj = obj.toObject(internalToObjectOptions);
        }
        projection = parseProjection2(projection);
        obj = applyProjection2(obj, projection);
      }
      const document2 = requireQueryHelpers().createModel(this, obj, projection, projection, options2);
      document2.$init(obj, options2);
      return document2;
    };
    Model.updateMany = function updateMany(conditions, update, options2) {
      _checkContext(this, "updateMany");
      if (update == null) {
        throw new MongooseError("updateMany `update` parameter cannot be nullish");
      }
      return _update(this, "updateMany", conditions, update, options2);
    };
    Model.updateOne = function updateOne(conditions, doc, options2) {
      _checkContext(this, "updateOne");
      return _update(this, "updateOne", conditions, doc, options2);
    };
    Model.replaceOne = function replaceOne(conditions, doc, options2) {
      _checkContext(this, "replaceOne");
      const versionKey = this?.schema?.options?.versionKey || null;
      if (versionKey && !doc[versionKey]) {
        doc[versionKey] = 0;
      }
      return _update(this, "replaceOne", conditions, doc, options2);
    };
    function _update(model2, op, conditions, doc, options2) {
      const mq = new model2.Query({}, {}, model2, model2.collection);
      if (conditions instanceof Document) {
        conditions = conditions.toObject();
      } else {
        conditions = clone(conditions);
      }
      options2 = typeof options2 === "function" ? options2 : clone(options2);
      const versionKey = model2?.schema?.options?.versionKey ?? null;
      decorateUpdateWithVersionKey2(doc, options2, versionKey);
      return mq[op](conditions, doc, options2);
    }
    Model.aggregate = function aggregate2(pipeline, options2) {
      _checkContext(this, "aggregate");
      if (typeof options2 === "function" || typeof arguments[2] === "function") {
        throw new MongooseError("Model.aggregate() no longer accepts a callback");
      }
      const aggregate3 = new Aggregate(pipeline || []);
      aggregate3.model(this);
      if (options2 != null) {
        aggregate3.option(options2);
      }
      return aggregate3;
    };
    Model.validate = async function validate(obj, pathsOrOptions, context) {
      if (arguments.length < 3 || arguments.length === 3 && typeof arguments[2] === "function") {
        context = obj;
      }
      if (typeof context === "function" || typeof arguments[3] === "function") {
        throw new MongooseError("Model.validate() no longer accepts a callback");
      }
      let schema2 = this.schema;
      const discriminatorKey = schema2.options.discriminatorKey;
      if (schema2.discriminators != null && obj != null && obj[discriminatorKey] != null) {
        schema2 = getSchemaDiscriminatorByValue2(schema2, obj[discriminatorKey]) || schema2;
      }
      let paths = Object.keys(schema2.paths);
      if (pathsOrOptions != null) {
        const _pathsToValidate = typeof pathsOrOptions === "string" ? new Set(pathsOrOptions.split(" ")) : Array.isArray(pathsOrOptions) ? new Set(pathsOrOptions) : new Set(paths);
        paths = paths.filter((p) => {
          if (pathsOrOptions.pathsToSkip) {
            if (Array.isArray(pathsOrOptions.pathsToSkip)) {
              if (pathsOrOptions.pathsToSkip.find((x) => x == p)) {
                return false;
              }
            } else if (typeof pathsOrOptions.pathsToSkip == "string") {
              if (pathsOrOptions.pathsToSkip.includes(p)) {
                return false;
              }
            }
          }
          const pieces = p.split(".");
          let cur = pieces[0];
          for (const piece of pieces) {
            if (_pathsToValidate.has(cur)) {
              return true;
            }
            cur += "." + piece;
          }
          return _pathsToValidate.has(p);
        });
      }
      for (const path of paths) {
        const schemaType2 = schema2.path(path);
        if (!schemaType2?.$isMongooseArray || schemaType2.$isMongooseDocumentArray) {
          continue;
        }
        const val = get2(obj, path);
        pushNestedArrayPaths2(paths, val, path);
      }
      let error2 = null;
      paths = new Set(paths);
      try {
        obj = this.castObject(obj);
      } catch (err) {
        error2 = err;
        for (const key of Object.keys(error2.errors || {})) {
          paths.delete(key);
        }
      }
      const promises = [];
      for (const path of paths) {
        const schemaType2 = schema2.path(path);
        if (schemaType2 == null) {
          continue;
        }
        const pieces = path.indexOf(".") === -1 ? [path] : path.split(".");
        let cur = obj;
        for (let i = 0; i < pieces.length - 1; ++i) {
          cur = cur[pieces[i]];
        }
        const val = get2(obj, path, void 0);
        promises.push(
          schemaType2.doValidate(val, context, { path }).catch((err) => {
            error2 = error2 || new ValidationError();
            error2.addError(path, err);
          })
        );
      }
      await Promise.all(promises);
      if (error2 != null) {
        throw error2;
      }
      return obj;
    };
    Object.defineProperty(Model, "~standard", {
      configurable: true,
      get() {
        return {
          version: 1,
          vendor: "mongoose",
          validate: (value, options2) => {
            return this.validate(value, options2?.libraryOptions).then(
              (value2) => ({ value: value2 }),
              (error2) => ({ issues: convertErrorToStandardSchemaIssues(error2) })
            );
          }
        };
      }
    });
    Model.populate = async function populate(docs, paths) {
      _checkContext(this, "populate");
      if (typeof paths === "function" || typeof arguments[2] === "function") {
        throw new MongooseError("Model.populate() no longer accepts a callback");
      }
      paths = utils2.populate(paths);
      if (paths.length === 0) {
        return docs;
      }
      if (paths.find((p) => p.ordered)) {
        for (const path of paths) {
          await _populatePath(this, docs, path);
        }
      } else {
        const promises = [];
        for (const path of paths) {
          promises.push(_populatePath(this, docs, path));
        }
        await Promise.all(promises);
      }
      return docs;
    };
    const excludeIdReg = /\s?-_id\s?/;
    const excludeIdRegGlobal = /\s?-_id\s?/g;
    async function _populatePath(model2, docs, populateOptions2) {
      if (populateOptions2.strictPopulate == null) {
        if (populateOptions2._localModel?.schema._userProvidedOptions.strictPopulate != null) {
          populateOptions2.strictPopulate = populateOptions2._localModel.schema._userProvidedOptions.strictPopulate;
        } else if (populateOptions2._localModel != null && model2.base.options.strictPopulate != null) {
          populateOptions2.strictPopulate = model2.base.options.strictPopulate;
        } else if (model2.base.options.strictPopulate != null) {
          populateOptions2.strictPopulate = model2.base.options.strictPopulate;
        }
      }
      if (!Array.isArray(docs)) {
        docs = [docs];
      }
      if (docs.length === 0 || docs.every(utils2.isNullOrUndefined)) {
        return;
      }
      const modelsMap = getModelsMapForPopulate2(model2, docs, populateOptions2);
      if (modelsMap instanceof MongooseError) {
        throw modelsMap;
      }
      const len = modelsMap.length;
      let vals = [];
      function flatten(item) {
        return void 0 !== item;
      }
      let hasOne = false;
      const params = [];
      for (let i = 0; i < len; ++i) {
        const mod = modelsMap[i];
        let select = mod.options.select;
        let ids = utils2.array.flatten(mod.ids, flatten);
        ids = utils2.array.unique(ids);
        const assignmentOpts = {};
        assignmentOpts.sort = mod && mod.options && mod.options.options && mod.options.options.sort || void 0;
        assignmentOpts.excludeId = excludeIdReg.test(select) || select && select._id === 0;
        if (mod.options && mod.options.options && mod.options.options.lean && mod.options.options.lean.transform) {
          mod.options.options._leanTransform = mod.options.options.lean.transform;
          mod.options.options.lean = true;
        }
        if (ids.length === 0 || ids.every(utils2.isNullOrUndefined)) {
          _assign(model2, [], mod, assignmentOpts);
          continue;
        }
        hasOne = true;
        if (typeof populateOptions2.foreignField === "string") {
          mod.foreignField.clear();
          mod.foreignField.add(populateOptions2.foreignField);
        }
        if (assignmentOpts.excludeId) {
          if (typeof select === "string") {
            select = select.replace(excludeIdRegGlobal, " ");
          } else if (Array.isArray(select)) {
            select = select.filter((field) => field !== "-_id");
          } else {
            select = { ...select };
            delete select._id;
          }
        }
        if (mod.options.options?.limit != null) {
          assignmentOpts.originalLimit = mod.options.options.limit;
        } else if (mod.options.limit != null) {
          assignmentOpts.originalLimit = mod.options.limit;
        }
        const splitParams = splitPopulateQuery(mod, ids, select, assignmentOpts);
        if (splitParams != null) {
          params.push(...splitParams);
          continue;
        }
        const match = createPopulateQueryFilter2(ids, mod.match, mod.foreignField, mod.model, mod.options.skipInvalidIds);
        params.push([mod, match, select, assignmentOpts]);
      }
      if (!hasOne) {
        if (modelsMap.length !== 0) {
          return;
        }
        if (populateOptions2.populate != null) {
          const opts = utils2.populate(populateOptions2.populate).map((pop) => Object.assign({}, pop, {
            path: populateOptions2.path + "." + pop.path
          }));
          return model2.populate(docs, opts);
        }
        return;
      }
      const deferredPopulatesPerParam = /* @__PURE__ */ new Map();
      const valsByParam = [];
      if (populateOptions2.ordered) {
        for (let i = 0; i < params.length; i++) {
          const arr = params[i];
          const { docs: docs2, deferredPopulates } = await _execPopulateQuery.apply(null, arr);
          valsByParam.push(docs2);
          if (!arr[0]._assignFromOwnResults) {
            vals = vals.concat(docs2);
          }
          if (deferredPopulates.length > 0) {
            deferredPopulatesPerParam.set(i, deferredPopulates);
          }
        }
      } else {
        const promises = [];
        for (const arr of params) {
          promises.push(_execPopulateQuery.apply(null, arr));
        }
        const results = await Promise.all(promises);
        for (let i = 0; i < results.length; i++) {
          const { docs: docs2, deferredPopulates } = results[i];
          valsByParam.push(docs2);
          if (!params[i][0]._assignFromOwnResults) {
            vals = vals.concat(docs2);
          }
          if (deferredPopulates.length > 0) {
            deferredPopulatesPerParam.set(i, deferredPopulates);
          }
        }
      }
      for (let i = 0; i < params.length; i++) {
        const arr = params[i];
        const mod = arr[0];
        const assignmentOpts = arr[3];
        const valsForMod = mod._assignFromOwnResults ? valsByParam[i] : vals;
        for (const val of valsForMod) {
          mod.options._childDocs.push(val);
        }
        _assign(model2, valsForMod, mod, assignmentOpts);
      }
      if (deferredPopulatesPerParam.size > 0) {
        for (let i = 0; i < params.length; i++) {
          const arr = params[i];
          const mod = arr[0];
          if (!Array.isArray(mod.match)) {
            continue;
          }
          const paramDeferredPopulates = deferredPopulatesPerParam.get(i);
          if (!paramDeferredPopulates || paramDeferredPopulates.length === 0) {
            continue;
          }
          const childDocsToPopulate = [];
          for (const doc of mod.docs) {
            const childVal = doc.$__ != null ? doc.get(mod.options.path) : doc[mod.options.path];
            if (Array.isArray(childVal)) {
              childDocsToPopulate.push(...childVal);
            } else if (childVal != null) {
              childDocsToPopulate.push(childVal);
            }
          }
          if (childDocsToPopulate.length > 0) {
            for (const pop of paramDeferredPopulates) {
              await mod.model.populate(childDocsToPopulate, pop);
            }
          }
        }
      }
      for (let i = 0; i < params.length; i++) {
        const mod = params[i][0];
        removeDeselectedForeignField2(mod.foreignField, mod.options, mod._assignFromOwnResults ? valsByParam[i] : vals);
      }
      for (let i = 0; i < params.length; i++) {
        const mod = params[i][0];
        if (mod.options?.options?._leanTransform) {
          const valsForMod = mod._assignFromOwnResults ? valsByParam[i] : vals;
          for (const doc of valsForMod) {
            mod.options.options._leanTransform(doc);
          }
        }
      }
    }
    function _execPopulateQuery(mod, match, select) {
      if (match == null) {
        return Promise.resolve({ docs: [], deferredPopulates: [] });
      }
      let subPopulate = clone(mod.options.populate);
      const queryOptions = {};
      if (mod.options.skip !== void 0) {
        queryOptions.skip = mod.options.skip;
      }
      if (mod.options.limit !== void 0) {
        queryOptions.limit = mod.options.limit;
      }
      if (mod.options.perDocumentLimit !== void 0) {
        queryOptions.perDocumentLimit = mod.options.perDocumentLimit;
      }
      Object.assign(queryOptions, mod.options.options);
      if (mod.count) {
        delete queryOptions.skip;
      }
      if (queryOptions.perDocumentLimit != null) {
        queryOptions.limit = queryOptions.perDocumentLimit;
        delete queryOptions.perDocumentLimit;
      } else if (queryOptions.limit != null) {
        queryOptions.limit = queryOptions.limit * mod.ids.length;
      }
      if (Array.isArray(mod.match)) {
        queryOptions._deferPopulate = true;
      }
      const query2 = mod.model.find(match, select, queryOptions);
      for (const foreignField of mod.foreignField) {
        if (foreignField !== "_id" && query2.selectedInclusively() && !isPathSelectedInclusive2(query2._fields, foreignField)) {
          query2.select(foreignField);
        }
      }
      if (mod.count) {
        for (const foreignField of mod.foreignField) {
          query2.select(foreignField);
        }
      }
      const shouldDeferSubPopulate = subPopulate && Array.isArray(mod.match);
      if (subPopulate) {
        if (mod.model.baseModelName != null) {
          if (Array.isArray(subPopulate)) {
            subPopulate.forEach((pop) => {
              pop.strictPopulate = false;
            });
          } else if (typeof subPopulate === "string") {
            subPopulate = { path: subPopulate, strictPopulate: false };
          } else {
            subPopulate.strictPopulate = false;
          }
        }
        const basePath = mod.options._fullPath || mod.options.path;
        if (Array.isArray(subPopulate)) {
          for (const pop of subPopulate) {
            pop._fullPath = basePath + "." + pop.path;
            if (shouldDeferSubPopulate && pop._localModel == null) {
              pop._localModel = mod.model;
            }
          }
        } else if (typeof subPopulate === "object") {
          subPopulate._fullPath = basePath + "." + subPopulate.path;
          if (shouldDeferSubPopulate && subPopulate._localModel == null) {
            subPopulate._localModel = mod.model;
          }
        }
      }
      if (subPopulate && !shouldDeferSubPopulate) {
        query2.populate(subPopulate);
      }
      return query2.exec().then(
        (docs) => {
          for (const val of docs) {
            leanPopulateMap2.set(val, mod.model);
          }
          const deferredPopulates = [];
          if (query2._deferredPopulate) {
            deferredPopulates.push(...query2._deferredPopulate);
            delete query2._deferredPopulate;
          }
          if (shouldDeferSubPopulate) {
            deferredPopulates.push(subPopulate);
          }
          return { docs, deferredPopulates };
        }
      );
    }
    function _assign(model2, vals, mod, assignmentOpts) {
      const options2 = mod.options;
      const isVirtual = mod.isVirtual;
      const justOne = mod.justOne;
      let _val;
      const lean = options2 && options2.options && options2.options.lean || false;
      const len = vals.length;
      const rawOrder = {};
      const rawDocs = {};
      let key;
      let val;
      const allIds = clone(mod.allIds);
      for (let i = 0; i < len; i++) {
        val = vals[i];
        if (val == null) {
          continue;
        }
        for (const foreignField of mod.foreignField) {
          _val = utils2.getValue(foreignField, val);
          if (Array.isArray(_val)) {
            _val = utils2.array.unique(utils2.array.flatten(_val));
            for (let __val of _val) {
              if (__val instanceof Document) {
                __val = __val._doc._id;
              }
              key = String(__val);
              if (rawDocs[key]) {
                if (Array.isArray(rawDocs[key])) {
                  rawDocs[key].push(val);
                  rawOrder[key].push(i);
                } else {
                  rawDocs[key] = [rawDocs[key], val];
                  rawOrder[key] = [rawOrder[key], i];
                }
              } else {
                if (isVirtual && !justOne) {
                  rawDocs[key] = [val];
                  rawOrder[key] = [i];
                } else {
                  rawDocs[key] = val;
                  rawOrder[key] = i;
                }
              }
            }
          } else {
            if (_val instanceof Document) {
              _val = _val._doc._id;
            }
            key = String(_val);
            if (rawDocs[key]) {
              if (Array.isArray(rawDocs[key])) {
                rawDocs[key].push(val);
                rawOrder[key].push(i);
              } else if (isVirtual || rawDocs[key].constructor !== val.constructor || (rawDocs[key] instanceof Document ? String(rawDocs[key]._doc._id) : String(rawDocs[key]._id)) !== (val instanceof Document ? String(val._doc._id) : String(val._id))) {
                rawDocs[key] = [rawDocs[key], val];
                rawOrder[key] = [rawOrder[key], i];
              }
            } else {
              rawDocs[key] = val;
              rawOrder[key] = i;
            }
          }
          if (!lean) {
            val.$__.wasPopulated = val.$__.wasPopulated || { value: _val };
          }
        }
      }
      assignVals2({
        originalModel: model2,
        // If virtual, make sure to not mutate original field
        rawIds: mod.isVirtual ? allIds : mod.allIds,
        allIds,
        unpopulatedValues: mod.unpopulatedValues,
        foreignField: mod.foreignField,
        rawDocs,
        rawOrder,
        docs: mod.docs,
        path: options2.path,
        options: assignmentOpts,
        justOne: mod.justOne,
        isVirtual: mod.isVirtual,
        allOptions: mod,
        populatedModel: mod.model,
        lean,
        virtual: mod.virtual,
        count: mod.count,
        match: mod.match
      });
    }
    Model.compile = function compile2(name, schema2, collectionName, connection2, base) {
      const versioningEnabled = schema2.options.versionKey !== false;
      if (versioningEnabled && !schema2.paths[schema2.options.versionKey]) {
        const o = {};
        o[schema2.options.versionKey] = Number;
        schema2.add(o);
      }
      let model2;
      if (typeof name === "function" && name.prototype instanceof Model) {
        model2 = name;
        name = model2.name;
        schema2.loadClass(model2, false);
        model2.prototype.$isMongooseModelPrototype = true;
      } else {
        model2 = function model3(doc, fields, skipId) {
          model3.hooks.execPreSync("createModel", doc);
          if (!(this instanceof model3)) {
            return new model3(doc, fields, skipId);
          }
          const discriminatorKey = model3.schema.options.discriminatorKey;
          if (model3.discriminators == null || doc == null || doc[discriminatorKey] == null) {
            Model.call(this, doc, fields, skipId);
            return;
          }
          const Discriminator = model3.discriminators[doc[discriminatorKey]] || getDiscriminatorByValue2(model3.discriminators, doc[discriminatorKey]);
          if (Discriminator != null) {
            return new Discriminator(doc, fields, skipId);
          }
          Model.call(this, doc, fields, skipId);
        };
      }
      model2.hooks = schema2.s.hooks.clone();
      model2.base = base;
      model2.modelName = name;
      if (!(model2.prototype instanceof Model)) {
        Object.setPrototypeOf(model2, Model);
        Object.setPrototypeOf(model2.prototype, Model.prototype);
      }
      model2.model = function model3(name2) {
        return this.db.model(name2);
      };
      model2.db = connection2;
      model2.prototype.db = connection2;
      model2.prototype[modelDbSymbol] = connection2;
      model2.discriminators = model2.prototype.discriminators = void 0;
      model2[modelSymbol] = true;
      model2.events = new EventEmitter();
      schema2._preCompile();
      const _userProvidedOptions = schema2._userProvidedOptions || {};
      const collectionOptions = {
        schemaUserProvidedOptions: _userProvidedOptions,
        capped: schema2.options.capped,
        Promise: model2.base.Promise,
        modelName: name
      };
      if (schema2.options.autoCreate !== void 0) {
        collectionOptions.autoCreate = schema2.options.autoCreate;
      }
      const collection2 = connection2.collection(
        collectionName,
        collectionOptions
      );
      model2.prototype.collection = collection2;
      model2.prototype.$collection = collection2;
      model2.prototype[modelCollectionSymbol] = collection2;
      model2.prototype.$__setSchema(schema2);
      applyMethods2(model2, schema2);
      applyStatics2(model2, schema2);
      applyHooks(model2, schema2);
      applyStaticHooks2(model2, schema2.s.hooks, schema2.statics);
      model2.schema = model2.prototype.$__schema;
      model2.collection = collection2;
      model2.$__collection = collection2;
      model2.Query = function() {
        Query.apply(this, arguments);
      };
      Object.setPrototypeOf(model2.Query.prototype, Query.prototype);
      model2.Query.base = Query.base;
      model2.Query.prototype.constructor = Query;
      model2._applyQueryMiddleware();
      applyQueryMethods(model2, schema2.query);
      return model2;
    };
    Model.clientEncryption = function clientEncryption() {
      const ClientEncryption = this.base.driver.get().ClientEncryption;
      if (!ClientEncryption) {
        throw new MongooseError("The mongodb driver must be used to obtain a ClientEncryption object.");
      }
      const client = this.collection?.conn?.client;
      if (!client) return null;
      const autoEncryptionOptions = client.options.autoEncryption;
      if (!autoEncryptionOptions) return null;
      const {
        keyVaultNamespace,
        keyVaultClient,
        kmsProviders,
        credentialProviders,
        proxyOptions,
        tlsOptions
      } = autoEncryptionOptions;
      return new ClientEncryption(
        keyVaultClient ?? client,
        { keyVaultNamespace, kmsProviders, credentialProviders, proxyOptions, tlsOptions }
      );
    };
    Model.$__updateConnection = function $__updateConnection(newConnection) {
      this.db = newConnection;
      this.prototype.db = newConnection;
      this.prototype[modelDbSymbol] = newConnection;
      const collection2 = newConnection.collection(
        this.collection.collectionName,
        this.collection.opts
      );
      this.prototype.collection = collection2;
      this.prototype.$collection = collection2;
      this.prototype[modelCollectionSymbol] = collection2;
      this.collection = collection2;
      this.$__collection = collection2;
    };
    function applyQueryMethods(model2, methods) {
      for (const i in methods) {
        model2.Query.prototype[i] = methods[i];
      }
    }
    Model.__subclass = function subclass(conn, schema2, collection2) {
      const _this = this;
      const Model2 = function Model3(doc, fields, skipId) {
        if (!(this instanceof Model3)) {
          return new Model3(doc, fields, skipId);
        }
        _this.call(this, doc, fields, skipId);
      };
      Object.setPrototypeOf(Model2, _this);
      Object.setPrototypeOf(Model2.prototype, _this.prototype);
      Model2.db = conn;
      Model2.prototype.db = conn;
      Model2.prototype[modelDbSymbol] = conn;
      _this[subclassedSymbol] = _this[subclassedSymbol] || [];
      _this[subclassedSymbol].push(Model2);
      if (_this.discriminators != null) {
        Model2.discriminators = {};
        for (const key of Object.keys(_this.discriminators)) {
          Model2.discriminators[key] = _this.discriminators[key].__subclass(_this.db, _this.discriminators[key].schema, collection2);
        }
      }
      const s = schema2 && typeof schema2 !== "string" ? schema2 : _this.prototype.$__schema;
      const options2 = s.options || {};
      const _userProvidedOptions = s._userProvidedOptions || {};
      if (!collection2) {
        collection2 = _this.prototype.$__schema.get("collection") || utils2.toCollectionName(_this.modelName, this.base.pluralize());
      }
      const collectionOptions = {
        schemaUserProvidedOptions: _userProvidedOptions,
        capped: s && options2.capped
      };
      Model2.prototype.collection = conn.collection(collection2, collectionOptions);
      Model2.prototype.$collection = Model2.prototype.collection;
      Model2.prototype[modelCollectionSymbol] = Model2.prototype.collection;
      Model2.collection = Model2.prototype.collection;
      Model2.$__collection = Model2.collection;
      Model2.init().catch(() => {
      });
      return Model2;
    };
    Model.recompileSchema = function recompileSchema() {
      this.prototype.$__setSchema(this.schema);
      if (this.schema._applyDiscriminators != null) {
        for (const disc of this.schema._applyDiscriminators.keys()) {
          this.discriminator(disc, this.schema._applyDiscriminators.get(disc));
        }
      }
      delete this.schema._defaultToObjectOptionsMap;
      applyEmbeddedDiscriminators(this.schema, /* @__PURE__ */ new WeakSet(), true);
    };
    Model.inspect = function() {
      return `Model { ${this.modelName} }`;
    };
    Model.namespace = function namespace() {
      return this.db.name + "." + this.collection.collectionName;
    };
    if (util.inspect.custom) {
      Model[util.inspect.custom] = Model.inspect;
    }
    Model._applyQueryMiddleware = function _applyQueryMiddleware() {
      const Query2 = this.Query;
      const queryMiddleware = this.schema.s.hooks.filter((hook) => {
        const contexts = _getContexts(hook);
        if (hook.name === "validate") {
          return !!contexts.query;
        }
        if (hook.name === "deleteOne" || hook.name === "updateOne") {
          return !!contexts.query || utils2.hasOwnKeys(contexts) === false;
        }
        if (hook.query != null || hook.document != null) {
          return !!hook.query;
        }
        return true;
      });
      Query2.prototype._queryMiddleware = queryMiddleware;
    };
    function _getContexts(hook) {
      const ret = {};
      if (Object.hasOwn(hook, "query")) {
        ret.query = hook.query;
      }
      if (Object.hasOwn(hook, "document")) {
        ret.document = hook.document;
      }
      return ret;
    }
    module.exports = Model;
  })(model);
  return model.exports;
}
var pluralize = { exports: {} };
var hasRequiredPluralize;
function requirePluralize() {
  if (hasRequiredPluralize) return pluralize.exports;
  hasRequiredPluralize = 1;
  (function(module, exports) {
    module.exports = pluralize2;
    exports.pluralization = [
      [/human$/gi, "humans"],
      [/(m|wom)an$/gi, "$1en"],
      [/(pe)rson$/gi, "$1ople"],
      [/(child)$/gi, "$1ren"],
      [/^(ox)$/gi, "$1en"],
      [/(ax|test)is$/gi, "$1es"],
      [/(octop|cact|foc|fung|nucle)us$/gi, "$1i"],
      [/(alias|status|virus)$/gi, "$1es"],
      [/(bu)s$/gi, "$1ses"],
      [/(buffal|tomat|potat)o$/gi, "$1oes"],
      [/([ti])um$/gi, "$1a"],
      [/sis$/gi, "ses"],
      [/(?:([^f])fe|([lr])f)$/gi, "$1$2ves"],
      [/(hive)$/gi, "$1s"],
      [/([^aeiouy]|qu)y$/gi, "$1ies"],
      [/(x|ch|ss|sh)$/gi, "$1es"],
      [/(matr|vert|ind)ix|ex$/gi, "$1ices"],
      [/([m|l])ouse$/gi, "$1ice"],
      [/(kn|w|l)ife$/gi, "$1ives"],
      [/(quiz)$/gi, "$1zes"],
      [/^goose$/i, "geese"],
      [/s$/gi, "s"],
      [/([^a-z])$/, "$1"],
      [/$/gi, "s"]
    ];
    const rules = exports.pluralization;
    exports.uncountables = [
      "advice",
      "energy",
      "excretion",
      "digestion",
      "cooperation",
      "health",
      "justice",
      "labour",
      "machinery",
      "equipment",
      "information",
      "pollution",
      "sewage",
      "paper",
      "money",
      "species",
      "series",
      "rain",
      "rice",
      "fish",
      "sheep",
      "moose",
      "deer",
      "news",
      "expertise",
      "status",
      "media"
    ];
    const uncountables = exports.uncountables;
    function pluralize2(str) {
      let found;
      str = str.toLowerCase();
      if (!~uncountables.indexOf(str)) {
        found = rules.filter(function(rule) {
          return str.match(rule[0]);
        });
        if (found[0]) {
          return str.replace(found[0][0], found[0][1]);
        }
      }
      return str;
    }
  })(pluralize, pluralize.exports);
  return pluralize.exports;
}
var setOptionError;
var hasRequiredSetOptionError;
function requireSetOptionError() {
  if (hasRequiredSetOptionError) return setOptionError;
  hasRequiredSetOptionError = 1;
  const MongooseError = requireMongooseError();
  const util = require$$1;
  const combinePathErrors2 = requireCombinePathErrors();
  class SetOptionError extends MongooseError {
    constructor() {
      super("");
      this.errors = {};
    }
    /**
     * Console.log helper
     */
    toString() {
      return combinePathErrors2(this);
    }
    /**
     * inspect helper
     * @api private
     */
    inspect() {
      return Object.assign(new Error(this.message), this);
    }
    /**
    * add message
    * @param {string} key
    * @param {string|Error} error
    * @api private
    */
    addError(key, error2) {
      if (error2 instanceof SetOptionError) {
        const { errors } = error2;
        for (const optionKey of Object.keys(errors)) {
          this.addError(optionKey, errors[optionKey]);
        }
        return;
      }
      this.errors[key] = error2;
      this.message = combinePathErrors2(this);
    }
  }
  if (util.inspect.custom) {
    SetOptionError.prototype[util.inspect.custom] = SetOptionError.prototype.inspect;
  }
  Object.defineProperty(SetOptionError.prototype, "toJSON", {
    enumerable: false,
    writable: false,
    configurable: true,
    value: function() {
      return Object.assign({}, this, { name: this.name, message: this.message });
    }
  });
  Object.defineProperty(SetOptionError.prototype, "name", {
    value: "SetOptionError"
  });
  class SetOptionInnerError extends MongooseError {
    /**
     * Error for the "errors" array in "SetOptionError" with consistent message
     * @param {string} key
     */
    constructor(key) {
      super(`"${key}" is not a valid option to set`);
    }
  }
  SetOptionError.SetOptionInnerError = SetOptionInnerError;
  setOptionError = SetOptionError;
  return setOptionError;
}
var printJestWarning = {};
var hasRequiredPrintJestWarning;
function requirePrintJestWarning() {
  if (hasRequiredPrintJestWarning) return printJestWarning;
  hasRequiredPrintJestWarning = 1;
  const utils2 = requireUtils();
  if (typeof jest !== "undefined" && !process.env.SUPPRESS_JEST_WARNINGS) {
    if (typeof window !== "undefined") {
      utils2.warn("Mongoose: looks like you're trying to test a Mongoose app with Jest's default jsdom test environment. Please make sure you read Mongoose's docs on configuring Jest to test Node.js apps: https://mongoosejs.com/docs/jest.html. Set the SUPPRESS_JEST_WARNINGS to true to hide this warning.");
    }
    if (typeof setTimeout.clock?.Date === "function") {
      utils2.warn("Mongoose: looks like you're trying to test a Mongoose app with Jest's mock timers enabled. Please make sure you read Mongoose's docs on configuring Jest to test Node.js apps: https://mongoosejs.com/docs/jest.html. Set the SUPPRESS_JEST_WARNINGS to true to hide this warning.");
    }
  }
  return printJestWarning;
}
var hasRequiredMongoose$1;
function requireMongoose$1() {
  if (hasRequiredMongoose$1) return mongoose$1.exports;
  hasRequiredMongoose$1 = 1;
  (function(module, exports) {
    const Document = requireDocument();
    const EventEmitter = require$$0.EventEmitter;
    const Kareem = requireKareem();
    const Schema = requireSchema();
    const SchemaType = requireSchemaType();
    const SchemaTypes = requireSchema$1();
    const VirtualType = requireVirtualType();
    const STATES = requireConnectionState();
    const VALID_OPTIONS = requireValidOptions();
    const Types = requireTypes();
    const Query = requireQuery();
    const Model = requireModel();
    const applyPlugins2 = requireApplyPlugins();
    const builtinPlugins = requirePlugins();
    const driver2 = requireDriver();
    const legacyPluralize = requirePluralize();
    const utils2 = requireUtils();
    const pkg = require$$17;
    const cast2 = requireCast();
    const Aggregate = requireAggregate();
    const trusted2 = requireTrusted().trusted;
    const sanitizeFilter2 = requireSanitizeFilter();
    const isBsonType = requireIsBsonType();
    const MongooseError = requireMongooseError();
    const SetOptionError = requireSetOptionError();
    const applyEmbeddedDiscriminators = requireApplyEmbeddedDiscriminators();
    const defaultMongooseSymbol = /* @__PURE__ */ Symbol.for("mongoose:default");
    const defaultConnectionSymbol = /* @__PURE__ */ Symbol("mongoose:defaultConnection");
    requirePrintJestWarning();
    const objectIdHexRegexp = /^[0-9A-Fa-f]{24}$/;
    const { AsyncLocalStorage } = require$$2;
    function Mongoose(options2) {
      this.connections = [];
      this.nextConnectionId = 0;
      this.models = {};
      this.events = new EventEmitter();
      this.__driver = driver2.get();
      this.options = Object.assign({
        pluralization: true,
        autoIndex: true,
        autoCreate: true,
        autoSearchIndex: false
      }, options2);
      const createInitialConnection = utils2.getOption("createInitialConnection", this.options) ?? true;
      if (createInitialConnection && this.__driver != null) {
        _createDefaultConnection(this);
      }
      if (this.options.pluralization) {
        this._pluralize = legacyPluralize;
      }
      if (!options2?.[defaultMongooseSymbol]) {
        const _this = this;
        this.Schema = function() {
          this.base = _this;
          return Schema.apply(this, arguments);
        };
        this.Schema.prototype = Object.create(Schema.prototype);
        Object.assign(this.Schema, Schema);
        this.Schema.base = this;
        this.Schema.Types = Object.assign({}, Schema.Types);
      } else {
        for (const key of ["Schema", "model"]) {
          this[key] = Mongoose.prototype[key];
        }
      }
      this.Schema.prototype.base = this;
      if (options2?.transactionAsyncLocalStorage) {
        this.transactionAsyncLocalStorage = new AsyncLocalStorage();
      }
      Object.defineProperty(this, "plugins", {
        configurable: false,
        enumerable: true,
        writable: false,
        value: Object.values(builtinPlugins).map((plugin) => [plugin, { deduplicate: true }])
      });
    }
    Mongoose.prototype.cast = cast2;
    Mongoose.prototype.STATES = STATES;
    Mongoose.prototype.ConnectionStates = STATES;
    Mongoose.prototype.driver = driver2;
    Mongoose.prototype.setDriver = function setDriver(driver3) {
      const _mongoose = this instanceof Mongoose ? this : mongoose2;
      if (_mongoose.__driver === driver3) {
        return _mongoose;
      }
      const openConnection = _mongoose.connections && _mongoose.connections.find((conn) => conn.readyState !== STATES.disconnected);
      if (openConnection) {
        const msg = "Cannot modify Mongoose driver if a connection is already open. Call `mongoose.disconnect()` before modifying the driver";
        throw new MongooseError(msg);
      }
      _mongoose.__driver = driver3;
      if (Array.isArray(driver3.plugins)) {
        for (const plugin of driver3.plugins) {
          if (typeof plugin === "function") {
            _mongoose.plugin(plugin);
          }
        }
      }
      if (driver3.SchemaTypes != null) {
        Object.assign(mongoose2.Schema.Types, driver3.SchemaTypes);
      }
      const Connection = driver3.Connection;
      const oldDefaultConnection = _mongoose.connections[0];
      _mongoose.connections = [new Connection(_mongoose)];
      _mongoose.connections[0].models = _mongoose.models;
      if (oldDefaultConnection == null) {
        return _mongoose;
      }
      for (const model2 of Object.values(_mongoose.models)) {
        if (model2.db !== oldDefaultConnection) {
          continue;
        }
        model2.$__updateConnection(_mongoose.connections[0]);
      }
      return _mongoose;
    };
    Mongoose.prototype.set = function getsetOptions(key, value) {
      const _mongoose = this instanceof Mongoose ? this : mongoose2;
      if (key == null) {
        const error3 = new SetOptionError();
        error3.addError(String(key), new SetOptionError.SetOptionInnerError(String(key)));
        throw error3;
      }
      if (arguments.length === 1 && typeof key !== "object") {
        if (VALID_OPTIONS.indexOf(key) === -1) {
          const error3 = new SetOptionError();
          error3.addError(key, new SetOptionError.SetOptionInnerError(key));
          throw error3;
        }
        return _mongoose.options[key];
      }
      let options2 = {};
      if (arguments.length === 2) {
        options2 = { [key]: value };
      }
      if (arguments.length === 1 && typeof key === "object") {
        options2 = key;
      }
      let error2 = void 0;
      for (const [optionKey, optionValue] of Object.entries(options2)) {
        if (VALID_OPTIONS.indexOf(optionKey) === -1) {
          if (!error2) {
            error2 = new SetOptionError();
          }
          error2.addError(optionKey, new SetOptionError.SetOptionInnerError(optionKey));
          continue;
        }
        _mongoose.options[optionKey] = optionValue;
        if (optionKey === "objectIdGetter") {
          if (optionValue) {
            Object.defineProperty(_mongoose.Types.ObjectId.prototype, "_id", {
              enumerable: false,
              configurable: true,
              get: function() {
                return this;
              }
            });
          } else {
            delete _mongoose.Types.ObjectId.prototype._id;
          }
        } else if (optionKey === "transactionAsyncLocalStorage") {
          if (optionValue && !_mongoose.transactionAsyncLocalStorage) {
            _mongoose.transactionAsyncLocalStorage = new AsyncLocalStorage();
          } else if (!optionValue && _mongoose.transactionAsyncLocalStorage) {
            delete _mongoose.transactionAsyncLocalStorage;
          }
        } else if (optionKey === "createInitialConnection") {
          if (optionValue && !_mongoose.connection) {
            _createDefaultConnection(_mongoose);
          } else if (optionValue === false && _mongoose.connection?.[defaultConnectionSymbol]) {
            if (_mongoose.connection.readyState === STATES.disconnected && utils2.hasOwnKeys(_mongoose.connection.models) === false) {
              _mongoose.connections.shift();
            }
          }
        } else if (optionKey === "returnOriginal") {
          if (_mongoose.options.returnDocument != null) {
            throw new MongooseError(
              "Cannot set `returnOriginal` when `returnDocument` is already set. Use `returnDocument` instead."
            );
          }
          const replacement = optionValue === true ? "'before'" : "'after'";
          utils2.warn(
            "Mongoose: the `returnOriginal` option for `mongoose.set()` is deprecated. Use `mongoose.set('returnDocument', " + replacement + ")` instead."
          );
        } else if (optionKey === "returnDocument") {
          if (_mongoose.options.returnOriginal != null) {
            throw new MongooseError(
              "Cannot set `returnDocument` when `returnOriginal` is already set. Use `returnDocument` instead of `returnOriginal`."
            );
          }
        }
      }
      if (error2) {
        throw error2;
      }
      return _mongoose;
    };
    Mongoose.prototype.get = Mongoose.prototype.set;
    Mongoose.prototype.createConnection = function createConnection(uri, options2) {
      const _mongoose = this instanceof Mongoose ? this : mongoose2;
      const Connection = _mongoose.__driver.Connection;
      const conn = new Connection(_mongoose);
      _mongoose.connections.push(conn);
      _mongoose.nextConnectionId++;
      _mongoose.events.emit("createConnection", conn);
      if (arguments.length > 0) {
        conn.openUri(uri, { ...options2, _fireAndForget: true });
      }
      return conn;
    };
    Mongoose.prototype.connect = async function connect(uri, options2) {
      if (typeof options2 === "function" || arguments.length >= 3 && typeof arguments[2] === "function") {
        throw new MongooseError("Mongoose.prototype.connect() no longer accepts a callback");
      }
      const _mongoose = this instanceof Mongoose ? this : mongoose2;
      if (_mongoose.connection == null) {
        _createDefaultConnection(_mongoose);
      }
      const conn = _mongoose.connection;
      return conn.openUri(uri, options2).then(() => _mongoose);
    };
    Mongoose.prototype.disconnect = async function disconnect() {
      if (arguments.length >= 1 && typeof arguments[0] === "function") {
        throw new MongooseError("Mongoose.prototype.disconnect() no longer accepts a callback");
      }
      const _mongoose = this instanceof Mongoose ? this : mongoose2;
      const remaining = _mongoose.connections.length;
      if (remaining <= 0) {
        return;
      }
      await Promise.all(_mongoose.connections.map((conn) => conn.close()));
    };
    Mongoose.prototype.startSession = function startSession() {
      const _mongoose = this instanceof Mongoose ? this : mongoose2;
      return _mongoose.connection.startSession.apply(_mongoose.connection, arguments);
    };
    Mongoose.prototype.pluralize = function pluralize2(fn) {
      const _mongoose = this instanceof Mongoose ? this : mongoose2;
      if (arguments.length > 0) {
        _mongoose._pluralize = fn;
      }
      return _mongoose._pluralize;
    };
    Mongoose.prototype.model = function model2(name, schema2, collection2, options2) {
      const _mongoose = this instanceof Mongoose ? this : mongoose2;
      if (typeof schema2 === "string") {
        collection2 = schema2;
        schema2 = false;
      }
      if (arguments.length === 1) {
        const model4 = _mongoose.models[name];
        if (!model4) {
          throw new _mongoose.Error.MissingSchemaError(name);
        }
        return model4;
      }
      if (utils2.isObject(schema2) && !(schema2 instanceof Schema)) {
        schema2 = new Schema(schema2);
      }
      if (schema2 && !(schema2 instanceof Schema)) {
        throw new _mongoose.Error("The 2nd parameter to `mongoose.model()` should be a schema or a POJO");
      }
      options2 = options2 || {};
      const originalSchema = schema2;
      if (schema2) {
        if (_mongoose.get("cloneSchemas")) {
          schema2 = schema2.clone();
        }
        _mongoose._applyPlugins(schema2);
      }
      const overwriteModels = Object.hasOwn(_mongoose.options, "overwriteModels") ? _mongoose.options.overwriteModels : options2.overwriteModels;
      if (Object.hasOwn(_mongoose.models, name) && options2.cache !== false && overwriteModels !== true) {
        if (originalSchema?.instanceOfSchema && originalSchema !== _mongoose.models[name].schema) {
          throw new _mongoose.Error.OverwriteModelError(name);
        }
        if (collection2 && collection2 !== _mongoose.models[name].collection.name) {
          const model4 = _mongoose.models[name];
          schema2 = model4.prototype.schema;
          const sub = model4.__subclass(_mongoose.connection, schema2, collection2);
          return sub;
        }
        return _mongoose.models[name];
      }
      if (schema2 == null) {
        throw new _mongoose.Error.MissingSchemaError(name);
      }
      const model3 = _mongoose._model(name, schema2, collection2, options2);
      _mongoose.connection.models[name] = model3;
      _mongoose.models[name] = model3;
      return model3;
    };
    Mongoose.prototype._model = function _model(name, schema2, collection2, options2) {
      const _mongoose = this instanceof Mongoose ? this : mongoose2;
      let model2;
      if (typeof name === "function") {
        model2 = name;
        name = model2.name;
        if (!(model2.prototype instanceof Model)) {
          throw new _mongoose.Error("The provided class " + name + " must extend Model");
        }
      }
      if (schema2) {
        if (_mongoose.get("cloneSchemas")) {
          schema2 = schema2.clone();
        }
        _mongoose._applyPlugins(schema2);
      }
      if (schema2 == null || !("pluralization" in schema2.options)) {
        schema2.options.pluralization = _mongoose.options.pluralization;
      }
      if (!collection2) {
        collection2 = schema2.get("collection") || utils2.toCollectionName(name, _mongoose.pluralize());
      }
      applyEmbeddedDiscriminators(schema2);
      const connection2 = options2.connection || _mongoose.connection;
      model2 = _mongoose.Model.compile(model2 || name, schema2, collection2, connection2, _mongoose);
      model2.init().catch(function $modelInitNoop() {
      });
      connection2.emit("model", model2);
      if (schema2._applyDiscriminators != null) {
        for (const disc of schema2._applyDiscriminators.keys()) {
          const {
            schema: discriminatorSchema,
            options: options3
          } = schema2._applyDiscriminators.get(disc);
          model2.discriminator(disc, discriminatorSchema, options3);
        }
      }
      return model2;
    };
    Mongoose.prototype.deleteModel = function deleteModel(name) {
      const _mongoose = this instanceof Mongoose ? this : mongoose2;
      _mongoose.connection.deleteModel(name);
      delete _mongoose.models[name];
      return _mongoose;
    };
    Mongoose.prototype.modelNames = function modelNames() {
      const _mongoose = this instanceof Mongoose ? this : mongoose2;
      const names = Object.keys(_mongoose.models);
      return names;
    };
    Mongoose.prototype._applyPlugins = function _applyPlugins(schema2, options2) {
      const _mongoose = this instanceof Mongoose ? this : mongoose2;
      options2 = options2 || {};
      options2.applyPluginsToDiscriminators = _mongoose.options?.applyPluginsToDiscriminators || false;
      options2.applyPluginsToChildSchemas = typeof _mongoose.options?.applyPluginsToChildSchemas === "boolean" ? _mongoose.options.applyPluginsToChildSchemas : true;
      applyPlugins2(schema2, _mongoose.plugins, options2, "$globalPluginsApplied");
    };
    Mongoose.prototype.plugin = function plugin(fn, opts) {
      const _mongoose = this instanceof Mongoose ? this : mongoose2;
      _mongoose.plugins.push([fn, opts]);
      return _mongoose;
    };
    Mongoose.prototype.__defineGetter__("connection", function() {
      return this.connections[0];
    });
    Mongoose.prototype.__defineSetter__("connection", function(v) {
      if (v instanceof this.__driver.Connection) {
        this.connections[0] = v;
        this.models = v.models;
      }
    });
    Mongoose.prototype.connections;
    Mongoose.prototype.nextConnectionId;
    Mongoose.prototype.Aggregate = Aggregate;
    Mongoose.prototype.BaseCollection = requireCollection$1();
    Object.defineProperty(Mongoose.prototype, "Collection", {
      get: function() {
        return this.__driver.Collection;
      },
      set: function(Collection) {
        this.__driver.Collection = Collection;
      }
    });
    Object.defineProperty(Mongoose.prototype, "Connection", {
      get: function() {
        return this.__driver.Connection;
      },
      set: function(Connection) {
        if (Connection === this.__driver.Connection) {
          return;
        }
        this.__driver.Connection = Connection;
      }
    });
    Mongoose.prototype.BaseConnection = requireConnection$1();
    Mongoose.prototype.version = pkg.version;
    Mongoose.prototype.Mongoose = Mongoose;
    Mongoose.prototype.Schema = Schema;
    Mongoose.prototype.SchemaType = SchemaType;
    Mongoose.prototype.SchemaTypes = Schema.Types;
    Mongoose.prototype.VirtualType = VirtualType;
    Mongoose.prototype.Types = Types;
    Mongoose.prototype.Query = Query;
    Mongoose.prototype.Model = Model;
    Mongoose.prototype.Document = Document;
    Mongoose.prototype.ObjectId = SchemaTypes.ObjectId;
    Mongoose.prototype.isValidObjectId = function isValidObjectId(v) {
      const _mongoose = this instanceof Mongoose ? this : mongoose2;
      return _mongoose.Types.ObjectId.isValid(v);
    };
    Mongoose.prototype.isObjectIdOrHexString = function isObjectIdOrHexString(v) {
      return isBsonType(v, "ObjectId") || typeof v === "string" && objectIdHexRegexp.test(v);
    };
    Mongoose.prototype.syncIndexes = function syncIndexes2(options2) {
      const _mongoose = this instanceof Mongoose ? this : mongoose2;
      return _mongoose.connection.syncIndexes(options2);
    };
    Mongoose.prototype.Decimal128 = SchemaTypes.Decimal128;
    Mongoose.prototype.Mixed = SchemaTypes.Mixed;
    Mongoose.prototype.Date = SchemaTypes.Date;
    Mongoose.prototype.Number = SchemaTypes.Number;
    Mongoose.prototype.Error = MongooseError;
    Mongoose.prototype.MongooseError = MongooseError;
    Mongoose.prototype.now = function now() {
      return /* @__PURE__ */ new Date();
    };
    Mongoose.prototype.CastError = MongooseError.CastError;
    Mongoose.prototype.SchemaTypeOptions = requireSchemaTypeOptions();
    Mongoose.prototype.mquery = requireMquery();
    Mongoose.prototype.sanitizeFilter = sanitizeFilter2;
    Mongoose.prototype.trusted = trusted2;
    Mongoose.prototype.skipMiddlewareFunction = Kareem.skipWrappedFunction;
    Mongoose.prototype.overwriteMiddlewareResult = Kareem.overwriteResult;
    Mongoose.prototype.overwriteMiddlewareArguments = Kareem.overwriteArguments;
    Mongoose.prototype.omitUndefined = requireOmitUndefined();
    function _createDefaultConnection(mongoose3) {
      if (mongoose3.connection) {
        return;
      }
      const conn = mongoose3.createConnection();
      conn[defaultConnectionSymbol] = true;
      conn.models = mongoose3.models;
    }
    const mongoose2 = module.exports = new Mongoose({
      [defaultMongooseSymbol]: true
    });
  })(mongoose$1);
  return mongoose$1.exports;
}
var lib;
var hasRequiredLib;
function requireLib() {
  if (hasRequiredLib) return lib;
  hasRequiredLib = 1;
  const mongodbDriver = requireNodeMongodbNative();
  requireDriver().set(mongodbDriver);
  const mongoose2 = requireMongoose$1();
  mongoose2.setDriver(mongodbDriver);
  mongoose2.Mongoose.prototype.mongo = requireLib$1();
  lib = mongoose2;
  return lib;
}
var hasRequiredMongoose;
function requireMongoose() {
  if (hasRequiredMongoose) return mongoose$2.exports;
  hasRequiredMongoose = 1;
  const mongoose2 = requireLib();
  mongoose$2.exports = mongoose2;
  mongoose$2.exports.default = mongoose2;
  mongoose$2.exports.mongoose = mongoose2;
  mongoose$2.exports.cast = mongoose2.cast;
  mongoose$2.exports.STATES = mongoose2.STATES;
  mongoose$2.exports.setDriver = mongoose2.setDriver;
  mongoose$2.exports.set = mongoose2.set;
  mongoose$2.exports.get = mongoose2.get;
  mongoose$2.exports.createConnection = mongoose2.createConnection;
  mongoose$2.exports.connect = mongoose2.connect;
  mongoose$2.exports.disconnect = mongoose2.disconnect;
  mongoose$2.exports.startSession = mongoose2.startSession;
  mongoose$2.exports.pluralize = mongoose2.pluralize;
  mongoose$2.exports.model = mongoose2.model;
  mongoose$2.exports.deleteModel = mongoose2.deleteModel;
  mongoose$2.exports.modelNames = mongoose2.modelNames;
  mongoose$2.exports.plugin = mongoose2.plugin;
  mongoose$2.exports.connections = mongoose2.connections;
  mongoose$2.exports.version = mongoose2.version;
  mongoose$2.exports.Aggregate = mongoose2.Aggregate;
  mongoose$2.exports.Mongoose = mongoose2.Mongoose;
  mongoose$2.exports.Schema = mongoose2.Schema;
  mongoose$2.exports.SchemaType = mongoose2.SchemaType;
  mongoose$2.exports.SchemaTypes = mongoose2.SchemaTypes;
  mongoose$2.exports.VirtualType = mongoose2.VirtualType;
  mongoose$2.exports.Types = mongoose2.Types;
  mongoose$2.exports.Query = mongoose2.Query;
  mongoose$2.exports.Model = mongoose2.Model;
  mongoose$2.exports.Document = mongoose2.Document;
  mongoose$2.exports.ObjectId = mongoose2.ObjectId;
  mongoose$2.exports.isValidObjectId = mongoose2.isValidObjectId;
  mongoose$2.exports.isObjectIdOrHexString = mongoose2.isObjectIdOrHexString;
  mongoose$2.exports.syncIndexes = mongoose2.syncIndexes;
  mongoose$2.exports.Decimal128 = mongoose2.Decimal128;
  mongoose$2.exports.Mixed = mongoose2.Mixed;
  mongoose$2.exports.Date = mongoose2.Date;
  mongoose$2.exports.Number = mongoose2.Number;
  mongoose$2.exports.Error = mongoose2.Error;
  mongoose$2.exports.MongooseError = mongoose2.MongooseError;
  mongoose$2.exports.now = mongoose2.now;
  mongoose$2.exports.CastError = mongoose2.CastError;
  mongoose$2.exports.SchemaTypeOptions = mongoose2.SchemaTypeOptions;
  mongoose$2.exports.mongo = mongoose2.mongo;
  mongoose$2.exports.mquery = mongoose2.mquery;
  mongoose$2.exports.sanitizeFilter = mongoose2.sanitizeFilter;
  mongoose$2.exports.trusted = mongoose2.trusted;
  mongoose$2.exports.skipMiddlewareFunction = mongoose2.skipMiddlewareFunction;
  mongoose$2.exports.overwriteMiddlewareResult = mongoose2.overwriteMiddlewareResult;
  return mongoose$2.exports;
}
var mongooseExports = requireMongoose();
const mongoose = /* @__PURE__ */ getDefaultExportFromCjs(mongooseExports);
export {
  mongooseExports as a,
  mongoose as m
};
